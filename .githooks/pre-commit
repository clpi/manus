#!/bin/sh
# Thin git hook — all admission logic lives in scripts/preflight.id (Idol only).
root=$(git rev-parse --show-toplevel) || exit 1
idol="$root/zig-out/bin/idol"
[ -x "$idol" ] || idol="$root/zig-out/bin/duo"
exec "$idol" run "$root/scripts/preflight.id" "$@"
