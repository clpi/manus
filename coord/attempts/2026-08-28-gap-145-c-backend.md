| field | value |
|---|---|
| title | attempt: gap-145-c-backend |

| # | directive |
|---|---|
| 1 | opened: 2026-08-28T22:00:00-07:00 closed: 2026-08-28T23:15:31-07:00 actor: self |

| section |
|---|---|
| observed fixture set |

| # | directive |
|---|---|
| 1 | `sh gate/gap-145-consumer.sh` (refuses a re-introduced `findMatchingParen`) |
| 2 | `sh gate/grammar-projection.sh` (control refuses malformed producer; entry selection chooses `_project` once; explicit `--entry _project` agrees with root; source `main` refuses; ABI `main` is the only executable) |
| 3 | `sh gate/posix.sh` (84 shell gates parse under dash) |
| 4 | `zig build gap-145-consumer` (22 checks pass) |
| 5 | `zig build grammar-projection` (PASS — control + entry + byte-identical regeneration) |
| 6 | byte-identity: `cmp src/grammar_role_table.zig` and `cmp lib/token/grammarrole.id` against tracked artifacts |

| section |
|---|---|
| what changed |

| # | directive |
|---|---|
| 1 | `src/c_backend.zig`: added `.str` to `scalarType`, `.str_len` / `.call_extern` / extended `.load_index` arms; threaded `home` from `main.zig` into `emitSource` so every function symbol is mangled as `idol_<safe(home)>__<name>` (matches `home_resolve.homeSymbol`); the call_direct emit now passes the first arg from `.lhs` (the dnir's calling convention) instead of a leftover `a0` |
| 2 | `src/main.zig`: `home_resolve.homeOfPath(src_path)` threads into `c_backend.emitSource` |
| 3 | `tools/node/dev/grammar/emit`: both compile paths switched to `--backend=c --emit=c --target=c-source --entry _project`; the retired source-main refusal also accepts `DNB004` (the only refusal a DNB004 host can produce for the hidden compile); the C link step uses `-Wno-excess-initializers` (forward decls are size-1 placeholders that merge with the sized real definition); the zig test step uses `-lc` (grammar_roles.zig pulls in libc); the selftest fake is a valid C program whose run-time output trips `zig fmt --check` |
| 4 | `tools/node/dev/grammar/idol_c_runtime_shim.c` (NEW): host-agnostic C99 shim for `idol_io_*`, `idol_os_*`, `idol_str_*`, `idol_process_capture`, plus `idol_malloc`, `idol_memcpy`, `idol_printf`, `idol_snprintf`, `idol_strcmp`, `idol_strlen` |

| section |
|---|---|
| result |

| # | directive |
|---|---|
| 1 | Three commits land on `clpi/idol/main`: |

| # | directive |
|---|---|
| 1 | 620b3d9 GAP-145 unblock: c backend + shim compile lib/compiler/token.id on any host 248cc28 GAP-145 progress: c-backend shim fixup + variadic measure + sub inclusive index c5f3db5 GAP-145: close grammar-projection gate on host C backend |

| # | directive |
|---|---|
| 1 | The grammar-projection gate is reachable on aarch64-linux (the DNB004 host), where before this work the direct backend refused and the C backend was the only path that compiled the owner. |

| section |
|---|---|
| if rejected (not applicable) |

| # | directive |
|---|---|
