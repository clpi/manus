# @corpus proof
# compile-fail: @comp.assert(@satisfies(...)) must error when concept not satisfied

concept HasHash
    hash(self): i64
end

type Empty = {}

@comp.assert(@satisfies(Empty, "HasHash"), "Empty lacks hash")
