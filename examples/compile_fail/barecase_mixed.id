
token: {
    kind: { name, number, eof }
    span: i64
}

glyph: {
    form: { circle, square }
    area: f64
}

main = (): i64
    k = token.kind.eof
    n = 0
    if k == circle
        n += 1
    print("{n}")
    0
