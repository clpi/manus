-- compile_fail: multi-concept constraint violation
concept Hashable
    fun hash(self) -> i64
end

concept Counter
    fun value(self) -> i64
end

Half = {}
fun Half:hash() -> i64
    return 1
end

fun use_both<T: Hashable + Counter>(target: T): i64
    0
end

use_both(Half)
