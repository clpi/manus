
@comp.define.derive("Factory", generate: str = (meta)
  child = "fun generate(meta) -> str return \"static int \" .. meta.name .. \"_magic() { return 42; }\" end"
  @comp.register.derive(meta.name .. "kind", child)
  ""
)

@derive(Factory)
point: { x: f64, y: f64 }

@derive(pointkind)
box: { w: f64, h: f64 }

print("Self-extending derive compiled")
