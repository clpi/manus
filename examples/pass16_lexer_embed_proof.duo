-- Pass 16 MP-04 — Duo lexer module embed smoke (std.compiler.lexer.new; next() proven via tokenize proof).

embed_smoke(): i64
    lex = std.compiler.lexer.new("fun x end", "proof.duo")
    0
end

main(): i64
    embed_smoke()
end
