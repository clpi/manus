
@derive(Display, Eq, Add, Sub, Neg, Len, Default)
pair: { x: f64, y: f64 }

a =
  x = 3.0
  y = 4.0
b =
  x = 1.0
  y = 2.0

print("a.x = " .. to(str)(a.x))
print("a.y = " .. to(str)(a.y))
print("b.x = " .. to(str)(b.x))

print("ALL DERIVE TESTS PASSED")
