
main: i64 = ()
    xs: [3]i64 = {3, 5, 8}
    zs: [0]i64 = {}
    hit = xs:any((x) x % 2 == 0)
    miss = xs:any((x) x > 100)
    empty = zs:any((x) x > 0)
    verdict = 0
    if hit
        verdict += 1
    if miss
        verdict += 10
    if empty
        verdict += 100
    verdict
