-- Test 'req' as a regular identifier
-- 'req' can be used as a fun name and provided by the standard library
-- as an alias for 'require'

req = fun(module: str): str
    return "loaded: " .. module
end

print(req("mylib"))