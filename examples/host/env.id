# Teaching: os.env table under os world (GAP-155).
#
# environment is NOT a thing. Never os.getenv. Never environment[...].
# Foreign getenv exists only at scripts/ingress bootstrap (@c.emit).

mode: str = os.env["IDOLTREE"]

main: i64 = ()
    print(mode)
    0
