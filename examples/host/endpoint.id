# Teaching: io world subject-first I/O (GAP-155).
#
# Never io.read() / io.write(). Use io:read / io:write under io world.
# Physical realization may be pipe, terminal, socket, or memory.

main: i64 = ()
    line = io:read()
    io:write(line)
    0
