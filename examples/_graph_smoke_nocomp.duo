Point: { x: f64, y: f64 }

@sealed
Box: { label: str, count: i64 }

enum Color
    Red
    Green
    Blue
end

main()
    p: Point = { x = 3.0, y = 4.0 }
    pt2: { x: f64, y: f64 } = { x = 1.0, y = 2.0 }
    b: Box = { label = "ok", count = 1 }
end
