# Cross-language metaprogramming paths that are implemented today.
#
# Run: duo run examples/cross_lang_showcase.id

--- @comp.c.include("math.h")
--- @comp.c.include("string.h")

@cinclude("math.h")

@cinclude("string.h")

@cinclude("math.h")


@cinclude("string.h")


@comp.schema("examples/schemas/point.json")

@comp.run("printf 'static const char harness_tag[] = \"duo-cross\";'")

@comp.c.emit([[
static double sigmoid(double x) {
    return 1.0 / (1.0 + exp(-x));
}

static double relu(double x) {
    return x > 0.0 ? x : 0.0;
}

static long fibonacci(long n) {
    if (n <= 1) return n;
    long a = 0;
    long b = 1;
    for (long i = 2; i <= n; ++i) {
        long next = a + b;
        a = b;
        b = next;
    }
    return b;
}

static long gcd_long(long a, long b) {
    while (b != 0) {
        long t = b;
        b = a % b;
        a = t;
    }
    return a;
}

static double vec2_dot(double x1, double y1, double x2, double y2) {
    return x1 * x2 + y1 * y2;
}

static double vec2_len(double x, double y) {
    return sqrt(x * x + y * y);
}
]])


@comp.c.emit(@comp.run("printf 'static const double sin_lut[8] = {0.00000000,0.38268343,0.70710678,0.92387953,1.00000000,0.92387953,0.70710678,0.38268343}; static double sin_lut_at(long i) { return sin_lut[i & 7]; }'"))

EMBEDDED = @comp.embed.str("examples/run_smoke.id")
HOST = @comp.run("hostname")
py = @comp.run("python3 --version 2>&1")
ZIG_VER = @comp.run("zig version 2>/dev/null || echo zig-unavailable")

print("=== Duo Cross-Language Metaprogramming Showcase ===")
print("")

print("-- Generated native C via @comp.run --")
@comp.c.call("printf", "  tag = %s\n", @(harness_tag))
@comp.c.call("printf", "  sigmoid(1.0) = %.6f\n", @comp.c.call("sigmoid", @(1.0)))
@comp.c.call("printf", "  relu(-5.0) = %.1f\n", @comp.c.call("relu", @(-5.0)))
@comp.c.call("printf", "  fibonacci(10) = %ld\n", @comp.c.call("fibonacci", @(10)))
@comp.c.call("printf", "  gcd(12, 8) = %ld\n", @comp.c.call("gcd_long", @(12), @(8)))
@comp.c.call("printf", "  vec2_dot(1,2,3,4) = %.1f\n", @comp.c.call("vec2_dot", @(1.0), @(2.0), @(3.0), @(4.0)))
@comp.c.call("printf", "  vec2_len(3,4) = %.1f\n", @comp.c.call("vec2_len", @(3.0), @(4.0)))
@comp.c.call("printf", "  sin_lut[2] = %.8f\n", @comp.c.call("sin_lut_at", @(2)))
print("")

print("-- Header/schema/embed surfaces --")
@comp.c.call("printf", "  strlen(\"duo\") = %zu\n", @comp.c.call("strlen", "duo"))
print("  embedded file bytes = " .. to(str)(EMBEDDED:len()))
print("  host = " .. HOST)
print("  python = " .. py)
print("  zig = " .. ZIG_VER)
print("")

print("=== done ===")
