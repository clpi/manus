# Auto-generate extern declarations from a C header at compile time
# Run: duo run examples/ffi_gen_showcase.id

@c.include("string.h")
@comp.ffi("string.h")

demo_len: i64 = (s: str)
  return @c.call("strlen", s)

print("=== @comp.ffi showcase ===")
print("strlen('duo') = " .. to(str)(demo_len("duo")))
