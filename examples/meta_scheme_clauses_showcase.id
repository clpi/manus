# @comp.scheme.clauses — named clause list → O(clauses) native C
# Run: duo run examples/meta_scheme_clauses_showcase.id

spec = "header: /* header clause */; body: /* body clause */"

code = @comp.scheme.clauses(spec)

print("=== @comp.scheme.clauses showcase ===")
print(code)
print("RESULT scheme_clauses_ok = true")
