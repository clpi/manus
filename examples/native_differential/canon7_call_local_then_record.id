tok: @{ kind: i64, start: i64, len: i64 }
one: i64 = (x: i64)
    x + 1
end
scan: tok = (pos: i64)
    k = one(pos)
    { kind = k, start = pos, len = 1 }
end
main(): i64
    t: tok = scan(5)
    t.kind + t.start
end
