tok: @{ kind: i64, start: i64, len: i64 }
pred: i64 = (c: i64)
    2
scan: tok = (pos: i64)
    k = pred(pos)
    if k == 1
        return { kind = 1, start = pos, len = 1 }
    { kind = 2, start = pos, len = 1 }
main(): i64
    t: tok = scan(9)
    t.kind
