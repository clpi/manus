Kind = @{ eof = 0, ident = 1 }
tok: @{ kind: i64, start: i64, len: i64 }
mk = (pos: i64): tok
    { kind = Kind.ident, start = pos, len = 2 }
end
main(): i64
    t: tok = mk(5)
    t.start + t.len
end
