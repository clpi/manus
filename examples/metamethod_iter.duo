custom_iter(state: any, index: any) -> any
  next = index + 1
  if next > 3
    return nil
  end
  return next, next * 10
end
iterable = setmetatable({}, {
  __iter = (self: any) -> any
    return custom_iter, nil, 0
  end
})
sum = 0
for i, value in iterable
  sum = sum + value
end
print(sum)
