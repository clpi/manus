t = {}
t_start = os.clock()
for i = 1, 100000
    t[i] = i
end
sum: f64 = 0.0
for i = 1, 100000
    v: f64 = t[i]
    sum += v
end
t_end = os.clock()
print("Table sum:", sum)
print("Table Time:", t_end - t_start, "seconds")
