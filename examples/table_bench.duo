local t = {}
local t_start = os.clock()
for i = 1, 100000 do
    t[i] = i
end
local sum: f64 = 0.0
for i = 1, 100000 do
    local v: f64 = t[i]
    sum = sum + v
end
local t_end = os.clock()
print("Table sum:", sum)
print("Table Time:", t_end - t_start, "seconds")
