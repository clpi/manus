@specialize({ fn = "twice", types = "i64, f64" })

twice < T > (x: T) -> T
  x
end
print("=== @specialize batch showcase ===")
print("twice(21) = " .. to(str)(twice(21)))
print("twice(2.5) = " .. to(str)(twice(2.5)))
