# The native pipeline `|>` must lower to a direct C call,
# not to a boxed invoke. The C must hold `double(21)` at the call site.

double: i64 = (x: i64)
  x * 2

main: i64 = ()
  y = 21 |> double
  y
