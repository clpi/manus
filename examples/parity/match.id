
top = @comp.match("a|b", fun(m)
  m.pattern .. "\n"
)
nested = @comp.map("q", fun(seed)
  @comp.match("a|b", fun(m)
    m.pattern .. "\n"
  )
)
parity: str = {}
@comp.match("a|b", fun(m)
  m.pattern .. "\n"
)
block = parity()
