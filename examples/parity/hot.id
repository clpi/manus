# Call-algebra fixture: a `@hot` callee, bound the same way as
# call.id so the only difference between the two is the attribute.

@hot
scale: i64 = (x: i64)
  x * 2

main: i64 = ()
  y = scale(21)
  y
