
top = @comp.each("a|b", fun(f)
  f.name .. "\n"
)
nested = @comp.map("q", fun(seed)
  @comp.each("a|b", fun(f)
    f.name .. "\n"
  )
)
parity: str = {}
@comp.each("a|b", fun(f)
  f.name .. "\n"
)
block = parity()
