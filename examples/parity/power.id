
concept Has1
  x: i64
offset: { x: i64 }
sig: str = t
t.name .. "\n"

top = @comp.power("Has1", sig)
nested = @comp.map("q", fun(seed)
  @comp.power("Has1", sig)
)
parity: str = {}
@comp.power("Has1", sig)
block = parity()
