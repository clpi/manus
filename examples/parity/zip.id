
top = @comp.zip("a|b", "1|2", fun(p)
  p.a .. p.b .. "\n"
)
nested = @comp.map("q", fun(seed)
  @comp.zip("a|b", "1|2", fun(p)
    p.a .. p.b .. "\n"
  )
)
parity: str = {}
@comp.zip("a|b", "1|2", fun(p)
  p.a .. p.b .. "\n"
)
block = parity()
