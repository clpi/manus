
top = @comp.tabulate(2, fun(i)
  tostring(i) .. "\n"
)
nested = @comp.map("q", fun(seed)
  @comp.tabulate(2, fun(i)
    tostring(i) .. "\n"
  )
)
parity: str = {}
@comp.tabulate(2, fun(i)
  tostring(i) .. "\n"
)
block = parity()
