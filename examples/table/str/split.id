# TEXT: empty-string edges of the split face. An empty field between two
# separators is KEPT, and the part count includes it.
# expect: 3 []
# @corpus current
s = "a,,b"
p = s:split(",")
n = p:len()
a = p(2)
print("{n} [{a}]")
