# TEXT: str IS bytes, 0-based. Two-byte UTF-8 answers 2 for `#`, and the
# indices are the encoding units, not characters or graphemes. §0.8, B-1.
# expect: 2 195 169
# @corpus current
s = "é"
n = s:len()
a = s(0)
b = s(1)
print("{n} {a} {b}")
