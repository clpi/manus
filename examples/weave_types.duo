-- Foreign module for @comp.weave cross-module type sweep
-- Run via: examples/meta_weave_showcase.duo

concept HasXY
    x: f64
    y: f64
end

Point: { x: f64, y: f64 }
Pair: { x: f64, y: f64 }
TagOnly: { tag: str }
