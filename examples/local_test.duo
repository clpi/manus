-- Test local-by-default scoping in .duo files

-- This should create a local variable automatically
x = 10
print(x)

-- This should also create a local variable
y = 20
print(y)

-- Using global keyword to explicitly declare a global
global z = 30
print(z)

-- Test that explicit local still works
local explicit_local = 40
print(explicit_local)