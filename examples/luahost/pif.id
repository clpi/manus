-- CF-0 — if/then/end. Declared, because `duo check` runs sema where the
-- Zig corpus ran the parser alone; the construct under test is unchanged.
local ready = true
local function run()
end
if ready then
    run()
end
