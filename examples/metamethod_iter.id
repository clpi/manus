custom_iter = (state, index)
  next = index + 1
  if next > 3
    return nil
  return next, next * 10

iterable = setmetatable({}, {
  __iter = (self)
    return custom_iter, nil, 0
})

sum = 0
for i, value in iterable
  sum = sum + value

print(sum)
