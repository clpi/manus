# Celsius to Fahrenheit and back. The float path, so the rendering of the
# result is part of the assertion.
# expect: 98.6 37.0 -40.0
# @corpus current
far: f64 = (c: f64)
    c * 9.0 / 5.0 + 32.0

cel: f64 = (f: f64)
    (f - 32.0) * 5.0 / 9.0

a = far(37.0)
b = cel(98.6)
c = far(-40.0)
print("{a} {b} {c}")
