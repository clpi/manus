-- Test generics with angle bracket syntax

-- Basic generic type annotation (simplified - just parses for now)
fun identity<T>(value: T): T
    return value
end

-- Using the function
local result = identity(42)
print(result)

local result2 = identity("hello")
print(result2)