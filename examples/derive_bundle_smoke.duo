@comp.define.derive("TagOnly", generate(meta) -> str
    "/* tagged " .. meta.name .. " */\n"
end)

@comp.derive.bundle("Debug")
type Tiny = { n: i64 }

@derive(TagOnly)
type Also = { v: f64 }

print("bundle ok")
