-- Pass 7 Milestone 1 smoke — `duo explain explain_smoke.duo`
Point: @{ x: f64, y: f64 }

distance2(p: Point): f64
    p.x * p.x + p.y * p.y
end

main(): f64
    p = Point { x = 3, y = 4 }
    distance2(p)
end
