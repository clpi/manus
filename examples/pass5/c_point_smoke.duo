-- Pass 5 smoke: @comp.c.import registers CPoint + distance2 for sema/codegen.
-- Requires linking against a C object providing distance2 (see pass5_foreign_tests.zig).

@comp.c.import("fixtures/point.h")

main(): f64
    p: CPoint = { x = 3.0, y = 4.0 }
    distance2(p)
end
