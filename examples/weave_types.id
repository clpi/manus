# Foreign module for @comp.weave cross-module type sweep
# Run via: examples/meta_weave_showcase.id

concept HasXY
  x: f64
  y: f64

point: { x: f64, y: f64 }
pair: { x: f64, y: f64 }
tag: { tag: str }
