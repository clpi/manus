# @corpus current
# Document read — subject-first; relation lives at lib/compiler/reader.id.
document: { value: i64 }

read: i64 = (subject: document)
  subject.value

print(document{ value = 42 }:read())
