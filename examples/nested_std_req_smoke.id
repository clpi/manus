path = "/tmp/duo_nested_req_smoke.json"
os.write_file(path, "{\"ok\":true,\"n\":7}")

data = json.decode(os.read_file(path))
ok = data.ok == true and data.n == 7

print("RESULT nested_std_req_ok = " .. tostring(ok))
