@comp.define.derive("TagOnly", generate(meta) -> str
    "/* tagged " .. meta.name .. " */\n"
end)

@comp.derive.bundle("Debug")
Tiny: { n: i64 }

@derive(TagOnly)
Also: { v: f64 }

print("bundle ok")
