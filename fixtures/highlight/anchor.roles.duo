-- Expected (line, col, len, role) for anchor.duo. HAND-WRITTEN from
-- docs/spec/roles.md §5.6.
--
-- What this fixture discriminates: bare `@` against postfix `X@rel`.
-- grammar.md R5 decides it with no lookahead — "postfix `@` requires a
-- left operand; bare `@` is a primary; immediately followed by `{` it is
-- the enclosing-descriptor constructor" — and closes the door on the
-- third reading: "No prefix-`@`-expression production exists: the grammar
-- cannot express a directive."
--
-- Both forms are ORANGE and H-4 splits them no further, so both are
-- `anchor` here. Whether that is one role or three is UNSTATED; if three,
-- the arithmetic in docs/spec/roles.md §4 goes from 17 to 19 and H-2's
-- "closed at 18" is further away, not nearer. -- gap[071]
spans = {
    { 1, 1, 9, "comment" },
    { 2, 1, 5, "name" },
    { 2, 7, 1, "bind" },
    { 2, 9, 1, "anchor" },
    { 2, 10, 1, "brace" },
    { 2, 12, 1, "name" },
    { 2, 14, 1, "bind" },
    { 2, 16, 1, "number" },
    { 2, 18, 1, "brace" },
    { 3, 1, 5, "name" },
    { 3, 7, 1, "bind" },
    { 3, 9, 5, "name" },
    { 3, 14, 1, "anchor" },
    { 3, 15, 5, "name" },
}
