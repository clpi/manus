# Agent Instructions — THIS FILE IS A POINTER, NOT A COPY

**Read `/AGENTS.md` and `/CLAUDE.md`. They are the law. This file holds none.**

This file used to be a 369-line FORK of the repository's `AGENTS.md`, drifted
independently, and taught `snake_case`, `end`, `@comp.*` directives, `then`,
`do`, and the File-as-`M` pattern — every one of which current law denies. Agents
that read it wrote epoch-1 duon in good faith and were right to trust it.

**That is why it is now a pointer.** The law having more than one home is the
defect: each copy drifts, and an agent cites whichever copy it found. There is
one home. Everything else points at it.

- **The law**: `/CLAUDE.md` (epoch 2, Pass 100 as amended) + `docs/spec/`
- **The mechanical deny list and the canonical forms**: `/AGENTS.md`
- **The teaching, with worked before/after pairs**: `docs/src/idiomatic-duon.md`
- **The gate that blocks you**: `.githooks/pre-commit` runs
  `scripts/idiomgate.duo` over the staged diff on EVERY commit, for every agent
- **Active work coordination** (the only thing this directory is for):
  `.agents/AGENT_COORDINATION.md`

If you have the old contents of this file in context from an earlier session,
**it is VOID** under the epoch-2 precedence rule and never applies.
