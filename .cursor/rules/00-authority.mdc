---
alwaysApply: true
---

# Idol authority router

Start every substantive session at `AGENTS.md`, then read
`.agents/HARNESS.md` (generated harness boot payload from
`docs/spec/harness-projection.md`). Follow the chain to
`docs/spec/constitution.md` (sole semantic law; **Idol algebra closure is
§67 only** — no independent algebra prompt is authority). `CLAUDE.md`,
`docs/spec/source.md`, `docs/spec/host.md` (host/shell/capability closure),
`docs/spec/AUTHORITY.md`, `docs/bootstrap.md`, `.agents/AGENT_CANONICAL.md`,
`.agents/AGENT_COORDINATION.md`, and `.agents/RELEASE_READINESS.md` are
projections only.

**Idol** (`idol`, `.id`) is the language and public command identity. Semantic
law lives in `docs/spec/constitution.md` (C0). Active development is **`clpi/duo`**. **`idollang/idol`** is the future release repository only; do
not migrate development there. Historical Duo, Duon, `.id`, and `duo`
bootstrap names are provenance only.

Run `./tools/node/dev/generate-harness`, `./tools/node/dev/orient`, and
`./tools/node/dev/doctor` before substantive work. Identify the semantic owner and bounded boundary before editing. Prefer
deletion or convergence over new mechanisms. Do not restore historical
architecture or duplicate MCP manifests, emitters, or coordination ledgers.
