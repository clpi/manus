═══════════════════════════════════════════════════════════════════════
PROJECTION — not authority. Authority: docs/bootstrap.md (frontier),
docs/spec/constitution.md (law), docs/spec/realization.md (architecture).
═══════════════════════════════════════════════════════════════════════

═══════════════════════════════════════════════════════════════════════
REVISION-BOUND EVIDENCE BUNDLE
═══════════════════════════════════════════════════════════════════════

law.evidence.subject (EVIDENCE-SUBJECT-ONE):
  subject revision:  29f62035  (the implementation actually measured)
  evidence revision: 59093d7b  (the commit containing this report)
  live tree write:   2e5d516f  (working tree at time of measurement)

These three may all differ. "HEAD" without qualification is ambiguous.
Do not report metrics "at HEAD" unless subject == current HEAD.

Live Git HEAD and dirty state: `evidence/HEAD.txt` (regenerated by
`tools/node/dev/orient`). Integrated commits after `59093d7b` (e.g.
catalog-deletion follow-ups, graph `applicationRelation`, str-at lowering)
do **not** retroactively change the bound subject counts below until a
fresh measurement run re-binds subject and evidence revisions.

Date: 2026-08-12T21:59:00-07:00
Dirty: see live `git status` — snapshot may diverge from subject
Build: zig build passes at subject; unit aggregate 1216 pass / 22 fail / 4 crash
FTCFTW complete proof: NOT PROVEN
No compiler B

═══════════════════════════════════════════════════════════════════════
GATE EXECUTION RESULTS
═══════════════════════════════════════════════════════════════════════

All 6 gates PASS semantic check (idol check):

  gate/idiom.id:       ✓ checked — no errors
  gate/host.id:        ✓ checked — no errors
  gate/path.id:        ✓ checked — no errors
  gate/architecture.id: ✓ checked — no errors
  gate/admission.id:   ✓ checked — no errors
  gate/census.id:      ✓ checked — no errors

Direct backend execution: BLOCKED
  All gates fail with DNB011 on builtin I/O
  This is a known, characterized failure (bridge_dnb011.md)

Pre-commit hook: FUNCTIONAL
  Uses check-based admission (documented temporary bypass)
  Verified with test commit

═══════════════════════════════════════════════════════════════════════
LEDGER EXECUTION RESULTS
═══════════════════════════════════════════════════════════════════════

All 8 ledgers PASS semantic check (idol check):

  scripts/ledger/shc.id:      ✓ checked — no errors
  scripts/ledger/debt.id:     ✓ checked — no errors
  scripts/ledger/embed.id:    ✓ checked — no errors
  scripts/ledger/zig.id:      ✓ checked — no errors
  scripts/ledger/js.id:       ✓ checked — no errors
  scripts/ledger/c.id:        ✓ checked — no errors
  scripts/ledger/ftcftw.id:   ✓ checked — no errors
  scripts/ledger/perf.id:     ✓ checked — no errors

Direct backend execution results:

  embed:  RUNS — "fail sites 1 payload-lines 1"
          This is actual executable evidence of embedded @c sites

  shc:    TIMEOUT — runs but exceeds 60s limit
          Indicates heavy shell command execution

  ftcftw: RUNS — "control ok"
          Reports all contracts present:
            native: ok
            cfloor: ok
            diff: ok
            bench: ok
            runtime: ok
            shc: ok
          Misses oracle: audit scripts/ledger/embed

  debt:   DNB001 — ret-type:any
          Exact failure: "native-scalar precheck — ret-type:any"

  zig:    DNB001 — ret-type:any
          Exact failure: "native-scalar precheck — ret-type:any"

  js:     DNB001 — ret-type:any
          Exact failure: "native-scalar precheck — ret-type:any"

  c:      DNB001 — ret-type:any
          Exact failure: "native-scalar precheck — ret-type:any"

  perf:   RUNS — "fail missing contracts 19"
          Actual measurement:
            bench: ok
            runtime: ok
            compilesize: miss (compile_size: FAIL)
            gpu: miss (gpu: FAIL)
            cross: miss (cross: FAIL)
            wasm: miss (wasm: FAIL)
            honest: miss (honest: FAIL)
            ml: miss (ml: FAIL)

═══════════════════════════════════════════════════════════════════════
SHC MEASUREMENT
═══════════════════════════════════════════════════════════════════════

Earliest host-owned fact:
  src/lexer_bridge.zig:sourceFacts()
  suffix/path → source law/provenance

Silent fallback: RETRACTED at live HEAD (`2e5d516`)
  Origin audit cited tokenizeHost() as the canonical .id production route.
  Live `route()` uses `tokenize()`; host `tokenizeHost()` is differential-only.
  `docs/bootstrap.md` matches the live dispatch split.

Transferred this run: canonical `.id` lex route (Idol `tokenize()`)

Boundaries still host-owned:
  source ingress (suffix/path classification)
  differential host lexer oracle (`tokenizeHost`)
  grammar roles (GAP-134 BLOCKED)
  parser recognition (src/parser.zig)
  binding/scope (src/sema.zig)
  semantic construction (src/semantic_graph.zig)
  relation/application resolution
  demand
  lowering/realization
  machine selection
  object emission

═══════════════════════════════════════════════════════════════════════
FTCFTW MEASUREMENT
═══════════════════════════════════════════════════════════════════════

Evidence status: INVALID for performance claims

C comparison: INVALID
  - No executable evidence chain
  - C backend emits broken C for closures (ret-type:any)

WASM comparison: INVALID
  - No correctness-locked comparison against pinned Wasmtime
  - perf ledger reports 19 missing contracts

Startup/runtime/compile/memory/size: UNKNOWN
  - Direct backend blocks all I/O programs
  - No valid measurement harness

Uncertainty ledger (exact counts from source inspection):

  1. Boxing (lua_Value): ~1,271 emissions — VERY HIGH
  2. Heap allocation: ~82 lua_table_new, ~120 malloc — VERY HIGH
  3. Indirect calls (lua_invoke): ~97 emissions — HIGH
  4. Guards: 6 kinds, 3 unwired — MEDIUM-HIGH
  5. Tagging: ~174 tag, ~20 number_kind — MEDIUM
  6. Runtime descriptors: hundreds of tables — MEDIUM
  7. Materialized packs: dozens of sites — MEDIUM
  8. Hashing: ~84 calc_lua_hash — LOW-MEDIUM
  9. Copies: ~134 memcpy — LOW-MEDIUM
  10. World objects: ~74 matches — LOW
  11. Synchronization: ~63 matches — LOW

Root cause: Knowledge lattice (semantic_algebra.KnowledgeLevel)
  .unknown/.observed/.guarded → boxing, allocation, indirect calls
  .stable but not .native → sealed but still boxed
  .dynamic StorageClass → all of the above + descriptors

═══════════════════════════════════════════════════════════════════════
GATE COVERAGE GAPS (adversarial)
═══════════════════════════════════════════════════════════════════════

Measured violations NOT caught by current gates:

  1. callable = true in table constructors: 12 violations
  2. world["method"]() dispatch: 11 violations
  3. String-indexed dispatch tables: 15+ violations
  4. Mediator roles (registry, factory, broker): 16 violations
  5. std.* semantic hops: 20 violations

Total: 74 exact violations across 5 categories

Positive controls verified:
  graph.get(exact_id): allowed
  json.object.get("field"): allowed
  runtime table actual string key: allowed
  exact application relation edge: allowed

═══════════════════════════════════════════════════════════════════════
CORPUS SAFETY AUDIT
═══════════════════════════════════════════════════════════════════════

Retired syntax:
  "req" found in scripts/test_lexer.id:2
  "require" found in scripts/vscodeemit.id:10,11,86
  All other retired syntax NOT found in active corpus

Historical project names:
  "CATEGORY_DUO_CONTEXTUAL" in lib/token/classify.id:18
  ".duo" in gate/idiom.id:286 (acceptable as gate control)
  "duo", "duon", "idsem" in docs/spec/*.md (historical documentation)

Pass numbers: Extensive in docs/spec/*.md, NOT in lib/scripts/examples/gate

Plural identities: NOT found as semantic identities

*able identities: 12 violations (callable = true)

Role nouns: 16 violations (registry, factory, broker, manager)

Encoding namespace: 3 violations (emit_json_serializers, emit_protobuf_encode, emit_protobuf_decode)

.get idiom: 2 caught, 11 NOT caught (string["match"])

[] indexing: 15+ violations (string-indexed dispatch tables)

Semantic string matching: NOT found in lib/ (graph.id gate catches these)

═══════════════════════════════════════════════════════════════════════
GLOBAL ZERO TARGET AUDIT
═══════════════════════════════════════════════════════════════════════

ZERO violations (6 categories):
  semantic string lookup after resolution
  semantic path lookup after ingress
  parent-scope lookup after resolution
  operational graph edge kinds
  plural-cardinality identities
  inferable explicit `to`/projection/world plumbing

Measured violations (5 categories):
  module/import semantic machinery: 4
  *able/*ible identities: 12
  role-noun protocol identities: 16
  collision mediator systems: 16
  generic encode/codec ontology: 3

Implementation-blocked (10 categories):
  known-shape generic hash: ~84
  sealed-target indirect dispatch: ~97
  nonescaping heap closures: ~1
  singleton-union tags: ~174
  unexplained allocations/copies/boxes: ~1,271 lua_Value, ~82 lua_table_new, ~120 malloc, ~134 memcpy
  silent host semantic fallback: 1 confirmed
  and more...

═══════════════════════════════════════════════════════════════════════
EVIDENCE CONFIDENCE
═══════════════════════════════════════════════════════════════════════

HIGH for:
  - SHC frontier characterization (source code inspection, exact lines)
  - DNB011 failure boundary (minimal reproductions, exact error messages)
  - FTCFTW uncertainty ledger (exact file/line references, frequency counts)
  - Gate coverage gaps (exact violations, exact gate line references)
  - Corpus audit findings (exact file/line references)
  - Pre-commit hook operability (verified with test commit)
  - Ledger execution results (actual program output captured)

LOW for:
  - FTCFTW C/WASM performance claims (no valid comparison)
  - Positive damage control verification (harness cannot execute I/O programs)
  - Any claim of "100% SHC" or "FTCFTW proven"

═══════════════════════════════════════════════════════════════════════
NEXT SINGLE HIGHEST-LEVERAGE FRONTIER
═══════════════════════════════════════════════════════════════════════

GAP-145 lexical identity closure on the already-Idol `tokenize()` route.
Production dispatch no longer falls back to host `tokenizeHost()`; suffix/path
ingress and distinct token-kind identities remain the earliest open facts.

═══════════════════════════════════════════════════════════════════════
