| # | directive |
|---|---|
| 1 | ═══════════════════════════════════════════════════════════════════════ PROJECTION — not authority. |
| 2 | Authority: docs/bootstrap.md (frontier), docs/spec/constitution.md (law), docs/spec/realization.md (architecture). ═══════════════════════════════════════════════════════════════════════ |

| # | directive |
|---|---|
| 1 | ═══════════════════════════════════════════════════════════════════════ REVISION-BOUND EVIDENCE BUNDLE ═══════════════════════════════════════════════════════════════════════ |

| # | directive |
|---|---|
| 1 | law.evidence.subject (EVIDENCE-SUBJECT-ONE): subject revision: 7f7d59f7 (the implementation actually measured) evidence revision: 7f7d59f7 (the commit containing this report) live tree write: 7f7d59f7 (working tree at time of measurement) |

| # | directive |
|---|---|
| 1 | These three may all differ. |
| 2 | "HEAD" without qualification is ambiguous. |
| 3 | Do not report metrics "at HEAD" unless subject == current HEAD. |

| # | directive |
|---|---|
| 1 | Live Git HEAD and dirty state: `evidence/HEAD.txt` (regenerated by `tools/node/dev/orient`). |
| 2 | Integrated commits after `7f7d59f7` (e.g. lex cursor migration, parser mirror, harvestCommentHints signature) do **not** retroactively change the bound subject counts below until a fresh measurement run re-binds subject and evidence revisions. |

| # | directive |
|---|---|
| 1 | Date: 2026-08-31T08:09:09+00:00 Dirty: see live `git status` — snapshot may diverge from subject Build: zig build passes at subject; unit aggregate 1832 pass / 62 skip / 0 fail (1894 total) FTCFTW complete proof: NOT PROVEN No compiler B |

| # | directive |
|---|---|
| 1 | ═══════════════════════════════════════════════════════════════════════ GATE EXECUTION RESULTS ═══════════════════════════════════════════════════════════════════════ |

| # | directive |
|---|---|
| 1 | All 6 gates PASS semantic check (idol check): |

| # | directive |
|---|---|
| 1 | gate/idiom.id: ✓ checked — no errors gate/host.id: ✓ checked — no errors gate/path.id: ✓ checked — no errors gate/architecture.id: ✓ checked — no errors gate/admission.id: ✓ checked — no errors gate/census.id: ✓ checked — no errors |

| # | directive |
|---|---|
| 1 | Direct backend execution: BLOCKED All gates fail with DNB011 on builtin I/O (Dated. |
| 2 | Re-measured 2026-08-24 on x86_64-Linux: the refusal is DNB004, not DNB011 — the direct backend has no machine realization for this host at all, so builtin I/O is never reached. |
| 3 | DNB011 was the AArch64-macOS answer. |
| 4 | The characterization document this line cited, root bridge_dnb011.md, was deleted with the other root analysis prose; the live single producer of the host-realization fact is gate/realization/direct.sh.) |

| # | directive |
|---|---|
| 1 | Pre-commit hook: FUNCTIONAL Uses check-based admission (documented temporary bypass) Verified with test commit |

| # | directive |
|---|---|
| 1 | ═══════════════════════════════════════════════════════════════════════ LEDGER EXECUTION RESULTS ═══════════════════════════════════════════════════════════════════════ |

| # | directive |
|---|---|
| 1 | All 8 ledgers PASS semantic check (idol check): |

| # | directive |
|---|---|
| 1 | scripts/ledger/shc.id: ✓ checked — no errors scripts/ledger/debt.id: ✓ checked — no errors scripts/ledger/embed.id: ✓ checked — no errors scripts/ledger/zig.id: ✓ checked — no errors scripts/ledger/js.id: ✓ checked — no errors scripts/ledger/c.id: ✓ checked — no errors scripts/ledger/ftcftw.id: ✓ checked — no errors scripts/ledger/perf.id: ✓ checked — no errors |

| # | directive |
|---|---|
| 1 | Direct backend execution results: |

| # | directive |
|---|---|
| 1 | embed: RUNS — "fail sites 1 payload-lines 1" This is actual executable evidence of embedded @c sites |

| # | directive |
|---|---|
| 1 | shc: TIMEOUT — runs but exceeds 60s limit Indicates heavy shell command execution |

| # | directive |
|---|---|
| 1 | ftcftw: RUNS — "control ok" Reports all contracts present: native: ok cfloor: ok diff: ok bench: ok runtime: ok shc: ok Misses oracle: audit scripts/ledger/embed |

| # | directive |
|---|---|
| 1 | debt: DNB001 — ret-type:any Exact failure: "native-scalar precheck — ret-type:any" |

| # | directive |
|---|---|
| 1 | zig: DNB001 — ret-type:any Exact failure: "native-scalar precheck — ret-type:any" |

| # | directive |
|---|---|
| 1 | js: DNB001 — ret-type:any Exact failure: "native-scalar precheck — ret-type:any" |

| # | directive |
|---|---|
| 1 | c: DNB001 — ret-type:any Exact failure: "native-scalar precheck — ret-type:any" |

| # | directive |
|---|---|
| 1 | perf: RUNS — "fail missing contracts 19" Actual measurement: bench: ok runtime: ok compilesize: miss (compile_size: FAIL) gpu: miss (gpu: FAIL) cross: miss (cross: FAIL) wasm: miss (wasm: FAIL) honest: miss (honest: FAIL) ml: miss (ml: FAIL) |

| # | directive |
|---|---|
| 1 | ═══════════════════════════════════════════════════════════════════════ SHC MEASUREMENT ═══════════════════════════════════════════════════════════════════════ |

| # | directive |
|---|---|
| 1 | Earliest host-owned fact: src/lexer_bridge.zig:sourceFacts() suffix/path → source law/provenance |

| # | directive |
|---|---|
| 1 | Silent fallback: RETRACTED at live HEAD (`2e5d516`) Origin audit cited tokenizeHost() as the canonical .id production route. |
| 2 | Live `route()` uses `tokenize()`; host `tokenizeHost()` is differential-only. `docs/bootstrap.md` matches the live dispatch split. |

| # | directive |
|---|---|
| 1 | Transferred this run: canonical `.id` lex route (Idol `tokenize()`) |

| # | directive |
|---|---|
| 1 | Boundaries still host-owned: source ingress (suffix/path classification) differential host lexer oracle (`tokenizeHost`) grammar roles (GAP-134 BLOCKED) parser recognition (src/parser.zig) binding/scope (src/sema.zig) semantic construction (src/semantic_graph.zig) relation/application resolution demand lowering/realization machine selection object emission |

| # | directive |
|---|---|
| 1 | ═══════════════════════════════════════════════════════════════════════ FTCFTW MEASUREMENT ═══════════════════════════════════════════════════════════════════════ |

| # | directive |
|---|---|
| 1 | Evidence status: INVALID for performance claims |

| # | directive |
|---|---|
| 1 | C comparison: INVALID |

| # | directive |
|---|---|
| 1 | No executable evidence chain |
| 2 | C backend emits broken C for closures (ret-type:any) |

| # | directive |
|---|---|
| 1 | WASM comparison: INVALID |

| # | directive |
|---|---|
| 1 | No correctness-locked comparison against pinned Wasmtime |
| 2 | perf ledger reports 19 missing contracts |

| # | directive |
|---|---|
| 1 | Startup/runtime/compile/memory/size: UNKNOWN |

| # | directive |
|---|---|
| 1 | Direct backend blocks all I/O programs |
| 2 | No valid measurement harness |

| # | directive |
|---|---|
| 1 | Uncertainty ledger (exact counts from source inspection): |

| # | directive |
|---|---|
| 1 | Boxing (lua_Value): ~1,271 emissions — VERY HIGH |
| 2 | Heap allocation: ~82 lua_table_new, ~120 malloc — VERY HIGH |
| 3 | Indirect calls (lua_invoke): ~97 emissions — HIGH |
| 4 | Guards: 6 kinds, 3 unwired — MEDIUM-HIGH |
| 5 | Tagging: ~174 tag, ~20 number_kind — MEDIUM |
| 6 | Runtime descriptors: hundreds of tables — MEDIUM |
| 7 | Materialized packs: dozens of sites — MEDIUM |
| 8 | Hashing: ~84 calc_lua_hash — LOW-MEDIUM |
| 9 | Copies: ~134 memcpy — LOW-MEDIUM |
| 10 | World objects: ~74 matches — LOW |
| 11 | Synchronization: ~63 matches — LOW |

| # | directive |
|---|---|
| 1 | Root cause: Knowledge lattice (semantic_algebra.KnowledgeLevel) .unknown/.observed/.guarded → boxing, allocation, indirect calls .stable but not .native → sealed but still boxed .dynamic StorageClass → all of the above + descriptors |

| # | directive |
|---|---|
| 1 | ═══════════════════════════════════════════════════════════════════════ GATE COVERAGE GAPS (adversarial) ═══════════════════════════════════════════════════════════════════════ |

| # | directive |
|---|---|
| 1 | Measured violations NOT caught by current gates: |

| # | directive |
|---|---|
| 1 | callable = true in table constructors: 12 violations |
| 2 | world["method"]() dispatch: 11 violations |
| 3 | String-indexed dispatch tables: 15+ violations |
| 4 | Mediator roles (registry, factory, broker): 16 violations |
| 5 | std.* semantic hops: 20 violations |

| # | directive |
|---|---|
| 1 | Total: 74 exact violations across 5 categories |

| # | directive |
|---|---|
| 1 | Positive controls verified: graph.get(exact_id): allowed json.object.get("field"): allowed runtime table actual string key: allowed exact application relation edge: allowed |

| # | directive |
|---|---|
| 1 | ═══════════════════════════════════════════════════════════════════════ CORPUS SAFETY AUDIT ═══════════════════════════════════════════════════════════════════════ |

| # | directive |
|---|---|
| 1 | Retired syntax: "req" found in scripts/test_lexer.id:2 "require" found in scripts/vscodeemit.id:10,11,86 All other retired syntax NOT found in active corpus |

| # | directive |
|---|---|
| 1 | Historical project names: "CATEGORY_DUO_CONTEXTUAL" in lib/token/classify.id:18 ".duo" in gate/idiom.id:286 (acceptable as gate control) "duo", "duon", "idsem" in docs/spec/*.md (historical documentation) |

| # | directive |
|---|---|
| 1 | Pass numbers: Extensive in docs/spec/*.md, NOT in lib/scripts/examples/gate |

| # | directive |
|---|---|
| 1 | Plural identities: NOT found as semantic identities |

| # | directive |
|---|---|
| 1 | *able identities: 12 violations (callable = true) |

| # | directive |
|---|---|
| 1 | Role nouns: 16 violations (registry, factory, broker, manager) |

| # | directive |
|---|---|
| 1 | Encoding namespace: 3 violations (emit_json_serializers, emit_protobuf_encode, emit_protobuf_decode) |

| # | directive |
|---|---|
| 1 | .get idiom: 2 caught, 11 NOT caught (string["match"]) |

| # | directive |
|---|---|
| 1 | [] indexing: 15+ violations (string-indexed dispatch tables) |

| # | directive |
|---|---|
| 1 | Semantic string matching: NOT found in lib/ (graph.id gate catches these) |

| # | directive |
|---|---|
| 1 | ═══════════════════════════════════════════════════════════════════════ GLOBAL ZERO TARGET AUDIT ═══════════════════════════════════════════════════════════════════════ |

| # | directive |
|---|---|
| 1 | ZERO violations (6 categories): semantic string lookup after resolution semantic path lookup after ingress parent-scope lookup after resolution operational graph edge kinds plural-cardinality identities inferable explicit `to`/projection/world plumbing |

| # | directive |
|---|---|
| 1 | Measured violations (5 categories): module/import semantic machinery: 4 *able/*ible identities: 12 role-noun protocol identities: 16 collision mediator systems: 16 generic encode/codec ontology: 3 |

| # | directive |
|---|---|
| 1 | Implementation-blocked (10 categories): known-shape generic hash: ~84 sealed-target indirect dispatch: ~97 nonescaping heap closures: ~1 singleton-union tags: ~174 unexplained allocations/copies/boxes: ~1,271 lua_Value, ~82 lua_table_new, ~120 malloc, ~134 memcpy silent host semantic fallback: 1 confirmed and more... |

| # | directive |
|---|---|
| 1 | ═══════════════════════════════════════════════════════════════════════ EVIDENCE CONFIDENCE ═══════════════════════════════════════════════════════════════════════ |

| # | directive |
|---|---|
| 1 | HIGH for: |

| # | directive |
|---|---|
| 1 | SHC frontier characterization (source code inspection, exact lines) |
| 2 | DNB011 failure boundary (minimal reproductions, exact error messages) |
| 3 | FTCFTW uncertainty ledger (exact file/line references, frequency counts) |
| 4 | Gate coverage gaps (exact violations, exact gate line references) |
| 5 | Corpus audit findings (exact file/line references) |
| 6 | Pre-commit hook operability (verified with test commit) |
| 7 | Ledger execution results (actual program output captured) |

| # | directive |
|---|---|
| 1 | LOW for: |

| # | directive |
|---|---|
| 1 | FTCFTW C/WASM performance claims (no valid comparison) |
| 2 | Positive damage control verification (harness cannot execute I/O programs) |
| 3 | Any claim of "100% SHC" or "FTCFTW proven" |

| # | directive |
|---|---|
| 1 | ═══════════════════════════════════════════════════════════════════════ NEXT SINGLE HIGHEST-LEVERAGE FRONTIER ═══════════════════════════════════════════════════════════════════════ |

| # | directive |
|---|---|
| 1 | GAP-145 lexical identity closure on the already-Idol `tokenize()` route. |
| 2 | Production dispatch no longer falls back to host `tokenizeHost()`; suffix/path ingress and distinct token-kind identities remain the earliest open facts. |

| # | directive |
|---|---|
| 1 | ═══════════════════════════════════════════════════════════════════════ |
