parse_expr: Res = (src: str, pos: i64)
    r = parse_expr(src, 0)
    while cont == 0
            return left
    if k == token.KIND_EQ
        left = "(| " .. left .. " " .. proj_bxor(lx) .. ")"
    left
proj_bxor: str = (lx: lexer)
        if t.kind == token.KIND_PLUS
        else
            return left
