| field | value |
|---|---|
| title | GAP-249 — native.id: 11th distinct variable clobbers x29 (FP); binary segfaults |
| status | OPEN |
| filed | 2026-09-12 |
| priority | P0 |
| kind | crash |
| files | lib/compiler/native.id |

| section |
|---|---|
| Crash |

| # | directive |
|---|---|
| 1 | A program using 11 or more distinct variable names compiles without error but the binary segfaults (exit 139 SIGSEGV; 13+ names: exit 138 SIGBUS). |
| 2 | 10 or fewer distinct names work correctly. Found 2026-09-12 while validating the adversarial benchmark corpus. |
| 3 | The clang/gcc oracle for the same program exits normally; this is a miscompile, not a semantics disagreement. |

| section |
|---|---|
| Reproducer |

| # | directive |
|---|---|
| 1 | 11 distinct names (i, v1..v9, t); expected exit 30, actual SIGSEGV: |

```text
i = 0
v1 = 1
v2 = 1
v3 = 1
v4 = 1
v5 = 1
v6 = 1
v7 = 1
v8 = 1
v9 = 1
t = 0
while i < 100
  v1 = v1 + 1
  t = t + v1
  v2 = v2 + 1
  t = t + v2
  v3 = v3 + 1
  t = t + v3
  v4 = v4 + 1
  t = t + v4
  v5 = v5 + 1
  t = t + v5
  v6 = v6 + 1
  t = t + v6
  v7 = v7 + 1
  t = t + v7
  v8 = v8 + 1
  t = t + v8
  v9 = v9 + 1
  t = t + v9
  i = i + 1
t
```

| section |
|---|---|
| Suspected mechanism |

| # | directive |
|---|---|
| 1 | reg(names, name) returns 19 + find(names, name) with no upper bound; the 11th name lands on x29 (frame pointer), the 12th on x30 (link register). |
| 2 | Variable spill exists only for deep expressions (deepexpr/spillpush/spillpop); plain variables have no spill path. |
| 3 | The compiler should refuse (>10 names) loudly or spill variables to the stack; silently emitting FP/LR-clobbering code is the worst option. |

| section |
|---|---|
| Suggested next step |

| # | directive |
|---|---|
| 1 | Decide: hard error past 10 names, or a variable spill slot allocator. |
| 2 | Until fixed, benchmark programs must use at most 10 distinct variable names; the adversarial corpus (bench/programs/adv*) was designed within this limit. |
