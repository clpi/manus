| field | value |
|---|---|
| title | GAP-095 — `runtime-global:*` is not ambient reach, and the native worklist has no idol-side rows |
| status | OPEN — the LAYERING claim is now demonstrated, not argued |
| filed | 2026-08-09 |

| section |
|---|---|
| 2026-08-09 — `runtime-global:string` went 0 -> 7, and nobody wrote a `string.` call |

| # | directive |
|---|---|
| 1 | The table below records `runtime-global:string` as **triaged 7, measured 0**, and explains it: the cause is real but sits UNDERNEATH another one, so a first-cause ranking cannot see it. |
| 2 | Re-swept at branch tip, that row now measures **7** — and they are the same seven programs, `examples/pass16_*_proof.id`. |

| # | directive |
|---|---|
| 1 | Nothing added a `string.` call. |
| 2 | What happened is that the barrier ABOVE them was removed: gap[086]'s top row, `global-decl-module-path` (12 programs), is now zero across the whole sweep, and the layer it was covering is the string reach. |

| # | directive |
|---|---|
| 1 | That is this file's central claim playing out in the tree rather than in an argument. |
| 2 | The number 7 was never wrong; it was never *visible*, and it became visible because someone fixed something else entirely. **A worklist ranked by first-reported cause does not just mis-size the work, it silently re-sorts itself whenever an unrelated repair lands** — which is worse, because the ranking looks stable right up until it is not. |

| # | directive |
|---|---|
| 1 | The three-program adjudication below is unchanged: `mem_raw_smoke` and `atomic_raw_smoke` still report `runtime-global:mem`, `pass12_m1_bench` still reports `runtime-global:os`, and none of the three is idol-side. |

| # | directive |
|---|---|
| 1 | *Original filing follows.* **Law:** §0e `std@{ ambient = false }` · §0b THE DESCENT IS DUO TO THE BYTE **Repair language:** every row below is `src/*.zig`. |
| 2 | Recorded, not patched. |

| section |
|---|---|
| The triage this corrects |

| # | directive |
|---|---|
| 1 | The native worklist was triaged as an AMBIENT REACH problem — eleven programs whose only barrier was touching `string.*` / `mem.*` / `os.*` / `io.*`, each removable by a value edge or a threaded world, and each also retiring a `capability-scan` site. |
| 2 | Measured at `1aa78fe`, sweeping every `examples/**.id` with `--backend=direct` and reading `hint: bail site:`: |

| triaged | measured | |
|---|---|---|
| `runtime-global:string` 7 | **0** | never a first bail cause |
| `runtime-global:mem` 2 | **2** | `mem_raw_smoke` · `atomic_raw_smoke` |
| `runtime-global:os` 1 | **1** | `pass12_m1_bench` |
| `runtime-global:io` 1 | **0** | does not appear |
| `keyed-table-export` 3 | **3** | idol-side in FORM, blocked behind a compiler row |

| # | directive |
|---|---|
| 1 | The cluster is **3 programs, not 11**, and none of the three is idol-side. |

| section |
|---|---|
| Why `runtime-global:string` read as 7 and measures 0 — BAIL CAUSES ARE LAYERED |

| # | directive |
|---|---|
| 1 | The precheck reports the FIRST refusal and stops. `pass12_m1_bench` reports `runtime-global:os`; neutralize `os.clock()` and the SAME program reports `runtime-global:string`; neutralize `string.format` and it reports `lowerBinop() dnir_lower.zig:2517 — concat`. |
| 2 | One program, three causes, one line of output. |

| # | directive |
|---|---|
| 1 | **Ranking a worklist by first-reported cause therefore counts neither programs nor work.** It counts which barrier happens to be checked first. |
| 2 | Any plan sized off that ranking is sized off an artifact of check order. |

| section |
|---|---|
| The three assigned programs, adjudicated |

| # | directive |
|---|---|
| 1 | **`examples/mem_raw_smoke.id`** — `runtime-global:mem`. |
| 2 | The reach is `mem.cast · mem.store(T)() · mem.load(T)() · mem.volatile_store/load · mem.add · mem.byte_add · mem.is_null · mem.sizeof · mem.ptr_from_addr · mem.copy · mem.move · mem.compare`. |
| 3 | The precheck admits exactly six: `alloc · free · read_byte · read_i64 · zero · addr` (`codegen.zig:4844`). |
| 4 | This is the RAW POINTER SURFACE, not an ambient capability — §0c's tier-0 substrate, and the fixture exists to prove it works. |
| 5 | Removing the reach deletes the subject. |
| 6 | Value under `--backend=c`: **100**, exit 0. |

| # | directive |
|---|---|
| 1 | **`examples/atomic_raw_smoke.id`** — same, plus the whole `atomic.*` family (`store · load · fetch_add/sub/or/and/xor · exchange · compare_exchange · fence · compiler_fence`), of which the precheck admits none. |
| 2 | Value under `--backend=c`: **93**, exit 0. |

| # | directive |
|---|---|
| 1 | Both need lowering arms in `dnir_lower.zig` paired with precheck rows in `codegen.zig` — the same paired edit gap[034] already names as the drift class. |

| # | directive |
|---|---|
| 1 | **`examples/pass12_m1_bench.id`** — `runtime-global:os`, and three more behind it (above). `os.clock` is a genuine clock capability, so the sanctioned repair is to thread a WORLD. **There is no world mechanism to thread.** `world` appears in `docs/spec/constitution.md` as vocabulary (`layers.std = { core, vocabulary, package, world }`, `effect{ "world.fs", .io }`) and in no implementation. `string.format("%.4f", t)` has no canonical idol replacement either: §0.8 mandates interpolation, §0c mandates shortest-round-trip float rendering, and floats measurably render 17 digits today. |

| section |
|---|---|
| `keyed-table-export` — idol-side in form, still not landable |

| # | directive |
|---|---|
| 1 | `examples/pass12_m1_diff.id` exports `NEG = { "foo", … }`, a positional STRING table at module scope, which `module_materializes_table` (`codegen.zig:3279`) correctly refuses; only an all-integer blob is exempt. |
| 2 | Moving it into `main` as a local is a legal `.id` edit and IT WORKS — the bail moves. |
| 3 | It moves to `gen-for-dynamic-iter`, because the program iterates it with `for w in neg`. |

| section |
|---|---|
| The rest of the worklist, so nobody re-derives it |

| # | directive |
|---|---|
| 1 | 61 bails at `1aa78fe`, every first cause: |

| cause | n | language |
|---|---|---|
| `gen-for-dynamic-iter` | 5 | **zig** — `codegen.zig:4090` refuses unconditionally; Pass 42 §1.2's `@iter` return-pack closure is the named unblock |
| `ret-type:inferred` | 4 | **zig** — closure-valued params (`use = (f, v) f(v)`) |
| `direct link — lua runtime` | 4 | **zig** |
| `method-unresolved:push` | 3 | **zig** — sequence protocol |
| `keyed-table-export` | 3 | idol form, zig behind it (above) |
| `lowerExprCons 2127` (`i64`/`str`) | 3 | **zig** |
| `lowerDynamicIndex 1821` | 3 | **zig** |
| `evalDnirValue — local 0 has no register` | 3 | **zig**, regalloc |
| `ret-type:any` | 2 | fixture subject IS the boxed path |
| `param-type:inferred` | 2 | **zig** — see below |
| `assign-value:nil` · `nil` literal | 4 | fixture subject IS nil-init |
| `method-unresolved:split` | 2 | **zig** |
| `loadStackField 2723` | 3 | **zig**, regalloc |
| `lowerExprCons 2155` (`neg`/`len`) · `lowerCall 3005` · `lowerBinop concat` · `lowerField 3206` · `evalDnirValueFp` | 9 | **zig** |
| `runtime-global:*` | 3 | adjudicated above |
| remainder (`assign-target` · `local-decl-typ` · `param-type:any` · `print-arg:any` · `ret-pack:i64\|error` · `codegen.zig:3405`) | 6 | conversion algebra, descriptor-as-value, and boxed-path fixtures |

| # | directive |
|---|---|
| 1 | **Zero rows are a idol-side annotation.** The two that look closest were probed and are not: |

| # | directive |
|---|---|
| 1 | `boring/binarysearch` · `boring/quicksort` — `param-type:inferred` on `xs`. Annotating `xs: [i64]` DOES clear the precheck (`.array` is admitted at `codegen.zig:4996`) and the program then fails one layer deeper with **DNB002 — function signature incompatible with direct ARM64 ABI** (`lowerModule() dnir_lower.zig:320`). The ABI cannot pass an array. A concurrent session reached the same wall from the other side and filed it as *"`seq(T)` parses on both sides of a binding and on neither side of a call"* — the sequence type is the root blocker, and no annotation routes around it. |
| 2 | `boring/fizzbuzz` — `print-arg:any`. Annotating `out: str = ""` lowers it natively AND SEGFAULTS. That is GAP-094, and it is a reason not to land the annotation, not a conversion. |

| # | directive |
|---|---|
| 1 | Two fixtures must never be "converted": `hash_agreement` states in its own method note that passing through an `any` parameter is what forces the compile-time-hash path the test measures, and `empty_table_smoke` exists to prove `{}` stays heterogeneous. |
| 2 | Removing the `any` from either deletes the control and leaves a green test that cannot fail (GAP-089's class). |

| section |
|---|---|
| A census finding, reported rather than acted on |

| # | directive |
|---|---|
| 1 | The unreachable rule excludes programs binding `std.compiler.*`, on the grounds that a C-emitter fixture is not a native-lowering target. `bench_simd_matmul` is the same thing under a different spelling — its body is a raw `@c.emit` C payload with `a_ptr = (void*)a` inside it — and it sits in the BAIL bucket. |
| 2 | Widening the exclusion would shrink the denominator without raising `native`, so it is left alone deliberately: a denominator edit that flatters a ratchet is the failure mode §3 exists for. |
| 3 | Recorded so the next reader knows it was seen. |
