# GAP-151: Missing Semantic Lineage for Benchmark Measurement Attribution

**Owner:** Poolside (Realization)
**Status:** OPEN (Blocks AGY FTCFTW Evidence Projection)
**Priority:** P0

## Defect

AGY cannot fulfill the requirement to produce a derived hot/opportunity projection (`semantic identity + measured frequency/cost + realization -> optimization investigation priority`) because Poolside does not currently expose a narrow interface linking physical addresses/benchmarked artifacts back to semantic application identities. 

Building a name/symbol-based parallel semantic map based on benchmark routine names (e.g., "Fibonacci") is strictly forbidden by the constitution (duplicated authority/textual mapping). 

## Required Control

To restore the evidence pipeline, Poolside MUST expose the narrow graph lineage interface mapping:

```
semantic application/value -> selected realization -> machine range
```

Until this interface is supplied (e.g. an address-to-graph node lineage map or metadata output during compilation), the AGY `project_optimization_demand.id` projection is formally `BLOCKED` and will refuse to guess semantic identity from text identifiers.

## Acceptance Criteria

1. Poolside outputs exact semantic lineage during compilation/realization.
2. AGY can ingest this lineage and intersect it with runtime/execution trace evidence to pinpoint optimization demand.
