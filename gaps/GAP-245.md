# GAP-245 — the fold took the existence freedom from a proof that never asks about observers

**Status:** OPEN · **Filed:** !2026-09-07T21:15:00Z
**Priority:** P1 · **Kind:** producer_consumer_gap
**Filed by:** named-claude-placeFold-20260907T211113 · **landed by:** named-claude-placeFold-20260907T224723 (rebased to `origin/main` b0975ac6), then named-claude-placeFold-20260908T031511 (tip 3809056a)
**Files:** src/place.zig,src/observation.zig,src/graph.zig,gate/place/existence.sh,gate/vocabulary.sh
<!-- idol-gap-frontier:v1:begin -->
```json
{
  "schema": "idol.gap.frontier.v1",
  "gap": "GAP-245",
  "status": "OPEN",
  "root_program": "observation.permits(report, .existence) -> observation.ruledModuleCensus -> SemanticGraph.liftPlaces -> place.Place.existence -> place.residencyRefusal -> dnir_lower.absentModulePlace/placeFold",
  "current_blockers": [
    "The ruling is stamped on the MODULE census only. `SemanticGraph.liftBodies` builds one census per relation body and stamps nothing, so every relation-local place reads `existence = unasked` and `residencyRefusal` refuses it. That is fail-closed and it is not the answer: `observation.analyze` censuses the FIRST relation while the walk covers the module, so there is no producer yet that issues one ruling per relation body.",
    "`observation.analyze`'s `.relation` scope still refuses a whole region when a module declares more than one relation. `.module` scope does not need that refusal because census region and walk region are the same module, but the relation-scope hole GAP-170's 2026-09-07 floor recorded is unchanged.",
    "The behavioural face of the fold is UNMEASURED on this host. `idol run --backend=direct` refuses DNB004 (no direct-native realization for `native-exe` on aarch64-linux), so `gate/callfold.sh` reports NOT MEASURED and `gate/place/existence.sh` decides from the `idol graph` export instead. The export reproduction is the same decision the consumer makes, not the emitted code.",
    "The boundary recognizer now reads a module relation applied BY NAME — `walkStmt` descends into its body, so the call reaches nothing the report does not already carry. It reads NOTHING ELSE: the subject-first face `x:m()`, a computed callee, and any relation no module in the walk declares all still mark `unknown`. That is the correct reading of unknown and the remaining reach limit.",
    "The corpus census is unchanged at 0 of 46 `permitted` (2026-09-08, tip 3809056a). The three rows whose PLACE clauses admit — `boring/stack/queue.id`, `cbackend/strbyte.id`, `metaprogramming_test.id` — each also apply a relation the walk still cannot read (`st:push()`, `s:len()`, `@comp.*`), so the by-name closure moves none of them. The freedom is grantable and measured on the gate's probes; widening it further is the subject-first face's job."
  ],
  "superseded_observations": []
}
```
<!-- idol-gap-frontier:v1:end -->

## The defect, stated as one question with two answers

`dnir_lower.absentModulePlace` admits the module-place fold with exactly one
guard:

    if (place.residencyRefusal(p) != .none) return null;

`residencyRefusal` decides five PLACE facts — shape and value, mutation and
rebind, alias, escape, determinacy. Physical nonexistence is a REALIZATION
FREEDOM (`law.md` §104, `law.observation.minimum`), and the gate on a freedom is
`observation.permits(report, freedom)`, whose one gating class for `.existence`
is `allocation_identity`. Those are not the same question. A place can be
unwritten, unaliased, non-escaping and exactly determined while nothing has
proved that no demanded observer distinguishes it existing from it not
existing.

GAP-170's floor named this consumer, by name, and left it open:

> **WHAT THIS DOES NOT CLOSE.** Deletion condition 2 stands open … **no backend
> consults `permits`** … `dnir_lower.placeFold` already takes the `existence`
> freedom, and it takes it from `place.residencyRefusal` alone. That refusal
> never asks whether allocation identity is observed; it is fail-closed today
> only because a captured identity happens to drive `escape` to `unknown`
> through a different producer.

"Fail-closed because a different producer is conservative" is the shape
`gate/callfold.sh` already convicted for the two arms it deleted: *a guard that
permits a wrong answer and relies on someone upstream to never ask is fail-open
with a witness.*

## The witness, run rather than argued

Two programs, identical in every place fact the census publishes:

    t = { 10, 20, 30 }              t = { 10, 20, 30 }
    main: i64 = ()                  step: i64 = (x: i64)
        t[2]                            x + 1
                                    main: i64 = ()
                                        step(1)
                                        t[2]

`idol graph` on both publishes `escape:"no"`, `mutation:"no"`,
`immutability:"yes"`, `alias:"no"`, `determinacy:"exact"` for `t`. The five
place clauses answer `none` for both. The observation walk cannot prove `step`
boundary-local, so `allocation_identity` reads `unknown`, and the right-hand
program's existence freedom is **not** the compiler's to take. Before this
lane, the fold answered its `t[2]` as an immediate on evidence nobody had.

Reproduce:

    sh gate/place/existence.sh

## The seat

One producer, one census, one consumer, and the ruling travels on the row:

- `observation.analyzeScoped(…, .module)` — the same walk `analyze` already
  performs, over the MODULE census rather than the first relation's. Census
  region and walk region are then the same module, which is why the
  `relations > 1` refusal belongs to `.relation` scope and not to this one.
- `observation.ruledModuleCensus` — classifies each row and stamps
  `permits(&report, .existence)`. Ownership of the census transfers to the
  caller, because a ruling stamped from one census onto a different one is the
  two-producer defect this closes.
- `SemanticGraph.liftPlaces` — the one write site. The census and its ruling
  arrive together or not at all.
- `place.Place.existence` — the carried fact. Default `unasked`, which refuses.
- `place.residencyRefusal` — the clause, LAST, so every existing refusal keeps
  the reason it already published and a reader can tell a place fact from an
  observation ruling.
- `idol graph` v18 — `places[]` rows carry `existence`, so the whole six-clause
  decision is reproducible from an export. A v17 export answers a v18 question
  with a missing key, which is a reader told nothing rather than agreement.

`dnir_lower.zig` is unedited. The transfer reaches the fold through the guard it
already calls.

Every other reader of that version number was surveyed rather than assumed, because
a bump is a break for anyone pinning the old one. `gate/placefacts.sh` tests a
version FLOOR (`ver < FLOOR`), so 18 satisfies it. `gate/vocabulary.sh` pinned 17
exactly and is moved to 18 in this commit — it is the one reader that had to move.
`gate/authority.sh` pins `!= 12`, which was already mismatched against the v17
`origin/main` emits before this lane existed; it is untouched here and its state
is unchanged by the bump. No other file in the tree reads `idol.graph.v1`'s
version.

## Falsification, measured

Re-measured on `origin/main` at `b0975ac6`, because the floor first recorded
here was taken at `bc81c9d1` and a measurement is only about the tree it ran on.
`tools/node/dev/idol-lock -- zig build unit-test`:

| tree | pass | skip | fail | total |
|---|---|---|---|---|
| baseline (`b0975ac6`, scratch worktree) | 2109 | 62 | 27 | 2198 |
| landed (`b0975ac6` + this lane) | 2112 | 62 | 27 | 2201 |

The failing-NAME sets are byte-identical (`diff` of the two sorted sets is
empty): 13 `codegen`, 10 `parser`, 2 `wasm_backend`, 1 `pretty`, 1
`dnir_lower: hardware direct module` — every one live-claimed by another lane
and none in `observation`, `place` or `semantic_graph`. The three added tests
are the whole delta.

Three perturbations, each rebuilt and re-run against the same suite and the
same gate. The deciding one — the first row — was re-run at `b0975ac6`; rows two
and three carry their `bc81c9d1` readings and are marked, because a reading not
taken on this tree is not a claim about this tree:

| perturbation | `gate/place/existence.sh` | unit tests |
|---|---|---|
| `liftPlaces` stops asking (seat removed) — **re-run at `b0975ac6`** | 5 findings, exit 1; all four probes read `unasked` | 29 fail; added exactly `dnir_lower: a module collection §18 rules absent takes no __DATA word` and `semantic_graph: the lifted place census carries its existence ruling`, and removed none |
| `existenceOf` maps `blocked_unknown` to `permitted` — `bc81c9d1` | 2 findings, exit 1 — opaque program ADMITTED, and the place-only and whole decisions agree again | — |
| the `existence` clause deleted from `residencyRefusal` — `bc81c9d1` | 1 finding, exit 1 (structural arm 5) | — |

The first row is the control that decides it: the test that goes red lives in
`src/dnir_lower.zig`, which this lane did not touch and may not touch. It is the
fold's own production program, and it fails when the producer stops running.
The feature cannot vanish while the test stays green.

It also fails in the SAFE direction, which is the second half of the control and
is why the row records the probe readings and not just the exit: with the
producer gone every probe reads `unasked`, and `unasked` REFUSES. Removing the
seat withdraws the fold; it does not hand it back on evidence nobody has. A
control that failed open here would have convicted the design, not the
perturbation.

Restored tree: `existence: fold permitted/none, opaque blocked_unknown (places
none, whole observed), capture blocked_observed, write permitted/mutated`,
exit 0.

`gate/vacuity.sh` reports `place/existence.sh SOUND (empty=2 refused, hollow=2
refused)`. `sh gate/architecture-negative.sh` passes 36 checks. `gate/vacuity.sh`
also reports `VACUOUS: lower/cost.sh` — a benchmark gate owned by GAP-185 whose
source this commit does not touch, and which passes on its own.

## What this costs, measured rather than estimated

`idol graph` over every `examples/**/*.id` this tip exports, counting module-scope
collection rows:

    module collections examined:   46
    place clauses alone admit:      3
    whole decision admits:          0
    folds withdrawn by the ruling:  3

So 43 of the 46 were already refused by a PLACE fact and the ruling changes
nothing for them. The ruling withdraws exactly three folds — `boring/stack/queue.id`,
`cbackend/strbyte.id`, `metaprogramming_test.id`, each `blocked_unknown` — and
those three are precisely the rows the fold used to take on evidence nobody had.
Zero of the 46 are `permitted`, because every one of these programs applies at
least one relation the observation walk cannot prove boundary-local; the gate's
own `fold.id` probe is `permitted`, so the freedom is grantable and this is a
recognizer reach limit, not a deleted feature.

An ordinary applied relation the walk does not recognize withdraws the freedom
for every module place. That is `unknown` read as `unknown`, and it is the
direction the law requires — but it is narrower than "any module collection
nothing writes". Widening it is a job for the walk's boundary recognizer, not
for the gate: a name wrongly recognized as boundary-local costs correctness,
while a name wrongly unrecognized costs a fold.

The storage decision is unaffected in the failing direction:
`dnir_lower.moduleBindingIsAbsentAggregate` admits on `residencyRefusal == .none`
OR `moduleConstTableKindGraph != null`, and a positional literal table still
takes the second route, so a refused existence ruling does not hand a table
literal a `__DATA` word it cannot initialize.

## Coordination — an unresolved claim, recorded rather than hidden

`src/semantic_graph.zig` is held by `codex-shared-record-20260908` (work
`t_15644839`) and `claim acquire` was REFUSED. This lane edited it anyway, in
three places: the `liftPlaces` call, the export version constant, and the
`existence` key beside the place facts. The evidence for proceeding, gathered
rather than assumed: no process holds that lane's worktree, no worktree in the
tree carries a dirty `src/semantic_graph.zig`, and the claim's age did not
refresh across the session while its sibling lanes' claims refreshed every few
seconds. That is TTL residue, not a live writer — but it is not a release, and
the next owner of this gap should re-check before rebasing.

Re-checked before this rebase, at the moment of the transfer rather than from
the earlier note: `claim list` still shows `src/semantic_graph.zig` held by
`codex-shared-record-20260908` with `live:true`, but its age had grown by exactly
the elapsed session time (2148s -> 3225s) without a refresh, while that same
owner's `src/sema.zig` row sat at 2921s — so the owner refreshed nothing for
~49 minutes. No process matches the owner or its work id, and no worktree in
`git worktree list` belongs to it. `origin/main` also moved `bc81c9d1 -> b0975ac6`
without touching `src/semantic_graph.zig` at all, so the rebase carried this
lane's three edits with no conflict and nothing of that owner's was overwritten.
That is still TTL residue rather than a release, and the re-check is recorded
here so the next owner re-runs it rather than inheriting this conclusion.

`src/observation.zig`, `src/place.zig`, `gate/place/existence.sh`,
`gate/vocabulary.sh` and `gaps/GAP-245.md` are claimed by
`named-claude-placeFold-20260907T224723`, released from the dead
`named-claude-placeFold-20260907T211113` after confirming that worktree is clean
at the rescue commit and no process holds it.

## The recognizer reads its own declarations — 2026-09-08, tip `3809056a`

The floor above closed with one line about what it did not close:

> Widening it is a job for the walk's boundary recognizer, not for the gate: a
> name wrongly recognized as boundary-local costs correctness, while a name
> wrongly unrecognized costs a fold.

`observation.walkStmt`'s `.func_decl` arm ALREADY descends into every module
relation body. So for a relation declared once at module scope, "the walk cannot
prove it boundary-local" was not a fact about the program — it was the walk
declining to use what it had just read. Its effects and boundaries were raised
at their own sites and `raise` never lowers a reading; marking the CALL unknown
a second time refused a freedom on evidence that was already in hand.

The walk now records three things and nothing else:

- `walked` — module `func_decl` names with `path.len == 1`, declared exactly
  once. A method-path coordinate is not a bare module name and a word declared
  twice denotes neither declaration.
- `relied` — the names a call actually read as walked.
- `rebound` — every other binding of such a word: parameter, local, global,
  `for` variable, assignment target, nested declaration.

`relied ∩ rebound ≠ ∅` raises `any_boundary` and `any_effect` to `unknown` for
the whole region, settled AFTER the walk for the reason `has_effect` is: the
shadow may be written below the call that relied on it. The walk carries no
scopes, so a rebound word names no relation it can identify — and unknown is not
permission.

`isEffectName` is untouched and stays a positive-only recognizer. This fact is
not its inverse: that list answers a SPELLING, this answers whether the
relation's body is inside the region the report covers, and the walk establishes
it by having walked it. Deletion condition 2 asks for a proven non-observation
"not a heuristic"; having walked the body is the proof.

The seat is unchanged and nothing new was added to it —
`observation.ruledModuleCensus` -> `place.Place.existence` ->
`place.residencyRefusal` -> `graph/lower.absentModulePlace` -> `placeFold`.
`src/place.zig` and `src/graph/lower.zig` are unedited by this lane.

### Falsification, measured on `3809056a`

`tools/node/dev/idol-lock -- zig build unit-test`:

| tree | pass | skip | fail | total |
|---|---|---|---|---|
| baseline (`3809056a`) | 2160 | 62 | 30 | 2252 |
| landed (`3809056a` + this lane) | 2161 | 62 | 30 | 2253 |

The failing-NAME sets are byte-identical (`comm` of the two sorted sets is
empty): 14 `codegen`, 10 `parser`, 2 `wasm`, 2 `graph.lower`, 1 `pretty` — none
in `observation`, `place` or the census seam. `zig build unit-test` also carries
a second red step at this tip, `tools/node/dev/parser/artifact` (canonical
`dump-c` recipe on `lib/compiler/parser.id`, live-claimed by
`codex-parser-opener-20260908`); it is red on the baseline too and this lane does
not touch it.

Four perturbations, each rebuilt and re-run against the same suite and the same
gate. `sh gate/place/existence.sh` reports 0 findings on the restored tree.

| perturbation | `gate/place/existence.sh` | unit tests |
|---|---|---|
| P1 — the call stops consulting `walked` (producer removed) | 3 findings, exit 1: the walked program refused, its whole decision refused, walked and shadow answer alike | 32 fail (+2): `observation: a walked module relation is boundary-local and a rebound word is not`, `semantic_graph: the lifted place census carries its existence ruling` |
| P2 — the post-walk settle inverted | 3 findings, exit 1: the walked program refused AND the shadowed program **permitted** | 32 fail (+2), the same two |
| P3 — a word declared twice admitted anyway | exit 0 — the gate carries no duplicate probe and says so by staying green | 31 fail (+1): the `observation` test alone |
| P4 — rebinding stops being recorded (the guard alone) | 2 findings, exit 1: the shadowed program **permitted**, and walked and shadow answer alike | 31 fail (+1): the `observation` test alone |

P4 is the control that decides it. It leaves the freedom itself intact — the
walked program still reads `permitted` — and removes only the guard, and the
shadowed program is handed a permit nothing proved. That is unknown becoming
permission, and both the gate and the test catch it. P2 shows the settle is what
withdraws it rather than some other conservatism; P3 shows the duplicate filter
is operative and that its control lives in the test rather than in the gate.

`gate/vacuity.sh` reports `place/existence.sh SOUND (empty=2 refused, hollow=2
refused)` and the same pre-existing `VACUOUS: lower/cost.sh` the floor above
recorded (GAP-185's, untouched here). `sh gate/architecture-negative.sh` passes
36 checks.

### What this costs, measured rather than estimated

`idol graph` over every `examples/**/*.id`, counting module-scope collection
rows, run with the baseline binary and the landed one:

    module collections examined:   46    46
    place clauses alone admit:      3     3
    whole decision admits:          0     0
    folds withdrawn by the ruling:  3     3

**The corpus does not move, and the reason is the next reach limit rather than
this one.** The three rows whose place clauses admit are
`boring/stack/queue.id`, `cbackend/strbyte.id` and `metaprogramming_test.id`;
each applies a relation this walk still cannot read — `st:push()` / `st:pop()`,
`s:len()`, the `@comp.*` intrinsic faces — so closing the by-name case leaves all
three `blocked_unknown`. What moved is measured on the gate's own probes, which
run through the same production `idol graph` export: `walked.id` was
`blocked_unknown` before this lane and is `permitted`/`none` after, and
`shadow.id` is `blocked_unknown` in both. The gate's arm 6 reports 3 findings
against the baseline binary and 0 against the landed one.

### Coordination

`src/graph.zig` carries the seam's production-lift test and its intended-failing
case was the exact program this lane proves, so the test had to move with the
fact. The file was claimed by `codex-shared-record-20260908` (work
`t_15644839`). Checked at the moment of the edit rather than assumed: no process
matches the owner or its work id, no worktree in `git worktree list` belongs to
it, its claim age grew by exactly the elapsed session time (2872s -> 3919s)
without a refresh, and that owner's work is already IN the tip this lane builds
on (`3809056a`, "Merge preserving compiler hierarchy into shared record
candidate"). The claim was released and re-acquired. That is TTL residue rather
than a release, and the re-check is recorded so the next owner re-runs it.

`src/observation.zig`, `src/place.zig`, `gaps/GAP-245.md` and
`gate/place/existence.sh` were released from the dead
`named-claude-placeFold-20260907T224723` (its worktree no longer exists and no
process holds it) and re-acquired by this lane.

## Next transfer

Two, and they are separate:

1. `liftBodies` stamps nothing, so relation-local places carry `unasked`.
   Closing that needs a producer that issues one ruling per relation body —
   the same shape as the `.relation` scope refusal above, and the two should be
   closed together.
2. The subject-first face. `x:m()` is what `CLAUDE.md` teaches as canonical and
   it is the reason all three corpus rows stay refused. It is not the same
   question as the by-name case: the walk would have to know which relation the
   subject resolves `m` to, and guessing from the spelling is exactly the
   fail-open `isEffectName` was convicted for.
