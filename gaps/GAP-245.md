# GAP-245 — the fold took the existence freedom from a proof that never asks about observers

**Status:** OPEN · **Filed:** !2026-09-07T21:15:00Z
**Priority:** P1 · **Kind:** producer_consumer_gap
**Filed by:** named-claude-placeFold-20260907T211113 · **landed by:** named-claude-placeFold-20260907T224723 (rebased to `origin/main` b0975ac6), then named-claude-placeFold-20260908T031511 (tip 3809056a, then tip ffb9a454)
**Files:** src/place.zig,src/observation.zig,src/graph.zig,gate/place/existence.sh,gate/vocabulary.sh
<!-- idol-gap-frontier:v1:begin -->
```json
{
  "schema": "idol.gap.frontier.v1",
  "gap": "GAP-245",
  "status": "OPEN",
  "root_program": "observation.permits(report, .existence) -> observation.ruledModuleCensus -> SemanticGraph.liftPlaces -> place.Place.existence -> place.residencyRefusal -> dnir_lower.absentModulePlace/placeFold",
  "current_blockers": [
    "The ruling is stamped on the MODULE census only. `SemanticGraph.liftBodies` builds one census per relation body and stamps nothing, so every relation-local place reads `existence = unasked` and `residencyRefusal` refuses it. That is fail-closed and it is not the answer: `observation.analyze` censuses the FIRST relation while the walk covers the module, so there is no producer yet that issues one ruling per relation body.",
    "`observation.analyze`'s `.relation` scope still refuses a whole region when a module declares more than one relation. `.module` scope does not need that refusal because census region and walk region are the same module, but the relation-scope hole GAP-170's 2026-09-07 floor recorded is unchanged.",
    "The behavioural face of the fold is UNMEASURED on this host. `idol run --backend=direct` refuses DNB004 (no direct-native realization for `native-exe` on aarch64-linux), so `gate/callfold.sh` reports NOT MEASURED and `gate/place/existence.sh` decides from the `idol graph` export instead. The export reproduction is the same decision the consumer makes, not the emitted code.",
    "The boundary recognizer reads a module relation the walk descended into, through BOTH faces — `step(n)` and `n:step()`. It reads NOTHING ELSE: an intrinsic relation no module declares (`st:push()`, `s:len()`), a computed callee, and the `@comp.*` faces all still mark `unknown`. That is the correct reading of unknown and the remaining reach limit.",
    "The corpus census is unchanged at 0 of 46 `permitted` (2026-09-08, tip ffb9a454). The three rows whose PLACE clauses admit — `boring/stack/queue.id`, `cbackend/strbyte.id`, `metaprogramming_test.id` — each apply an INTRINSIC relation, which the graph's own export publishes as an unresolved application with no relation: 4 method sites in `queue.id`, 1 (`s:len()`) in `strbyte.id`, ~85 `@comp.*` sites in `metaprogramming_test.id`. Closing the face moved none of them, and `strbyte.id` is now measured at exactly one relation from admitting.",
    "`SemanticGraph.unobservableStringFace` already rules `x:len()` and `x:byte(i)` unobservable, and `applicationIsUnobservableStringFace` is its public query face — but `observation.analyzeScoped` walks the AST and cannot reach it, so the walk contradicts a ruling the graph has already made. Its source comment cites `gate/collapse.sh` as the pin; NO SUCH FILE EXISTS in this tree (`find gate -name '*collapse*'` is empty), so the claimed evidence cannot be read from where it points."
  ],
  "superseded_observations": []
}
```
<!-- idol-gap-frontier:v1:end -->

## The defect, stated as one question with two answers

| # | directive |
|---|---|
| 1 | `dnir_lower.absentModulePlace` admits the module-place fold with exactly one guard: |

| # | directive |
|---|---|
| 1 | if (place.residencyRefusal(p) != .none) return null |

| # | directive |
|---|---|
| 1 | `residencyRefusal` decides five PLACE facts — shape and value, mutation and rebind, alias, escape, determinacy. |
| 2 | Physical nonexistence is a REALIZATION FREEDOM (`law.md` §104, `law.observation.minimum`), and the gate on a freedom is `observation.permits(report, freedom)`, whose one gating class for `.existence` is `allocation_identity`. |
| 3 | Those are not the same question. |
| 4 | A place can be unwritten, unaliased, non-escaping and exactly determined while nothing has proved that no demanded observer distinguishes it existing from it not existing. |

| # | directive |
|---|---|
| 1 | GAP-170's floor named this consumer, by name, and left it open: |

> **WHAT THIS DOES NOT CLOSE.** Deletion condition 2 stands open … **no backend
> consults `permits`** … `dnir_lower.placeFold` already takes the `existence`
> freedom, and it takes it from `place.residencyRefusal` alone. That refusal
> never asks whether allocation identity is observed; it is fail-closed today
> only because a captured identity happens to drive `escape` to `unknown`
> through a different producer.

| # | directive |
|---|---|
| 1 | "Fail-closed because a different producer is conservative" is the shape `gate/callfold.sh` already convicted for the two arms it deleted: *a guard that permits a wrong answer and relies on someone upstream to never ask is fail-open with a witness.* |

## The witness, run rather than argued

| # | directive |
|---|---|
| 1 | Two programs, identical in every place fact the census publishes: |

| # | directive |
|---|---|
| 1 | t = { 10, 20, 30 } t = { 10, 20, 30 } main: i64 = () step: i64 = (x: i64) t[2] x + 1 main: i64 = () step(1) t[2] |

| # | directive |
|---|---|
| 1 | `idol graph` on both publishes `escape:"no"`, `mutation:"no"`, `immutability:"yes"`, `alias:"no"`, `determinacy:"exact"` for `t`. |
| 2 | The five place clauses answer `none` for both. |
| 3 | The observation walk cannot prove `step` boundary-local, so `allocation_identity` reads `unknown`, and the right-hand program's existence freedom is **not** the compiler's to take. |
| 4 | Before this lane, the fold answered its `t[2]` as an immediate on evidence nobody had. |

| # | directive |
|---|---|
| 1 | Reproduce: |

| # | directive |
|---|---|
| 1 | sh gate/place/existence.sh |

## The seat

| # | directive |
|---|---|
| 1 | One producer, one census, one consumer, and the ruling travels on the row: |

- `observation.analyzeScoped(…, .module)` — the same walk `analyze` already
  performs, over the MODULE census rather than the first relation's. Census
  region and walk region are then the same module, which is why the
  `relations > 1` refusal belongs to `.relation` scope and not to this one.
- `observation.ruledModuleCensus` — classifies each row and stamps
  `permits(&report, .existence)`. Ownership of the census transfers to the
  caller, because a ruling stamped from one census onto a different one is the
  two-producer defect this closes.
- `SemanticGraph.liftPlaces` — the one write site. The census and its ruling
  arrive together or not at all.
- `place.Place.existence` — the carried fact. Default `unasked`, which refuses.
- `place.residencyRefusal` — the clause, LAST, so every existing refusal keeps
  the reason it already published and a reader can tell a place fact from an
  observation ruling.
- `idol graph` v18 — `places[]` rows carry `existence`, so the whole six-clause
  decision is reproducible from an export. A v17 export answers a v18 question
  with a missing key, which is a reader told nothing rather than agreement.

| # | directive |
|---|---|
| 1 | `dnir_lower.zig` is unedited. |
| 2 | The transfer reaches the fold through the guard it already calls. |

| # | directive |
|---|---|
| 1 | Every other reader of that version number was surveyed rather than assumed, because a bump is a break for anyone pinning the old one. `gate/placefacts.sh` tests a version FLOOR (`ver < FLOOR`), so 18 satisfies it. `gate/vocabulary.sh` pinned 17 exactly and is moved to 18 in this commit — it is the one reader that had to move. `gate/authority.sh` pins `!= 12`, which was already mismatched against the v17 `origin/main` emits before this lane existed; it is untouched here and its state is unchanged by the bump. |
| 2 | No other file in the tree reads `idol.graph.v1`'s version. |

## Falsification, measured

| # | directive |
|---|---|
| 1 | Re-measured on `origin/main` at `b0975ac6`, because the floor first recorded here was taken at `bc81c9d1` and a measurement is only about the tree it ran on. `tools/node/dev/idol-lock -- zig build unit-test`: |

| tree | pass | skip | fail | total |
|---|---|---|---|---|
| baseline (`b0975ac6`, scratch worktree) | 2109 | 62 | 27 | 2198 |
| landed (`b0975ac6` + this lane) | 2112 | 62 | 27 | 2201 |

| # | directive |
|---|---|
| 1 | The failing-NAME sets are byte-identical (`diff` of the two sorted sets is empty): 13 `codegen`, 10 `parser`, 2 `wasm_backend`, 1 `pretty`, 1 `dnir_lower: hardware direct module` — every one live-claimed by another lane and none in `observation`, `place` or `semantic_graph`. |
| 2 | The three added tests are the whole delta. |

| # | directive |
|---|---|
| 1 | Three perturbations, each rebuilt and re-run against the same suite and the same gate. |
| 2 | The deciding one — the first row — was re-run at `b0975ac6`; rows two and three carry their `bc81c9d1` readings and are marked, because a reading not taken on this tree is not a claim about this tree: |

| perturbation | `gate/place/existence.sh` | unit tests |
|---|---|---|
| `liftPlaces` stops asking (seat removed) — **re-run at `b0975ac6`** | 5 findings, exit 1; all four probes read `unasked` | 29 fail; added exactly `dnir_lower: a module collection §18 rules absent takes no __DATA word` and `semantic_graph: the lifted place census carries its existence ruling`, and removed none |
| `existenceOf` maps `blocked_unknown` to `permitted` — `bc81c9d1` | 2 findings, exit 1 — opaque program ADMITTED, and the place-only and whole decisions agree again | — |
| the `existence` clause deleted from `residencyRefusal` — `bc81c9d1` | 1 finding, exit 1 (structural arm 5) | — |

| # | directive |
|---|---|
| 1 | The first row is the control that decides it: the test that goes red lives in `src/dnir_lower.zig`, which this lane did not touch and may not touch. |
| 2 | It is the fold's own production program, and it fails when the producer stops running. |
| 3 | The feature cannot vanish while the test stays green. |

| # | directive |
|---|---|
| 1 | It also fails in the SAFE direction, which is the second half of the control and is why the row records the probe readings and not just the exit: with the producer gone every probe reads `unasked`, and `unasked` REFUSES. |
| 2 | Removing the seat withdraws the fold; it does not hand it back on evidence nobody has. |
| 3 | A control that failed open here would have convicted the design, not the perturbation. |

| # | directive |
|---|---|
| 1 | Restored tree: `existence: fold permitted/none, opaque blocked_unknown (places none, whole observed), capture blocked_observed, write permitted/mutated`, exit 0. |

| # | directive |
|---|---|
| 1 | `gate/vacuity.sh` reports `place/existence.sh SOUND (empty=2 refused, hollow=2 refused)`. `sh gate/architecture-negative.sh` passes 36 checks. `gate/vacuity.sh` also reports `VACUOUS: lower/cost.sh` — a benchmark gate owned by GAP-185 whose source this commit does not touch, and which passes on its own. |

## What this costs, measured rather than estimated

| # | directive |
|---|---|
| 1 | `idol graph` over every `examples/**/*.id` this tip exports, counting module-scope collection rows: |

| # | directive |
|---|---|
| 1 | module collections examined: 46 place clauses alone admit: 3 whole decision admits: 0 folds withdrawn by the ruling: 3 |

| # | directive |
|---|---|
| 1 | So 43 of the 46 were already refused by a PLACE fact and the ruling changes nothing for them. |
| 2 | The ruling withdraws exactly three folds — `boring/stack/queue.id`, `cbackend/strbyte.id`, `metaprogramming_test.id`, each `blocked_unknown` — and those three are precisely the rows the fold used to take on evidence nobody had. |
| 3 | Zero of the 46 are `permitted`, because every one of these programs applies at least one relation the observation walk cannot prove boundary-local; the gate's own `fold.id` probe is `permitted`, so the freedom is grantable and this is a recognizer reach limit, not a deleted feature. |

| # | directive |
|---|---|
| 1 | An ordinary applied relation the walk does not recognize withdraws the freedom for every module place. |
| 2 | That is `unknown` read as `unknown`, and it is the direction the law requires — but it is narrower than "any module collection nothing writes". |
| 3 | Widening it is a job for the walk's boundary recognizer, not for the gate: a name wrongly recognized as boundary-local costs correctness, while a name wrongly unrecognized costs a fold. |

| # | directive |
|---|---|
| 1 | The storage decision is unaffected in the failing direction: `dnir_lower.moduleBindingIsAbsentAggregate` admits on `residencyRefusal == .none` OR `moduleConstTableKindGraph != null`, and a positional literal table still takes the second route, so a refused existence ruling does not hand a table literal a `__DATA` word it cannot initialize. |

## Coordination — an unresolved claim, recorded rather than hidden

| # | directive |
|---|---|
| 1 | `src/semantic_graph.zig` is held by `codex-shared-record-20260908` (work `t_15644839`) and `claim acquire` was REFUSED. |
| 2 | This lane edited it anyway, in three places: the `liftPlaces` call, the export version constant, and the `existence` key beside the place facts. |
| 3 | The evidence for proceeding, gathered rather than assumed: no process holds that lane's worktree, no worktree in the tree carries a dirty `src/semantic_graph.zig`, and the claim's age did not refresh across the session while its sibling lanes' claims refreshed every few seconds. |
| 4 | That is TTL residue, not a live writer — but it is not a release, and the next owner of this gap should re-check before rebasing. |

| # | directive |
|---|---|
| 1 | Re-checked before this rebase, at the moment of the transfer rather than from the earlier note: `claim list` still shows `src/semantic_graph.zig` held by `codex-shared-record-20260908` with `live:true`, but its age had grown by exactly the elapsed session time (2148s -> 3225s) without a refresh, while that same owner's `src/sema.zig` row sat at 2921s — so the owner refreshed nothing for ~49 minutes. |
| 2 | No process matches the owner or its work id, and no worktree in `git worktree list` belongs to it. `origin/main` also moved `bc81c9d1 -> b0975ac6` without touching `src/semantic_graph.zig` at all, so the rebase carried this lane's three edits with no conflict and nothing of that owner's was overwritten. |
| 3 | That is still TTL residue rather than a release, and the re-check is recorded here so the next owner re-runs it rather than inheriting this conclusion. |

| # | directive |
|---|---|
| 1 | `src/observation.zig`, `src/place.zig`, `gate/place/existence.sh`, `gate/vocabulary.sh` and `gaps/GAP-245.md` are claimed by `named-claude-placeFold-20260907T224723`, released from the dead `named-claude-placeFold-20260907T211113` after confirming that worktree is clean at the rescue commit and no process holds it. |

## The recognizer reads its own declarations — 2026-09-08, tip `3809056a`

| # | directive |
|---|---|
| 1 | The floor above closed with one line about what it did not close: |

> Widening it is a job for the walk's boundary recognizer, not for the gate: a
> name wrongly recognized as boundary-local costs correctness, while a name
> wrongly unrecognized costs a fold.

| # | directive |
|---|---|
| 1 | `observation.walkStmt`'s `.func_decl` arm ALREADY descends into every module relation body. |
| 2 | So for a relation declared once at module scope, "the walk cannot prove it boundary-local" was not a fact about the program — it was the walk declining to use what it had just read. |
| 3 | Its effects and boundaries were raised at their own sites and `raise` never lowers a reading; marking the CALL unknown a second time refused a freedom on evidence that was already in hand. |

| # | directive |
|---|---|
| 1 | The walk now records three things and nothing else: |

- `walked` — module `func_decl` names with `path.len == 1`, declared exactly
  once. A method-path coordinate is not a bare module name and a word declared
  twice denotes neither declaration.
- `relied` — the names a call actually read as walked.
- `rebound` — every other binding of such a word: parameter, local, global,
  `for` variable, assignment target, nested declaration.

| # | directive |
|---|---|
| 1 | `relied ∩ rebound ≠ ∅` raises `any_boundary` and `any_effect` to `unknown` for the whole region, settled AFTER the walk for the reason `has_effect` is: the shadow may be written below the call that relied on it. |
| 2 | The walk carries no scopes, so a rebound word names no relation it can identify — and unknown is not permission. |

| # | directive |
|---|---|
| 1 | `isEffectName` is untouched and stays a positive-only recognizer. |
| 2 | This fact is not its inverse: that list answers a SPELLING, this answers whether the relation's body is inside the region the report covers, and the walk establishes it by having walked it. |
| 3 | Deletion condition 2 asks for a proven non-observation "not a heuristic"; having walked the body is the proof. |

| # | directive |
|---|---|
| 1 | The seat is unchanged and nothing new was added to it — `observation.ruledModuleCensus` -> `place.Place.existence` -> `place.residencyRefusal` -> `graph/lower.absentModulePlace` -> `placeFold`. `src/place.zig` and `src/graph/lower.zig` are unedited by this lane. |

### Falsification, measured on `3809056a`

| # | directive |
|---|---|
| 1 | `tools/node/dev/idol-lock -- zig build unit-test`: |

| tree | pass | skip | fail | total |
|---|---|---|---|---|
| baseline (`3809056a`) | 2160 | 62 | 30 | 2252 |
| landed (`3809056a` + this lane) | 2161 | 62 | 30 | 2253 |

| # | directive |
|---|---|
| 1 | The failing-NAME sets are byte-identical (`comm` of the two sorted sets is empty): 14 `codegen`, 10 `parser`, 2 `wasm`, 2 `graph.lower`, 1 `pretty` — none in `observation`, `place` or the census seam. `zig build unit-test` also carries a second red step at this tip, `tools/node/dev/parser/artifact` (canonical `dump-c` recipe on `lib/compiler/parser.id`, live-claimed by `codex-parser-opener-20260908`); it is red on the baseline too and this lane does not touch it. |

| # | directive |
|---|---|
| 1 | Four perturbations, each rebuilt and re-run against the same suite and the same gate. `sh gate/place/existence.sh` reports 0 findings on the restored tree. |

| perturbation | `gate/place/existence.sh` | unit tests |
|---|---|---|
| P1 — the call stops consulting `walked` (producer removed) | 3 findings, exit 1: the walked program refused, its whole decision refused, walked and shadow answer alike | 32 fail (+2): `observation: a walked module relation is boundary-local and a rebound word is not`, `semantic_graph: the lifted place census carries its existence ruling` |
| P2 — the post-walk settle inverted | 3 findings, exit 1: the walked program refused AND the shadowed program **permitted** | 32 fail (+2), the same two |
| P3 — a word declared twice admitted anyway | exit 0 — the gate carries no duplicate probe and says so by staying green | 31 fail (+1): the `observation` test alone |
| P4 — rebinding stops being recorded (the guard alone) | 2 findings, exit 1: the shadowed program **permitted**, and walked and shadow answer alike | 31 fail (+1): the `observation` test alone |

| # | directive |
|---|---|
| 1 | P4 is the control that decides it. |
| 2 | It leaves the freedom itself intact — the walked program still reads `permitted` — and removes only the guard, and the shadowed program is handed a permit nothing proved. |
| 3 | That is unknown becoming permission, and both the gate and the test catch it. |
| 4 | P2 shows the settle is what withdraws it rather than some other conservatism |
| 5 | P3 shows the duplicate filter is operative and that its control lives in the test rather than in the gate. |

| # | directive |
|---|---|
| 1 | `gate/vacuity.sh` reports `place/existence.sh SOUND (empty=2 refused, hollow=2 refused)` and the same pre-existing `VACUOUS: lower/cost.sh` the floor above recorded (GAP-185's, untouched here). `sh gate/architecture-negative.sh` passes 36 checks. |

### What this costs, measured rather than estimated

| # | directive |
|---|---|
| 1 | `idol graph` over every `examples/**/*.id`, counting module-scope collection rows, run with the baseline binary and the landed one: |

| # | directive |
|---|---|
| 1 | module collections examined: 46 46 place clauses alone admit: 3 3 whole decision admits: 0 0 folds withdrawn by the ruling: 3 3 |

| # | directive |
|---|---|
| 1 | **The corpus does not move, and the reason is the next reach limit rather than this one.** The three rows whose place clauses admit are `boring/stack/queue.id`, `cbackend/strbyte.id` and `metaprogramming_test.id`; each applies a relation this walk still cannot read — `st:push()` / `st:pop()`, `s:len()`, the `@comp.*` intrinsic faces — so closing the by-name case leaves all three `blocked_unknown`. |
| 2 | What moved is measured on the gate's own probes, which run through the same production `idol graph` export: `walked.id` was `blocked_unknown` before this lane and is `permitted`/`none` after, and `shadow.id` is `blocked_unknown` in both. |
| 3 | The gate's arm 6 reports 3 findings against the baseline binary and 0 against the landed one. |

### Coordination

| # | directive |
|---|---|
| 1 | `src/graph.zig` carries the seam's production-lift test and its intended-failing case was the exact program this lane proves, so the test had to move with the fact. |
| 2 | The file was claimed by `codex-shared-record-20260908` (work `t_15644839`). |
| 3 | Checked at the moment of the edit rather than assumed: no process matches the owner or its work id, no worktree in `git worktree list` belongs to it, its claim age grew by exactly the elapsed session time (2872s -> 3919s) without a refresh, and that owner's work is already IN the tip this lane builds on (`3809056a`, "Merge preserving compiler hierarchy into shared record candidate"). |
| 4 | The claim was released and re-acquired. |
| 5 | That is TTL residue rather than a release, and the re-check is recorded so the next owner re-runs it. |

| # | directive |
|---|---|
| 1 | `src/observation.zig`, `src/place.zig`, `gaps/GAP-245.md` and `gate/place/existence.sh` were released from the dead `named-claude-placeFold-20260907T224723` (its worktree no longer exists and no process holds it) and re-acquired by this lane. |

## The face is not the relation — 2026-09-08, tip `ffb9a454`

| # | directive |
|---|---|
| 1 | The floor above closed by refusing its own next step: |

> The subject-first face. `x:m()` is what `CLAUDE.md` teaches as canonical and
> it is the reason all three corpus rows stay refused. It is not the same
> question as the by-name case: the walk would have to know which relation the
> subject resolves `m` to, and guessing from the spelling is exactly the
> fail-open `isEffectName` was convicted for.

| # | directive |
|---|---|
| 1 | The caution is right and the premise is wrong, and the difference is a measurement rather than an argument. |
| 2 | THE WALK DOES NOT HAVE TO GUESS — the production resolver already publishes the answer, and asking it costs one `idol graph` run per program: |

| # | directive |
|---|---|
| 1 | n:step() with `step` declared once at module scope applications[0] = { relation: 1, applied: { card: one, id: 1 } } call_shapes[0] = { callee_kind: "method", relation: 1 } |

| # | directive |
|---|---|
| 1 | st:push(1) with no module declaration of `push` applications = [] unresolved_applications = [3] call_shapes[0] = { callee_kind: "method", relation: null } |

| # | directive |
|---|---|
| 1 | s:len() with `len` declared once at module scope applications[0] = { relation: 1, applied: { card: one, id: 1 } } |

| # | directive |
|---|---|
| 1 | So the resolver's rule for the method face is the SAME rule `observation.analyzeScoped` already computes for the by-name face: a word declared once at module scope denotes that declaration, and a word no module declares resolves to nothing. `walked` is not a spelling list standing in for a resolution — it is that resolution, computed from the declarations the walk has in hand, and the third line above settles the one case that would have made it one: a module declaration of `len` wins over the intrinsic face even when the subject is a `str`. |

### The intended failing case

| # | directive |
|---|---|
| 1 | Two spellings of ONE application, every place fact identical, measured through the production export at `ffb9a454`: |

| program | `existence` |
|---|---|
| `step(n)` | `permitted` |
| `n:step()` | `blocked_unknown` |

| # | directive |
|---|---|
| 1 | The freedom was decided by which face the call was written with, and the face that lost it is the one `CLAUDE.md` teaches as canonical. `src/observation.zig` already convicts exactly this shape one arm above, about the clock: |

> A world fact is a property of the relation, never of the face it was written
> with.

| # | directive |
|---|---|
| 1 | The `.method_call` arm now consults `walked` where the `.call` arm does, in the same position — AFTER the effect and clock spellings, so a module that declares `print` does not buy `t:print()` a proof that nothing is observed. `relied` and `rebound` are unchanged and the post-walk settle is unchanged, so the withdrawal travels with the reading through both faces at once. |

| # | directive |
|---|---|
| 1 | Nothing else moved. `src/place.zig`, `src/graph/lower.zig`, `src/graph.zig`, `isEffectName` and the seat are unedited by this step; the diff is one `else if` branch and its controls. |

### Falsification, measured on `ffb9a454`

| # | directive |
|---|---|
| 1 | `tools/node/dev/idol-lock -- zig build unit-test`: |

| tree | pass | skip | fail | total |
|---|---|---|---|---|
| baseline (`ffb9a454`) | 2161 | 62 | 30 | 2253 |
| landed (`ffb9a454` + this step) | 2162 | 62 | 30 | 2254 |

| # | directive |
|---|---|
| 1 | The failing-NAME sets are byte-identical (`diff` of the two sorted 30-name sets is empty): 14 `codegen`, 10 `parser`, 3 `graph.lower`, 2 `wasm`, 1 `pretty` — none in `observation`, `place` or `graph`. |
| 2 | The one added test is the whole delta. |
| 3 | The second red step at this tip, `tools/node/dev/parser/artifact` (live-claimed by `codex-parser-opener-20260908`), is red on the baseline too and this step does not touch it. |

| # | directive |
|---|---|
| 1 | The gate reports **4 findings, exit 1** against the baseline binary and **0, exit 0** against the landed one, over 9 probes. |

| # | directive |
|---|---|
| 1 | Three perturbations, each rebuilt and re-run against the same suite and the same gate: |

| perturbation | `gate/place/existence.sh` | unit tests |
|---|---|---|
| P1 — the face stops consulting `walked` (producer removed) | 4 findings, exit 1: the two faces disagree, the subject-first program refused, and the declared and intrinsic relations answer alike | 31 fail (+1): the added `observation` test alone |
| P2 — the face recognizes EVERY method (the guard removed) | 4 findings, exit 1: the gate's own `stdin:read()` probe is **ADMITTED**, and `st:push(2)` with it | 37 fail (+6): 5 `observation`, 1 `graph` |
| P3 — recognized, but the reliance is not recorded | 2 findings, exit 1: the freedom is intact and the **shadowed** subject-first program is **permitted** | 31 fail (+1): the added `observation` test alone |

| # | directive |
|---|---|
| 1 | P2 and P3 are the two that decide it, and they fail in opposite directions. |
| 2 | P2 is the fail-open the floor above warned about, exhibited: reading the colon instead of the declaration re-admits `opaque.id` — the exact program this whole gap exists to refuse. |
| 3 | P3 leaves the freedom itself intact and removes only the withdrawal, and the shadowed program is handed a permit nothing proved. |
| 4 | P1 is the producer control: with the branch gone the two faces disagree again and the gate says so in the words of the disagreement. |

| # | directive |
|---|---|
| 1 | `gate/vacuity.sh` reports `place/existence.sh SOUND (empty=2 refused, hollow=2 refused)` and the same pre-existing `VACUOUS: lower/cost.sh` (GAP-185's, untouched here). `sh gate/architecture-negative.sh` passes 36 checks. |

### What this costs, measured rather than estimated

| # | directive |
|---|---|
| 1 | `idol graph` over every `examples/**/*.id`, counting module-scope collection rows, run with the baseline binary and the landed one: |

| # | directive |
|---|---|
| 1 | module collections examined: 46 46 place clauses alone admit: 3 3 whole decision admits: 0 0 folds withdrawn by the ruling: 3 3 |

| # | directive |
|---|---|
| 1 | **The corpus still does not move, and this time the reason is named exactly rather than by class.** The graph's own export says which sites the walk cannot read, so the residual is counted rather than described: |

| row | unresolved applications remaining |
|---|---|
| `boring/stack/queue.id` | 4 method sites — `st:push()` ×3, `st:pop()` (lines 6–9) |
| `cbackend/strbyte.id` | 1 method site — `s:len()` (line 47) |
| `metaprogramming_test.id` | ~85 direct/indirect `@comp.*` sites |

| # | directive |
|---|---|
| 1 | Every one of them is an INTRINSIC relation: the export publishes the site with `relation: null` because no module declares the word, so there is no body the walk could have read. |
| 2 | That is the same answer the walk gives, which is why closing the face moved nothing — the face was never what those three rows were blocked on. |

| # | directive |
|---|---|
| 1 | `strbyte.id` is now measured at exactly one relation from admitting. |
| 2 | Its two by-name relation calls (lines 53, 54) already resolve and are already read; replacing `s:len()` with a module-declared relation applied through the SAME subject-first face and re-running the export gives: |

| # | directive |
|---|---|
| 1 | baseline binary xs -> blocked_unknown landed binary xs -> permitted |

| # | directive |
|---|---|
| 1 | so this step is what put that row within reach, and `s:len()` is the whole of what remains between it and the fold. |

### Coordination

| # | directive |
|---|---|
| 1 | `src/observation.zig`, `src/graph.zig`, `gate/place/existence.sh` and `gaps/GAP-245.md` were re-acquired by this lane at the start of the step (`granted:true, reentrant:true` on all four). |
| 2 | This step edited only `src/observation.zig` and `gate/place/existence.sh`; `src/graph.zig` is claimed and unedited. |

| # | directive |
|---|---|
| 1 | A FLEET RESCUE COMMITTED THIS WORKTREE MID-PERTURBATION and the history records it: `2ec8da94 rescue(WIP): placeFold/GAP-245 subject-first probe` (authored `minimax GAP-232`, 05:02:55) snapshots the tree during P2, so that commit's `src/observation.zig` carries `else if (true)` — the fail-open arm, not the guard. |
| 2 | It was not reset; the restored working tree was verified byte-identical to the saved landed copies (`diff` empty for both files) and the transfer commit sits on top of it. |
| 3 | A reader bisecting through `2ec8da94` is reading a perturbation, not a proposal. |

## Next transfer

| # | directive |
|---|---|
| 1 | Three, and they are separate: |

1. `liftBodies` stamps nothing, so relation-local places carry `unasked`.
   Closing that needs a producer that issues one ruling per relation body —
   the same shape as the `.relation` scope refusal above, and the two should be
   closed together.
2. **The intrinsic relation, and it already has a producer.**
   `SemanticGraph.unobservableStringFace` rules `x:len()` (zero operands) and
   `x:byte(i)` (one) unobservable when the receiver is not a world handle and no
   module declares the name — which is `strbyte.id`'s exact remaining site —
   and `applicationIsUnobservableStringFace` is its public query face, consumed
   today only by `graph_query`. `observation.analyzeScoped` walks the AST and
   cannot reach it, so the walk contradicts a ruling the graph has already made.
   That is `law.fact.producer.one`, not a missing recognizer, and copying the
   predicate into `observation.zig` would be the second producer rather than the
   close. TWO THINGS MUST BE CHECKED FIRST, and neither is settled here: that
   comment cites `gate/collapse.sh` as what pins `len` and `byte` against the
   emitted code, and **no such file exists in this tree** (`find gate -name
   '*collapse*'` returns nothing), so the pin the ruling rests on cannot be read
   from where it points; and `st:push()` is a MUTATING sequence relation, which
   is a different question from an unobservable string face and must not be
   admitted by the same edge.
3. `@comp.*`. `metaprogramming_test.id` is ~85 unresolved sites and is not a
   boundary-recognizer question at all.
