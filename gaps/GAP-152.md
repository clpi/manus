# GAP-152 — Idol-only FTCFTW claim chain lacks aggregate pass

**Status:** OPEN · **Filed:** !2026-08-12T02:04:00Z
**Priority:** P0 · **Kind:** perf
**Owner:** Cursor (coordination / evidence)
**Files:** scripts/ftcftwledger.id,scripts/perfledger.id,scripts/native_census.id,scripts/cfloor.id,scripts/runtime_bench.id,scripts/shcledger.id,gaps/GAP-127.md,gaps/GAP-128.md,gaps/GAP-151.md

## Claim under test

The release-facing construction is **not** "faster than C across the board
without exception" (C0 explicitly refuses that claim). The admissible claim is
`law.project.ftcftw` measured on the exact exercised path:

```text
direct native (--backend=direct)
  > C-equivalent (--backend=c, same graph)
  > wasm oracle (wasmtime on the same semantic answer)
```

plus **100% reachable native lowering** over the Idol examples corpus
(`native_census`), and **SHC authority** with zero bootstrap blockers
(`shcledger` once GAP-127 closes).

Each realization family reports separately. Losses are published, not hidden.

## Cursor lane deliverable (this gap)

`scripts/ftcftwledger.id` indexes the gate scripts that must exist and name
their contracts before any broad Idol-only performance statement is admissible.
It is wired through `scripts/perfledger.id` and `zig build ftcftw-ledger`.

This gap tracks **aggregate evidence presence**, not benchmark wins.

## Open blockers (disjoint lanes)

| Blocker | Owner | Effect on claim |
|---|---|---|
| GAP-127 | Cursor/Devin | `shcledger.id` still uses forbidden `std.script`; SHC census not canonical |
| GAP-128 | Poolside | `runtime_bench.id` ward env contract broken; wasm vs wasmtime cells invalid |
| GAP-151 | Poolside | No semantic lineage interface; optimization demand projection blocked |
| native bail worklist | Poolside | `native_census` reachable set not yet 100% (`NATIVE_FLOOR` ratchet) |
| boxing debt | SHC/Poolside | `semanticgate` `law.shc.native` cap 1271 `lua_Value` sites |

## Acceptance

1. `duo run scripts/ftcftwledger.id` exits 0 (all indexed contracts present).
2. Each indexed runner executes on the release candidate tree with recorded
   revision, dirty state, command, and outcome artifact.
3. No agent publishes a single-number "across the board" dominance claim; the
   ledger prints `claim.refused` and `claim.made` on every run.
4. When Poolside closes GAP-128, rerun `runtime_bench.id` and append losses to
   this gap before any wasm speed statement.

## Evidence

- Index added: `scripts/ftcftwledger.id`
- Parent index extended: `scripts/perfledger.id` (`ftcftw` row)
- Build step: `zig build ftcftw-ledger`
- Gate syntax repairs: `scripts/cfloor.id` (`main: i64 = ()`), `scripts/native_census.id` (offside syntax)
- Index execution unblock (2026-08-12): ledgers migrated off broken `std.script` /
  non-lowering `std.proc` helper calls to the `shcledger.id` shell bridge
  (`io.popen` + `lines: any = ()` mixed-mode anchor + `text:find` file probes)

### Index pass (contract presence only)

Tree: `f4c8b8b` (dirty). Commands:

```text
duo run #backend=c scripts/ftcftwledger.id  → exit 0
  ftcftwledger: pass all evidence contracts present
duo run #backend=c scripts/perfledger.id    → exit 0
  perfledger: pass all evidence contracts present
```

This satisfies acceptance item 1 (indexed scripts name their contracts). It does
**not** satisfy item 2 (each runner executed green with recorded artifacts).

Aggregate pass remains **open** until dependent gates execute green on a clean
tree run bound to HEAD.
