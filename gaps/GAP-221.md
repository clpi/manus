# GAP-221 — a shadowed write lands in the module's field storage

**Status:** OPEN
**Priority:** P0 · **Kind:** wrong_answer

<!-- idol-gap-frontier:v1:begin -->
```json
{
  "schema": "idol.gap.frontier.v1",
  "gap": "GAP-221",
  "status": "OPEN",
  "root_program": "src/dnir_lower.zig moduleFieldWord -> semantic_graph.bindingNamedIn -> module field storage",
  "current_blockers": [
    "the guard admits only a binding that bindingNamedIn resolves AND whose scope is module_root; those two cannot both hold, so moduleFieldWord returns null in every relation body and the module's own field word is unreachable from the relation that wrote it",
    "the four-fixture acceptance run cannot be obtained at 2954038e: the .id grammar refuses a descriptor, a typed binding, a subject relation, and two bare relations in one file, so all four fixtures refuse at parse (GAP-145)",
    "and it could not be obtained on this host even with the parse restored: the direct backend answers DNB004 for native-exe on aarch64 Linux and the C realizer emits portable source only, so no .id program executes here at all"
  ],
  "superseded_observations": [
    "#superseded-observations"
  ]
}
```
<!-- idol-gap-frontier:v1:end -->

## Current frontier notes

**REOPENED 2026-09-03 at `2954038e`.** The 2026-09-02 closure met the deletion
witness and inverted the guard. It was landed without ever running the
acceptance fixtures — its own commit message says so — and the gate that was
recorded as pinning the answer ran no program at all.

### The guard cannot admit anything

`moduleFieldWord` (`src/dnir_lower.zig:827`) admits the module's field word only
when both hold:

```zig
const base_binding = ctx.graph.bindingNamedIn(relation, base) orelse return null;
const base_node = ctx.graph.get(base_binding) orelse return null;
if (base_node.scope != ctx.graph.module_root) return null;
```

**Those two conditions are mutually exclusive.** `bindingNamedIn`
(`src/semantic_graph.zig:4352`) skips every node whose `enclosingCallable` is
not the relation it was asked about, so it can never answer with a module-scope
binding; and anything it does answer with is scoped inside the relation, so its
scope is never `module_root`. Both arms take the same `return null`.
`moduleFieldStorageBase` carries the identical shape.

The consequence is the complement of the filing, from the same line. The
original defect was the SHADOW's write reaching the module's word. This one is
the module's word becoming unreachable from the body that genuinely owns it:
with `moduleFieldWord` null, `lowerFieldAssignTarget` falls through to the
fresh-local fallback its own comment calls "the defect" — it mints a frame slot
named `M.x`, so the write the module can see never happens and every later
reader answers the literal.

### Measured

Executed at `2954038e`, ReleaseFast (`tools/node/dev/build-mode` says so):

```text
zig test src/tests.zig --test-filter GAP-221   (production frontend link flags)
1/2 tests.test_0...OK
2/2 dnir_lower.test.dnir_lower: GAP-221 a module field base is unresolvable
    from a relation body...OK
All 2 tests passed.
```

The test is in `src/dnir_lower.zig` and carries both directions:

* **control** — `bump(M)` holds its own `M`, `bindingNamedIn` resolves it, and
  its scope is not `module_root`. The guard declines, and declining is RIGHT
  here: the shadow's write is not the module's. This arm is what proves the
  query works and the other arm's null is the defect rather than a broken test.
* **defect** — `poke()` declares no `M` at all, so its `M.x = 7` writes the
  MODULE's binding. `bindingNamedIn(poke, "M")` is `null`, so the guard declines
  the word that write owns.

It asks through the LUA face on purpose; see the next section for why, and note
that the lift is the same lift, so the face the source wore does not enter the
question.

### Why the acceptance run is not the evidence here

The four-fixture acceptance run **cannot be obtained on this head**. The `.id`
grammar refuses every shape the fixtures need. Measured with the same binary:

| probe | result |
| --- | --- |
| `bank = { x = 1 }` and one relation reading `bank.x` | parses |
| `cell: { x: i64 }` | ``error: write `{` at this token edge`` / ``got `name` here instead`` |
| `main: i64 = ()` | same, at 1:1 |
| `y:len()` inside a relation body | same |
| two bare relations in one file | `error: expected expression, got '='` |
| first 60 tracked `examples/*.id` under `idol fmt` | 0 pass, 60 fail |

All four fixtures refuse at parse — `shadowstore.id:3:1`, `shadow.id:18:1`,
`shadow_field_storage.id:21:1`, `keyed_table_write_wins.id:15:1` — and so does
`lib/compiler/parser.id:64:1`, which is why `zig build unit-test` cannot reach
its own tests: it fails first at `tools/node/dev/parser/artifact`. That is the
pre-existing red aggregate `gaps/GAP-148.md:797` already records on this head,
and it is GAP-145's, not this gap's.

**And the parse is not the only thing in the way.** This host cannot execute a
`.id` program at all. `print(7)` — which parses — answers:

```text
error: DNB004: target object format or host is unsupported by the direct
backend: direct backend has no native machine realization for target
'native-exe' on this host; explicitly select --backend=c or --backend=wasm if
that realization is intended
```

`--backend=c` answers that the graph-backed C realizer emits portable source
only, and an explicit `--target=aarch64-linux` does not change the refusal.
So "Fixture execution requires backend support" in `4a0b2eef` was TRUE on the
machine it was written on. It was not a reason to record a closure; it was the
reason the closure could not be established, and the two are opposite.

**Positive control that the refusal is the head's and not this build's:**
compilers built from 2026-09-01 heads in sibling worktrees (`9a733bb6`,
`252e3af0`) accept both the typed-binding and two-relation probes. Every such
head predates `aa936dfa`, so no green binary anywhere in the tree contains the
repair — which is exactly why the repair was never run.

### What the repair owes

Restoring the module's word is one sign flip in the query — absence of a
body-local binding is what MEANS the module's binding is reached — but the
distinction the deleted `bodyDeclaresBinding` carried has to come back with it,
from the graph rather than from a spelling: a bare `M = { … }` in a relation
ASSIGNS the module binding and is not a shadow, while `M: cell = { … }` and a
parameter are. `place.BindOrigin` is the published fact for that, and
`registerTableFieldGlobals` already declines storage when `moduleAssignCount`
exceeds one, so the assignment case may not even reach here — that is a claim to
MEASURE, not to assume, and this gap is reopened rather than repaired precisely
because measuring it needs the parse GAP-145 owns.

**Nothing here is landed unrun again.** A repair to `moduleFieldWord` whose
acceptance cannot be executed is the same act as `4a0b2eef`, and it is what
produced this state.

## The 2026-09-02 closure, and what it actually established

`4a0b2eef` deleted `bodyDeclaresBinding` and rewrote the guard; `aa936dfa` made
`moduleFieldWord` answer the descriptor and the storage key together, so the
by-spelling `storageKey` consult after the binding decision has no call site.
**Both of those hold.** The deletion witness is met and stays met.

What did not hold is the answer. `4a0b2eef`'s commit message records it plainly
— "Fixture execution requires backend support; the deletion witness is met" —
and `gate/gap-221-shadowstore.sh` shipped as four `grep`s over
`src/dnir_lower.zig`, while `gaps/ROOT-PROGRAM.md` recorded it as the gate that
"pins the answer". A text scan is not an answer. The gate now compiles and runs
all four fixtures, and reports BLOCKED with the exact parse diagnostic rather
than PASS when it cannot; the moment GAP-145 restores the parse it becomes the
pin this gap has asked for since it was filed, with no edit to it.

The withdrawal recorded at `e66fdbf5` ruled that a quiet headline is not a
closure. This reopening is that same ruling applied to its own resolution: a met
deletion witness is not a closure either, when the acceptance it was supposed to
unblock was never run.

## Acceptance

Unchanged. `examples/shadowstore.id` answers `7 99 7`, `examples/shadow.id`
still answers `99 1`, `examples/shadow_field_storage.id` answers `0 99 7`, and
`examples/keyed_table_write_wins.id` still answers `2` with a storage-disabled
arm answering `1`. All four in one run: the first two prove the shadow is
respected, the last two prove storage is still granted where it is genuinely
owned. `gate/gap-221-shadowstore.sh` is the runner, and it must report all four
answered rather than BLOCKED.

## Superseded observations

The block below is the original 2026-08-21 filing. Its DELETION WITNESS is met
and its RULING stands. Its headline measurement is not current and its
acceptance run is not met: `7 99 7` was measured at a tree that still carried
`bodyDeclaresBinding`, and no tree since `4a0b2eef` has produced that answer,
because no tree since `4a0b2eef` has been able to parse the fixture. Read this
section for the RULING it records — why a write read off a spelling answers the
shadow — not for the state of the tree.

## Status
open — LIVE SILENT WRONG ANSWER at `5ddcd3b1`. Reproducing fixture landed as
`examples/shadowstore.id`.

## Measured

    # expect: 7 99 7
    cell: { x: i64 }
    bank = { x = 1 }

    poke: i64 = ()
      bank.x = 7          # writes the MODULE's bank.x -> storage is granted
      bank.x

    bump: i64 = ()
      bank: cell = { x = 5 }   # DECLARES its own bank
      bank.x = 99              # writes the SHADOW
      bank.x

    read: i64 = ()
      bank.x                   # must still see poke's 7

    answers 7 99 **99**

`read()` must answer 7: `poke` wrote 7 to the module's binding, and `bump`
wrote 99 to a binding it declared itself. The 99 is the shadow's write
arriving in the module's word.

## Why the neighbouring fix does not cover it

`examples/shadow.id` covers the case where the module's table is written by
NOBODY. That was repaired by teaching the field-write scan not to descend
into scopes that rebind the base name, so the module's table is no longer
granted storage it does not own.

This case is the complement: the module's table genuinely DOES own storage,
because `poke` really writes it. The scan is right to grant it. The defect
is downstream — `lowerRecordLiteralFields` and `lowerRecordLiteralAssign`
resolve a field write against `ctx.module_globals` BY SPELLING, so the
shadow's `bank.x = 99` finds the module's word and stores into it.

## The missing fact

`LowerCtx` carries no per-body binding identity. It can answer "is there a
module global spelled `bank.x`" and cannot answer "does the `bank` in scope
at THIS point denote the module binding". Those are different questions and
only the second is semantic.

This is the same frontier the aggregate-projection lane reached from the
other side: `AggregateFact.place` is `.one(site)` and `boundAggregateAtPlace`
returns null when two aggregates share a place, so the graph can name THE
COLLECTION AT SITE s but not WHICH BINDING A READ RESOLVES TO, nor ITS
CONTENTS BETWEEN WRITE w_i AND w_{i+1}. Two independent consumers, one
missing fact.

## Deletion witness for the eventual repair
Whatever supplies the per-body binding fact must DELETE the by-spelling
`ctx.module_globals` consults in `lowerRecordLiteralFields` and
`lowerRecordLiteralAssign`. A patch that adds binding identity while leaving
the spelling lookups in place leaves two stores that can disagree — which is
this defect, not its repair.

## Acceptance
`examples/shadowstore.id` answers `7 99 7`, `examples/shadow.id` still
answers `99 1`, and `examples/keyed_table_write_wins.id` still answers `2`
with a storage-disabled arm answering `1`. All four in one run: the first two
prove the shadow is respected, the last two prove storage is still granted
where it is genuinely owned.
