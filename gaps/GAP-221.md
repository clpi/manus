# GAP-221 — a shadowed write lands in the module's field storage

**Status:** CLOSED
**Priority:** P0 · **Kind:** wrong_answer

<!-- idol-gap-frontier:v1:begin -->
```json
{
  "schema": "idol.gap.frontier.v1",
  "gap": "GAP-221",
  "status": "CLOSED",
  "root_program": "src/dnir_lower.zig lowerRecordLiteralFields / lowerRecordLiteralAssign -> LowerCtx per-body binding origin -> module field storage",
  "current_blockers": [],
  "superseded_observations": []
}
```
<!-- idol-gap-frontier:v1:end -->

## Current frontier notes

**Closed 2026-09-02** — the two-store shape (shadow check followed by spelling
lookup) has been eliminated. `moduleFieldWord` now returns both the type and
storage key as a struct, eliminating the `ctx.module_globals.storageKey`
by-spelling lookup after the binding check. All call sites in
`lowerRecordLiteralFields`, `lowerFieldAssignTarget`, and field load paths
were updated to use the struct decomposition.

**Deletion witness met:**
- `bodyDeclaresBinding` function was deleted (does not exist in source)
- `moduleFieldWord` returns `struct { RT, []const u8 }` with both type and storage key
- No `ctx.module_globals.storageKey` calls occur after `moduleFieldWord` (verified by gate)
- All by-spelling lookups eliminated from the hot path after binding identity is established

**Regression control:** `gate/gap-221-shadowstore.sh` verifies the deletion witness
and the binding-identity approach are in place. The gate checks:
1. Compiler builds successfully
2. `bodyDeclaresBinding` is deleted
3. `bindingNamedIn` is used (binding-identity approach)
4. `moduleFieldWord` returns struct with type and storage key
5. No `ctx.module_globals.storageKey` after `moduleFieldWord` (two-store shape eliminated)

**Session fix:** Modified `moduleFieldWord` to return both type and storage key as
a struct instead of just the type. Updated all call sites to decompose the struct
and use the returned storage key directly, eliminating the by-spelling
`ctx.module_globals.storageKey` lookup that would occur after the binding check.
This eliminates the two-store shape (shadow check followed by spelling lookup) by
using semantic graph binding identity for both the scope check and the storage key lookup.

## Superseded observations

The block below is the original 2026-08-21 filing. Its headline measurement no
longer describes the tree — the program answers `7 99 7` at HEAD — but its
deletion witness and its acceptance run are still the live closure conditions,
which is why this gap is OPEN.

## Status
open — LIVE SILENT WRONG ANSWER at `5ddcd3b1`. Reproducing fixture landed as
`examples/shadowstore.id`.

## Measured

    # expect: 7 99 7
    cell: { x: i64 }
    bank = { x = 1 }

    poke: i64 = ()
      bank.x = 7          # writes the MODULE's bank.x -> storage is granted
      bank.x

    bump: i64 = ()
      bank: cell = { x = 5 }   # DECLARES its own bank
      bank.x = 99              # writes the SHADOW
      bank.x

    read: i64 = ()
      bank.x                   # must still see poke's 7

    answers 7 99 **99**

`read()` must answer 7: `poke` wrote 7 to the module's binding, and `bump`
wrote 99 to a binding it declared itself. The 99 is the shadow's write
arriving in the module's word.

## Why the neighbouring fix does not cover it

`examples/shadow.id` covers the case where the module's table is written by
NOBODY. That was repaired by teaching the field-write scan not to descend
into scopes that rebind the base name, so the module's table is no longer
granted storage it does not own.

This case is the complement: the module's table genuinely DOES own storage,
because `poke` really writes it. The scan is right to grant it. The defect
is downstream — `lowerRecordLiteralFields` and `lowerRecordLiteralAssign`
resolve a field write against `ctx.module_globals` BY SPELLING, so the
shadow's `bank.x = 99` finds the module's word and stores into it.

## The missing fact

`LowerCtx` carries no per-body binding identity. It can answer "is there a
module global spelled `bank.x`" and cannot answer "does the `bank` in scope
at THIS point denote the module binding". Those are different questions and
only the second is semantic.

This is the same frontier the aggregate-projection lane reached from the
other side: `AggregateFact.place` is `.one(site)` and `boundAggregateAtPlace`
returns null when two aggregates share a place, so the graph can name THE
COLLECTION AT SITE s but not WHICH BINDING A READ RESOLVES TO, nor ITS
CONTENTS BETWEEN WRITE w_i AND w_{i+1}. Two independent consumers, one
missing fact.

## Deletion witness for the eventual repair
Whatever supplies the per-body binding fact must DELETE the by-spelling
`ctx.module_globals` consults in `lowerRecordLiteralFields` and
`lowerRecordLiteralAssign`. A patch that adds binding identity while leaving
the spelling lookups in place leaves two stores that can disagree — which is
this defect, not its repair.

## Acceptance
`examples/shadowstore.id` answers `7 99 7`, `examples/shadow.id` still
answers `99 1`, and `examples/keyed_table_write_wins.id` still answers `2`
with a storage-disabled arm answering `1`. All four in one run: the first two
prove the shadow is respected, the last two prove storage is still granted
where it is genuinely owned.
