# GAP-230 — the compile cache key had two producers; the typed record is the survivor

**Status:** CLOSED · **Filed:** !2026-08-25T02:30:00Z · **Closed:** !2026-08-25T04:10:00Z
**Priority:** P1 · **Kind:** reconciliation_conflict
**Files:** src/main.zig,src/dnir_lower.zig,gate/cachedeps.sh,gate/cachepublish.sh,gate/callfold.sh

## What this gap asserted, and why it was wrong

| # | directive |
|---|---|
| 1 | Filed against `origin/sound16/place-and-cache-contract`, the one ref the branch census named as carrying unlanded work that a diff could not land. |
| 2 | The filing read the merge conflict — 92 lines against 0, the branch deleting the open-coded cache-key hash and minting `buildCacheKey` over a typed `BuildDependencies` — and concluded that taking the branch would silently drop four MEASURED soundness facts: the source quotient, the rule set the path compiles under, the worlds the launcher granted, and the demanded observers. |

| # | directive |
|---|---|
| 1 | **That conclusion was drawn from the SHAPE of the conflict and not from the code, and it was false.** The record carries all four, by name and by type: |

| fact the filing said was dropped | field that carries it |
|---|---|
| source quotient, in its own keyspace | `subject: [32]u8` + `subject_form: enum { quotient, raw }` |
| rule set / validation waiver | `gate_transport_waived: bool` |
| worlds the launcher granted | `worlds: [32]u8` |
| demanded observers | `observer_demand: u32` |

| # | directive |
|---|---|
| 1 | It also carries `unroll_factor`, `module_promote`, `sdkroot` and `developer_dir`. |
| 2 | All fifteen facts the open-coded sequence hashed are present. |
| 3 | A 92-against-0 hunk is what a refactor into a typed record looks like from the outside; the deletion was of the SPELLING, not of the facts. |

| # | directive |
|---|---|
| 1 | The general error is the one `law.source.not.proof` names from the other side: a diff is not the program. |
| 2 | The repair for a conflict whose two sides are one question is to read both producers, never to infer loss from hunk size. |

## The one real regression, which the filing missed

| # | directive |
|---|---|
| 1 | Reading the code found a different defect, in the opposite direction from the one filed. `BuildDependencies.reach` was populated by `hashReqClosure` ALONE: |

| # | directive |
|---|---|
| 1 | if (!hashReqClosure(arena.allocator(), io, root, src, &reach_h, &seen, 0)) |

| # | directive |
|---|---|
| 1 | That is the `req` half only — the banned spelling. |
| 2 | The lane was cut before `hashHomeClosure` existed, so the field named `reach` answered for less than its own comment claims, and dotted home projection — THE ONLY REACH A CANONICAL `.id` FILE HAS — was absent from the key. |
| 3 | That is exactly the defect `land/homeclosure-20260823` measured and closed on the other producer: |

| # | directive |
|---|---|
| 1 | idol compile --backend=direct main.id -o m.out -> 42 |

    # edit helper.id so it reaches deeper.plus(x) first
| # | directive |
|---|---|
| 1 | idol compile --backend=direct main.id -o m.out -> (cached) 42 idol compile --no-cache --backend=direct … -> 44 |

| # | directive |
|---|---|
| 1 | So the two producers were not equivalent, and neither side was simply correct: the open-coded one had the reach fix and no record; the record had every fact enumerated and a reach that predated the fix. |

## How it closed

| # | directive |
|---|---|
| 1 | The record survives — a hash DERIVED from a declared, typed field set is one producer, which is what `law.fact.producer.one` asks for, where an open-coded `h.update` sequence is a thing any call site may quietly extend. |
| 2 | The reach half is ported into it, sharing `seen` with the `req` walk so a partition reached both ways is hashed once and the key does not depend on which walk ran first. |

| # | directive |
|---|---|
| 1 | Landed with the lane's other commit, `place: ()` is not a projection, which deletes two arms of `placeFold` that were rival authorities over questions `place.zig` already settled — `.call` folding `t(k)` to an index against `law.md` §5 ("`()` is ordinary application. |
| 2 | It never means table indexing"), and `.name` answering a bare scalar's VALUE against §6 ("value != place"). |
| 3 | Both cited §18 and §19 for authority; current `law.md` ends at §17, so they were justified by a retired edition. |
| 4 | Neither arm was reachable at this revision, so their deletion is a law repair with no behavioural delta. |
