# GAP-115 — test evidence is corruptible by concurrent sessions, so gate results are not facts

**Status:** CLOSED · **Filed:** 2026-08-09 · **Closed:** 2026-08-25
**Law:** §3 MEASUREMENT HONESTY · "one fact, one authority, explicit provenance"
(owner ruling 2026-08-09 §28)

## 2026-08-25 closure — each channel re-measured against the current tree

Tree: `fc48acea`, linux-x86_64, worktree; known red aggregate on this host is
DNB004 (no direct-native realization; the direct runtime objects target
aarch64-macos), attributed once by `gate/realization/direct.sh` — it is a host
fact, not evidence corruption, and every one of the 15 red steps in the
serialized `idol-lock -- zig build test` run names it.

**Channel 1 — fixed log paths under /tmp.** The compiler's side was already
closed by `src/scratch.zig` (TMPDIR-routed scratch root + per-process salt;
its header carries the measured collision history), and every `gate/*.sh`
already minted private roots via mktemp. Re-measurement found the class still
alive in three evidence OWNERS, each of which wrote a fixed `/tmp/idol-*.out`
and (in two cases) read it back as a verdict: `tools/node/dev/doctor` (six
files, five grep-read — 'keychain is locked' and '<name>: ready' were
verdict-selecting grep targets any concurrent writer could plant),
`tools/node/dev/census/projection` (evidence written, then displayed from the
same fixed name), `tools/node/dev/positive-controls` (fixed sink). All three
now route through run-private mktemp roots. `/tmp/as.log` itself no longer
exists anywhere in the tree. A serialized `zig build test` left only
salted/mktemp names in /tmp (`duo_layout_probe_<salt>.c`,
`idol-probe.XXXXXX`) — no fixed evidence name was written by the exercised
path.

Residual, named: `.id` measurement scripts still spell fixed workdir literals
(`scripts/boring_corpus.id` `/tmp/duo-boring-corpus`,
`scripts/capability_matrix.id`, `scripts/capability_table.id`,
`scripts/tailslot.id`, `scripts/transform.id`, `scripts/convert_canon.id`,
`scripts/nominal_zero_cost.id`, benchmark runners; `os.time()`-suffixed
variants in `scripts/run_compile_fail_tests.id` and
`scripts/assert_no_ansi_reports.id` collide within one second). They are
unvalidatable on this host (every `idol run` is DNB004-blocked), so they are
recorded here rather than blind-edited; the pinning gate's scan population
(`gate/`, `tools/node/dev/`, `.githooks/`) deliberately excludes them until a
direct-native host can prove the edit.

**Channel 2 — the phantom smoke failure.** Not reproducible as a phantom.
`agent-smoke` on this host fails for exactly one attributed reason (DNB004)
and the same failure reproduces identically run directly — measured and
re-measured in the same serialized run. The original phantom's producer (a
shared fixed log) has no remaining instance in the gate path.

**Channel 3 — `zig build test` mutating tracked files.** CLOSED, measured:
`git status --porcelain` was empty before and after a full serialized
`zig build test` at `fc48acea`. The offending LSP generator and its tracked
output were deleted 2026-08-17; nothing replaced them.

**Channel 4 — shared cache under a running gate.** The Zig local cache is
per-worktree (this worktree built its own `.zig-cache`; the main checkout
keeps its own), and the shared Zig global cache is Zig's content-addressed
store. The idol executable cache is content-keyed (source bytes, backend, opt
level, compiler stat, gate waiver, injected worlds, `req` closure, observer
roster — `src/observer_demand.zig` `cacheKey`), and `TMPDIR` gives any run a
fully private cache namespace in one export (`src/scratch.zig`);
`gate/cachepublish.sh` carries the poison-entry arms and
`gate/cache-home.sh` the home-identity arms. Serialization itself is owned by
`tools/node/dev/idol-lock`, hardened 2026-08-25 on main (reentrancy by
process ancestry, dead-holder reclamation, exact-token removal) and pinned by
`gate/lock.sh` — re-run green on this tree.

**Channel 5 — provenance on gate results.** `SUBJECT revision=… dirty=…`
lines are emitted by `gate/cache-home.sh`, `gate/taint.sh`,
`gate/envcache.sh`, `gate/defaults.sh`, `gate/world-launch.sh`,
`gate/surface/reach.sh`, and now `gate/gap-115-evidence.sh`. Not yet
universal; the remaining gates are migration pressure, not a corruption
channel — a result without the line is unattributed, not wrong.

**The pin: `gate/gap-115-evidence.sh`.** Two arms, both falsifiable. (1) A
class scan: no tracked gate, hook, or dev tool redirects into a literal
`/tmp` path; positive-controlled against the exact retired shape, which the
detector must catch in a planted file. (2) A planted concurrent writer: the
production evidence script `tools/node/dev/census/projection` runs while a
writer that knows only the RETIRED fixed name fires deterministically inside
the write→read window (it rides the script's own `sed` invocation); the
displayed evidence must be this run's bytes and the writer's marker must
prove it fired. The same writer against a copy sed-damaged back to the
fixed-name shape MUST poison the displayed evidence, so the isolation
assertion is proven capable of failing.

Validation on this tree, serialized: `zig build` green; `zig build unit-test
--summary all` green (via the lock); `sh gate/gap-115-evidence.sh` PASS;
`sh gate/posix.sh` PASS (74 gates parse under dash); `sh gate/lock.sh` PASS.

---

## The ruling

> Current smoke-test evidence has been corrupted by concurrent sessions sharing
> mutable artifacts such as logs/cache state. For an agent-native compiler, each
> agent run should receive a unique run ID, private temp directory, private
> mutable logs, private mutable cache namespace, isolated generated artifacts.
> Only immutable/content-addressed cache data should be shared freely.
>
> **A test result whose evidence can be overwritten by another agent is not an
> authoritative fact.**

## The evidence, all from one day

**1. Two independent agents saw `agent-smoke` report a failure the program does
not have.** One traced its cause: `/tmp/as.log` was being written by a
concurrent session in the main checkout, so its first result was another tree's.
It re-ran into a private log and got a different answer.

**2. The failure I measured does not reproduce.** `agent-smoke` reports
`std_metaprogramming_modules_smoke` FAIL; run directly the program exits **0**
and prints `RESULT std_meta_modules ok`.

**3. `zig build test` MUTATES A TRACKED FILE.** Two separate agents reported
`tools/lsp/src/server.id` modified as a side effect of running the gate — the
lsp-gate regenerates the legend and the committed copy is stale. Both reverted it
rather than sweep it. **A test that edits the repository is a shared mutable
artifact inside version control**, and it makes `git status` unreliable as a
signal of what an agent changed.

**4. The `.zig-cache` is shared across every worktree**, so a concurrent rebuild
can land under a running gate.

**5. I propagated a stale gate fact into five agent briefs.** I told each that
`agent-smoke` is red at `explain: FAIL` and not to chase it. By the time they
ran, `explain` PASSED and the failure had moved. One agent reported this
correctly and I did not believe it until I re-ran.

## Why this is P0 rather than an annoyance

**It silently inverts conclusions.** Item 5 is the worst: five agents were told
to ignore a red gate that had gone green, so any of them who saw a NEW failure
had been pre-instructed to attribute it to the known baseline. That is a
manufactured false negative, distributed.

And it defeats the session's own method. Every real finding today came from
perturbing one input and diffing against the general path — a discipline that
assumes the measurement apparatus is stable. When the apparatus is shared and
mutable, "I changed X and the gate changed" is not evidence.

## What is owed, in order

1. **Private logs, mechanically.** No gate may write a fixed path under `/tmp`.
   A run ID in the path is sufficient and costs nothing.
2. **Stop tests mutating tracked files.** The offending in-tree LSP generator
   and tracked output were deleted on 2026-08-17. The general rule remains: a
   gate may compare generated output in a private build directory, never repair
   a tracked source while measuring it.
3. **A private cache namespace per worktree**, or an explicit statement that the
   cache is content-addressed and therefore safe. Today it is neither documented
   nor namespaced.
4. **Gate results carry provenance** — which tree, which commit, which run ID —
   so a result can be attributed rather than assumed.

## The rule that follows, and it is cheap

**Re-measure before citing a gate's state, and never carry a known-failing fact
across a session boundary.** Both of my errors above came from treating a
measured fact as durable. In a tree with four concurrent sessions, a gate result
is true for a moment, not for a day.

## Related

gap[101] carries the correction to the `agent-smoke` baseline and the two
sessions that saw phantom failures.
