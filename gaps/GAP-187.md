| field | value |
|---|---|
| title | GAP-187 — relation mode and solution cardinality |
| status | OPEN |
| filed | !2026-08-18T16:10:00Z |
| priority | P0 |
| kind | research_gap |
| filed by | devin-semantic-20260818 |
| files | docs/spec/constitution.md, docs/spec/law.md, src/sema.zig, src/dnir_lower.zig, src/semantic_graph.zig |

| # | directive |
|---|---|
| 1 | A relation is not only `subject × operands → result pack`. |
| 2 | It has slots with admissible directional modes, a solution cardinality law, and a failure/divergence law. |
| 3 | This absorbs Icon generators, Mercury determinism, optional results, iterators, search, pattern matching, parser ambiguity, database queries, constraints, logic relations and exceptions as one algebra. |

| section |
|---|---|
| Current measured boundary |

| # | directive |
|---|---|
| 1 | Application is modeled as a function-like call producing one result. `hex(b)` versus `b:hex()` already reveals slot roles are under-determined by semantics. |
| 2 | There is no canonical mode/cardinailty fact family. |

| # | directive |
|---|---|
| 1 | **Crossed:** |

| # | directive |
|---|---|
| 1 | `pack`, `cardinality`, `effect` and `demand` exist as graph facts. |
| 2 | `law.application` and `law.relation` are specified in C0. |

| # | directive |
|---|---|
| 1 | **Still missing:** |

| # | directive |
|---|---|
| 1 | `mode` per slot: `input`, `output`, `unique`, `unknown`. |
| 2 | `solution` per relation: `exactly one`, `zero or one`, `one or more`, `zero or more`, `never returns`, `divergent`. |
| 3 | `failure` law: `succeeds`, `fails`, `throws`, `blocks`. |
| 4 | Demand can select `first`, `one`, `all`, `any`, `count` — `any` is the canonical existential relation (`xs:any(p)`, law §112 ANY-DESCRIPTOR-ZERO); a second spelling for the existence quotient would be a plurality defect. |
| 5 | Reordering of goals/producers based on mode/dataflow. |
| 6 | `b:hex()` and `hex(b)` converge to one relation with two admissible faces. |

| section |
|---|---|
| Required order |

| # | directive |
|---|---|
| 1 | Add `mode` and `solution` as graph facts on every relation declaration. |
| 2 | Make surface orientation (subject/operand) a provenance face, not a semantic authority. |
| 3 | Implement solution/demand interaction: `:first` stops at one, `:all` collects, `:has` checks existence, `:count` returns cardinality. |
| 4 | Derive generator/iterator/optional/search/parser/query from the same algebra. |
| 5 | Reorder applications by producer-consumer dataflow. |

| section |
|---|---|
| Closure acceptance |

| # | directive |
|---|---|
| 1 | Close when `hex(b)` and `b:hex()` are one relation with two admissible modes, when `x:hex(b)` is rejected by mode arity, and when `users:find(.age > 18):first`, `:all`, `:any`, and `:count` choose different realizations. |

<!-- idol-gap-c0:v1:begin -->
<!-- c0-schema: Instance of the schema in `gaps/RESEARCH-SPINE.md`; `tools/node/dev/gapc0` fails closed without it. -->
| field | value |
|---|---|
| constitutional owners | relation (slot modes, solution cardinality, failure law are relation facts) · demand (`first`/`one`/`all`/`any`/`count` selection) · effect (failure/divergence) |
| kind delta | 0 |
| registry delta | 0 |
| source syntax delta | 0 |
| semantic invariant | a relation carries per-slot mode facts (`input`, `output`, `unique`, `unknown`), a solution cardinality law (`exactly one`, `zero or one`, `one or more`, `zero or more`), and a termination/failure law; surface orientation (subject vs operand) is provenance, never authority. |
| physical mechanism | mode-aware application resolution; demand-selected realization dispatch. |
| prerequisites | none — root. |
| foreign implication | foreign generators, iterators, optionals, and queries ingest as mode/cardinality facts on ordinary relations. |
| world implication | worlds qualify which solutions are admissible, not the algebra. |
| projection implication | `:first`/`:all`/`:has`/`:count` are ordinary relation projections of one application. |
| demand implication | consumer demand selects the solution strategy — the demand face this GAP gives the rest of the spine. |
| realization implication | optional, iterator, generator, search, query, logic relation, parser ambiguity, and pattern match collapse into one algebra; `:first` stops at a witness (complexity-class reduction, not faster iteration). |
| evidence | `hex(b)` ≡ `b:hex()` as one relation with two faces; `:first` vs `:all` vs `:count` measured choosing different algorithms. |
| deletion witness | iterator/optional/generator kingdoms and the `hex(b)`/`b:hex()` role ambiguity. |
<!-- idol-gap-c0:v1:end -->
