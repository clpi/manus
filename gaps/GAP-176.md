# GAP-176 — information and demand as quotient semantics

**Status:** OPEN · **Filed:** !2026-08-18T14:50:00Z
**Priority:** P1 · **Kind:** research_gap
**Filed by:** devin-semantic-20260818
**Files:** docs/spec/constitution.md, docs/spec/law.md, docs/demand-quotient.md, src/sema.zig, src/dnir_lower.zig

| # | directive |
|---|---|
| 1 | Generalize demand beyond "is this value used?" to "which equivalence classes must remain distinguishable under the admitted observer." Replace ad hoc notions of projection, quotient, approximation, lossy conversion, and privacy with a single information order `A ⪰ B` over semantic values. |

## Current measured boundary

| # | directive |
|---|---|
| 1 | The quotient/demand machinery already collapses exact state spaces by bit/field observability. |
| 2 | It is not yet a general information-theoretic fact family over every semantic value. |
| 3 | Conversion classes, demand, and approximation remain separate subsystems. |

| # | directive |
|---|---|
| 1 | **Crossed:** |

- `demand` exists as a graph fact and drives optimization.
- Periodicity work demonstrates quotienting by demanded bits.

| # | directive |
|---|---|
| 1 | **Still missing:** |

- An `information` lattice per value: `full value ⪰ hash`, `text ⪰ length`,
  `f64 ⪰ rounded f32`, `table ⪰ membership`, `value ⪰ sign`.
- Demand as a quotient `Q(value)` rather than a yes/no use flag.
- Standard observer quotients: `length`, `sign`, `cardinality`, `membership`,
  `topk`, `prefix`, `low n bits`, `order`, `shape`, `distribution`.
- `preserve no more information than the observer can distinguish` as a
  realization constraint.
- Integration with conversion safety, serialization, privacy, and approximation.

## Required order

1. Define `information order` in C0: `A ⪰ B` means `A` distinguishes at least
   every difference `B` distinguishes.
2. Add `quotient` as a semantic descriptor fact: the observer demands `Q(value)`.
3. Express `projection`, `conversion`, `demand`, and `approximation` as
   instances of the information order.
4. Make optimization legality `Q(original) = Q(candidate)` for demand `Q`.
5. Compute lower bounds from the demanded quotient: `log₂(distinctions)`
   for information-theoretic minima; communication complexity for data
   movement minima.

## Closure acceptance

| # | directive |
|---|---|
| 1 | Close when `demand` is uniformly an observer quotient, when conversions are ordered by information loss, and when the optimizer can prove legality from `Q(original) = Q(candidate)` across all transformations. |

## C0 alignment

| # | directive |
|---|---|
| 1 | Instance of the schema in `gaps/RESEARCH-SPINE.md`; `tools/node/dev/gapc0` fails closed without it. |

- **constitutional owners:** demand · descriptor · witness
- **kind delta:** 0
- **registry delta:** 0
- **source syntax delta:** 0
- **semantic invariant:** demand is a quotient `Q(value)` — the distinctions an admitted observer can require; the information order `A ⪰ B` orders facts on existing values; transform legality is `Q(original) = Q(candidate)`.
- **physical mechanism:** quotient comparison inside the legality check; `log₂(distinctions)` floor computation.
- **prerequisites:** GAP-187 (demand face), GAP-175 (fact engine).
- **foreign implication:** a foreign boundary need not reconstruct information the downstream observer never demands.
- **world implication:** narrower worlds tighten quotients — capability precision makes code faster.
- **projection implication:** projection is an information reduction (member/length/cardinality quotients), replacing a projection taxonomy.
- **demand implication:** this GAP is the demand extension — quotients replace yes/no use flags.
- **realization implication:** compute only the demanded quotient; changes complexity class (`sort(xs)` then `first` is `min(xs)`); approximation and sketches lawful when the contract does not distinguish. Never compute information nobody can observe.
- **evidence:** one complexity-class reduction and one legality proof by `Q(original) = Q(candidate)`.
- **deletion witness:** ad hoc dead-code flags, lossy-cast taxonomies, privacy redaction as a separate mechanism.
