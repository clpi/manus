# GAP-119 — Process execution lacks subject and world semantic vocabulary

**Status:** OPEN · **Filed:** !2026-08-09T20:08:39Z
**Priority:** P0 · **Kind:** expressiveness_gap
**Filed by:** codex_semantic_normalization
**Files:** lib/proc.id,lib/script.id,scripts/semanticgate.id

| # | directive |
|---|---|
| 1 | The semantic architecture gate must currently use `std.proc.capture(command)` repeatedly because the live standard vocabulary exposes process execution as a namespace call returning a string, not as a command subject relation under an explicit process world with structured stdout, status, stderr, provenance and failure. |
| 2 | The gate classifies and ratchets this bootstrap debt; do not disguise it with a module alias. |
| 3 | GAP-117 prevents factoring the repeated calls behind a canonically typed result-demand helper. |

## Do not expand the retiring bridge

| # | directive |
|---|---|
| 1 | The in-flight GAP-115 evidence-integrity work adds public `runid` and `provenance` relations to `lib/script.id`. |
| 2 | Both read environment state and execute shell commands through `capture`. |
| 3 | Private run identity and attributable evidence are required, but this placement makes a new public capability API in the exact home being retired. |

| # | directive |
|---|---|
| 1 | Deleting convenience aliases from `std.script` is aligned. |
| 2 | Adding new public operations is not. |
| 3 | Represent run identity and provenance through the admitted environment/process worlds when that vocabulary lands. |
| 4 | Until then, any bridge must remain gate-private, declare `authority = false`, and name deletion by the world-backed replacement. |
| 5 | It may not become standard vocabulary that agents learn by imitation. |

## Release Gate Zero measurement

| # | directive |
|---|---|
| 1 | Measured on committed HEAD `b4fe9af`: |

| surface | occurrences |
|---|---:|
| direct `std.script.*` calls | 803 |
| `req "std.script"` imports | 11 |
| direct `std.proc.*` calls | 82 |

| # | directive |
|---|---|
| 1 | The shared working tree had 782 direct `std.script.*` calls and 13 imports at the same measurement. |
| 2 | Direct spelling fell while imports rose. |
| 3 | That is aliasing, not semantic migration, and must never decrement the authority-debt ratchet. |

| # | directive |
|---|---|
| 1 | The largest committed `std.script` spellings were: |

| spelling | occurrences |
|---|---:|
| `capture` | 224 |
| `chomp` | 198 |
| `run` | 145 |
| `getenv` | 118 |
| `status` | 90 |
| `fs_exists` | 55 |
| `fs_write` | 26 |
| `getenv_or` | 24 |
| `fs_read` | 18 |
| `echo` | 13 |

| # | directive |
|---|---|
| 1 | The live `lib/script.id` graph exposed 53 public callables. |
| 2 | They span at least seven independent semantic authorities: |

1. process execution and capture;
2. environment observation and update;
3. filesystem observation and mutation;
4. path projection;
5. build locking and run provenance;
6. argument observation and JSON loading;
7. string trimming and output.

| # | directive |
|---|---|
| 1 | `std.script` is therefore both a forbidden capability namespace and a miniature junk drawer. |
| 2 | Renaming it, importing it under a shorter binding, or dividing it into `std.proc`, `std.fs`, and `std.env` preserves the architecture defect. |

## Closure order

1. Admit process specifications, structured process results, process-world
   requirements, and shell interpretation as distinct facts. A command string
   is not automatically a process specification.
2. Admit environment presence semantics under the environment world through
   GAP-118. Preserve absent, present empty, and present nonempty.
3. Map filesystem and path operations to subject relations and filesystem
   worlds. Prefer atomic state relations over observe-then-mutate pairs.
4. Move build locking and provenance to their actual semantic owners; neither
   belongs to process vocabulary.
5. Keep pure trimming, path projection, JSON, and formatting as value relations
   with no world authority.
6. Migrate consumers by resolved relation identity, not text replacement. An
   alias or renamed home must carry the same debt until the forbidden definition
   is unreachable.
7. Delete `lib/script.id` and its import projection. The target count is
   zero definitions, zero direct calls, and zero aliases.

| # | directive |
|---|---|
| 1 | Until the first five families have admitted relations and world/case facts, their state is `SEMANTIC-VOCABULARY-BLOCKED`. |
| 2 | Do not create `execute`, `command`, `process.run`, or another plausible public spelling merely to make this gap's counts move. |
