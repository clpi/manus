| field | value |
|---|---|
| title | GAP-133 — Generate the agent alignment projection from C0 |
| status | CLOSED |
| filed | !2026-08-09T22:08:56Z |
| closed | !2026-08-25 |
| priority | P1 |
| kind | agent |
| filed by | codex-root-shc-20260809 |
| files | docs/AGENT_ALIGNMENT.md, scripts/alignment_emit.id, gate/alignment-projection.sh |

| section |
|---|---|
| 2026-08-25 closure — one producer, an executed emitter, a named live doc gate |

| # | directive |
|---|---|
| 1 | Tree `fc48acea`. |
| 2 | The premise was re-measured before anything was built. |
| 3 | The four specimens this record filed against — deleted pass plans, Duo-as-Lua-superset identity, legacy `@comp` authority, retired `scripts/duo_lock.sh` — were already hand-repaired after filing (none appears at this HEAD). |
| 4 | The CLASS was still open and still producing: the compass cited `gate/selfhost.sh` twice as a runnable local gate while the file lives only in the sibling tree (`../idol-native/gate/selfhost.sh`, per `docs/METRICS.md` and `gate/all.sh`), and nothing owned the projection — no generator, no manifest row, no gate. |
| 5 | A hand repair had fixed specimens; the producer was still the hand (`law.repair.class`). |

| # | directive |
|---|---|
| 1 | What landed, per C0 `law.semantic.service` ("the constitution … generate[s] the teaching corpus"; fails "independently handwritten examples") and `law.doc.corpus`: |

| # | directive |
|---|---|
| 1 | **`scripts/alignment_emit.id`** — an executed Idol emitter, sibling of `scripts/treesitter_emit.id` and using its honest partition: the compass is a GENERATED frame plus an AUTHORED residue between `@@residue:begin` / `@@residue:end` markers, carried byte-for-byte and counted. Generated from C0 on every run: the cited-law table (every `law.*` identity cited anywhere in the file, resolved against its C0 block for kind and holds — an unresolvable citation is exit 3, so the filed defect class, a retired authority still being taught, cannot survive a regeneration) and the six docs-gate conditions of C0 §57 read from the `docgate` block verbatim. The residue is the operative content C0 does not carry; claiming to derive it would be lying, so it is carried and measured instead. |
| 2 | **`docs/AGENT_ALIGNMENT.md`** regenerated by that emitter: 45 generated lines framing 311 authored residue lines, 4 cited laws resolved, 6 docs-gate conditions, and the `gate/selfhost.sh` citations repaired to the sibling-tree path inside the residue. |
| 3 | **`gate/alignment-projection.sh`** — the named live doc gate, in `gate/table-projection.sh`'s shape and enumerated by `gate/all.sh`: controls first (a tampered frame is restored by regeneration; a planted `law.retired.control` citation refuses with exit 3; deleted residue markers refuse with exit 3; the reference scan catches a planted absent file and passes resolvable text), then §3 byte-identity of a scratch regeneration against the tracked compass, §4 idempotence (a second run over the first run's output changes nothing), §5 every repo-relative file the compass cites in backticks exists. `--emit` is the documented regeneration path. A `gate/generated.manifest` row is deliberately absent: layering L2b fails any diff touching the projection without its generator, and residue edits are the one hand edit this design is FOR — byte-identity is the sharper law and the gate asserts it directly (reasoning in the gate header). |

| # | directive |
|---|---|
| 1 | Execution evidence, this host: the direct backend answers DNB004, so the emitter executes through the same `idol dump-c` + cc bridge `gate/gap-114-boxed-len.sh` binds to. `sh gate/alignment-projection.sh` → all controls pass, §3 byte-identical, §4 idempotent, §5 all citations resolve, exit 0. `idol check scripts/alignment_emit.id` → no errors. `sh gate/posix.sh` → 74 gates parse. `tools/node/dev/census/identity` → pass. |
| 2 | Building the emitter surfaced a real bridge miscompile — `rows[i]:trim()` segfaults where the pre-bound form is correct — filed as `gaps/GAP-237.md`; the emitter binds every indexed element first and cites it. |

| # | directive |
|---|---|
| 1 | Generated-C evidence only; not direct-native or wasm evidence. |
| 2 | The gate is transport for the projection law, not semantic authority (`law.gate.protocol`). |

| section |
|---|---|
| The filed defect (2026-08-09) |

| # | directive |
|---|---|
| 1 | docs/AGENT_ALIGNMENT.md is a clean but stale handwritten projection: it teaches deleted pass plans, Duo-as-Lua-superset identity, legacy @comp authority, and retired scripts/duo_lock.sh. |
| 2 | C0 law.semantic.service/law.doc.corpus requires a generated short projection and a named live doc gate; no generator or aggregate gate currently owns it. |
| 3 | This does not block unrelated implementation with a resolved semantic owner. |
