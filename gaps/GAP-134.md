# GAP-134 - SHC grammar authority is not a generatable canonical artifact

**Status:** OPEN - **Filed:** !2026-08-09T23:08:57Z
**Priority:** P0 - **Kind:** expressiveness_gap
**Filed by:** codex-root-shc-audit
**Files:** docs/spec/grammar.md,docs/spec/constitution.md,lib/compiler/parser.id,scripts/treesitter_emit.id

<!-- idol-gap-frontier:v1:begin -->
```json
{
  "schema": "idol.gap.frontier.v1",
  "gap": "GAP-134",
  "status": "OPEN",
  "root_program": "lib/compiler/token.id -> production token facts -> lib/compiler/parser.id {header_signal_lx,return_starts_value_lx,_clause,_layout_verdict,event,boundary} -> src/parser.zig",
  "current_blockers": [
    "Twenty-six bounded production decisions now execute from lib/compiler/parser.id: callable header, return-value start, match-arm clause, line-head lead, expression prefix, end-of-expression demands_operand, the Pratt binding-power triple, primitive-descriptor identity, literal-kind identity, quoted-payload identity, unary relation identity, glued-update relation identity, single-token update relation identity, layout-terminator identity, empty-body-terminator identity, layout-frame recognition, layout-verdict recognition, static-member identity, complete block-edge action, written closure, clause binding, direct statement dispatch, source-family statement admission, contextual `type` alias-head classification, bare declaration-path recognition, and authoritative `@`-statement face; the rest of production recognition remains host-executed.",
    "Graph-proven public `event` emits two contiguous words per immutable producer token through one capacity-checked caller-owned buffer. Lane one carries dispatch/admission/member/boundary/static/Pratt/relation facts through bit 61. Lane two carries match bits 0..1, return bits 2..3, callable-header variants bits 4..5, contextual `type` alias head in bit 6, propagated bare declaration head in bit 7, and authoritative `@`-statement face bits 62..63. Parser caches both lanes beside parser_facts and uses the same sole cursor. Public `boundary` receives no raw kind and reads no token row. All host-facing per-decision parser ABIs are deleted; only `event` and `boundary` remain as parser externs. The artifact covers 228 event cases, 2 trivia-lead cases, 8 header-lane, 6 return-lane, 8 match-lane, 5 type-lane, 5 bare-lane, 37 at-face cases, 114 event-to-boundary slots, and 7 opening faces; GAP-145 passes 359 checks and admission proves those two graph relations.",
    "Tree-sitter literal, numeric, keyword-literal, and comment identity rules are owner-derived and mechanically pinned; its wider structural/offside editor grammar is not a production parser replacement.",
    "GAP-145 remains OPEN: the host-authored TokenKind enum and duplicate Lexer cursor are deleted, but the generated Zig enum bridge and remaining lexical/host-kind observers still mediate recognition.",
    "The executed parser.id boundary covers twenty-six decisions; statement internals, expression internals beyond prefix/Prefix/Prefix Pratt, and `@`-statement internals (paren-arg parse, attached-declaration attribute dispatch) remain host-executed, so this is not a complete parser stage, graph differential, or compiler B.",
    "No compiler B exists."
  ],
  "superseded_observations": [
    "#current-evidence-2026-08-13",
    "#one-producer-ruling-lawgrammarone",
    "#relation-ontology-transferred-tree-sitter-measured-base-c77dea60",
    "#the-parser-slice-cannot-start-and-the-reason-is-mechanical-2026-08-23-dec7d509"
  ]
}
```
<!-- idol-gap-frontier:v1:end -->

## Twenty-sixth parser decision: authoritative `@`-statement face (2026-09-01)

The host `parse_at_starts_attribute_decl` rebuilt the qualified name from
token text, asked `meta_module.resolveBuiltin` + `isAttachingMetaAttribute` +
`isStandaloneModuleStatement` + `isCHeaderImportDirective` +
`directives.isBuildDirective/...` whether it was a known directive or
attaching attribute, then re-tokenized the trailing sequence to decide
attaching-vs-standalone-vs-expression. That path was a duplicate producer
of the same fact the host catalogs already settled elsewhere.

Whole-pack event lane-one bits 62..63 now carry one producer-owned
`@`-statement face, encoded by the private `_at_face` relation:

```text
0  expression               — name not in any canonical attaching or
                              standalone directive catalog
1  standalone directive     — @build.*, @debug.*, @trace.*, @cinclude,
                              @comp.define.derive.*, @comp.pipeline,
                              @comp.c.import, @comp.c.emit, @comp.c.include,
                              @comp.foreign, @comp.wasm, @comp.sql,
                              @comp.lua, and the @meta.* mirrors.
2  attaching declaration    — @inline, @noinline, @hot, @cold, @pure,
                              @packed, @raw, @nopanic, @profile, @unroll,
                              @deprecated, @implements, @derive, @ffi,
                              @align, @arc, @asm, @bench, @bitfield, @bitcast,
                              @concurrent, @consteval, @device,
                              @differentiable, @autodiff, @dispatch,
                              @likely, @unlikely, @prefetch, @noreturn,
                              @repr, @restrict, @sealed, @native, @guarded,
                              @section, @simd, @specialize, @target,
                              @test, @time, @volatile, @flatten, @export,
                              @c.export, @c.call, @c.ffi, @c.link, @c.type,
                              and their @comp.* / @meta.* namespace mirrors.
```

The `_at_face` relation walks `name (. name)*` from the `@` token, skipping
`kinddot` separators, and matches first/second/third-segment lexeme hashes
against the canonical reduction table. Bit-shape choices and lexeme hash
derivation mirror the existing event lane-one encoding (length + up to
six bytes packed into `fact[i*2+2]`).

Bridge-death witness: the host's
`meta_module.resolveBuiltin` + `isAttachingMetaAttribute` +
`isStandaloneModuleStatement` + `isCHeaderImportDirective` +
`directives.isBuildDirective/...` remain the IDENTITY owners for every
alias and ergonomic spelling (`comp.c.export` ↔ `c.export`, `meta.pipeline`
↔ `pipeline`, `meta.c.export` ↔ `__c_export`, etc.). parser.id owns only
the structural reduction (qualified name → {0,1,2}) for the canonical
names it enumerates below. When parser.id can read the host catalog
directly, those host functions become redundant and are deleted
(`law.bridge.death`). Next host-owned boundary after that: parser-side
classification for the `@comp.X` namespace where the third segment is the
identity authority.

The host's `parse_stmt_face` face=1 (`@`) branch now reads bits 62..63 and
dispatches: face=2 → `parse_attributed_decl` directly; face=1 → the host's
existing lookahead (`parse_at_starts_attribute_decl`) decides expression
versus attached-declaration via the trailing-token probe (the structural
face bit says "name is known", but the trailing declaration keyword is
host-owned). face=0 → `parse_expr_stmt`. The catalog membership is now
the event-bit's responsibility; the lookahead is the structural truth the
host must still own.

Three vertical RED/GREEN cycles proved: standalone directive (9 controls —
`@cinclude`, `@build.*`, `@debug.*`, `@trace.*`, `@comp.define.derive`,
`@comp.pipeline`, `@comp.c.import`, `@comp.c.emit`, `@comp.c.include`),
attaching declaration (19 controls — `@inline`, `@noinline`, `@hot`,
`@cold`, `@pure`, `@packed`, `@raw`, `@nopanic`, `@profile`, `@unroll`,
`@deprecated`, `@implements`, `@ffi`, `@c.export`, `@c.call`, `@c.ffi`,
`@c.link`, `@c.type`, `@c.export("n") name()`), and expression
(3 controls — `@something_unknown(x)`, `@xyz`, `@comp.unknown_thing()`).
The 37-case test (37 cases including `@meta.*` and `@export`/`@flatten`
mirrors) reports `at-face-cases=37` and `failed=0`; GAP-145 passes
359 checks; the pinned build passes 5/5; the fresh aggregate passes
1845 with 62 skipped and zero failed.

## Twenty-fifth parser decision: bare declaration path (2026-09-01)

The host `starts_bare_func_decl` saved parser state, advanced across
`name ("." name)* (":" name)?`, selected the callable-header decision at `(`,
and restored. Four statement/attribute consumers repeated that host scan.

Whole-pack `event` already settles callable-header bit 4 at each `(`. When that
bit is true it now walks backward over the exact admitted path and writes
lane-two bit 7 at the starting name coordinate. Direct, dotted, and
subject-specialized declarations therefore consume one precomputed fact;
`a:b.c(...)` remains false at `a` because colon is admitted only immediately
before the final relation name. The Zig recognizer and all four calls are
deleted without an alias. No ABI, event word, allocation, cursor, runtime call,
or fallback was added.

Three vertical RED/GREEN cycles proved direct `f(...)`, dotted `home.f(...)`,
and subject-specialized `body:weight(...)` independently. The final artifact
reports `bare-lane-cases=5` and `failed=0`; the consumer gate passes 359 checks,
the pinned build passes 5/5, and the fresh aggregate passes 1,845 with 62 skipped
and zero failed. The exact debug compiler is 28,803,480 bytes,
`56ff5d51941b10ebc785f1d6ea8e8878ca7e3d6ad2f22d7b65e137e7659ba587`:
a 608-byte win over the type-only candidate but still an open 752-byte loss from
the original two-lane artifact. Runtime, startup, compile work, memory, cross-host
identity, and oracle comparisons remain unmeasured.

This advances one more production recognition decision, not Parser SHC or
Compiler B. `@` attributed-declaration classification is now the exact remaining
statement-head frontier, but its compatibility/directive policy must reduce to
owner facts before transfer rather than move host registries into `parser.id`.

```text
needed_fact: one owner-projected @ statement face — expression, attaching declaration, or standalone directive
current_producer: src/parser.zig parse_at_starts_attribute_decl plus directives.zig/meta_module.zig name catalogs
consumer: src/parser.zig parse_stmt_face and parse_attributed_decl
blocking_interface: event carries token identity/raw source facts but no authoritative directive/attribute role; copying host names would violate GRAMMAR-ONE and CATALOG-ZERO
owner_lane: grammar + parser, jointly with the existing directive/meta fact owner
```

## Twenty-fourth parser decision: contextual type head (2026-09-01)

The statement parser previously compared source text to `"type"` at five host
sites. The direct path also saved, advanced, peeked, and restored the parser to
separate alias declaration `type Name = ...` from compatibility call
`type(value)`. The attributed-declaration classifier repeated the same source
word decision before and after its scan, and the alias materializer revalidated
the spelling once more.

Whole-pack `event` now consumes the existing raw short-word fact and following
token identity once. Lane-two bit 6 is set only for exact contextual `type` when
the following identity is not `(`. Four Zig consumers select that settled fact;
the five text comparisons and the direct save/advance/peek branch are deleted.
The alias materializer retains only a physical token-class sanity check. No new
relation, parser ABI, event word, allocation, cursor, or fallback was added.

The artifact was RED only on the two alias-positive controls (`type-event=0`,
expected 1); `type(x)`, spaced call, and near-spelling controls remained zero.
After generation it reports `type-lane-cases=5` and `failed=0`. The
positive-controlled consumer gate passes 349 checks, the pinned-Zig build passes
5/5, and the fresh aggregate passes 1,845 with 62 skipped and zero failed.
The exact debug compiler is 28,804,088 bytes,
`0efc298400dfeb72920a12b8508eb3f8c3c26dd6eb006ebe6cf4083ebe56a678`:
an explicit open +1,360-byte loss from the pre-slice artifact. Latency, startup,
compile work, and memory were not measured, so no speed claim is active.

This advances one production recognition decision, not the parser stage or
Compiler B. The exact next host-owned statement boundary is bare-name callable
path recognition in `starts_bare_func_decl`; `@` attributed-declaration
classification remains host-owned and carries larger compatibility-policy risk.

## Second whole-pack lane and two-ABI parser surface (2026-09-01)

`event` now writes two contiguous words per coordinate. Lane two carries match,
return, and both callable-header faces, with exact 4/4/4 RED differentials before
implementation. Event derives previous visible identity/line, line opener,
contextual short word, nesting, separators, and source-family layout once. Direct
header/return/match externs, build renames, production calls, and standalone
artifact calls are deleted after equivalence. The no-pack return control still
refuses, preventing a newly installed pack at coordinate zero from masking a
host-only advance.

Locked proof: artifact `failed=0`; GAP-145 340 checks; aggregate 1845 pass / 62
skip / 0 fail; parser externs exactly `event` and `boundary`. Artifact:
28,802,728 bytes,
`dd5397c8f7fb5f3c945f9d93e124d18fc1cf8e00f1b980e9ff4a5adbe94cd109`.
This closes per-decision C crossings, not the host-owned parser stage. Paired
`event-lane-consolidation.json` evidence validates with five damaged controls:
correctness is `bound`, artifact bytes are a 4,032-byte `win`, and every latency
cell is `unknown` because Pi throttle history started nonzero. Raw boundary,
clause, statement, and member intervals point toward losses; startup is
inconclusive.

## Complete static event word and direct-ABI deletion (2026-09-01)

Public `event` now owns every per-coordinate static parser face in one positive
i64. Its layout is:

```text
0..4 statement       5..8 admission       9 member
10 boundary          11..12 branch        13 return
14 written end       15 layout close      16 empty-body close
17 primitive         18 literal           19 quoted
20 prefix            21 operand demand    22 line-head lead
23..46 Pratt triple  47..51 unary         52..56 glued update
57..61 update
```

Line-head bit 22 is computed over parser-visible line history and does not let
shebang/comment identities advance that history. `advRaw` captures demand bit 21
before advancing the sole pack cursor. Quoted identity is captured before any
speculative advance where the physical token survives. Absent relation faces
encode as zero; admitted ordinals encode as `ordinal + 1`, so every invalid face
refuses rather than aliasing ordinal zero.

The artifact oracle was RED in four vertical slices: 18 prefix mismatches, 36
demand mismatches, 6 line-head mismatches, then 70 primitive/literal/quoted/
relation mismatches. It now exhausts 228 event rows plus two trivia-history
controls with `failed=0`. Standalone `lead`, `prefix`, `demands_operand`,
`infix_prec`, primitive/literal/quoted, `_unary`, `_glue`, and `_update`
relations, symbols, build renames, production calls, and direct differentials are
deleted. The pretty-printer round-trip test also consumes event bits 23..46.

Current locked proof: build 5/5; aggregate 1845 pass / 62 skip / 0 fail;
GAP-145 325 checks; admission `ADMITTED` with exactly `event` and `boundary` as
graph-proven public relations. Exact artifact: 28,806,760 bytes,
`fa2162b262a10c5bb93ca4563487e48688e98d389e17515ebc965cb60ad46284`.
This consolidates transport and deletes authority seams; the decision count
remains twenty-three and Parser SHC remains open. Paired evidence in
`static-event-consolidation.json` validates but cannot activate latency claims:
throttle history was nonzero before measurement. Artifact bytes are an explicit
`open` loss of +11,704; raw boundary, clause, statement, and member paired
intervals point toward losses.

## Eighteenth parser-recognition boundary: static member identity (2026-08-31)

The host `is_name_like_kind` helper reconstructed member identity from the first
byte of `TokenKind.spelling()`. That admitted literal presentation labels and
refused contextual keyword members. The grammar owner now emits `member()` over
all physical slots, parser.id graph-proven public `member` consumes it, and the production
parser calls `idol_parser_member` at both consumers. The spelling helper is
deleted rather than retained as an oracle.

`tools/node/dev/parser/artifact` executes every owner slot and bounds controls;
the GAP-145 gate damages both the host-spelling pattern and the owner row. The
fresh locked aggregate succeeds with zero failed tests. This moves one grammar
fact consumer; it does not make the bounded L3 projection parser authoritative,
does not complete the production parser, and does not produce Compiler B.

## Nineteenth parser-recognition boundary: complete block action (2026-08-31)

The owner now projects `boundary()` (written/block/bracket closers) and
`branch()` (`else`/`elseif`) across all 114 physical slots. Parser relation
`boundary` combines those facts with the existing layout verdict and returns
one packed continue/close/misindent/return-and-close action plus body column.
`src/parser.zig` no longer has `LayoutVerdict`, `layout_verdict`, or the
`parse_block_open` token-kind action switch.

The test-first generated-C artifact covers every slot and the layout/branch edge
cases; the consumer gate damages both owner rows. The fresh pinned-Zig aggregate
passes 1846, skips 62, and fails 0. This deletes one host decision class; most
statement/expression recognition and AST materialization remain host-owned.

## Nineteenth through twenty-first decisions: one complete boundary answer (2026-08-31)

Public relation `boundary` now returns the complete block-edge answer: loop
action in bits 0..1, none/elseif/else in bits 2..3, written closure in bits
4..6, and body-column establishment in bits 8..35. Standalone public `clause`
and `closure` relations, generated symbols, build renames, Zig externs/calls,
and their separate artifact functions are deleted. The generated-C differential
covers 48 written-closure combinations and every else/elseif identity/column
combination across 114 slots. The return-and-close path re-observes the following
token edge through the same relation; its dedicated parser regression remains.
The host retains diagnostics, token consumption, and physical frame storage but
no longer owns any of the three decisions.
The pinned aggregate is 1847 pass, 62 skip, 0 fail; complete parser and
Compiler B remain open.

## Twenty-second decision: direct statement dispatch (2026-09-01)

The host `parse_stmt` switch directly mapped token identities to local/global/
const/function/control/label materializers. `lib/compiler/token.id` now owns a
compact 114-slot `statement()` row. Existing `boundary` returns that face in
bits 36..40, so the normal block loop pays no new ABI call. `parse_stmt_face`
switches on the settled numeric face; the old direct token branches are deleted.

The generated-C differential checks the owner row and boundary result against an
explicit 114-slot oracle, including bounds. The owner-damage control changes the
`while` face and requires regeneration to move the row. Contextual `name`
(type-alias vs call vs bare relation) and contextual `@` remain host-executed
and are the exact next statement frontier. The fresh aggregate remains 1847
pass, 62 skip, 0 fail. This is one decision transfer, not
complete statement recognition, parser SHC, or Compiler B.

## Twenty-third decision: source-family statement admission (2026-09-01)

The host `denyRetiredStmtKeyword` switch owned which identities are always denied
(`macro`) and which are denied only in canonical Idol (`try`/`catch`, `defer`,
`goto`, `extends`, `private`, `async`/`await`, and `let`). Owner row
`admission()` now carries the nine faces over all 114 slots. Existing `boundary`
settles the source-family answer in bits 41..44. The normal block loop consumes
it without another ABI call; attributed, `if let`/`while let`, and await paths
query the same relation only at their exceptional token edges.

Zig now switches only on the settled admission face to choose diagnostic prose;
the token-identity and `idol_mode` admission switch is deleted. The generated-C
artifact covers 114 kinds × two family states plus bounds (`admission-cases=228`),
and the owner-damage control moves the `let` face. GAP-145 passes 272 checks; the
fresh aggregate remains 1847 pass, 62 skip, 0 fail.

Paired internal evidence is retained in `statement-admission-consolidation.json`.
All four latency rows are `unknown`, correctness is `bound`, artifact bytes are
an `open` +2,680 loss, and the Pi acquired throttle bit `0x8` during measurement.
No performance or strongest-oracle claim follows. Contextual `name` and `@`
lookahead are the remaining statement-head decisions.

## First production whole-pack event buffer (2026-09-01)

The existing `parser_facts` array already gives every producer token one metadata
word and one six-byte raw-lexeme word while preserving a single Parser cursor.
Graph-proven `event(fact,count,out,capacity,idol)` now consumes that complete pack
once and writes one static event word per token into caller-owned storage. It
refuses short capacity before any mutation. Event bits currently carry direct
statement face, source-family admission, member, unconditional boundary, and
branch identity.

Parser allocates and caches `parser_events` beside `parser_facts`, validates the
exact written count, indexes both by `producerStreamIndex`, and releases both with
the owned pack. Normal and exceptional statement paths now read dispatch and
admission from this array. Those static rows and bits 36..44 were deleted from
the dynamic per-token `boundary` result; boundary retains only layout-dependent
loop, clause, closure, and body-column facts.

The generated-C RED initially failed because `idol_parser_event` did not exist.
The final 228-case differential covers all 114 identities in both family states,
short-capacity non-mutation, exact count, and guard slots. GAP-145 passes 288
checks, admission proves ten graph relations, and the aggregate remains 1847 pass,
62 skip, 0 fail.

`whole-pack-event-consolidation.json` validates with five damaged controls. The
current Pi run acquired throttle bit `0x8`, so all latency rows remain `unknown`;
three parser-heavy paired medians were slower and stay visible. Artifact bytes are
an `open` +6,240 loss and correctness is `bound`. This is the first production
whole-pack transport seam, not complete Parser SHC: dynamic events, contextual
lookahead, statement/expression structure, and AST materialization remain.

### Standalone member seam deleted

All three production member consumers (`accept_name_like`, glued-anchor lookahead,
and `parse_at_path_segment`) now index event bit 9 at the current producer
coordinate. The public `member(kind)` relation, generated symbol, build rename,
Zig extern, direct artifact function, and per-call sites are deleted. The owner
`member()` row remains and is consumed once by `event`.

The deletion gate was planted first and failed on seven old surfaces. The final
gate passes 291 checks; admission now proves nine public graph relations; the
aggregate remains 1847 pass, 62 skip, 0 fail. Exact paired evidence is retained
in `whole-pack-member-consolidation.json`: the thermally affected five-workload
run classifies every latency row `unknown`, artifact bytes are an `open` +912
loss, and correctness is `bound`. An earlier four-workload run produced an open
statement summary but its raw JSON was overwritten before archival, so it is
explicitly inadmissible as frontier evidence.

### Static boundary/layout seams deleted

`event` now carries unconditional boundary at bit 10, branch face at bits 11..12,
return at bit 13, written `end` at bit 14, layout terminator at bit 15, and the
five-member empty-body face at bit 16. `open_layout`, the relation empty-body
path, `close_block`, the block loop, and the return-following-clause edge all
index that same event coordinate. Public `boundary` receives the event plus only
physical count/line/column/frame facts; it reads no token row and receives no raw
kind. `_layout_verdict` remains private and boundary-internal.

The standalone `_layout_terminator`, `_empty_body_terminator`, and direct
`idol_parser_layout_verdict` seams are deleted from canonical source, generated C,
build renames, Zig externs/calls, and direct tests. The artifact was RED on 23
static-edge cases before event bits 13..16 and event-backed boundary/opening were
implemented. It now covers 114 event-to-boundary slots, 228 family event cases,
48 closure cases, and 686 clause cases with `failed=0`. GAP-145 passes 301 checks;
the aggregate remains 1847 pass / 62 skip / 0 fail.

The exact compiler is 28,795,056 bytes,
`5a332c3b48641e699c5a7372ff534da79768099bfacd052c3db7a56b8340635a`.
`whole-pack-boundary-consolidation.json` validates from raw paired samples and
five damaged controls. All latency cells are `unknown`: this Pi already had
nonzero historical throttle bits before measurement, so equal pre/post bytes do
not prove a clean run. The raw clause interval points toward a loss; boundary,
statement, member, and startup intervals are inconclusive. Artifact bytes are an
`open` +328 loss and correctness is `bound`. No fastest claim follows.

### Opening folded into the complete boundary

The private `_opening` relation was the last direct layout-frame seam. Public
`boundary` now takes an explicit `opening` fact plus `open_line`; the opening face
returns offside/inline/open/body-column facts, while edge calls retain the packed
loop/clause/closure contract. This is one relation identity with an explicit face,
not a sentinel, repurposed payload, or automatic currying trick.

The standalone relation, generated C symbol, build rename, Zig extern/call, and
direct differential are deleted. The RED artifact failed compilation on all seven
new eleven-operand calls while boundary still had nine operands. Final evidence:

```text
parser artifact → boundary-opening-cases=7; event-boundary-slots=114;
                  closure=48; clause=686; failed=0; PASS
GAP-145 consumer → PASS (305 checks)
unit-test        → 9/9 steps; 1847 pass / 62 skip / 0 fail
artifact         → 28,795,056 bytes; 1b423570da2a0400ae1de176b1097f2eeef2bb06475c2a0d8f3f864e5914e537
```

`boundary-opening-consolidation.json` validates raw binding/output/sample/order/
summary/artifact controls. All latency cells are `unknown`; the Pi changed from
historical-only `0xe0000` to current throttle bits `0xe0006`. The raw boundary
interval points toward a loss; clause, statement, member, and startup are
inconclusive. Artifact bytes are equal (`unknown`), correctness is `bound`, and no
fastest claim follows.

## Sole producer-pack cursor (2026-08-31, base `6e8abf5a` + diff)

Parser now owns the only immutable producer slice and cursor. The duplicate
Lexer fields/body (`duo_tokens`, `duo_index`, `duo_next`), lexer-side token-view
adapter, one-consumer installer, and private main-driver wrapper are deleted.
Parser state snapshots include the pack index, so every speculative recognizer
restores the same production coordinate rather than synchronizing two cursors.

The 77-check GAP-145 gate positive-controls literal-zero aliases; the parser
artifact regenerates byte-identically and executes every bounded Idol recognizer;
the complete pinned-Zig aggregate passes 1832, skips 62, and fails 0. This is a
bridge deletion that makes the next bounded recognizer transfer smaller. It is
not a fourth executed recognizer, complete parser SHC, or Compiler B.

## Fifth executed parser-recognition boundary: `demands_operand` (2026-08-31, base `6e8abf5a` + diff)

`advRaw` runs once per consumed token and used to ask
`grammar_roles.lookup(kind).demands_operand` whether the next token is an
operand — one host row-cache lookup on the Pratt hot path. Parser.id now
carries `demands_operand: bool = (kind: i64)`, which reads the same owner
`demand(): str` `grammar_role_table.zig` uses to fill `.demands_operand`, so
the parser-side fact and the generated row emitter cannot drift apart.

The C ABI symbol is `idol_parser_demands_operand(kind)`; `build.zig` renames
it from the parser.id relation name. `gate/gap-145-consumer.sh` section
2f'' forbids the host row lookup, forbids the `fn demandsOperand` helper,
requires the parser.id relation, the projection.c symbol, and the
`build.zig` rename, and adds two damage-control probes; the gate now passes
100 checks (was 93). The complete pinned-Zig aggregate still passes 1835,
skips 62, fails 0.

`token_view.zig` and the remaining `infix_prec` host reach for
`grammar_roles.lookup` and `grammar_roles.infixRelation` are the next two
hot-path reads; the producer `precedence` / `assoc` / `relation` slots
already exist as owner fact slices and only the parser-side relations remain
to be authored.

## Sixth executed parser-recognition boundary: `infix_prec` (2026-08-31)

`parse_prec` runs once per Pratt step and used to ask
`grammar_roles.infixRelation(kind)` for the operation identity AND
`grammar_roles.lookup(kind)` for `.precedence` and `.assoc` — two host row
reads per peek, on the Pratt hot path, and a third read on the only
remaining `Parser.infixBinOp` helper. Parser.id now carries
`infix_prec: i64 = (kind: i64)`, which reads the owner's `_roleinfix`,
`roleprecedence`, `roleassoc`, and compact generated `relation()` ordinal row and packs the
triple (relation ordinal + left + right precedence) into one i64.

The C ABI symbol is `idol_parser_infix_prec(kind)`; `build.zig` renames it
from the parser.id relation name. `gate/gap-145-consumer.sh` section 2f'''
forbids the host row lookup of `.precedence` and `.assoc`, forbids the
`fn infixBinOp(` helper, requires the parser.id relation, the projection.c
symbol, and the `build.zig` rename, and adds two damage-control probes.
The `pretty.zig` `BinOp` round-trip test now reads the packed ABI call
directly instead of going through `Parser.infixBinOp`, so the closure
control that the deleted helper was tested for survives.

The gate now passes 109 checks (was 100). The complete pinned-Zig
aggregate still passes 1835, skips 62, fails 0. This is the seventh
bounded recognizer, not a complete parser stage or Compiler B.

## Seventh executed parser-recognition boundary: `is_primitive_descriptor_kind` (2026-08-31)

10 type-atom / typed-binding / type-table-key / record-field / layout-arg
sites (`parse_type_primary`, `parse_attributed_decl`, `parse_table_literal`,
`parse_record_field_list`, `parse_offside_pack`, `parse_pack_decls`,
`layout_arg_can_start_type`, `parse_suffixed_expr`) asked
`grammar_roles.isDescriptor(kind)` on every probe — one host row-cache
lookup per probe, with no way for the parser to agree with the generated
role row except by re-reading the same cached `descriptor` field. Parser.id
now carries `is_primitive_descriptor_kind: bool = (k: i64)`, which reads the
producer's `roledescriptor` directly and exposes the same predicate through
the C ABI as `idol_parser_is_primitive_descriptor_kind`.

Distinct from `is_descriptor_kind` (the broader `roledescriptor OR
is_type_kind` predicate used by `colon_is_method_call_lx`); the two
predicates answer different questions and the ABI symbol names them
separately so a future host that confuses them surfaces as a link error
rather than a semantic drift. `gate/gap-145-consumer.sh` section 2f''''
forbids the host row lookup, requires the parser.id relation, the
projection.c symbol, and the `build.zig` rename, and adds two
damage-control probes. The gate now passes 115 checks (was 109). This is
the eighth bounded recognizer, not a complete parser stage or Compiler B.

## Ninth and tenth executed parser-recognition boundaries: `is_literal_kind` + `is_quoted_kind` (2026-08-31)

10 Pratt / record / match-arm / descriptor / table / pretty sites across
`parse_stmt`, `parse_match_arm`, `parse_descriptor_slot`,
`parse_table_literal`, and the deduped `int_lit` case in
`parse_table_literal`'s unboxed-key sugar asked
`grammar_roles.isLiteralKind(kind)` or
`grammar_roles.isQuotedKind(kind)` on every probe — two host row-cache
lookups for the predicate pair, with no way for the parser to agree with
the generated role row except by re-reading the same cached `literal_kind`
and `quoted` fields. Parser.id now carries two new relations:

- `is_literal_kind: bool = (kind: i64)`, reading the producer's
  `literal(): str` row (a dense 0/1 char slice with `1` set for `int_lit`,
  `float_lit`, and the four text/bytes/compat slots).
- `is_quoted_kind: bool = (kind: i64)`, reading the producer's
  `quoted(): str` row (the same `kind[8]` shape, with `1` set for the
  four text/bytes/compat slots).

Both predicates cross the C ABI as `idol_parser_is_literal_kind` and
`idol_parser_is_quoted_kind`; `build.zig` enrolls both
`-Dis_literal_kind=idol_parser_is_literal_kind` and
`-Dis_quoted_kind=idol_parser_is_quoted_kind` renames.

The producer added the two zero-arg bit-slice relations in
`lib/compiler/token.id` (next to `lead()` / `prefix()` / `demand()`) and
emits them in the `idbody()` emitter, so both
`lib/token/grammarrole.id` and `src/grammar_role_table.zig` regenerate
with the new bit strings; the host `roleliteral` / `rolequoted`
predicate pair and the generated row fields (`.literal_kind`,
`.quoted`) agree with the parser-side facts at the source level — there
is one producer of the predicate, one producer of the row emitter, and
one parser ABI call per kind.

`gate/gap-145-consumer.sh` sections 2f''''' and 2f'''''' forbid the host
row reads (`grammar_roles.isLiteralKind(` / `grammar_roles.isQuotedKind(`),
require the parser.id relations, the projection.c symbols, and the
build.zig renames, and add two damage-control probes; the gate now passes
131 checks (was 115). Two production parser tests
(`parse: production is_literal_kind decision executes the Idol relation`
and `parse: production is_quoted_kind decision executes the Idol relation`)
iterate every role row and assert the new ABI calls reproduce the
generated `.literal_kind` and `.quoted` row fields for every kind. The
parser artifact regenerates byte-identically. Fresh-cache complete unit
run passes 1839, skips 62, fails 0 (was 1837 / 62 / 0). The grammar-
projection gate and the full admission gate also pass.

The table-sugar `else if (tok.kind == .int_lit or isQuotedKind(tok.kind))`
arm collapses to `(is_literal_kind(...) or is_quoted_kind(...))` so the
literal/quote question lives in one place rather than split between a
kind-class check and a host row read. This is the ninth and tenth bounded
recognizers, not a complete parser stage or Compiler B.

## Eleventh through thirteenth parser decisions: unary, glue, update (2026-08-31)

The final executable `grammar_roles.*Relation` reads in the production parser
were two unary-prefix sites, the adjacency-glued compound-update probe, and the
single-token compound-update probe. They now cross three parser.id decisions:
private `_unary`, `_glue`, and `_update`, exposed at the C boundary as
`idol_parser_unary`, `idol_parser_glue`, and `idol_parser_update`. The private
source face is a bounded vocabulary-freeze bridge: `gate/vocabulary.sh` refuses
new public module declarations, and the deletion condition is a vocabulary-
proven canonical relation surface or complete parser-pack execution.

The relation/prefix order remains solely in `lib/compiler/token.id`. Its
generator projects one compact 114-byte ordinal row per ontology (`.` absent,
`A` ordinal zero); parser.id performs O(1) byte lookup. The two generated
per-slot name rows, runtime name scanner, two copied order strings, and the
`relationordinal` / `prefixordinal` scans are deleted. The four host relation
facades (`infixRelation`, `unaryRelation`, `gluedRelation`, `updateRelation`)
had no production consumers after transfer and are deleted rather than kept as
test-only compatibility APIs; their tests now inspect generated row facts.

Executed evidence on the live diff:

```text
IDOL=./zig-out/bin/idol sh tools/node/dev/parser/artifact --write
IDOL=./zig-out/bin/idol sh tools/node/dev/parser/artifact
→ generated bytes match; relation-slots=114; failed=0; PASS (tracked and regenerated)
sh gate/gap-145-consumer.sh  # run twice
→ PASS (168 checks), including mirrored-order and old-host plants
IDOL=./zig-out/bin/idol sh gate/grammar-projection.sh
→ malformed-producer control refused; both projections byte-identical; PASS
sh gate/admission-all.sh --base HEAD
→ vocabulary, layering, directive, frontier, and source quarantine ADMITTED
./tools/node/dev/idol-lock -- zig build --summary all
→ 5/5 steps succeeded
```

Direct execution of `gate/idiom.id` on this Linux ARM64 host still refuses at
`DNB004`; `idol check gate/idiom.id` succeeds. Generated-C execution is not
substituted as direct gate proof. This law/implementation delta does not change
the three executed parser decisions or their artifact evidence.

## Fourth executed parser-recognition boundary: `lead` (2026-08-31)

The generated owner already carried the three identities that can be both infix
and line-leading prefix/attribute faces, but Zig read `.opens_line` and rescanned
source bytes. Parser state now restores its previous-token line on every
speculative read, and `parser.id` relation `lead` combines exact token identity,
token line, previous line, and `token.grammarrole.lead()`.

The host recognizer and source rescan are deleted. The artifact harness checks
all 114 owner slots in same-line and later-line arms; the production parser test
pairs a line-leading unary minus with a multiplication continuation. The
positive-controlled consumer gate passes 85 checks, and a fresh-cache complete
unit run passes 1834, skips 62, and fails 0. Paired check-latency ranges overlap;
no performance claim is activated. This is the fourth bounded recognizer, not
complete parser recognition, graph differential closure, or Compiler B.

## First executed parser-recognition boundary (2026-08-30, base `61e0e951` + diff)

The syntax-only frontier below is superseded at one exact production edge.
`src/parser.zig` now projects the immutable producer pack once into compact
physical facts — metadata plus a raw short lexeme per token — and
`scan_func_header_signal` calls `idol_parser_header_pack`. Metadata carries the
owner ordinal, line, and column. That
symbol is `lib/compiler/parser.id` `header_signal_lx`, compiled from the tracked generated
artifact `src/parser/projection.c`. The 209-line Zig `headerSignal` /
`viewColonIsMethodCall` recognizer is deleted; the gate refuses its return.

The compact representation is physical only. Generated relations decode
kind[8], line[28], column[27], then ask owner-projected `token.grammarrole.*`
facts. A second word carries a length marker plus at most six producer bytes for
contextual words; Zig assigns no role. The host refuses locations outside the
physical bounds rather than truncating. One padding slot preserves Idol sequence
index one at the C ABI.

The transfer exposed and repaired two real defects in the previously unexecuted
Idol recognizer: zero-parameter `main(): i64` re-peeked its `:` instead of the
result descriptor, and a consumed subject colon was tested as though the cursor
still sat on it, misclassifying `print(t:concat("-"))` as a declaration. The
tracked artifact probe pins typed=1, call=0, offside=1, condition=0, empty=1,
nested=0; a second probe requires pack/cursor agreement on eight adversarial
shapes, two statement wrappers, and eight match-clause cases.
`gate/gap-145-consumer.sh` now passes 46 checks and requires the production calls
and deletion of their Zig decisions.

Measured correctness on the complete Zig aggregate:

    1835 pass / 62 skip / 0 fail (1897 total)

An exact two-compiler `idol check` differential over 565 tracked `examples/**/*.id`
and `lib/compiler/*.id` subjects changed 0 normalized status/stdout/stderr rows
with 0 infrastructure outcomes.

The generated bytes are independently regenerated by
`tools/node/dev/parser/artifact`, compiled, linked, and executed against both
candidate and tracked projections. `build.zig` enrolls that gate in ordinary
`test` and `unit-test`; `gate/generated.manifest` binds the 166571-byte tracked
C bridge to `lib/compiler/parser.id`.

One adjacent gate remains red on both arms and is not counted as evidence:
`gate/lower/fallback.sh` passes its controls and the bind/token-view demos, then
exits 3 because `lib/compiler/arm64check.id` now source-refuses unresolved
`process:capture` before reaching the older pinned `codegen refused` class. The
clean `61e0e951` compiler and this compiler both exit 1 with zero artifact bytes
and the same diagnostic modulo tree path. That is a stale gate baseline, not a
transfer regression and not a pass.

Physical frontier, same host and workload `idol check lib/compiler/parser.id`,
two warmups and nine balanced paired rounds over the final clean artifact: base
median 221.742 ms, transfer median 219.637 ms, transfer lower in 4/9 pairs. The
ranges overlap, so this is no speed or regression claim; the latency row is a
bound. Debug binary footprint rises 84640 bytes (0.295%). A 1 ms `/proc` sample
over four balanced runs measured 31568–31584 KiB base and 31584 KiB transfer;
those ranges overlap, so no memory win or loss is claimed. The binary footprint
increase remains an open physical loss.

NOT closure. `src/parser.zig` still owns nearly all recognition and AST
construction; the generated `TokenKind` projection and `Lexer.duo_tokens`
cursor remain load-bearing; Tree-sitter and grammar-document projection debts
remain; no complete Idol parser stage, graph equivalence proof, Compiler B, or
FTCFTW result exists. `returnStartsValue` and `startsMatchArm` subsequently
crossed this same boundary. `useDuoTokens` subsequently fell; the next deletion
target is the `duo_tokens` / `duo_index` cursor or one owner-projected quote face,
not another table or a port of the host parser.

## Third executed parser-recognition boundary (2026-08-30, base `8c3ae345` + diff)

`startsMatchArm` and the contextual-face selection in `parse_match_arm` now
consume `lib/compiler/parser.id` `_clause`. The relation returns 0 (not an arm),
1 (ordinary pattern), 2 (compatibility `case` face), or 3 (`else`). It owns the
pattern role, same-line boundary, delimiter nesting, and `then` / `do` / `=>`
separator decision. The former 30-line Zig scanner and both host text comparisons
are deleted.

`case` is deliberately not made a global keyword: existing syntax treats it as
a contextual name. The physical parser pack therefore widens from one to two
nonnegative `i64` facts per token. The first remains kind[8], line[28],
column[27]. The second carries only a length marker and the first six raw producer
bytes. Zig copies those bytes without naming a word role; `parser.id` compares
the contextual spelling. Long lexemes carry marker 7 and cannot alias `case`.

The artifact gate derives those bytes by tokenizing source through the included
generated lexer, then requires eight match-clause answers in both candidate and
tracked parser projections. `gate/gap-145-consumer.sh` section 2e refuses the
old `case` text comparison, pattern-role branch, and separator/depth switch while
requiring the C ABI rename and production call. The gate now passes 46 checks.
The generated projection is 166571 bytes; the complete Zig aggregate passes
1835, skips 62, and fails 0 (1897 total).

The run corrected two false return-slice controls: `function` and `#` are
owner-admitted expression starts, so the rejected-role fixtures are now `)` and
`+`. No production return behavior changed.

Same-toolchain physical comparison against clean `8c3ae345`, workload
`idol check lib/compiler/parser.id`, two warmups and nine balanced rounds: base
median 224.176 ms, candidate 224.413 ms, candidate lower in 5/9 pairs. Ranges
overlap, so no speed or regression claim is made. The debug binary grows 1016
bytes (0.00353%). A 1 ms `/proc` sample measured 31808–31840 KiB base and
31648–31680 KiB candidate; that bounded sample is recorded, not generalized as
a memory win.

NOT closure. AST construction remains in Zig; generated `TokenKind`,
`Lexer.duo_tokens` / `duo_index`, direct `tok.kind` switches,
and 13 quote-blind arms remain load-bearing. This is the third bounded parser
decision, not a complete parser stage, Compiler B, or FTCFTW result.

## Producer-record kind bridge deletion (2026-08-30, base `0182e1ee` + diff)

`lexer_dispatch.tokenFromRecord` no longer translates a raw ordinal through
`grammar_role_table.rows[ordinal].kind`. It validates the physical value against
the field values of the owner-generated sparse `TokenKind` enum and performs one
checked backing conversion. `kindFromRecord` and the production grammar-row
import are deleted; unpublished slot 3, negative, and >u8 values refuse.

This removes one runtime identity projection before the parser pack is built.
It does not remove the generated enum or token slice. All 27 lexer-dispatch tests
and the 58-check GAP gate pass. The constant `TokenizeAuthority` enum/query is
also deleted without an alias; production has one generated route. The complete
aggregate passes 1834, skips 62, and fails 0 (1896 total). The next exact
deletion is `useDuoTokens` / `Lexer.duo_tokens` or an owner-projected quote face,
not a compatibility alias.

Linux static taint checks confirm the cache quotient executes the producer route
with no host scan, while `tokenizeHost` remains reachable only from the
differential. Darwin-only dynamic interposition is not claimed on this host.

Physical comparison against clean `0182e1ee` with the same pinned Zig: 21
balanced three-process samples of `idol check lib/compiler/parser.id` give base
median 232.835 ms and candidate 236.724 ms, with candidate lower in 9/21 and
overlapping ranges. A final nine-round clean-artifact repeat measured 290.050 vs
301.183 ms, candidate lower in 5/9, also overlapping. No speed or regression
claim is made. The changed decoder/helper machine code shrinks 328 bytes; the
whole debug binary grows 1688 bytes (0.00586%).

## Dead explicit pack API deletion (2026-08-30, base `0a430ecb` + diff)

The unused `ProductionPack` wrapper, `tokenizePack` constructor, and
`token_view.fromPack` adapter only exercised one another. They are deleted, not
renamed. Production continues through `lexer_dispatch.route`; direct observation
continues through `tokenize` and `fromTokens`. Seven token-view tests and the
58-check GAP gate pass; the complete aggregate passes 1833, skips 62, and fails
0 (1895 total). At that revision the remaining live deletion target was
`Lexer.duo_tokens` / `useDuoTokens`.

## Live token installer deletion (2026-08-30, base `44f007b0` + diff)

`useDuoTokens` and `TokenStreamError` are deleted, not renamed. The sole
production route installs the already validated token slice directly, preserving
leading shebang seating, peek state, generated comment hints, and ownership.
Twenty-seven lexer-dispatch tests and the 63-check GAP gate pass. Remaining live
representation debt is `Lexer.duo_tokens` / `duo_index` and the generated enum.
The complete aggregate passes 1832, skips 62, and fails 0 (1894 total); the
same-toolchain debug binary shrinks 1368 bytes.

## Current cross-home transfer frontier (2026-08-26)

### Grammar.md is now a generated projection of the owner (2026-08-27)

Item 5 of the ordered list below — "`docs/spec/grammar.md` as a generated
projection of the owner" — has its first executed slice on this DNB004 host.
`docs/spec/grammar.md` now carries a GENERATED region between
`<!-- grammar:begin -->` and `<!-- grammar:end -->` holding two owner fact
tables (the generated `begin_expr` identities and precedence/associativity
ladder), projected from `src/grammar_role_table.zig` — the bridge
`lib/compiler/token.id` regenerates, and the same read target
`gate/treesitter/agreement.id` already uses. No production is invented:
every row is a fact the owner already publishes. The prose around the
region is authored law and stays authored.

Witness, executable on this host:

    sh gate/grammar/spec.sh

The gate regenerates privately and fails unless the tracked markdown is
byte-identical, and carries a vacuity control: a one-fact damage to the
owner bridge (`plus` loses `precedence = 17`) must CHANGE the projection
or the gate refuses — a projection that ignores its owner is decoration.
Both controls measured PASS on `f23facf` + this diff.

NOT closure: the region is fact tables, not the machine-readable grammar;
Tree-sitter still owns authored literal/comment recognition (GAP-145 O1);
the generator runs through the host shell because `idol run` refuses DNB004
here (`scripts/grammar/spec.id` carries the Idol front); and all parser
recognition except the header edge above remains host-executed. The next item on the same ladder is
widening the generated region until the hand-written grammar tables in the
document are gone.

At current head, the immutable token-view prerequisite named by this gap and the
parser source both emit C translation units accepted by this DNB004 host. The
current-head witness is:

    ./zig-out/bin/idol check lib/compiler/token_view.id
    ./zig-out/bin/idol dump-c lib/compiler/token_view.id --lib >/tmp/token_view.c
    cc -fsyntax-only /tmp/token_view.c

The parser witness is:

    ./zig-out/bin/idol check lib/compiler/parser.id
    ./zig-out/bin/idol dump-c lib/compiler/parser.id --lib >/tmp/parser_id.c
    cc -fsyntax-only /tmp/parser_id.c

Both `check` and C syntax validation pass, with zero `unlowered native call`
sites in either generated translation unit. That was the prerequisite; the
executed header edge above is the first production movement past it. It remains
one recognizer, not parser SHC: `src/parser.zig` still constructs the AST and
owns every other production. GAP-145 is OPEN, Tree-sitter still owns
literal/comment residue, and no compiler B exists. The older token-view
counterfactuals below remain as provenance for the route that became executable.

### Superseded realization evidence (pre-16a9fa3e)

`lib/compiler/token_view.id` — the immutable token view this gap names as the
prerequisite — calls `token.grammarrole.rolebeginexpr`,
`token.grammarrole.roleprecedence` and `token.grammarrole.rolecompatonly`. The
callee is `lib/token/grammarrole.id`, GENERATED from `lib/compiler/token.id`,
which is the one owner `law.grammar.one` names. Those three applications were
`duo_fatal("unlowered native call")` under exit 0, then compile refusals, and
are now direct calls. The transfer artifact compiles, links, and answers the
owner's own tables:

    idol dump-c lib/compiler/token_view.id --lib   ->  0 aborts, cc -fsyntax-only clean
    len=6  canbeginexpr(1,2,3)=1,0,1  roleprecedence(4,5)=4,19

`beginexpr` is 1 at kind 0, 0 at kind 3 and 1 at kind 55; `precedence` is 4 at
kind 4 and 19 at kind 66. No source, relation, graph identity, or body was
invented: the answers are the generated table's, read through the projection
the token view declares.

The same route carries `lib/compiler/bind.id` calling the self-hosted Idol
lexer (`lib/compiler/lexer.id`, 1221 lines): `unresolved` answers 0 on a clean
source and 1 on one referencing an unbound name.

So an Idol parser recognition decision no longer has to choose between a
runtime abort and its own copy of the grammar facts, which was the choice
`law.grammar.one` forbids and the mechanical reason this slice could not start.

The executable evidence owner is:

    tools/node/dev/idol-lock -- sh gate/lower/fallback.sh

That gate runs three controls and both demonstrations before its corpus census.
Its planted `compiler.reader.read` application must realize, compile, LINK and
ANSWER; its local-only application must emit a non-empty artifact that
compiles; an application into a home that resolves to nothing must still refuse
with empty stdout. Every subject carries two pins — the abort count and whether
its artifact compiles — because a census whose every abort row reads zero
guards nothing on its own.

WHAT IS NOT CLOSED. Realization is a prerequisite, not the slice. Parser
recognition itself is still host-executed; `lib/compiler/parser.id`'s artifact
compiles 17 C errors short of linking, none of them cross-home; and no executed
Idol parser production boundary or compiler B exists.

C0 section 33 requires SHC slice 2 to consume the GENERATED constitutional
grammar. The current normative grammar cannot generate a parser:

- its opening says the parser is generated while section 6 says it is not;
- productions contain literal ellipses (`... per precedence`, `fn-slot...`);
- C0 records an unresolved contradiction (`primary drops .name;
  docs/spec/grammar.md and c0 must agree`); and
- the existing historical compiler parser/proof implements the migration
  `end`/`do`/`fun`/`local`/`type`/directive/semicolon s-expression grammar.

`scripts/treesitter_emit.id` explicitly says its structural productions are
not generated from the normative EBNF. It emits hand-authored registries,
retains an authored legacy residue, uses direct `io.open` plus `std.os`
environment/exit capability APIs, and cannot emit the offside scanner. It is a
useful partial editor projection, not the constitutional grammar authority or
an SHC parser generator.

Closure requires one complete machine-readable canonical Idol grammar
authority, generated human grammar and parser projections, a
positive/negative/fuzz differential, and zero handwritten semantic parsing
authority. Do not add Zig, another Markdown parser, or another generated-C
stage around the historical parser.

## Ownership transfer executed 2026-08-21 (measured)

The host role table is deleted. `lib/compiler/token.id` — the file that already
owned the KIND_* identities — now owns every grammar role, precedence and
associativity fact as executable Idol relations keyed by producer slot ordinal.
It emits both projections:

```text
lib/compiler/token.id            (ONE executable grammar-fact owner)
    → src/grammar_role_table.zig (generated host bridge, read by src/parser.zig)
    → lib/token/grammarrole.id   (generated Idol projection)
```

`src/grammar_role_gen.zig` (179 lines) and `src/emit_grammar_role.zig` (12
lines) are deleted; `src/grammar_roles.zig` fell from 353 lines to 221 and
holds no grammar fact — only accessors, doc, and the adversarial controls.
Hand-authored host grammar facts: 544 lines → 0.

**Equivalence.** The Idol owner regenerates the entire tracked 1554-line role
projection byte-for-byte. Only the banner and one dead line differ:
`grammar = req("std.compiler.token")` is deleted, because it was measured to
resolve to nothing (`req("std.…")` refuses `DNB001 assign-module-nonnative`;
`req("compiler.token")` resolves the module and then refuses `DNB011
unresolved-application-facts`).

**Counterfactual, both directions** (mirror of HEAD `31453966`, isolated
`ZIG_GLOBAL_CACHE_DIR` per arm, every binary verified on a trivial program
before use):

| arm | owner | `src/grammar_role_table.zig` | compiler sha256 | corpus 609 |
|-----|-------|------------------------------|-----------------|------------|
| P repaired/clean | intact | `.kw_return … body_start = true` | `5780c2bf…` | 546 pass / 63 refuse, 0 rows differ from HEAD |
| D damage | `KIND_RETURN` dropped from `rolebodystart` | `.{ .kind = .kw_return }` | `85457b71…` | 1 row differs (`examples/meta_implements.id`, different diagnostic and position) |
| R repair | restored | byte-identical to P | — | — |

`f = (x)` over `return x + 1` — the untyped-header shape — is **accepted** by
P and **refused** by D (`write \`<eof>\` at this token edge`). Repair used the
damaged compiler to run the repaired owner and reproduced both artifacts
byte-identically.

**What the tracked projection was worth before this.** `lib/token/grammarrole.id`
had zero verified consumers. Damaged (expression-start stripped from `name`,
`int_lit`, `float_lit`; every precedence and associativity zeroed):

- corpus differential over 609 tracked `.id`: **0 rows changed**, exit status
  and output;
- the former `scripts/grammarconvergence.id` was not convergence evidence: it
  only checked that two files read non-empty, compared zero facts, and is now
  deleted from the executable surface; Git ancestry retains the refutation;
- `tools/parity/grammar` prints *"parity: generated grammar role agrees with
  canonical token identity"* and exits **0** (it compares counts and slot 3);
- `lib/compiler/parser.id` / `lib/compiler/token_view.id` call
  `token.grammarrole.*`, but `idol check` accepts any dotted name —
  `token.grammarrole.totallynotarealrelation(1)` checks clean — so those
  consumers were never verified against the projection.

It was, however, **not stale**: regenerating from `src/grammar_roles.zig` at
HEAD reproduced it byte-for-byte. `lib/token/classify.id` is stale by 745 lines.

**Still not closure.** `docs/spec/grammar.md` does not generate the parser;
Tree-sitter remains a second authored grammar (`scripts/treesitter_emit.id`
does not run at HEAD — `DNB001 method-unresolved:read`); the parser's 29
kind→`BinOp` arms remain reconstruction debt.

### Refutation at the producer-only boundary

The counterfactual was run a second time with a much larger damage —
`KIND_INT_LIT` and `KIND_FLOAT_LIT` dropped from `rolebeginexpr`, so numeric
literals no longer open an expression. Regenerated, rebuilt (sha256
`dfef2366…`), swept: **0 of 609 corpus rows changed**, exit status and output.

The reason is not that the owner failed to reach production. It is that
`begin_expr` has no live consumer. Outside tests it is referenced twice:
`src/parser.zig:5441 is_expr_start`, which **no code calls**, and
`src/token_view.zig:29 View.canBeginExpression`, whose only caller is its own
test. `BEGINEXPR` is simultaneously the largest role family (24 members), the
one `tools/parity/grammar` uses as its drift probe, and dead.

Live families, by production call site (excluding tests and the unused view):
`descriptor` 14, `quoted` 7, `precedence`/`assoc` 3, `literal_kind` 3,
`lookup` 4, `body_start` 1, `pattern` 1, `compat_only` 1, `begin_expr` 0.

So "the grammar owner reaches production" was proven by the `body_start`
damage while "every role in the table reaches production" remained false.

### `begin_expr` now reaches one parser decision through the immutable view

Canonical `return` operand recognition now asks the generated
`View.canBeginExpression` observation over the producer token pack. The parser
does not pass a locally selected kind to the grammar table, and a missing view
fails closed at the lookahead. The dead parser-local `is_expr_start` wrapper is
deleted. Compatibility parsing keeps its admitted surface, but canonical Idol
fails closed when the owner does not admit the same-line token as a value start.

The first consumer also exposed a producer-domain defect instead of silently
moving that defect into the parser. The original fact omitted admitted
`function` / `fun`, subject-section, conditional and primitive-descriptor
starts, while claiming `[` and `,`, which the expression parser rejects. The
owner now publishes the exact admitted starts and keeps `compat_only` as an
independent fact: a token may begin an admitted compatibility expression
without making that spelling canonical Idol.

The counterfactual is executable on the whole compiler, not inferred from a
source grep. With only `.begin_expr` removed from the generated `int_lit` row,
the damaged ReleaseFast compiler (`8775dc23…`) refuses `return 99` at `99` with
`expected a return value or line boundary`. The repaired compiler
(`a066f060…`) checks and runs the same canonical program and prints `99`.
The parser module tests pin the admitted integer, function, subject-section,
conditional and primitive-descriptor identities, the independent
compatibility fact, and rejected `)` / `[` / `,` identities.

After routing this decision through the immutable view, the repaired
ReleaseFast compiler (`53234bfd…`) accepts the same `return 99` program. A
one-row damage to the generated bridge which removes only
`int_lit.begin_expr` produces compiler `7b0922c1…` and refuses at the integer
with `expected a return value or line boundary`. The focused missing-view
control bypasses `parse_module` and requires refusal rather than host-token
reconstruction; the complete Zig unit suite passes `1694/1694`.

The producer repair was selected by observation, not by making the new
consumer permissive. The first exact differential changed 44 of 944 subjects;
all were traced to admitted starts missing from the owner, led by
`return function`. After repairing that domain, 943 of 943 declared
legacy-equivalent subjects were byte-for-byte observation-identical between
the frozen base (`6c3a6d12…`) and repaired (`a066f060…`) compilers, with zero
infrastructure outcomes. `examples/shc/cwd.id` is excluded from that declared
set because the differential intentionally executes its two arms in distinct
private working directories and that program observes the directory itself.

### `pattern` now reaches match-arm discovery through the immutable view

`startsMatchArm` was the next bounded reconstruction. It saved the mutable host
lexer, selected a host `TokenKind`, asked a parser-local wrapper for the
generated pattern fact, scanned by mutating the lexer, and restored it. The
first transfer moved that scan to the immutable production view. The third
executed boundary above supersedes that intermediate state: `parser.id` `_clause`
now owns contextual word, pattern role, line, nesting, and separator recognition;
the Zig scanner and text comparisons are deleted. A missing view now causes
installation of the fail-closed producer pack, never host scanning.

The independent whole-compiler damage removes only `int_lit.pattern` from the
generated bridge. Repaired ReleaseFast compiler `6f3c831d…` checks a canonical
match containing consecutive integer arms. Damaged compiler `47be0641…` refuses
the same source at the second arm with `expected expression, got 'then'`.
Restoring the row restores acceptance. Focused tests require both cursor
immutability and fail-closed missing-view behavior; the complete Zig unit suite
passes `1696/1696`.

### `body_start` now reaches the header body decision through the immutable view

The untyped-header recognizer's last decision — may the token after `)` begin a
callable body — was the one production `canStartBody` consumer, and it selected
a host `TokenKind` tag from the view token it already held
(`grammar_roles.canStartBody(after.kind)` inside `headerSignal`). It now asks
the view at the same ordinal (`view.canStartBody(idx)` in `src/parser.zig`
`headerSignal`; accessor `View.canStartBody` in `src/token_view.zig`). No host
tag is selected at the site, and an ordinal past the producer pack answers
false rather than reconstructing a role.

Chosen as the smallest remaining class: among the live role families,
`body_start` had exactly one production call site, and that site already stood
on the immutable view with the ordinal in hand — the transfer deletes the
site's host-tag selection and nothing else.

Counterfactual, executable on the whole compiler at `fc48acea` plus this diff
(debug builds, this Linux host has no direct native realization so the
observation is `idol check`): the repaired compiler (`3a3eb095…`) checks

```id
f = (x)
    return x + 1
print(f(3))
```

clean. With only `.body_start` dropped from the generated `kw_return` row of
`src/grammar_role_table.zig`, the rebuilt compiler (`0fb67f6c…`) refuses the
same source with ``write `<eof>` at this token edge`` — the untyped header
stops being recognized because the owner no longer admits `return` as a body
start. Restoring the row restores acceptance and leaves the tracked table
byte-identical.

Movement measured against a pristine `git archive` mirror of the same HEAD:
`zig build` and `zig build unit-test` exit 0; `sh gate/gap-145-consumer.sh`
PASS (14 checks) with counters identical on both arms (21 blind quoted arms of
36, ceiling 21; 88 tag-test sites, ceiling 88 — no rise); an `idol check`
differential over 812 tracked `examples/` and `lib/` subjects between the
mirror's compiler and this one changed 0 rows. `gate/grammar-projection.sh`
exits 1 on BOTH arms on this host — `DNB004`, the direct backend has no native
realization for this Linux target, so the owner cannot compile from its root
here; that red measures the environment, not this diff.

These are three live parser lookahead edges, not grammar or parser closure.
Other parser decisions still reconstruct roles locally, the generated Idol
projection still has no verified Idol parser consumer, and
`docs/spec/grammar.md` still does not generate the parser. The next host
boundary by name: `Parser.demandsOperand` and `Parser.opensLineAndExpression`
(`src/parser.zig`), which still select a host tag from the mutable cursor
rather than asking the view at an ordinal; after those, the `descriptor`
family, the largest. The next reduction is
another bounded host recognition decision with an independently damaged owner
fact, never a new parser-local token list.

## Relation ontology transferred; Tree-sitter measured (base c77dea60)

Every number in this section was taken on `c77dea60` unless it names another
base. The lane before it re-measured on the moved base and found the operation
census unchanged from `ce03c7eb` except that the new base deleted
`Parser.parse_unary`, one of three verbatim copies of the same unary switch.

### The second operation ontology is gone

Before, on `c77dea60`:

| site | map | arms |
|---|---|---|
| `src/parser.zig` `infixBinOp` | `TokenKind` -> `ast.BinOp` | 24 |
| `src/parser.zig` `peek_glued_assign` | `TokenKind` -> `ast.BinOp` | 5 |
| `src/parser.zig` `compound_assign_op` | `TokenKind` -> `ast.BinOp` | 6 |
| `src/parser.zig` unary switch x2 | `TokenKind` -> `ast.UnOp` | 10 |
| `src/pretty.zig` `tokenFor` | `ast.BinOp` -> `TokenKind` | 24 |
| **total** | | **69** |

After: **0**. `lib/compiler/token.id` owns `_relationname` and `_unaryname` per
producer slot and three FACE facts — `_roleinfix`, `_roleupdate`, `_roleglue` —
that say WHERE a relation is applied and never WHICH one. `a + b`, `a += b` and
the adjacency-glued `a >>= b` are three faces reading one row: `plus` and
`plus_assign` share the relation `add`, and `rshift`'s glued face reads
`rshift`'s own relation.

The owner also owns the ONTOLOGY. `_relationorder` and `_prefixorder` generate
`Relation` and `Prefix` into `src/grammar_role_table.zig`, and `src/ast.zig`
ALIASES them. `gate/layers.manifest` says the AST layer "owns shape. Owns NO
semantic relation identity", and `gate/layering.sh` rejected the first attempt
— a `TOKEN -> AST` import edge — which is what moved the enums to the layer
that should have held them. The generated enum is member-for-member and
order-for-order the enum it replaced.

`pretty.tokenFor` is now the COMPILE-TIME inverse of the same fact. Totality
and injectivity are `@compileError`s, so an operator the owner names no token
for, or two identities claiming one relation, is a build failure. The test that
existed only to check two hand-written maps still agreed is now a witness that
one derivation composes to the identity.

Two more parser token lists moved: `demandsOperand` (48 identities — the exact
dual of `rolebeginexpr`, and not derivable from the infix and prefix faces
because `(`, `[`, `,`, `:`, `->`, `=>`, `if`, `while`, `until`, `return` and
`by` demand an operand without applying a relation) and
`opensLineAndExpression` (3 identities — those with both an infix and a
line-leading reading). Both are owner facts with controls; the 48 is pinned by
a counting control.

### Tree-sitter: a PROOF GATE, not generation

This is stated precisely because the two are different outcomes.
`ext/tree-sitter-idol/grammar.js` is still AUTHORED. Nothing here generates it.

`gate/treesitter.id` (run by `gate/treesitter-agreement.sh`, `zig build
treesitter-agreement`) reads three files and restates no fact from any of them:
the owner's generated role table, `src/lexer.zig`'s spelling map (the one
source-spelling producer; keyword spellings derive from the identity name), and
`grammar.js`. It joins on token identity and compares binding order PAIRWISE,
because the two ladders use different scales and only the induced order is a
shared fact.

**SIXTEEN divergences, measured:**

- 7 comparisons (`in < > == != <= >=`) the owner calls `nonassoc` and
  tree-sitter calls `left` — the editor grammar accepts `a < b < c`, which the
  compiler refuses;
- `..` is right-associative for the owner and left for tree-sitter;
- infix `@` (matmul) exists for the compiler and is absent from the editor;
- `~=` is recognised by the editor as an operator the owner does not own;
- 6 induced-order disagreements, all `..` and `~` in the wrong band.

Fail-closed on both edges: a divergence not in `gate/treesitter/agreement.baseline`
fails, and a pinned line that no longer describes a divergence fails, so the
baseline is a ratchet and not a budget. Counterfactual: dropping `plus` from
precedence 17 to 12 in the generated bridge produces four new keys and exit 1;
restoring returns exit 0. The wrapper positive-controls the reader — an owner
table with no rows must exit 3, not report agreement, which is exactly how
`scripts/grammarconvergence.id` came to exit 0 on a fully damaged owner.

### Why generation was not reachable, measured

`scripts/treesitter_emit.id` DOES NOT RUN, for two independent reasons:

1. `f:read("a")` refuses `DNB001 method-unresolved:read`. Repairing `slurp` to
   the working `path:read()` face advances it exactly one step, to
   `DNB001 method-unresolved:split` — six `:split` sites at lines 105, 312,
   1419, 1426, 1530, 1602, 1621. `:find` is also unavailable
   (`DNB011 unknown relation: find`). The working string subset is
   `:read :len :byte :sub`.
2. Its operator ladder is cross-checked against a numbered precedence section
   of `docs/spec/grammar.md` that NO LONGER EXISTS. `specrows()` counts lines
   beginning with a digit; `grammar.md` has none, so `report()` would refuse
   with "docs/spec/grammar.md yielded 0 precedence rows" even once it lowered.

So the generator is dead twice over, and its `spell` / `level` / `spec` /
`assoc` rows (24 binary, 6 unary) are the second grammar authority the gate now
measures. A THIRD authority is implied by that cross-check: prose in
`docs/spec/grammar.md`.

### Ordered remaining work to close GAP-134

1. **Restore the generator's ability to execute.** Replace the six `:split`
   sites and any `:find` in `scripts/treesitter_emit.id` with byte scans over
   the working subset, or land the `:split`/`:find` lowering. Until this runs,
   `zig build treesitter-projection` proves nothing and grammar.js cannot be
   regenerated at all.
2. **Delete the `docs/spec/grammar.md` §2 cross-check.** It compares against a
   section that does not exist. Replace it with a comparison against the one
   grammar owner — the facts `gate/treesitter.id` already reads.
3. **Derive the tree-sitter operator ladder from the owner.** The owner emits
   the level/associativity rows; the generator formats them. This drives
   `gate/treesitter.baseline` from 16 divergences toward 0 and turns the proof
   gate into a generation check. Note that `@` and the nonassoc comparisons
   change the emitted grammar, so `ext/tree-sitter-idol/src/grammar.json` must
   be regenerated with the tree-sitter CLI in the same diff.
4. **Give the source-spelling fact one producer.** `src/lexer.zig` `spelling()`
   and `scripts/treesitter_emit.id` `spell()` are two. This is `GAP-145`'s
   lexical-identity work and should not be duplicated here; the proof gate
   already reads only the `lexer.zig` one.
5. **Make `docs/spec/grammar.md` a generated projection of the owner.** It is
   the remaining hand-written grammar document and the one this gap's title
   names. Nothing above closes that.
6. **Then, and only then, production parsing as a stage.** Still host-owned;
   untouched by this lane and owned elsewhere.

### Two authority conflicts observed on c77dea60, not routed around

- `gate/vocabulary.sh` (new on this base) refuses every new NON-PRIVATE
  module-scope declaration and states that no word-list edit can waive it;
  `gate/idiom.id` rejects the leading underscore that is the only remaining
  option, under LAW-ONE and `law.path.name`. The same diff measures 469 idiom
  findings underscore-private and 20 with public names, so 449 of them are
  attributable to the underscore alone. `vocabulary.sh` is a commit blocker via
  `gate/admission-all.sh` and idiom is the manual preflight, so only one arm is
  committable.
- `gate/idiom.id` did not RUN on `ce03c7eb` at all: it checks clean and then
  refuses to lower with `DNB011 relation: dot__std` from `"std." .. world`,
  regardless of input. It runs on `c77dea60`. The lane instruction to preflight
  every `.id` diff through it was unsatisfiable on the earlier base.

## Review round, rebased onto `debad1df`

### The proof gate was proving the wrong pair agree — and the fix landed elsewhere

The first version of this gate joined the editor grammar to the compiler
through `src/lexer.zig`'s `spelling()` switch. That is the HOST DIFFERENTIAL
ORACLE, not the production authority: had `lib/compiler/lexer.id` changed an
operator spelling without the oracle following, the gate would have reported
AGREEMENT while the production compiler and the editor actually disagreed. A
gate that can be green while the thing it guards is broken is worse than no
gate. The finding was correct.

The repair landed on main from the `GAP-145` lane while this one was in review:
`lib/compiler/token.id` now publishes the source spelling per producer slot and
`TokenKind.spelling()` is one field read off the generated row. Two host tables
— a 60-arm switch and `token_semantic.keywords` — stopped being producers.

This gate therefore reads `src/grammar_role_table.zig`: one row per producer
slot carrying identity, spelling, infix face, precedence and associativity
TOGETHER, so no join can pair one identity's spelling with another's binding
power, and `src/lexer.zig` answers out of the same row. Reading a generated
`.zig` file is an act of reading the owner precisely because
`gate/grammar-projection.sh` regenerates privately and fails unless the tracked
bytes are identical.

### The ratchet ratcheted, and named what it dropped

Rebasing onto `debad1df` picked up upstream work on the editor grammar. The
gate immediately reported SEVEN of its sixteen pins as STALE, by name, and
refused to pass on the old count:

```text
STALE   assoc .. right left
STALE   order & ..      order | ~      order | ..
STALE   order ~ ..      order .. <<    order .. >>
```

`..` and `~` had been moved into the owner's band upstream. The baseline is now
NINE, and that number was produced by the gate rather than asserted:

- 7 comparisons (`in < > == != <= >=`) the owner calls `nonassoc` and
  tree-sitter calls `left` — the editor accepts `a < b < c`, the compiler
  refuses it, and tree-sitter can express the difference with a plain
  `prec(n, …)` that leaves the conflict unresolved;
- infix `@` (matmul) exists for the compiler and not for the editor;
- `~=` is recognised by the editor as an operator the owner does not own —
  `lib/compiler/lexer.id` refuses to glue it ("`~` is XOR and nothing else").

Three counterfactuals, all executed: `precedence[plus]` 17 → 12 in the
generated bridge gives 4 NEW keys and exit 1; a pin describing no divergence is
reported STALE and exits 1; an owner bridge with no rows exits 3 rather than
reporting agreement.

The comparison also stopped running twice. It had traversed once to print and
once to count, under a `want` selector; it now returns the divergence KEY SET
from one traversal and the caller derives both totals, so the gate cannot
disagree with itself.

### The deadlock had a lawful door, and it is not the one GAP-224 names

The earlier report here concluded that a new module-scope relation could not
be added to a `.id` file at all. That was wrong, and the escalation produced
the correction. But the door is not the one `GAP-224` describes.

**`name(...) = (...)` is not Idol.** All four spellings of it are refused by
the parser, not merely unseen by the classifier:

```text
pick(row, kind) = (row)        error: expected expression, got '='
pick(row, kind) = row          error: expected expression, got '='
add(a: i64, b: i64) = a + b    error: expected expression, got '='
add(a, b) = a + b              error: expected expression, got '='
```

`gate/vocab-extract.awk` misses that string because it is not a declaration
form, not because it is the canonical one. The four forms that DO compile and
run are all classified and therefore all refused:

| form | compiles | classifier |
|---|---|---|
| `brk(text: str, from: i64)` | runs | `relation` — refused |
| `brk(text: str, from: i64): i64` | runs | `relation` — refused |
| `brk = (text: str, from: i64)` | runs | `relation` — refused |
| `brk: i64 = (text: str, from: i64)` | runs | `relation` — refused |
| `brk(text, from)` | check fails | free |
| `brk(text, from): i64` | `DNB001 param-type:inferred` | free |

**The real door is the ZERO-PARAMETER relation.** `check(): i64` compiles,
runs, composes with other zero-parameter relations, carries no underscore, and
is not classified — the classifier's two parenthesised patterns both require a
`:` INSIDE the parens, which a zero-parameter list cannot have. It is a real
canonical form on its own merits, not a spelling chosen to slip past a regex.

So `gate/treesitter/agreement.id` is now six zero-parameter relations —
`editor`, `joined`, `keys`, `loose`, `stale`, `held`, `rows`, `check` — and the
parameterised primitives (substring search, line walk, field split, numeric
scan) are written out where they are used. `lib/compiler/token.id` `zigrow`
carries the same duplication for the same reason. The two owner facts this lane
added, `roledemand` and `roleopensline`, took the same shape: `demand(): str`
and `lead(): str` return one character per producer slot, and the generated
Idol projection carries them as the same two zero-parameter relations rather
than as a column-1 table plus an accessor.

Measured on this lane's diff, `idol run gate/idiom.id`:

| naming | findings |
|---|---|
| underscore-private, against `c77dea60` | 480 |
| underscore-private, against `debad1df` | 288 |
| zero-parameter relations | 121 |
| … with the owner facts converted too | 81 |
| … with `law.update.face` and `law.sentinel.zero` cleared | 12 |
| … final | **0** |

The last steps were ordinary canonical repairs the underscore had been hiding:
`k = k + 1` became `k += 1`; a search that reported "not found" as a zero index
now reports a `bool`; `lvl = lvl * 10 + d` became `lvl *= 10` then `lvl += d`;
and a five-way byte comparison became `row:sub(k, k + 4) == "prec."`, which is
both clearer and not a `== 114` that a substring scanner reads as `== 1`.
Two `rolebit` arms fell out as dead when the two tables became relations.

**`GAP-224` should be re-stated.** The freeze does fail open, but on a string
that is not a declaration. What it refuses is every parameterised declaration
form the language actually has, which is a fail-CLOSED that has no lawful
escape except dropping the parameters. Duplication at each call site is the
price, and `lib/compiler/token.id` was already paying it before this lane.

### Path law

`gate/treesitter-agreement.sh` was kebab-case, which `law.path.name` forbids.
Decomposed to `gate/treesitter/agreement.{id,sh,baseline}`, which `gate/all.sh`
auto-enrols through its `gate/*/*.sh` glob. Measured with `gate/path.id` over
`git ls-files`: **2296 findings before, 2294 after** — the drop is exactly the
two the kebab path contributed, and the three new paths contribute zero.

## The parser slice cannot start, and the reason is mechanical (2026-08-23, `dec7d509`)

This lane was sent to transfer one production parser recognition decision into
Idol. It did not, and the reason is not the written verdict in `GAP-145` — it
is a measurement that verdict did not name. Every number below is the output of
the command beside it, taken at `dec7d509` with a debug `./zig-out/bin/idol`.

### The transfer route replaces cross-module bodies with a silent abort

`idol dump-c <file> --lib` is not one recipe among several. It is THE recipe
that produced this tree's only executed Idol authority: `tools/node/dev/lexer/
artifact` runs exactly that command on `lib/compiler/lexer.id`, and the C it
emits is `src/lexer_tokenize.c`, which `build.zig` links into the compiler.

On that route, a qualified call whose callee body is not at file scope is
lowered to a runtime abort, and the command reports success:

    ./zig-out/bin/idol dump-c lib/compiler/token_view.id --lib > tv.c
    → EXIT=0, stderr EMPTY, 8852 bytes

    bool canbeginexpr(duo_rec_… *pack, int64_t index) {
        return duo_fatal("unlowered native call");
    }
    int64_t roleprecedence(…) { return duo_fatal("unlowered native call"); }
    bool rolecompatonly(…)   { return duo_fatal("unlowered native call"); }

The emit site is `src/codegen.zig:17698`, in the `!moduleNeedsLuaRuntime()`
arm. `kindat` and `peek` in the same file lower correctly, because their calls
are file-scope local. The three that do not are exactly the three that read
`token.grammarrole.*`.

So `lib/compiler/token_view.id` — the immutable token view this gap names as
the parser-slice prerequisite, and which `.agents/RELEASE_READINESS.md` lists
as item 3 — compiles to a library in which every grammar-role observation
aborts. `evidence/mop/objects.tsv:20` already recorded that this unit refuses
under `run`; what was not recorded is that under the `--lib` transfer route it
does not refuse, it succeeds and emits the abort.

### The one grammar owner is unreachable from Idol, in both directions

    ./zig-out/bin/idol check tmp/probe/role.id     → ✓ checked — no errors
    ./zig-out/bin/idol run   tmp/probe/role.id
    → Undefined symbols for architecture arm64:
        "_idol_token_grammarrole__rolebeginexpr", referenced from:
          _idol_tmp_probe_role__main
      error: native linker failed (exit 1)

for a three-line program whose whole body is
`token.grammarrole.rolebeginexpr(1)`. The graph RESOLVES the relation — it
emits a mangled reference to it — and nothing ever produces that symbol.

The negative control separates two failures this gap previously reported as
one. `idol check` accepting `token.grammarrole.totallynotarealrelation(1)` is
recorded above and still holds; but the two are distinguishable at `run`:

| call | `check` | `run` |
|---|---|---|
| `token.grammarrole.rolebeginexpr(1)` | clean | LINK: undefined `_idol_token_grammarrole__rolebeginexpr` |
| `token.grammarrole.totallynotarealrelation(1)` | clean | `DNB011 unresolved-application-facts`, producer graph |

And the owner cannot traverse the route at all:

    ./zig-out/bin/idol dump-c lib/compiler/token.id --lib
    → error: codegen refused a binding whose value it cannot represent

`lib/compiler/token.id` therefore executes only as a build-time GENERATOR of
`src/grammar_role_table.zig` and `lib/token/grammarrole.id`. Production reads
its TABLES. No relation it declares has ever executed inside the compiler.

### It is not specific to the grammar owner

Every cross-module `.id` → `.id` relation call measured on this route behaves
the same way, including a call to the one executed Idol authority:

    lib/compiler/bind.id:65   lx: lexer = lexer.new(src, file, family)
    → duo_rec_… lx = duo_fatal("unlowered native call");

The Idol lexer cannot be called from Idol. A parser slice cannot obtain its
token stream from the producer that owns it; it would have to receive the pack
back across the host C ABI.

The refusal machinery exists and is correct one arm over — an UNKNOWN module
refuses (`lib/compiler/emit_c.id` → "codegen refused a reference to a module it
cannot find"), while a KNOWN module with a non-local body aborts silently. That
asymmetry, not a missing capability, is what is being pinned.

### Census over the transfer surface

`sh gate/lower/fallback.sh`, 21 subjects, ~1s:

| subject | silent aborts |
|---|---|
| `lib/compiler/parser.id` | **291** |
| `lib/compiler/bind.id` | 17 |
| `lib/compiler/graph.id` | 5 |
| `lib/compiler/rewrite.id` | 3 |
| `lib/compiler/token_view.id` | 3 |
| `lib/compiler/lexer.id` | **0** |
| `lib/token/grammarrole.id` | 0 |
| `lib/compiler/{token,symbol,emit,arm64check}` | REFUSED (fail closed) |

**319 abort sites over 5 subjects.** `lexer.id` measures 0 for the reason it is
the only transfer that ever succeeded: 1221 lines with no cross-module call.
`lib/compiler/parser.id` measures 291, which settles its status — it is a
1917-line shadow authority documented against `docs/GRAMMAR_SPEC.md` in
`end`/`fun`/`local` dialect, and it could not execute if it were adopted.

### The executed Idol surface, exactly

    git grep -n 'extern fn' -- src/*.zig | grep -E 'duo_|idol_'
    src/keyword_bridge.zig:6   duo_keyword_classify
    src/lexer_dispatch.zig:39  duo_lexer_tokenize_full
    src/lexer_dispatch.zig:51  duo_lexer_error_line

    build.zig:15  addCSourceFile src/keyword_classify.c
    build.zig:27  addCSourceFile src/lexer_tokenize.c

Two C objects are linked into the compiler. `keyword_classify.c` is generated
from `src/token_classify_gen.zig` — the HOST, and `GAP-145` O4's second keyword
producer — so it is not Idol authority. **The executed Idol authority in this
compiler is two exported symbols from one self-contained file.** That is S0,
stated as a symbol count rather than a file percentage.

### Ordered, and this list is now ahead of the old item 6

1. **Make the transfer route fail closed** (`law.fallback.zero`).
   `src/codegen.zig:17698` must refuse a resolved-but-non-local callee the way
   the unknown-module arm already refuses, instead of emitting
   `duo_fatal("unlowered native call")` under EXIT 0. Until this lands, no
   transfer larger than one self-contained file can be trusted, and a green
   `dump-c` proves nothing about the artifact it produced.
2. **Give cross-module relation calls a physical realization.** Either emit the
   callee alongside the caller under `--lib`, or link the projections. Without
   one of these, an Idol consumer of `token.grammarrole.*` cannot exist, and
   the only way to write a parser recognition decision in Idol is to inline the
   grammar facts — the fourth grammar authority `law.grammar.one` forbids and
   this gap exists to prevent.
3. Then items 1–5 of the previous list (generator, spec cross-check, ladder
   derivation, one spelling producer, `grammar.md` as projection).
4. Then, and only then, production parsing as a stage.

Ratchet: `gate/lower/fallback.sh` + `gate/lower/fallback.baseline`, on
`zig build test` and `zig build lower-fallback`. Its controls run before it
reports: a planted cross-module call must be COUNTED and a local-only file must
measure ZERO, or it exits 3 rather than agreeing with the tree. Rise, fall,
unpinned subject, stale pin and class change all fail; all five were executed.

**Which compiler the pins belong to** (`law.evidence.subject.one`). Build mode
is not the sensitive axis: debug and ReleaseFast builds of `dec7d509` reproduce
all 21 rows exactly. REVISION is. Pointed at the then-tracked, stale
`out/bin/idol` (since deleted; the demonstration survives only in git history)
the gate reported

    CLASS CHANGED lib/compiler/parser.id pinned 291, measured REFUSED

and fails, which is the correct answer — that binary predates `authority` and
refuses `parser.id` at `prev_kind` before it reaches lowering. The run now
prints the compiler it measured with, so a stale binary is distinguishable from
a regression.

## Superseded observations

The `idol.gap.frontier.v1` block at the top of this file is the authority on
which observations this gap declares historical, and `gate/frontier.sh` requires
every anchor it lists to resolve. This section is the PHYSICAL grouping of the
ones that are purely dated measurement, so that reading top-down no longer hands
a fossil to someone looking for the blocker. The two lists are not identical and
do not need to be: a section can be listed above and still stand here, or stand
above this boundary while being listed, when it carries a standing ruling or the
most recent evidence as well as a dated measurement.

The five sections below are dated measurements from 2026-08-10 through
2026-08-13, and they are the reason this gap needed a frontier section at all:
read top-down they describe a transitional host grammar owner
(`src/grammar_roles.zig`), surviving parser `BinOp` maps, and a next parser
slice IMPLEMENTATION-BLOCKED on facts that now exist. All of that was true when
written and none of it is true at HEAD -- the ownership transfer above replaced
it, and the relation-ontology transfer after that took the operation maps from
69 arms to 0.

They are preserved because this repository keeps research by ancestry, and
because two things in them are still LAW rather than measurement: the
`law.grammar.one` ONE-PRODUCER prohibition on parallel grammar authorities, and
the list of adversarial controls closure requires. Read them for those. Do not
read them for the state of the tree, and do not implement from them.

## Production parser boundary observed 2026-08-10

The production lexer now supplies token content, token identity, and exact
source spans from Idol. The next host decision for canonical callable bindings
is still `src/parser.zig:scan_func_header_signal`: it walks the token stream and
locally reconstructs parameter, operator, body-start, and compatibility roles.

Moving that recognizer verbatim into `lib/compiler/parser.id` would not
close this gap. It would create a fourth grammar authority. A rejected scratch
candidate demonstrated the failure shape directly: mutable save/scan/restore,
punctuation and keyword comparisons through token text, and a handwritten
expression-start list. None of that candidate entered the working tree.

The authoritative inputs currently stop one level too early:

- historical `lib/compiler/token.id` carries numeric token identities but
  no canonical grammar-role projection;
- `lib/token/classify.id` is a generated lexical classifier only;
- `src/token_semantic.zig` is host-owned keyword metadata and does not expose
  parameter, expression-start, prefix, postfix, precedence, associativity, or
  canonical/compatibility grammar facts; and
- `docs/spec/grammar.md` is normative prose and explicitly says the parser is
  not generated from it.

The minimum interface required by the next SHC parser slice is therefore:

1. a machine-readable canonical Idol token/case identity projection;
2. grammar-derived roles for parameter slots, callable body starts, prefix and
   postfix positions, precedence, associativity, and compatibility status;
3. an immutable token view over the production Idol token pack so lookahead is
   observation rather than save/mutate/restore; and
4. a compact generated lookup representation suitable for the parser hot path.

Only after those facts exist can the production parser replace one host
recognition decision without reconstructing grammar from token text or
introducing a bootstrap parser taxonomy.

## Fail-closed lexer prerequisite

The direct production lexer route no longer treats allocation or record-buffer
failure as permission to resume the host scanner. Every route failure now
propagates, and partially allocated route storage is released. A focused
control exercises the production routing function with a failing allocator,
and the common test aggregator imports that control. This closes the hidden
fallback at the routing seam; it does not close this gap. Code-generation
callers still convert route errors into declined embed or optimization paths,
so production-wide failure propagation remains an adjacent realization-owned
deletion gate.

The next parser slice remains `IMPLEMENTATION-BLOCKED` on the same two upstream
facts: the generated grammar-role projection and an immutable view over the
production token pack. `GAP-145` must first or jointly distinguish current text,
bytes, compatibility literal/comment, shebang, and reserved-token identities.
Hand-authoring those roles in the parser would recreate the authority this gap
exists to remove.

New production parser fixtures use canonical `.id`. Historical `.id` remains
source-family provenance only and cannot select a grammar or semantic result.
The generated role projection must also preserve current delimiter authority:
parentheses identify ordinary application/grouping, braces structured packs
and descriptor application/homes, brackets computed projection, dot static
projection, and colon only its admitted descriptor/subject/home roles. These
faces remain syntax provenance after resolution and contribute zero semantic
identity bits.

## Current evidence (2026-08-13)

`src/grammar_roles.zig` has one row per `TokenKind` (ROLECOUNT 114, KINDEOF
109), including shebang/comment trivia, quoted identities, `body_start`, and
infix associativity. `lib/token/grammarrole.id` is regenerated from that
table. Parser consumers now observe roles rather than handwritten token
lists for expression-start, callable-body start, infix-in-header,
literal/quoted kinds, prefix unary, match-pattern start (`pattern` /
`canStartPattern` / generated `rolepattern`), parenless call args,
table field continuation, and primitive descriptor identities
(`isDescriptor` — host `isTypeKeyword` and `typeKeywordName` deleted;
type-primary and type-name expression arms observe the role). Adding a
quoted, descriptor, or pattern identity updates the role table, not those
parser switches. Host `is_keyword_token(text)` is deleted — keywords are
token identities; a `.name` is never a keyword.
The immutable token view remains observation over the production pack.
This is **not** parser SHC and **not** closure:
`docs/spec/grammar.md` still does not generate the parser.
Header recognition is one `headerSignal` over the producer token pack
(`parse_module` / `scan_func_header_signal` call `route()` when the pack
is missing). The host save/scan/restore snapshot walk is deleted. That is
not parser SHC and not a port of the host recognizer into `parser.id`. Pratt `infix_prec` now takes left/right from generated
`precedence`/`assoc`
(comparisons are `nonassoc`, so `a < b < c` stays non-associative). The
parser still maps kinds to `BinOp` — that is AST realization, not a
second precedence table.

`zig test src/grammar_roles.zig -fno-emit-bin` covers identity-not-spelling
and trivia-not-expression-start. Generated `rolebodystart` exists. The
production parser still owns remaining recognition.

**GRAMMAR-ONE (`law.grammar.one`):** exactly one executable grammar-fact
owner. Canonical Idol grammar facts are the authority. Generated Zig/C
tables are a bridge. `grammar.md` and Tree-sitter are human/editor
projections from that owner. `src/grammar_roles.zig` is the **current
transitional** host table and has a deletion condition once Idol owns the
facts. Do not add `lib/compiler/grammar.id` until it *replaces* the Zig
table as the one executable owner. Parser-local `BinOp` maps remain
reconstruction debt. Tree-sitter, LSP, MCP, and formatter must consume the
same owner — never a parallel grammar.

Required adversarial controls for closure:

- token spelling changes do not affect role lookup when token identity is held;
- token identity changes do affect recognition when spelling is held;
- canonical and compatibility faces remain distinct facts;
- adding an admitted expression-start case changes the grammar owner and its
  generated projection, not parser consumers; and
- punctuation and hot-path category recognition perform no text search.

## ONE-PRODUCER ruling (`law.grammar.one`)

Exactly one executable grammar-fact owner must produce every grammar role,
precedence, and associativity fact. Current transitional owner:

```text
src/grammar_roles.zig  (host executable table — bridge, law.bridge.death)
    → lib/token/grammarrole.id (generated projection)
    → parser / Pratt observation
```

Forbidden parallel authorities:

- C0 prose alone (`docs/spec/grammar.md` without generation),
- a future `lib/compiler/grammar.id` that coexists with the Zig table,
- parser-local `BinOp` / token-text / spelling-category maps,
- Tree-sitter hand-authored grammar as a second normative source.

Destination (one producer):

```text
canonical Idol grammar facts (single owner)
    → generated Zig/C role tables
    → grammar.md (human projection)
    → Tree-sitter (editor projection, mechanically verified)
```

When an Idol grammar-fact owner lands, it **replaces** `grammar_roles.zig` as
the one executable owner; the Zig table becomes generated bridge only until
deleted. Do not add `grammar.id` beside the Zig table — migrate ownership.

## Implementation blockers (authority: docs/spec/law.md only)

Normative law lives in `docs/spec/law.md` §4–§9 and the structured expansion in
`docs/spec/constitution.md` §5–§6. This gap reports **implementation state only**.
Do not treat this section as a second constitution.

**Status:** IMPLEMENTATION-BLOCKED on grammar-role ownership (this gap) and lexical
identity closure (`GAP-145`). The compact law and delimiter closure are reconciled;
the host frontend has not caught up.

| Surface | Law reference | Host state (measured) |
|---------|---------------|---------------------|
| World `@` faces, injection `@{…}`, `@member` access | `law.md` §4–§6 | Lexical gate admits faces; resolver/lowering incomplete |
| Delimiter closure: `f(x)`, `table[key]`, `table[key]=v` | `law.md` §5 | `access` in constitution §6 matches; table_apply converged |
| Descriptor `{ x: t }` vs injection `@{}` | `law.md` §5, `law.injection.only` | Host still rejects plain `{ x: f64, … }` descriptor packs |
| `@.` / `@:` retired; `@member` canonical | `law.md` §4, `law.at.one` | `gate/idiom.id` rejects; anchor corpus still host-parsed |
| One application algebra; no `method_call` kingdom | `law.md` §9 | `src/parser.zig` / `dnir_lower.zig` still split `.call` / `.method_call` |

Retired corpus (`examples/anchor/*`, `@{}`-descriptor specimens listed in Git
history) stays frozen until the grammar-role owner lands — do not rewrite line by
line against current `idol check`.

Each of these must land as exact graph facts (world membership, application
relation/subject/place, protocol-requirement witness) — never as source-text
reconstruction, a directive zoo, or a runtime protocol object.
