# GAP-140 — CLI advertises selfhost authority that has no dispatch

**Status:** CLOSED · **Filed:** !2026-08-10T02:08:49Z · **Closed:** !2026-08-10T10:32:18Z
**Priority:** P0 · **Kind:** agent
**Filed by:** codex-root
**Files:** src/main.zig,lib/compiler/bootstrap.duo

`src/main.zig` admits `selfhost` as a known command and prints `selfhost <sub> (manifest|targets|verify|proof|stage|...)`, but has no `cmd == selfhost` dispatch before the generic project-input path. Current `./zig-out/bin/duo selfhost matrix` reports no build source while still printing the advertised entry. This is a false public proof surface and prevents one authoritative B→C status command. Do not add another Zig selfhost kingdom: retire the false usage entry now, then restore only as a Duon-owned projection over real graph/world/bootstrap evidence.

## Closure

The false command was deleted from both the help projection and the recognized
command set. No replacement command or Zig self-hosting subsystem was added.
`duo selfhost matrix` now follows the ordinary project-input path and reports
that no build source exists; `duo --help` retains a real neighboring command and
contains no `selfhost` entry.

Focused evidence:

```text
zig fmt --check src/main.zig
git diff --check -- src/main.zig gaps/GAP-140.md
zig build
./zig-out/bin/duo --help
./zig-out/bin/duo selfhost matrix
```

The locked MCP build endpoint returned exit 1 without diagnostics, while the
same current source built successfully under an explicit emergency atomic lock.
That fallback establishes the source build only; it does not repair the MCP
evidence defect or count as a canonical coordination interface. The locked
unit-test gate passed. The locked `agent-smoke` gate remained red; a serialized
diagnostic run attributed the failure to the existing
`examples/std_metaprogramming_modules_smoke.duo` baseline, outside this slice.
