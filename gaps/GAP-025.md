# GAP-025 — the epoch-2 surface has no implementation yet

**Status:** open · **Filed:** 2026-08-07 (epoch 2, Pass 93)
**Rules cited:** Pass 93 §0.1 (one lowercase word), §0.2 (no directives),
Pass 90 (no `_` prefix), Pass 91 (no interior `_`)

Filed under §6 QUERY-FIRST — *never invent; no answer = spec gap = report it.*
This is a gap report, not a refusal: no rule from the conflict table's OLD
column is being cited, and the canonical spellings are the target.

## The blocker: the repair direction does not compile

| epoch-2 spelling | in the compiler today |
| --- | --- |
| `check(c)` | no binding |
| `why(q)(subject)` | no binding |
| `graph.descriptors` / `graph.modules` | no binding |
| `add(module)(…)` family | no binding |
| `todo(gap[23])` | no binding; `gap` is not a trie |
| no prefix `@` | `@c.export` is the ONLY export mechanism implemented — every export site in `lib/std/compiler/lexer.duo` and the SH-03 artifact rests on it |

Emitting the canonical spellings today produces source the compiler rejects, so
the repair is currently unreachable rather than merely unapplied.

## Second obstruction: `_` privacy is load-bearing, not decoration

- `lib/std/compiler/graph.duo` — `dead_private`'s entire query is *"a
  `_`-prefixed binding with in-degree 0 is unreachable"*. The asymmetry against
  public bindings IS the query.
- `scripts/module_surface_gate.duo` — enforces that nothing outside a file
  reaches a `_` name.

Both pass today. Pass 90's own decomposition names the successors (protocol
faces `mod@api`, a `dependents()` query), so the sweep must land WITH them.

## Why filed rather than swept

The canon records this exact failure mode (GAP-014): applying a rule whose
surface does not exist replaces correct code with silently wrong code. 659
`.duo` files, two deleted gates, and every export site broken is not a repair.

## Closing it, in order

1. Land `check` / `why` / `graph.*` / `add(…)` / `todo(gap[n])` as real bindings.
2. Land `mod@api` faces and `dependents()` so `_` privacy has a successor.
3. Then sweep `_` prefixes, interior `_`, uppercase, prefix-`@` as ONE
   canonicalizer pass (L2 structural erasure) with the corpus as its fixture.

Steps 1 and 2 are the work. Step 3 is mechanical once they exist.

## MEASURED epoch-2 debt, four files (AUDIT v3, 2026-08-07)

Scanned `lib/std/compiler/lexer.duo`, `host.duo`,
`examples/pass16_lexer_host_proof.duo`, `scripts/language_census.duo`:

    lexical [a-z]_[a-z]   366
    uppercase identifier  273
    string. / table.       53
    prefix-@ directives    11
    std.script paths       10
    tonumber / tostring     4
    req "                   1

Four files. The corpus is ~662. This is the scale the ordered plan above has
to absorb, and it is why step 3 must be ONE canonicalizer pass with the corpus
as its fixture rather than a hand sweep.

**The first run of this scan printed all zeros**, and I nearly filed that as a
clean audit. Cause: fish assigns `F="a b c"` as a single string, so `grep $F`
looks for one file named "a b c", finds nothing, and reports 0. This is the
SECOND time the identical trap landed in one session — the first cost a
published deny-scan that was silently empty. Any audit that reports all zeros
should be assumed broken until one row is verified non-zero by hand.

## Epoch-2 debt in this session's own output

Recorded so it is not lost: `_int_of`, `_fail`, `duo_lexer_tokenize_full`,
`has_error`, `read_number`, `@c.export`. All violate §0.1/§0.2 and all are
rewritten by step 3.
