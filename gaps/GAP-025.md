# GAP-025 — the epoch-2 surface has no implementation yet

**Status:** open · **Filed:** 2026-08-07 (epoch 2, Pass 93)
**Rules cited:** Pass 93 §0.1 (one lowercase word), §0.2 (no directives),
Pass 90 (no `_` prefix), Pass 91 (no interior `_`)

Filed under §6 QUERY-FIRST — *never invent; no answer = spec gap = report it.*
This is a gap report, not a refusal: no rule from the conflict table's OLD
column is being cited, and the canonical spellings are the target.

## The blocker: the repair direction does not compile

| epoch-2 spelling | in the compiler today |
| --- | --- |
| `check(c)` | no binding |
| `why(q)(subject)` | no binding |
| `graph.descriptors` / `graph.modules` | no binding |
| `add(module)(…)` family | no binding |
| `todo(gap[23])` | no binding; `gap` is not a trie |
| no prefix `@` | `@c.export` is the ONLY export mechanism implemented — every export site in `lib/std/compiler/lexer.duo` and the SH-03 artifact rests on it |

Emitting the canonical spellings today produces source the compiler rejects, so
the repair is currently unreachable rather than merely unapplied.

## Second obstruction: `_` privacy is load-bearing, not decoration

- `lib/std/compiler/graph.duo` — `dead_private`'s entire query is *"a
  `_`-prefixed binding with in-degree 0 is unreachable"*. The asymmetry against
  public bindings IS the query.
- `scripts/module_surface_gate.duo` — enforces that nothing outside a file
  reaches a `_` name.

Both pass today. Pass 90's own decomposition names the successors (protocol
faces `mod@api`, a `dependents()` query), so the sweep must land WITH them.

## Why filed rather than swept

The canon records this exact failure mode (GAP-014): applying a rule whose
surface does not exist replaces correct code with silently wrong code. 659
`.duo` files, two deleted gates, and every export site broken is not a repair.

## Closing it, in order

1. Land `check` / `why` / `graph.*` / `add(…)` / `todo(gap[n])` as real bindings.
2. Land `mod@api` faces and `dependents()` so `_` privacy has a successor.
3. Then sweep `_` prefixes, interior `_`, uppercase, prefix-`@` as ONE
   canonicalizer pass (L2 structural erasure) with the corpus as its fixture.

Steps 1 and 2 are the work. Step 3 is mechanical once they exist.

## CASE-SETS FIXED 2026-08-07 — the machinery existed behind a deleted sigil

`kind: { name, number, eof }` and `shape: { circle(r: f64), rect(w: f64) }` now
parse. Nothing was implemented to achieve it: `parse_descriptor_table` has
always handled bare cases AND payload variants. The declaration site gated it
on `@`:

```zig
if ((try self.pk()).kind == .at) { ... stmt_from_descriptor ... }
```

Pass 92 deleted prefix-`@`, which made the canonical spelling unreachable while
`kind: @{ … }` kept checking clean — the feature was live and unspellable.

Routed on LOOKAHEAD, not unconditionally: a case is a name followed by `,` or
`(`; a record field always has `:`. So `{ x: i64, y: i64 }` still goes to
`parse_type`, verified (`pt: { x: i64, y: i64 }` → `p.x + p.y` == 7).

This unblocks case payloads, which is the shape gap[024] needs — `Tok`'s
`int_val`/`float_val` siblings now have somewhere to go.

Still blocked: **method slots** (`bump = (self) …` inside a descriptor), which
FACE-CALL, BIND-1 and `:peek()` all need. Same file, likely the same shape of
fix — the descriptor entry parser accepts `name: type` and `name(payload)` but
not `name = callable`.

## The descriptor body accepted ONE slot form — measured (historical)

Everything epoch 2 asks of a descriptor lands on the same wall. A descriptor
body parses `name: shape` and nothing else:

```duo
tok: { kind: { name, number, eof } }
error: expected ':', got ','          -- case-set

tok: { kind: { name, number(value: i64), eof } }
error: expected ':', got ','          -- case-set with payload

r: { n: i64, bump = (self) self.n + 1 }
error: expected ':', got '='          -- method slot
```

So three separate rulings are blocked by one missing production:

- **case-sets** (Pass 100 §7, §0.3) — `kind: { name, number, eof }`, and with
  them `.eof` case inference, dispatch keys `@{ [.eof] = f }`, and the
  retirement of companion `*_kind` descriptors.
- **case payloads** — the decomposition that gap[024] needs. `Tok` carries
  `int_val` AND `float_val` as sibling fields because the payload has nowhere
  else to live; `kind: { number(value) }` is the fix and does not parse.
- **method slots** (Pass 95 FACE-CALL, BIND-1, `:peek()`) — `lx:read(number)(b)`
  needs `read` homed on the descriptor.

**Pass 100 §20's golden `token` and `lexer` do not compile.** Both are built
from these forms, and §3 instructs agents to pattern-match only from the golden
corpus — so following that instruction produces source the compiler rejects.
That is worth stating plainly because it inverts the usual failure: the corpus
is not aspirational decoration, it is the thing agents are told to copy.

**One production unblocks the largest batch of epoch-2 rules.** Ahead of the
stage surface (`check`/`why`/`graph`/`add`), ahead of the naming sweep: the
descriptor body is the bottleneck, and it is a parser change with a measurable
gate (the golden corpus compiling).

## FACE-CALL (Pass 95) is blocked at the parser — measured

Pass 95 makes row-1 calls a receiver face: `lx:read(number)(b)`, never
`read(number)(lx, b)`. My code is full of the denied form (`L.next_tok(lex)`).

The repair needs methods to live ON the descriptor. They cannot:

```duo
r: { n: i64, bump = (self) self.n + 1 }
```

    error: expected ':', got '='   at the `bump =` slot

**Descriptors are field-only today.** A `name: shape` slot parses; a
`name = (args) body` slot does not. So there is nowhere to home `read`,
`next`, `peek` such that `lx:read(number)(b)` could resolve — the canonical
`lexer: { peek = () …, next = () … }` shape from the context file's §3 does
not parse either.

This is the same shape as the rest of this gap: the denied spelling works, the
canonical one has no implementation. It also means the golden corpus's `lexer`
descriptor is currently unbuildable as written, which is worth knowing before
anyone tries to pattern-match from it.

Add to the ordered plan, at step 1: **descriptor method slots must parse**
before FACE-CALL, BIND-1, or the `:peek()` sibling-call form can be applied
anywhere.

## MEASURED epoch-2 debt, four files (AUDIT v3, 2026-08-07)

Scanned `lib/std/compiler/lexer.duo`, `host.duo`,
`examples/pass16_lexer_host_proof.duo`, `scripts/language_census.duo`:

    lexical [a-z]_[a-z]   366
    uppercase identifier  273
    string. / table.       53
    prefix-@ directives    11
    std.script paths       10
    tonumber / tostring     4
    req "                   1

Four files. The corpus is ~662. This is the scale the ordered plan above has
to absorb, and it is why step 3 must be ONE canonicalizer pass with the corpus
as its fixture rather than a hand sweep.

**The first run of this scan printed all zeros**, and I nearly filed that as a
clean audit. Cause: fish assigns `F="a b c"` as a single string, so `grep $F`
looks for one file named "a b c", finds nothing, and reports 0. This is the
SECOND time the identical trap landed in one session — the first cost a
published deny-scan that was silently empty. Any audit that reports all zeros
should be assumed broken until one row is verified non-zero by hand.

## Epoch-2 debt in this session's own output

Recorded so it is not lost: `_int_of`, `_fail`, `duo_lexer_tokenize_full`,
`has_error`, `read_number`, `@c.export`. All violate §0.1/§0.2 and all are
rewritten by step 3.
