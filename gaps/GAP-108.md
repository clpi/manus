# GAP-108 — the native backend has no module storage: a file-scope binding is per-function

**Status:** SILENT WRONG ANSWER CLOSED 2026-08-09 · MODULE STORAGE STILL OWED
**Filed:** 2026-08-09

## 2026-08-09 — re-measured, and the account below is HALF stale

This file says the direct backend answers `8|5` where C answers `8|8`, silently,
with no bail. Re-measured at branch tip, that is TRUE FOR THIS PROGRAM and FALSE
in general, and the difference is one character of spelling:

```
total = 5        (this file's program)   direct  ok compile  8 5     C  8 8
total: i64 = 5   (the same program)      direct  DNB001 mod-global-written:total
```

The gap[066] guard (`module_top_level_written_binding`, `src/codegen.zig`) was
already refusing the written file-scope binding — but it scanned exactly two
statement kinds, `.local_decl` and `.global_decl`. There is no `local` / `var`
keyword in duon, so **with the type annotation removed there is no declaration
left to parse**: module scope receives an `.assign`, the same node `total = 8`
produces inside `main`. The guard saw nothing.

So the diagnosis in this file — "there is no module-level storage in the native
path at all" — is right, and its evidence was accidental: it picked the one
spelling that slipped past the existing guard. The defect was never
shape-independent in the way the section below claims; it was SPELLING-dependent,
which is worse, because the identical program is refused with a type annotation
and admitted without one.

**Repaired:** the precheck now also scans module-scope `.assign` targets.
`promote_module_captured_locals`, thirty lines away in the same file, already
treated module-scope `.assign` as a binding site — which is what makes this an
omission rather than a decision.

By value, before and after:

```
before   direct  8 5   C  8 8      ok compile, no diagnostic
after    direct  DNB001 (mod-global-written:total)   duo run -> 8 8 through C
```

**Native census cost: ZERO — 129 before, 129 after.** And that number is the
finding, not the reassurance: the census could not see this change because *no
corpus program had the shape*. A gate that cannot see a change does not approve
it, so two things were added rather than asserted:

- `examples/native_differential/unsupported/g108_untyped_file_scope_global.id`,
  the untyped program itself; and
- **`unsupported/` is now COMPILED instead of listed.** The folder's contract is
  "an honest capability gap, NOT a miscompilation" and that was an assertion —
  `native_differential.id` printed the file name and never built it. Delete the
  guard in `codegen.zig` and the old gate still printed "unsupported" and still
  passed. Each program is now built both ways: direct refuses (expected,
  reported) · direct compiles and AGREES (the capability landed — promote it) ·
  **direct compiles and DIVERGES (a miscompilation, and it BLOCKS)**.
  Positive-controlled by putting `g065_integer_division` — which compiles and
  diverges — into a scratch `unsupported/`: the gate exits 1.

That gate change found two STALE residents on its first run. `canon4_str_scan_lambda`
and `rec6_nested` compile and agree today and had been sitting in `unsupported/`
unwatched; both are promoted into the corpus. **native-differential 65 -> 67 agree,
0 diverge.** `rec6_nested` was canonicalized on the way in (`Inner`/`Outer` ->
`inner`/`outer`, trailing `return` dropped) — debt that was invisible only
because the audit does not reach into `unsupported/`.

Also measured while trying to canonicalize `canon4`: **`src[i]` on a `str` is
worse than unsupported.** `--backend=c` emits `lua_eq(src[i], 32)` against an
undeclared `lua_eq` and fails to build; `--backend=direct` compiles it and
answers 2 where the `string.byte` form answers 3. §0.8 says `s[i]` IS the byte,
so this is a real hole and not a style preference. Same class as GAP-025.

**What is still owed is the title of this file.** Module storage does not exist:
the refusal is correct and it is not the repair. Scope, measured rather than
assumed — `mod-global-written` is **1 of 88** first-cause bails over
`examples/**`, so it is a correctness row, not a coverage row, and the
expectation that it "subsumes several rows below" is not supported (see GAP-098:
the tail is long and flat).

---

*Original filing follows.*
**Law:** §3 MEASUREMENT HONESTY · backend-independent semantic contracts
**Found by:** the constant-branch lane, reproduced here on the branch tip.

## Reproduced

```duo
total = 5

show(): i64
    total

main(): i64
    total = 8
    a: i64 = total
    b: i64 = show()
    print(a)
    print(b)
```

```
--backend=direct   ok compile   8|5|
--backend=c                     8|8|
```

**C is right.** A file-scope binding is one storage location; writing 8 in
`main` must be visible in `show`. The direct backend answers with the
INITIALIZER.

## Cause

There is no module-level storage in the native path at all:

```
grep -nE "global"  src/native_backend.zig   -> one comment, no storage
grep -nE "global|module_scope|file_scope"  src/dnir_lower.zig
                                            -> unrelated ("runtime global by name")
```

A file-scope binding is lowered as an ordinary LOCAL, per function, each
re-materialized from its initializer. So `main` and `show` hold two different
`total`s, and neither write nor read crosses. Writes are invisible in both
directions — `show` writing would be equally lost.

## Why this is worse than its size

**It is silent and it is shape-independent.** No bail, no diagnostic, `ok
compile`. Any program with a mutable file-scope binding read in more than one
function answers wrongly on the native path and correctly through C, so:

- the two backends disagree on a basic semantic contract;
- the disagreement is invisible to any test asserting only one backend;
- and it was masked further because `--backend=c` could not compile a mutable
  file-scope scalar AT ALL until earlier today, so the differential that would
  have caught it did not run.

Two defects hid each other. That is the argument for backend differentials being
a gate rather than a convenience.

## The repair, and the interim

**Real repair:** module storage — a data section (or frame-independent slot)
with one address per file-scope binding, loaded and stored rather than
re-initialized. That is a backend capability, and it is also what a self-hosted
compiler needs for its own globals, so it is on the SHC spine rather than beside
it.

**Interim, if the real repair is not immediate:** REFUSE. If a file-scope
mutable binding is assigned anywhere outside its initializer, decline to lower
natively any function that reads it. A bail is a correct answer through C; the
current behaviour is a wrong answer. Same trade as the spiller refusal shipped
today, and it should be measured the same way — name the native-census cost
rather than asserting it is small.

**Not attempted here** because the cost is unmeasured and the conservative rule
("any module with a written file-scope binding") could be broad. Whoever takes
it should measure the census delta first and report it, per the spiller
precedent.

## Do not

**Do not fix this by making the initializer run in every function.** That makes
reads agree and leaves writes lost, which converts a visible disagreement into a
subtler one.

**Do not assume `const` is unaffected.** An immutable file-scope binding
re-materialized per function is only accidentally correct — it answers the same
value because nothing writes it. The storage defect is identical; only the
symptom is absent.

## Related

The parameter-re-read divergence (direct `3|2|` vs C `3|3|`) is a separate
finding from the same lane, recorded as a measurement rather than a diagnosis.
gap[101] holds the backend-differential method that surfaced both.
