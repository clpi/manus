| field | value |
|---|---|
| title | GAP-203 — the injection half of the world algebra has no graph fact |
| status | OPEN |
| filed | !2026-08-21T17:40:00Z |
| priority | P0 |
| kind | expressiveness_gap |
| filed by | claude-gap145 |
| files | src/semantic_graph.zig,src/ast.zig,src/parser.zig,src/sema.zig,docs/spec/world.md |

| # | directive |
|---|---|
| 1 | `docs/spec/world.md` §30-34 states one surface, and `law.injection.only` — the GAP-110 reconciliation of 2026-08-13 — rules that `@{ … }` derives a world. |
| 2 | Measured against the compiler that ships that spec, two of the five faces do not compile and a third derives nothing. |

| section |
|---|---|
| Measured 2026-08-21 (subject 4c95b497) |

| face | spec | parser | graph |
| --- | --- | --- | --- |
| `@x` access | select one exact fact | accepts | reaches ambient world |
| `@{ k = v }` inject | derive a closed world | accepts | **derives NO world** |
| `thing@world` qualify | evaluate under world | accepts (name glued only) | anchored `.field`, i.e. retrieval |
| `thing@{ k = v }` interject | evaluate under injected world | **refuses** | none |
| `@k = v` mutate | world member as place | **refuses** | none |

| # | directive |
|---|---|
| 1 | The spec's own examples do not compile. `world.md:67` (`sale.quote@{ tax = (sum) 0 }(100)`) and the `world.md:107` block containing `@fee = 0` are both rejected. |

| # | directive |
|---|---|
| 1 | `w = @{ clock = fake }` type-checks and exports `"worlds":[]`. |
| 2 | A program that merely reaches `os` exports a populated row, so the world column is live — the empty result is the derivation producing no fact, not absent machinery. |

| section |
|---|---|
| The missing fact |

| # | directive |
|---|---|
| 1 | `semantic_graph.WorldFact` is `{ world, home, reach, members }` — a module-REACHES-world fact, with `Draw` keyed by application occurrence. |
| 2 | There is no `parent` and no fact-delta range, so a derived world is unrepresentable. |
| 3 | There is also no AST node for qualification or interjection: `grep -nE 'qualif\|interject\|world_expr' src/ast.zig` is empty. |

| # | directive |
|---|---|
| 1 | `thing@world` is not qualification today. `src/parser.zig` builds an anchored `.field` node and its own comment says RETRIEVAL, NEVER INVOCATION — it retrieves a member at an anchored home rather than evaluating under a world. |

| section |
|---|---|
| Why interjection could not parse |

| # | directive |
|---|---|
| 1 | `src/parser.zig:5481` maps `.at => .matmul`. |
| 2 | The escape hatch `at_is_glued_anchor` admits only a name-like token glued to the sigil, so a glued `{` fell through to matmul and died in sema as "matmul over non-tensor operands" — whose repair hint is "close the space", advice the spec's example has already taken. `world.md:64`'s exact reduction `thing@(@{ k = v })` failed the same way, so this was never missing sugar: qualification by any non-name expression is unimplemented. |

| # | directive |
|---|---|
| 1 | Crossed 2026-08-21: `at_is_glued_world_face` now refuses both glued faces where the shape is known and names the missing fact. |
| 2 | Spaced `a @ b` remains matmul and `X@rel` is unchanged; suite held at 11 failures + 3 crashes over 1666. |
| 3 | That is a diagnostic repair. |
| 4 | It closes nothing. |

| section |
|---|---|
| Why `@k = v` cannot parse (measured 2026-08-21) |

| # | directive |
|---|---|
| 1 | Not a routing problem, and an attempted parser-only repair is recorded here so the next agent does not spend the same hours. |

| # | directive |
|---|---|
| 1 | `@name` at statement start is an ATTRIBUTE (`@hot`, `@export`, `@build.*`), so `@fee = 0` entered attribute dispatch and met `=` where a declaration belonged. |
| 2 | Routing the shape away from attributes before dispatch is correct and was verified reaching `parse_expr_stmt` with an instrumented predicate observing `kind=name text='fee'` then `kind=assign`. |
| 3 | It still fails, one layer down: |

| # | directive |
|---|---|
| 1 | `parse_expr_stmt` builds its target with `parse_suffixed_expr`, which handles names, literals, calls and field access. `@fee` is none of those. |
| 2 | Routing the target through a bounded `parse_prec(2)` instead — bounded because the assignment family is precedence 1 right-associative and full expression parsing would swallow the `= v` — reaches the primary `.at` arm. |
| 3 | That arm recognises the bare anchor and otherwise falls through to `parse_macro_call_expr`, which expects `@name( … )`. Bare `@fee` dies there demanding `(`, which is the original error at its true origin. |

| # | directive |
|---|---|
| 1 | So the world place has no expression form in statement-target position, and would still need to survive `check_assign_target` and reach a place in lowering. `@k = v` is blocked on the same missing fact as interjection: there is no world place. |
| 2 | It is not a parser bug with a parser fix. |

| section |
|---|---|
| The sigil is OCCUPIED, not merely unimplemented (measured 2026-08-21) |

| # | directive |
|---|---|
| 1 | This is the finding that changes the shape of the work, and it was missed by reading the spec instead of the parser. |

| # | directive |
|---|---|
| 1 | `@{ … }` in expression position is not unimplemented. `parse_macro_call_expr` routes it to `parse_pack` under a `.compile` staging wrapper — it is the descriptor/pack face, and its own comment says the measured `= @{ … }` sites in the tree "are frozen constant tables that depend on the staging". |

| # | directive |
|---|---|
| 1 | Those sites are live. `grep -rn '= *@{' --include='*.id'` over lib, examples, scripts, tools and gate finds 11, and they are keywordless enum descriptors: |

```id
kind = @{ eof = 0, ident = 1 }
Kind = @{ eof = 0, ident = 1, number = 2, punct = 3 }
```

| # | directive |
|---|---|
| 1 | `parse_expr_stmt` names this face directly: "`Color: @{ Red, Green, Blue }` keywordless enum descriptor". |

| # | directive |
|---|---|
| 1 | So GAP-110's ruling — that the `@{}` collision is "resolved decisively in favor of world injection only", with a descriptor being an ordinary table `{ … }` carrying no sigil — was RECORDED BUT NEVER IMPLEMENTED. |
| 2 | The descriptor meaning still holds the sigil. |

| # | directive |
|---|---|
| 1 | This explains every measurement above without appealing to missing features: `@{ k = v }` type-checks because it parses as a descriptor; it derives no world because a descriptor is not a world; and `thing@{ … }` refuses because the postfix position has no descriptor reading at all. |

| # | directive |
|---|---|
| 1 | **Consequence for closure: this is a breaking migration of an occupied sigil, not the addition of an absent one.** A derived-world fact cannot simply be published from `@{ … }` while 11 corpus sites read the same spelling as a frozen constant table. |
| 2 | The migration must come first, or the two meanings must be separated by position with an executable control proving which face each site took. |

| section |
|---|---|
| Corpus re-measured 2026-08-21: 69 lines in 36 files, not 11 |

| # | directive |
|---|---|
| 1 | The "11 live dependents" figure recorded above came from `grep '= *@{'`, and that undercounts by six times. |
| 2 | The DOMINANT face is the copula `Name: @{ … }`, which that pattern never sees. |

```
= @{   (what was counted)   11
@{     (actual)             69 lines across 36 files
```

| # | directive |
|---|---|
| 1 | 24 of those are live `DECL_COLON` sites, and two are the self-hosting frontier itself — `lib/compiler/parser.id:64` (`Res: @{ val: i64, pos: i64 }`) and `lib/cursor.id:5` (`bytecursor: @{ data: str, pos: i64, len: i64 }`). |
| 2 | Plus four source-GENERATING string sites in `scripts/applyone.id` and `scripts/audit100.id` that write `.id` files and run them, and roughly 50 Zig test fixtures. |

| section |
|---|---|
| The law's literal words break; the copula does not |

| # | directive |
|---|---|
| 1 | `law.injection.only` says a descriptor is an ordinary table `{ … }` with no sigil. |
| 2 | Taken literally for the bind form, `Kind = { eof = 0, ident = 1 }` passes `check` and then the direct backend REFUSES with `DNB001 … keyed-table-export` — because the descriptor became a materialized table (`places: []` for `@{}` against one runtime record place with `extent {exact: 4}` for `{}`). |

| # | directive |
|---|---|
| 1 | The copula case-set `Kind: { eof, ident, number, punct }` is exit-identical AND strictly better: it produces a real `enum_shape` with variant children and `member` edges, and a `duo:enum:kind` with `layout_completeness: complete`. |
| 2 | The `= @{ … }` form exports NOTHING — zero nodes, zero edges. |
| 3 | All four corpus enums are dense `0..n-1` in order, so implicit ordinals match exactly. |

| # | directive |
|---|---|
| 1 | Full corpus differential, 83 programs migrated: **zero divergences**. |
| 2 | Every `ok compile` held, every exit code matched, and every pre-existing refusal kept its exact code. |

| section |
|---|---|
| `idol fmt` is already on the law's side |

| # | directive |
|---|---|
| 1 | Formatting `Base: @{ x: i64 }` emits `Base: { x: i64 }`. |
| 2 | The canonical formatter performs the migration today; the parser is what still accepts the retired form. |
| 3 | That also means leaving `pretty.zig`'s `@{` reprint in place after the parser stops accepting it would make formatting a legal file produce an illegal one. |

| section |
|---|---|
| The parser cites an authority that no longer exists |

| # | directive |
|---|---|
| 1 | `parse_macro_call_expr`'s comment justifies the descriptor reading with "c0 §43 `anchor.brace`". `grep -n 'anchor\.' docs/spec/constitution.md` returns only `anchor.recover` and `anchor.apply`, and both say the opposite: `@{ … }` derives a world by injection, descriptor construction is `name{ … }` or plain `{ … }`. |
| 2 | The cited authority was replaced by its own negation. |

| section |
|---|---|
| A live law violation, measured |

| # | directive |
|---|---|
| 1 | `docs/spec/constitution.md:2884` records: |

```
deny = "p: point = @{ x, y } — @{ … } is world injection, not a descriptor
        construction face (law.injection.only)"
```

| # | directive |
|---|---|
| 1 | That exact spelling checks clean today: `✓ checked — no errors`. |
| 2 | The constitution denies it by name and the compiler accepts it. |

| section |
|---|---|
| Position-disambiguation is foreclosed |

| # | directive |
|---|---|
| 1 | `constitution.md:2761` binds `@{ … }` as **exclusively** world derivation/injection, and `:2199` adds it is NOT name-elided descriptor construction. |
| 2 | Keeping the descriptor meaning in copula position and giving worlds the expression position is a second meaning for the sigil, which `exclusively` forecloses — and it would preserve the one spelling `:2884` denies by name. |
| 3 | It is also unnecessary: every corpus site has a sigil-free spelling that already compiles. |

| section |
|---|---|
| Re-measured 2026-08-23 (subject ce03c7eb, lane/world-20260823) |

| # | directive |
|---|---|
| 1 | **The migration this gap said must come first is DONE. |
| 2 | No face closed.** |

| # | directive |
|---|---|
| 1 | `fd85e7b8` (2026-08-21, "WORLD-FACE-ZERO: retire the @{ } descriptor face, migrate all 31 sites") landed before this gap was re-read, and it is an ancestor of ce03c7eb. |
| 2 | Every count in the sections above is the PRE-MIGRATION state and should be read as history: |

| # | directive |
|---|---|
| 1 | The live count is not written here. `gate/world/face.sh` §3 is the scanner that computes it, and it is the only authority for it: |

| # | directive |
|---|---|
| 1 | sh gate/world/face.sh |

| # | directive |
|---|---|
| 1 | §3 enumerates every tracked `.id` subject, classifies each `@{` occurrence as live / comment / string-literal, and FAILS on any live descriptor site. |
| 2 | The number it holds at is zero, and it is controlled against a planted violation, so a scan that stops early cannot report a clean zero. |

| # | directive |
|---|---|
| 1 | What survives the migration is the migration's own narrative plus deliberate gate fixtures. |
| 2 | Reading the current split means running the gate; a total copied into this table would go stale the next time a fixture comment moved, while the gate kept passing. |

| section |
|---|---|
| The five faces, re-measured |

| face | spec | ce03c7eb | closed? |
| --- | --- | --- | --- |
| `@x` access | select one exact fact | `error: write `(` at this token edge` | **no** |
| `@{ k = v }` inject | derive a closed world | `error: injection '@{ … }' has no derived-world fact yet` | no |
| `thing@world` qualify | evaluate under world | checks clean; still anchored `.field` RETRIEVAL | no |
| `thing@{ k = v }` interject | evaluate under injected world | `error: interjection 'thing@{ … }' has no derived-world fact yet` | no |
| `@k = v` mutate | world member as place | `error: write `(` at this token edge` | no |

| # | directive |
|---|---|
| 1 | Two corrections to the table as filed. `@x` does **not** accept: the filed row read "accepts / reaches ambient world", and at this subject every spelling tried — `@target`, `@env["HOME"]`, `@os.env`, at statement and expression position — dies in `parse_macro_call_expr` at `_ = try self.expect(.lparen)`. `@name` is the DIRECTIVE reader and it is unconditional, so there is no world-member access expression at all. **Zero of five faces compile, not two.** |

| # | directive |
|---|---|
| 1 | `thing@world` is confirmed retrieval by execution, not inference: `t: trial = { tax = 7 }` then `print(t@tax)` compiles, runs, and prints `7`. |

| # | directive |
|---|---|
| 1 | The spec's own examples still refuse, verbatim: `world.md:67` (`sale.quote@{ fee = 0 }(100)`) and the `trial = @{ tax = (sum) 0 }` block at `world.md:57`. |

| section |
|---|---|
| What the migration changed, and what it did not |

| # | directive |
|---|---|
| 1 | The sigil is FREE. |
| 2 | That was this gap's stated precondition — "a derived-world fact cannot simply be published from `@{ … }` while 11 corpus sites read the same spelling as a frozen constant table" — and it no longer holds anyone back. |
| 3 | Nothing else changed: `semantic_graph.WorldFact` is still `{ world, home, reach, members }` with no parent and no fact-delta range, and `grep -nE 'qualif\|interject\|world_expr' src/ast.zig` is still empty. |
| 4 | Closure items 1-6 below are untouched. |

| section |
|---|---|
| Debt the migration left, now closed |

| # | directive |
|---|---|
| 1 | Three halves of `fd85e7b8` were missing and are repaired on this lane: |

| # | directive |
|---|---|
| 1 | **The printer.** This gap warned that leaving `pretty.zig`'s reprint in place would make formatting a legal file produce an illegal one. It did, for two days: `x = @({ a = 1 })` formatted to `x = @{ a = 1 }` and then refused. Filed and fixed as gap[223]. |
| 2 | **A generator.** `scripts/applyone.id` emits its three subject programs from string literals, so the migration's greps never saw them; two carried the retired sigil and the apply-one row was reading a C file that was never written. |
| 3 | **The other frontier.** `lib/compiler/parser.id` still consumed `token.kindat` in its descriptor slot, so the self-host parser accepted what the Zig parser refused and both projected the identical `(descriptor …)`. |

| section |
|---|---|
| Ratchet |

| # | directive |
|---|---|
| 1 | `sh gate/world/face.sh` — five sections, each proved non-vacuous by planting the violation and observing the named failure. |
| 2 | It also prints what it does NOT prove: a free sigil is not a closed algebra. |

| section |
|---|---|
| Re-measured 2026-08-23 (lane/worldstage) — `@x` ACCESS CLOSES |

| # | directive |
|---|---|
| 1 | **One of the five faces now compiles, runs, and is held by a gate.** The table in the previous section read `error: write `(` at this token edge` for `@x`, because `@name` was the directive reader and its `expect(.lparen)` was unconditional. |
| 2 | It no longer is. |

| # | directive |
|---|---|
| 1 | sh gate/world/access.sh |

| # | directive |
|---|---|
| 1 | is the authority for what closed, in six sections, each proved non-vacuous. |
| 2 | What the face now does, and none of it is inferred from a parse: |

| # | directive |
|---|---|
| 1 | `@x` resolves against the LAUNCH WORLDS and never the lexical scope, which is the whole capability. `world.md` rules that a bare lexical name wins for the bare spelling and that `@x` accesses the world member explicitly, so the disagreement case is the test: `env = (k) "shadowed"` followed by `@env("IDOLWORLDACCESSPROBE")` runs and answers the ENVIRONMENT, while bare `env(k)` in the same shape still answers the relation. §3 holds both halves. |
| 2 | A member the current world does not have FAILS CLOSED and by name. There is no parent-directory, other-world, global-registry, library, or default-namespace fallback, and `k = 7` then `@k` still refuses — a lexical binding is not a world member. |
| 3 | The graph DRAWS the world at the occurrence, including under the shadow. Three producers were each publishing a fact that contradicted the machine and all three are repaired: `Sema` selected a module relation by SPELLING from `callable_defs`, so the graph exported `relation: <env decl>` over a site realized as `getenv`; `semantic_graph.worldOfApplication` let `findFunc` claim the same occurrence; and `table_apply.bareMember` declined the world ingress whenever any binding shared the spelling, which made the program PRINT the relation's answer. Measured before the repair: `env = (k) 99` + `@env("HOME")` compiled, ran, and printed `99`. |
| 4 | The printer writes the sigil back. Dropping it is a worse failure than gap[223]'s: the reprint still compiles and means something else. §5 asserts the reprint's ANSWER, not its bytes. |
| 5 | The production EXPANDS WHAT IT RETURNS. `@x(args)` is decided in the macro expander, so its arguments go back through the same hygiene clone every other arm uses; handing them over untouched left a nested access as an unexpanded `.macro_call` that sema refused. §6 pins it with an indirection (`@env(@env(OUTER))` answers the value `OUTER` names, which only an evaluated inner access can produce) and controls it against an absent member inside an argument, which must still refuse by name. |

| # | directive |
|---|---|
| 1 | **What did NOT close, measured rather than assumed.** The DOTTED face `@x.y` is still the directive path: `@os.env("HOME")` does not check. |
| 2 | The parser consumes the whole dotted path before any world question is asked, and it must keep doing so while `@c.type(…)`, `@c.call(…)` and `@c.export` are live — `c` is a declared world NAME, so a rule turning `@world.member` into an access would take those spellings with it. |
| 3 | The dotted face closes with the directive catalog, not before it. `gate/world/access.sh` §1 holds that fact and FAILS if `@os.env(…)` starts checking, so the row cannot go stale silently. |

| # | directive |
|---|---|
| 1 | Corpus control for the whole change: every tracked `.id` subject (1014) checked before and after, comparing exit code AND first diagnostic. **Zero divergences.** |

| # | directive |
|---|---|
| 1 | Items 2-6 below are untouched. |
| 2 | Item 1 — the derived-world fact — is untouched. |

| section |
|---|---|
| Item 1 is not the first move — measured 2026-08-23 (lane/worldstage) |

| # | directive |
|---|---|
| 1 | Closure item 1 has been read as "add `parent` and a delta range to `WorldFact`". |
| 2 | It is the wrong thing to do first, and the reason is measurable rather than stylistic. |

| # | directive |
|---|---|
| 1 | **There is no realization to evaluate under.** A world derived on `tax` has nothing behind it: `(sale.quote@trial)(100)` needs `sale.quote` specialized with `trial`'s `tax` bound, and that is a lowering capability, absent whether or not the graph carries the fact. |
| 2 | Publish the fact first and `expr@{ tax = 1 }` CHECKS CLEAN and refuses at emit — strictly worse than today's refusal, which names the missing fact at the point the author wrote it, and it is the check-clean / emit-fail shape this tree calls the worst available answer. |

| # | directive |
|---|---|
| 1 | The order that works is realization first, then the identity of the world it evaluated in. |
| 2 | The COMPILE STAGE is the first derived world to have both halves and it got them in that order: `gate/world/stage.sh` executes `expr@{ stage = compile }` and holds `derive(W, {}) = W`, same-fact idempotence, and no-fallthrough on running programs — with `WorldFact` still unchanged. |
| 3 | See gap[227], which also records that `@( … )` DID NOT EXECUTE ON ANY BACKEND before that lane, so the "128 live sites" this gap's neighbours cited were 96 code-position spellings with a type-checker behind them and nothing else. |

| section |
|---|---|
| Advanced 2026-08-25 — the fact EXISTS in the graph; no source face moved |

| # | directive |
|---|---|
| 1 | **Premise re-verified before work:** `semantic_graph.WorldFact` was still `{ world, home, reach, members }` with no parent and no delta range, `Draw` still `{ application, world: Card }`, and the parser still refused `@{ tax = 1 }` by name. |
| 2 | All held. |

| # | directive |
|---|---|
| 1 | **What landed (src/semantic_graph.zig):** |

| # | directive |
|---|---|
| 1 | `DerivedWorldFact { world, parent, deltas, witness }` with packed `WorldDelta { member, value }` rows — sparse columns `derived_worlds` / `world_deltas`. |
| 2 | ONE producer, `SemanticGraph.deriveWorld(parent, specs, witness)` (`law.fact.producer.one`). The identities are applied AT FORMATION so they are graph properties: `derive(W, {}) = W` publishes nothing; injecting exactly what resolution already answers returns the same world; the fact is hash-consed on (parent, delta set) so deriving twice answers ONE identity. Formation does not mutate the parent — the derived world is a SIBLING (parent's scope), never a parent child, and no parent row moves. |
| 3 | ONE resolution owner, `SemanticGraph.worldMemberValue(world, name)`: delta rows answer first, an untouched name answers through the parent, and at an ambient world the three answers stay distinct — `.one` (reached member entity), `.unknown` (declaration provides it, module reached no entity), `.none` (the world does not hold the fact; fails closed, no other-world/registry fallback). Every row read is immutable within one graph incarnation, so the walk reads formation-time state (item 5's guarantee at this layer). |
| 4 | Refusals, failing closed by name: `DuplicateInjectionMember` (one literal, one name, twice), `InjectionCannotGrantAuthority` (a delta spelled `authority` — no value entity is a valid witness, so `@{ authority = true }` refuses rather than admitting a label; and a delta spelled as a WORLD name — grants come only from the launcher, `law.world.grant`), `ParentNotAWorld`, `FormationWitnessUnknown`. A derived fact carries no `home`/`reach` at all: authority questions delegate through `parent`, so a delta structurally cannot widen what the launcher granted. |

| # | directive |
|---|---|
| 1 | **Pinned by three Zig tests** in src/semantic_graph.zig ("derived world resolves delta first…", "derivation identities are graph properties", "injection cannot manufacture authority…"): (a) overridden member answers the delta value, (b) the parent still answers the original entity and gains no child, (c) a non-overridden member falls through to the parent's exact entity, (d) the authority/duplicate/non-world/unknown-witness shapes refuse. |
| 2 | Non-vacuity proved by planting a wrong expectation in (a) and observing the named failure; `zig build` and `zig build unit-test` green after revert. |

| # | directive |
|---|---|
| 1 | **What deliberately did NOT move, and why.** No parser, sema, or lowering change: `@{ tax = 1 }` still refuses at the point the author wrote it (re-verified on this tree), because per the ordering finding above and gap[227], wiring the source face to this fact before a realization can evaluate under a derived world would trade that refusal for check-clean / emit-fail — the worst available answer. |
| 2 | The producer's only current callers are its executable property tests; the named first production consumer is the compile-stage world (gap[227] items 1–3), which should become a `DerivedWorldFact` whose parent is the module's ambient world. |

| # | directive |
|---|---|
| 1 | **Per-item status after this increment:** |

| # | directive |
|---|---|
| 1 | Derived-world fact — **representation, identities, and refusals DONE at the graph layer**; still unpublished from any source occurrence, and the ambient CURRENT world of a module is still not an entity a parent can name (gap[227] item 1 remainder). |
| 2 | AST face for qualification/interjection — untouched. |
| 3 | `Draw` recording ambient-vs-derived per application — untouched (`Draw` already carries a world id `Card`, so a derived id fits its shape when something draws one). |
| 4 | `@k = v` place — untouched. |
| 5 | No parent fact missing at formation may be read — holds structurally at this layer (immutable rows within one incarnation, resolver documented to it); becomes a live obligation once worlds can form against a mutable ambient roster. |
| 6 | Interjection ≡ qualification-under-derived-world — untouched. |

| section |
|---|---|
| Closure |

| # | directive |
|---|---|
| 1 | A derived-world fact: parent world id plus exact fact deltas, with empty injection identity and same-fact idempotence as executable properties. **Ordering, per the measurement above: the realization comes first.** The two identity properties are already executable for the stage world without this fact; what the fact adds is WHICH world an occurrence evaluated in, and that is only worth publishing where something evaluated. |
| 2 | An AST face for qualification and interjection that is not `.field`. |
| 3 | `Draw` records which world — ambient or derived — each application drew from. |
| 4 | `@k = v` yields a place only where projection actually yields one. |
| 5 | A derived world may not read a parent fact missing at formation. |
| 6 | `thing@{ k = v }` proved exactly equivalent to qualification under the derived world, per `world.md:64`. |

| # | directive |
|---|---|
| 1 | Close only when the spec's own examples compile and execute under the world they name, and when the algebra's identities are executable property tests rather than prose. |
