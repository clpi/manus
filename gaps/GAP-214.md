| field | value |
|---|---|
| title | GAP-214 — the GAP-209 fix turned a working program into a refusal |
| status | CLOSED |
| filed | !2026-08-22T04:00:00Z |
| priority | P0 |
| kind | regression |
| filed by | claude-gap145 |
| files | src/dnir_lower.zig |

```id
t: i64 = 0
t = t + 5
stdout:write("{t}\n")
```

| compiler | result |
| --- | --- |
| `b64dc075` | prints `5`, exit 0 |
| `44d8f98e` | prints `5`, exit 0 |
| **`5edd3ced` (GAP-209 fix) onward** | **REFUSED** — `DNB001 … missing: concat`, `bail site: lowerBinop() at dnir_lower.zig:9438` |

| # | directive |
|---|---|
| 1 | Bisected across three compilers built from those exact subjects. **This is a regression introduced by GAP-209's fix**, and the 809-file corpus differential that accompanied that fix reported **0 status differences**. |

| section |
|---|---|
| The exact shape |

| # | directive |
|---|---|
| 1 | A module-scope binding **declared with an explicit type**, reassigned from an expression that **mentions itself**, then **interpolated**. |

| source | result |
| --- | --- |
| `t: i64 = 0` / `t = t + 5` / `stdout:write("{t}\n")` | **REFUSED** |
| `t: i64 = 0` / `t = t + 5` / `s: str = "{t}"` / `print(s)` | **REFUSED** |
| `t: i64 = 0` / `t = t + 5` / `print(t)` | compiles, `5` |
| `t: i64 = 0` / `t = 5` / `stdout:write("{t}\n")` | compiles, `5` |
| `u: i64 = 1` / `t: i64 = 0` / `t = u + 5` / `stdout:write("{t}\n")` | compiles, `6` |
| `t = 0` (untyped) / `t = t + 5` / `stdout:write("{t}\n")` | compiles, `5` |
| the same body inside `main: i64 = ()` | compiles, `5` |

| # | directive |
|---|---|
| 1 | Not `stdout:write` — interpolation into a `str` binding refuses too. |
| 2 | Not the loop — no loop is needed. |
| 3 | Not reassignment alone. |

| section |
|---|---|
| Why |

| # | directive |
|---|---|
| 1 | GAP-209's fix correctly drops `t` from `ModuleConsts` (it is rewritten, so the recorded initializer is stale) and grants it module storage instead. |
| 2 | The interpolation hole's operand then has to resolve its type through the module global rather than a constant — and `exprIsStr` does not resolve it, so the concat chain is not taken and `.concat` falls to the `else => bailWith` at `src/dnir_lower.zig:9438`. |

| # | directive |
|---|---|
| 1 | **The hole in `exprIsStr` pre-existed.** GAP-209's fix exposed it by moving this shape from the constant path onto the storage path. |

| section |
|---|---|
| The trade, stated plainly |

| # | directive |
|---|---|
| 1 | GAP-209's fix turns a class of **silent wrong answers** into correct answers (`42 43` where the oracle says `42 43`, previously `42 1`). |
| 2 | It also turns this class of **working programs** into refusals. |
| 3 | Refusing beats answering wrong, so the fix should not be reverted — but the cost was measured as zero and was not. |

| section |
|---|---|
| Why the corpus missed it |

| # | directive |
|---|---|
| 1 | 809 files, 179 both compiled and ran, 0 status differences. |
| 2 | The corpus contains no program with a typed module binding, self-referential reassignment, and interpolation of that binding. |
| 3 | As with GAP-207 and GAP-209, **only a hand-written program in the neighbouring shape finds it.** |

| section |
|---|---|
| Acceptance |

| # | directive |
|---|---|
| 1 | The graph publishes the checked interpolation-hole value with its exact descriptor and value → module-binding edge. `exprIsStr` and the integral/float/pointer siblings consume that graph route. `ModuleGlobals.types` remains only a transitional fallback for occurrences outside the producer's declared exact-known domain; damaging the published descriptor must make this occurrence fail closed rather than reconstructing its type from the name. |
| 2 | Then this program compiles again **and** GAP-209's oracle rows still hold. |
| 3 | Both controls are required in the same run. |

| section |
|---|---|
| Closure |

| # | directive |
|---|---|
| 1 | The checked-name producer now owns name occurrences inside application operand expression trees. |
| 2 | It publishes one value id carrying Sema's exact descriptor and the exact value → binding edge. |
| 3 | Immediate operands of resolved applications remain owned by the existing application-pack producer; nested function and block bodies are explicitly outside this producer domain and stay unvisited until their scope-owned producer admits them. |

| # | directive |
|---|---|
| 1 | DNIR distinguishes three states for the exact occurrence: |

| # | directive |
|---|---|
| 1 | `known`: the graph descriptor decides the scalar class; |
| 2 | `unknown`: the producer resolved the binding but Sema did not prove a descriptor, so transitional physical facts may still answer; |
| 3 | `unvisited`: the occurrence is outside this bounded producer domain. |

| # | directive |
|---|---|
| 1 | The graph does not yet publish a producer-domain completeness certificate, so an occurrence without an exact binding is deliberately unvisited rather than an edge-less placeholder that would conflate producer refusal with damage. |

| # | directive |
|---|---|
| 1 | This closes the reported interpolation refusal without pretending the bootstrap `stdout:write` call has a resolved `ApplicationFact`. |
| 2 | General projection/application publication, including `xs[1]` and foreign-field result identity, remains open. |

| section |
|---|---|
| Executed evidence |

| # | directive |
|---|---|
| 1 | the semantic-graph positive proves exactly one nested `t` value, descriptor `i64`, and `Card.one(module_binding)` under the honest bootstrap call; |
| 2 | the DNIR damage control corrupts that value's descriptor and requires the named `concat` refusal, proving `ModuleGlobals.types` cannot mask damage; |
| 3 | the focused Zig suite passes `1693/1693` tests; |
| 4 | Debug direct, ReleaseFast direct, and wasm32-wasi/Wasmtime each print `5`; |
| 5 | GAP-209's linear control still prints `42` then `43`, and its loop control still prints `10` then `11`; |
| 6 | the 211 previously direct-lowerable corpus subjects, excluding the deliberately cwd-sensitive fixture, have identical stdout and status. |

| # | directive |
|---|---|
| 1 | The coverage census records the acquisition cost rather than calling it a performance win: application publication stays `4992/14932` and blockers stay `4655`, while graph size grows from `80719` to `82299` nodes and `15566` to `17146` edges (`44270` to `45850` values). |
| 2 | The new facts have a real DNIR consumer, but packed storage and broader application continuity remain required before this is a closed optimization route. |
