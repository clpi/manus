# GAP-158 — Path names violate LAW-ONE across scripts, lib, and src

## State

Open.

## Problem

C0 (`law.path.name`) says project-controlled semantic paths obey the same
lowercase one-word law as Idol source identifiers and forbids underscores,
compounds, and numeric taxonomy suffixes. Current tracked paths massively
violate this.

81 `.id` files in `scripts/` and `lib/` contain underscores. Many `.zig`
files in `src/` also violate.

## Evidence

```
scripts/abi_matrix.id          → compound: abi + matrix
scripts/bootstrap_scan.id      → compound: bootstrap + scan
scripts/parser_corpus_coverage.id → triple compound
scripts/language_census2.id    → compound + numeric suffix
scripts/duo_idiom_gate.id      → historical name + compound
scripts/run_wasm_benchmark.id  → compound
lib/compiler/emit_c.id     → compound
lib/compiler/token_view.id → compound
lib/ffi_gen.id             → compound
lib/fs_watch.id            → compound
lib/mem_ring.id            → compound
```

## Census 2026-08-13 (gate-vocabulary reused, read-only)

Reran with the exact gate logic (`gate/path.id` ∪ `gate/idiom.id` `known`/`sep`/
`tail`/`mash`/`compound`) over `git ls-files`. Binding-head `sep()` count is **0**
everywhere — LAW-ONE snake/camel is already clean at the binding level; the debt
is concentrated in **path stems** and **mashed compounds**.

Canonical path stems: **10 MASH** + 23 TAIL (mostly domain/RFC/ISA names —
`sha256`, `crc32`, `base64`, `arm64`, `utf8`: UNKNOWN whether intended
exceptions) + **105 SEP** + 3 bad dirs (`runtime/`, `compile_fail/`, `u64/`).
Identifier compounds: gate **catches 42**, **misses ≥129** genuine compounds
(vocabulary-limited — see GAP-162).

### In-lane decompositions (no `src/*.zig` dependency; ordered by consumer safety)

| Path (violation) | Target | Consumer |
|---|---|---|
| `scripts/scratchcensus.id` | `census/scratch.id` (per GAP-168) | none |
| `scripts/relationid.id` | `scripts/relation.id` or eliminate | none (verify dead) |
| `examples/boring/jsonparse.id` | `examples/boring/json/parse.id` | none |
| `scripts/perfledger.id` | `ledger/perf.id` (per GAP-168) | self + `gate/path.id` negative-control string |
| `scripts/proof/ambientfold.id` | `scripts/proof/ambient/fold.id` | `scripts/capability_scan.id` |
| `scripts/vscodeemit.id` | `scripts/vscode/emit.id` | `scripts/vscodeproj.id`, `tools/node/dev/` |
| `lib/path/filepath.id` | fold into `lib/path.id` / `lib/path/file.id` | `scripts/runtime_bench.id` |
| `scripts/embedledger.id` | `ledger/embed.id` (per GAP-168) | `build.zig:86` |

Do not bulk-move without repointing each consumer and any `gate/path.id`
negative-control string. `scripts/census/*` and `scripts/ledger/*` targets should
land at their GAP-168 rehomed roots (`census/`, `ledger/`), not re-nest under
`scripts/`.

### Cross-lane blocked (exact `.id` path embedded in dirty `src/*.zig` — do not move now)

| Path | Blocking dirty owner |
|---|---|
| `lib/token/grammarrole.id` (mash) | `src/main.zig:835` emit path + log |
| `lib/wasm/opcode_lookup.id` (sep) | `src/lexer_bridge.zig`, `src/main.zig` |
| `lib/wasm/ward_mvp_opcodes.id` (sep) | `src/lexer_bridge.zig`, `src/main.zig` |

### Resolved this session

`lib/token/grammar_role.id` (snake_case duplicate of generated `grammarrole.id`,
divergent `ROLE_COUNT`) — **deleted**. Its vacuous nonempty-file consumer was
also deleted when exact schema, semantic digest, row, slot, and projection
agreement became executable authority; `.agents/RELEASE_READINESS.md` reference
fixed.

### Gate-vocabulary gaps (feed GAP-162 before any bulk pass)

`mash()`/`compound()` miss ≥129 identifier compounds because one part is absent
from the gate `words` string. High-confidence additions: `byte code point count
word type kind sum map set key stamp out new prefix postfix precedence literal
parameter descriptor pattern rpc data query recursion sort max norm watch group
wait strip lock`. Purely-lexical detection over-splits real single words
(`styled`→`sty+led`) — final canonicality must route through the graph (GAP-124),
so complete vocabulary is migration pressure, not authority.

## Classification

| Category | Count | Action |
|---|---|---|
| tool_workflow | 59 | Relocate outside canonical semantic-source teaching space or give one irreducible operational name |
| needs_review | 13 | Audit: semantic home → decompose into real home hierarchy; or tool → relocate |
| obsolete_or_experimental | 7 | `_`-prefixed experiments: delete or classify |
| history_bootstrap | 2 | `duo_*`: rename to remove historical brand |

## Rule

Do NOT merely do `abi_matrix → abimatrix`. That still violates semantic
decomposition. Each path must be classified:

- **semantic home** → decompose into real home hierarchy
- **tool/workflow label** → relocate outside canonical semantic-source
  teaching space or give one irreducible operational name
- **history/bootstrap** → classify mechanically
- **obsolete** → delete

## Enforcement

PATH-ONE must become a real new-path gate, not just constitutional prose.
The existing `gates/path.id` checks added lines but does not prevent new
underscore paths from being created.

## Deletion prerequisite

- Gates must reject new underscore/compound paths at creation time
- Existing debt ratcheted downward via migration ledger
- `.zig` files are foreign realization; path law applies but migration
  priority follows SHC frontier

## Negative control

Create a file `scripts/foo_bar.id` — gate must reject it.
Create a file `scripts/foo.id` — gate must accept it.
