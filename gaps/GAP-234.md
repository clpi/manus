| field | value |
|---|---|
| title | GAP-234 — the `__*` intrinsic namespace is 129 relations with no relation entity |
| status | OPEN |
| filed | !2026-08-25T07:30:00Z |
| priority | P1 |
| kind | expressiveness_gap |
| filed by | claude-reconcile |
| files | src/meta_module.zig,src/parser.zig,src/dnir_lower.zig,docs/spec/directive-ledger.json,gate/directive.sh |

| section |
|---|---|
| Why this gap exists at all |

| # | directive |
|---|---|
| 1 | `gaps/GAP-226.md` excludes compile-time directives by name — "`comptime-directive` (353) is `@comp.*`, whose applications are compile-time and whose relation entities are **a separate owner's**." `gaps/GAP-233.md` repeats the exclusion. |

| # | directive |
|---|---|
| 1 | That owner does not exist. |
| 2 | Measured: |

| # | directive |
|---|---|
| 1 | cd gaps && grep -il 'comptime\|intrinsic\|metaprog\|@comp\.' *.md # 25 files, none owns it grep -rln 'comptime-directive' gaps/ gate/ # gaps/GAP-226.md, gate/application.sh only |

| # | directive |
|---|---|
| 1 | Two gaps route this population to a third that was never filed. |
| 2 | This is that gap. |

| section |
|---|---|
| What the population is |

| # | directive |
|---|---|
| 1 | `@comp.c.emit(x)` is rewritten by the PARSER into an ordinary call node with a `.name` callee spelled `__emit` — `src/parser.zig:7404-7407`, via `meta_module.resolveBuiltin`. |
| 2 | The rewrite is skipped under `self.formatting` (`src/parser.zig:7382`), which is the proof it is a parse-time desugar and not a semantic identity. |

| # | directive |
|---|---|
| 1 | src/meta_module.zig:80 `builtins`, 234 rows -> 129 distinct `__*` internals src/legacy_directives.zig:15-43 25 legacy flat/underscore ingress rows src/meta_dispatch.zig:63-98 34 combinator dispatch rows src/sema.zig:1710 blanket admission: startsWith "__" -> true |

| # | directive |
|---|---|
| 1 | 63 subjects in `examples/` refuse the C realizer on one of 30 distinct `__*` faces — the largest family in `gaps/GAP-233.md`'s split, and 66 once the three `simd.v4f64` subjects are moved here from the non-callable family. |

| section |
|---|---|
| They are lifted as call nodes, and the only phase that consumed them is gone |

| # | directive |
|---|---|
| 1 | There is no pre-graph expansion. `src/macro_expand.zig` contains one `__` occurrence and it is a gensym suffix. |
| 2 | The comptime evaluator expands combinators only when a host hook is installed (`src/comptime.zig:766-769`), and `grep -n meta_hook src/*.zig` finds exactly one production installer: `src/codegen.zig:802 .meta_hook = comptimeMetaHook`. |

| # | directive |
|---|---|
| 1 | `--backend=c` never constructs that CodeGen. `src/main.zig:6042-6046` says so in its own comment — the branch "returns before constructing the retired AST/Lua CodeGen". |
| 2 | So the entire consumption surface (`src/codegen.zig:17232` `__emit`, `:17254` `__c_call`, `:17320` `__static_assert`, `:17410` `__comptimefor`, `:17550` `__type_name`, `:19646`, `:22389`) is unreachable on the graph path. |

| # | directive |
|---|---|
| 1 | They then reach `OccurrenceBridge` as `.call` nodes with no application fact and refuse. **No fact can be published for them**: `recordApplicationInHome` (`src/sema.zig:1342-1358`) requires a `*const ast.FuncDecl`, and `src/sema.zig:1706-1710` states the design outright — these names are "recognised in the call handler by name, never declared in scope". |

| # | directive |
|---|---|
| 1 | **This is a capability regression across backends, not a static gap.** The intrinsics WORK on the retired emitter and are unreachable on the graph path; the migration deleted the consumer without minting the replacement. `idol check` accepts all 63 subjects and the only host-portable realizer refuses them. |

| section |
|---|---|
| Law rules them relations, so the refusal is correct |

| # | directive |
|---|---|
| 1 | `docs/spec/law.md:265-267` (§9): "There is one application algebra. |
| 2 | Function-like, subject-oriented, generic, **builtin**, foreign, and transformed calls are **applications of exact relation/value identities**." `builtin` is named; there is no exemption for a `__`-spelled callee. |

| # | directive |
|---|---|
| 1 | `docs/spec/law.md:219-222` (§6) removes the escape hatch: "an operation whose whole content is 'do this at compile time' is an ordinary relation applied under a stage-delta world". `law.stage.world` (`docs/spec/constitution.md:2794`): "`@comp.*` `@meta.*` `@compiler.*` carry no capability the stage world lacks". `law.at.one` (`:2817`) denies the prefix-directive plane outright. |

| # | directive |
|---|---|
| 1 | So the lawful outcome is NOT "comptime should have consumed them" — that is the retired second control plane. |
| 2 | It is that they are relations and need relation entities. |
| 3 | The refusal is the law being enforced; only its ATTRIBUTION is wrong, which is `gaps/GAP-233.md`'s subject. |

| section |
|---|---|
| Four faces have no implementation anywhere |

| # | directive |
|---|---|
| 1 | `__schema` (`src/meta_module.zig:263`), `__run` (`:273`), `__pipeline` (`:265`), `__fieldsmap` (`:193`) appear in that file and nowhere else: |

| # | directive |
|---|---|
| 1 | grep -rn '__schema\\|__pipeline\\|__fieldsmap' src/*.zig \| grep -v meta_module.zig |

    # (empty)

| # | directive |
|---|---|
| 1 | Seven subjects refuse on them. |
| 2 | They parse, pass the blanket `__` admission, and are consumed by nobody. |
| 3 | These are dead spellings and should be DELETED rather than given relation entities — they are inflating the ledger's denominator. |

| section |
|---|---|
| The destination is already assigned |

| # | directive |
|---|---|
| 1 | `docs/spec/directive-ledger.json` inventories every `@comp.*` form at a code position — `code_occurrences 790, distinct_forms 101, files 132` — and each form already carries a `disposition`: `relation` 55, `transformation` 23, `foreign` 9, `stage` 7, `evidence` 6, `target` 1. `@comp.c.emit` is `foreign`; `@comp.map` is `relation`, rationale "the directive face adds nothing law.infer.one does not already recover". |

| # | directive |
|---|---|
| 1 | `gate/directive.sh` is the ratchet: "This gate does not delete that plane — migration is per-form semantic work recorded in docs/spec/directive-ledger.json. |
| 2 | It stops the plane GROWING." |

| # | directive |
|---|---|
| 1 | A ledger plus a ratchet is a containment instrument, not an owner with acceptance. |
| 2 | That is the hole this gap fills. |

| section |
|---|---|
| Acceptance item 1 is NOT a four-row deletion — measured |

| # | directive |
|---|---|
| 1 | The obvious edit — delete the alias rows for `__schema`, `__run`, `__pipeline`, `__fieldsmap` — was attempted and stopped by the parser's miss path. |
| 2 | When `resolveBuiltin` returns null, `parse_at_form` does not refuse: it falls through `at_builtin_internal_name` and then mints |

| # | directive |
|---|---|
| 1 | return self.new_expr(.{ .macro_call = .{ .name = first.text, ... } }) |

| # | directive |
|---|---|
| 1 | so a deleted spelling does not become "unknown directive"; it becomes a `.macro_call` node routed into the macro-expansion path. |
| 2 | Measured over the corpus, the public spellings behind these four internals appear in 22 files (`comp.pipeline` 11, `comp.run` 4, `comp.fields.map` 4, `comp.schema` 2, `meta.fields.map` 1) — the seven refusing subjects are only the ones that reach the realizer today. |
| 3 | Deleting the rows would silently reroute all 22 through `.macro_call`, which is a semantic change wearing a cleanup's clothes. |

| # | directive |
|---|---|
| 1 | So item 1 decomposes, in order: |

| # | directive |
|---|---|
| 1 | The parser's miss path refuses `@comp.*`/`@meta.*` spellings BY NAME in `.id` mode — the same shape the `@constexpr` arm already has — instead of minting a `.macro_call`. |
| 2 | Only then is a deleted row a truthful refusal. |
| 3 | The five alias rows are deleted and the resolver test carries NEGATIVES for all four spellings, per the `meta.register.derive` precedent already in that test block. |
| 4 | Measured flip set — exactly ten files, every one an examples/ showcase or smoke of the dead faces: |

| # | directive |
|---|---|
| 1 | c_emit_comptime_smoke, comptime_pipeline_showcase, cross_compiler_harness, cross_lang_showcase, exponential_codegen_showcase, pipeline_comptime_fuse_showcase, run_showcase, run_smoke, schema_showcase, user_derive_showcase |

| # | directive |
|---|---|
| 1 | The census moved the honest direction: refused-by-check 43 -> 53, refused-by-realizer 197 -> 187 — the same ten subjects, refusing EARLIER and by name instead of as missing-application-id vapor. |
| 2 | The two lib/ hits (`lib/agent.id`, `lib/meta/codegen.id`) are string literals and a comment; text mentions are not code positions, which is why the candidate grep said 20 and the flip set is 10. `gate/directive.sh` stays green — it stops the plane growing, and this shrinks it. |
| 3 | The owner decided the directive plane is TOTALLY retired, so the ten files above were DELETED rather than rewritten: a showcase of retired vocabulary has no lawful rewrite target until the per-disposition mechanisms of item 2 exist, and keeping a broken teaching example of forbidden vocabulary teaches the forbidden thing. |
| 4 | All ten refused at `idol check` by name before deletion (verified per file). §2 of `gate/realize/census.sh` moved 253 -> 243 subjects and 64 -> 54 refused-by-check under the current binary — exactly the ten. |
| 5 | (The "53" recorded under 1b predates the sema aggregate-parens refusal commit, which moved unrelated subjects into check-refusal.) |

| section |
|---|---|
| Measured residue after 1c |

| # | directive |
|---|---|
| 1 | What remains of the retired plane. |
| 2 | The LIVE numbers are the runner's — `sh gate/directive.sh` prints the current census against the ledger's pins, and `docs/spec/directive-ledger.json` is where a count is allowed to live; values quoted below are the measurement AT the 1c deletion commit, kept as history of what moved, never as current authority: |

| # | directive |
|---|---|
| 1 | A strict code-position census (comment, string, and `--- @` hint positions classified separately — the loop is in this gap's history via the audit) counted 637/121/91 immediately before the deletion, below the gate's comment-strip-only counting at the same tree because `sed 's/#.*//'` strips comments but not string literals. Recompute, don't cite. |
| 2 | `@compiler.` had zero occurrences in tracked `.id` (`git grep -c '@compiler\.' -- '*.id'`). |
| 3 | `@meta.` was previously OUTSIDE the ratchet — `gate/directive.sh` matched only `@comp\.`. The gate now censuses all three retired namespaces; the amended census found the `@meta.*` forms (string literals in `lib/meta/codegen.id`) and they are ledger rows now, so the gate itself reports them from here on. |
| 4 | Ledger drift existed before this pass — the tree had already shrunk under the pins without the ledger being lowered. The 1c regeneration locked the whole ledger to the post-deletion tree by the gate's own counting; the per-form values live in the ledger, and `sh gate/directive.sh` fails if the tree ever exceeds them. Forms at zero keep their rows: a zero row records the form as known-and-closed rather than reopening the namespace. |
| 5 | Eight `--- @comp.c.include(...)` doc-hint occurrences were SEMANTICALLY LIVE (`src/lexer.zig:938-943` harvests `--- @` triple-comment text into `pending_hints`); the ten-file deletion removed the four in `run_showcase.id` and `cross_lang_showcase.id`, leaving four in `examples/c_interop_showcase.id`. |
| 6 | `src/parser.zig:7472` `compatibility_at_diagnostic` is still WARN-ONLY for RESOLVED prefix directives, so resolved `@comp.*`/`@meta.*`/`@compiler.*` spellings still parse in `.id` mode. Full retirement requires the item-2 per-disposition migration to land before that diagnostic can become refusal — turning it now would refuse the 710 live uses with no destination. |

| section |
|---|---|
| Acceptance |

| # | directive |
|---|---|
| 1 | Items 1a-1c above, in that order — 1a is the guard that makes 1b safe. |
| 2 | Per-disposition, not per-face: the 55 `relation` forms become ordinary relations applied under `expr@{ stage = compile }` (GAP-227's world), and the 9 `foreign` forms become foreign origin/ABI facts — which `src/directives.zig:59-65` already anticipates ("The operations behind them are foreign origin/ABI realizations awaiting an owning graph fact"). |
| 3 | `gate/realize/census.sh` shows the `__*` faces leaving the refusal histogram as each disposition lands, rather than a count pinned here. |
| 4 | Explicitly disjoint from GAP-226 (43 host-relation subjects) and sequenced after GAP-233 (the note split), so the two families stop being read as one. |
