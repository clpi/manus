# GAP-216 — 128 declared answers have no live checker

**Status:** REFUTED (Break 1) · **Break 2 stands** · **Filed:** !2026-08-22T05:30:00Z
**Priority:** P0 · **Kind:** evidence_gap
**Filed by:** claude-gap145
**Files:** scripts/boring_corpus.id,scripts/capability_table.id,scripts/native_differential.id

| # | directive |
|---|---|
| 1 | The example corpus declares its own expected answers. **Nothing reads them.** Two independent breaks, either of which alone would be sufficient. |

## REFUTED — Break 1. The live checker exists.

| # | directive |
|---|---|
| 1 | `../idol-native/gate/expect.sh` — 46 KB, dated 2026-08-20 — reads `# expect:` with the correct spelling, runs both realizations, carries a pinned per-file/per-backend ledger, and fails on any verdict that moves. |

| # | directive |
|---|---|
| 1 | **Run as shipped it is RED: `rc=26`, 26 verdicts moved over 131 files.** |

| # | directive |
|---|---|
| 1 | It is cited from inside this corpus at HEAD — `examples/text/brace/escape.id` says *"`gate/expect.sh` records it"* and *"the oracle is pinned `fail` rather than deleted"*; `examples/projection/{place,remove,shadow_place}.id`, `gaps/GAP-173.md`, `gaps/GAP-174.md` and `docs/module-globals.md` all name it. |

| # | directive |
|---|---|
| 1 | **This gap was filed by grepping only `/Volumes/d 1/x/idol`** — the same census defect as GAP-212, one level down, in the same session. |
| 2 | The census is also off by three: **131 files carry `# expect:`, not 128**; `grep -rl` drops two deliberately-invalid-UTF-8 fixtures as binary. |

## What the live checker actually reports

| # | directive |
|---|---|
| 1 | **6 red files, 4 classes**, and two of the classes are one defect seen twice — each is a `main`-scope and a module-tail spelling of the same program, so the pair is its own control that the defect is not a scoping artifact. **Both realizations agree on both**, which is why the C-vs-direct differential is structurally blind to them. |

| class | files | declared | measured |
| --- | --- | --- | --- |
| byte length under an embedded NUL | `table/strnul.id`, `table/str/nul.id` | 3 | 1 |
| signed-char comparison in `str` ordering | `table/strorder.id`, `table/str/order.id` | `1 1 1` | `1 1 0` |
| §12 TAIL, no frame elision | `table/tailcall.id` | 10000000 | **exit 139, SIGSEGV** |
| ORACLE-AMBIGUOUS, not a defect | `src/testdata/result_member_record.id` | 41 | prints 7, exits 41 |

| # | directive |
|---|---|
| 1 | The last row must not be made green: the fixture deliberately prints 7 *and* declares exit 41, and the corpus has no marker distinguishing the two conventions. |
| 2 | No `# expect:` line was edited. |

## The real finding: 74 of 131 declared answers are never compared

| # | directive |
|---|---|
| 1 | **56% produced no answer at all** — the compiler refused them, so the declared answer is neither green nor a defect. |
| 2 | The existing gate records those rows as `dead` **with no reason**. |
| 3 | That reason is one `sed` on the compiler's `bail site:` line, and adding exactly that column produces the frontier: |

```
17 unresolved-application-facts     3 method-unresolved:split
 8 param-type:inferred              3 method-unresolved:push
 8 assign-target                    3 concat
 5 gen-for-dynamic-iter             2 unsupported-conversion
 5 DNB007 undefined symbol          2 block-tail-no-return
 4 keyed-table-export               4 front-end errors
 3 neg                              7 singletons
```

## Nine regressions the reconnection exposed

| # | directive |
|---|---|
| 1 | All nine were pinned `pass` on 2026-08-15 and **none has been touched since**, so the source did not move — the compiler did. |
| 2 | Notably `examples/text/brace/raw.id` now fails at the FRONT END: `json: str = '{"k": 1}'` errors *"declared as 'str', but initializer has type '[]u8'"*. |
| 3 | The byte face stopped coercing to `str`, and the sibling gate's own header documents this file as `pass pass`. |
| 4 | Eight other rows moved the other way and need re-pinning. |

## Break 2 stands, and is worse than filed

```
files carrying `# expect:`    128
files carrying `-- expect:`     1
```

| # | directive |
|---|---|
| 1 | `scripts/boring_corpus.id` and `scripts/native_differential.id` both grep for `-- expect:`: |

```
sed -n 's/^-- expect: *//p' {path} | head -1
"  FAIL  {name}  carries no '-- expect:' line\n"
```

| # | directive |
|---|---|
| 1 | GAP-145's comment migration retired `--` in favour of `#`. |
| 2 | The corpus moved; the checkers did not. **128 declared answers were silently disconnected from the only thing that compares them.** |

### The three `.id` checkers cannot be repaired inside the admitted subset

| # | directive |
|---|---|
| 1 | `scripts/boring_corpus.id`, `scripts/capability_table.id` and `scripts/native_differential.id` all fail to compile at HEAD — `print-arg:any`, `ret-type:any`. |
| 2 | So even with the right spelling there is nothing to run. |

## What it cost, measured

| # | directive |
|---|---|
| 1 | `examples/table/strnul.id` declares `# expect: 3`. |
| 2 | It measures **1**. |
| 3 | Its own comment claims *"this row was red earlier in the epoch and is now green."* |

| # | directive |
|---|---|
| 1 | That is the complete answer to *why nothing caught the NUL-truncation class*: the answer was written down, the file says it is green, and no live instrument has compared the two since the comment migration. |

## The class

| # | directive |
|---|---|
| 1 | This is GAP-212 one level up. |
| 2 | GAP-212 found 27 cited gates that never existed; this is a checker that exists, is cited, and reads a spelling the corpus stopped using. **A dead instrument and a mis-addressed one are indistinguishable from a passing one** — both are silent. |

## Acceptance

1. The checkers compile and run.
2. They read `# expect:`, and a file carrying `# expect:` that answers something
   else **fails**.
3. Positive control: `examples/table/strnul.id` must be **red** on the first
   run, because it declares 3 and measures 1. A checker that reports the corpus
   green on day one has not been connected — that is exactly the state being
   filed.

## Re-filing

| # | directive |
|---|---|
| 1 | This gap should be re-filed as **"the `dead` column has no `why`, and the corpus tooling that would supply it is 73 of 81 refused"**. |
| 2 | Corpus-wide, **8 of 81 `scripts/*.id` compile at HEAD**. |
| 3 | Top blockers across the tooling layer: 21 `unresolved-application-facts`, 9 `ret-type:any`, 8 `method-unresolved:read`, 5 `keyed-table-export`, 4 each `gen-for-dynamic-iter` and `concat`, 6 front-end errors. |

| # | directive |
|---|---|
| 1 | Minimal repro isolated for `concat`, four lines: |

```id
n = 0
main: i64 = ()
  print("count {n} done")
  0
```

| # | directive |
|---|---|
| 1 | A module-scope **string** in a hole compiles; a **local** integer in a hole compiles; `m = n` then `print("{m}")` compiles. |
| 2 | The refusal is specifically an interpolation hole naming a module-scope **integer** binding — the same seam as gap[214]. |

| # | directive |
|---|---|
| 1 | **Do not land a second gate.** The correct move is one column on the gate that already exists. |
