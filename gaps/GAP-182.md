# GAP-182 — runtime guards as semantic experiments

**Status:** OPEN · **Filed:** !2026-08-18T15:35:00Z
**Priority:** P1 · **Kind:** research_gap
**Filed by:** devin-semantic-20260818
**Files:** docs/spec/constitution.md, docs/spec/law.md, src/effect.zig, src/deopt.id

Unify inline caches, type guards, branch profiling, shape checks, version checks,
and hardware counters as one mechanism: observe proposition `P` at runtime, refine
semantic knowledge, and unlock a conditional realization. Static proof, profile
evidence, and guarded speculation remain distinct epistemic categories, but the
same graph handles all three.

## Current measured boundary

Guards exist as JIT/deopt machinery in foreign systems. Idol has no unified
runtime-experiment fact family; profile and guard are not first-class semantic
entities.

**Crossed:**

- `law.assumption` and `law.guard` are recognized in C0.
- `deopt` is listed as a research direction.
- 0a4f820fa: deopt boundary consumes graph-emitted assumptions; measured
  invalidation selects the recorded next realization (order 4).
- `SemanticShare` + `selectPreferred` in `src/effect.zig`: measured subject
  shares select among ADMISSIBLE candidates only (`law.profile.evidence`
  required pattern — evidence selects, guard/fallback preserve truth); a
  maximal share never promotes an invalidated candidate, and zero evidence
  degrades to plain `select` order. No epistemic-probability arithmetic
  exists beside it (order 2). Evidence: `zig build effect-test`.
- `CounterObservation` + `observeCounter` in `src/effect.zig`: hardware
  counters produce experiment-shaped facts through exactly one construction
  seam (`law.fact.producer.one`). A counter reading that names no measured
  subject revision constructs NO fact — provenance is the admission
  condition (`law.evidence.subject.one`), and the produced fact is still
  profiled evidence that never admits a realization without a guard or
  proof (order 5; hardware counters as a fact-producer).
  Evidence: `zig build effect-test` (test "hardware counters are
  fact-producers with construction-forced provenance").
- `RuntimeFact` + `observeFact` + `refine` in `src/effect.zig`: runtime
  facts refine the candidate set (`if P then candidate C is admissible`).
  Assumption truth enters as a FACT with construction-forced provenance —
  an observation naming no measured subject revision constructs NO fact —
  and `refine` admits a candidate only when the fact set affirmatively
  answers its proposition and every stated assumption; absent affirmation
  is not affirmation, facts never promote past the guard, and the walk
  composes with `deoptOrFallback` verbatim. Evidence: `zig build
  effect-test` (test "runtime facts refine the candidate set").
- `GuardInvalidation` + `guardFalse` + `invalidationRefutes` +
  `selectUnderInvalidation` in `src/effect.zig`: guard invalidation is a
  NAMED fact — `guard <proposition> is false on <measured subject
  revision>` — with exactly one construction seam and provenance forced at
  construction (an invalidation naming no measured subject revision
  constructs NO fact). The named fact refutes exactly the experiment whose
  proposition it names (sound experiments untouched), and deopt selection
  under the fact answers exactly what `select` answers — identity added,
  no second authority over which realizations hold. Evidence: `zig build
  effect-test` (test "guard invalidation is a named fact with
  construction-forced provenance").
- `selectByEpistemicLevel` in `src/effect.zig`: one executed boundary
  walks a candidate set spanning all producer-backed epistemic categories
  (proven, inferred-sound, guarded — the guarded candidate built from an
  assumption emitted by `assumption_guard.buildFromModule` over an
  executed semantic graph — profiled, sampled, heuristic) plus the
  seventh level `axiom`, which NO producer maps to and whose law
  (`admitsWithoutGuard`) is nonetheless consumed uniformly. The category
  decides admissibility — sound levels answer on their fact alone,
  evidence levels admit only under an affirming runtime fact — and never
  priority. With no affirming facts an evidence-only candidate set
  selects nothing, exactly `select`'s answer, so deopt fallback
  composition is unchanged. Evidence: `zig build effect-test` (test "all
  seven epistemic categories wired through one boundary").

- `constitutional_axiom` + `axiomOf` in `src/effect.zig` (this revision):
  the seventh epistemic level `axiom` is wired to exactly one evidence
  producer through exactly one construction seam. `axiomOf` mints the
  axiom-level `Experiment`; its producer maps to `.axiom` in the `level()`
  taxonomy, is never invalidated by any named `GuardInvalidation`
  (`invalidatedExperiment` answers `false` for every `admitsWithoutGuard`
  level), demands no measured subject revision (`isEvidence` false, so
  `provenanceComplete` is unconditional), and admits without a guard. The
  one executed boundary `selectByEpistemicLevel` consumes the level exactly
  as it consumes `proven` and `inferred_sound` — on its fact alone, never
  through priority. Evidence: `zig build effect-test` (test "axiom producer
  wires the seventh epistemic level through one seam").

**Still missing:**

- `experiment(P)` = `{ proposition, evidence kind, cost, conditional theorem }`
  AS A GRAPH-EMITTED tuple (the struct exists effect-side; no graph fact
  emission owns it yet — required order 1's graph face).
- ~~Distinct epistemic categories~~ WIRED THROUGH ONE BOUNDARY, AND the
  `axiom` level wired to its producer: axiom, proven, inferred-sound,
  guarded, profiled, sampled, heuristic — crossed above
  (`selectByEpistemicLevel` + `axiomOf`), with the guarded candidate
  emitted from an executed graph.
- ~~Consuming the boundary from an optimizer path outside the test seam~~ —
  crossed above (`src/main.zig` explain command now converts graph-emitted
  assumptions into Guarded candidates and calls `selectByEpistemicLevel`,
  demonstrating the epistemic taxonomy is consumed in an actual optimizer
  path rather than only in test seams).
- ~~Guard invalidation = assumption false ⇒ realization inadmissible~~ —
  crossed above (`GuardInvalidation` + `selectUnderInvalidation`).
- Profile evidence does not prove semantics; it is evidence only —
  the rule is effect-side law (`producesTruth`/`profileNeedsGuard`); the
  graph-owned enforcement that no optimizer path bypasses it remains open.
- ~~Hardware counters as a fact-producer~~ — crossed above (`observeCounter`).

## Required order

1. Add `experiment` and `guard` as graph facts with provenance.
2. Distinguish semantic probability from compiler epistemic probability.
3. Make a guarded realization admissible only under its stated assumptions.
4. Implement invalidation: assumption becomes false ⇒ candidate ceases to be
   admissible and deoptimization selects another.
5. Treat profiles, samples, and hardware counters as evidence producers,
   not semantic truth.

## Closure acceptance

Close when all runtime specialization (inline caches, type/shape/version guards,
profiling, hardware counters) is modeled as a `guard(P) ⇒ candidate` fact with
explicit invalidation semantics, and when profile evidence can never justify a
semantics-changing optimization without a guard or proof.

## C0 alignment

Instance of the schema in `gaps/RESEARCH-SPINE.md`; `tools/node/dev/gapc0` fails
closed without it.

- **constitutional owners:** witness (a guard is a conditional witness) · effect · trust (`guarded` is an existing level in C0 `trust.order`)
- **kind delta:** 0
- **registry delta:** 0
- **source syntax delta:** 0
- **semantic invariant:** `experiment(P)` = proposition + evidence kind + conditional theorem; a guarded realization is admissible only under its stated assumptions; assumption false ⇒ realization inadmissible.
- **physical mechanism:** guard emission and invalidation; deopt selects the next candidate; hardware counters as fact producers.
- **prerequisites:** GAP-181.
- **foreign implication:** foreign runtimes' inline caches and deopt machinery expressed as the same fact family.
- **world implication:** guards are world-qualified evidence; a guard is not "JIT machinery", it is runtime experiment under an effectful relation.
- **projection implication:** none.
- **demand implication:** the conditional theorem is demanded-quotient scoped.
- **realization implication:** static proof, profile evidence, and guarded speculation are epistemic levels of ONE mechanism — one graph handles all three; profile evidence never justifies a semantics-changing optimization without a guard or proof.
- **evidence:** one guarded specialization with measured invalidation and fallback behavior.
- **deletion witness:** JIT guard machinery as a separate subsystem; unlabelled profile-truth.
