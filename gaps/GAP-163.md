# GAP-163 — ZERO-HISTORY reconciliation

**Status:** OPEN · **Priority:** P0 · **Kind:** architecture

## Current obligation

Active tree contains current Idol, current foreign interoperability, and bounded
executed bootstrap bridges only. Git is the sole historical archive.

## Acceptance (merge priority)

1. C0 `law.zero.history` — no `language.history` table
2. Generated harness stable vs ephemeral session envelope split
3. Open gaps current-only (no pass chronology)
4. Pass-era example topology deleted — fixtures under semantic homes
5. Retired names absent from agent projections (`identitycensus`)
6. `tools/node/dev/zerohistorycensus` passes on committed tree

## Remaining cutover

- `duo`/`DUO_` prefix purge in build, env, MCP tool names (semantic gate not grep-only)
- `lib/` rehome and STD-ZERO
- req/module metadata removal (MODULE-ZERO)

## Deletion condition

`zerohistorycensus` all required counts zero including duo/DUO_ warnings.
