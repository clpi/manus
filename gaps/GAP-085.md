# GAP-085 — the ad-hoc trust system already exists, and absence is not expressible

**Status:** PARTLY CLOSED — **the disconfirming case HOLDS and the finding is
smaller than filed.** · **Filed:** 2026-08-08 · **Adjudicated:** 2026-08-08
· **Re-measured 2026-08-09**

## RE-MEASURED 2026-08-09 — the numbers hold, the GATE WAS NEVER WIRED

The adjudication below re-runs clean. `scripts/bootstrap_scan.duo` reports:

```
row          count   budget
   upper       433     444
   companion   40      41
   relation    44      45
   assume      17      17
bootstrap scan: PASS — 517 sites, none rising
```

So `assume` is still exactly 17 at a budget of 17, and item 1 of "What landed"
is accurate about the row.

**It is not accurate about the gate.** "What landed" reads *"`zig build
bootstrap-scan` gains an `assume` row"*. Measured today:

```
$ zig build bootstrap-scan
error: no such step: bootstrap-scan
```

The SCRIPT has existed since `c8d6e06`. **The build step never did** — `grep
bootstrap build.zig` returned a single hit, in an unrelated comment. So the row
that exists *"so the eighteenth cannot arrive unread"* was not being read by
anything: nothing ran it, and an eighteenth flag would have arrived silently.

**Wired 2026-08-09** as `zig build bootstrap-scan`. Both exit directions were
checked before wiring, per gap[070]'s rule that printing FAIL is not evidence a
step fails: PASS exits 0, and a copy with the `assume` budget lowered to 1
prints `!! assume 17 1` and exits 1.

**This is the third gate in this cluster found written-but-unrun**, and the
pattern is worth naming because all three were recorded as delivered:
`scripts/fsremoveproof.duo` (cited by two gaps, never written),
`scripts/fsmkdirproof.duo` (written, unwired), and this one. **A gap saying a
gate landed is not evidence the gate runs.** The check is one command and none
of the three filings ran it.

---
*Adjudication of 2026-08-08 follows, unamended and re-verified.*

## VERDICT FIRST (2026-08-08)

The disconfirming case at the bottom of this file was run before anything was
built, as it demanded. **It holds.** Not one of the unverified flags is an
unguarded value claim. The 35 was a miscount three ways over, and the real
finding is the smaller one this file already anticipated: they are
**unenumerable**, and nothing stops the next one arriving unread.

| | filed | measured |
|---|---:|---:|
| `use_*` flags reaching codegen | 63 | **55** (`use_*: bool` on the func-body descriptor in `src/ast.zig`) |
| unverified | 35 | **27** by name-match, **17** after retirements and dead fields |
| unverified AND an unguarded value claim | implied 35 | **0** |

Where the 63 came from: `grep use_[a-z0-9_]+` over `src/*.zig` matches
**substrings of unrelated identifiers** — `clause_binds`, `fuse_const_binop`,
`reuse_audit`, `__fish_use_subcommand`, `func_body_should_use_expr_stmt` — plus
locals (`use_flag` = a `-fprofile-instr-use=` string), a diagnostic repair kind
(`"use_backend"`), a comment, a `.duo` string literal in a test fixture, and a
`use_count: u32` field that counts uses. Thirteen of the 68 unique matches are
not flags at all.

Of the **27** real declared-and-unverified fields:

- **8 are HARD-RETIRED** — `fb.use_x = false;` unconditionally, each with a
  measured comment saying why: `dense_table_max`, `ema_smooth`,
  `grid_sum_inline`, `life_native`, `matmul_native`, `nbody_native`,
  `sparse_dot_inline`, `table_lookup_sum`. A retired flag cannot reach an
  emitter. This is §3's rule already being obeyed, by hand, invisibly to a grep.
- **2 are DEAD FIELDS** never assigned anywhere: `use_dense_table_sum` (its
  emitter at `codegen.zig:9219` is unreachable) and
  `use_fp_strict_always_inline`.
- **17 are live** — and all seventeen were read individually. See below.

## The ten (seventeen) read, individually

| flag | assumes | what checks it | verdict |
|---|---|---|---|
| `use_dense_table_identity_sum` | the fill composed with the reduction is exactly `p(i) = i` | the detector at `sema.zig:4940-5090` reads the fill polynomial out of the AST (`dense_table_assign_poly`), reads the reduction (`dense_table_sum_reduction`), composes them, and requires degree 1 with `c1 == 1, c0 == 0`. Any body statement that is not an assign → `return`. | **SOUND**, grep-invisible |
| `use_dense_table_quadratic_sum` … `_decic_sum` (10 flags) | a degree-2..10 closed form | same detector; every coefficient is **read from the source** and stored on the descriptor (`dense_table_sum_*_mul`). The emitter prints no constant it did not extract. | **SOUND** |
| `use_dense_table_faulhaber_sum` | degree 11-12 | same, coefficients in `dense_table_sum_coeffs` | **SOUND** |
| `use_dense_table_square_sum` | `p(i) = i²` | same detector, exact-coefficient arm | **SOUND** |
| `use_dot_product_dense` | the n(n+1)(n+2)/6 template | `verify_dot_product_identity(fb)` — **deliberately shared** with the identity flag, with the reason in the source: both emitters print the same closed form, so gating only one "would have handed every declined function straight to the identical frozen answer one arm down the chain." A name-matching grep cannot see this. | **SOUND** |
| `use_iterative_fib` | the body IS naive Fibonacci | `detect_naive_fib_pattern` is a **total structural match**: 1 param, exactly 2 stmts, `if p <= 1 return p`, `return f(p-1) + f(p-2)`, and both calls must be **self**-recursive. Constants 1/1/2 are Fibonacci's definition, not a benchmark's parameters. This is where §3's name-keying defect was repaired, with the measurement recorded in the source. | **SOUND** |
| `use_simd_reduction` | a counted loop accumulates | nothing — and it needs nothing: the emitter prints `#pragma clang loop vectorize(enable) interleave(enable) unroll_count(8)`. It **computes no value**. A wrong guess costs speed, never an answer. | **SOUND — semantically neutral** |
| `use_force_always_inline` | inlining is wanted | set by the user's `inline` attribute (`directives.zig:519`). An inlining hint. | **SOUND — semantically neutral** |
| `use_dense_table` | a table is densely indexed | not a value claim: it selects a **representation**. Guarded by `dense_walk` clearing `ok` on anything unmodelled, `solve_index_bound`, `cap_safe`, alias-group pruning — and the accessors **grow on demand**, so a wrong capacity costs memory, not correctness. | **SOUND** |

### Verified by value, not by reading

Built in an isolated worktree (`zig-out/bin/duo` is a shared mutable surface).

```
t[i] = i        s += t[i]   n=10 → 55           n=1000 → 500500        closed form FIRED: return (n*(n+1))/2
t[i] = i*i      s += t[i]   n=10 → 385          n=1000 → 333833500     DECLINED to the array path
t[i] = i*3+7    s += t[i]   n=10 → 235          n=1000 → 1508500       DECLINED
t[i] = i*i*i    s += t[i]   n=10 → 3025         n=1000 → 250500250000  DECLINED
t[i] = i³+5i+3  s += t[i]   n=10 → 3330         n=1000 → 250502755500  DECLINED, and equals the declining reference path
t[i] = i³+9i+3  s += t[i]   n=10 → 3550         n=1000 → 250504757500  perturbation TRACKS the source
```

Every value matches hand-computed ground truth. And the range guard, tested
directly: `while i <= n  if i > 5 break  t[i] = i  s += t[i]` over `n = 50`
answers **15**, not 1275 — the detector declines rather than answering a
question the program stopped asking.

Two things this measured that reading alone did not: the closed form fires for
**only the identity arm** in practice (the other 12 polynomial flags are live in
source but the fill extractor never matches a higher-degree spelling, so they
are dead in practice too), and the general path is correct at every degree.

## What is actually wrong, then

Not the flags. **The absence of a census.** Six mechanisms (`use_`, `verify_`,
`detect_`, `assume`, `noalias`, `likely`), none composable, none a fact, and no
way to ask "which claims here are asserted rather than proven" — so the honest
adjudications above (eight retirements with measured reasons, a deliberately
shared verifier, three semantically-neutral hints) are invisible, and the next
unadjudicated flag would be equally invisible. That is the real defect and it is
exactly frontier item #3.

## What landed

1. **`zig build bootstrap-scan` gains an `assume` row**, budget **17**,
   ratcheting down — a flag that is *declared* in `src/ast.zig`, *live*
   (assigned anything but `false`), and has *no `verify_` companion*. Five
   controls run before any row prints, per that file's existing discipline.
   Positive-controlled four ways: an 18th flag → `!! assume 18 17`, exit 1;
   adding a verifier → back to 17; retiring to `false` → back to 17; breaking a
   pattern → exit 3 with **no rows printed**.
2. **`trust` levels named in `docs/spec/constitution.duo`** — inferred · proven ·
   observed · asserted · foreign · guarded, with `law.fact.trust` and
   `trust.rest = .asserted` (an unlabelled claim takes the WEAKEST level, not
   the strongest — a default of `proven` is how six mechanisms became
   invisible).
3. **Negative facts enumerated** as `absence@{ sharing, reentry, failure,
   observer, effect }` with `law.fact.negative`. **No new grammar** — see below.

## Correction to §"absence is barely expressible"

Two errors in this file's own measurement.

**The spelling already exists and is in daily use in C0.** `constitution.duo`
writes absence as a false-valued descriptor field in at least six places:
`syntax.anchor@{ prefix = false }` · `syntax.name@{ underscore = false,
uppercase = false }` · `syntax.block@{ semicolon = false }` · `subject@{ named =
false }` · `kinds.authority = false` — beside `std@{ ambient = false }`. The
count of 4 saw only the last. A2 NNS is satisfied by inspection: nothing is owed
at the surface, only the enumeration.

**`alias` is a RESERVED WORD.** This file proposes `x@{ alias = false }` by
name; the parser rejects it as a field. Measured: `sharing`, `reentry`,
`failure`, `observer`, `effect`, `purity`, `region`, `contract`, `world`,
`ownership` all pass; only `alias` does not. The negative fact is spelled
**`sharing`**.

---
*Original filing follows, unamended.*

**Status:** OPEN · **Filed:** 2026-08-08
**Law:** A5 WITNESS (no optimization or claim without an inspectable witness) ·
`law.fact.primitive` · §3 MEASUREMENT HONESTY
**Frontier items:** #1 negative facts / proof of absence · #3 trust/proof
lattice for unverifiable user knowledge

## The warning, and the measurement

The frontier review warns that performance pressure "will eventually create
ad-hoc `unsafe`, `assume`, `likely`, `noalias`, `aligned`, `pure`, `constexpr`
mechanisms all over again" unless one trust/proof lattice covers inferred,
proven, observed, asserted, foreign-guaranteed and guarded facts.

Measured over tracked `src/*.zig`, 2026-08-08. **It is not a future risk.**

| | count |
|---|---:|
| `use_*` assumption flags reaching codegen | **63** |
| `verify_*` companions | **28** |
| **assumptions with NO verifier** | **35** |
| `detect_*` recognizers | 42 |
| `assume` / `noalias` / `is_pure` / `likely` / `unlikely` sites | **51** |

Six distinct ad-hoc trust mechanisms — `use_`, `verify_`, `detect_`, `assume`,
`noalias`, `likely` — none of them a fact, none of them witnessed, none of them
composable, and **35 assumptions with nothing checking them.**

This is exactly the system the frontier review says to avoid building, already
built, and it grew by accretion rather than decision — which is how it escaped
notice.

## Why 35 unverified is the sharp number, not 63

CLAUDE.md §3 already carries this rule, earned the hard way:

> **A detector must verify every constant its emitter assumes**, and decline to
> the general path otherwise. Measured 2026-08-08, **46 `use_*` flags reach
> codegen and 7 carry a `verify_*`**.

The count has since moved to 63/28, so the ratio improved (7/46 = 15% to 28/63 =
44%) while the absolute hole grew (39 to 35, roughly flat). Neither number is
tracked by a gate. §3 states the rule and nothing enforces it.

That section also records what unverified assumptions cost here: ten fabricated
benchmark kernels, three returning frozen literals, and
`use_dot_product_identity` emitting `n(n+1)(n+2)/6` while **verifying nothing
about the fill** — the identical defect that made `detect_binary_search_dense`
answer 200000 where C answered 100000.

## The other half: absence is barely expressible

Optimization depends on negative facts — no alias, no metatable write, no
foreign re-entry, no other reachable implementation, no failure edge, no
observing thread. Measured:

| negative fact spelling | sites |
|---|---:|
| `@{ ambient = false }` | **4** |
| every other negative fact | **0** |

One spelling, four uses, all of it the `std@{ ambient = false }` protocol. There
is no `@{ alias = false }`, no `@{ reentry = false }`, no `@{ failure = false }`.

So the 63 `use_*` flags are what stands in for absence: **the compiler cannot
say "no alias exists," so it says `use_noalias` and hopes.** The two frontier
items are one defect — an unmodelled trust dimension — and the ad-hoc mechanisms
are the shape it takes when the model is missing.

## What a lattice would have to carry

Not six mechanisms. One fact with a trust level, per the review:

```
inferred            the compiler derived it
proven              witnessed, A5 satisfied
observed            profile-time, carries its sample
asserted            the user knows and the compiler cannot check
foreign             a lawset guarantees it, trust-tagged
guarded             true under a runtime check, with its descent
```

`asserted` is the one that matters and the one the tree lacks entirely. There is
no way to say "I know this and you cannot prove it" and have it (a) recorded,
(b) counted, (c) invalidatable, (d) attributable when it turns out false. The 35
unverified `use_*` flags are asserted facts wearing no label, which is why
nobody can enumerate them.

## Fix, in order

1. **A gate over the 63/28 split**, ratcheting the unverified count DOWN.
   §3 states the rule; make it a number. `zig build bootstrap-scan` already
   ratchets three rows over `src/*.zig` and is the natural home.
2. **Name the trust levels as facts** in `constitution.duo`, so `asserted` has
   a spelling and therefore a census.
3. **Negative facts get a spelling** beyond `ambient = false`. `@{ alias =
   false }` is the same form the anchored protocols already use — NNS, no new
   surface (A2).
4. **Every `use_*` becomes a fact with a trust level.** The ones that survive
   are `guarded` or `proven`; the rest are `asserted` and countable.

## The disconfirming case, stated

If the 35 unverified flags turn out individually sound — each one guarded by
something the grep cannot see — then the number is noise and the finding is only
that they are *unenumerable*, which is still worth fixing but is a smaller
claim. **Read ten of them before writing the gate.** §3's own rule applies to
this gap: verify by value, not by count.
