| field | value |
|---|---|
| title | GAP-233 — one diagnostic identity is carrying four causes |
| status | OPEN |
| filed | !2026-08-25T06:20:00Z |
| priority | P1 |
| kind | diagnostic_identity |
| filed by | claude-reconcile |
| files | src/dnir_lower.zig,src/main.zig,gate/application.sh |

| section |
|---|---|
| The measurement |

| # | directive |
|---|---|
| 1 | `idol compile <subject> --backend=c --emit=c` over `examples/`, x86_64-linux-gnu. |
| 2 | Of 253 subjects, 210 pass `idol check`; of those, 141 refuse with `missing-application-id` and 13 realize. |

| # | directive |
|---|---|
| 1 | Until `5f74ee27` the C arm printed only the note, so all 141 reached a reader as the same six words. `refuseApplication` had been binding `diagnostic.relation` from `applicationFace` the whole time and the renderer discarded it — the arm twenty lines below it, for `c_backend.emitSource`, already printed its own `in relation:`. |
| 2 | With the face printed, 141 separates into four: |

| # | directive |
|---|---|
| 1 | 63 compiler intrinsic (`__*`) __emit 10, __comptimematch 6, __c_call 5, __static_assert 4, __comptimemap 3, and ~20 more, each 1–2 43 host relation, no entity new 9, assert 5, setmetatable 4, type 2, cast 2, typeof, sqrt, random, … 20 no face bound at all curried `f(a)(b)`; see the correction below 15 non-callable subject applied tt 11, u, v4f64 3 |

| # | directive |
|---|---|
| 1 | 60 distinct faces in total. |

| # | directive |
|---|---|
| 1 | **MEMBERSHIP CORRECTED, and the total is a coincidence.** `v4f64` is NOT a non-callable subject: the three subjects spell `simd.v4f64(1.0, …)`, `simd` is a builtin global (`src/sema.zig:2861`), and both sema (`src/sema.zig:5029`) and codegen (`src/codegen.zig:23138`) already treat it as a constructor. |
| 2 | The face reads `v4f64` only because it is the last dotted segment. |
| 3 | Those 3 belong with the intrinsic/builtin family, taking it to 66. |

| # | directive |
|---|---|
| 1 | Three subjects were missed by the face grep and belong here instead: `examples/call_index_assign.id` (face `arr` — the file this gap's own acceptance |

#2 names), `examples/mem_demo.id` (`self._bufs(...)`), and
| # | directive |
|---|---|
| 1 | `examples/json_direct_iteration_smoke.id` (`decoded_arr(1)`). |
| 2 | So the family is still 15, with three out and three in. |
| 3 | The arithmetic coincidence is why acceptance #4 asks for a runner: a hand-derived membership survived a recount that it should not have. |

| section |
|---|---|
| Why this is a defect and not a report |

| # | directive |
|---|---|
| 1 | **`law.fact.producer.one` in the diagnostic layer.** One identity that four causes reach is not an identity; it is a bucket. |
| 2 | A reader who greps `missing-application-id` cannot tell which question was asked, and a gate that pins the count cannot tell which one it caught — the same sentence `a59c8ef9` used when it renamed `result-pack-arity` because two producers spelled one note. |

| # | directive |
|---|---|
| 1 | Each family wants a different answer: |

| # | directive |
|---|---|
| 1 | **compiler intrinsic** — `__emit`, `__c_call`, `__static_assert` and the `__comptime*` set are compile-time faces. Whether they should have relation entities at all is a question for that owner, and `GAP-226`'s acceptance does not cover them. They are the LARGEST family and were being read as that gap. |
| 2 | **host relation with no entity** — this is `GAP-226` proper, and it is 43 of 141, not 141. |
| 3 | **no face bound** — the refusal reaches `invalidGraphFacts` with no occurrence to attribute. That is either a scan that found nothing to bind or a consumer refusing without one; the two are not distinguishable from the outside today. |
| 4 | **non-callable subject applied** — `examples/call_index_module.id` is |

| # | directive |
|---|---|
| 1 | tt: [4]i64 = {1, 2, 3, 4} tt(2) |

| # | directive |
|---|---|
| 1 | which `law.md` §5 rules in one sentence ("`()` is ordinary application. |
| 2 | It never means table indexing") and `gate/application.sh:333` already names as a NEGATIVE probe. |
| 3 | It passes `idol check` and then refuses here as a missing id. |
| 4 | A correct refusal wearing the wrong cause is worse than a wrong answer, because the ratchet that counts it will move when the wall is repaired and nothing about this subject changed. |

| section |
|---|---|
| Three sites, but ONE producer — corrected |

| # | directive |
|---|---|
| 1 | `grep -n '"missing-application-id"' src/dnir_lower.zig` returns three non-test sites (169, 2954, 2955), and this gap first read the `no face bound` family as the third of them. **That was wrong, and the measurement is decisive.** |

| # | directive |
|---|---|
| 1 | All 141 come from **2954** alone. |
| 2 | Two independent facts settle it: |

| # | directive |
|---|---|
| 1 | every one of the 141 prints an application id, which 2955 cannot produce because it binds nothing; and |
| 2 | for all 141, the printed id equals `blocking_applications[0]` from `idol graph <f>` — exactly `OccurrenceBridge.first_unresolved`. 141/141, no exceptions. |

| # | directive |
|---|---|
| 1 | 169 is unreachable for this corpus: the guard at 2946 (`if (!graph.gateTransportModule() and occurrences.unresolved != 0)`) refuses before `lowerModuleFromGraph` is ever called, and no `examples/*.id` is gate-transport. |
| 2 | So this file is NOT a `law.fact.producer.one` instance for this note. |
| 3 | The other two sites exist and may be reachable from other corpora; nothing here measured that. |

| # | directive |
|---|---|
| 1 | **The real cause of the missing face was `applicationFace` not recursing.** A curried `f(a)(b)` is a `.call` whose callee is itself a `.call`, and both `exprFace` and `applicationFace`'s AST tail answered only `.name` and `.field`, falling to `node.name` — null, because an indirect-callee node carries no name. |
| 2 | 17 of the 20 were literally `to(str)(x)`, the operation-first face `CLAUDE.md` records as migratable debt; the rest were `greet("Duo")(…)` and `concat("Hello")(…)`. |
| 3 | One construct, one repair. |
| 4 | Both faces walk to the chain head now, and the 20 answer `to`, `greet`, `concat`. |

| section |
|---|---|
| Its effect on GAP-226's own ranking |

| # | directive |
|---|---|
| 1 | The wall's site census ranks `host-relation-no-entity` + `host-relation-name-declared-in-home` at 74% of unpublished applications. |
| 2 | That is a count of SITES. |
| 3 | This is a count of SUBJECTS THAT CANNOT BE REALIZED, and the ranking inverts: intrinsics 63, wall 43. |
| 4 | Both are true and they answer different questions — one program is refused by its first unresolved occurrence, so a subject is attributed to whichever cause it meets first, not to the cause with the most sites in it. |

| # | directive |
|---|---|
| 1 | Neither number is wrong and neither is the other's substitute. |
| 2 | Recording that here because the two were about to be read as one. |

| section |
|---|---|
| Acceptance #2 is answered: `check` holds the descriptor and does not ask |

| # | directive |
|---|---|
| 1 | `idol graph examples/call_index_module.id` publishes both halves in one graph — node 2 is `tt` and the aggregate row carries `"descriptor":{"kind":"array","extent":4,"element":{"kind":"i64"}}` — while node 8 is the `.call`. |
| 2 | Nothing joins them, and the join point is three lines wide: |

| # | directive |
|---|---|
| 1 | src/sema.zig:4967 const ft = try self.check_expr(c.func); // descriptor in hand src/sema.zig:4968 if (c.form == .braced and ft == .func) // the MIRROR guard, // c0 §43 law.brace src/sema.zig:5094 const result: RT = switch (ft) { .func => \|f\| f.ret.*, else => .any, // .array falls here } |

| # | directive |
|---|---|
| 1 | So the dual of an existing guard is simply unwritten: `law.brace` refuses a CALLABLE subject wearing BRACES, and `law.md` §5's rule — a DESCRIPTOR subject wearing PARENS — has no guard. |
| 2 | That sema uses this same descriptor elsewhere is proven by the `.index` arm at `src/sema.zig:4656` (`if (ot == .array) return ot.array.elem.*;`): `tt[2]` reads it, `tt(2)` drops it. |

| # | directive |
|---|---|
| 1 | Bounded honestly: the descriptor is available for a BARE-NAME callee (`tt`, `arr`). |
| 2 | For `self._bufs(...)` and `json.decode(...)` `check_expr` returns `.any` (`src/sema.zig:4626`), so two of the fifteen are the family by shape without sema holding their descriptor. |

| # | directive |
|---|---|
| 1 | **A GATE PINS THE CURRENT BEHAVIOUR AND MUST MOVE WITH THE REPAIR.** `gate/architecture-companion.sh:98-113` asserts `CHECK-NOT-DIRECT-ADMISSION: call_index_assign.id must pass idol check (sema path)` — it requires `check` to exit 0 on a subject law says is ill-formed. |
| 2 | Whoever writes the guard changes that gate in the same commit, or lands a green tree that contradicts itself. |
| 3 | Recording it here because it is invisible from the sema side. |

| # | directive |
|---|---|
| 1 | Four producers already rule `()`-is-not-indexing — `docs/spec/law.md:152`, `src/place.zig` (pinned by `test "place: an ordinary call is not an indexed read"`), `gate/delimiter-projection-law.sh`, and `gate/callfold.sh` — and sema is the one that does not. |

| section |
|---|---|
| Notes are identities now — the producer side of this gap is repaired |

| # | directive |
|---|---|
| 1 | The same defect this gap files against `missing-application-id` — one spelling carrying several causes — had an inverse living in the same file: several spellings carrying one cause each, because `bailWith` was handed a NAME where a cause belongs. |
| 2 | A bare `fname`, a bare field key, `@tagName(stmt.*)`, and an `"{path}: {reason}"` ineligibility note meant the census grew one row per refused subject and the cause was unfindable by prefix. |

| # | directive |
|---|---|
| 1 | Repaired at the producer, in two moves: |

| # | directive |
|---|---|
| 1 | `bailNamed` in `src/dnir_lower.zig` formats `cause:name` — kebab identity first, provenance after the colon — and every `bailWith` site that carried a name went through it: `statement-not-lowered:directive`, `unresolved-name:mcp`, `local-slot-missing:…`, `record-field-duplicate:…`, and eight more. `mod-global-written:` was the precedent. |
| 2 | `functionEligibleReason`'s nineteen prose reasons became kebab identities (`result-type-unsupported`, `parameter-type-unsupported`, `record-parameter-overflows-16-gp-slots`, …) and the ineligible-declaration note flipped from `"{path}: {reason}"` to `"{reason}:{path}"` — so `two: unsupported return type` is now `result-type-unsupported:two`. The count-mismatch fallback `"?"` became `function-count-mismatch`. |

| # | directive |
|---|---|
| 1 | `gate/realize/census.sh` §3 groups colon notes by their identity prefix (`unresolved-name:*`) and its name-shape test validates the prefix, so the histogram keeps one row per CAUSE while each refusal still names its subject. |
| 2 | Measured after the change: **0 name-carrying notes** in the corpus, down from the mixed population the census header used to enumerate. |
| 3 | The remaining `<no note>` rows (5) are refusals that recorded nothing and stay a distinct class. |

| section |
|---|---|
| Acceptance |

| # | directive |
|---|---|
| 1 | PARTIALLY DONE for the non-callable family, and at a better layer than this item asked for: the §5 sema guard (`src/sema.zig`, the mirror of the `law.brace` arm three lines above it) refuses a descriptor-bound aggregate wearing parens AT CHECK, naming the subject and the law — so the eleven bare-name `tt` subjects never reach the realizer to need a separate note. Measured: exactly 11 corpus flips, zero collateral (64 total check refusals, 53 + 11), and `missing-application-id` fell from 141 toward 120 as this and the GAP-234 1b wave left the population. |

| # | directive |
|---|---|
| 1 | The guard is deliberately narrow — `ft == .array`, bare non-world name — because that is where the descriptor is IN HAND. `call_index_assign.id` (unannotated `arr = {0,0,0,0}`), `mem_demo.id` (`self._bufs`, a field), and `json_direct_iteration_smoke.id` (a call result) still pass check: sema's `sym.typ` does not carry their descriptors, and guessing without the fact would be a fourth wrong producer. |
| 2 | So acceptance #2's `call_index_assign` half stays open on descriptor propagation, not on the guard. |

| # | directive |
|---|---|
| 1 | `gate/realize/census.sh`'s §1 control moved with the law: its known-refusal subject WAS `tt(2)`, which now classifies CHECK; the realizer-refusal control is `assert(1)` (GAP-226's wall) and the §5 shape is pinned as CHECK-class. `gate/architecture-companion.sh`'s CHECK-NOT-DIRECT-ADMISSION control still holds, because its subject is the unannotated file the guard cannot see. |

| # | directive |
|---|---|
| 1 | `examples/call_index_module.id` and `examples/call_index_assign.id` refuse with the non-callable note, and `idol check` is the arm that decides whether they should have been accepted at all. |
| 2 | The `no face bound` family either binds one or says why it cannot, so that 20 subjects stop reporting a cause nobody can act on. |
| 3 | The four-way split is recomputed by a runner rather than pinned here. The loop that produced it is in `gaps/GAP-226.md`; the face is now available in the refusal output, so classifying is `grep 'in relation:'`. |
