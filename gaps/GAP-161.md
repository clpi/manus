| field | value |
|---|---|
| title | GAP-161 — Corpus role machine-visible to agent retrieval |
| status | OPEN |
| priority | P0 |
| kind | architecture |

| section |
|---|---|
| Current obligation |

| # | directive |
|---|---|
| 1 | Agent retrieval must filter on `@corpus current` vs `@corpus foreign` only. |
| 2 | Git owns all historical archive; no durable `historical`/`migration`/`compat` states. |

| section |
|---|---|
| Evidence |

| # | directive |
|---|---|
| 1 | `docs/spec/corpus-role.md` — two durable states |
| 2 | Semantic examples under `examples/conversion/`, `examples/demand/`, etc. |
| 3 | `tools/node/dev/corpuscensus` — mechanical tag gate |

| section |
|---|---|
| Acceptance |

| # | directive |
|---|---|
| 1 | MCP search excludes `@corpus foreign` by default. |
| 2 | No retired corpus labels in tracked `.id`. |
| 3 | Deleted historical example files (`metatable_class_semantics.id`, `concept_introspect.id`). |

| section |
|---|---|
| Deletion condition |

| # | directive |
|---|---|
| 1 | Graph-owned corpus classification replaces comment tags (GAP-124). |
