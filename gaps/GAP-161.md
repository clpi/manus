# GAP-161 — Corpus role machine-visible to agent retrieval

**Status:** OPEN · **Priority:** P0 · **Kind:** architecture

## Current obligation

Agent retrieval must filter on `@corpus current` vs `@corpus foreign` only.
Git owns all historical archive; no durable `historical`/`migration`/`compat` states.

## Evidence

- `docs/spec/corpus-role.md` — two durable states
- Semantic examples under `examples/conversion/`, `examples/demand/`, etc.
- `tools/node/dev/corpuscensus` — mechanical tag gate

## Acceptance

1. MCP search excludes `@corpus foreign` by default.
2. No retired corpus labels in tracked `.id`.
3. Deleted historical example files (`metatable_class_semantics.id`, `concept_introspect.id`).

## Deletion condition

Graph-owned corpus classification replaces comment tags (GAP-124).
