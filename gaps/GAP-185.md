# GAP-185 — world authority to hardware capability realization

**Status:** OPEN · **Filed:** !2026-08-18T15:55:00Z
**Priority:** P1 · **Kind:** research_gap
**Filed by:** devin-semantic-20260818
**Files:** docs/spec/constitution.md, docs/spec/law.md, src/world.zig, src/lower.zig

World/authority facts should select among alternate physical realizations of the
same semantic authority: software check, pointer bounds, MPK, CHERI capability,
process isolation, Wasm sandbox. CHERI demonstrates that hardware capabilities
can enforce source-level pointer authority; Idol should derive capability
construction directly from world facts.

## Current measured boundary

Worlds are semantic contexts. The path from a world/authority fact to an
enforcement mechanism now EXISTS as a bounded decision pair (`src/world.zig`
facts, `src/lower.zig` selection), and the BACKEND consumer during realization
now EXISTS (the graph-backed C99 realization arm in `src/main.zig` calls
`lower.selectModule` per census place); what remains is the per-place plans
constraining lowering itself, and measured per-mechanism costs.

**Crossed:**

- `world` and `authority` are C0 fact families.
- `law.world` and `law.capability` exist.
- Required order 1–4 (`09c7d1c38`): the `Mechanism` index, `TargetWorld`
  availability derived from triple facts, the per-mechanism cost relation, and
  CHERI construction derived from `unique`/`nonescape`/`readonly`/extent facts
  — never hand-written.
- Required order 5 through the graph (`5fb4c433`): `lower.selectPlace` is the
  graph-facing consumer — authority facts reach `select` through the census
  the graph carries (`SemanticGraph.places`), accesses are the census's own
  multiplicities, an unknown multiplicity is an unknown workload (never a
  small one), and a statically fixed census place realizes at zero cost.
  Measure: `zig build unit-test` — every `lower:`/`world:` test passes.
- The `foreign_boundary` admissibility ruling (`0043c7f5`): foreign code
  in-image can forge pointers, so admissibility is a relation over the
  mechanism AND the observation world — `world.enforces` reads
  `software_check`, `bounds` and `mpk` as enforcing nothing under the fact
  (an emitted test+trap, an unprivileged bounds register, and an
  unprivileged permission-register write are conventions the foreign code
  never agreed to), while `cheri`, `process`, `wasm_sandbox` and
  `network_isolation` survive; the same premise denies `lower.select`'s
  static rung, whose proof quantifies over the program's own accesses only.
  Measure: filtered unit tests (`--test-filter "test.lower:"` /
  `"test.world:"` over `src/tests.zig`) — every decision-level test passes.
  The four census-level `lower:` tests remain GAP-145-blocked at HEAD (the
  table-primary parser regression, Codex's lane), so this ruling's
  census-path witness lands with the backend consumer, not here.
- The BACKEND consumer through the graph: the graph-backed C99 realization
  arm (`src/main.zig`) lifts the place census (`graph.liftPlaces`) and calls
  `lower.selectModule` between the graph lift and graph-to-DNIR lowering —
  one `lower.selectPlace` per census row over the census the graph carries,
  never a second census; the walk is counted by rung so its completeness is
  a measured fact, and a census place with no admissible enforcement fails
  the realization closed, named. Portable C99 source is costed as the world
  that admits the software check and nothing else — exactly the mechanism
  the arm realizes. The deletion witness's next host boundary moves to the
  per-place plans constraining lowering and to measured costs
  (`src/lower.zig` header). Measure: `zig test src/lower.zig --test-filter
  'lower.test.lower:'` (the production frontend C objects linked as
  `build.zig` links them) — every decision-level and realization-walk test
  passes; `zig test src/world.zig --test-filter 'world.test.world:'` — all
  pass. The walk tests build census rows with known facts directly
  (selection consumes `facts` and access counts only), so they run under
  the GAP-145 table-primary parser regression that still blocks the four
  census-PRODUCTION tests at HEAD (Codex's lane). The wired arm runs at
  HEAD: `idol compile --backend=c --emit=c --target=c-source` over a
  plain-assignment module walks the census the graph carries and selects
  per place; `name: T = v` statements join the parser regression, so the
  arm's typed-source run lands with GAP-145's recovery.

**Still missing:**

- The per-place PLANS constraining lowering itself — the static rung eliding
  the enforcement `dnir_lower` emits per place; the landed consumer gates
  realization admissibility, it does not yet move an emitted check.
- Measured (not estimated) per-mechanism costs once a target exists to measure
  them — `lower.costOf` states orders (`law` §14).

## Required order

1. Define authority realization candidates in C0.
2. Add target-world facts for available hardware mechanisms.
3. Build a cost model for authority enforcement.
4. Derive CHERI/MPK/bounds construction from semantic authority facts.
5. Prove zero overhead when the compiler can show authority is statically fixed.

## Closure acceptance

Close when a world fact can select a hardware/software authority realization and
when CHERI capabilities are derived from semantic authority rather than
hand-written.

## C0 alignment

Instance of the schema in `gaps/RESEARCH-SPINE.md`; `tools/node/dev/gapc0` fails
closed without it.

- **constitutional owners:** world · capability · realization
- **kind delta:** 0
- **registry delta:** 0
- **source syntax delta:** 0
- **semantic invariant:** authority stays semantic; enforcement is a realization candidate — no `SafePointer`, `CapabilityObject`, or `SandboxType` kinds are ever minted.
- **physical mechanism:** realization selection with a cost model per enforcement: zero software check, bounds check, capability pointer, MPK, CHERI, process boundary, Wasm sandbox, network isolation.
- **prerequisites:** GAP-184.
- **foreign implication:** a sandbox is an authority realization, not a world of its own.
- **world implication:** capability precision makes code FASTER — narrower authority yields stronger effect facts, admitting more motion, fusion, and parallelization; injection changes fact availability and never manufactures authority.
- **projection implication:** none.
- **demand implication:** the observation/attack model (including timing and access-pattern observation) is a demand fact constraining admissible enforcement.
- **realization implication:** CHERI/MPK/bounds construction derived from `unique`, `nonescape`, `readonly`, and world facts; zero overhead when authority is statically fixed.
- **evidence:** one authority fact selecting different enforcement on two targets with cost measured on each.
- **deletion witness:** hand-written capability plumbing and sandbox glue.
