# GAP-185 — world authority to hardware capability realization

**Status:** OPEN · **Filed:** !2026-08-18T15:55:00Z
**Priority:** P1 · **Kind:** research_gap
**Filed by:** devin-semantic-20260818
**Files:** docs/spec/constitution.md, docs/spec/law.md, src/world.zig, src/lower.zig

World/authority facts should select among alternate physical realizations of the
same semantic authority: software check, pointer bounds, MPK, CHERI capability,
process isolation, Wasm sandbox. CHERI demonstrates that hardware capabilities
can enforce source-level pointer authority; Idol should derive capability
construction directly from world facts.

## Current measured boundary

Worlds are semantic contexts. The path from a world/authority fact to an
enforcement mechanism now EXISTS as a bounded decision pair (`src/world.zig`
facts, `src/lower.zig` selection), and the BACKEND consumer during realization
now EXISTS (the graph-backed C99 realization arm in `src/main.zig` calls
`lower.selectModule` per census place); the per-place plans constraining
lowering now EXIST (`lower.collectStaticPlaces` populates
`graph.static_places` so `dnir_lower` elides enforcement for statically fixed
places); what remains is measured per-mechanism costs.

**Crossed:**

- `world` and `authority` are C0 fact families.
- `law.world` and `law.capability` exist.
- Required order 1–4 (`09c7d1c38`): the `Mechanism` index, `TargetWorld`
  availability derived from triple facts, the per-mechanism cost relation, and
  CHERI construction derived from `unique`/`nonescape`/`readonly`/extent facts
  — never hand-written.
- Required order 5 through the graph (`5fb4c433`): `lower.selectPlace` is the
  graph-facing consumer — authority facts reach `select` through the census
  the graph carries (`SemanticGraph.places`), accesses are the census's own
  multiplicities, an unknown multiplicity is an unknown workload (never a
  small one), and a statically fixed census place realizes at zero cost.
  Measure: `zig build unit-test` — every `lower:`/`world:` test passes.
- The `foreign_boundary` admissibility ruling (`0043c7f5`): foreign code
  in-image can forge pointers, so admissibility is a relation over the
  mechanism AND the observation world — `world.enforces` reads
  `software_check`, `bounds` and `mpk` as enforcing nothing under the fact
  (an emitted test+trap, an unprivileged bounds register, and an
  unprivileged permission-register write are conventions the foreign code
  never agreed to), while `cheri`, `process`, `wasm_sandbox` and
  `network_isolation` survive; the same premise denies `lower.select`'s
  static rung, whose proof quantifies over the program's own accesses only.
  Measure: filtered unit tests (`--test-filter "test.lower:"` /
  `"test.world:"` over `src/tests.zig`) — every decision-level test passes.
  The four census-level `lower:` tests remain GAP-145-blocked at HEAD (the
  table-primary parser regression, Codex's lane), so this ruling's
  census-path witness lands with the backend consumer, not here.
- The BACKEND consumer through the graph: the graph-backed C99 realization
  arm (`src/main.zig`) lifts the place census (`graph.liftPlaces`) and calls
  `lower.selectModule` between the graph lift and graph-to-DNIR lowering —
  one `lower.selectPlace` per census row over the census the graph carries,
  never a second census; the walk is counted by rung so its completeness is
  a measured fact, and a census place with no admissible enforcement fails
  the realization closed, named. Portable C99 source is costed as the world
  that admits the software check and nothing else — exactly the mechanism
  the arm realizes. Measure: `zig test src/lower.zig --test-filter
  'lower.test.lower:'` (the production frontend C objects linked as
  `build.zig` links them) — every decision-level and realization-walk test
  passes; `zig test src/world.zig --test-filter 'world.test.world:'` — all
  pass. The walk tests build census rows with known facts directly
  (selection consumes `facts` and access counts only), so they run under
  the GAP-145 table-primary parser regression that still blocks the four
  census-PRODUCTION tests at HEAD (Codex's lane). The wired arm runs at
  HEAD: `idol compile --backend=c --emit=c --target=c-source` over a
  plain-assignment module walks the census the graph carries and selects
  per place; `name: T = v` statements join the parser regression, so the
  arm's typed-source run lands with GAP-145's recovery.
- The per-place PLANS constraining lowering: `lower.collectStaticPlaces`
  walks the census and returns a name-keyed set of places whose authority
  is statically fixed (the zero-cost rung). `main.zig` populates
  `graph.static_places` after `selectModule` succeeds; `dnir_lower` reads
  it in `guardedTableIndex` to elide the bounds check for those places —
  the compiler's own proof (unique, nonescape, readonly, exact determinacy)
  covers spatial safety, so no runtime enforcement is needed. Three new
  `lower:` tests verify the set construction (marks static places, returns
  null on refusal, empty when none are static). Measure: `zig build
  unit-test` — 1646 pass (3 new), 328 fail (pre-existing GAP-145 debt),
  62 skip.
- The MEASURED per-mechanism cost face (this revision): `lower.MeasuredCost`
  is the fact shape — one mechanism, one target triple, one unit, and the
  exact measured subject revision (`law.evidence.subject.one`: a measurement
  naming no subject constructs no fact, enforced in `measurementOf`).
  `lower.selectMeasured` lets measured costs decide ONLY a uniform
  comparison: every admissible candidate measured on the named target in one
  shared unit; a partial measurement, a foreign-triple measurement, mixed
  units, or a missing subject revision all leave `costOf`'s stated orders to
  decide — a measured nanosecond never settles a comparison against a stated
  cycle estimate. The selected plan records the unit its cost is stated in,
  and the evidence record carries it. The producer is
  `gate/lower/cost.sh`: it measures the software check's per-access
  enforcement cost on the host (the one mechanism the compiler both admits
  everywhere and realizes today, as the C99 arm's emitted test+trap), with a
  selftest control that requires the check to be LIVE — a compiled probe
  whose check was folded away is CANNOT MEASURE, never a fabricated row.
  Measure: five new `lower:` tests — `zig test src/lower.zig --test-filter
  'lower.test.lower:'` (production frontend C objects linked as `build.zig`
  links them) passes 27/27; `sh gate/lower/cost.sh` emits the
  `idol.world.cost.v1` row for the host.
- The MEASURED-WALK face (this revision): measured cost facts REACH the
  realization walk — `lower.selectPlaceMeasured`, `selectModuleMeasured` and
  `collectStaticPlacesMeasured` thread a `[]const MeasuredCost` slice through
  `selectMeasured`, so a uniform measurement of every admissible candidate on
  the target decides a census place's cost instead of `costOf`'s stated
  orders. The existing `selectPlace`, `selectModule` and `collectStaticPlaces`
  remain the empty-measurement face (they delegate with an empty slice), so
  the graph-backed C99 arm's current wiring is unchanged. Measured facts never
  manufacture a static witness: the static rung is the authority proof alone,
  so `collectStaticPlacesMeasured` returns the same name-keyed set. Measure:
  three new `lower:` tests — `zig test src/lower.zig --test-filter
  'lower.test.lower:'` (production frontend C objects linked as `build.zig`
  links them) passes 30/30; `zig test src/world.zig --test-filter
  'world.test.world:'` passes 6/6.
- The BOUNDARY-COST face (this revision): `gate/lower/cost.sh` now measures
  the host-executable boundary mechanisms, `process` and
  `network_isolation`, in the same `idol.world.cost.v1` schema as
  `software_check`. Each boundary row reports a one-way `cost.crossing`
  derived from a live two-way ping-pong; the process row crosses a forked
  child through pipes, and the network row crosses loopback TCP. The runner
  prints no rows until the software trap control, the process round trip, and
  the loopback round trip all succeed, so unavailable boundary mechanisms are
  CANNOT MEASURE instead of fabricated facts. Measure: `sh -n
  gate/lower/cost.sh`; `K=1024 B=16 N=2 sh gate/lower/cost.sh`; `sh
  gate/lower/cost.sh`.
- The PROVENANCE-PARSE face (this revision): the measured world crosses back
  into a typed fact. `lower.parseMeasured` reconstructs one `MeasuredCost`
  from one `idol.world.cost.v1` row — exactly the row `gate/lower/cost.sh`
  emits — the single producer of a measured fact from that world
  (`law.fact.producer.one`); no host reads the numbers a second time. It
  FAILS CLOSED: the schema must be `idol.world.cost.v1`, the
  mechanism/arch/os/abi/unit must each parse to a known identity read from
  their own spelling (never an ordinal, `law.magic.code.zero`), the cost must
  carry `access` and `crossing`, and the subject revision must be NON-EMPTY
  (`law.evidence.subject.one`) — a foreign schema, unknown name, missing
  field, or empty revision leaves the stated orders to decide exactly as an
  absent measurement does. The measured subject revision is copied into a
  caller-owned buffer so the fact does not borrow the transient row. Seven new
  `lower:` tests cover the software and boundary rows, the empty-revision
  refusal, a foreign schema, an unknown mechanism name, malformed input, and a
  parsed row feeding `selectMeasured` to the same winner the equivalent struct
  facts select. Measure: `zig build unit-test` — every `lower:`/`world:` test
  passes; the pre-existing GAP-145 codegen/parser failures are unchanged.
- The ARM-CONSUMER face (this revision): `idol compile --backend=c --emit=c
  --authority-cost <file>` reads the newline-delimited rows emitted by
  `gate/lower/cost.sh`, reconstructs them only through `lower.parseMeasured`,
  and hands the resulting owned fact slice to both `selectModuleMeasured` and
  `collectStaticPlacesMeasured`. `parseMeasurements` preserves each measured
  subject revision independently; an unreadable file, empty stream, or any
  invalid nonblank row refuses the realization instead of allowing partial
  evidence to decide it. Supplying the evidence to another backend is also a
  refusal, and the physical build cache is disabled when this realization
  input is present. Measure: `zig build unit-test` covers complete and partial
  multi-row streams; a graph-backed C99 compile with a producer-format row
  reaches the arm, while an invalid row is the positive refusal control.
- The VALUE-DOMAIN face (this revision): the seam's value domain is the
  producer's, not the tests'. `gate/lower/cost.sh` emits computed costs as
  `%.4f` floats — including a negative software-check delta when timer noise
  inverts it, exactly as its header documents — while every parse test used
  integer costs, so the float arms were unpinned and a genuine noise-floor row
  refused the whole stream (`u64Field` returned null, `parseMeasurements`
  refused, the C99 arm exited 1 on valid producer output — reproduced at HEAD
  with a producer-shape row carrying `"access":-0.0312`). `u64Field` now
  reconstructs a negative float as zero: a cost is a physical quantity, the
  noise floor is no measurable cost, and a negative quantity must never settle
  a comparison; an unrepresentable magnitude stays no fact (fail closed, never
  a trapping conversion). The row keeps its exact checked/plain fields for
  audit — only the quantity floors. Measure: four new `lower:` tests — `zig
  test src/lower.zig --test-filter 'lower.test.lower:'` (production frontend
  C objects linked as `build.zig` links them) passes 45/45; `zig test
  src/world.zig --test-filter 'world.test.world:'` passes 6/6; a graph-backed
  C99 compile with the noise-floor row reaches the arm where HEAD refused it.

**Still missing:**

- Measured costs for hardware mechanisms not measurable on this host — MPK on
  x86_64. Unavailable hardware remains CANNOT MEASURE rather than a fabricated
  fact; the realized C99 arm now consumes every measured fact supplied to it.

## Required order

1. Define authority realization candidates in C0.
2. Add target-world facts for available hardware mechanisms.
3. Build a cost model for authority enforcement.
4. Derive CHERI/MPK/bounds construction from semantic authority facts.
5. Prove zero overhead when the compiler can show authority is statically fixed.

## Closure acceptance

Close when a world fact can select a hardware/software authority realization and
when CHERI capabilities are derived from semantic authority rather than
hand-written.

## C0 alignment

Instance of the schema in `gaps/RESEARCH-SPINE.md`; `tools/node/dev/gapc0` fails
closed without it.

- **constitutional owners:** world · capability · realization
- **kind delta:** 0
- **registry delta:** 0
- **source syntax delta:** 0
- **semantic invariant:** authority stays semantic; enforcement is a realization candidate — no `SafePointer`, `CapabilityObject`, or `SandboxType` kinds are ever minted.
- **physical mechanism:** realization selection with a cost model per enforcement: zero software check, bounds check, capability pointer, MPK, CHERI, process boundary, Wasm sandbox, network isolation.
- **prerequisites:** GAP-184.
- **foreign implication:** a sandbox is an authority realization, not a world of its own.
- **world implication:** capability precision makes code FASTER — narrower authority yields stronger effect facts, admitting more motion, fusion, and parallelization; injection changes fact availability and never manufactures authority.
- **projection implication:** none.
- **demand implication:** the observation/attack model (including timing and access-pattern observation) is a demand fact constraining admissible enforcement.
- **realization implication:** CHERI/MPK/bounds construction derived from `unique`, `nonescape`, `readonly`, and world facts; zero overhead when authority is statically fixed.
- **evidence:** one authority fact selecting different enforcement on two targets with cost measured on each.
- **deletion witness:** hand-written capability plumbing and sandbox glue.
