# GAP-207 — the uncommitted `exprIsStr` byte-face correction prints a pointer

**Status:** OPEN · **Filed:** !2026-08-21T22:40:00Z
**Priority:** P0 · **Kind:** wrong_answer
**Filed by:** claude-gap145
**Files:** src/dnir_lower.zig

<!-- idol-gap-frontier:v1:begin -->
```json
{
  "schema": "idol.gap.frontier.v1",
  "gap": "GAP-207",
  "status": "OPEN",
  "root_program": "lib/compiler/token.id quote face -> src/semantic_graph.zig source_quote -> src/dnir_lower.zig concat decision",
  "current_blockers": [
    "The tracked out/bin/idol predates the regression and reports the repaired program as correct, so the stale oracle blocker this gap named is untouched.",
    "Reclassification is owed: the wrong answer is repaired and controlled, and this gap is still carried as an active P0 in gaps/ROOT-PROGRAM.md."
  ],
  "superseded_observations": [
    "#original-filing-2026-08-21",
    "#the-measurement",
    "#mechanism"
  ]
}
```
<!-- idol-gap-frontier:v1:end -->

## Current frontier notes

The machine-read frontier is the `idol.gap.frontier.v1` block above; it is the
one `gate/frontier.sh` validates and the one to trust. This section is the
EVIDENCE behind those blockers -- what was measured, how it was bisected, and
the command that reproduces it. It is not a second frontier.

**Frontier:** THE DEFECT IS AT `HEAD` AND IT LANDED. This gap was filed about
uncommitted work and opens by saying "`HEAD` is correct"; that sentence is now
false. A single-quoted byte-face value concatenated with text prints a POINTER,
exit 0, no diagnostic, and the address differs between runs:

```text
s = 'abc'
t = "abc"
print(s .. "Z")     ->  4309877940Z     (address; varies per run)
print(t .. "Z")     ->  abcZ
```

**Bisected to one commit.** Two isolated worktrees, private
`ZIG_GLOBAL_CACHE_DIR` per arm, identical two-line source, no cached artifact:

| build | `print(s .. "Z")` where `s = 'abc'` |
| --- | --- |
| `ae5fea3b^` | `abcZ` |
| `ae5fea3b` "Make checked quote classification graph-owned" | `<pointer>Z` |

The commit that made `.quoted` classification graph-owned -- the repair this
gap asked for -- is the commit that introduced the pointer. `graphTextConst`
answers "not text" for the byte face, `concatOperandOk` then declines it, and
the operand falls through to a raw pointer instead of refusing BY NAME. The
required control this gap already states is the right one and it was never
built: the program must print `abcZ` or refuse naming the byte face; printing
an address is neither.

**Masked by a stale oracle.** `out/bin/idol` is a TRACKED prebuilt binary last
committed at `301ac0b0` (2026-08-20), which predates `ae5fea3b`. It compiles the
same source and prints `abcZ` -- correct. Anyone who verifies this gap with
`./out/bin/idol` instead of a fresh `zig build` gets a false green. That is how
this gap came to be read as superseded. The same stale binary reports
`scripts/proof/relation.id` as `relation proof: pass` while a fresh build of
`HEAD` refuses it with `UnsupportedProgram` at
`lowerExprCons() dnir_lower.zig:9769`.

**Crossed:** the bisection. Do not re-bisect this. Two isolated worktrees, a
private `ZIG_GLOBAL_CACHE_DIR` per arm and no cached artifact already place the
regression at `ae5fea3b` exactly, and the same two-line source is correct at
`ae5fea3b^`. The mechanism is located as well: `graphTextConst` answers "not
text" for the byte face and `concatOperandOk` then declines it. What is NOT
crossed is any repair, any fixture, and any control.
**Next:** make the byte face refuse by name at the concat decision rather than
fall through, and land the required control below as an executable fixture.
There is no fixture for `= 'abc'` anywhere in `examples/` or `tests/`, which is
why nothing caught it.
**Verify:** build fresh (`zig build`, do NOT use the tracked `out/bin/idol`),
then run the four lines above. Correct is `abcZ` twice, or a named refusal.
**Blocked on:** nothing. This is a two-line reproduction with a named commit.
**Measured at:** `dec7d509`, with a fresh `zig-out/bin/idol`.

## Superseded observations

Everything below this boundary is dated filing history, not the state of the
tree. It is kept because it states the required control correctly; it must not
be read as guidance about `HEAD`. Its opening sentence — "`HEAD` is correct" —
is FALSE at `HEAD` and is the reason this gap was almost closed by mistake.

## Original filing (2026-08-21)

Kept verbatim. Its framing is superseded: the work it describes as uncommitted
has landed, and its opening claim that `HEAD` is correct no longer holds.

**This gap is about UNCOMMITTED work in the shared working tree, not about
`HEAD`.** `HEAD` is correct. Nothing has been reverted; this records the
measurement so the holder of `src/dnir_lower.zig` sees it before committing.

## The measurement

```id
main: i64 = ()
    s = 'abc'
    print(s .. "Z")
    0
```

Subject `c27810a4`, clean `git archive HEAD` mirror, `zig build install
-Doptimize=ReleaseFast`, three runs each:

| compiler | run 1 | run 2 | run 3 | exit |
| --- | --- | --- | --- | --- |
| `HEAD` | `abcZ` | `abcZ` | `abcZ` | 0 |
| `HEAD` + the uncommitted `exprIsStr` line | `4370531440Z` | `4342023280Z` | `4372497520Z` | **0** |

Three ASLR'd pointers rendered through `%lld`. Compile 0, run 0, no diagnostic.

## Isolation

The working tree carries two uncommitted lines. **Only the `exprIsStr` one
causes this**, applied alone:

```zig
-        .quoted => true,
+        .quoted => |lit| !ast.quotedLiteralIsByteSequence(lit.quote),
```

The companion line in `concatOperandOk` — `if (expr.* == .quoted) return true;`
— is a partial mitigation. It covers a **literal** operand and does not cover a
**name bound to** a byte literal, which is the shape above.

## Mechanism

At `HEAD`, `exprIsStr` answers `.quoted => true`, so `s` enters `str_slots` and
`planConcat` emits `%s`. Correcting `exprIsStr` to the quote face takes `s` out
of `str_slots`; `concatOperandOk(s)` then falls through to `exprIsIntegral`,
which admits it, and `planConcat` emits `%lld` on a `const char*`.

The correction is right and the fall-through is what is wrong. `exprIsStr`
returning false must not mean "integral".

## Why nothing caught it

- `zig build unit-test`: unchanged, `1667 pass, 9 fail, 0 crash / 1676`,
  identical nine names.
- Compile-status sweep, 538 `examples/**/*.id`: **zero diff**.
- Output differential over all 169 examples that compile: **zero diff**.

The corpus contains **no program with a name bound to a byte literal in a
concat**. Only an output differential on a hand-written program finds this, and
that program is the missing control.

## The class

This is APPLICATION-CONSUMER-ZERO's failure mode in the shape GAP-204 already
measured: an admission rule (`concatOperandOk`) and a capability rule
(`exprIsStr` -> `str_slots` -> `planConcat`) decided in different places, so
narrowing one silently widens the other. The narrowing is correct; it needs the
third arm — a byte-sequence operand is neither text nor integral, and the
lowering has no member to carry it (`native_ir.Value` has
`void/i64/f64/str/local/temp/record` and no byte-sequence face).

Until that member exists, the lawful shape is a named refusal, not a
fall-through.

## Required control before this line is committed

`s = 'abc'; print(s .. "Z")` must either print `abcZ` or refuse by name. A suite
pass, a compile-status sweep and an example output differential are all
insufficient — measured above, all three are clean while the program prints a
pointer.


## The wrong answer is repaired and controlled (2026-08-23, lane/gap145zero)

**Status stays OPEN because the gated block above still names two live
blockers, neither of which is the pointer.** The pointer is gone; the stale
tracked oracle is not, and reclassifying an active P0 out of
`gaps/ROOT-PROGRAM.md` belongs to the roster owner, not to this lane.

The third arm exists. `LowerCtx.byteseq_slots` records the byte face at the
same store that records the text face; membership is the descriptor the
semantic graph already published for the quote (`publishSourceQuote` ->
`types.quotedLiteralType`), never `expr.quoted.quote` off the AST, and
`types.isQuotedByteSequence` owns the shape so no consumer restates it. The
two predicates that were answering by elimination — `concatOperandOk` and the
`.name` tail of `exprIsIntegral` — now ask the positive question, and the
verdict is the named refusal `const` / `local` / `global` already gave.

Searching for the same shape found a FOURTH spelling this gap did not name.
`planConcat` is written `if (p.* == .quoted)` — a tag test, not a switch arm —
and folded the bytes of every quoted part into the printf format string
without asking which face they had. It runs BEFORE the binop guard, because
`lowerPrintFormat` is tried first for a print argument, so:

    print('abc' .. "Z")            abcZ      folded in planConcat
    x = 'abc' .. "Z" ; print(x)    refused   lowerBinop

Both refuse now.

**The control this gap demanded is `gate/byte/face.sh`**, on
`zig build test` and `zig build byteface`. It carries its own subjects
because, as this gap measured, the corpus has none of this shape: stdout is
compared BYTE FOR BYTE, a refusal must be non-zero AND silent on stdout AND
have said something on stderr (a segfault satisfies the first two), and its
comparator is positive-controlled on the three transcripts in the table above
rather than on a defect the gate invented.

    sh gate/byte/face.sh                       PASS
    BYTEFACE_IDOL=<pristine 9be8767e> sh ...   FAIL, and it names the pointer
    BYTEFACE_IDOL=/nonexistent sh ...          exit 2, nothing measured
    BYTEFACE_IDOL=<silent stub> sh ...         FAIL, every subject

The gate prints its own subject count on every run; nothing here restates it.

Corpus movement, pristine `9be8767e` vs the repair, stated as the command
rather than as a corpus size: compiling every `git ls-files '*.id'` subject
under both compilers gives an identical exit status on every one, and running
the subjects that compile gives an identical stdout digest on every one
(`examples/shc/cwd.id` prints the cwd and agrees once the cwd is fixed).
`zig build unit-test` exits 0. `zig build test` exits 1 on `agent-smoke`, and
does so on a pristine `git archive` mirror of `origin/main` as well.
