# GAP-207 — the uncommitted `exprIsStr` byte-face correction prints a pointer

**Status:** OPEN · **Filed:** !2026-08-21T22:40:00Z
**Priority:** P0 · **Kind:** wrong_answer
**Filed by:** claude-gap145
**Files:** src/dnir_lower.zig

<!-- idol-gap-frontier:v1:begin -->
```json
{
  "schema": "idol.gap.frontier.v1",
  "gap": "GAP-207",
  "status": "OPEN",
  "root_program": "lib/compiler/token.id quote face -> src/semantic_graph.zig source_quote -> src/dnir_lower.zig concat decision",
  "current_blockers": [
    "The print repair now reaches one refusal in every executed realization. Direct and Wasm refuse the byte-sequence print by name (DNB001 / print-byte-sequence), and the c99 slice refuses by the SAME name as of 2026-09-05; the remaining open defect is the missing extent-carrying carrier for dynamic byte/text operations and concat.",
    "The value carrier is unchanged. There is still no extent-carrying byte-sequence member in native_ir.Value; the byte face travels through the existing pointer-shaped slot and remains unsupported for operations that require its extent.",
    "Because that carrier is a C string, every octet sequence still stops at the first NUL through print, .., :sub and ==, identically on every realization that accepts the program; gate/nul.sh section 4 pins those four answers as losses on the direct backend.",
    "The stale-oracle blocker this gap named is CLOSED: out/bin/idol is deleted from the tree, so no prebuilt binary predating the regression can answer for a fresh build.",
    "Reclassification is owed: the wrong answer is repaired and controlled on the direct realization, and this gap is still carried as an active P0 in gaps/ROOT-PROGRAM.md."
  ],
  "superseded_observations": [
    "#original-filing-2026-08-21",
    "#the-measurement",
    "#mechanism"
  ]
}
```
<!-- idol-gap-frontier:v1:end -->

## Current frontier notes

| # | directive |
|---|---|
| 1 | The machine-read frontier is the `idol.gap.frontier.v1` block above; it is the one `gate/frontier.sh` validates and the one to trust. |
| 2 | This section is the EVIDENCE behind those blockers -- what was measured, how it was bisected, and the command that reproduces it. |
| 3 | It is not a second frontier. |

| # | directive |
|---|---|
| 1 | **Frontier:** THE DEFECT IS AT `HEAD` AND IT LANDED. |
| 2 | This gap was filed about uncommitted work and opens by saying "`HEAD` is correct"; that sentence is now false. |
| 3 | A single-quoted byte-face value concatenated with text prints a POINTER, exit 0, no diagnostic, and the address differs between runs: |

```text
s = 'abc'
t = "abc"
print(s .. "Z")     ->  4309877940Z     (address; varies per run)
print(t .. "Z")     ->  abcZ
```

| # | directive |
|---|---|
| 1 | **Bisected to one commit.** Two isolated worktrees, private `ZIG_GLOBAL_CACHE_DIR` per arm, identical two-line source, no cached artifact: |

| build | `print(s .. "Z")` where `s = 'abc'` |
| --- | --- |
| `ae5fea3b^` | `abcZ` |
| `ae5fea3b` "Make checked quote classification graph-owned" | `<pointer>Z` |

| # | directive |
|---|---|
| 1 | The commit that made `.quoted` classification graph-owned -- the repair this gap asked for -- is the commit that introduced the pointer. `graphTextConst` answers "not text" for the byte face, `concatOperandOk` then declines it, and the operand falls through to a raw pointer instead of refusing BY NAME. |
| 2 | The required control this gap already states is the right one and it was never built: the program must print `abcZ` or refuse naming the byte face; printing an address is neither. |

| # | directive |
|---|---|
| 1 | **Masked by a stale oracle — CLOSED by deletion.** `out/bin/idol` was a TRACKED prebuilt binary last committed at `301ac0b0` (2026-08-20), predating `ae5fea3b`. |
| 2 | It compiled the same source and printed `abcZ` -- correct -- so anyone verifying this gap with `./out/bin/idol` instead of a fresh `zig build` got a false green, which is how this gap came to be read as superseded. |
| 3 | The same stale binary reported `scripts/proof/relation.id` as `relation proof: pass` while a fresh build refused it with `UnsupportedProgram` at `lowerExprCons() dnir_lower.zig:9769`. |
| 4 | The binary is now deleted from the tree and `out/` no longer exists, so the mask cannot recur; the four other gap and gate files that warned readers away from it describe a path that is gone. |

| # | directive |
|---|---|
| 1 | **Crossed:** the bisection. |
| 2 | Do not re-bisect this. |
| 3 | Two isolated worktrees, a private `ZIG_GLOBAL_CACHE_DIR` per arm and no cached artifact already place the regression at `ae5fea3b` exactly, and the same two-line source is correct at `ae5fea3b^`. |
| 4 | The mechanism is located as well: `graphTextConst` answers "not text" for the byte face and `concatOperandOk` then declines it. |
| 5 | What is NOT crossed is any repair, any fixture, and any control. **Next:** make the byte face refuse by name at the concat decision rather than fall through, and land the required control below as an executable fixture. |
| 6 | There is no fixture for `= 'abc'` anywhere in `examples/` or `tests/`, which is why nothing caught it. **Verify:** build fresh (`zig build`, do NOT use the tracked `out/bin/idol`), then run the four lines above. |
| 7 | Correct is `abcZ` twice, or a named refusal. **Blocked on:** nothing. |
| 8 | This is a two-line reproduction with a named commit. **Measured at:** `dec7d509`, with a fresh `zig-out/bin/idol`. |

## The repair reached one realization (2026-08-24)

| # | directive |
|---|---|
| 1 | The direct backend refuses; wasm still prints the pointer. |
| 2 | Measured against a fresh `zig build` of `edc726bf`, never `out/bin/idol`: |

```text
s = 'abc'
print(s)
```

| realization | answer | exit |
| --- | --- | --- |
| direct | `error: direct backend: DNB001 application: unknown missing: print-arg:array` | 1 |
| wasm | `8192` | **0** |

| # | directive |
|---|---|
| 1 | `8192` is the data-segment offset -- the same class of value the direct backend used to print as an ASLR'd address, and the number this gap's own frontier recorded for wasm before the repair. |
| 2 | Reproduce: |

```sh
printf "s = 'abc'\nprint(s)\n" > /tmp/s.id
./zig-out/bin/idol run /tmp/s.id
./zig-out/bin/idol compile --backend=wasm -o /tmp/s.wasm /tmp/s.id && wasmtime /tmp/s.wasm
```

| # | directive |
|---|---|
| 1 | WHY THE GATE IS GREEN OVER IT. `gate/byte/face.sh` runs every subject through `idol run`, which is the direct realization and only that one. |
| 2 | A wasm column would have convicted this on the commit that added the gate. |
| 3 | The gate is not wrong about what it measures; it is silent about what it does not, and its PASS line does not say which. |

| # | directive |
|---|---|
| 1 | WHY THE ANSWER-SIDE REPAIR ON `wip/textrep-20260823` IS NOT THE FIX FOR IT. |
| 2 | That lane made both realizations ANSWER `abcZ`, with `luajit` as the oracle. `law.oracle.bounded` bars that: a host differential oracle is valid only for the legacy-equivalent subset, and the text/bytes distinction (`law.text.byte`) is a deliberate Idol divergence from Lua, which has one string type. |
| 3 | Lua cannot rule on what `bytes .. text` means here. |
| 4 | Answering also renders through `%s` on a carrier with no extent, so it would answer correctly for NUL-free bytes and truncate silently otherwise -- the losses `gate/nul.sh` section 4 pins. |
| 5 | The refusal stands until the carrier exists; what is owed is the SAME refusal on wasm, and a wasm column in `gate/byte/face.sh` that would have said so. |

## The wasm column landed (2026-09-03)

| # | directive |
|---|---|
| 1 | The blocker that named `gate/byte/face.sh`'s silence is crossed. |
| 2 | The gate now carries a second column that measures the COMPILE-TIME byte-face verdict on the wasm backend, on the same subjects the direct column runs. |
| 3 | It is independent of the direct backend, so it runs on a host with no direct-native realization — the exact host where the direct column cannot, and where the gate's PASS used to say nothing about the wasm answer. |

| # | directive |
|---|---|
| 1 | The wasm column's comparator is positive-controlled on the recorded defect: a compile exit 0 on a byte-face print (the pointer-print shape) is convicted, a clean named refusal is not, and a byte-face egress that must still compile is not. |
| 2 | The verdict line names which columns ran, so a PASS no longer reads as agreement on a realization it did not measure. |

| # | directive |
|---|---|
| 1 | Reproduce: |

```sh
sh gate/byte/face.sh
```

| # | directive |
|---|---|
| 1 | On a host with no direct-native realization the line reads `PASS N subject(s) on wasm (direct column not measured on this host)`; on a host with both realizations it reads `PASS N subject(s) on direct and wasm`. |
| 2 | The wasm column is the compile-time half of the cross-backend agreement this gap's § "The repair reached one realization" measured by hand; the extent carrier blocker below is unchanged and still open. |

## The C realization reached the same refusal, and the gate measures it (2026-09-05)

| # | directive |
|---|---|
| 1 | The wasm column's PASS still said nothing about the third realization, and the third realization is the only one that EXECUTES on a host with no direct-native machine — including the host that runs this gate. |
| 2 | Measured at `c0adff39` with a fresh build, this host compiled `s = 'abc' ; print(s)` through `--backend=c`, linked it against `idol_c_runtime_shim.c`, ran it, and read the pointer back as a decimal: |

| realization | answer | exit |
| --- | --- | --- |
| c99 slice, before | `93827861911360` | **0** |
| c99 slice, after | named refusal, empty stdout | 1 |

| # | directive |
|---|---|
| 1 | The producer already carried the fact: `lowerPrint` publishes the graph-derived `.byte_sequence` flag on the print instruction (the same flag that reaches wasm's refusal), covering the bound and unnamed spellings alike. |
| 2 | Only the consumer was missing — the c99 emitter dispatched on `.ty` alone, so the pointer-shaped slot fell through to `%lld`. |
| 3 | The consumer now refuses with wasm's exact name, `print-byte-sequence`, and the two consumers share the flag's one producer. |

| # | directive |
|---|---|
| 1 | **The gate's column is the third one, with its own controls.** The C column runs on every subject the other two run: a compile exit 0 on a refuse subject IS the pointer-print shape (positive-controlled on the measured transcript above), a refusal must be named, and an answer subject is LINKED AND EXECUTED, byte for byte, against the shim that carries its ABI. |
| 2 | A host with no C toolchain reports the missing column in the verdict line rather than a silent PASS. |
| 3 | Reproduce: |

```sh
tools/node/dev/idol-lock -- sh gate/byte/face.sh
```

| # | directive |
|---|---|
| 1 | On this host the line reads `PASS 17 subject(s) on wasm and c; c comparator convicted 3/3`. |
| 2 | With the C refusal neutralized, the same command FAILs naming `bound byte literal printed alone (c): expected a named refusal, got compile exit 0 (the pointer-print shape)` — the column can see the defect it exists for, and the extent-carrier blocker below is unchanged and still open. |

## Superseded observations

| # | directive |
|---|---|
| 1 | Everything below this boundary is dated filing history, not the state of the tree. |
| 2 | It is kept because it states the required control correctly; it must not be read as guidance about `HEAD`. |
| 3 | Its opening sentence — "`HEAD` is correct" — is FALSE at `HEAD` and is the reason this gap was almost closed by mistake. |

## Original filing (2026-08-21)

| # | directive |
|---|---|
| 1 | Kept verbatim. |
| 2 | Its framing is superseded: the work it describes as uncommitted has landed, and its opening claim that `HEAD` is correct no longer holds. |

| # | directive |
|---|---|
| 1 | **This gap is about UNCOMMITTED work in the shared working tree, not about `HEAD`.** `HEAD` is correct. |
| 2 | Nothing has been reverted; this records the measurement so the holder of `src/dnir_lower.zig` sees it before committing. |

## The measurement

```id
main: i64 = ()
    s = 'abc'
    print(s .. "Z")
    0
```

| # | directive |
|---|---|
| 1 | Subject `c27810a4`, clean `git archive HEAD` mirror, `zig build install -Doptimize=ReleaseFast`, three runs each: |

| compiler | run 1 | run 2 | run 3 | exit |
| --- | --- | --- | --- | --- |
| `HEAD` | `abcZ` | `abcZ` | `abcZ` | 0 |
| `HEAD` + the uncommitted `exprIsStr` line | `4370531440Z` | `4342023280Z` | `4372497520Z` | **0** |

| # | directive |
|---|---|
| 1 | Three ASLR'd pointers rendered through `%lld`. |
| 2 | Compile 0, run 0, no diagnostic. |

## Isolation

| # | directive |
|---|---|
| 1 | The working tree carries two uncommitted lines. **Only the `exprIsStr` one causes this**, applied alone: |

```zig
-        .quoted => true,
+        .quoted => |lit| !ast.quotedLiteralIsByteSequence(lit.quote),
```

| # | directive |
|---|---|
| 1 | The companion line in `concatOperandOk` — `if (expr.* == .quoted) return true;` — is a partial mitigation. |
| 2 | It covers a **literal** operand and does not cover a **name bound to** a byte literal, which is the shape above. |

## Mechanism

| # | directive |
|---|---|
| 1 | At `HEAD`, `exprIsStr` answers `.quoted => true`, so `s` enters `str_slots` and `planConcat` emits `%s`. |
| 2 | Correcting `exprIsStr` to the quote face takes `s` out of `str_slots`; `concatOperandOk(s)` then falls through to `exprIsIntegral`, which admits it, and `planConcat` emits `%lld` on a `const char*`. |

| # | directive |
|---|---|
| 1 | The correction is right and the fall-through is what is wrong. `exprIsStr` returning false must not mean "integral". |

## Why nothing caught it

- `zig build unit-test`: unchanged, `1667 pass, 9 fail, 0 crash / 1676`,
  identical nine names.
- Compile-status sweep, 538 `examples/**/*.id`: **zero diff**.
- Output differential over all 169 examples that compile: **zero diff**.

| # | directive |
|---|---|
| 1 | The corpus contains **no program with a name bound to a byte literal in a concat**. |
| 2 | Only an output differential on a hand-written program finds this, and that program is the missing control. |

## The class

| # | directive |
|---|---|
| 1 | This is APPLICATION-CONSUMER-ZERO's failure mode in the shape GAP-204 already measured: an admission rule (`concatOperandOk`) and a capability rule (`exprIsStr` -> `str_slots` -> `planConcat`) decided in different places, so narrowing one silently widens the other. |
| 2 | The narrowing is correct; it needs the third arm — a byte-sequence operand is neither text nor integral, and the lowering has no member to carry it (`native_ir.Value` has `void/i64/f64/str/local/temp/record` and no byte-sequence face). |

| # | directive |
|---|---|
| 1 | Until that member exists, the lawful shape is a named refusal, not a fall-through. |

## Required control before this line is committed

| # | directive |
|---|---|
| 1 | `s = 'abc'; print(s .. "Z")` must either print `abcZ` or refuse by name. |
| 2 | A suite pass, a compile-status sweep and an example output differential are all insufficient — measured above, all three are clean while the program prints a pointer. |

## The wrong answer is repaired and controlled (2026-08-23, lane/gap145zero)

| # | directive |
|---|---|
| 1 | **Status stays OPEN because the gated block above still names two live blockers, neither of which is the pointer.** The pointer is gone; the stale tracked oracle is not, and reclassifying an active P0 out of `gaps/ROOT-PROGRAM.md` belongs to the roster owner, not to this lane. |

| # | directive |
|---|---|
| 1 | The third arm exists. `LowerCtx.byteseq_slots` records the byte face at the same store that records the text face; membership is the descriptor the semantic graph already published for the quote (`publishSourceQuote` -> `types.quotedLiteralType`), never `expr.quoted.quote` off the AST, and `types.isQuotedByteSequence` owns the shape so no consumer restates it. |
| 2 | The two predicates that were answering by elimination — `concatOperandOk` and the `.name` tail of `exprIsIntegral` — now ask the positive question, and the verdict is the named refusal `const` / `local` / `global` already gave. |

| # | directive |
|---|---|
| 1 | **The Wasm print consumer now fails closed as well (2026-08-24).** The graph-derived byte-face fact is carried on the DNIR print instruction and the Wasm emitter refuses `print-byte-sequence` before it can reinterpret the pointer-shaped value as an integer. |
| 2 | A fresh compiler therefore gives the same unsupported outcome for direct and Wasm on `s = 'abc'; print(s)`. |
| 3 | This closes the cross-backend pointer-print disagreement, but not the larger extent carrier required by byte concat, slicing, equality, embedded NULs, or dynamic byte egress. |

| # | directive |
|---|---|
| 1 | Searching for the same shape found a FOURTH spelling this gap did not name. `planConcat` is written `if (p.* == .quoted)` — a tag test, not a switch arm — and folded the bytes of every quoted part into the printf format string without asking which face they had. |
| 2 | It runs BEFORE the binop guard, because `lowerPrintFormat` is tried first for a print argument, so: |

| # | directive |
|---|---|
| 1 | print('abc' .. |
| 2 | "Z") abcZ folded in planConcat x = 'abc' .. |
| 3 | "Z" ; print(x) refused lowerBinop |

| # | directive |
|---|---|
| 1 | Both refuse now. |

| # | directive |
|---|---|
| 1 | **The control this gap demanded is `gate/byte/face.sh`**, on `zig build test` and `zig build byteface`. |
| 2 | It carries its own subjects because, as this gap measured, the corpus has none of this shape: stdout is compared BYTE FOR BYTE, a refusal must be non-zero AND silent on stdout AND have said something on stderr (a segfault satisfies the first two), and its comparator is positive-controlled on the three transcripts in the table above rather than on a defect the gate invented. |

| # | directive |
|---|---|
| 1 | sh gate/byte/face.sh PASS BYTEFACE_IDOL=<pristine 9be8767e> sh ... |
| 2 | FAIL, and it names the pointer BYTEFACE_IDOL=/nonexistent sh ... exit 2, nothing measured BYTEFACE_IDOL=<silent stub> sh ... |
| 3 | FAIL, every subject |

| # | directive |
|---|---|
| 1 | The gate prints its own subject count on every run; nothing here restates it. |

| # | directive |
|---|---|
| 1 | Corpus movement, pristine `9be8767e` vs the repair, stated as the command rather than as a corpus size: compiling every `git ls-files '*.id'` subject under both compilers gives an identical exit status on every one, and running the subjects that compile gives an identical stdout digest on every one (`examples/shc/cwd.id` prints the cwd and agrees once the cwd is fixed). `zig build unit-test` exits 0. `zig build test` exits 1 on `agent-smoke`, and does so on a pristine `git archive` mirror of `origin/main` as well. |
