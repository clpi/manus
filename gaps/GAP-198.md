# GAP-198 — Andorra optimization law

**Status:** OPEN · **Filed:** !2026-08-18T16:45:00Z
**Priority:** P1 · **Kind:** research_gap
**Filed by:** devin-semantic-20260818
**Files:** docs/spec/constitution.md, docs/spec/law.md, src/optimizer.id

Defer nondeterministic choices until deterministic semantic refinement is
exhausted. Never branch the realization/search state while a deterministic fact
propagation can still reduce the candidate set. This is stronger than "run cheap
passes first"; it is an invariant over the optimizer search.

## Current measured boundary

The optimizer can explore candidate spaces. It does not explicitly postpone
branching until deterministic propagation saturates.

**Crossed:**

- `demand`, `effect`, `range`, `shape` facts already reduce candidates.
- `FRONTIER.md` records rung priority.

**Still missing:**

- Producer determinacy as an ordinary relation/effect/solution fact — not a producer class or type.
- Scheduler that runs deterministic rules until saturation before search.
- Representation search: reduce to 2 candidates before exploring them.
- Application overload resolution: use known facts before disjunction.
- Proof search: propagate constraints before case split.

## Required order

1. Carry producer determinacy as relation facts the scheduler consumes.
2. Schedule all deterministic rule applications before search/branch.
3. Implement a `branch only after saturation` gate in the optimizer.
4. Demonstrate reduced branching in representation, overload, and proof search.
5. Measure the search-space contraction.

## Closure acceptance

Close when the optimizer can prove it only branches after deterministic
semantic refinement has reached a local fixed point.

## C0 alignment

Instance of the schema in `gaps/RESEARCH-SPINE.md`; `tools/node/dev/gapc0` fails
closed without it.

- **constitutional owners:** relation/effect/solution facts (producer determinacy) · demand
- **kind delta:** 0
- **registry delta:** 0
- **source syntax delta:** 0
- **semantic invariant:** while a sound deterministic refinement is demanded and available, refine; only then branch/search. The rule governs application ambiguity, representation selection, proof search, layout search, algorithm synthesis, and foreign-equivalence matching alike.
- **physical mechanism:** a `branch only after saturation` gate in the optimizer scheduler.
- **prerequisites:** GAP-197.
- **foreign implication:** foreign-equivalence matching defers its case splits until native facts saturate.
- **world implication:** none.
- **projection implication:** none.
- **demand implication:** only demanded refinements gate the branch; undemanded determinism does not block.
- **realization implication:** candidate sets are reduced to their minimum before exploration (representation search reaching 2 candidates before exploring either); measured search-space contraction, not faster branching.
- **evidence:** measured branching reduction on one search family (representation, overload, or proof).
- **deletion witness:** heuristic pass-ordering folklore; speculative branching before facts saturate.
