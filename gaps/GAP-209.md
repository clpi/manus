| field | value |
|---|---|
| title | GAP-209 — a module binding written by anything but a top-level literal is read stale inside every relation |
| status | CLOSED |
| filed | !2026-08-22T00:20:00Z |
| priority | P0 |
| kind | wrong_answer |
| filed by | claude-gap145 |
| files | src/dnir_lower.zig |

| # | directive |
|---|---|
| 1 | A module-scope binding whose value is established by **anything other than a top-level integer-literal assignment** is folded to its *declared initializer* inside every relation body. |
| 2 | Compile 0, run 0, no diagnostic. |

| section |
|---|---|
| Reproduction — verified on `fc93fba6`, Debug and ReleaseFast alike |

```id
s: i64 = 0
k: i64 = 41
peek: i64 = ()
    s + 1
s = k + 1
print(s)        # 42
print(peek())   # 1   -- Lua oracle says 43
```

```lua
local s = 0
local k = 41
local function peek() return s + 1 end
s = k + 1
print(s)        -- 42
print(peek())   -- 43
```

| # | directive |
|---|---|
| 1 | Measured side by side, `.id` and `.lua` twins diffed line for line: |

| | `print(s)` | `print(peek())` |
| --- | --- | --- |
| idol | 42 | **1** |
| lua (oracle) | 42 | **43** |

| # | directive |
|---|---|
| 1 | Second shape, a loop-established value: |

```id
s: i64 = 0
i: i64 = 1
peek: i64 = ()
    s + 1
while i <= 4
    s = s + i
    i = i + 1
print(s)        # 10   correct
print(peek())   # 1    -- oracle says 11
```

| section |
|---|---|
| Mechanism |

| # | directive |
|---|---|
| 1 | `dnir_lower.collectModuleConsts` (`src/dnir_lower.zig:563`) walks **top-level module statements only** and records `name -> int` whenever `intLiteralStep` succeeds. `s: i64 = 0` records 0. |

| # | directive |
|---|---|
| 1 | The later `s = k + 1` is **not an int literal**, so it never overwrites the recorded 0. |
| 2 | The `while`-body write is **nested**, so the walk never sees it. |

| # | directive |
|---|---|
| 1 | `collectModuleGlobals` (`:419`) would grant bss storage instead, but only when `moduleFunctionsAssignName` — that is, when a **function** assigns the name. |
| 2 | Here the assignment is at module scope, so no storage is granted either. |

| # | directive |
|---|---|
| 1 | `peek` therefore folds `s` to the stale `0` and answers `1`. |

| # | directive |
|---|---|
| 1 | `s = 41` would work, but only by accident: it *is* an int literal, so the walk overwrites the record. |

| section |
|---|---|
| The class |

| # | directive |
|---|---|
| 1 | Same shape as GAP-204, GAP-207 and GAP-208: a fact is reconstructed by an AST walk that models a narrower program than the language admits, and the reconstruction is consulted by a consumer that has no way to know the walk did not cover its case. |
| 2 | Here the walk covers exactly one statement form and the language admits four. |

| # | directive |
|---|---|
| 1 | `ModuleConsts` is the collapsed module-scope authority GAP-145 already names. |
| 2 | This is a second wrong answer from it, after the `global j = "abc"` reads recorded during the same session (empty, `(null)Z`, and `0` for a value that is plainly `"abc"`). |

| section |
|---|---|
| Required control |

| # | directive |
|---|---|
| 1 | The oracle differential is the control, and no cheaper one is sufficient: |

| # | directive |
|---|---|
| 1 | `zig build unit-test` is clean on both shapes. |
| 2 | The 538-file `examples/**/*.id` compile-status sweep is clean. |
| 3 | The output differential over the examples that compile is clean. |

| # | directive |
|---|---|
| 1 | The corpus contains no module binding established by a non-literal expression and then read from a relation. |
| 2 | As in GAP-207, only a hand-written program with a language oracle finds this. |

| section |
|---|---|
| Measured: invalidating the const map alone MOVES the wrong answer |

| # | directive |
|---|---|
| 1 | An invalidation was built and measured, not merely proposed. `assignCountInStmts` counts every assignment to a name across top-level statements, every nested block and every relation body, honouring `stmtsAssignName`'s shadowing rule; a recorded constant is dropped unless the module writes the name exactly as many times as the recording statement itself accounts for. |

| # | directive |
|---|---|
| 1 | It fires correctly and **the answer does not change**: |

| | before | with invalidation | oracle |
| --- | --- | --- | --- |
| `s = k + 1` shape | `42 1` | `42 1` | `42 43` |
| `while` shape | `10 1` | `10 1` | `10 11` |

| # | directive |
|---|---|
| 1 | `peek` still emits `mov x9, #0x0`. |
| 2 | With the constant dropped, the read resolves through module **storage** instead — and `collectModuleGlobals` grants a bss word only when `moduleFunctionsAssignName`, which is false for a module-scope write, so the initializer is skipped and the read loads zero. |

| # | directive |
|---|---|
| 1 | **There are two producers of "the value of a module binding", and they answer the same stale value by different routes.** That is the same shape the gap already records for `global j = "abc"`: `print(j)` empty, `print(j .. "Z")` giving `(null)Z`, `print(j:len())` giving `0`. |

| # | directive |
|---|---|
| 1 | So the const-map invalidation is **necessary and not sufficient**, and landing it alone would exchange a stale constant for an uninitialized word — a different wrong answer, not a fix. |
| 2 | It is recorded here rather than shipped. |

| section |
|---|---|
| Both halves together do not fix it either, and the machine code says why |

| # | directive |
|---|---|
| 1 | The storage half was built on top of the invalidation — `collectModuleGlobals` granting storage whenever the module writes the name at all, not only when a relation body does. |
| 2 | Measured, both halves live: |

| | before | both halves | oracle |
| --- | --- | --- | --- |
| `s = k + 1` shape | `42 1` | `42 1` | `42 43` |
| `while` shape | `10 1` | `10 1` | `10 11` |

| # | directive |
|---|---|
| 1 | The emitted code is the diagnosis: |

```
_..._peek:
    mov  x9, #0x0        <- an IMMEDIATE, not a load
    add  x10, x9, #0x1
    ret

_main:
    str  x9, [sp]        <- the module body writes s to a STACK SLOT
    str  x10, [sp]
```

| # | directive |
|---|---|
| 1 | `nm` reports **no globals in the binary at all**. |

| # | directive |
|---|---|
| 1 | So it is not two producers of one value. **The module body and the relation do not share a location for `s` in any form.** The module body keeps it in a stack slot of the module's own frame — which is why `print(s)` correctly answers 42 — while the relation carries a compile-time immediate that no invalidation in `collectModuleConsts` removes. |

| # | directive |
|---|---|
| 1 | Two further readers were eliminated by control rather than assumed: |

| # | directive |
|---|---|
| 1 | **the comptime folder is not the source.** Giving `peek` an effect (`stdout:write("")`) blocks `foldRelationBody`, and the answer is still `1`. |
| 2 | **module storage is not the source.** There is no `__bss` word to read; the binary has no globals. |

| section |
|---|---|
| CLOSED — and the predicate had the same bug twice |

| # | directive |
|---|---|
| 1 | The invalidation and the storage grant both answered wrong for one reason: each counted writes with `assignCountInStmts`, which carries `stmtsAssignName`'s shadowing rule and **stops counting at a `local_decl` of the name**. |
| 2 | Inside a relation body that is right — such a declaration shadows the module binding. |
| 3 | At MODULE scope it is exactly wrong: the declaration IS the binding. |
| 4 | So the count stopped at `s: i64 = 0` and never reached `s = k + 1`, and the predicate answered "stable" for the one program this gap is about. |

| # | directive |
|---|---|
| 1 | `moduleAssignCount` skips the declaring statement at top level and recurses into every other statement — including relation bodies — with the shadow rule intact, because inside a block it is a shadow again. **Both** predicates use it. |

| program | before | after | oracle |
| --- | --- | --- | --- |
| `s: i64 = 0 … s = k + 1` | `42 1` | **`42 43`** | `42 43` |
| the `while` shape | `10 1` | **`10 11`** | `10 11` |
| `s: i64 = 7`, never rewritten | `8` | `8` | `8` |
| `examples/native_differential/g066.id` | `6 5` exit 6 | `6 5` exit 6 | `6 5` |

| # | directive |
|---|---|
| 1 | The g066 row is why the first attempt was wrong and is the reason the fix is this shape: it is gap[066]'s own regression test, a file-scope global a RELATION writes, promoted on 2026-08-18 when the direct backend started giving it a `__DATA,__bss` word. |
| 2 | An earlier form of the storage predicate refused it, which the corpus caught as the single status change. |

| # | directive |
|---|---|
| 1 | unit-test 1673 pass, 8 fail, 0 crash / 1681, failing set identical corpus 809 files under examples/ and lib/, 179 both compiled and ran, 0 status differences, 0 genuine output differences |

| section |
|---|---|
| Superseded acceptance, kept as record |

| # | directive |
|---|---|
| 1 | The real acceptance is one location, not two coordinated readers: |

| # | directive |
|---|---|
| 1 | `collectModuleConsts` refuses a name the module later writes — measured, the predicate above. |
| 2 | `collectModuleGlobals` grants storage for a module-scope write, not only a write from a relation body, **and** publishes the declared initializer instead of skipping it. |

| # | directive |
|---|---|
| 1 | A prior attempt at (2) alone was measured elsewhere in this session to turn seven corpus programs from running into named refusals, because it conflates *foldable* with *resident*. |
| 2 | The two facts are different: **written after binding** kills folding; **observed across frames** forces storage. |
| 3 | Landing either half alone moves the defect. |

| section |
|---|---|
| Related operational leak |

| # | directive |
|---|---|
| 1 | Every `idol compile` on the direct-exe path writes `/tmp/duo_<stem>_<hash>_<pid>_native.o` (`src/main.zig:5038`) and **never deletes it**. |
| 2 | Tens of thousands accumulated during this session and the boot disk reached 100%, corrupting one differential run. |
| 3 | A physical index with no deletion condition. |
