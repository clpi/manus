# GAP-145 — lexical authority is not projected into distinct canonical token identities

**Status:** OPEN · **Filed:** !2026-08-10T12:08:08Z
**Priority:** P0 · **Kind:** expressiveness_gap
**Filed by:** codex-root-lexical-20260810
**Files:** docs/spec/constitution.md,docs/spec/grammar.md,AGENTS.md,CLAUDE.md,lib/compiler/token.id,lib/compiler/lexer.id

<!-- idol-gap-frontier:v1:begin -->
```json
{
  "schema": "idol.gap.frontier.v1",
  "gap": "GAP-145",
  "status": "OPEN",
  "root_program": "lib/compiler/lexer.id -> src/lexer_dispatch.zig -> producer facts -> lib/compiler/parser.id {header_signal_lx,return_starts_value_lx,_clause,_layout_verdict,event,boundary} -> src/parser.zig",
  "current_blockers": [
    "Tree-sitter literal, numeric, keyword-literal, and comment identity rules are emitted from owner-derived rows and mechanically pinned; the wider structural/offside editor grammar is not production lexical authority.",
    "The host-authored TokenKind enum and kindFromRecord row lookup are deleted. Record decode validates through the owner-generated sparse TokenKind enum and performs one physical backing conversion. The generated enum and remaining host parser kind consumers are still load-bearing.",
    "O4 residue: the 54-row host identity table src/token_semantic.zig is DELETED and src/token_classify_gen.zig now reads the owner's generated `.keyword` rows (src/grammar_role_table.zig <- lib/compiler/token.id), so keyword identity has one producer end to end. Still standing, no longer identity producers: lib/token/classify.id and src/keyword_classify.c (owner-rooted artifacts `idol token-tables emit` regenerates byte-stably per gate/table-projection.sh; classify.id is consumed by the SH-02/SH-03 native_only examples and the directLinkInputs classifier swap, keyword_classify.c is src/main.zig boot()'s embedded link input for user programs importing duo_keyword_classify), plus the vendored tools/lsp/src/duo_keyword_classify.c and tools/wasm/src/duo_keyword_classify.c (separate tool projections, untouched). The quote/text/byte consumer collapse is closed at every site measured observable and the remainder is enumerated in the byte-face section below.",
    "O6 closed 2026-08-26: findMatchingParen and interpolationHoleEnd are deleted from src/parser.zig; delimiter extent is observed over Parser.pack_tokens (matchingTokenClose + token_view.fromTokens). gate/gap-145-consumer.sh section 2b refuses raw scans and lexer-pack aliases with planted positive controls; remaining findMatchingParen sites are foreign C header ingress, upstream of any Idol token.",
    "One callable-header recognition edge now executes from lib/compiler/parser.id; remaining parser recognition is host-executed, so lexical observer zero, complete parser SHC, and Compiler B are not established.",
    "A second bounded parser decision now executes from lib/compiler/parser.id: `returnStartsValue` reads the immutable producer pack through `return_starts_value_lx` (returns 0/1/2 over the same kind[8] | line[28] encoding used by `header_signal_lx`).",
    "A third bounded decision now executes from parser.id `_clause`: match-arm pattern role, contextual `case`, same-line/nesting, and separator recognition. The physical pack carries metadata plus raw short-lexeme bytes; Zig assigns no word role.",
    "The fourth through thirteenth bounded decisions execute from whole-pack `event` bits 17..61: primitive/literal/quoted/prefix/demand and trivia-aware line-head facts occupy 17..22; the packed Pratt triple occupies 23..46; unary/glue/update ordinals occupy 47..61 with zero absent and ordinal+1 present. The host role reads, copied order/name scans, and standalone `lead`, `prefix`, `demands_operand`, `infix_prec`, primitive/literal/quoted, `_unary`, `_glue`, and `_update` relations/ABIs/calls/tests are deleted. Demand is captured before cursor advance; quoted identity is captured before saved-token advance; trivia does not update line-head history.",
    "A fourteenth through seventeenth bounded decision group executes through public `boundary` plus private `_layout_verdict`. Event bits 15 and 16 carry the six layout terminators and five empty-body closers. `boundary(opening,...)` uses an explicit opening face to return the physical layout frame; edge calls pass bit 15 into `_layout_verdict`, and the empty-body path consumes bit 16. The host switches and standalone `_opening`, `_layout_terminator`, `_empty_body_terminator`, and direct `idol_parser_layout_verdict` relations/ABIs/calls/tests are deleted. `_layout_verdict` is boundary-internal.",
    "An eighteenth bounded decision executes from whole-pack public `event` bit 9: static member identity after `.` / `:` / `@` consumes the owner-generated `member()` row once per pack. The spelling-based host helper and standalone public `member` relation/ABI are deleted. The owner admits ordinary name plus all keyword identities in fixed member position and refuses literal payload labels and punctuation.",
    "A nineteenth through twenty-first decision group executes from graph-proven public parser.id relation `boundary`, exposed as `idol_parser_boundary`: its explicit `opening` face returns the physical layout frame; edge results select continue/close/misindent/return-and-close in bits 0..1, carry none/elseif/else in bits 2..3, carry written closure in bits 4..6, and return the possibly established body column in bits 8..35. Its `edge` operand is the whole-pack event, not a raw token kind: bits 10..15 settle unconditional boundary, clause face, return, written end, and layout terminator. Boundary reads no token row, validates reserved branch face 3 to none, and privately executes layout geometry. The return-and-close path observes the following event coordinate so a post-return branch face cannot be lost. Standalone opening, clause, closure, and direct layout-verdict ABI seams are deleted; the one public boundary ABI remains until complete parser-pack execution.",
    "A twenty-second bounded decision executes through whole-pack `event` bits 0..4: owner row `statement()` maps all 114 token identities to direct statement faces. The direct `parse_stmt` TokenKind switch is deleted; `@` lookahead remains host-executed.",
    "A twenty-third bounded decision executes through `event` bits 5..8: owner row `admission()` maps macro plus canonical-Idol retirements over all 114 identities, and `idol` settles the family answer. The host token-kind and `idol_mode` deny switch is deleted; Zig materializes only the selected diagnostic.",
    "A twenty-fourth bounded decision executes through lane-two bit 6: exact contextual `type` plus following non-`(` identity selects alias declaration while `type(value)` remains a call. Four Zig consumers read the settled face; five source-text comparisons and the direct save/advance/peek/restore branch are deleted.",
    "A twenty-fifth bounded decision executes through lane-two bit 7: callable-header bit 4 propagates backward over exact direct, dotted, or subject-specialized name paths. Four Zig consumers read the settled head; `starts_bare_func_decl` and its save/scan/restore path walk are deleted.",
    "A twenty-sixth bounded decision executes through lane-two bits 9..12: the exact attributed-declaration dispatch face selects function, async function, enum, concept, alias, binding, loop, or name-shaped declaration. The host TokenKind dispatch switch is deleted; Zig selects only the settled face.",
    "Transport consolidation: graph-proven public `event` consumes the complete immutable parser_facts pack once, capacity-checks before mutation, and writes two contiguous words per coordinate. Lane one carries bits 0..61 static/event facts. Lane two carries match face bits 0..1, return face bits 2..3, callable-header variants bits 4..5, contextual type head bit 6, bare declaration head bit 7, assignment-body bit 8, attributed-declaration face bits 9..12, and delimiter boundary bits 13..63. Parser caches and releases both lanes beside facts and uses the same sole cursor. All per-coordinate owner-derived decisions now cross once; this does not add an ABI call.",
    "Record/cursor bridge deletion: kindFromRecord, lexer_dispatch's grammar_role_table runtime import, TokenizeAuthority/query, ProductionPack/tokenizePack/fromPack, useDuoTokens, TokenStreamError, Lexer.duo_tokens/duo_index/duo_next, installProducerPack, token_view.fromLexer, and routeThroughDuoLexer are gone without aliases. Route returns one proven slice; Parser alone owns pack/index and speculative snapshots. The positive-controlled gate passes 359 checks; the artifact includes 228 event cases, 2 trivia-lead controls, 8 header-lane, 6 return-lane, 8 match-lane, 5 type-lane, 5 bare-lane, 114 event-to-boundary slots, and 7 opening cases. Admission proves only `event` and `boundary` as public graph relations, and those are the only two parser externs. NOT closure: generated TokenKind, contextual `@` lookahead, internal parser kind switches, host staging/AST construction, and 13 face-neutral quote-blind arms remain."
  ],
  "superseded_observations": [
    "#current-measured-boundary-2026-08-17",
    "#measured-observer-census-2026-08-23-lanegap145-20260823",
    "#current-head-reconciliation-2026-08-23-b8073cef"
  ]
}
```
<!-- idol-gap-frontier:v1:end -->

## Attributed-declaration dispatch transferred (2026-09-01)

The attributed-declaration consumer previously switched on host `TokenKind` to
choose function, async function, enum, concept, alias, local/global binding, or
loop parsing. Whole-pack `event` now projects that exact face through lane-two
bits 9..12. The consumer selects the settled face, and the matching host kind
switch is deleted. The existing declaration lookahead observes nonzero face;
the mutually exclusive matching-parenthesis boundary moves to bits 13..63. No
ABI, event word, allocation, cursor walk, or fallback was added.

`tools/node/dev/parser/artifact` proves every dispatch face and negative control
against both the generated and tracked projection. `zig build parser-artifact`
passes, and `sh gate/gap-145-consumer.sh` passes with its planted controls.

Semantic diff: before, Idol supplied only a boolean declaration fact and the
host reconstructed the exact attributed-declaration relation from token kind;
after, Idol supplies the exact relation face and the host only invokes the
selected parser action. The named next refusal is the remaining name-shaped
attributed-declaration selection in `parse_attributed_decl`: contextual `type`,
bare callable, and Jai type-definition routing still use host `.name` tests
around already-projected lane-two facts. GAP-145 remains **OPEN**.

## Dormant directive-face identity copy deleted (2026-09-01)

The `f484a227d` event consolidation carried one unreported lane-one residue:
bit 62 invoked `_at_face` for every producer coordinate.  That relation copied
the host directive and attribute spelling catalogs into 349 lines of
`lib/compiler/parser.id`, reduced raw packed lexeme bytes, and had no production
consumer.  Its sole observer was 37 generated-C artifact cases.  It therefore
moved no parser recognition, duplicated the host identity authority, added one
whole-pack computation, and left the next seam standing.

The relation, event bit, artifact-only observer, and generated C are deleted
without an alias.  The production `src/parser.zig` diff is zero.  The retained
event and boundary differentials still report `failed=0`; the positive-controlled
GAP gate passes; the serialized build passes 5/5 steps; and the fresh unit
aggregate passes 9/9 steps.  The resulting debug artifact is 28,454,904 bytes
with SHA-256
`fb3904ad63b50b187b19693c0d1c9f799acb681255017cef8051016925b7a879`.
Artifact bytes are recorded as a revision-specific observation, not an FTCFTW
claim; latency, memory, cross-host, and external-oracle cells remain unknown.

Semantic diff: before, host directive identity was reconstructed from source
bytes inside an unconsumed Idol fact; after, that duplicate fact, its runtime
work, and its test-only materialization do not exist.  Authority remains with
the current host catalog until a single upstream producer can project the exact
identity.  This reduces reconstruction and physical state but does not transfer
another production parser decision.  Generated `TokenKind`, remaining host
parser recognition and AST staging, lexical observer zero, Parser SHC, and
Compiler B remain open.  GAP-145 therefore remains **OPEN**.

## Bare declaration path transferred (2026-09-01)

`starts_bare_func_decl` and four callers previously replayed a
save/scan/restore walk across direct, dotted, and subject-specialized declaration
paths. `event` now reuses its already-settled callable-header bit 4, walks
backward over the exact name path, and writes lane-two bit 7 at the statement
head. The host function and all calls are deleted; no ABI, event word,
allocation, cursor, runtime call, or fallback was introduced.

Direct, dotted, and subject-specialized cases each failed before their owner
step and passed afterward; ordinary call and `a:b.c(...)` controls remained
false. Final artifact: `bare-lane-cases=5`, `failed=0`. The consumer gate passes
359 positive-controlled checks, the pinned build passes 5/5, and the fresh unit
aggregate passes 1,845 / 62 skipped / 0 failed. Exact debug artifact:
28,803,480 bytes,
`56ff5d51941b10ebc785f1d6ea8e8878ca7e3d6ad2f22d7b65e137e7659ba587`.
That is a 608-byte slice win but remains 752 bytes above the original two-lane
artifact; every latency, memory, cross-host, and external-oracle cell is unknown.
This is the twenty-fifth bounded decision, not lexical closure, Parser SHC,
Compiler B, cross-platform admission, or FTCFTW.

## Contextual type statement head transferred (2026-09-01)

Five Zig source-text comparisons formerly decided whether contextual name
`type` began an alias declaration or remained the compatibility call
`type(value)`. The direct statement path also saved, advanced, peeked, and
restored the parser. Whole-pack `event` now sets lane-two bit 6 from the existing
bounded raw producer word plus the following token identity. Four consumers
select that fact, and all five text decisions plus the direct lookahead are
deleted. No new ABI, event word, allocation, cursor, or fallback was introduced.

The generated-C artifact failed first on only the two alias-positive cases, then
passed all five type controls with `failed=0`. `gate/gap-145-consumer.sh` passes
349 positive-controlled checks; the pinned build passes 5/5 and the fresh unit
aggregate passes 1,845 / 62 skipped / 0 failed. Exact debug artifact:
28,804,088 bytes,
`0efc298400dfeb72920a12b8508eb3f8c3c26dd6eb006ebe6cf4083ebe56a678`.
The +1,360-byte change is an open footprint loss; latency, startup, compile work,
and memory are unmeasured. This is the twenty-fourth bounded parser decision,
not lexical closure, Parser SHC, Compiler B, cross-platform artifact admission,
or an FTCFTW result.

## Second whole-pack lane; host-facing decision ABIs zero (2026-09-01)

`event` now capacity-checks and writes two contiguous words per producer
coordinate. Lane one is unchanged. Lane two packs match face in bits 0..1,
return face in bits 2..3, and callable-header `allow=false/true` variants in
bits 4/5. The event walk derives previous visible identity and line, the line
opener column, source family, contextual `case`, delimiter nesting, and
separator presence. Zig selects settled faces but no longer replays these facts.

Strict RED evidence was four missing match faces, four missing return faces, and
four missing header faces. Each old direct ABI remained as the differential
until lane equivalence was green, then its extern, build rename, production call,
and standalone artifact call were deleted. A fail-closed regression also proved
that bypassing pack installation still refuses rather than installing a fresh
pack at the wrong coordinate.

Current proof: artifact `failed=0`; GAP-145 `PASS (340)`; aggregate 1845 pass /
62 skip / 0 fail. `src/parser.zig` has exactly two parser externs: whole-pack
`event` and complete dynamic `boundary`. Exact artifact: 28,802,728 bytes,
`dd5397c8f7fb5f3c945f9d93e124d18fc1cf8e00f1b980e9ff4a5adbe94cd109`.
This closes host-facing per-decision C seams, not Parser SHC: Zig still stages
statements/expressions and constructs the AST. Paired evidence validates;
correctness is `bound` and artifact bytes are a 4,032-byte `win`. Thermal history
keeps every latency row `unknown`; raw boundary/clause/statement/member intervals
point toward losses and startup is inconclusive.

## Static event lane baseline (2026-09-01)

`event` now carries every owner-derived per-coordinate static face through bit
61. The former standalone decisions `lead`, `prefix`, `demands_operand`,
`infix_prec`, primitive/literal/quoted identity, `_unary`, `_glue`, and
`_update` are deleted from canonical source, generated C, build renames, Zig
externs/calls, and direct artifact tests. `advRaw` observes demand before cursor
advance; saved literal keys retain quoted identity before advance; line-head
history ignores shebang/comment slots. The negative control that bypasses the
production pack still refuses, so the migration did not install a hidden lexer
fallback.

RED evidence was 18/36/6/70 event mismatches across four vertical slices.
GREEN evidence is 228 exhaustive family/slot rows plus two trivia-history cases,
`failed=0`; GAP-145 `PASS (325)`; admission `ADMITTED` with public `event` and
`boundary` only; aggregate 1845 pass / 62 skip / 0 fail. The exact artifact is
28,806,760 bytes with SHA-256
`fa2162b262a10c5bb93ca4563487e48688e98d389e17515ebc965cb60ad46284`.
Five parser externs remain (`header_pack`, `return_starts_value`, `match_clause`,
`event`, `boundary`), so complete Parser SHC and Compiler B remain open. Paired
internal evidence validates with five damaged controls; every latency cell is
`unknown` because throttle history started nonzero. Artifact bytes are an
explicit `open` +11,704 loss, and raw boundary/clause/statement/member paired
intervals point toward losses rather than a fastest claim.

## Eighteenth executed parser decision: static member identity (2026-08-31)

`src/parser.zig` used `TokenKind.spelling()` plus
`std.ascii.isAlphabetic(spelling[0])` to decide whether the token after a fixed
`.` / `:` / `@` member edge could name that member. Presentation text therefore
became grammar authority: the generated labels `integer`, `float`, `text`, and
`bytes` were accepted as names, while contextual keyword member `end` was
refused.

`lib/compiler/token.id` now owns zero-parameter row `member()`: ordinary `name`
and every owner keyword identity are true; literal payloads and punctuation are
false. `lib/compiler/parser.id` public `member` reads that row and crosses the
bootstrap ABI as `idol_parser_member`. The host `is_name_like_kind` function,
its spelling read, and its ASCII test are deleted. The parser retains only the
already-fixed member context and the source text it stores in the AST.

Executed evidence on the exact working-tree subject:

```text
idol check lib/compiler/{token,parser}.id
→ both checked with no errors
IDOL=./zig-out/bin/idol sh tools/node/dev/parser/artifact
→ generated bytes match; member-slots=114; failed=0; PASS
sh gate/gap-145-consumer.sh
→ PASS (200 checks), including planted host-spelling and owner-row damage
zig build unit-test --summary all through tools/node/dev/idol-lock
→ 9/9 steps succeeded; 1846 pass, 62 skip, 0 fail
```

Semantic diff: before, a host display label produced member identity; after,
the owner token identity produces the fact and the production consumer executes
it through parser.id. No new semantic concept, allocation, copy, parser state,
or runtime table was added. The source relation is admitted by the graph-backed
vocabulary witness; the C ABI remains a bounded bridge with deletion at complete
parser-pack execution.

Physical evidence is retained in
`/home/clp/hermes-jobs/allowance/openai-codex/member-frontier.json`. Against exact
`7053423b` with the pinned Zig toolchain, the candidate debug compiler is 200
bytes smaller. On one identical 1,500-member `idol check` workload, 21 strict
alternating pairs put the candidate median 1.258485 ms above the base and the
base lower in 15 pairs. That latency cell is `open`, not hidden by the semantic
win; its deletion condition is the complete parser-pack boundary removing this
per-member C call.
This is not complete parser SHC or Compiler B.

## Nineteenth executed parser decision: complete block boundary (2026-08-31)

`parse_block_open` formerly combined two host decisions after every candidate
statement edge: `layout_verdict` mapped the generated relation back into a Zig
enum, then a TokenKind switch independently listed written/block/bracket
closers, conditional `else`/`elseif`, and `return`. This was one semantic action
split across an Idol relation and a host switch.

The token owner now emits `boundary()` for unconditional closers and `branch()`
for conditional branches. Public graph-proven parser relation `boundary` joins
those exact identities with statement count, frame columns, token line, prior
line, and token column. Its packed result is continue/close/misindent/
return-and-close plus the possibly established body column. Zig validates and
materializes that action; it carries no closer or branch list. The host
`LayoutVerdict` enum/function and the token-kind switch are deleted.

The executable artifact test was planted before production code. It first
failed with missing `token_grammarrole__boundary`,
`token_grammarrole__branch`, and `idol_parser_boundary`. After implementation,
three behavioral assertions exposed a test-fixture error: the fixed producer ABI
puts `kw_return` at slot 24, not slot 22. Correcting that expectation made the
same 114-slot harness report `boundary-slots=114, failed=0`.

Executed evidence:

```text
zig build --summary all through tools/node/dev/idol-lock
→ 5/5 steps succeeded
sh gate/gap-145-consumer.sh
→ PASS (219 checks), including planted host-switch and two-row owner damage
IDOL=./zig-out/bin/idol sh tools/node/dev/parser/artifact
→ generated bytes match; boundary-slots=114; failed=0; PASS
zig build unit-test --summary all through tools/node/dev/idol-lock
→ 9/9 steps succeeded; 1846 pass, 62 skip, 0 fail
```

Semantic diff: before, host TokenKind lists and a host enum selected the block
action; after, owner identities and parser.id select it once. This is still a
bounded C ABI seam, not complete parser SHC or Compiler B. Its deletion
condition is complete parser-pack execution.

Physical evidence is retained in
`/home/clp/hermes-jobs/allowance/openai-codex/boundary-frontier.json`. Against
exact base `7053423b`, the candidate debug compiler is 3,016 bytes smaller. On
one identical 500-relation block-heavy `idol check` workload, 51 strict AB/BA
pairs put the candidate lower in 40 pairs and the base lower in 11; paired
median delta is -0.252742 ms, with bootstrap 95% interval
[-0.321725, -0.138723] ms and two-sided sign-test p = 0.0000570381. The
candidate/base aggregate medians are 60.184763/60.431264 ms. This cell is a
workload- and revision-specific `win`.

The retained vector does not hide the other axes. Startup/CLI latency is also a
paired `win` (candidate lower in 39/51; median delta -0.279796 ms; bootstrap 95%
interval [-0.360686, -0.154556] ms). Peak RSS is `open`: candidate median
14,496 KiB versus base 14,464 KiB, a +32 KiB loss in all 31 pairs whose exact
code/link cause is not yet isolated. Minor faults are a measured `win` (777 vs
778 median; candidate lower in 24/31), while major faults are `bound` at zero.
Three strace runs establish the zero-network-call lower bound for traffic;
overall syscall counts overlap at 122/123 versus 123 and remain `unknown`.
Dynamic relocations are equal at 105 but remain `unknown` absent a lower-bound
proof. User/system CPU, PMU, energy, and portability remain `unknown`. None of
these rows is a strongest-external-oracle or universal-fastest claim.

## Nineteenth through twenty-first decisions: one complete block-edge answer (2026-09-01)

The transfer first split block semantics across `boundary`, `clause`, and
`closure`. That proved each decision but left three generated-C calls around one
token edge. The final consolidation deletes the standalone public `clause` and
`closure` relations, generated symbols, build renames, Zig externs/calls, and
separate differential functions.

Graph-proven public `boundary` now carries all four settled facts: loop action in
bits 0..1, none/elseif/else in bits 2..3, written closure in bits 4..6, and body
column in bits 8..35. `LayoutFrame` stores the clause face;
`parse_if_clauses` consumes it; `close_block` consumes only the closure bits and
retains diagnostics/token actions. The return-and-close path still re-observes
the following token edge through the same relation, preserving its dedicated
post-return clause regression.

The closure consolidation's generated-C differential was planted first and
failed to compile in both candidate and tracked projections because the old
`boundary` accepted eight operands, not the required ninth `idol` fact. The
implemented artifact exhausts 48 written-closure combinations plus 686 clause
identity/layout/column combinations. No host token identity or column condition
is re-decided.

```text
zig build --summary all through tools/node/dev/idol-lock
→ 5/5 steps succeeded
sh gate/gap-145-consumer.sh
→ PASS (248 checks)
IDOL=./zig-out/bin/idol sh tools/node/dev/parser/artifact
→ boundary-closure-cases=48; boundary-clause-cases=686; failed=0; PASS
zig build unit-test --summary all through tools/node/dev/idol-lock
→ 9/9 steps succeeded; 1847 pass, 62 skip, 0 fail
```

These remain twenty-one bounded decisions, not complete parser SHC or Compiler
B. The one `boundary` C seam deletes with complete parser-pack execution.

Physical evidence for the superseded standalone-clause subject is retained in
`/home/clp/hermes-jobs/allowance/openai-codex/clause-frontier.json`; the discarded
two-call candidate remains in `clause-two-call-frontier.json`. Replacing
two clause booleans with one none/elseif/else face reduced the clause regression
from +0.476409 ms to +0.201816 ms, but did not erase it. Against the exact
boundary-only compiler, 51 AB/BA pairs classified closure check latency `open`
(+0.526745 ms paired median; base lower in 33/51), clause check latency `open`
(+0.201816 ms; base lower in 39/51), and startup `open` (+0.185279 ms; base
lower in 33/51). The artifact was 80 bytes smaller. RSS, traffic, relocations,
syscalls, PMU, energy, and portability remain `unknown` for that historical
subject. These losses remain visible; no fastest claim follows.

Current consolidation evidence is retained in
`/home/clp/hermes-jobs/allowance/openai-codex/boundary-clause-consolidation.json`
and passes `validate_pair_frontier.py` plus five damaged controls. Against the
exact standalone-clause artifact, 51 AB/BA raw pairs point lower for boundary
(-0.295463 ms), clause (-0.262148 ms), and startup (-0.300907 ms), but all three
latency statuses are now `unknown`: historical Raspberry Pi throttle bits were
already set before the run, so equal pre/post bytes do not prove no recurrence.
The artifact remains a measured 16-byte `win`; correctness is `bound` to
byte-identical exit/stdout/stderr plus the 1847/62/0 aggregate. CPU, RSS, traffic,
relocations, syscalls, faults, PMU, energy, and portability remain `unknown`.
No row implies a strongest-external-oracle or universal-fastest claim.

Closure consolidation evidence is retained in
`/home/clp/hermes-jobs/allowance/openai-codex/boundary-closure-consolidation.json`
and passes `validate_pair_frontier.py` plus five damaged controls. Against the
exact boundary-plus-standalone-closure artifact, all 51-pair latency rows remain
`unknown`: boundary-heavy check paired median -0.317668 ms with interval
[-0.906559, +0.208760] ms; clause-heavy check -0.236667 ms with interval
[-0.449781, -0.019890] ms; startup +0.046427 ms with interval
[-0.214464, +0.313779] ms. The artifact is an explicit `open` loss of 88 bytes.
Correctness is `bound` to identical exit/stdout/stderr plus the 1847/62/0
aggregate. The Pi's throttle state changed from `0xe0000` before measurement to
`0xe0008` after it, so this run cannot activate a close performance claim. CPU,
RSS, traffic, relocations, syscalls, faults, PMU, energy, and portability remain
`unknown`. No strongest-external-oracle or universal-fastest claim follows.

## Twenty-second decision: owner-projected direct statement dispatch (2026-09-01)

`parse_stmt` used a host `TokenKind` switch to select direct local/global/const/
function/control/label materializers. `lib/compiler/token.id` now owns compact
row `statement()` over all 114 identities. The existing `boundary` result carries
the face in bits 36..40; the normal block loop pays no new ABI call.

The generated-C test was planted first and failed because
`token_grammarrole__statement` did not exist. The final differential compares the
owner row and boundary result with an explicit 114-slot oracle plus bounds. The
owner-damage control changes one `while` face and requires regeneration to move
the row. The direct host branches are deleted. `parse_stmt_face` materializes the
settled face; its wrapper asks `boundary` only at exceptional callers outside the
normal block loop. Contextual `name` and `@` lookahead remain host-executed.

```text
sh gate/gap-145-consumer.sh
→ PASS (260 checks)
IDOL=./zig-out/bin/idol sh tools/node/dev/parser/artifact
→ statement-slots=114; failed=0; PASS
zig build unit-test --summary all through tools/node/dev/idol-lock
→ 9/9 steps succeeded; 1847 pass, 62 skip, 0 fail
```

Raw evidence is `statement-dispatch-consolidation.json`, validated with four
damaged controls. Its historical throttle bits were already nonzero before the
run, so all latency cells now fail closed to `unknown`. Raw pairs point lower for
boundary-heavy check (-0.907410 ms) and startup (-0.201834 ms); clause and
statement intervals cross zero. Artifact bytes are an explicit `open` +2,040
loss. Correctness is `bound`; CPU, RSS, traffic, relocation, syscall, fault, PMU,
energy, and portability rows remain `unknown`. These are internal
revision-specific rows, not a strongest-external-oracle or universal-fastest
claim.

## Twenty-third decision: owner-projected statement admission (2026-09-01)

`denyRetiredStmtKeyword` formerly selected denial from `tok.kind` and
`self.idol_mode`. Owner `admission()` now maps every physical slot to accepted,
always-denied macro, or one of seven canonical-Idol retirement diagnostics.
`boundary` settles the family answer in bits 41..44. The normal block loop pays
no new ABI call; five exceptional attributed/let/await sites query the same
relation. Zig retains only the numeric diagnostic materializer.

The test-first generated-C differential covers all 114 identities in both family
states plus bounds (`admission-cases=228`). Owner damage changes the `let` face;
the gate rejects restoration of macro or try/catch token switches and requires
all exceptional calls.

```text
sh gate/gap-145-consumer.sh
→ PASS (272 checks)
IDOL=./zig-out/bin/idol sh tools/node/dev/parser/artifact
→ admission-cases=228; failed=0; PASS
zig build unit-test --summary all through tools/node/dev/idol-lock
→ 9/9 steps succeeded; 1847 pass, 62 skip, 0 fail
```

Raw evidence is `statement-admission-consolidation.json`. Its validator and four
damaged controls pass, but all four latency rows are `unknown`; the paired
medians are boundary +0.002963 ms, clause +0.492353 ms, statement +0.033760 ms,
and startup +0.018426 ms, each with an interval crossing zero. Artifact bytes
are an explicit `open` +2,680 loss and correctness is `bound`. The Pi acquired
throttle bit `0x8` during measurement, so no close performance claim activates;
unmeasured resource and portability axes remain `unknown`.

## First production whole-pack event transport (2026-09-01)

The existing immutable `parser_facts` pack is now consumed once by public
`event(fact,count,out,capacity,idol)`. The relation writes one static fact word per
token into caller-owned storage: statement face, family-settled admission, member,
unconditional boundary, and branch. Short capacity returns `-1` before mutation.
Parser caches `parser_events`, checks the exact written count, indexes it with the
same sole producer cursor, and releases it with the owned pack.

Production dispatch and admission now consume event bits 0..8. Their former bits
36..44 and row reads are deleted from dynamic `boundary`, which retains only
layout-dependent loop/clause/closure/body facts. There is one event ABI declaration
and one production call, not one call per token.

```text
sh gate/gap-145-consumer.sh
→ PASS (288 checks)
IDOL=./zig-out/bin/idol sh tools/node/dev/parser/artifact
→ event-cases=228; guards preserved; failed=0; PASS
admission-all.sh
→ ADMITTED; 10 graph-proven module-scope relations
zig build unit-test --summary all through tools/node/dev/idol-lock
→ 9/9 steps succeeded; 1847 pass, 62 skip, 0 fail
```

`whole-pack-event-consolidation.json` validates with five damaged controls. All
latency rows remain `unknown` because throttle changed from `0xe0000` to `0xe0008`;
three parser-heavy paired medians were slower and remain visible. Artifact bytes
are an `open` +6,240 loss; correctness is `bound`; other resource and portability
axes are `unknown`. This transport is the foothold for moving dynamic parser
events and deleting per-decision seams, not a claim of complete Parser SHC.

### Member seam deletion

The three production member observers now call `currentParserMember`, which reads
bit 9 from the event at the sole producer coordinate. The standalone public
`member(kind)` relation, generated function, build rename, Zig extern, direct
artifact differential, and every per-call site are deleted. The owner row remains
inside `event`, and a direct Zig event test covers all 114 physical slots.

The deletion gate was RED on seven old surfaces, then passed 291 checks. Admission
proves nine graph relations and the aggregate remains 1847 pass / 62 skip / 0
fail. `whole-pack-member-consolidation.json` validates with five workloads and
five damaged controls; the retained run is thermally affected, so every latency
row is `unknown`, artifact bytes are an `open` +912 loss, and correctness is
`bound`. A prior four-workload summary with an open statement loss is rejected
because its raw JSON was overwritten before archival.

### Boundary/layout seam deletion

The event word now carries unconditional boundary (bit 10), a two-bit branch face
(bits 11..12), return (13), written `end` (14), layout terminator (15), and the
five-member empty-body face (16). `open_layout`, empty-body recognition,
`close_block`, the body loop, and the return-following-clause edge read the exact
current event. Public `boundary` receives no raw token kind and reads no token row;
it combines only settled event facts with physical count/line/column/frame state.
Reserved branch face 3 fails closed to none.

The standalone `_layout_terminator` and `_empty_body_terminator` relations plus
the direct `idol_parser_layout_verdict` ABI are deleted from canonical source,
generated C, build mappings, Zig declarations/calls, and direct tests.
`_layout_verdict` remains private and boundary-internal; `_opening` remains the
single direct layout-frame ABI. The generated-C RED failed on 23 static-edge
cases before the new bits were implemented. Final evidence is:

```text
parser artifact → event-boundary-slots=114; event-cases=228;
                  boundary-closure-cases=48; boundary-clause-cases=686;
                  failed=0; PASS
GAP-145 consumer → PASS (301 checks)
admission-all    → ADMITTED; 9 graph-proven public relations
unit-test        → 9/9 steps; 1847 pass / 62 skip / 0 fail
artifact         → 28,795,056 bytes; 5a332c3b48641e699c5a7372ff534da79768099bfacd052c3db7a56b8340635a
```

`whole-pack-boundary-consolidation.json` validates all raw paired samples and
five damaged controls. All latency cells are `unknown` because the Pi had
nonzero historical throttle bits before measurement; equal pre/post bytes cannot
prove a clean run. The raw clause interval points toward a loss; boundary,
statement, member, and startup intervals are inconclusive. Artifact bytes are an
`open` +328 loss and correctness is `bound`. No fastest claim follows.

### Opening folded into the public boundary

`_opening` was the last direct layout-frame relation. `boundary` now has an
explicit `opening` operand and `open_line`; opening calls return the same physical
frame bits, while edge calls retain loop/clause/closure/body output. No sentinel,
repurposed payload, or positional currying selects the face.

The relation, generated symbol, build rename, Zig extern/call, and direct
artifact function are deleted. The RED artifact failed every new eleven-operand
call while boundary still accepted nine. The final artifact adds
`boundary-opening-cases=7`; GAP-145 passes 305 checks and the aggregate remains
1847 pass / 62 skip / 0 fail. Exact compiler:

```text
28,795,056 bytes
1b423570da2a0400ae1de176b1097f2eeef2bb06475c2a0d8f3f864e5914e537
```

`boundary-opening-consolidation.json` validates with five damaged controls. All
latency rows are `unknown`; throttle changed from `0xe0000` to `0xe0006`. The raw
boundary interval points toward a loss; the other four intervals are
inconclusive. Artifact bytes are equal (`unknown`) and correctness is `bound`.
No fastest claim follows.

## O3 duplicate lexer cursor deletion (2026-08-31, base `6e8abf5a` + diff)

The producer already returned one immutable token slice, but Lexer and Parser
both stored and advanced it. `Lexer.duo_tokens`, `duo_index`, and `duo_next`
owned a second cursor; Parser mirrored that index after every `peek`/`next`, and
`token_view.fromLexer` exposed the alias again. The duplicate is deleted.

Parser now reads `pack_tokens[pack_index]` directly, skips producer trivia while
retaining it for formatter/cache observers, and snapshots `pack_index` together
with host-oracle cursor and prior-token state on every speculative parse. Main's
three pre-routed production paths bind the returned slice directly to Parser.
Lexer `next`/`peek` are host differential only. The one-consumer
`installProducerPack` and private `routeThroughDuoLexer` wrappers are deleted
rather than renamed.

Executed evidence on the final wrapper-free diff:

```text
/tmp/idol-zig/zig-aarch64-linux-0.17.0-dev.1567+f0354179a/zig fmt --check <five changed Zig files>
→ exit 0
zig build --summary all through tools/node/dev/idol-lock
→ 5/5 steps succeeded
sh gate/gap-145-consumer.sh
→ PASS (77 checks), including planted duplicate-cursor controls
IDOL=./zig-out/bin/idol sh tools/node/dev/parser/artifact
→ generated bytes match; all header/return/match cases; failed=0; PASS
zig build unit-test --summary all through tools/node/dev/idol-lock
→ 9/9 steps succeeded; 1832 pass, 62 skip, 0 fail
```

This removes one physical bridge and one cursor state; it does not remove the
owner-generated `TokenKind`, the host-majority recognizer, or the 13
face-neutral quote arms. No performance or Compiler-B claim follows.

## Fourth executed parser decision: line-head `lead` (2026-08-31)

`src/parser.zig` formerly read `grammar_roles.lookup(tok.kind).opens_line`, then
rescanned source bytes backward to decide whether `-`, `~`, or `@` began a new
expression. That was a host recognizer and depended on source views even though
the grammar owner already published the identity set.

`lib/compiler/parser.id` now owns relation `lead(kind, line, before)`. It asks
the generated owner's `token.grammarrole.lead()` row and combines that fact with
the exact token/previous-token line relation. Zig crosses those three physical
facts at the two Pratt sites; it assigns no grammar role. The host
`opensLineAndExpression` function, `.opens_line` read, and byte rescan are
deleted. Parser state restores `prev_line`, so speculative reads cannot corrupt
the input fact.

Controls and evidence:

```text
throwaway 114-slot C probe against token.grammarrole.lead()
→ slots=114 failed=0
IDOL=./zig-out/bin/idol sh tools/node/dev/parser/artifact --write
→ generated bytes match; lead-slots=114; failed=0; PASS
sh gate/gap-145-consumer.sh
→ PASS (85 checks), with planted old-host/new-Idol detector
focused production parser pair
→ `x = 3\n-2` preserves unary `-2` as block tail
→ `x = 3\n* 2` remains one multiplication assignment
fresh ZIG_LOCAL_CACHE_DIR zig build unit-test --summary all
→ 9/9 steps succeeded; 1834 pass, 62 skip, 0 fail
```

Same-authority, clean-base paired measurement on `idol check
lib/compiler/parser.id`, two warmups and 21 alternating pairs: base median
228.057 ms, candidate median 222.164 ms; candidate lower in 16/21; ranges
207.442–254.461 ms and 204.909–260.864 ms overlap. No speed or regression claim.
The combined cursor-deletion plus fourth-recognizer debug binary is 4,488 bytes
smaller than the exact-base binary. This is four bounded recognizers, not a
complete parser stage or Compiler B.

Implement STRING-ONE, BYTES-ONE, COMMENT-ONE, reserved backtick, compatibility
provenance, generated grammar roles, and derived tooling projections in
migration order without text reconstruction.

## Fifth executed parser decision: unary `prefix` (2026-08-31)

`src/parser.zig` formerly read `grammar_roles.lookup(first_tok.kind).prefix`
inside `parse_expr_stmt` to decide whether the first token began an expression
even with no caller in front of it. That single host read at the only call
site asked a fact the grammar owner already published, exactly parallel to the
recently transferred `lead`.

`lib/compiler/token.id` now exposes relation `prefix()` (a dense zero/one fact
slice of length `roleslotcount` like `lead()`), generated into
`lib/token/grammarrole.id` and `src/parser/projection.c`. `lib/compiler/parser.id`
relation `prefix(kind)` reads that row and returns the owner's verdict; Zig
crosses only the physical kind ordinal at `parse_expr_stmt`. The host `.prefix`
role read is deleted. The same row shape as `lead` because both are dense
zero/one fact slices over every role slot.

Controls and evidence:

```text
throwaway 114-slot C probe against token.grammarrole.prefix()
→ slots=114 failed=0
IDOL=./zig-out/bin/idol sh tools/node/dev/parser/artifact --write
→ generated bytes match; lead-slots=114; prefix-slots=114; failed=0; PASS
sh gate/gap-145-consumer.sh
→ PASS (93 checks), with planted old-host/new-Idol detector
focused production parser test
→ `parse: production prefix decision executes the Idol relation`
   iterates every role row and asserts row.prefix == idol_parser_prefix(kind)
fresh ZIG_LOCAL_CACHE_DIR zig build --summary all
→ 5/5 steps succeeded
```

This is six bounded recognizers, not a complete parser stage or Compiler B.
The hot `demands_operand` read has moved from `parse_prec`'s `advRaw` to the
canonical `parser.id` relation (2026-08-31); the infix-precedence table
remains the next candidate and is still admitted as an optimization hotspot,
not a single cool candidate.

The next measured frontend candidate is `parse_prec`'s hot `infix_prec`
reads (the `lookup`/`infixRelation` pair that currently decides relation
identity + precedence + associativity on every Pratt step) or
`token_view`'s remaining `isLiteralKind`/`isDescriptor` host-side reach into
`grammar_roles.lookup`, each gated by an exhaustive producer-row probe and
a paired timing run before commit.

## Seventh executed parser decision: Pratt binding-power `infix_prec` (2026-08-31, base `6e8abf5a` + diff)

`parse_prec` runs once per Pratt step and used to ask
`grammar_roles.infixRelation(kind)` for the operation identity AND
`grammar_roles.lookup(kind)` for `.precedence` and `.assoc` — two host row
reads per peek, on the Pratt hot path. The third reach was the deleted
`Parser.infixBinOp` helper, which exposed the relation identity the
production parser still needed. The hot read is now one C ABI call to
`idol_parser_infix_prec`, and the helper is gone.

Parser.id relation `infix_prec: i64 = (kind: i64)` packs (relation
ordinal | left precedence | right precedence) into one i64. The Relation
ordinal is the position in `lib/compiler/token.id` `_relationorder`,
which `src/grammar_role_table.zig` and `src/ast.zig`'s `BinOp` alias.
The owner now projects that ordinal in a compact per-slot row, so parser.id
does not reconstruct it from a name string. The right binding
power follows Pratt convention: left-associative tightens the right
side to `left + 1`, right-associative loosens it to `left - 1`,
non-associative keeps it at `left`.

Controls and evidence:

```text
throwaway 114-slot C probe against token.grammarrole._roleinfix /
  token.grammarrole.roleprecedence / token.grammarrole.roleassoc /
  token.grammarrole.relation()
→ relations=24 ordered add..pipeline; precedence+assoc match owner
IDOL=/home/clp/src/idol/zig-out/bin/idol sh tools/node/dev/parser/artifact --write
→ generated bytes match tracked artifact; failed=0; PASS
sh gate/gap-145-consumer.sh
→ PASS (109 checks, was 100), with planted old-host/new-Idol detector
focused production parser test
→ `parse: production infix_prec decision executes the Idol relation`
   iterates every role row and asserts the packed triple matches
   the generated row's `.precedence`, `.assoc`, and `.relation` facts
focused production pretty-printer test
→ `pretty: every BinOp round-trips through the grammar`
   iterates every BinOp enum value and asserts the packed op ordinal
   reproduces the BinOp via the unpacked `idol_parser_infix_prec`
fresh ZIG_LOCAL_CACHE_DIR zig build unit-test --summary all
→ 9/9 steps succeeded; 1835 pass, 62 skip, 0 fail
```

This is seven bounded recognizers, not a complete parser stage or
Compiler B. The hot Pratt step now crosses one ABI call; the host row
lookup and the `infixBinOp` helper are deleted. The remaining parser
recognition is still host-executed.

## Eighth executed parser decision: primitive-descriptor `is_primitive_descriptor_kind` (2026-08-31)

10 type-atom / typed-binding / type-table-key / record-field / layout-arg
sites (`parse_type_primary`, `parse_attributed_decl`, `parse_table_literal`,
`parse_record_field_list`, `parse_offside_pack`, `parse_pack_decls`,
`layout_arg_can_start_type`, `parse_suffixed_expr`) asked
`grammar_roles.isDescriptor(kind)` on every probe — one host row-cache
lookup per probe, with no way for the parser to agree with the generated
role row except by re-reading the same cached `descriptor` field.

The producer's `roledescriptor(kind)` row already published the fact. Parser.id
now carries `is_primitive_descriptor_kind: bool = (k: i64)` that reads it
directly, exposed through the C ABI as
`idol_parser_is_primitive_descriptor_kind`; `build.zig` enrolls the
`-Dis_primitive_descriptor_kind=idol_parser_is_primitive_descriptor_kind`
rename. The 10 host `grammar_roles.isDescriptor` call sites are gone.

Distinct from the existing `is_descriptor_kind` (the broader
`roledescriptor OR is_type_kind` predicate used by `colon_is_method_call_lx`
and the `header_signal_lx` path): the two predicates answer different
questions and the ABI symbols are named separately so a future host that
confuses them surfaces as a link error rather than a semantic drift.

`gate/gap-145-consumer.sh` section 2f'''' forbids the host row lookup,
requires the parser.id relation, the projection.c symbol, and the build.zig
rename, with two damage-control probes; the gate now passes 115 checks
(was 109). The parser artifact regenerates byte-identically. This is
eight bounded recognizers, not a complete parser stage or Compiler B.

## Ninth and tenth executed parser decisions: literal-kind `is_literal_kind` and quoted-payload `is_quoted_kind` (2026-08-31)

10 Pratt / record / match-arm / descriptor / table / pretty sites across
`parse_stmt`, `parse_match_arm`, `parse_descriptor_slot`,
`parse_table_literal`, and the deduped `int_lit` case in
`parse_table_literal`'s unboxed-key sugar asked
`grammar_roles.isLiteralKind(kind)` or
`grammar_roles.isQuotedKind(kind)` on every probe — two host row-cache
lookups for the predicate pair, with no way for the parser to agree with
the generated role row except by re-reading the same cached `literal_kind`
and `quoted` fields. Parser.id now carries two new relations:

- `is_literal_kind: bool = (kind: i64)`, reading the producer's
  `literal(): str` row (a dense 0/1 char slice with `1` set for `int_lit`,
  `float_lit`, and the four text/bytes/compat slots).
- `is_quoted_kind: bool = (kind: i64)`, reading the producer's
  `quoted(): str` row (the same `kind[8]` shape, with `1` set for the
  four text/bytes/compat slots).

Both predicates cross the C ABI as `idol_parser_is_literal_kind` and
`idol_parser_is_quoted_kind`; `build.zig` enrolls both
`-Dis_literal_kind=idol_parser_is_literal_kind` and
`-Dis_quoted_kind=idol_parser_is_quoted_kind` renames. The
table-sugar `else if (tok.kind == .int_lit or isQuotedKind(tok.kind))`
arm collapses to `(is_literal_kind(...) or is_quoted_kind(...))` so the
literal/quote question lives in one place rather than split between a
kind-class check and a host row read.

The producer added the two zero-arg bit-slice relations in
`lib/compiler/token.id` (next to `lead()` / `prefix()` / `demand()`) and
emits them in the `idbody()` emitter, so both
`lib/token/grammarrole.id` and `src/grammar_role_table.zig` regenerate
with the new bit strings; the host `roleliteral` / `rolequoted`
predicate pair and the generated row fields (`.literal_kind`, `.quoted`)
agree with the parser-side facts at the source level — there is one
producer of the predicate, one producer of the row emitter, and one
parser ABI call per kind.

`gate/gap-145-consumer.sh` sections 2f''''' and 2f'''''' forbid the host
row reads, require the parser.id relations, the projection.c symbols,
and the build.zig renames; each adds two damage-control probes; the gate
now passes 131 checks (was 115). Two production parser tests
(`parse: production is_literal_kind decision executes the Idol relation`
and `parse: production is_quoted_kind decision executes the Idol relation`)
iterate every role row and assert the new ABI calls reproduce the
generated `.literal_kind` and `.quoted` row fields for every kind. The
parser artifact regenerates byte-identically. Fresh-cache complete unit
run passes 1839, skips 62, fails 0 (was 1837 / 62 / 0). The grammar-
projection gate and the full admission gate also pass.

This is the ninth and tenth bounded recognizers, not a complete parser
stage or Compiler B. The host `grammar_roles.isLiteralKind` /
`isQuotedKind` facades and the `grammar_roles.lookup(...).literal_kind
/ .quoted` reads are gone from `src/parser.zig`; the remaining test in
`src/token_view.zig` still exercises the host facade so the parser
artifact and the gate can catch a future regression that re-introduces a
private copy of the predicate.

## Eleventh through thirteenth executed parser decisions: unary, glue, update (2026-08-31)

Two Pratt prefix sites and both compound-update paths were the final
executable `grammar_roles.*Relation` calls in `src/parser.zig`. They now call
`idol_parser_unary`, `idol_parser_glue`, and `idol_parser_update`, projected
from private parser.id bridges `_unary`, `_glue`, and `_update`. These source
faces are explicitly bounded by the current public-vocabulary freeze; they are
deleted when a canonical public relation surface is graph-proven or when the
complete parser consumes the producer pack without this C seam.

`lib/compiler/token.id` remains the sole operation-order producer. It emits
compact 114-byte `relation()` and `unary()` rows (`.` absent; `A` ordinal zero),
and parser.id reads each slot in O(1). This replaces and deletes the generated
relation/unary name rows, `_slotword` scan, parser-side 24-name and five-name
order copies, and both ordinal name scans. After the production calls moved,
the four `src/grammar_roles.zig` relation facade functions had only self-test
consumers; they too are deleted and the tests inspect generated row facts.

Executed evidence:

```text
IDOL=./zig-out/bin/idol sh tools/node/dev/parser/artifact --write
IDOL=./zig-out/bin/idol sh tools/node/dev/parser/artifact
→ generated bytes match tracked artifact; relation-slots=114; failed=0; PASS twice
sh gate/gap-145-consumer.sh  # run twice
→ PASS (168 checks), with planted host-call and copied-order controls
IDOL=./zig-out/bin/idol sh gate/grammar-projection.sh
→ malformed-producer control refused; both tracked projections byte-identical; PASS
sh gate/admission-all.sh --base HEAD
→ ADMITTED (source quarantine, vocabulary freeze, layering, directive, frontier)
./tools/node/dev/idol-lock -- zig build --summary all
→ Build Summary: 5/5 steps succeeded
```

The direct `gate/idiom.id` execution path refuses at DNB004 on this Linux ARM64
host; its source checks clean. No generated-C run is represented as direct-gate
proof. These are thirteen bounded parser decisions in total, not complete
parser SHC or Compiler B.

## O3 first direct parser consumer (2026-08-30, base `61e0e951` + diff)

Callable-header classification is now the first production parser decision to
leave Zig. `src/parser.zig` projects each producer token once into a compact
physical fact (owner ordinal, line, column), then `lib/compiler/parser.id`
`header_signal_lx` answers typed/call/offside/condition/empty/nested header cases through
the same `token.grammarrole.*` owner. The 209-line Zig `headerSignal` and
`viewColonIsMethodCall` bodies are deleted, and `gate/gap-145-consumer.sh`
refuses their return while requiring the executed pack call.

At that revision this was movement, not O3 closure. `src/lexer_dispatch.zig` still materialized
the owner-generated `TokenKind` from the producer record (`kindFromRecord`),
and the compact fact currently obtains the identical ordinal with
`@backingInt(token.kind)`. `Lexer.duo_tokens`, `useDuoTokens`, and almost every
other parser switch still consume that physical bridge. The transfer proves one
parser role decision can bypass Zig semantic reconstruction; it does not prove
the enum bridge or token cursor deletable yet. Exact correctness, artifact,
latency, RSS, and footprint evidence is recorded in GAP-134's
"First executed parser-recognition boundary" section.

## O3 second direct parser consumer (2026-08-30, base `db7dada0` + diff)

`returnStartsValue` joins `header_signal_lx` as a producer-pack consumer.
`lib/compiler/parser.id` `return_starts_value_lx` returns one of three verdicts
over the same immutable `[]i64` pack:

- `0` — no value (next kind is a line-terminator, or `.id` mode saw a
  cross-line next token, or `start` is past the pack)
- `1` — a value expression follows
- `2` — the next kind cannot begin an expression in `.id` mode; the host
  emits its own diagnostic from the live token

The function reads `kind[8]` and `line[28]` from `fact[start + 1]` and asks
`token.grammarrole.rolebeginexpr(kind)` only when `idol_mode` is true. The
host keeps one diagnostic (`term.locErr`) for the 2 path so the message can
name the offending spelling; the silent 0 and 1 paths map directly.

`gate/gap-145-consumer.sh` section 2d refuses reintroduction: the host
`.kw_end, .kw_else, .kw_elseif, .kw_until, .kw_catch, .eof, .semi => return false`
switch is forbidden, the Idol call `idol_parser_return_starts_value(`
is required, the parser.id function is required to exist, the projected
`src/parser/projection.c` must carry the symbol, and `build.zig` must rename
the C ABI to the `idol_parser_return_starts_value` Zig name. Two planted
detectors (one executor, one host switch) confirm the gate cannot pass while
the decision lives outside the producer pack.

Measured at HEAD on this aarch64 Linux host, freshly built `zig-out/bin/idol`:

```
sh gate/gap-145-consumer.sh                PASS (37 check(s))
IDOL=./zig-out/bin/idol sh tools/node/dev/parser/artifact
  parser artifact: generated bytes match tracked artifact
  parser artifact: typed=1 call=0 offside=1 condition=0 empty=1 nested=0
                  cursor-pack-cases=8 statement-cases=2 failed=0
  parser artifact: PASS
zig build --summary all                    5/5
```

At that revision the remaining `.quoted` blind arms (ceiling 13), the
`kindFromRecord` / `Lexer.duo_tokens` / `useDuoTokens` bridge, the generated
`TokenKind` enum, and the parser switches that consume `tok.kind` directly
remain load-bearing. `startsMatchArm` subsequently crossed the same boundary;
the next deletion target is the reconstructed token bridge or an exact
owner-projected quote face.

## O3 third direct parser consumer (2026-08-30, base `8c3ae345` + diff)

`lib/compiler/parser.id` `_clause` now owns match-arm classification. It returns
0 (not an arm), 1 (ordinary pattern), 2 (contextual `case`), or 3 (`else`) after
observing the owner `pattern` fact, same-line boundary, balanced delimiter depth,
and `then` / `do` / `=>` separator. `src/parser.zig` consumes the verdict in
`startsMatchArm` and `parse_match_arm`; it no longer compares text or scans token
kinds to decide an arm.

`case` remains contextual rather than becoming a globally reserved token. To
cross that physical observation without host reconstruction, each token now
contributes two nonnegative facts: kind/line/column metadata and a raw short-word
fact containing a length marker plus at most six producer bytes. Length marker 7
means longer than six, so no long identifier aliases the exact `case` word.

The tracked parser artifact derives those facts from the generated lexer and
executes eight clause cases. Section 2e of `gate/gap-145-consumer.sh` refuses the
former host text, pattern-role, and separator/depth decisions and requires the
projected `_clause` symbol, C rename, physical lexeme fact, and production call.
The executable gate reports 46 checks. The generated projection is 166571 bytes;
the complete Zig aggregate passes 1835, skips 62, and fails 0 (1897 total).

The aggregate also exposed two invalid controls in the preceding return slice:
they claimed `function` and `#` could not begin expressions, while the executable
owner admits both. The controls now use `)` and `+`, which the same owner rejects;
the production return relation did not change.

NOT closure. Generated `TokenKind`, `Lexer.duo_tokens` / `duo_index`,
remaining direct `tok.kind` consumers, and 13 quote-blind arms
are still load-bearing. No complete parser stage, Compiler B, or FTCFTW result
is claimed.

## O3 producer-record enum conversion deletion (2026-08-30, base `0182e1ee` + diff)

`src/lexer_dispatch.zig` no longer calls `kindFromRecord` or imports
`grammar_role_table` at runtime to translate a producer ordinal through
`rows[ordinal].kind`. `tokenFromRecord` now bounds the raw value to `u8`, checks
it against the field values of the owner-generated sparse `TokenKind` enum, and
performs one `@fromBackingInt` conversion. The unpublished physical slot 3,
negative values, and values above `u8` fail as `InvalidTokenKind`; no invalid enum
value reaches the parser.

This deletes a runtime identity projection rather than renaming it. The enum
itself remains generated from `lib/compiler/token.id`, and the gate refuses both
the old function and the production grammar-row import. Focused record tests and
all 27 lexer-dispatch tests pass; the GAP-145 gate reports 58 checks. The
constant `TokenizeAuthority` enum/query is also deleted without a replacement:
production has one generated route, while the host scanner remains only in the
differential function. The complete Zig aggregate passes 1834, skips 62, and
fails 0 (1896 total).

The static taint boundary passes on this Linux host: cache quotient calls the
producer route and contains no host scan; `tokenizeHost` still scans and is read
only by `differential`. The dynamic interposition half of `gate/taint.sh` is not
claimed here because that gate explicitly requires Darwin.

Same pinned-Zig comparison against clean `0182e1ee`, workload
`idol check lib/compiler/parser.id`, three processes per sample and 21 balanced
rounds: base median 232.835 ms, candidate 236.724 ms, candidate lower in 9/21;
ranges overlap. A final nine-round clean-artifact repeat measured 290.050 vs
301.183 ms with candidate lower in 5/9; those ranges overlap too. No speed or
regression claim is made. `tokenFromRecord` plus the deleted helper shrink by 328
machine-code bytes, while the whole debug binary grows 1688 bytes (0.00586%);
both physical facts are retained.

At that revision decode still materialized `[]lexer.Token`; `Lexer.duo_tokens`
and `useDuoTokens` still installed that slice, parser switches still consumed the
generated enum, and 13 quote-blind arms remain. No complete parser stage,
Compiler B, or FTCFTW result is claimed.

## Dead explicit pack API deletion (2026-08-30, base `0a430ecb` + diff)

`lexer_dispatch.ProductionPack`, `tokenizePack`, and `token_view.fromPack` had no
production caller and only tested one another. They are deleted without aliases;
the sole production route remains `lexer_dispatch.route` into `Lexer.duo_tokens`,
and direct tests may still observe owned tokens through `tokenize` / `fromTokens`.
Seven surviving token-view tests pass. Section 2g refuses all three names and
raises the GAP-145 gate to 58 checks. The complete aggregate passes 1833, skips
62, and fails 0 (1895 total).

At that revision the live token-slice installer and generated enum remained. This
deletes a closed test-only bridge loop; it is not parser SHC, Compiler B, or an
FTCFTW result.

## Live token installer deletion (2026-08-30, base `44f007b0` + diff)

`Lexer.useDuoTokens` and its `TokenStreamError` type are deleted without an
installer alias. `lexer_dispatch.route` already receives a nonempty,
EOF-terminated, span/location/kind-validated slice from `decodeRecords`; it now
installs that slice exactly once, seats past a leading shebang, clears peeked
state, and calls the existing comment-hint observer. The old empty/missing-EOF
method test is removed because those malformed records are refused upstream.

All 27 lexer-dispatch tests pass, including shebang hiding, comments/trivia,
source spans, allocation failure, temporary-copy release, full field
differential, family propagation, and malformed record refusal. Focused parser
tests prove manual identity damage and out-of-range parser facts still behave.
Section 2h refuses the installer/error type and requires direct route
installation plus hint harvesting; the GAP gate reports 63 checks. The complete
aggregate passes 1832, skips 62, and fails 0 (1894 total). The same-toolchain
debug binary shrinks 1368 bytes; no speed claim is made.

NOT closure. `Lexer.duo_tokens`, `duo_index`, `duo_next`, `duoStreamIndex`, and
the generated `TokenKind` representation remain live. This deletes the mutable
installer path, not the parser cursor representation, parser SHC, Compiler B, or
an FTCFTW result.


## Byte-face observer closure (2026-08-23, lane/gap145zero)

Base `9be8767e`, pristine `git archive` mirror as the counterfactual. Every
number below is the output of the command beside it.

### The consolidation

**The P0 in `GAP-207` and the O5 quote collapse are the same defect at two
depths, and the O5 census could not see the deeper one.**

`s = 'abc' ; print(s .. "Z")` printed `4308223088Z` at exit 0 with no
diagnostic on the pristine mirror of the base — a fresh ASLR'd address every
run. The mechanism: `exprIsStr` correctly answers "not text" for the byte
face, `concatOperandOk` read that as permission to ask `exprIsIntegral`, and
that predicate's `.name` tail admits any slot which is not f64 / str / ptr —
a NEGATIVE test sitting directly beneath a doc comment explaining that the
predicate is positive on purpose. `planConcat` then chose `%lld` for a
`const char*`.

So a face added anywhere in the compiler became a wrong answer HERE by
default. That is why the two are one subject: GAP-145 adds faces, and this
predicate answered for every face nobody had told it about.

### What is closed

`LowerCtx.byteseq_slots` is the third arm `GAP-207` asked for. It records the
byte face at the same store that records the text face; membership is the
descriptor the semantic graph already published for the quote
(`publishSourceQuote` -> `types.quotedLiteralType`), never `expr.quoted.quote`
off the AST; `types.isQuotedByteSequence` owns the shape so no consumer
restates `elem == .u8 and size == null`. `concatOperandOk` and the
`exprIsIntegral` `.name` tail now ask the positive question.

Three collapse sites are closed with it:

- `src/comptime.zig` folded a byte sequence into `.string`, a `Value` kingdom
  with no member for it. It declines now, which every caller of `eval`
  already handles as "fail closed, leave the body lowered".
- `src/codegen.zig` `known_lua_type_name_without_eval` answered the Lua
  `type()` name `"string"` for element descriptor `byte`. It declines, which
  is what its own `.name` arm two lines below already did.
- `src/dnir_lower.zig` `planConcat` folded the bytes of every quoted part
  into the printf format string without asking which face they had.

### The census instrument was structurally blind, and this is what it missed

`planConcat` is written `if (p.* == .quoted)` — a TAG TEST. The O5 number
that has been carried in this gap since it was first taken greps for a SWITCH
ARM, `^\s*(\.[a-z_, .]*)?\.quoted =>`, and cannot match a tag test at all.

    grep -h '^[[:space:]]*\(\.[a-z_, .]*\)\?\.quoted =>' src/*.zig | wc -l
    grep -h '== \.quoted\b' src/*.zig | wc -l

The second form is the larger surface by a factor of two and a half, it had
never been counted, and one of its members carried a wrong answer. One fact
had two answers decided only by whether the operand had a name:

    print('abc' .. "Z")            abcZ      folded in planConcat
    x = 'abc' .. "Z" ; print(x)    refused   lowerBinop

`gate/gap-145-consumer.sh` now counts it. It counts the SURFACE and says
plainly that it does not judge each member: most of those sites read a
literal as a NAME — a concept name, an `__emit` template, a field spelling —
where no text/byte law is observable, and no grep decides that line-locally.
A ceiling on a surface that was invisible is worth more than a blind count
that is wrong.

### The same defect at the egress relation, found by following the hint

`src/parser.zig` carries a hint on a real diagnostic telling people that "a
payload that is all braces and no holes — JSON, a C body, an awk program —
belongs in the byte face `'…'`". Following the compiler's own advice reaches
`stdout:write`, and `lowerWrite` ends in the same `else .i64` that
`exprIsIntegral` did:

    stdout:write('{"a":1}')            {"a":1}
    j = '{"a":1}' ; stdout:write(j)    4371891320     exit 0, no diagnostic

Both spellings reproduce on the pristine base. The literal one answered only
because it lowers to a `.str` value that `holds` recognises; the bound one
fell through. So the recommended idiom was the broken one, and the two
spellings of one fact disagreed again.

Here the answer is not a refusal. **Writing bytes to a byte stream needs no
textual law**, so the byte face belongs on the `.str` side of that selection —
which is also what makes the literal spelling right. `print` is deliberately
NOT changed with it: it is line-oriented text egress and already refuses a
byte sequence at the native-scalar precheck for both spellings.

### The gate, and why the three existing instruments could not have found this

`GAP-207` ran all three and recorded all three CLEAN while the program
printed a pointer: `zig build unit-test` unchanged, a compile-status sweep
over every example with zero diff, and an output differential over every
example that compiles with zero diff. The third is the one worth dwelling
on — it was a real output differential and it saw nothing, because THE CORPUS
CONTAINS NO PROGRAM OF THAT SHAPE. An output differential can only compare
programs that exist.

`gate/byte/face.sh` therefore carries its own subjects. Stdout is
compared BYTE FOR BYTE; a refusal must be non-zero AND silent on stdout AND
have said something on stderr, because a segfault satisfies the first two;
its comparator is positive-controlled on the recorded transcripts out of
`GAP-207` rather than on a defect the gate invented, and it aborts before
reporting anything if it fails to convict all six. It is on `zig build test`
and `zig build byteface` — O7 is the class where the control exists and
nothing runs it.

    sh gate/byte/face.sh                       PASS
    BYTEFACE_IDOL=<pristine 9be8767e> sh ...   FAIL — the concat pointer, the
                                               folded literal, and the written
                                               pointer, by name
    BYTEFACE_IDOL=/nonexistent sh ...          exit 2, nothing measured
    BYTEFACE_IDOL=<silent stub> sh ...         FAIL, every subject

Byte-for-byte means FILES, not `$(...)`. The first version of this gate held
both sides in shell variables, and POSIX command substitution strips every
trailing newline — so `abcZ`, `abcZ\n` and `abcZ\n\n` were one value to a
comparator whose own header claimed byte-for-byte. That is not academic here:
`print` ends a line and `stdout:write` ends nothing, which is a distinction
`lowerStreamWrite` exists to preserve and which was measured wrong once
already. Every subject now names its ending, the streams are compared with
`cmp`, and one of the comparator's own controls is an extra trailing newline
that it must refuse.

### Ordered-work item 1: the obvious `read` substitution is a trap, measured

`scripts/treesitter_emit.id` `slurp` is `io.open` / `f:read("a")` / `f:close()`,
the Lua handle face, which the direct backend refuses at
`method-unresolved:read`. The subject-first face `path:read()` IS lowered
(`idol_io_read_path`), and swapping to it does move the generator past that
refusal to the next one, `method-unresolved:split`.

**It was tried in this lane and REVERTED, because the substitution is not
behaviour-preserving and the measurement that said it was, was itself reading
undefined memory.** `src/idol_io_runtime.zig` `idol_io_read_path` returns NULL
when `fopen` fails, where `io.open`'s nil branch left `body` as the empty text.
The first probe asked `c:len()` on the missing-file result and got 0, which
looked like agreement; it was `strlen` on a null pointer. Asked again with more
of the value observed, the same program printed NOTHING AT ALL — not the
length, not the following lines — at exit 0.

    "<a path that does not exist>":read()   ->  NULL, not ""

`slurp` feeds `rulesonly`, the check-mode comparison, and `onfile:len()`, all of
which would receive that null. Trading a named refusal for undefined behaviour
is a worse position than the blocker, so the handle face stays until the byte
face has a defined absent-file answer.

The blocker chain is therefore longer than ordered item 1 states: `read` is a
compat-face spelling standing in front of `split`, and `split` returns a list,
which this file's own header records does not survive a function boundary on
this compilation path. Neither is a line; both are capability lanes.

### Ordered-work item 1, closed at the producer (2026-08-25, generator lane)

The trap above is repaired where it was made, not routed around. Decision (b),
FAIL-CLOSED: `src/idol_io_runtime.zig` `idol_io_read_path` no longer answers
NULL on any of its five failure legs (`fopen`, two `fseek`s, `ftell`, `malloc`,
short `fread`). Each leg now refuses with an identity-first diagnostic on
stderr — `read-refused:absent:<path>` for a path that will not open,
`read-refused:io:<path>` / `read-refused:oom:<path>` for the rest — and exits
1, the `strFatal` convention from `idol_str_runtime.zig` minus the abort,
because a refusal is an answered outcome, not a trap. An absent file is not a
value (`law.id.one`: downstream semantic use fails closed when the required
facts are absent; the `stdin:line()` end-of-input precedent in the same file).
The structured absent|present outcome family stays OPEN under
GAP-154/GAP-118 — this increment does not admit it; the fail-closed sentence
is now also in `docs/spec/host.md`.

With NULL gone, the reverted substitution is safe and is re-applied:
`scripts/treesitter_emit.id` `slurp` is now `path:read()` behind the
already-executable `io.open`/`f:close()` existence probe (the `lib/fs.id`
`exists` face), which preserves absent → `""` for the three consumers that
rely on it — the bootstrap slurp of a not-yet-emitted output, the
`TSEMIT_RESIDUE` seed, and check-mode. `scripts/assert_no_ansi_reports.id`
took the bare swap (`tmp:read()`, no guard): its subject is a capture file the
runner wrote one line earlier, so a refusal there is a broken gate named as
one. The other `io.open` read faces measured for the same swap were LEFT, each
for a stated reliance: `lib/io/util.id` `read` contracts absent →
`("", "file not found: …")`; `lib/fs.id` `read` contracts absent → `""` and
its subject is typed `any` where `lowerSubjectRead` is `exprIsStr`-guarded;
`lib/fs.id` `lines` and `lib/mproc.id` `pid` are line-at-a-time faces
(`f:read()` / `f:read("*l")`), not the whole-file byte face, and `mproc`'s
absent `/proc/self/stat` is its living macOS fallback; `lib/script.id` is the
contract-documented bridge.

`gate/readpath.sh` pins both directions: §1 an absent path refuses nonzero,
stdout-silent, naming `read-refused:absent:<path>` on stderr; §2 the positive
control, a present file answers its exact bytes (trailing newline deliberately
absent, `cmp` against files). Off a direct-native host it reports NOT MEASURED
and exits 2 through `gate/realization/direct.sh`, the one producer of that
fact.

Where the generator stops, measured on THIS lane's host (x86_64-linux):

    ./zig-out/bin/idol run scripts/treesitter_emit.id
    -> error: DNB004 ... no native machine realization for target 'native' on this host

DNB004 fires before lowering, so `method-unresolved:*` is unreachable from
this host entirely — the `read` → `split` movement recorded above is an
aarch64-macos measurement and stays one; `gate/readpath.sh` carries the
runtime pin to the host that can execute it. `idol check` passes on both
edited scripts here, and `zig build` cross-compiles the repaired runtime
object, so the seam this host can verify is verified. Next face on the
generator, unchanged from the section above: `method-unresolved:split`, and
behind it the list-across-function-boundary lane.

### Measured movement

Corpus movement is stated as the COMMAND and its verdict, not as a corpus
size — the sizes decay and the runners do not. Both sweeps run the pristine
mirror and this tree over the SAME enumeration and diff the results:

    for f in $(git ls-files '*.id'); do idol compile "$f" -o "$out"; echo "$?"; done
    -> identical exit status on every subject, both compilers

    for f in <the subjects the first sweep compiled>; do idol run "$f"; done
    -> identical stdout digest on every subject, both compilers
       (examples/shc/cwd.id prints the cwd and agrees once the cwd is fixed)

    sh gate/byte/face.sh              PASS, subject count in its own output
    sh gate/gap-145-consumer.sh       PASS, arm and tag-test counts in its own
                                      output, each against a ceiling the gate
                                      carries
    zig build unit-test               exit 0
    zig build --summary all           exit 0

`zig build test` exits 1 on `agent-smoke`, and it does so **on a pristine
`git archive` mirror of `origin/main` as well** — same step, same transitive
failure — so it is not this lane's. Run it on both to see it.

`gate/gap-145-consumer.sh` was **RED on the pristine base**: its blind-arm
count exceeded its own ceiling before this lane touched anything, because the
gate is reachable only through `gate/all.sh`, which is on no build step and no
hook, so nothing had run it since the arm was added. The ceiling falls here;
the gate prints both numbers on every run.

### What this section does NOT claim

`src/dnir_lower.zig` `.quoted => |s| .{ .str = s.val }` still lowers a byte
literal through the `.str` member. That is the PHYSICAL carrier — `dnir.Value`
has one member for "a pointer to a run of bytes" and both faces are realized
through it — and it is left as a realization fact, not repaired as a semantic
one, because every consumer that can answer differently for the two faces now
asks `exprIsByteSequence` rather than the value tag. It is named here so the
next reader does not have to rediscover that the decision was deliberate.

Measured and NOT repaired, because the answers are correct rather than
plausible: `s:len()` on a byte sequence answers 3, `s:byte(1)` answers 97,
and `'abc' == "abc"` answers true while `'abc' == "abd"` answers false. The
cross-face equality is a design question for the element-descriptor model,
not a wrong answer.

Embedded NUL in a byte sequence is NOT addressed here and is the subject of
`origin/wip/nulrep-20260823`. Physical realization through a NUL-terminated
`const char*` is exactly where that lane's work and this one meet.

## Measured observer census (2026-08-23, lane/gap145-20260823)

**Two bases.** Everything below was first measured at `ce03c7eb` and then
re-measured on the pristine tree of `c77dea60` after main advanced 48 commits
(including `codex/grammar-main-zero` and `codex/nul-quote-extent-20260823`,
which touch this area). Every number is the same on both bases except one,
which is recorded in O5 and is an improvement. Where a number is base-sensitive
the base is named beside it.

Derived from the code, not from the category list above. It found MORE
observers than the four categories predict: two of the controls that were
supposed to police this gap cannot run, and one duplicate host lexical
authority had escaped every previous count because it has zero consumers and
therefore appears in no call graph.

Every number below is the output of the command beside it. No number here is
carried forward without its command.

### O1 — Tree-sitter is a second grammar authority, and its generator no longer runs

    ./zig-out/bin/idol run scripts/treesitter_emit.id
    → error: direct backend: DNB001 application: unknown missing:
      method-unresolved:read consumer: native realization producer: dnir lower
    → EXIT=1

`zig build treesitter-projection` is that command with `TSEMIT_CHECK=1`, so the
byte-identity control on the second grammar authority **cannot execute at all**.
Its sibling cannot either on this host:

    ./zig-out/bin/idol run scripts/tree_sitter_coverage.id
    → error: DNB004 ... no native machine realization for target 'native' on this host

So **both** tree-sitter build steps are dead on this x86_64-linux host for the
same environment reason, and neither `treesitter-projection` nor
`tree-sitter-coverage` is a dependency of `test` or of any aggregate step
anyway (`grep -n 'ts_projection_step|ts_coverage_step' build.zig` shows those
steps are declared, but no aggregate step depends on them).

With the control dead, `ext/tree-sitter-idol/grammar.js` has been hand-authored
in exactly the region GAP-145 owns. The generator's `tail()`
(`scripts/treesitter_emit.id:1494-1505`) emits only the Lua comment rule:

    comment: $ => token(choice(seq('--', ...)))

The tracked file instead carries the GAP-145 comment identities
(`ext/tree-sitter-idol/grammar.js:1232-1242`):

    comment: $ => choice($.hash_comment, $.compat_comment, $.compat_long_comment)
    hash_comment / compat_comment / compat_long_comment

and the GAP-145 literal identities (`grammar.js:1127-1136`) `text` / `bytes` /
`long_text`, which `tokens()` (`scripts/treesitter_emit.id:1328-1392`) also does
not emit — it emits only `string` over `double_quoted_string` /
`single_quoted_string` / `long_string`. The banner at `grammar.js:20` claims
`0 lines` of authored residue. That claim is false and unmeasured, because the
measurement is the step that cannot run.

`ext/tree-sitter-idol/src/grammar.json` is absent from `gate/generated.manifest`
entirely, so nothing relates it to `grammar.js`.

The operator table is the sharpest case. `scripts/treesitter_emit.id:1408-1421`
carries four parallel authored rows (`spell` / `level` / `spec` / `assoc`) and
cross-checks `level` against `spec` — a **hand-copied restatement of
`docs/spec/grammar.md` §2**, a file whose own first paragraph says it "is not a
grammar authority and must not be used to hand-build parser tables." The
authority is `lib/compiler/token.id` `roleprecedence` / `roleassoc`. Compared
against it by producer ordinal:

    ops compared: 24     pairs compared: 276     divergent pairs: 6
    adjacent pairs the in-file check inspects: 23     divergent: 0

    DIVERGENCE  |   (ts  7 / auth  7)  vs  ~   (ts  7 / auth  9)
    DIVERGENCE  |   (ts  7 / auth  7)  vs  cat (ts  6 / auth 16)
    DIVERGENCE  &   (ts  8 / auth 11)  vs  cat (ts  6 / auth 16)
    DIVERGENCE  ~   (ts  7 / auth  9)  vs  cat (ts  6 / auth 16)
    DIVERGENCE  <<  (ts  9 / auth 13)  vs  cat (ts  6 / auth 16)
    DIVERGENCE  >>  (ts  9 / auth 13)  vs  cat (ts  6 / auth 16)

The in-file check compares only ADJACENT rows, so it is structurally incapable
of seeing any of the six: `cat` and `|` are fourteen rows apart. Consequence in
the editor grammar: `a | b .. c` groups as `(a | b) .. c` under tree-sitter and
as `a | (b .. c)` under the one owner.

Associativity diverges on 9 of 24 rows: `assoc` is `left` for everything but
`^`, while the owner gives `roleassoc(kindconcat) = 2` (right) and
`roleassoc = 3` (nonassoc) for the eight comparison/`in` identities.

Second-authority surface: `ext/tree-sitter-idol/grammar.js` (1261 lines),
`ext/tree-sitter-idol/src/grammar.json`, `ext/tree-sitter-idol/queries/{injections,locals,tags}.scm`,
`scripts/treesitter_emit.id` (1748 lines), `scripts/tree_sitter_coverage.id`.

### O2 — Duplicate host lexical classifier with zero consumers and a divergent rule

    git grep -n 'lexical_identity' -- .
    gate/layers.manifest:36:TOKEN    lexical_identity.zig
    src/tests.zig:10:    _ = @import("lexical_identity.zig");

    git grep -n 'hashOperatorAllowed|backtickAllowed|classifyTrivia|classifyQuote|isCanonicalLineComment|LiteralKind|TriviaClass' \
      -- src/*.zig build.zig gate/ tools/ | grep -v '^src/lexical_identity.zig'
    → no line names any symbol this module exports

`src/lexical_identity.zig` (111 lines) declares itself "canonical lexical token
identities (production source of truth)" and is imported only by `src/tests.zig`
so that its own tests run. It is a **second producer** of `law.fact.producer.one`
facts the Idol producer already owns, and it does not agree with it.
`lib/compiler/lexer.id:193-200`:

    classify_quote: i64 = (family: i64, quote: i64)
      if quote == 34 return 105
      if quote == 39
        if family == 1 return 106
        return 107
      107

`src/lexical_identity.zig:44-54` keys the same decision on a different fact set
(`facts.law` x `facts.provenance`, three-way `.idol/.lua/.unknown`), so
`.id` content that is `idol` law but non-canonical provenance answers
`compat_text` here and `bytes` at the producer. The divergence is unobservable
only because nothing reads it.

### O3 — host-authored `TokenKind` is deleted; generated bridge remains

Before this slice, `src/lexer.zig` authored the 113-member `TokenKind` enum and
its `spelling()` method while `lib/compiler/token.id` separately owned the same
114 physical producer slots and spellings in `kindzig`. The generated role
table imported the host enum, so the supposed owner projection could not stand
without the second identity kingdom.

Now `lib/compiler/token.id` projects `TokenKind = enum(u8)` with each explicit
producer ordinal, deliberately omitting unpublished slot 3, and projects
`spelling()` from the same role row. `src/grammar_role_table.zig` imports no
lexer. `src/lexer.zig` contains only:

```zig
pub const TokenKind = @import("grammar_role_table.zig").TokenKind;
```

The bridge is load-bearing rather than decorative. In a private tree, changing
the owner's `kw_return` identity to `kw_return_broken`, regenerating, and
compiling the consumer fails with:

```text
enum 'grammar_role_table.TokenKind' has no member named 'kw_return'
```

Measured on the integrated tree:

```text
grammar-role focused lexer/parser/view suite  -> 348/348 pass
tools/node/dev/grammar/emit --check           -> both projections byte-identical
sh tools/parity/grammar                       -> 113 identities / 114 slots agree
sh gate/gap-145-consumer.sh                   -> PASS (25 checks)
```

This closes the parallel HOST AUTHORSHIP, not O3 or GAP-145 as a whole. The
generated Zig enum remains a physical bootstrap projection, `Token.kind` still
carries it, and host parser switches still consume it. O3 closes only when the
production parser consumes the producer pack's token-role identity directly and
the generated enum/alias bridge can be deleted. No parser SHC or compiler B is
claimed here.

### O4 — Second keyword identity producer, generated from the host and stale

`src/token_semantic.zig` (54 keyword rows, `KeywordEntry.kind: lexer.TokenKind`)
→ `src/token_classify_gen.zig` → `lib/token/classify.id`,
`src/keyword_classify.c`, `tools/lsp/src/duo_keyword_classify.c`,
`tools/wasm/src/duo_keyword_classify.c`. The Idol producer already owns
keyword → ordinal in `lib/compiler/lexer.id` `lookup()` and
`lib/compiler/token.id` (`kindand = 4` …). `lib/token/classify.id` is written in
`--` comment syntax with uppercase mashed identifiers (`KINDAND`,
`CATEGORYDUOTYPE`) — noncanonical Idol projecting a lexical identity table —
and `gaps/GAP-134.md:259` already records it as stale by 745 lines.
`src/lexer.zig:199` still falls back to `token_semantic.entryForKind` for the
spelling of every keyword identity.

**Update (2026-08-25): the host table is deleted and the generator is
re-rooted.** `src/token_semantic.zig` no longer exists. `src/token_classify_gen.zig`
reads the `.keyword` rows of `src/grammar_role_table.zig` — the generated
projection of the one owner, `lib/compiler/token.id` — and emits the same two
artifacts. `lib/token/classify.id` and `src/keyword_classify.c` are KEPT, not
deleted, because they have live consumers that are not the compiler's lexer:
the SH-02/SH-03 `examples/native_differential/native_only/` programs consume
`classify.id`'s KIND constants and its `@c.export classify` (the
`directLinkInputs` classifier swap, `src/main.zig`), and `keyword_classify.c`
is `boot()`'s embedded link input for user programs importing
`duo_keyword_classify`. Regeneration delta, measured: `keyword_classify.c`
strcmp body byte-identical (only the banner's consumer line moved);
`classify.id` KIND/SORTEDTEXT/SORTEDID and all three classifier bodies
byte-identical, with the category taxonomy (`CATEGORYLUA`/`CATEGORYDUOTYPE`/
`CATEGORYDUOCONTEXTUAL`, `SORTEDCATEGORY`, `categoryof`) DROPPED — its only
producer was the deleted host table and it had zero consumers in the tree; the
owner's real versions of those facts are `compat_only` and `descriptor` on the
role rows. `gate/table-projection.sh` green (4 subjects byte-identical,
idempotent, canonical); `gate/lower/fallback.sh` green (classify.id still
0/CLEAN). The retired drift test in `src/grammar_roles.zig` ("host keyword
table cannot drift from the owner") is deleted as vacuous — the census pin
lives in `src/keyword_bridge.zig` (54 rows, spelling round-trip) and
`src/token_classify_gen.zig` (census = 54). Remaining O4 residue: the two
vendored tool projections `tools/lsp/src/duo_keyword_classify.c` and
`tools/wasm/src/duo_keyword_classify.c` (out of scope this increment), and the
`lib/compiler/lexer.id` `sourceentrypattern` row 5 still classifying
`lib/token/classify.id` as generated-canonical ingress — correct while the
artifact stays.

### O5 — Quote/text/byte collapse in AST consumers

**This is the one observer that moved between the two bases, and it moved the
right way.** At `ce03c7eb`: 34 arms, 11 observing the producer quote. At
`c77dea60`: 34 arms, **12** observing it — `src/dnir_lower.zig:7033` changed
from a bare `.quoted => true` to `graphTextConst(ctx.graph, expr)`, which reads
`sourceQuoteValue` off the semantic graph and answers from the published
descriptor instead of assuming text. One fewer reconstruction.

    grep -n '^\s*\(\.[a-z_, .]*\)\?\.quoted =>' src/*.zig | wc -l   → 34
    ... | grep -c '\.quote'                                          → 12 observe
                                                                    → 22 blind

The 12 that observe the producer identity at `c77dea60` are
`src/codegen.zig:2607,5355,10813`, `src/sema.zig:1494,1615,4401,13236`,
`src/dnir_lower.zig:544,7033`, `src/semantic_graph.zig:4320,4426`,
`src/pretty.zig:564`. Three of the 22 blind arms are places where byte-vs-text
law is observable rather than physical:

- `src/codegen.zig:22128` — `.quoted => "string"` answers the Lua `type()`
  name for a `.bytes` literal, which canonical law says is not text;
- `src/comptime.zig:371` — `.{ .string = lit.val }` collapses a byte sequence
  into the compile-time string kingdom;
- `src/dnir_lower.zig:9737` — `.{ .str = s.val }` carries a byte sequence as a
  `.str` DNIR constant.

The remainder are span, truthiness, or emit-side realization and carry no
text/byte claim.

### O6 — Second lexer inside the parser (text reconstruction) — CLOSED

`src/parser.zig` `findMatchingParen` (was :19-42) and `interpolationHoleEnd`
(was :5766-5794) scanned raw bytes and decided "this is inside a string" from
`'"'` / `'\''`. `law.md` §1 forbids reconstructing a fact from syntax once the
producer owns it; both ran over already-lexed literal payloads. **Deleted
2026-08-26** — see the dated section below for the replacement and its
enforcement.

### O7 — Two controls that cannot fail, and one wired to nothing

    sh tools/parity/grammar
    lib/token/grammarrole.id generated         ABSENT
    DRIFT: generated lib/token/grammarrole.id is absent or malformed
    exit=1

`tools/parity/grammar` searches for `ROLEIDENTITYCOUNT` / `ROLECOUNT` /
`KINDEOF` / `BEGINEXPR` with case-sensitive regexes. The generated projection
spells them `roleidentitycount` / `rolecount` / `kindeof` / `beginexpr`
(`lib/token/grammarrole.id:5-8`). The probe can never match, so the tool has
reported DRIFT on every run since the projection was lowercased, and the four
real drift checks that depend on `generated` are dead code.

    sh gate/gap-145-consumer.sh
    gap-145 consumer gate: PASS (8 check(s))
    exit=0

    git grep -n 'gap-145-consumer' -- .
    gaps/GAP-145.md:89   gate/gap-145-consumer.sh:2

Passing. `gate/all.sh` loops over every `gate/*.sh`, so it does run there — but
nothing runs `gate/all.sh`: it is absent from `build.zig` and from
`.githooks/pre-commit`, which runs `gate/admission-all.sh` and then
`idol check` over the `.id` gates. So the gate is reachable only by a person
typing its name.

### Census totals

| Observer | Count | Executable control today |
|---|---|---|
| O1 tree-sitter second authority | 6 divergent operator pairs / 276; 9 divergent assoc rows / 24; 2 hand-authored GAP-145 rule groups in a "generated" file | BOTH controls cannot run (DNB001) |
| O2 duplicate host lexical classifier | 1 module, 111 lines, 0 consumers, divergent rule | none |
| O3 host `TokenKind` | 218 occurrences / 30 files; 113 identities / 114 slots | `grammar-projection` byte-identity |
| O4 second keyword producer | 1 host table (54 rows) + 4 generated projections | none (projection stale by 745 lines) |
| O5 quote collapse | 22 quote-blind arms / 34 at `c77dea60` (23 at `ce03c7eb`); 3 observationally divergent | `gate/gap-145-consumer.sh` (run only by `gate/all.sh`) |
| O6 second lexer in parser | 2 sites | `gate/gap-145-consumer.sh` §2b: forbid + planted control (runs on `zig build test`) |
| O7 dead controls | 2 (`tools/parity/grammar` unmatched regex; `gate/gap-145-consumer.sh` unwired) | — |

### What this lane closed, and what it did not (2026-08-23, on `c77dea60`)

**O2 CLOSED by deletion.** `src/lexical_identity.zig` is gone. It was a second
producer of `law.fact.producer.one` facts with a rule that did not match the
one producer, and nothing consumed it. Behaviour-preserving evidence:
`gate/layering.sh` moves 118 units / 583 edges → 117 / 580 at `ce03c7eb` (one
unit, its three imports), forbidden-edge count unmoved at the pinned 75 on both
bases, and `zig build unit-test` green before and after. Ratcheted by
`gate/gap-145-consumer.sh`.

**O3 PARTIALLY CLOSED.** Source spelling had two host producers —
`src/lexer.zig` `spelling()`, a 60-arm switch, and its `else` fallthrough into
`src/token_semantic.zig`. The owner now carries `_kindspell`, one source
spelling per physical slot, and `spelling()` is an indexed read of the
generated projection. All 113 identities compared against the pristine
`c77dea60` host tables: **0 differences**. `zig build grammar-projection` is
the counterfactual — damaging `_kindspell` and regenerating now changes what
the compiler prints. The `TokenKind` enum itself is NOT closed: 218 occurrences
across 30 files, and `src/grammar_role_table.zig` names `lexer.TokenKind` on
every row, so it stays until the parser consumes role rows by ordinal
(`GAP-134`).

**O1 PARTIALLY CLOSED.** The tree-sitter operator table is now the ranks of
`roleprecedence` in the owner, its associativity follows `roleassoc`, and
`gate/gap-145-consumer.sh` compares all 276 pairs against
`src/grammar_role_table.zig` with a planted inversion as its positive control.
The behaviour change is observable:

    x = a | b .. c     before  (a | b) .. c      after  a | (b .. c)
    y = p .. q .. r    before  (p .. q) .. r     after  p .. (q .. r)

(`tree-sitter parse`, pristine `c77dea60` vs this lane.) Recognition over the
tracked corpus is unchanged, which is the negative control:

    tree-sitter parse --quiet --stat --paths <1012 tracked .id>
    before  1012 parses, 67 successful, 945 failed, 6.62%
    after   1012 parses, 67 successful, 945 failed, 6.62%

Identical, and that is the point: a precedence error produces no ERROR node, so
the coverage ratchet could not have seen this defect even if it ran. The
doc-derived cross-check, `specrows`, and the `spec` row are deleted. What remains open is
larger than the table: the literal and comment RULES in `grammar.js`
(`text` / `bytes` / `long_text`, `hash_comment` / `compat_comment` /
`compat_long_comment`) are still hand-authored, because
`scripts/treesitter_emit.id` does not emit them and cannot be run to find out —
see the blocker below.

**O7 CLOSED.** `tools/parity/grammar` matches the projection it reads, its four
identity checks execute for the first time since the projection was lowercased,
and a planted `roleidentitycount = 999` is now detected where before it was
indistinguishable from agreement. "File absent" and "probe matched nothing" are
different sentences.

**O5 ratcheted, not closed.** `gate/gap-145-consumer.sh` now COUNTS the arms
on every run — 34 arms, 12 observing the producer quote, 22 blind — and refuses
a rise above 22. It is a ceiling that falls, not a claim of zero, and it is
measured rather than written down: the first version of that check grepped
`\.quote`, which `\.quoted` contains, and reported 0 blind arms.

**O4 and O6 OPEN, untouched.**

### O7 found one more, inside this lane's own gate (2026-08-23, review round 2)

`gate/gap-145-consumer.sh` was reported PASSING with 13 checks. It was, under
this host's `/bin/sh`, which is bash 3.2 in POSIX mode. Under `dash`:

    dash -n gate/gap-145-consumer.sh   Syntax error: end of file unexpected, 2
    dash    gate/gap-145-consumer.sh   aborts after the Python section, 2

A backtick pair inside a DOUBLE-quoted argument. bash parses a substitution
body lazily, so an unexecuted branch never complains; dash parses it eagerly
and refuses the file. Bisected:

    commit 2, 12 checks   dash PASS, exit 0   -- that PASS was real
    commit 3, 13 checks   dash abort, exit 2  -- that PASS was NOT real

`gate/all.sh` runs `sh "$gate"`, so on any host whose `/bin/sh` is dash this
gate was counted as failed on every run. This is the O7 class — a control that
cannot fail — occurring inside the gate written to police it, which is why the
repair is `gate/posix.sh` and not the one line: it asks the strictest shell
present whether every gate in the home can be READ at all (39 subjects under
dash), fails on zero subjects, and positive-controls itself with the ORIGINAL
defect rather than an invented one.

Two further review findings closed in the same round:

- the tree-sitter operator check required the set to be merely NONEMPTY, so
  deleting a row from `grammar.js` and its generator passed — the remaining
  rows still agreed with each other. It now demands SET EQUALITY against the
  owner (34 infix identities = 23 rendered + 11 declared exclusions, each
  naming the rule that owns it instead), and plants exactly that deletion as a
  second positive control.
- `_kindspell`, `_pick`, `_zigname`, `_spellname` are gone. Underscores are
  forbidden in `.id` by `gate/idiom.id` `under()`, and `gate/vocabulary.sh`
  refuses new public module-scope declarations — but `gate/vocab-extract.awk`
  skips any added line whose first byte is whitespace, so an indented binding
  is not a module-scope declaration. The row and its scan moved into `zigrow`'s
  body: zero underscores, zero new declarations, both gates green on one diff.
  This was NOT a law deadlock. The price is that the field scan repeats
  `zigname`'s, because a shared `pick(row, kind)` is exactly the module-scope
  relation the freeze refuses; that duplication is stated at the site.

### Ordered remaining work

1. **Unblock the tree-sitter generator.** `scripts/treesitter_emit.id` refuses
   at `DNB001 method-unresolved:read` (`slurp` → `f:read("a")`), so
   `zig build treesitter-projection` — the byte-identity control on the second
   grammar authority — cannot run. Everything else in O1 is downstream of this:
   while it is dead, `grammar.js` is an authored file with a banner claiming
   `0` authored lines. Either lower `read` in the direct backend or move the
   emitter to a face the backend supports.
2. **Emit the GAP-145 literal and comment identities from the generator.**
   `tokens()` emits only `string` over the three physical forms; `tail()` emits
   only the Lua `--` comment. The tracked artifact carries `text` / `bytes` /
   `long_text` and the three comment identities that the generator does not.
   Needs (1) first, or the byte-identity check cannot confirm the result.
3. **Retire the second keyword identity producer (O4).** DONE in two
   increments (2026-08-25): the production lookup reads the owner's `.keyword`
   rows and the weak C link is gone (`src/keyword_bridge.zig`), then
   `src/token_semantic.zig` was deleted and `src/token_classify_gen.zig`
   re-rooted onto those same rows — see the dated update in the O4 section
   above. Residue: the two vendored tools/ projections.
4. **Close the three observationally divergent quote arms (O5).**
   `src/codegen.zig:21987` (`.quoted => "string"` for a byte sequence),
   `src/comptime.zig:366`, `src/dnir_lower.zig:9520`. The pattern to follow is
   `src/dnir_lower.zig:7033`, which now reads `sourceQuoteValue` off the graph
   instead of assuming text.
5. **Delete the second lexer in the parser (O6).** `matchingParen` and
   `interpolationHoleEnd` decide "inside a string" from `'"'` / `'\''` over
   already-lexed payloads. Blocked on the `GAP-134` immutable token view.
6. **Retire host `TokenKind` (O3 remainder).** Blocked on the parser consuming
   generated role rows by ordinal — `GAP-134`.
7. **Wire the controls into something that runs them.**
   `gate/gap-145-consumer.sh` and `tools/parity/grammar` are reachable only
   through `gate/all.sh`, which is on no build step and no hook.
   `gate/posix.sh` was in the same state and is now on `zig build test` and
   `zig build posix`. It was also under-measuring its own home: written to ask
   the strictest shell whether every gate can be READ, it enumerated
   `gate/*.sh` and so examined 43 of the 49 present. `law.path.name` is
   actively moving gates out of that glob — this gap's own lane decomposed
   `gate/treesitter-agreement.sh` into `gate/treesitter/agreement.sh` and
   removed it from the measurement without noticing, and four of the six it
   could not see are on `zig build test`. Repaired to `gate/*.sh gate/*/*.sh`,
   the enumeration `gate/all.sh` has always used: 43 → 49 subjects, and a
   planted backtick defect in a NESTED gate is now refused where before it was
   invisible.

8. **The parser slice is blocked by a mechanism, not only by this verdict.**
   Measured at `dec7d509` and recorded in `GAP-134`: `idol dump-c <file> --lib`
   — the exact recipe that produced `src/lexer_tokenize.c` — lowers every
   cross-module `.id` → `.id` call to `duo_fatal("unlowered native call")` and
   exits 0 with empty stderr. `lib/compiler/token_view.id` emits three, one per
   `token.grammarrole.*` consumer; `lib/compiler/parser.id` emits 291;
   `lib/compiler/lexer.id` emits 0, which is why it is the only transfer that
   ever succeeded. `idol dump-c lib/compiler/token.id --lib` refuses outright,
   so the one grammar owner has never executed inside the compiler — only its
   generated tables have. Ratchet: `gate/lower/fallback.sh`.

**GAP-145 cannot be declared closed.** The acceptance test is literal zero
observers; O1 is partial, and O4, O5 and O6 are untouched. Do not start parser
SHC.

## Current-head reconciliation (2026-08-23, `b8073cef`)

The operator-ontology change was rechecked at this exact head rather than
inferred from its commit message:

    zig build grammar-projection --summary all
    → PASS, both generated projections byte-identical

    sh gate/gap-145-consumer.sh
    → PASS, the measured quote ceiling and Tree-sitter/operator controls hold

    sh tools/parity/grammar
    → PASS, semantic identity and physical-slot projections agree

    sh gate/posix.sh
    → PASS, every enumerated shell gate parses under dash and the planted
      original defect is refused

The two Tree-sitter executable projections remain honestly red:

    zig build treesitter-projection --summary all
    → DNB001 method-unresolved:read in scripts/treesitter_emit.id

    zig build tree-sitter-coverage --summary all
    → DNB001 concat in scripts/tree_sitter_coverage.id

The host-authored `TokenKind` is now deleted. Its owner-generated Zig enum
remains a nonempty physical bridge; the commands above measure its semantic-
identity and physical-slot projection rather than pinning another prose count. The operation maps are
gone; the observer categories O1, O3, O4, O5, and O6 therefore remain the
closure boundary, not a parser `BinOp` map.

## Required order

1. Add distinct Idol-owned token identities and compatibility/lawset status for
   text, bytes, Lua long text, canonical comment, Lua comments, shebang, and
   reserved backtick.
2. Generate compact grammar roles from the constitutional grammar owner. Parser
   consumers must use identities/roles, never delimiter text.
3. Add canonical and compatibility lexer differentials, including unchanged
   spelling with changed identity and changed spelling with preserved role.
4. Implement canonical multiline text normalization exactly once: omit the
   opening and closing structural newlines, remove the closing-delimiter indent
   from every nonblank content line, and diagnose insufficient indentation.
5. Preserve single-quoted Lua text under compatibility provenance and migrate
   it to double-quoted text before native Idol interpretation. Native single
   quotes then produce byte-sequence semantics; Unicode escapes remain rejected
   pending separate admission.
6. Make hash comment-only in canonical Idol and migrate length to the admitted
   subject-oriented `len` relation. Compatibility `#value` remains provenance,
   not a second native relation.
7. Project the same authority into parser, formatter, canonicalizer,
   Tree-sitter, LSP, MCP, generators, documentation, and corpus ratchets.
8. Migrate canonical source only after exact rewrites exist. Never replace
   historical single quotes or long strings textually without a semantic
   equivalence witness.

## Required controls

- Double-quoted text, single-quoted bytes, compatibility single-quoted text,
  and Lua long text have distinct token identity/provenance.
- One-byte demand can scalarize a byte sequence without changing its semantic
  literal identity.
- Canonical hash comments are trivia; canonical `#value` is rejected; Lua hash
  length is compatibility-only and resolves to canonical `len` semantics only
  through a witnessed projection.
- Backtick cannot reach process execution and is rejected in canonical source.
- No `bytes` descriptor kingdom: a byte sequence is a value with element
  descriptor `byte` and shape/cardinality/layout facts; text carries
  textual/encoding law that an arbitrary byte sequence does not
  (`law.text.byte`). The quote/source-law distinction lives in provenance.
- A byte-zero shebang is distinct from an ordinary hash comment by source
  position fact.
- Parser classification changes with token identity, not delimiter text.
- Removing comments changes only trivia provenance, not semantic graph facts.
- Generators and Tree-sitter consume the authority projection rather than local
  spelling lists.

## ABI consumer (do not start parser SHC)

Host `RECORD_SLOTS`, `lexErrorFromCode`, `tokenKindFromOrdinal`, and the
host-authored enum are **deleted**. The consumer queries `recordslots()` /
`field*()` / `rejectionname()`; `kindFromRecord` indexes the owner-generated
role row by producer ordinal and refuses unpublished or out-of-range slots
(`law.schema.one`, `law.magic.zero`). Remaining ABI debt:

- owner-generated `TokenKind` still bridges producer ordinals into host parser switches;
- `duo_lexer_*` / `useDuoTokens` remain bridge-death names (`GAP-107`);
- do not start parser SHC — missing identities will be reconstructed from
  token kind plus token text until `GAP-134` immutable token view closes.

Physical forms and corpus-home admission are now executed producer facts rather
than resolver lists. The `sourceFacts` Zig function is a filesystem/name-binding
bridge only. Neither suffix nor path is consulted after the returned source-law
operand exists. Remaining closure is lexical/grammar consumer convergence, not
another source admission implementation.

## Performance and closure

Token role selection must compile to constant-time ID/bitset or equivalent
lookup. Text and byte payloads retain source spans/views where lawful; multiline
normalization must not force an intermediate allocation when indentation
metadata suffices. Record parse latency, copied bytes, and allocations across
the production lexer differential.

Close only when all canonical producers and consumers use the generated lexical
authority, the complete classified canonical corpus has zero debt for the six
retired forms, compatibility fixtures preserve their source lawsets, and no
downstream text reconstruction remains. An incomplete grep census cannot claim
zero.

## Closure acceptance (literal zero)

Passing the lexer differential is necessary but not sufficient. Close when:

- `KIND_STRING_LIT` and host `TokenKind.string_lit` are **deleted**, not a hole
  or unused-looking legacy member (GAP-107 token-role-id)
- AST `.string_lit` arm is **removed**; `.quoted` retains exact quote
  provenance without a text/bytes AST taxonomy
- no downstream consumer can observe or depend on the old collapsed identity
- Tree-sitter and editor projections are generated or mechanically verified from
  the same grammar/lexical authority — never a second normative source
- source law/provenance admission executes in Idol; host code may normalize
  physical provenance and bind the producer ABI but owns no roster or mapping

The test is **literal zero on the old identity**, not “new kinds exist beside
legacy members.”

The first closure bullet is now executable: `@hasField(TokenKind,
"string_lit")` is false, the producer has no `string_lit`, raw slot 3 fails
closed, and grammar parity separately compares semantic identity count with the
physical producer-slot span. The remaining bullets keep this gap open.

## Superseded observations

The `idol.gap.frontier.v1` block at the top of this file is the authority on
which observations this gap declares historical, and `gate/frontier.sh` requires
every anchor it lists to resolve. This section is the PHYSICAL grouping of the
ones that are purely dated measurement, so that reading top-down no longer hands
a fossil to someone looking for the blocker. The two lists are not identical and
do not need to be: a section can be listed above and still stand here, or stand
above this boundary while being listed, when it carries a standing ruling or the
most recent evidence as well as a dated measurement.

Two dated 2026-08-17 sections follow. The first of them, `Current measured
boundary`, is the specific hazard this frontier section exists to fix: it is
titled "current", it was the first thing in this file, and it is the oldest
measurement in it. Both sections predate the 2026-08-23 observer census above
and predate the operation-ontology transfer, so their account of what remains
host-owned is behind the tree.

They are preserved because the crossings they enumerate are the ancestry of the
identity work -- quote x family, shebang, comment and compatibility-comment
identities, the reserved backtick, the delimiter level published in `int_val` --
and because the acceptance framing they state (zero observers, not "new kinds
exist beside legacy members") is still the closure test. Read them for that. Do
not read them for the state of the tree.

## Current measured boundary (2026-08-17)

The ruling is encoded in C0. Documentation is not closure. Identities below
cross the production `tokenize()` / `dump-c` artifact. GAP-145 is **not
closed**. Do not start parser SHC (`GAP-134`).

**Acceptance is zero observers of the collapsed identity**, not “lexer emits
new kinds.” Producer `KIND_STRING_LIT` and host `TokenKind.string_lit` are now
deleted. The physical producer slot remains unpublished, while the next live
host identity has an explicit backing value so later ordinals do not shift.
The collapsed AST `.string_lit` arm is now deleted. One syntax-provenance arm,
`.quoted`, carries the exact `Quote` projected from the producer; it does not
create text/bytes AST kingdoms. This is not closure by itself: semantic
consumers that still map every `.quoted` value directly to `.str` must consume
the quote/source-law fact where the distinction is observable. The repair
target is the element/shape/descriptor fact set — element descriptor `byte`
plus shape, cardinality, and layout (`law.text.byte`) — never a parallel
`bytes` descriptor kingdom.

**Crossed (producer emit + host differential `next_tok`):**

- quote × family: `KIND_TEXT_LIT` 105, `KIND_BYTES_LIT` 106,
  `KIND_COMPAT_TEXT_LIT` 107, `KIND_COMPAT_LONG_TEXT_LIT` 108
  (`classify_quote`; long bracket is 108)
- `KIND_SHEBANG` 110 at byte-zero `#!`; parser stream strips via
  `takeParserToken` / `duo_next`
- `KIND_COMMENT` 111 (`#` in family 1); skip_ws is whitespace only
- `KIND_COMPAT_COMMENT` 112 (`--` line); `KIND_COMPAT_LONG_COMMENT` 113 (`--[[`)
- `KIND_BACKTICK` 84 emitted; canon parser-facing `refuseReserved` →
  `UnexpectedChar` (not producer `_fail`)

**Still host / remaining:**

- Slot 3 is unpublished (`kindname(3)=""`, `KIND_STRING_LIT` deleted), and host
  `TokenKind.string_lit` is deleted. `kw_and = 4` preserves all live backing
  values without manufacturing an enum identity for the empty slot. Dispatch
  refuses both `string_lit` and raw ordinal 3. The grammar-role projection is
  sparse over the physical slot span and the empty row has no roles.
- Tree-sitter `grammar.js` names `text` / `bytes` / `long_text` and
  `#` vs `--` comments; `grammar.json` is regenerated from that file.
  Generated `parser.c` stays untracked (editor artifact, not production
  lex). Queries still see `string` as the union.
- Source-law admission now executes in the Idol producer. `sourceform*` owns
  physical forms; `sourceentry*` owns the transition corpus roles;
  `sourcefact*` performs longest-match admission, unlisted fallback, and exact
  law/provenance selection. Zig supplies only a filesystem-normalized
  provenance spelling and binds returned names to the temporary host ABI.
- owner-generated `TokenKind` still bridges producer ordinals into host parser switches;
  decode is an indexed load from the generated role row and no longer binds
  identity from producer names.
- generated grammar roles and immutable token view: `GAP-134`

**Crossed 2026-08-13 (consumer reconstruction):**

- `compat_long_text_lit` publishes delimiter level in `int_val` (producer +
  host + `dump-c`). Parser attribute spans use identity + that level
  (`tokenSourceSpan`); `longBracketSpan` / delimiter-width source scans are
  deleted. Token text remains content. This is not parser SHC.

**Crossed 2026-08-20 (semantic graph quote facts):**

- lifted `.quoted` application values publish `SourceQuoteFact` on the semantic
  graph (`publishSourceQuote` / `sourceQuote`), mirroring `ExactI64` for integer
  literals
- sim-v0 graph JSON version 7 exports a `source_quote` array alongside
  `exact_i64`; consumers can observe producer `Quote` without re-lexing token
  text
- this does **not** close the gap: sema/codegen/dnir paths that answer `.str`
  for every `.quoted` expr still ignore quote facts where byte/text law must
  diverge (`law.text.byte`, element descriptor `byte`)

**Crossed 2026-08-20 (sema/codegen consumer wiring):**

- `ast.quotedLiteralIsByteSequence` and `types.quotedLiteralType` route producer
  `Quote` identity into sema/codegen typing instead of bare `.quoted => .str`
- `gate/gap-145-consumer.sh` ratchets the helper and forbids reintroducing the
  collapse in sema/codegen
- DNIR lowering and other downstream paths may still ignore quote facts; gap
  remains open until observer count of the collapsed identity is zero

**Crossed 2026-08-23 (one operation ontology, no parser-local operation maps):**

- `lib/compiler/token.id` now owns the relation and prefix identities together
  with the infix, update, and glued source-face facts; precedence and
  associativity remain facts on the same producer rows
- `src/ast.zig` aliases the generated relation ontology, and the parser's former
  infix, compound-update, glued-update, and repeated unary switches now consume
  `src/grammar_roles.zig` accessors
- the pretty-printer's authored inverse map is gone; it derives the inverse from
  the same generated rows with compile-time totality and injectivity checks
- this removes one grammar reconstruction class but does **not** transfer parser
  recognition: the host recognizer, host `TokenKind`, Tree-sitter literal and
  comment rules, keyword duplication, quote-law consumers, and raw-byte parser
  scans keep GAP-145 open

## Production lexer evidence (2026-08-17)

Production route is Idol `tokenize()`; host `tokenizeHost()` walks `next_tok`
as the differential oracle. `src/lexer_tokenize.c` is regenerated from
`lib/compiler/lexer.id` via `dump-c --lib`. Family is a tokenize operand;
`suffix(file)` is deleted. Compile/fmt/embed call `sourceFacts` then
`initFacts`. `ledger/shc` PASS on this transfer; complete FTCFTW remains
NOT PROVEN. No compiler B.

Changing only the host lexer would regress SHC; changing only the Idol lexer
without regen would break the generated-C projection. AST `.quoted` carries
`quote` (`text` / `bytes` / `compat_text` / `compat_long` / `host`) without
minting one AST arm per source law. The old `.string_lit` arm is absent and an
executable `@hasField` control keeps it absent. This rename is not parser SHC.
Parser long-bracket source reconstruction is deleted. Pattern/call/table/span
consumers use quoted and literal roles, not delimiter spelling. Pratt
left/right, relation identity, prefix identity, and update/glued faces come from
the generated owner. Parser recognition remains host-executed.

## Gate census counterfeit, measured and repaired (2026-08-26, glm-5/zai lane)

The consumer gate's §3b ratchet was defeated, not satisfied. Between 17:17 and
17:24 UTC, twenty-one `.quoted` arms across nine `src/*.zig` files were
rewritten as `blk: { _ = s.quote; break :blk ... }`. `_ = x.quote` is a Zig
DISCARD: it observes nothing. But the §3b observing filter counted any line
matching `\.quote[^d]`, so every discard counted as observing, and the gate
printed `quoted arms: 37, observing: 37, blind: 0 (ceiling 21)` — a zero with
no observation behind it. No arm had been converted; the named blocker
(`dnir_lower.zig` `lowerExprCons` `.quoted` → `.str`) was cosmetically hidden
rather than repaired, and the gap file's own adjudication ("What this section
does NOT claim") records that site as a deliberate physical-carrier fact.

Commands, on the tree as found and on the pristine mirror of the base:

    git worktree add /tmp/idol-pristine f33bb37
    sh gate/gap-145-consumer.sh                 (as found)   blind: 0
    cd /tmp/idol-pristine && sh gate/gap-145-consumer.sh    blind: 21

The repair:

1. All 21 discard sites reverted to their HEAD forms (verified: the only
   remaining diff vs HEAD in those files is one comment at the carrier site;
   §3b again prints observing: 16, blind: 21 — identical to pristine).
2. `gate/gap-145-consumer.sh` §3b now STRIPS `_ = x.quote;` discards from the
   observing count before counting, with a planted control that is the verbatim
   defeating shape (3 planted arms, 2 discard baits, must classify as 2 blind).
3. New §3b post-check: a `.quote` discard anywhere in `src/*.zig` is a failure
   outright. Stripping closes the add-an-arm hole; the outright refusal closes
   the rewrite-an-existing-blind-arm hole, where blind would stay at ceiling
   and the line would vanish from every census. The only use of the shape ever
   measured in this tree was census bait.
4. New §3d `DIVERGENT_QUOTE_ARMS`: the task's actual seam — an arm that can
   answer differently for the text face and the byte face of one quoted
   literal — must be 0. Judged by an allowlist ledger of face-neutral kinds
   (span / truthy / emit / name / inert / carrier / test) derived from the live
   tree; observing arms route through the producer (graph/descriptor) and are
   exempt. Raising it requires classifying the new arm in the ledger or
   converting it to a producer route.

Commands and verdicts after repair:

    sh gate/gap-145-consumer.sh    PASS (16 checks)
      quoted arms: 37, observing the producer quote: 16, blind: 21 (ceiling 21)
      divergent quote arms: 0 (must be 0)
      .quoted tag-test sites: 88 (ceiling 88)
    dash gate/gap-145-consumer.sh  exit 0
    sh gate/posix.sh               PASS — 82 gates parse under dash
    zig build --summary all        exit 0

Controls exercised: planting one discard bait fails §3b (blind 22 > 21) AND the
discard refusal; rewriting an existing blind arm as a discard fails the discard
refusal alone. Both plants restored; final tree re-verified green.

Not claimed: GAP-145 closure (O1 tree-sitter authorship and O6-related
token-ABI debt remain), parser SHC, token ABI, FTCFTW, compiler B. `zig build
unit-test` fails `c_header_parse: header paths are argv data` identically on
the pristine mirror of the base (1783 pass / 1 fail there; 1786 / 1 here) —
environment debt, pre-existing, not this lane's.

## O6 enforcement: the ratchet the deletion never had (2026-08-26, glm-5/zai)

The O6 deletion was measured and reported as a WIN, then left with nothing
enforcing it. This gate — the one object in the tree that runs and compares on
this seam — held the quote/text/byte ceilings and never refused a raw-byte scan
coming back. Per this file's own founding rule ("what held instead was
everything that RUNS AND COMPARES"), the deletion was one unwatched edit away
from regressing invisibly: nothing would have printed a different number.

`gate/gap-145-consumer.sh` §2b now holds the ratchet:

- `forbid` on `findMatchingParen` and `interpolationHoleEnd` in `src/parser.zig`
  (the remaining sites are foreign C header ingress — `c_frontend.zig`,
  `c_header_parse.zig` — upstream of any Idol token; no producer pack exists
  there to observe, so they are out of this seam and not counted).
- `has` on `fn matchingTokenClose(` and `token_view.fromLexer(` — the
  replacement must stay, not merely the retired scans stay gone.
- A planted positive control in a scratch parser.zig (both retired scans plus
  the live replacement; a clean scratch beside it), because a `forbid` that
  cannot fail is the `tools/parity/grammar` defect this gap records at O7:
  green for months while matching nothing.

Commands and verdicts, run on the live tree at subject revision f33bb37
(working tree, dirty sha 88ffddd7…):

    sh gate/gap-145-consumer.sh
    → gap-145 consumer gate: PASS (21 check(s))          EXIT=0
    sh gate/posix.sh
    → posix gate: PASS — 82 shell gate(s) parse under dash  EXIT=0
    ./tools/node/dev/idol-lock -- zig build gap-145-consumer
    → gap-145 consumer gate: PASS (21 check(s))          EXIT=0

Controls exercised, in a scratch GAP145_ROOT (repo `src/` copied in, `findMatchingParen`
appended to the scratch `parser.zig`; the shared tree never touched):

    GAP145_ROOT=<scratch> sh <scratch>/gate/gap-145-consumer.sh
    → gap-145 consumer gate: FAIL parser.zig reacquired findMatchingParen — …
    → EXIT=1

The planted conviction fails the gate exactly as required; the clean tree
passes. The scratch run also fails the tree-sitter section (the scratch lacks
`ext/`), which is a scratch artifact that can only fail more, never less.

Not claimed: parser SHC (not started, correctly blocked on the board),
token-ABI debt (O3), O1 tree-sitter authorship — the generator's own execution
is still refused on this host class, now with a sharper name: `dump-c` of
`scripts/treesitter_emit.id` refuses at the `io.open` existence-probe binding
in `slurp` ("'f' has no native representation in this module: the C backend
would have to discard its value", scripts/treesitter_emit.id:106), ahead of the
known `read` → `split` chain. That is C-backend capability debt in the
generator's own seam, recorded here so the next lane does not rediscover it.

## C graph-root and byte-control repair (2026-08-29, Hermes continuation)

**Exact clean subject:** `ab3a9d114052ef70db1613b1aea02d27f7f37cd7`
(2026-08-29T20:17:46-07:00).

`test/byte.id` exposed four physical C defects behind successful `idol check`:
the compiler-owned DNIR graph root `main` collided with the platform ABI
`main`; direct `print_value` emitted libc-shaped calls against the shim's fixed
`idol_printf(fmt, a3..a16)` signature; one reachable string caused 4,097
tentative declarations; and `idol_io_read_stdin` returned null, so the first
byte observation crashed at exit 139.

The bounded repair is physical only:

- raw DNIR graph root `main` maps to private C symbol `idol_entry`; home-mangled
  semantic relation symbols remain verbatim;
- string, integer, and valueless `print_value` calls emit the complete fixed
  shim ABI; f64 printing refuses by name as
  `print-value-f64-not-in-c99-slice` until a float-ABI witness exists;
- a reachable-DNIR prepass interns `lhs`, `rhs`, `third`, and `vals`, then emits
  one initialized definition per unique byte run before functions — no
  tentative pool, fixed ceiling, or warning-suppression flag;
- the C shim reads complete stdin into one owned NUL-terminated buffer, and the
  producer selftest byte-compares a multiline round trip.

Commands and observed verdicts on that clean subject:

```text
/tmp/idol-zig/zig-aarch64-linux-0.17.0-dev.1567+f0354179a/zig test \
  -cflags -std=c11 -w -Dmain=duo_lexer_tokenize_main -- \
  src/lexer_tokenize.c -ODebug -Mroot=src/tests.zig -lc \
  --test-filter c_backend
→ 12 passed, 0 failed

./tools/node/dev/idol-lock -- \
  /tmp/idol-zig/zig-aarch64-linux-0.17.0-dev.1567+f0354179a/zig \
  build --summary all
→ 5/5 steps succeeded

sh gate/grammar-projection.sh
→ control PASS; entry PASS; both tracked projections regenerate byte-identically

./zig-out/bin/idol compile --no-cache --backend=c --emit=c \
  --target=c-source test/byte.id -o /tmp/idol-c-debug/byte.c
cc -O2 /tmp/idol-c-debug/byte.c \
  tools/node/dev/grammar/idol_c_runtime_shim.c -o /tmp/idol-c-debug/byte
printf '\n' | /tmp/idol-c-debug/byte
→ byte10, exit 0
```

This is not a C-oracle promotion or a speed claim. C remains an orthogonal
physical output/control. Still open: C f64 print realization, embedded-NUL byte
extent, the `read` → `split` generator chain above, O1 Tree-sitter authorship,
O3 token ABI, parser SHC, compiler B, and FTCFTW.

## Inline record type-primary face transferred (2026-09-02)

The inline record type reader formerly entered through the final host
`tok.kind` switch in `parse_type_primary`. Whole-pack `event` now projects the
`{` face in lane two. The Zig consumer selects that settled face, and the
matching host switch is deleted. The lane reuses a bit only on a mutually
exclusive name coordinate; no ABI, event word, allocation, cursor walk, or
fallback was added.

`sh gate/gap-145-consumer.sh` passes with its owner-damage and planted positive
controls. The transfer does not remeasure or alter the previously landed
generic, array, grouped, optional, or pointer faces.

Semantic diff: before, the producer pack carried the token while the host
reconstructed inline-record type-primary recognition from `tok.kind`; after,
`lib/compiler/parser.id` produces the face and Zig only invokes the record type
reader. Record fields, descriptor slots, layout refinements, worlds, demands,
and realization facts are unchanged. The host kind switch and its duplicated
recognition cease to exist. Remaining host parser recognition, generated token
ABI debt, lexical observer zero, Parser SHC, Compiler B, and FTCFTW remain open.
GAP-145 remains **OPEN**.

## Match-expression primary face transferred (2026-09-02)

Match-expression parsing formerly entered expression-primary handling through
the residual host `tok.kind` switch arm for `.kw_match`. Whole-pack `event` now
projects that exact choice through the existing primary lane at value fourteen.
Zig consumes the settled face and retains match parsing and value
materialization; the matching host arm is deleted. No ABI, event word,
allocation, cursor walk, fallback, or semantic concept was added.

The focused consumer gate covers the producer, consumer, host-arm deletion,
tracked projection, and planted old-vs-new controls. This slice does not
remeasure or alter if-expression, function-expression, vararg, boolean, or any
earlier primary transfer.

Semantic diff: before, the producer pack carried the exact `match` keyword
identity while Zig reconstructed expression-primary selection from `tok.kind`;
after, `lib/compiler/parser.id` produces that selection and Zig only parses and
materializes the already-selected match value. Match arms, values, locations,
descriptors, worlds, demands, and realization facts are unchanged. The
`.kw_match` host switch arm and its duplicate recognition cease to exist.
Remaining host parser recognition, generated token ABI debt, lexical observer
zero, Parser SHC, Compiler B, and FTCFTW remain open. GAP-145 remains **OPEN**.

## If-expression primary face transferred (2026-09-02)

If-expression parsing formerly entered expression-primary handling through the
residual host `tok.kind` switch arm for `.kw_if`. Whole-pack `event` now
projects that exact choice through the existing primary lane at value thirteen.
Zig consumes the settled face and retains conditional parsing and value
materialization; the matching host arm is deleted. No ABI, event word,
allocation, cursor walk, fallback, or semantic concept was added.

The focused consumer gate covers the producer, consumer, host-arm deletion,
tracked projection, and planted old-vs-new controls. This slice does not
remeasure or alter function-expression, vararg, boolean, or any earlier primary
transfer.

Semantic diff: before, the producer pack carried the exact `if` keyword identity
while Zig reconstructed expression-primary selection from `tok.kind`; after,
`lib/compiler/parser.id` produces that selection and Zig only parses and
materializes the already-selected conditional value. Conditional bodies,
values, locations, descriptors, worlds, demands, and realization facts are
unchanged. The `.kw_if` host switch arm and its duplicate recognition cease to
exist. Remaining host parser recognition, generated token ABI debt, lexical
observer zero, Parser SHC, Compiler B, and FTCFTW remain open. GAP-145 remains
**OPEN**.

## Floating primary face transferred (2026-09-02)

The floating reader formerly entered expression-primary materialization through
the host `tok.kind` switch arm for `.float_lit`. Whole-pack `event` now projects
that face through the existing primary lane: one selects closure, two selects
integer, three selects float, and the owner-projected literal and quoted facts
distinguish the numeric faces from the mutually exclusive matching-parenthesis
coordinate. Zig consumes the settled face before its residual primary switch,
and the matching host arm is deleted. No ABI, event word, allocation, cursor
walk, fallback, or semantic concept was added.

`sh gate/gap-145-consumer.sh` passes with owner-damage and planted positive
controls. This transfer does not remeasure or alter integer-primary, closure,
applied-width, inline-record, parenthesized expression-primary, nominal
acceptance, generic, array, grouped, optional, pointer, or type-primary evidence.

Semantic diff: before, the producer pack carried the exact floating identity but
Zig reconstructed the expression-primary choice from `tok.kind`; after,
`lib/compiler/parser.id` produces the choice and Zig only materializes the
floating value. The `.float_lit` host switch arm and its duplicated recognition
cease to exist. Floating value, location, descriptor, worlds, demands, and
realization facts are unchanged. Remaining host parser recognition, generated
token ABI debt, lexical observer zero, Parser SHC, Compiler B, and FTCFTW remain
open. GAP-145 remains **OPEN**.

## Integer primary face transferred (2026-09-02)

The integer reader formerly entered expression-primary materialization through
the host `tok.kind` switch arm for `.int_lit`. Whole-pack `event` now projects
that face through the existing primary lane: one selects closure, two selects
integer, and the owner-projected literal and quoted facts distinguish integer
from the mutually exclusive empty-parenthesis boundary at the same lane value.
Zig consumes the settled face before its residual primary switch, and the
matching host arm is deleted. No ABI, event word, allocation, cursor walk,
fallback, or semantic concept was added.

`sh gate/gap-145-consumer.sh` passes with owner-damage and planted positive
controls. This transfer does not remeasure or alter closure, applied-width,
inline-record, parenthesized expression-primary, nominal acceptance, generic,
array, grouped, optional, pointer, or floating-primary evidence.

Semantic diff: before, the producer pack carried the exact integer identity but
Zig reconstructed the expression-primary choice from `tok.kind`; after,
`lib/compiler/parser.id` produces the choice and Zig only materializes the
integer value. The `.int_lit` host switch arm and its duplicated recognition
cease to exist. Integer value, location, descriptor, worlds, demands, and
realization facts are unchanged. Remaining host parser recognition, generated
token ABI debt, lexical observer zero, Parser SHC, Compiler B, and FTCFTW remain
open. GAP-145 remains **OPEN**.

## Parenthesized expression-primary face transferred (2026-09-02)

The expression primary reader formerly entered its parenthesized group, pack,
or callable-body path through the host `switch (tok.kind)` arm for `.lparen`.
The whole-pack `event` relation already produces the exact matching-delimiter
boundary in lane two bits 13..63. The Zig consumer now selects the existing
nonzero boundary fact before the residual primary switch, and the matching host
arm is deleted. No fact, ABI, event word, allocation, cursor walk, or fallback
was added.

`sh gate/gap-145-consumer.sh` passes with its planted positive controls. This
transfer does not remeasure or alter nominal acceptance, generic type-primary,
or inline-record type-primary evidence.

Semantic diff: before, `parser.id` carried the exact `(` matching boundary but
the host reconstructed the expression-primary choice from `tok.kind`; after,
the host consumes that existing boundary and only executes the selected group,
pack, or callable-body reader. The `.lparen` host switch arm and its duplicated
recognition cease to exist. Remaining host parser recognition, generated token
ABI debt, lexical observer zero, Parser SHC, Compiler B, and FTCFTW remain open.
GAP-145 remains **OPEN**.

## Field-projection primary face transferred (2026-09-02)

Leading field projection formerly entered expression-primary handling through
the residual host `tok.kind` switch arm for `.dot`. Whole-pack `event` now
projects that exact choice through the existing primary lane at value fifteen.
The already-produced prefix fact distinguishes that value from a matching
parenthesis whose absolute close coordinate is also fifteen. Zig consumes the
settled facts and retains projection parsing and value materialization; the
matching host arm is deleted. No ABI, event word, allocation, cursor walk,
fallback, or semantic concept was added.

The serialized build passes all steps. The focused consumer gate reaches and
passes the field producer, consumer, host-arm deletion, tracked projection, and
planted old-vs-new controls; its only failures are the seven pre-existing
owner-order positive-control defects present at the match-expression baseline.
The unit aggregate retains the independently stale bootstrap refusal at the
pre-existing `Res: { ... }` source and its broader baseline failures. This slice
does not remeasure or alter match-expression, if-expression,
function-expression, vararg, boolean, or any earlier primary transfer.

Semantic diff: before, the producer pack carried the exact dot identity while
Zig reconstructed expression-primary selection from `tok.kind`; after,
`lib/compiler/parser.id` produces that selection and Zig only parses and
materializes the already-selected field projection. Projection subjects,
fields, values, locations, descriptors, worlds, demands, and realization facts
are unchanged. The `.dot` host switch arm and its duplicate recognition cease
to exist. Remaining host parser recognition, generated token ABI debt, lexical
observer zero, Parser SHC, Compiler B, and FTCFTW remain open. GAP-145 remains
**OPEN**.

## Table primary face transferred (2026-09-02)

The expression reader formerly entered table materialization through the host
`switch (tok.kind)` arm for `.lbrace`. Whole-pack `event` already projected the
exact `{` face for inline-record type position. That face is now named for the
brace identity rather than one consumer, and expression context consumes it
before the residual primary switch. The type and expression consumers are
context-exclusive; no ABI, event word, allocation, cursor walk, fallback, or
semantic concept was added.

`sh gate/gap-145-consumer.sh` passes with its owner-damage and planted positive
controls. This transfer does not remeasure or alter floating, integer, closure,
applied-width, inline-record, parenthesized expression-primary, nominal,
generic, array, grouped, optional, pointer, or quote evidence.

Semantic diff: before, `lib/compiler/parser.id` produced the exact brace face
but named it for only its type-primary consumer, while Zig reconstructed the
expression-primary choice from `tok.kind`; after, the producer names the exact
brace face and Zig only invokes the table reader selected by expression
context. The `.lbrace` expression-primary host switch arm and its duplicated
recognition cease to exist. Table values, fields, locations, descriptors,
worlds, demands, and realization facts are unchanged. Remaining host parser
recognition, generated token ABI debt, lexical observer zero, Parser SHC,
Compiler B, and FTCFTW remain open. GAP-145 remains **OPEN**.

## Table primary face retracted (2026-09-02)

The table-primary transfer above reused the inline-record type face as though
the brace identity also carried expression-context selection. It does not. The
result sent name-led statement parses through table materialization and left
the aggregate unit suite broadly red. The host `.lbrace` expression arm is
restored, the expression consumer of lane-two bit 12 is deleted, and the bit is
again named only for the inline-record type fact it actually owns.

`zig build unit-test` is the regression witness. `sh gate/gap-145-consumer.sh`
remains the parser-consumer authority check; its current independent
owner-order positive-control failure is recorded by the command outcome rather
than hidden by this repair.

Semantic diff: before, one producer fact represented the brace identity for
inline-record type position while an expression consumer treated it as a
settled table-primary choice; after, that invalid consumer and claim disappear,
and expression table recognition returns to the residual host switch. Table
values, fields, locations, descriptors, worlds, demands, and realizations are
unchanged. The table-primary host reconstruction therefore remains open along
with the other parser, ABI, observer, Parser SHC, Compiler B, and FTCFTW debt.
GAP-145 remains **OPEN**.

## Applied-width type-primary face transferred (2026-09-02)

The `i(64)` / `u(16)` / `f(32)` type reader formerly recognized its four-token
shape in Zig from `tok.kind`, including adjacency and speculative cursor
restoration. Whole-pack `event` now projects that exact recognition through
lane-two bit 4 at the mutually exclusive name coordinate. Zig consumes the
settled face and retains only descriptor-width validation and materialization.
The matching host kind recognizer and rollback state are deleted. No ABI, event
word, allocation, cursor walk, fallback, or third lane was added.

`sh gate/gap-145-consumer.sh` passes with planted host-kind controls. This transfer does
not remeasure or alter inline-record, parenthesized expression-primary, nominal
acceptance, generic, array, grouped, optional, or pointer evidence.

Semantic diff: before, the producer pack carried every token and coordinate but
Zig reconstructed the applied-width type-primary shape and restored its cursor
when that reconstruction failed; after, `lib/compiler/parser.id` produces the
shape once and Zig only consumes and validates the selected descriptor width.
Remaining host parser recognition, generated token ABI debt, lexical observer
zero, Parser SHC, Compiler B, and FTCFTW remain open. GAP-145 remains **OPEN**.

## Closure primary face transferred (2026-09-02)

The expression primary reader formerly entered its closure path through the
host `switch (tok.kind)` arm for `.pipe`. Whole-pack `event` now projects that
face through the existing matching-boundary lane: one selects closure, while
greater values retain the exact matching-parenthesis coordinate. Zig consumes
the settled face before its residual primary switch, and the matching host arm
is deleted. No ABI, event word, allocation, cursor walk, fallback, or third lane
was added.

`sh gate/gap-145-consumer.sh` passes with its planted positive controls. This
transfer does not remeasure or alter applied-width, inline-record,
parenthesized expression-primary, nominal acceptance, generic, array, grouped,
optional, or pointer evidence.

Semantic diff: before, the producer pack carried the `|` identity while Zig
reconstructed the closure-primary choice from `tok.kind`; after,
`lib/compiler/parser.id` produces the choice and Zig only executes the selected
closure reader. The `.pipe` host switch arm and its duplicated recognition
cease to exist. Remaining host parser recognition, generated token ABI debt,
lexical observer zero, Parser SHC, Compiler B, and FTCFTW remain open. GAP-145
remains **OPEN**.

## Nil primary face transferred (2026-09-02)

The expression primary reader formerly entered absence materialization through
the residual host `tok.kind` switch arm for `.kw_nil`. Whole-pack `event` now
projects that exact choice through the existing primary lane at value four.
Zig consumes the settled face before its residual primary switch, and the
matching host arm is deleted. The coordinate is mutually exclusive with the
closure, integer, floating, and matching-parenthesis faces; no ABI, event word,
allocation, cursor walk, fallback, or semantic concept was added.

On measured subject `0365b107`, `src/parser/projection.c` regenerates
byte-identically. `zig build unit-test` is the semantic regression command.
`sh gate/gap-145-consumer.sh` reaches the nil owner/consumer/deletion checks and
retains its seven independent owner-order positive-control failures. The parser
artifact retains its independent cursor/pack disagreement; neither pre-existing
red outcome is hidden or claimed as a pass. This transfer does not remeasure or
alter the retracted table-primary evidence.

Semantic diff: before, the producer pack carried the exact nil identity while
Zig reconstructed the expression-primary choice from `tok.kind`; after,
`lib/compiler/parser.id` produces the choice and Zig only materializes absence.
The `.kw_nil` host switch arm and its duplicated recognition cease to exist.
Remaining host parser recognition, including table-primary context, generated
token ABI debt, lexical observer zero, Parser SHC, Compiler B, and FTCFTW remain
open. GAP-145 remains **OPEN**.

## Exact table-primary face transferred (2026-09-02)

The retracted table-primary attempt tested lane-two bit 12 alone. That bit is
also present in the declaration encoding for `@`, so the consumer treated an
attribute or macro primary as a table and changed working programs. The
whole-pack event now names its `{` result as the brace face, while both the
inline-record type consumer and expression-table consumer select the complete
declaration nibble value eight. The distinct `@` value twelve remains on its
existing path. The residual `.lbrace` arm in the expression-primary
`tok.kind` switch is deleted; no ABI, event word, allocation, cursor walk, or
fallback was added.

The serialized pinned build succeeds. The Zig unit runner dependency completes
without a reported failure; the aggregate `unit-test` step remains red only
because the independently stale installed compiler refuses the current
`Res: { ... }` parser source while regenerating the parser artifact. The same
base condition prevents byte regeneration. The tracked projection carries the
mechanical identifier-only regeneration delta. `sh gate/gap-145-consumer.sh`
reaches and passes the exact-brace producer, consumer, deletion, and planted
old-vs-new controls; its seven pre-existing owner-order positive-control
failures remain visible.

Semantic diff: before, the producer pack carried the exact brace identity but
Zig reconstructed expression table-primary recognition from `tok.kind`; after,
`lib/compiler/parser.id` produces the brace face and both parser contexts
consume its exact noncolliding value. The `.lbrace` host switch arm and its
duplicated recognition cease to exist. Table and record values, fields,
locations, descriptors, worlds, demands, and realization facts are unchanged.
Remaining host parser recognition, generated token ABI debt, lexical observer
zero, Parser SHC, Compiler B, and FTCFTW remain open. GAP-145 remains **OPEN**.

## Exact name primary face transferred (2026-09-02)

Expression-primary and type-primary recognition both retained a host `.name`
choice after the producer pack had already settled the exact name identity.
Whole-pack `event` now carries that face in lane-two bit 5 at the name
coordinate. The bit remains the callable-header comma face at its mutually
exclusive `(` coordinate; both name consumers additionally require the
existing member fact, so a parenthesized header cannot be mistaken for a name.
The residual `.name` arm in the expression-primary `tok.kind` switch is deleted.
No ABI, event word, allocation, cursor walk, or fallback was added.

The first attempted encoding in static bit 63 was rejected: it made the signed
event negative and contaminated later arithmetic shifts, and the aggregate
reported 441 failures and three crashes. That encoding is absent from the
landed tree. The retained lane-two encoding passes the serialized 5/5 build.
`sh gate/gap-145-consumer.sh` reaches 424 checks and passes the exact-name
producer, both consumers, host-arm deletion, tracked projection, and planted
old-vs-new detector; its only seven findings are the pre-existing owner-order
positive-control failures already recorded at the exact-table baseline. The
aggregate remains independently red: its parser artifact cannot regenerate
because the installed bootstrap compiler refuses the pre-existing
`Res: { ... }` source, and its test runner reports 1480 pass, 62 skip, 441 fail,
and three crash. The tracked projection therefore carries the same bounded
mechanical source projection form as the preceding exact-table transfer.

Semantic diff: before, both parser contexts reconstructed name-primary
recognition from `tok.kind`; after, `lib/compiler/parser.id` produces the exact
name face and Zig only materializes the already-settled name value or type.
Names, descriptors, locations, worlds, demands, and realization facts are
unchanged. Remaining host parser recognition, generated token ABI debt,
lexical observer zero, Parser SHC, Compiler B, and FTCFTW remain open. GAP-145
remains **OPEN**.

## Compatibility-text primary face transferred (2026-09-02)

The historical compatibility-text reader formerly entered expression-primary
materialization through the residual host `tok.kind` switch arm for
`.compat_text_lit`. Whole-pack `event` now projects that exact choice through
the existing primary lane at value five. Zig consumes the settled face before
its residual primary switch and retains the same decoding and interpolation
materialization; the matching host arm is deleted. No ABI, event word,
allocation, cursor walk, fallback, or semantic concept was added.

The pinned serialized build passes 5/5 steps. `sh gate/gap-145-consumer.sh`
reaches 430 checks, including the new producer, consumer, host-arm deletion,
tracked projection, and planted old-vs-new controls; it remains red only on the
same seven pre-existing owner-order positive-control defects recorded by the
exact-name baseline. The parser artifact retains the same independently stale
bootstrap refusal at the pre-existing `Res: { ... }` source, so the tracked C
projection carries the bounded mechanical two-line delta. No already-landed
primary face was remeasured.

Semantic diff: before, the producer pack carried the exact compatibility-text
identity while Zig reconstructed expression-primary selection from `tok.kind`;
after, `lib/compiler/parser.id` produces that selection and Zig only decodes and
materializes the already-selected text value. Text bytes, protection marks,
interpolation parts, locations, descriptors, worlds, demands, and realization
facts are unchanged. The `.compat_text_lit` host switch arm and its duplicate
recognition cease to exist. Remaining host parser recognition, generated token
ABI debt, lexical observer zero, Parser SHC, Compiler B, and FTCFTW remain open.
GAP-145 remains **OPEN**.

## Canonical-text primary face transferred (2026-09-02)

Canonical text decoding and interpolation formerly entered expression-primary
materialization through the residual host `tok.kind` switch arm for
`.text_lit`. Whole-pack `event` now projects that exact choice through the
existing primary lane at value six. Zig consumes the settled face and retains
the same decoding, protection marks, and interpolation materialization; the
matching host arm is deleted. No ABI, event word, allocation, cursor walk,
fallback, or semantic concept was added.

The focused consumer gate reaches 436 checks and passes the canonical-text
producer, consumer, host-arm deletion, tracked projection, and planted
old-vs-new controls. It remains red only on the seven pre-existing owner-order
positive-control defects recorded by the exact-name baseline. The tracked
artifact's canonical-text lane control passes; its aggregate retains the same
independent event disagreement present at the compatibility-text baseline.
The installed bootstrap compiler still refuses the pre-existing
`Res: { ... }` source, and the repository's pinned Zig binary is absent on this
box, so byte regeneration and the pinned build remain unavailable rather than
being restated as passes. No already-landed primary face was remeasured.

Semantic diff: before, the producer pack carried the exact canonical-text
identity while Zig reconstructed expression-primary selection from `tok.kind`;
after, `lib/compiler/parser.id` produces that selection and Zig only decodes and
materializes the already-selected text value. Text bytes, protection marks,
interpolation parts, locations, descriptors, worlds, demands, and realization
facts are unchanged. The `.text_lit` host switch arm and its duplicate
recognition cease to exist. Remaining host parser recognition, generated token
ABI debt, lexical observer zero, Parser SHC, Compiler B, and FTCFTW remain open.
GAP-145 remains **OPEN**.

## Bytes primary face transferred (2026-09-02)

Bytes materialization formerly entered expression-primary handling through the
residual host `tok.kind` switch arm for `.bytes_lit`. Whole-pack `event` now
projects that exact choice through the existing primary lane at value seven.
Zig consumes the settled face and retains the same byte copy and quoted-value
materialization; the matching host arm is deleted. No ABI, event word,
allocation, cursor walk, fallback, or semantic concept was added.

The serialized build passes 5/5 steps. The focused consumer gate reaches 442
checks and passes the bytes producer, consumer, host-arm deletion, tracked
projection, and planted old-vs-new controls; it remains red only on the same
seven pre-existing owner-order positive-control defects recorded by the exact
name baseline. The parser artifact and unit aggregate retain the same installed
bootstrap refusal at the pre-existing `Res: { ... }` source. The direct idiom
gate is likewise unavailable because that compiler refuses the gate's
pre-existing callable declaration. This slice does not remeasure or alter any
already-landed primary, name, compatibility-text, or canonical-text face.

Semantic diff: before, the producer pack carried the exact bytes identity while
Zig reconstructed expression-primary selection from `tok.kind`; after,
`lib/compiler/parser.id` produces that selection and Zig only copies and
materializes the already-selected bytes value. Bytes, locations, descriptors,
worlds, demands, and realization facts are unchanged. The `.bytes_lit` host
switch arm and its duplicate recognition cease to exist. Remaining host parser
recognition, generated token ABI debt, lexical observer zero, Parser SHC,
Compiler B, and FTCFTW remain open. GAP-145 remains **OPEN**.

## Compatibility long-text primary face transferred (2026-09-02)

Compatibility long-text materialization formerly entered expression-primary
handling through the residual host `tok.kind` switch arm for
`.compat_long_text_lit`. Whole-pack `event` now projects that exact choice
through the existing primary lane at value eight. Zig consumes the settled face
and retains the same byte copy and quoted-value materialization; the matching
host arm is deleted. No ABI, event word, allocation, cursor walk, fallback, or
semantic concept was added.

The serialized build passes 5/5 steps. The focused consumer gate reaches 448
checks and passes the compatibility long-text producer, consumer, host-arm
deletion, tracked projection, and planted old-vs-new controls; it remains red
only on the same seven pre-existing owner-order positive-control defects. The
parser artifact and unit aggregate retain the installed bootstrap refusal at
the pre-existing `Res: { ... }` source; the aggregate reports 1491 pass, 62
skip, 447 fail, and three crash. This slice does not remeasure or alter any
already-landed primary, name, compatibility-text, canonical-text, or bytes
face.

Semantic diff: before, the producer pack carried the exact compatibility
long-text identity while Zig reconstructed expression-primary selection from
`tok.kind`; after, `lib/compiler/parser.id` produces that selection and Zig
only copies and materializes the already-selected quoted value. Text bytes,
locations, descriptors, worlds, demands, and realization facts are unchanged.
The `.compat_long_text_lit` host switch arm and its duplicate recognition cease
to exist. Remaining host parser recognition, generated token ABI debt, lexical
observer zero, Parser SHC, Compiler B, and FTCFTW remain open. GAP-145 remains
**OPEN**.

## Function-expression primary face transferred (2026-09-02)

Function-expression materialization formerly entered expression-primary
handling through the residual host `tok.kind` switch arm for `.kw_function`
and `.kw_fun`. Whole-pack `event` now projects their shared exact choice
through the existing primary lane at value twelve. Zig consumes the settled
face and retains body parsing and value materialization; the matching host arm
is deleted. No ABI, event word, allocation, cursor walk, fallback, or semantic
concept was added.

The focused consumer gate covers the producer, consumer, host-arm deletion,
tracked projection, and planted old-vs-new controls. This slice does not
remeasure or alter vararg, boolean, or any earlier primary transfer.

Semantic diff: before, the producer pack carried the exact function keyword
identity while Zig reconstructed expression-primary selection from `tok.kind`;
after, `lib/compiler/parser.id` produces the shared function-expression face
and Zig only parses and materializes the already-selected value. Function
bodies, values, locations, descriptors, worlds, demands, and realization facts
are unchanged. The host switch arm and its duplicate recognition cease to
exist. Remaining host parser recognition, generated token ABI debt, lexical
observer zero, Parser SHC, Compiler B, and FTCFTW remain open. GAP-145 remains
**OPEN**.

## Boolean primary faces transferred (2026-09-02)

Boolean materialization formerly entered expression-primary handling through
the residual host `tok.kind` switch arms for `.kw_true` and `.kw_false`.
Whole-pack `event` now projects those exact choices through the existing
primary lane at values nine and ten. Zig consumes the settled face and retains
only the corresponding value materialization; both host arms are deleted. No
ABI, event word, allocation, cursor walk, fallback, or semantic concept was
added.

The focused consumer gate reaches the new producer, consumer, host-arm
deletion, artifact controls, and planted old-vs-new controls. Its only failures
before integration are the seven pre-existing owner-order positive-control
defects. The parser artifact remains unavailable through the independently
stale installed compiler, which refuses the pre-existing `Res: { ... }`
source. This slice does not remeasure or alter any already-landed attribute,
delimiter, declaration, or transfer-compatibility face.

Semantic diff: before, the producer pack carried the exact boolean identities
while Zig reconstructed expression-primary selection from `tok.kind`; after,
`lib/compiler/parser.id` produces the exact true/false face and Zig only
materializes the already-selected value. Values, locations, descriptors,
worlds, demands, and realization facts are unchanged. The two boolean host
switch arms and their duplicate recognition cease to exist. Remaining host
parser recognition, generated token ABI debt, lexical observer zero, Parser
SHC, Compiler B, and FTCFTW remain open. GAP-145 remains **OPEN**.

## Vararg primary face transferred (2026-09-02)

Vararg materialization formerly entered expression-primary handling through
the residual host `tok.kind` switch arm for `.dots`. Whole-pack `event` now
projects that exact choice through the existing primary lane at value eleven.
Zig consumes the settled face and retains only vararg materialization; the host
arm is deleted. No ABI, event word, allocation, cursor walk, fallback, or
semantic concept was added.

The focused consumer gate covers the producer, consumer, host-arm deletion,
tracked projection, and planted old-vs-new controls. This slice does not
remeasure or alter the already-landed boolean-primary or earlier primary
faces.

Semantic diff: before, the producer pack carried the exact dots identity while
Zig reconstructed expression-primary selection from `tok.kind`; after,
`lib/compiler/parser.id` produces that selection and Zig only materializes the
already-selected vararg value. Values, locations, descriptors, worlds,
demands, and realization facts are unchanged. The `.dots` host switch arm and
its duplicate recognition cease to exist. Remaining host parser recognition,
generated token ABI debt, lexical observer zero, Parser SHC, Compiler B, and
FTCFTW remain open. GAP-145 remains **OPEN**.

## Subject-method-reference primary face transferred (2026-09-02)

Leading subject-method-reference materialization formerly entered
expression-primary handling through the residual host `tok.kind` switch arm
for `.colon`. Whole-pack `event` now projects that exact choice through the
existing primary lane at value sixteen. Zig consumes the settled face and
retains method-reference parsing and value materialization; the matching host
arm is deleted. No ABI, event word, allocation, cursor walk, fallback, or
semantic concept was added.

The focused consumer gate covers the producer, consumer, host-arm deletion,
tracked projection, artifact controls, and planted old-vs-new controls. The
tracked projection changes by the same bounded generated statement as its Idol
owner; regeneration remains blocked by the installed compiler's refusal of the
pre-existing `Res: { ... }` source. This slice does not remeasure or alter field
projection, match, if, function, vararg, boolean, or any earlier primary face.

Semantic diff: before, the producer pack carried the exact colon identity while
Zig reconstructed expression-primary selection from `tok.kind`; after,
`lib/compiler/parser.id` produces the subject-method-reference face and Zig
only parses and materializes the already-selected reference. Method names,
subjects, operands, values, locations, descriptors, worlds, demands, and
realization facts are unchanged. The `.colon` host switch arm and its duplicate
recognition cease to exist. Remaining host parser recognition, generated token
ABI debt, lexical observer zero, Parser SHC, Compiler B, and FTCFTW remain open.
GAP-145 remains **OPEN**.

## Macro/anchor primary face transferred (2026-09-02)

Macro/anchor materialization formerly entered expression-primary handling
through the residual host `tok.kind` switch arm for `.at`. Whole-pack `event`
now projects that exact choice through the existing primary lane at value
seventeen. Zig consumes the settled face and retains the existing bare-anchor
versus macro-call distinction and value materialization; the matching host arm
is deleted. No ABI, event word, allocation, cursor walk, fallback, or semantic
concept was added.

The focused consumer gate covers the producer, consumer, host-arm deletion,
tracked projection, artifact controls, and planted old-vs-new controls. The
tracked projection changes by the same bounded generated statement as its Idol
owner; regeneration remains blocked by the installed compiler's refusal of the
pre-existing `Res: { ... }` source. This slice does not remeasure or alter the
subject-method-reference, field-projection, match, if, function, vararg,
boolean, or any earlier primary face.

Semantic diff: before, the producer pack carried the exact at-sign identity
while Zig reconstructed expression-primary selection from `tok.kind`; after,
`lib/compiler/parser.id` produces the macro/anchor primary face and Zig only
distinguishes and materializes the already-selected bare anchor or macro call.
Names, arguments, values, locations, descriptors, worlds, demands, and
realization facts are unchanged. The `.at` host switch arm and its duplicate
recognition cease to exist. Remaining host parser recognition, generated token
ABI debt, lexical observer zero, Parser SHC, Compiler B, and FTCFTW remain open.
GAP-145 remains **OPEN**.

## Reserved backtick primary face transferred (2026-09-02)

Reserved backtick rejection formerly entered expression-primary handling
through the last explicit `tok.kind` arm in `parse_simple_expr`. Whole-pack
`event` now projects that exact choice through the existing primary lane at
value eighteen. Zig consumes the settled face and retains the existing
`law.backtick.zero` diagnostic; the matching host arm and residual switch are
deleted. No ABI, event word, allocation, cursor walk, fallback, or semantic
concept was added.

The focused consumer gate covers the producer, consumer, host-arm deletion,
tracked projection, and planted old-vs-new controls. This slice does not
remeasure or alter macro/anchor, subject-method-reference, field-projection,
match, if, function, vararg, boolean, or any earlier primary face.

Semantic diff: before, the producer pack carried the exact backtick identity
while Zig reconstructed the reserved-expression rejection from `tok.kind`;
after, `lib/compiler/parser.id` produces the rejection face and Zig only emits
the already-selected diagnostic. Locations, diagnostics, values, descriptors,
worlds, demands, and realization facts are unchanged. The `.backtick` host
switch arm and its duplicate recognition cease to exist. Remaining host parser
recognition, generated token ABI debt, lexical observer zero, Parser SHC,
Compiler B, and FTCFTW remain open. GAP-145 remains **OPEN**.

## Pattern-entry face transferred (2026-09-02)

Pattern entry formerly selected rest, table, array, literal, else, name, and
negative-literal handling through a host `tok.kind` switch. Whole-pack `event`
now projects the few pattern-only choices through unused values of the existing
primary lane and reuses its already-settled exact faces for the remaining
choices. Zig consumes one pattern face and retains the existing pattern
materialization. The matching host token-kind switch is deleted. No ABI, event
word, allocation, cursor walk, fallback, or semantic concept was added.

The focused consumer gate covers the producer, consumer, host-switch deletion,
tracked projection, and planted old-vs-new controls. This slice does not
remeasure or alter reserved backtick, macro/anchor, subject-method-reference,
field-projection, type-primary, or any earlier primary face.

Semantic diff: before, the immutable producer pack carried every exact token
identity while Zig reconstructed the pattern-entry relation from `tok.kind`;
after, `lib/compiler/parser.id` produces that relation face and Zig only invokes
the selected pattern materialization. Locations, diagnostics, pattern values,
worlds, demands, and realization facts are unchanged. The host pattern-entry
switch and its duplicate recognition cease to exist. Remaining host parser
recognition, generated token ABI debt, lexical observer zero, Parser SHC,
Compiler B, and FTCFTW remain open. GAP-145 remains **OPEN**.

## Match-scrutinee suffix face transferred (2026-09-02)

Match scrutinee parsing formerly selected field, method-call, and ordinary-call
suffixes through a host `tok.kind` switch. Whole-pack `event` now carries the
exact parenthesized-call face in its remaining lane-one bit and composes it with
the already-produced dot and colon faces. Zig consumes that settled suffix face
and retains only suffix parsing and materialization. The matching host switch is
deleted. No ABI, event word, allocation, cursor walk, fallback, or semantic
concept was added.

The focused consumer gate covers the producer, consumer, host-switch deletion,
tracked projection, and planted old-vs-new controls. This slice does not
remeasure or alter pattern entry, reserved backtick, macro/anchor,
subject-method-reference, or any earlier primary face.

Semantic diff: before, the immutable producer pack carried the three exact
suffix token identities while Zig reconstructed match-scrutinee continuation
from `tok.kind`; after, `lib/compiler/parser.id` produces that selection and Zig
only invokes the selected field, method, or call materialization. Subjects,
methods, arguments, values, locations, descriptors, worlds, demands, and
realization facts are unchanged. The host suffix switch and its duplicate
recognition cease to exist. Remaining host parser recognition, generated token
ABI debt, lexical observer zero, Parser SHC, Compiler B, and FTCFTW remain open.
GAP-145 remains **OPEN**.
