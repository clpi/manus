| field | value |
|---|---|
| title | GAP-250 — native.id: mulred hangs on multiply by 6364136223846793005 |
| status | OPEN |
| filed | 2026-09-12 |
| priority | P0 |
| kind | crash |
| files | lib/compiler/native.id |

| section |
|---|---|
| Hang |

| # | directive |
|---|---|
| 1 | Compiling a program containing `x = x * 6364136223846793005` never terminates; nativebench had to be killed (no output after 120s). |
| 2 | The same shape with 2862933555777941757, 1442695040888963407, or 123456789123456789 compiles instantly. |
| 3 | 9223372036854775783 hangs the same way. Found 2026-09-12 while validating the adversarial benchmark corpus (case mulc). |

| section |
|---|---|
| Reproducer |

| # | directive |
|---|---|
| 1 | Any program with a pending multiply by the constant; minimal: |

```text
x = 1
i = 0
while i < 10
  x = x * 6364136223846793005
  i = i + 1
x
```

| # | directive |
|---|---|
| 1 | Run: `nativebench < repro.id` — hangs, no output. |

| section |
|---|---|
| Suspected mechanism |

| # | directive |
|---|---|
| 1 | The pending-multiply path calls mulred(code, prd, pendname, pv, names) to strength-reduce the constant multiply. |
| 2 | mulred's shift-add decomposition likely loops on constants whose bit pattern defeats its reduction step (both hanging constants end in ...2D / ...F7; non-hanging neighbors differ). |
| 3 | A hang is worse than a slow path: mulred needs a termination bound or a fallback to plain mul when no reduction applies. |

| section |
|---|---|
| Suggested next step |

| # | directive |
|---|---|
| 1 | Read mulred in lib/compiler/native.id; find the loop that fails to terminate for these constants. |
| 2 | Add a fuel counter with fallback to `imm(r10, pv); mul(rd, rd, r10)`. |
| 3 | Regression test: compile the reproducer with a timeout in the verify suite's build path. |
