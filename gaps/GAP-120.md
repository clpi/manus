# GAP-120 — File boundaries lack durable semantic concept identity

**Status:** OPEN · **Filed:** !2026-08-09T20:08:31Z
**Priority:** P1 · **Kind:** expressiveness_gap
**Filed by:** codex_semantic_normalization
**Files:** src/semantic_graph.zig,docs/spec/constitution.md

C0 law.file.one now requires each file to be the durable home of one semantic concept across ownership, docs, tests, transforms and generated projections. The live graph and MCP can inventory paths but do not yet attach or validate a durable concept identity, so utility buckets and unrelated declaration bundles cannot be rejected semantically. Implement from the persistent graph/manifest, not filename regexes; expose the same structured finding through compiler, LSP, MCP and generated docs.

## 2026-09-02 — the adjudicator refuses the utility bucket (this worktree)

The demand join and the stated refusal shape landed on the graph, produced from
the same persistent graph as the identity — never from a filename regex. Slice:

- `ConceptFact.shared_demand_count` (`src/semantic_graph.zig`): how many
  distinct caller relations drive two or more RESOLVED applications onto this
  module's relations, gathered while `publishConceptIdentities` already holds
  the applications (`law.fact.producer.one`: one producer, counted not
  decided). A caller that applies the same relation twice counts once.
- `ConceptRefusal` — module, reason enum (`no_shared_demand` today; a future
  reason joins the enum, not a second column), and the exact applied cohort
  (`split_start`/`split_len` into the packed `concept_split` column), in graph
  id order. `publishConceptRefusals` runs AFTER the identities it reads; it
  refuses exactly when two or more relations are applied from elsewhere and
  no caller drives two of them — the gap's "relation cohort sharing no
  demand". A module with fewer than two applied relations, or with a shared
  caller, is NOT refused.
- `conceptRefusal` / `conceptRefusalSplit` answer only from the refusal
  column; a damaged row answers null, never a re-derivation (test-enforced:
  removing the row index loses the refusal).
- `conceptRefusalRows` walks produced rows only (not counted): absent rows
  refuse, damaged rows refuse, and produced rows answer the caller who
  reads this one surface. It is what `gate/concept.sh` holds the graph to.
- JSON schema 16 -> 17: `refusals[]` (module, reason, split) plus
  `shared_demand` on every `concepts[]` row, so compiler, MCP and docs
  readers answer the same structured finding. `writeJson` re-verifies every
  refusal row and its cohort before writing; damage refuses the export.
- CONSUMER ENFORCEMENT: `gate/concept.sh` + `gate/concept.subjects` refuse
  the produced cohort over the export's `refusals[]` (module, reason,
  split) and read `shared_demand` as the demand evidence on the same row;
  a produced row whose evidence contradicts it is damage, not a verdict.
  Three subjects, three demanded verdicts:
  `bucket` (no subject drives both -> refused);
  `cohort` (one subject drives both -> pass);
  `unwitnessed` (nothing applies one relation -> pass; one applied
  relation is not a cohort). Zero examined subjects fails (GAP-201). A
  shape the gate cannot materialize, or a roster row naming one, FAILS —
  the roll is read from `materialize`'s arms themselves, never a second
  list.
  MEASURED (reproducible — `sh gate/concept.sh`):
      bucket demanded refused, measured refused
      cohort demanded pass, measured pass
      unwitnessed demanded pass, measured pass
      3 subject(s) examined, 0 failed — exit 0
  The graph test "the adjudicator refuses a module its environment asks
  nothing of" (src/semantic_graph.zig) is the test-side lane's own
  conviction of the same produced finding (`shared_demand == 0`,
  `refusals.len == 1`), and gate/concept.sh's own measurement of the same
  refused cohort is the exported lane's.

  The parse boundary the gate's bucket shape had to respect — a body whose
  tail is a bare call or literal (with no intermediate binding on the
  line) cannot be followed by a declaration at the CLI while the same
  source parses through the test layer's parser configuration — is
  parser-shape debt owned by the toolchain lane, not this gap's. The
  subjects here use multi-segment bodies (`x = plus(2, 3)\n  x + 1`)
  that both parser configurations accept, and declare relations in the
  `name(params): type` face the CLI accepts after the GAP-145 transfers
  (the `name: type = (params)` face is refused in both lanes after the
  transfers — same toolchain-lane debt, recorded here so the next agent
  does not re-derive it). The test-lane sources in src/semantic_graph.zig
  carry the same `name(params): type` face: the concept tests migrate heads
  only, bodies and counts untouched.

  Verified on origin/main `540f83b2` (this worktree, commands not counts):
  `sh gate/concept.sh` -> 3 subject(s) examined, 0 failed;
  `zig build unit-test` -> all 5 concept tests pass and the full-suite
  failing set loses exactly those 4 concept names while gaining 0 (the
  remaining failures are the GAP-145 transfer debt in other lanes, owned
  elsewhere); `sh gate/architecture-negative.sh` -> PASS (36 check(s));
  `sh gate/admission.sh` -> ADMISSION OK.

Measured in this worktree (isolated verify copy differs from baseline by
`src/semantic_graph.zig` ONLY; `src/parser/projection.c` and
`tools/node/dev/parser/artifact` in the shared worktree belong to
codex-gap145-lbrace's live GAP-145 lane and were held at HEAD for both runs):

    zig build unit-test --summary none  (verify)  -> 1760 pass, 110 fail, 2 crash
    zig build unit-test --summary none  (base)    -> 1715 pass, 154 fail, 3 crash
    failing-set diff: +45 passing, -44 failing; all four concept/refusal tests
      pass; remaining failures in both runs are the parser-face debt this
      host already carries (DNB004 lineage) and crash poisoning — 0 new
      failure names introduced by the slice.
    sh gate/architecture-negative.sh -> PASS (36 check(s))
    sh gate/admission.sh             -> ADMISSION OK (0 new std. spellings)
    run ./tools/node/dev/parser/artifact -> FAIL in BOTH runs (base and
      verify) at lib/compiler/parser.id under the direct runner — the
      pre-existing direct-run blocker recorded below, not this slice.

Test repair in the slice: the shared-caller cohort test undercounted its own
source (`main` applies plus twice and minus once — 3 resolved applications,
not 2); the expectation now matches the program the test holds.

Still OPEN (what this slice did not do):

- CONSUMER ENFORCEMENT: LANDED at gate/concept.sh + gate/concept.subjects
  (this worktree, measured above). The produced refusal reaches a gate over
  the export's `refusals[]` and `shared_demand`; a gate that reads zero
  produced rows for a demanded bucket fails, a gate that reads a row where
  the evidence exists fails, and the same three verdicts the proven graph
  answers over the test lane answer over the export here.
- LSP/MCP/generated-docs dedicated projections of `concepts[]`/`refusals[]`
  (reachable through `writeJson`, no dedicated surface reads them yet).
- `gate/layering.sh` is BLOCKED repo-wide: `src/effect.zig` (landed on
  origin/main in 29f241f32) has no row in `gate/layers.manifest`, so
  `gate/admission-all.sh` fails on any tree including origin/main. Not this
  slice's file; recorded as an exact fact for its owner.

## 2026-09-03 — the explain document projects the produced verdict (this worktree)

The first dedicated docs/MCP face beyond the `refusals[]` consumer: `idol
explain` renders `"concept"` — the same produced verdict `writeJson` exports
as `concepts[]`/`refusals[]`, as one object a docs generator, MCP reader, or
LSP surface consumes without parsing the full graph JSON. Slice:

- `SemanticGraph.appendConceptVerdictJson` (`src/semantic_graph.zig`): every
  identity row re-read through `conceptIdentity` with the same
  re-verification `appendConceptsJson` performs; every refusal walked through
  `conceptRefusalRows` — the produced consumer surface `gate/concept.sh`
  holds — so the explain projection and the gate hold the same rows. Damage
  refuses the render (`InvalidConceptFact` / `DamagedConceptRefusal`), never
  a verdict the graph no longer backs: fail-closed, matching the MCP
  projection law (`tools/mcp/TOOLS.md`).
- `do_explain` (`src/main.zig`) renders `,"concept":` between `assumptions`
  and `transform_provenance`. The key is additive; no existing key moved
  (`tools/node/dev/explain-gate` asserts presence only, and the additive-key
  precedent is `transform_provenance` / `transform_registry` themselves).
- Unit test "the explain verdict projects the produced concept finding":
  the same bucket the gate holds answers one identity row with
  `shared_demand == 0` and one `no_shared_demand` refusal naming the
  two-relation split with the produced home on both; removing the produced
  refusal index refuses the render.

  MEASURED (reproducible, commands not counts):
      zig build -> exit 0
      zig build unit-test -> failing-set diff against the pre-slice run is
        exactly one name leaving the set (the new test); zero names enter.
        The four remaining `concept`/`refusal`-named failures are the
        pre-existing transfer debt in other lanes (native_backend, parser,
        quotient_synth, sema), identical before and after.
      sh gate/concept.sh -> 3 subject(s) examined, 0 failed (regression
        hold of 2977463b, not a remesure of it)
      sh gate/architecture-negative.sh -> PASS (36 check(s));
        sh gate/admission.sh -> ADMISSION OK
      idol explain bucket.id | jq .concept agrees with
        idol graph bucket.id | jq '{concepts, refusals}' on home,
        shared_demand, reason, and split; the cohort explains with
        refusals[] and shared_demand 1.

Still OPEN (what this slice did not do):

- MCP server dedicated tools: `tools/mcp/native.id` still serves
  `status`/`head`/`orient` only, and the compiler-query `idol-native`
  server lives in another repository with no local checkout on this host.
  No MCP tool reads `.concept` yet; the explain document is the surface one
  would project.
- LSP surface and generated-docs generator: the verdict now leaves the
  process inside `explain`, but no LSP/docs reader consumes it yet.
- `tools/node/dev/explain-gate` is RED on current origin/main independent
  of this slice: its fixture `examples/cfloor/fact.id` uses the retired
  `name: type = (params)` face and is refused at parse (GAP-145 transfer
  debt, toolchain lane). This slice's fixtures use the transferred
  `name(params): type` face both parser configurations accept.
- `gate/layering.sh` is BLOCKED repo-wide (unchanged): `src/effect.zig`
  has no row in `gate/layers.manifest`. Not this slice's file.

## 2026-09-01 — concept identity landed on the graph (aa48675c3)

The graph now ATTACHES a durable concept identity per file boundary, produced
from the persistent graph and never from a filename regex. Slice:

- `ConceptFact` (`src/semantic_graph.zig`): identity = the module's dotted
  home (`self.home` for the root, the node's `foreign_home` for a reached
  partition — the same `homeOfPath` derivation the definer made); composition
  = the graph's own census over that home (relations declared inside it,
  descriptor shapes it owns, applications resolved onto its relations via
  `ApplicationFact.target`).
- One producer, `publishConceptIdentities`, runs at the END of checked lift
  (`law.fact.producer.one`). A graph lifted without the checked pipeline has
  no rows and `conceptIdentity` refuses to synthesize one; damaging the row
  index cannot recover an identity from the path (test-enforced).
- Structured finding exposed through the compiler's graph export: JSON
  schema version 15 -> 16, `concepts[]` plus a `concept` face on each module
  node, so compiler, MCP and tooling readers answer the same fact. LSP and
  generated docs still consume `writeJson` indirectly; a dedicated LSP
  surface and doc generation remain OPEN below.

Measured at `aa48675c3` (origin/main), commands not counts:

    zig build unit-test --summary none
      -> failure set byte-identical (md5 a29820a1dae6fd0edb62b2605121989f)
         to a pristine baseline worktree at the parent commit; +2 passing
         concept tests; 0 new failures in any lane.
    sh gate/architecture-negative.sh   -> PASS (36 check(s))
    sh gate/admission.sh               -> ADMISSION OK (0 new std. spellings)
    git diff -U0 -- '*.id' > diff; idol run gate/idiom.id < diff
      -> the pre-existing direct-run blocker fires on any invocation on this
         host (DNB004 lineage), and the diff carries 0 `.id` lines.

Still OPEN (what this slice did not do):

- ADJUDICATION: the producer publishes composition facts; nothing yet
  REJECTS a utility bucket or an unrelated declaration bundle. The split
  decision (a relation cohort sharing no demand) needs the demand column
  joined to `ConceptFact` and a stated refusal shape.
- LSP/MCP/generated-docs own projections of `concepts[]` (the JSON fact is
  reachable through `writeJson`, but no dedicated surface reads it yet).
- `gate/layering.sh` is BLOCKED repo-wide: `src/effect.zig` (landed on
  origin/main in 29f241f32) has no row in `gate/layers.manifest`, so
  `gate/admission-all.sh` fails on any tree including origin/main. Not this
  slice's file; recorded as an exact fact for its owner.
