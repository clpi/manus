| field | value |
|---|---|
| title | GAP-238 — `local function` published a graph linkage no definer lowered, so the .lua→wasm caller held a symbol nothing defines |
| status | CLOSED |
| filed | !2026-08-25T09:20:00Z |
| closed | !2026-08-25T09:20:00Z |
| priority | P1 |
| kind | capability_regression |
| filed by | claude-ci-diagnosis |
| files | src/dnir_lower.zig,gate/wasm/local.sh,build.zig |

| section |
|---|---|
| Defect |

| # | directive |
|---|---|
| 1 | zig-out/bin/idol compile examples/hello.lua --target wasm32-wasi error: wasm32-wasi: no native realization (UnsupportedProgram) hint: refused at: call_direct/call-target-unknown hint: in relation: main |

| # | directive |
|---|---|
| 1 | Measured at `fc48acea` (this filing's parent) and, per the CI diagnosis that surfaced it, identically at pre-#164 `7b025afe`. |
| 2 | The refusal's producer is `src/wasm_backend.zig:2033` (`emitCallDirect`; the tail-call arm at `:2022` is the same lookup): `e.func_index.get(ins.callee)` finds no function named `idol_examples_hello__greet`, because no such function was ever lowered. |

| # | directive |
|---|---|
| 1 | The same program spelled in `.id` — module-level `greet = (name)` with the same two `print` applications — compiles and executes. |
| 2 | So does the same `.lua` program with `function greet(name)` instead of `local function greet(name)`. |
| 3 | The isolated semantic difference is one AST bit: `ast.FuncDecl.is_local`, the Lua `local function` face. |

| section |
|---|---|
| The bisect: both ingests publish the SAME callable-linkage fact |

| # | directive |
|---|---|
| 1 | `idol graph` on both ingests answers structurally identical graphs. |
| 2 | Both carry |

| # | directive |
|---|---|
| 1 | "callable_linkages":[{"callable":1,"origin":"idol","exposure":"internal", "symbol":"idol_examples_hello__greet"}] |

| # | directive |
|---|---|
| 1 | and both resolve the application to `"target":{"card":"one","id":1}`. |
| 2 | The .lua ingest does NOT fail to publish the linkage fact — `sema.note_callable_linkage` (`src/sema.zig`, since 320c2433) publishes an internal row for every checked declaration, local or not, and `liftModuleWithCheckedCalls` (`src/semantic_graph.zig:5844` region) copies it onto the callable id and sets `callable_linkage_required`. |

| # | directive |
|---|---|
| 1 | The two consumers of that one fact then disagreed: |

| # | directive |
|---|---|
| 1 | **Caller side** — `dnir_lower.linkageForTarget` (`src/dnir_lower.zig:8267`) emits the graph-owned symbol as the `call_direct` callee. Correct. |
| 2 | **Definer side** — `dnir_lower.shouldIncludeFuncDecl` (`src/dnir_lower.zig:3315`) answered `false` on `fd.is_local` BEFORE reading `declarationLinkage`, so the declaration never entered module-function lowering; the root body skips `func_decl` statements outright (`src/dnir_lower.zig:4818`). The published linkage was left with no body. |

| # | directive |
|---|---|
| 1 | Result: a call site holding an internal symbol no function defines. |
| 2 | Silent disappearance of a definer whose linkage the graph published is a `law.fallback.zero` shape; a source spelling (`local`) selecting realization after resolution violates the semantic-identity law, and the AST flag acted as a second producer of the definer set against the graph-owned linkage (`law.fact.producer.one`). |

| section |
|---|---|
| Suspect commit |

| # | directive |
|---|---|
| 1 | 320c2433 "semantic: make callable linkage graph-owned" moved the caller side and the export-name derivation onto the graph fact and demoted the `@ffi` attribute reading to the no-graph-facts arm of `shouldIncludeFuncDecl` — but left the `is_local` early-return ahead of the graph fact. |
| 2 | The commit's own direction (graph owns callable linkage) is what the fix completes; the defect is that one predicate kept reading the retired source face first. |

| section |
|---|---|
| Fix (this change) |

| # | directive |
|---|---|
| 1 | `shouldIncludeFuncDecl` now consults `declarationLinkage` first. |
| 2 | When the graph published this callable's linkage, the graph owns the definer set: `is_local` and the `@ffi` attribute are read only on the legacy no-graph-linkage arm. |
| 3 | A graph-owned local declaration therefore lowers exactly as its `.id`-spelled equivalent does — scope stays a graph fact and is untouched. |

| # | directive |
|---|---|
| 1 | Measured after the fix, same tree: |

| # | directive |
|---|---|
| 1 | zig-out/bin/idol compile examples/hello.lua --target wasm32-wasi # ok wasmtime hello.wasm Hello from duo! |
| 2 | Hello, world! |

    # byte-identical to the .id equivalent's wasm stdout; exit 0

| # | directive |
|---|---|
| 1 | Negative control (unchanged behavior): a genuinely unknown call target still refuses by name, at its owner — |

| # | directive |
|---|---|
| 1 | print(mystery("world")) # .lua error: use of undeclared global 'mystery' |

| # | directive |
|---|---|
| 1 | and the backend refusals `call-target-unknown` / `call-signature-unknown` remain live for any callee that reaches emission undefined. |

| section |
|---|---|
| Acceptance |

| # | directive |
|---|---|
| 1 | `sh gate/wasm/local.sh` — the pin. Compiles `examples/hello.lua` to wasm32-wasi, executes it under wasmtime, and compares stdout byte-for-byte against the `.id`-spelled equivalent compiled through the same realization; plus the refuse-by-name negative control. Registered as build step `wasm-local` beside `wasm-global`. |
| 2 | `zig build` and `zig build unit-test` green (measured: 1765 passed, 62 skipped, 0 failed). |
| 3 | No wasm-backend workaround: `src/wasm_backend.zig` unchanged; the callee is never reconstructed from a name. The one producer of callable linkage remains the graph. |
