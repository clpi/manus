# GAP-030 — a `gmatch` nested inside a `gmatch` drops iterations

**Status:** open · **Filed:** 2026-08-07 · **Frequency:** 1
**Cited at:** `scripts/run_benchmark.duo` (`extract_times`)

(Renumbered from 028, which a parallel session claimed for a Duo-parser gap
while this was being written — see the recorded gap-numbering collision.)

## Repro

```duo
txt = "a Time \t1.5\tsec\nb Time \t2.5\tsec\nc Time \t3.5\tsec"

n = 0
for line in string.gmatch(txt, "([^\n]+)")
    n = n + 1
end
-- n == 3, correct

m = 0
for line in string.gmatch(txt, "([^\n]+)")
    for w in string.gmatch(line, "([^%s]+)")
        m = m + 1
    end
end
-- m == 4.  Expected 12 (3 lines x 4 words).
```

The outer loop alone is correct, so the inner `gmatch` corrupts the outer
iterator's state. No diagnostic; the loop simply ends early.

## Why it mattered

`extract_times` used exactly this shape and returned **2** timings for a
40-benchmark run. Every row printed `N/A`, so every winner defaulted to C and
the suite reported Duo losing all 40 benchmarks for a reason that had nothing
to do with speed.

## Interim

One match per line instead of an inner iterator:

```duo
num = string.match(line, "Time%s+([0-9%.eE%+%-]+)")
```

## Note

`gmatch` is a deny row in Epoch 2 (`scan(grammar)(s)` is canonical), so the
long-term fix may be to land `scan` rather than repair the iterator. Filed
against the behaviour either way: code in the tree uses it today, and silently
losing iterations survives review.
