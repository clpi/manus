# GAP-142 - concurrent merge candidates lack one truthful admission boundary

**Status:** SUPERSEDED · **Filed:** !2026-08-10 · **Priority:** P0 · **Kind:** agent

## SUPERSEDED — verified 2026-08-23 at `dec7d509`

| # | directive |
|---|---|
| 1 | **Verdict:** SUPERSEDED as filed. **Superseded by:** `gate/admission.id` plus `gate/admission.sh` -- the two-rule quarantine-and-census admission boundary this gap asked for -- with `evidence/mop/merge/readiness.md` as the merge ledger. |
| 2 | All three exist and `gate/admission.sh` runs inside `gate/admission-all.sh`, which `.githooks/pre-commit` invokes before anything else, so the boundary is enforced rather than described. |

| # | directive |
|---|---|
| 1 | Every specific merge candidate this document adjudicates is retired: |

| # | directive |
|---|---|
| 1 | git branch -a \| grep -cE 'recovery/jul-30\|test-coverage-analysis\|wasm-wasi-compat-perf\|pass104-evaluator\|pass108-grammar\|gap083-mangle\|gap065-for-effect' -> 0 |

| # | directive |
|---|---|
| 1 | The overclaim it was filed to stop never landed either: `lib/compiler/application.id` opens "Application structural roles (documentation only)" and publishes no instruction, byte-range or object projection. |

| # | directive |
|---|---|
| 1 | **Where the residue went.** Two live observations survive and both already have owners: the branch surface is large, and concurrent sessions can mutate a shared working tree -- that is `GAP-115`, which owns evidence corruptibility. |
| 2 | This gap's 873 lines were an adjudication of a specific merge window, and that window is closed. |

## Observed defect

| # | directive |
|---|---|
| 1 | Current concurrent work contains useful changes, but the combined tree cannot be admitted as one SHC advance. |
| 2 | Three independent boundaries currently contradict their evidence or ownership contract. |

### Application ownership overclaim

| # | directive |
|---|---|
| 1 | The staged `GAP-137` wording says public machine-byte lineage is already owned by `lib/compiler/application.id`. |
| 2 | That module explicitly says it preserves a structural shape and does not observe identities. |
| 3 | Its `application` descriptor contains no instruction, byte-range, object, allocation, or transformation projection. |
| 4 | The unstaged wording correctly keeps public application-to-machine lineage as a separate unowned requirement. |

| # | directive |
|---|---|
| 1 | The staged wording must not merge. |
| 2 | A schema is not production ownership, and source-level application shape is not machine-byte lineage. |

### Subject-authority integration collision

| # | directive |
|---|---|
| 1 | Session 1 correctly requires an admitted semantic subject role and refuses to infer subject from parameter position, parameter spelling, or colon syntax. |
| 2 | Session 2 correctly consumes the graph subject without reconstructing it. |
| 3 | However, several in-flight Session 2 lineage fixtures still declare top-level relations such as `observe(value)` or `observe(subject)` and invoke them through a subject-call face. |
| 4 | After Session 1 lands, those fixtures no longer prove the subject-bearing checked path. |

| # | directive |
|---|---|
| 1 | The combined merge must use an existing admitted descriptor-home subject fixture for subject-bearing lineage and retain a separate subjectless ordinary call fixture. |
| 2 | No fallback subject inference may be added to make the tests pass. |

| # | directive |
|---|---|
| 1 | The locked combined `zig build unit-test --summary all` run is currently red: **1168/1175 passed, 7 failed**. |
| 2 | Three failures are directly in the in-flight checked-call expansion: |

- `checked ordinary call reaches regions and machine lineage` passes
  `ordinary-object` to an API accepting only the real `native-object` or
  `native-mach-o` targets, so the claimed object-lineage proof never runs;
- `checked call result descriptor does not select argument ABI` is refused by
  `lowerModuleFromGraph`, so its intended ABI invariant is unproved;
- `discarded checked calls do not retain return registers` reports 28 branch
  instructions where the lineage helper requires exactly one, exposing
  overlapping or over-wide machine ranges for repeated calls.

| # | directive |
|---|---|
| 1 | These are merge blockers. |
| 2 | A newly added test that fails before its asserted boundary is not evidence, and a lineage range containing unrelated calls is not exact machine provenance. |

| # | directive |
|---|---|
| 1 | A later exact locked run against the current dirty tree reports **1171/1178 passed, 7 failed**. |
| 2 | The count moved as concurrent tests were added, but the admission result did not change. |
| 3 | The ordinary and record lineage controls still use target spellings the real object API rejects, and the descriptor/argument control is still refused before its intended invariant. |
| 4 | Physical target validation also still begins with `instruction.callee` through `functions_by_symbol`; therefore the checked slice has not yet eliminated name selection at that boundary. |
| 5 | The other four failures are the interpolation index, register-spill, root-relation projection, and relation-family controls. |
| 6 | This exact result supersedes any current dirty-tree ledger claim of a passing unit gate. |

| # | directive |
|---|---|
| 1 | Commit `559c85b` landed this realization slice on `main` while these admission conditions remained open. |
| 2 | It is partial implementation, not a closed checked call boundary. |
| 3 | The performance ledger was corrected immediately afterward so the landed history no longer presents the red unit result as a pass. |

| # | directive |
|---|---|
| 1 | The post-merge repair makes the two object-lineage controls use the production target and derives argument register placement independently of the result descriptor. |
| 2 | The exact locked unit result improves to **1174/1178**, with all three failures introduced by `559c85b` resolved. |
| 3 | The gap remains open for the other combined admission requirements and the four independently owned unit failures; this repair does not manufacture a green aggregate. |

| # | directive |
|---|---|
| 1 | After `59d3e0e` adds one passing target-selection control, the exact locked result is **1175/1179**, with the same four failures: interpolation recognition, register allocation, root relation projection, and relation derivation. |
| 2 | The new control preserves the baseline but does not exercise the producer's multiple-target relation cardinality described below. |

### Value

| # | directive |
|---|---|
| 1 | The next in-flight checked-call expansion is not admissible in its observed form. `appendCheckedRecordOperand` accepts a semantic argument only when its source expression is a name, joins that binding spelling with each field spelling, and queries `ctx.locals` for the resulting string. |
| 2 | A value produced by another application is then rejected because those host field locals do not exist. |

| # | directive |
|---|---|
| 1 | That rebuilds meaning from a source name instead of realizing a semantic value. |
| 2 | It also turns a structured value into a place merely because the earlier host lowerer represented a local aggregate as separate field locals. |
| 3 | More tests around the bridge cannot make it authoritative. |

| # | directive |
|---|---|
| 1 | The slice must instead consume graph-owned value projections or an explicit realization slot map keyed by semantic value identity. |
| 2 | Descriptor matching may select a lawful layout, but binding and field spellings cannot identify its physical values. |
| 3 | If the graph does not yet expose those projections, this structured-argument expansion is implementation blocked and must remain unsupported rather than extending the text bridge. |

### Subject

| # | directive |
|---|---|
| 1 | The in-flight application producer currently makes parse recognition supply semantic subject authority. |
| 2 | The parser creates a hidden `ambient-subject` parameter for a descriptor-home relation and marks that parameter with the subject role. |
| 3 | Sema then reads the parser field as the resolved role. |

| # | directive |
|---|---|
| 1 | The parser may retain the descriptor home and ambient invocation face as source provenance. |
| 2 | It may not decide the semantic subject or require a hidden source binding to make that role exist. |
| 3 | Sema and the graph must resolve the subject from the admitted home and relation facts, then make the resolved value available to the body without treating the synthetic spelling as identity. |

| # | directive |
|---|---|
| 1 | The latest expansion deepens that ownership error. `ast.OperandRole` creates a syntax-tree taxonomy for subject, level, operand, world, and demand; the parser assigns `.subject`, and macro expansion copies the role. |
| 2 | A bare ambient anchor also falls back to a synthetic `"self"` name when no homed binding is present. |
| 3 | Those are semantic application roles, not parser or macro facts. |
| 4 | Carrying them farther through the host syntax tree does not make them authoritative. |

| # | directive |
|---|---|
| 1 | The syntax projection may retain the descriptor home, declared parameter shape, ambient face, and exact span. |
| 2 | The semantic resolver must produce role edges and the graph must own their identities. |
| 3 | Macro expansion may preserve syntax provenance, but it must neither copy nor invent semantic role facts. |

| # | directive |
|---|---|
| 1 | The branch test claiming that subject role is independent of operand order does not close this requirement: it mutates a parameter role directly in the AST before Sema runs. |
| 2 | That proves a consumer accepts an injected role, not that production semantic authority creates it. |
| 3 | The claim must remain partial until an admitted producer supplies the role without parser ownership or test mutation. |

### Origin

| # | directive |
|---|---|
| 1 | The same producer currently admits `ApplicationConstant.nil` and publishes it to graph constant facts without retaining the source language, origin, or law. |
| 2 | Native Idol does not admit bare `nil`; compatibility and foreign sources may retain their own case only with exact provenance. |
| 3 | The graph must either carry that origin and law with the value or decline to publish it as a native semantic constant. |

### Pack

| # | directive |
|---|---|
| 1 | The application producer currently derives a two-slot result pack from the parser field `ret_fallible`. |
| 2 | Both slots cite the same application expression; the second slot has no descriptor, label, constant, or independently resolved failure value. |
| 3 | The graph then mints two value incarnations from that fabricated shape. |

| # | directive |
|---|---|
| 1 | A syntax flag may preserve that a source face declared a failure alternative. |
| 2 | It cannot prove the cardinality, cases, correspondence, or values of the semantic result pack. |
| 3 | Native Idol requires a result union and an obligated failure relation; an anonymous second slot is not that relation, and absence of a descriptor is not a failure case. |

| # | directive |
|---|---|
| 1 | The result pack must be projected from the resolved result descriptor and failure semantics. |
| 2 | Each produced value needs its own identity and case, descriptor, position or label, provenance, and demand correspondence. |
| 3 | Until that producer exists, cardinality is unknown. |
| 4 | It must not be guessed from `ret_fallible`, tuple syntax, or return spelling. |

### Realization

| # | directive |
|---|---|
| 1 | Commit `59d3e0e` removes symbol-first selection for the checked native slice, but replaces it with one physical function per relation. |
| 2 | The native validator indexes functions by `function.semantic_identity`, rejects a second function with the same relation as `function-identity-collision`, and selects linkage from the application relation alone. |

| # | directive |
|---|---|
| 1 | The application producer deliberately proves the opposite cardinality. |
| 2 | Its overload control gives the `i64` and `f64` implementations of `measure` one relation identity, distinct selected target identities, and distinct result descriptors. `applicationTarget` is already the graph projection of the exact selected implementation. |
| 3 | One relation may therefore have more than one lawful target and realization. |

| # | directive |
|---|---|
| 1 | The native map conflates semantic meaning with selected realization. |
| 2 | It will either refuse a valid second implementation or route an application to the wrong linkage and ABI. |
| 3 | A symbol map was not semantic authority, but a relation map with one-value cardinality is not a valid replacement. |

| # | directive |
|---|---|
| 1 | The checked path must carry and consume the selected target identity in addition to relation, application, value, subject, operand, and descriptor identity. |
| 2 | Relation validates semantic meaning. |
| 3 | Target selects a physical implementation. |
| 4 | Linkage and ABI are facts of that selected target. |
| 5 | A link symbol may validate the projection but may never select meaning or target. |

| # | directive |
|---|---|
| 1 | The deletion gate is a graph-owned target projection carried through DNIR and validated at machine emission. |
| 2 | Two implementations sharing one relation must remain distinct targets without a collision, and changing only their linkage must preserve relation, application, and value lineage. |

### Evidence

| # | directive |
|---|---|
| 1 | The exact serialized unit gate on the uncommitted application producer worktree reports **1168/1191 passed, 22 failed, 1 crashed**. |
| 2 | Current `main` reports **1174/1178**, with four known failures. |
| 3 | The producer therefore adds nineteen failing or crashing tests rather than closing its claimed boundary. |

| # | directive |
|---|---|
| 1 | The regressions include callable result recognition, ambient invocation, pretty-printer abort, graph application validation, checked DNIR identity, eight checked native lineage paths, two additional codegen paths, and the branch's own ambient-subject test. |
| 2 | The test added to distinguish ambient and ordinary zero-argument calls fails before finding the homed relation it claims to prove. |

| # | directive |
|---|---|
| 1 | Command: |

```text
./zig-out/bin/duo run scripts/duo_lock.id # zig build unit-test #summary all
```

| # | directive |
|---|---|
| 1 | This result is an admission failure, not a request to weaken existing tests or merge only the passing producer assertions. |
| 2 | The branch must reconcile with the landed checked-identity path and restore the baseline before any part is presented as production application authority. |

| # | directive |
|---|---|
| 1 | The latest isolated-cache rerun against the application worktree does not reach tests. |
| 2 | It fails compilation at four downstream joins: |

- two `dnir_lower.zig` sites still read the removed `Node.stable_id`;
- one `region_graph.zig` site still reads the removed `Node.stable_id`; and
- one `dnir_lower.zig` site still calls the removed textual
  `funcResultDescriptor` query.

| # | directive |
|---|---|
| 1 | An earlier run without an isolated cache reported the current main result (`1182/1185`) and main source paths despite targeting the branch. |
| 2 | That artifact is not branch evidence. |
| 3 | The reproducible command binds the branch build file and an empty local cache: |

```text
./zig-out/bin/duo run scripts/duo_lock.id # \
  zig build #build-file <application-worktree>/build.zig \
  #cache-dir /tmp/idol-application-audit-cache unit-test #summary all
```

| # | directive |
|---|---|
| 1 | The producer therefore remains compile-blocked until it rebases onto the exact handle consumer or supplies compatible exact graph joins. |
| 2 | Restoring removed hash/name APIs is not an admissible repair. |

### Evidence-integrity change contains unrelated canonical migration

| # | directive |
|---|---|
| 1 | The claimed GAP-115 slice is private per-run evidence storage and provenance. |
| 2 | Its dirty diff also performs broad identifier and callable-face rewrites across many gates and adds new `std.script` authority such as `runid`, `provenance`, `fs_tmpdir`, `getenv`, `run`, `capture`, and `status` usage. |
| 3 | The additions retain namespace authority, sentinel absence, command concatenation, and historical `end` forms. |
| 4 | They are not justified by private temporary-directory ownership and do not satisfy the canonical Idol preflight. |

| # | directive |
|---|---|
| 1 | The private-path repair is valuable, but it must be separated from the broad canonical rewrite. |
| 2 | No new public `std.script` vocabulary or use site may be counted as Idol convergence. |
| 3 | The deletion gate is a world-backed Idol filesystem/process/environment realization, not a larger compatibility home. |

| # | directive |
|---|---|
| 1 | The required exact-diff preflight was run across the claimed GAP-115 and LSP files in that reviewed snapshot. `scripts/idiomgate.id` reported **125 findings on added lines** with all 20 positive controls passing. |
| 2 | Findings include `law.authority.home`, `law.lua.hosted`, `law.subject.one`, output materialization, underscore-bearing identities, and deleted `end`. |
| 3 | That snapshot therefore failed its own merge gate independently of this review; live findings must be regenerated. |

| # | directive |
|---|---|
| 1 | The separately synthesized diff for every untracked `.id` candidate reports **149 additional findings**, again with all 20 controls passing. |
| 2 | These include deleted block closure, underscore and uppercase identities, Lua `nil` and `require`, capability homes, and output materialization. |
| 3 | Untracked state is not an exemption: each file must reach zero findings before it is staged as canonical repository Idol. |

## Exact identity lookup

| # | directive |
|---|---|
| 1 | The active application producer correctly describes `StableId.hash` as a collision-prone fingerprint, but its `id_index` still maps that fingerprint directly to one `NodeId`. `findId` recomputes only the hash and returns the indexed node without checking kind, scope-qualified path, generation, or any other reversible identity material. `addNode` also keeps the first node on a collision without reporting it. |

| # | directive |
|---|---|
| 1 | That makes the API comment claiming an exact identity lookup false. |
| 2 | A `NodeId` is exact only after it has been selected by an exact join; wrapping a hash-selected node in `NodeId` does not make the selection exact. |
| 3 | Current shape queries such as `findTableShape` and `findEnumShape` consume this path, and new application descriptor publication may therefore inherit a wrong descriptor node after a collision. |

| # | directive |
|---|---|
| 1 | The hash may remain a lookup acceleration. |
| 2 | The index must retain enough exact material to verify the complete key before returning a node, or the producer must use declaration-owned node joins where an exact declaration already exists. |
| 3 | A forced-collision control must prove that two distinct identities are both retrievable or that publication fails closed; silently selecting the first node is not admissible. |

## Current exact-handle candidate

| # | directive |
|---|---|
| 1 | The active realization candidate removes the downstream `SemanticRef` wrapper and carries the graph's compact `NodeId` through DNIR, regions, transforms and machine lineage. |
| 2 | That correctly demotes `StableId.hash` from semantic selection, and the independently locked native census passes at **96 native / 206 reachable**, with 110 reachable programs still bailing. |

| # | directive |
|---|---|
| 1 | It is not yet a durable identity closure. |
| 2 | The candidate scopes every handle by storing a raw `*const SemanticGraph` owner pointer. |
| 3 | That distinguishes two live graphs at different addresses, but it cannot distinguish graph deinit followed by a new graph incarnation at the same address. |
| 4 | The graph owner must provide a resident lease/incarnation contract before downstream may describe pointer plus node as an exact lifetime-safe identity. |
| 5 | Cross-snapshot continuity remains an explicit relation, never an inferred property of the pointer or node number. |

| # | directive |
|---|---|
| 1 | The independently locked unit run on the same shared tree is red: **1182/1185 passed, 3 failed**. |
| 2 | The failures are interpolation recognition, ordinary root relation projection, and relation derivation. |
| 3 | The pending performance entry says `zig build unit-test` passed; that statement must be corrected before the candidate commits even if all three failures are pre-existing or owned elsewhere. |
| 4 | The independently locked native differential also remains red at **47 agree / 20 diverge / 5 unsupported**. |
| 5 | These are honest baseline results, not reasons to reject the handle reduction itself. |

## Delimiter-derived identity

| # | directive |
|---|---|
| 1 | Current main still records `.braced` and `.parenless` invocation forms in the AST, and `CallShape.identityHash` includes `invocation_form`. |
| 2 | Under delimiter closure, braces identify structured/descriptor source syntax and parentheses identify ordinary call syntax; neither source face may create a second semantic application identity after resolution. |
| 3 | Compatibility faces may remain syntax provenance, but punctuation must contribute zero relation, application, pack, value or selected-target identity bits. |

| # | directive |
|---|---|
| 1 | The application producer owns the active parser/AST slice, so this is a merge condition rather than a competing repair. |
| 2 | GAP-092 is open until callable braces and string-postfix calls are retired from canonical recognition and invocation form no longer selects semantic call shape. |

## Application identity root

| # | directive |
|---|---|
| 1 | The active producer correctly deletes `StableId`, `stable_id`, `id_index`, and path/hash lookup from the graph. |
| 2 | Its replacement then wraps every graph handle in `SemanticIdentity { meaning, content, incarnation }`, where the first two coordinates are usually unknown and the third contains the `NodeId`. |
| 3 | That is a second identity schema. |
| 4 | Under the current root ruling, the graph entity is the identity. |
| 5 | Meaning, content, source continuity, and cross-snapshot correspondence are facts or relations involving graph entities, not parallel identity coordinates attached to each field. |

| # | directive |
|---|---|
| 1 | The producer also lets a Sema `ApplicationCandidates` allocation survive resolution as `fact.relation`. |
| 2 | Candidate groups are initially indexed by source name, the pointer is cast to an opaque relation witness, and graph `RelationPlan` groups applications by that pointer to mint relation nodes. |
| 3 | Names are valid source-resolution input, but neither a source-name candidate registry nor a host allocation address may become the resolved relation identity. |
| 4 | Sema must publish an admitted relation fact whose graph entity the graph can retain directly; deleting the candidate allocation must not change meaning. |

| # | directive |
|---|---|
| 1 | This is independent of the valuable pack correspondence work in the same branch. |
| 2 | Pack slots, labels, positions, descriptors, constants, provenance, and cardinality should remain graph facts while the parallel identity wrapper and candidate-pointer witness are removed. |

## Unmerged branch admission

| # | directive |
|---|---|
| 1 | The following branches predate current C0 grammar or duplicate superseded compiler architecture and must not be merged wholesale: |

- `evalmerge` and `pass104-evaluator`: a large parser-projection evaluator with
  historical `end` syntax and a parallel evaluation model;
- `pass108-grammar`: an obsolete grammar authority plus independent
  conformance/fuzz tables;
- `gap083-mangle`: broad C-codegen ownership and historical script vocabulary;
- `gap065-for-effect`: useful differential cases mixed with old DNIR/codegen
  changes and historical harness vocabulary;
- `audit-ownership-ab5f51c`: a historical gap report plus a codegen patch;
- `native-runtime-globals` and `untyped-bails`: findings may be reconciled, but
  their old branch state is not merge authority.
- `worktree-pass81-family-registry`: eighteen historical commits add pass plans,
  ten independent census scripts, compatibility `nil` type behavior, parser
  expansion, host codegen decisions, and another DNIR operation. Its findings
  may be remeasured, but the branch is a superseded authority bundle.
- `worktree-agent-ad323c867f702f091`: ambient fields are implemented through a
  parser `slot_receiver`, the literal host spelling `self`, a Sema AST rewrite,
  method-name tables, and DNIR reconstruction through a synthetic `math` home.
  This directly conflicts with the current subject and application owners.
- `worktree-agent-aae952b91324af64f`: a 615-line standalone basis scan plus a
  build gate observes source text but does not make the declared compiler basis
  reachable from compiler B or C. It is proof surface, not bootstrap authority.
- `worktree-agent-a3e43a721419bf110`: constant-branch fixtures contain useful
  differential cases, but the branch combines them with C symbol spelling and
  AST-shaped DNIR tail-effect reconstruction. Re-derive the cases against graph
  demand and current lowering; do not merge the host repairs wholesale.
- `worktree-perf-losing-rows`: its dense-access measurements are useful, but the
  implementation specializes a boxed `VAL_NUMBER` inside C codegen by scanning
  AST names and assignments. It preserves the Lua box, creates host-only loop
  authority, and cannot support a direct-native C-floor or SHC claim.
- `rescue/u64-codegen`: the rescue tree is not an isolated unsigned fix; its
  remaining delta includes guarded string-face name tables and backend method
  dispatch. Preserve any unsigned boundary fixture separately and re-derive the
  lowering from descriptor and relation facts.
- `claude/wasm-wasi-compat-perf-HhvNU`: useful Wast cases are mixed with a
  737-line host-codegen diff, 567 lines of new shell test drivers, broad
  distribution changes, Lua examples, and a tracked `zig-out/bin/duo` binary;
  this is the opposite of the current Idol-owned Wasm and compiler boundary;
- `claude/test-coverage-analysis-AYYVb`: its compile-fail runner repair is mixed
  with a historical shell implementation and broad host `main`/codegen edits.

| # | directive |
|---|---|
| 1 | Individual evidence or tests may be re-derived against current main and C0. |
| 2 | Branch existence is not evidence that its implementation remains admissible. |

## Admission requirements

1. `GAP-137` must distinguish the Idol application schema from observed
   production graph facts and from machine/object lineage.
2. Session 1 and Session 2 must pass together with no parameter/name/colon
   subject inference and no callee-text semantic fallback.
3. Subject-bearing fixtures must obtain subject identity from admitted semantic
   authority; subjectless fixtures must preserve authoritative absence.
4. GAP-115 must land only its claimed evidence-integrity behavior or explicitly
   re-scope and pass current canonical gates on every touched Idol line.
5. Added canonical Idol must introduce zero `std.script` authority, sentinel
   absence, output concatenation plumbing, uppercase/underscore identifiers,
   or historical block ceremony.
6. Every stale branch must be re-derived as a bounded current-main patch. No
   wholesale merge is admissible.
7. The combined tree must run focused Session 1/2 tests followed by the locked
   unit gate. A green result from either branch in isolation is insufficient.
8. Checked structured arguments must not require a source name, reconstruct
   `binding.field` keys, or infer places from host locals. Their values and
   projections must be selected by semantic identity.
9. Parse recognition must not create semantic subject role or use a hidden
    binding spelling as subject identity. Sema and graph authority produce the
    role from admitted relation and home facts.
10. Compatibility `nil` must not enter native constant facts without explicit
    origin and law provenance.
11. The application producer must restore the current unit baseline and pass
    its own producer, graph, DNIR, and machine lineage controls together.
12. Native target selection must consume the graph-selected target identity;
    one relation may map to multiple descriptor-, demand-, or target-specific
    implementations without collision or name selection.
13. Result pack cardinality and slots must come from resolved result and failure
    facts. Parser fallibility or tuple syntax may remain provenance but cannot
    fabricate values or cases.
14. Parser and macro syntax records must not own or copy application role
    categories. Ambient source faces and homes remain provenance until semantic
    resolution supplies the role edges.
15. Every graph query described as exact must compare or reversibly intern the
    complete semantic identity. A fingerprint may accelerate lookup but may not
    select a node without collision verification.
16. A resident graph handle must be scoped by a graph-owned lifetime contract
    that cannot alias a later graph incarnation at the same memory address.
17. Evidence must report the exact locked aggregate outcome. The current unit
    gate is red at 1182/1185 and the native differential is red at 47/20/5;
    neither may be summarized as a pass.
18. Invocation delimiter provenance must not contribute semantic call-shape or
    application identity after resolution.
19. Graph entities must be carried directly as identity. Meaning/content/
    incarnation continuity may not become a parallel identity coordinate
    schema.
20. A Sema candidate-set pointer may remain transient resolution state, but it
    may not mint or select a graph relation. The resolved relation must survive
    deleting or reallocating the candidate set.
21. Renaming `identityHash` to `fingerprint` is necessary but insufficient:
    specialization and transformation lineage must not select or identify
    resolved meaning through callee/method spelling or fingerprint-only
    coordinates.

## Positive controls

- Changing only a checked call's callee spelling must fail before machine
  emission while its relation identity remains unchanged.
- Removing a subject edge from a subject-bearing application must fail; adding
  no subject to an admitted subjectless application must remain valid.
- A staged claim that `application.id` owns machine bytes must fail review
  until an actual Idol projection can answer application to instruction and
  object byte ranges.
- The evidence-integrity patch must still prevent two concurrent runs from
  sharing scratch artifacts after all unrelated canonical rewrites are removed.
- A structured value produced by an application must remain eligible for the
  same checked call as an equivalent bound literal; changing its source binding
  spelling must not alter argument realization.
- Changing parser source-face metadata must not change a resolved subject role
  when the relation and home facts are unchanged. Removing the semantic role
  producer must fail closed rather than falling back to parser position.
- A compatibility `nil` value and any admitted native absence case must remain
  distinguishable after graph publication.
- Two implementations with one relation identity and different target
  identities must both link to the application that selected them. Removing
  target identity must fail closed rather than choosing by relation or symbol.
- A declared failure alternative with no resolved result case must leave pack
  cardinality unknown. Supplying resolved success and failure cases must create
  distinct value identities with preserved correspondence.
- Two deliberately colliding fingerprints with different exact identities must
  never resolve to the same node. The graph must preserve both or refuse the
  collision before publishing either as an exact relation, value, or
  descriptor identity.
- Destroying and recreating a graph at the same address must not make an old
  module, region or machine lineage valid against the new graph incarnation.
- Equivalent source faces that resolve to one relation, subject and operand
  pack must retain that semantic shape even when their delimiter provenance
  differs. Distinct occurrences still retain distinct application identities.
- Reallocating or deleting Sema candidate storage after resolution must not
  change relation identity. A graph lift that lacks the resolved relation fact
  must fail closed rather than grouping by candidate pointer or source name.

## Current producer gate

| # | directive |
|---|---|
| 1 | The active application worktree was measured after its changed files had been quiet long enough for a stable snapshot. |
| 2 | Its exact Idol diff passes `idiomgate` with zero findings and all 20 controls, but the locked unit command does not compile: |

```text
src/macro_expand.zig:1151:45: error: no field named 'block' in struct 'ast.Block'
const tail = binding.rhs.func_expr.body.block.tail_expr ...
```

| # | directive |
|---|---|
| 1 | The command exits before any application, graph, DNIR, or machine lineage test runs. |
| 2 | This supersedes the earlier partial unit count for the current dirty branch state. |
| 3 | The producer must first restore compilation, then meet or improve the current main unit baseline; a clean Idol schema diff alone is not producer evidence. |

## Idol identity migration

| # | directive |
|---|---|
| 1 | The production frontend accepts equivalent canonical `.id` and historical `.id` inputs under one language law, but current main does **not** give them the same graph identity. |
| 2 | The executed differential below proves that the path-derived `StableId` projection changes every serialized identity. |
| 3 | The source-family ingress is therefore established; semantic identity equivalence is not. |

| # | directive |
|---|---|
| 1 | The current snapshot also serializes identities with a `duo:` project-brand prefix, while origin language, importer, calling convention, and pass mode use `duo` as a current label. |
| 2 | Session 1 must remove the project brand from native semantic identity rather than replace the prefix with `idol:`. |
| 3 | Project identity is provenance, not a relation qualifier. |
| 4 | Current-language origin labels should become Idol while historical source family remains explicit provenance. |

| # | directive |
|---|---|
| 1 | The deletion gate is an `.id`/`.id` differential with identical resolved relation, application, value, pack, descriptor, world, effect, demand, and witness facts after normalization; only source-family provenance may differ. |

## Executed source-suffix negative control

| # | directive |
|---|---|
| 1 | Current main was exercised with byte-identical copies of `examples/shc/application.id`, one named `application.id` and one retaining the historical suffix. |
| 2 | Both pass the production checker. |
| 3 | Direct-native object emission produced byte-identical objects with the same SHA-256 digest, proving that the suffix does not select machine realization for this slice. |

| # | directive |
|---|---|
| 1 | The graph differential failed the stronger identity requirement. |
| 2 | The graph topology and `source_hash` matched, while every serialized `stable_id` differed. |
| 3 | The only input difference was the source path, and current `SemanticGraph.computeStableId` hashes `module_path`. |
| 4 | Thus renaming historical source to `.id` changes the identity projection even when source content and resolved structure are unchanged. |

| # | directive |
|---|---|
| 1 | This is a P0 negative control, not migration success. |
| 2 | Closure requires removing path-derived `StableId` authority so exact graph entities own identity and the suffix survives only as source provenance. |
| 3 | The repeated differential must then show equal resolved relation/application/value/pack facts while allowing only the explicit source-family provenance to differ. |
| 4 | Byte-identical machine output alone is insufficient. |

## Post-merge evidence defect in `cb31638`

| # | directive |
|---|---|
| 1 | `cb31638` correctly removes the realization-owned fingerprint wrapper and carries compact `NodeId` handles for its checked direct subset. |
| 2 | It also records the raw graph-address lifetime limitation and the required graph-owned lease, so this is accepted as a migration bridge rather than durable continuity. |

| # | directive |
|---|---|
| 1 | Its appended performance entry nevertheless says `zig build unit-test` passed. |
| 2 | An independent serialized run on committed main after `cb31638` remains red: **1182/1185 passed, 3 failed**. |
| 3 | The failing controls are parser interpolation indexed holes, ordinary root relation visibility, and relation `eq` derivation. |
| 4 | This reproduces the previously recorded integration result exactly. |

| # | directive |
|---|---|
| 1 | The commit therefore advances fingerprint-free realization identity for its declared subset, but its aggregate unit evidence is false and must be corrected in `docs/performance.md` by that file's active owner. |
| 2 | The exact graph lease and red aggregate remain open; neither blocks describing the narrower handle migration, and neither may be reported as closed. |

## Current router projection regression

| # | directive |
|---|---|
| 1 | The active uncommitted rewrites of `.agents/AGENT_CANONICAL.md` and `.agents/AGENT_INTEGRATION.md` restore the retired name as the current language name even though checked-in `AGENTS.md`, `CLAUDE.md`, C0, and `docs/spec/AUTHORITY.md` identify the current language as Idol and canonical source as `.id`. |
| 2 | That rewrite would make the session router contradict the authority it tells agents to read. |

| # | directive |
|---|---|
| 1 | The integration rewrite also embeds a dated MCP tool census as setup truth. |
| 2 | An independent serialized `zig build mcp-gate` now passes on the exact dirty tooling snapshot: `duo-bench` serves 32 tools, `duo-lsp` serves 31, and `zls` serves 4. |
| 3 | The gate also proves real claim acquire, refusal, listing and release behavior plus representative value-returning handlers. |
| 4 | This validates the recorded census for that dirty snapshot. |
| 5 | It does not bind the result to a commit, repair the router's project-name conflict, or reopen the already closed MCP transport of this running agent session. |

| # | directive |
|---|---|
| 1 | The router changes must not merge until current project/source identity agrees with C0, `.id` is described only as historical source provenance, and every MCP count/status claim cites a fresh serialized gate over the exact committed implementation. |

## Current call-compaction evidence defect

| # | directive |
|---|---|
| 1 | The active realization candidate removes the separate argument-placement row for one checked `i64` operand with an `i64` result. |
| 2 | Its focused controls are useful: they reconstruct the staged form, prove identical ARM64 assembly/text and Mach-O object bytes, and preserve distinct nested application lineage. |

| # | directive |
|---|---|
| 1 | The appended performance entry nevertheless says `zig build unit-test` passed. |
| 2 | The exact serialized command on the corrected, narrowed dirty snapshot exits 1: |

```text
./zig-out/bin/duo run scripts/duo_lock.id # \
  zig build unit-test #summary all
```

| # | directive |
|---|---|
| 1 | Result: **1187/1190 passed, 3 failed**. |
| 2 | The failures are interpolation indexed holes, ordinary root relation projection, and independent `eq` derivation. |
| 3 | The four added compaction controls are among the passing tests, so this is not a rejection of the compaction. |
| 4 | It is a rejection of false aggregate evidence. |

| # | directive |
|---|---|
| 1 | Before merge, the ledger must report the exact red aggregate, distinguish its passing focused controls from the repository gate, and bind every other census, differential, smoke, and benchmark result to the same tested snapshot. |
| 2 | A known baseline failure remains a failure; it may be classified, never renamed a pass. |

## Current application worktree integration blockers

| # | directive |
|---|---|
| 1 | The active application producer worktree is still based before `cb31638` and current main. |
| 2 | Its present uncommitted state cannot be integrated until it rebases onto the exact-handle realization contract and removes these remaining parallel authorities: |

- `src/sema.zig` groups application candidates through a source-name map and
  candidate allocation pointers. That storage may help resolution, but neither
  a spelling nor an allocation address may mint or select the graph relation.
- `src/parser.zig` and `src/ast.zig` lower leading ambient-subject syntax through
  a hidden `ambient-subject` binding and a method-call category. The parser may
  preserve the source face; the resolver must establish the subject edge.
- invocation form remains part of `CallShape.identityHash`, so parentheses,
  braces, and parenless compatibility spelling still change semantic
  call-shape selection after resolution.
- postfix braces are still recognized as ordinary calls in the branch even
  though C0 now reserves braces for structured packs and descriptor
  application.

| # | directive |
|---|---|
| 1 | The latest worktree revision improves one part of this list: `CallShape` now calls its derived hash a fingerprint, excludes invocation delimiter provenance from that fingerprint, and verifies candidate equality with exact stored facts. |
| 2 | That is a valid collision-safe acceleration repair. |
| 3 | It is not graph identity closure: |

- the exact comparison and fingerprint still contain callee and method source
  spellings, so host codegen specialization remains name-selected after
  resolution;
- codegen passes the fingerprint as both the input and output coordinates to
  `transform_engine.logProvenance`, whose public record contains only hashes;
  and
- no graph application, value, selected target, or transformation entity is
  retained by that log.

| # | directive |
|---|---|
| 1 | Calling the hash a fingerprint makes its physical role more honest, but a fingerprint-only transform record is not semantic lineage. |
| 2 | The deletion gate is a graph-owned transformation projection carrying exact input application and value entities, exact produced value or realization entities, and the transformation entity. |
| 3 | A fingerprint may accompany that projection for cache lookup or integrity, but deleting every fingerprint must leave transformation meaning and lineage unchanged. |

| # | directive |
|---|---|
| 1 | The current producer must not widen into a parallel codegen provenance repair to make its application claim pass. |
| 2 | It may either leave this existing host bridge explicitly classified and bounded, or consume an exact transformation interface from the realization owner when that interface exists. |

| # | directive |
|---|---|
| 1 | These are merge blockers, not follow-up style debt. |
| 2 | A rebased producer must prove relation selection survives deleting candidate storage, ambient subject resolution survives without the hidden parser binding, and delimiter provenance contributes zero bits to semantic identity. |

## Closure

| # | directive |
|---|---|
| 1 | Close only after the conflicting owners have landed together on current main, the mixed GAP-115 diff has been split or repaired, and the stale branches are either explicitly rejected or reduced to independently validated current-law patches. |

## 2026-08-10 branch salvage census

| # | directive |
|---|---|
| 1 | After fetching every remote at `7c43ae6`, GitHub reports zero open pull requests. |
| 2 | Thirty-six local or remote refs are not ancestors of `origin/main`. |
| 3 | Nineteen add `.id`, none add `.id`, twenty-six add `std.*`, and fourteen add `lua_Value`/`lua_table_*`/`lua_to_*`/`lua_val_*` reachability. |
| 4 | These counts are ref counts rather than unique commits because several local/remote names share one tip; they are an admission census, not a completion metric. |

| # | directive |
|---|---|
| 1 | No ref is mergeable wholesale under current law. |
| 2 | That does not make deletion the default when a branch still contains a required behavior. |
| 3 | For every old branch: |

1. identify the exact still-current semantic or physical obligation and its
   current owner;
2. reproduce the defect or preserve the useful fixture against current main;
3. rederive the smallest patch using one `id`, one vocabulary, canonical `.id`,
   and no new `std`, Lua fallback, compatibility mode, or host semantic owner;
4. run the focused negative control and the serialized combined admission;
5. commit and push the bounded current-main repair promptly so other sessions
   audit the same public state; and
6. retire the old ref only after the replacement evidence is public.

| # | directive |
|---|---|
| 1 | The zero-`.id` branches with small host diffs are not automatic deletions or automatic merges. |
| 2 | For example, the six-line temporary-path security change on `origin/jules-12741752911094824883-f3c3066b` must be rechecked against current physical path ownership and reimplemented on main if the defect survives. |
| 3 | The large grammar, recovery, Wasm, parser, and codegen branches must be mined the same way: preserve valid laws, fixtures, and measured algorithms; do not revive their source family, identity wrappers, semantic taxonomies, namespace roots, or fallback architecture. |

## 2026-08-10 staged GAP-138 correction review

| # | directive |
|---|---|
| 1 | The staged source-substrate change preserves a valid location/traversal obligation and correctly deletes the `std.compiler.source` namespace owner, byte-zero EOF sentinel, and unused scratch caller. |
| 2 | It is not yet admissible as the replacement: |

1. `examples/pass16_source_module_proof.id` adds 45 lines of new project-owned
   `.id` implementation after SOURCE-ONE. Moving a deleted library body into a
   searchable historical-source example preserves the training surface rather
   than closing it.
2. The replacement is a pass-number example and not production compiler
   authority. A build-wired smoke fixture can prove behavior but cannot support
   the staged GAP-138 claim that the source substrate is an executed Idol-owned
   relation set.
3. `pos <= c:len()` followed by `c:at()` reconstructs presence as a boolean
   query guarding a partial operation. This is the PREDICATE-ZERO and
   query-to-transition shape the settlement itself says remains
   `vocabularyblocked`; it cannot be labeled canonical merely because byte zero
   is no longer returned.
4. `formatloc` retains explicit text-concatenation and conversion plumbing;
   `while` plus cursor reassignment transliterates the host traversal shape; and
   the fixture continues to teach the historical `t.id` source suffix.
5. Renaming this file to `.id` would not repair the semantic findings. The
   current gate must block the new source until the absence/case and iteration
   vocabulary are admitted or the proof is reduced to structured foreign
   evidence with no native implementation claim.

| # | directive |
|---|---|
| 1 | Correct the staged work rather than discarding its useful obligation: retain the deletion of genuinely unused `std` authority only if exact current-main reachability proves it is dead; keep the source/location requirement OPEN as `vocabularyblocked`; place no replacement implementation in `.id`; and move the production boundary only when canonical `.id` executes in the real compiler path. |
| 2 | Commit and push the corrected bounded slice promptly, then retire the old carrier after that public proof exists. |

## 2026-08-10 Jules temporary-path branch disposition

| # | directive |
|---|---|
| 1 | The valid obligation from `origin/jules-12741752911094824883-f3c3066b` is already preserved on current main. |
| 2 | Old commit `d6a1995` identified a real TOCTOU window: `mkstemp`, close, unlink, then append a suffix before later use. |
| 3 | Public main commit `3eb8c31` rederived the repair rather than merging the branch: |

- POSIX constructs the final suffixed template and reserves it with `mkstemps`;
- Windows preserves the suffix and uses `O_CREAT | O_EXCL`;
- the descriptor is closed without unlinking the reserved path; and
- the pinned test requires both exclusive creation paths and rejects
  `unlink(tmpl)`.

| # | directive |
|---|---|
| 1 | `git blame` binds the production body and control to `3eb8c31`. |
| 2 | The branch's second commit only changes CI Zig pins to `master` and carries no surviving security obligation. |
| 3 | This ref is therefore retire-ready without losing work; do not cherry-pick or merge either old commit. |
| 4 | Remote-ref deletion remains a separate repository-administration action, not evidence that the defect was fixed. |

## 2026-08-10 public branch retirement

| # | directive |
|---|---|
| 1 | No pull request was opened and no private merge queue was created. |
| 2 | After a fresh `git fetch origin --prune`, each of these remote tips was proved to be an ancestor of `origin/main` with `git merge-base --is-ancestor` and then deleted from `origin` in one public push: |

- `canonical-to-relation`
- `claude/wasm-runtime-benchmark-ibuf77`
- `gap081-conversion`
- `gap082-conversion-algebra`
- `jules-11101233073844159445-0fcb9db9`
- `jules-16533213871356411533-61cad70a`
- `jules-2378565021381472090-2420083d`
- `kiro/pass2-pass3-convergence`
- `pr-7`
- `semantic-example-corpus`

| # | directive |
|---|---|
| 1 | The separately audited `jules-12741752911094824883-f3c3066b` ref was also deleted from `origin` after the disposition above proved its valid obligation present on main and rejected its unpinned-CI remainder. |
| 2 | Dirty local worktrees and local branch refs were not removed. |
| 3 | Unique remote commits remain until this same obligation-first review either rederives their valid work on current main or proves that current main already owns it. |

## 2026-08-10 rejected unique carriers

| # | directive |
|---|---|
| 1 | Five additional remote refs contain unique commits but no admissible implementation to preserve. |
| 2 | Their surviving obligations are already explicit in current owners, so retaining the branches would preserve obsolete training surface rather than reduce future work: |

- `recovery/jul-30` is an incomplete transcript reconstruction with generated
  artifacts, sidecars, binaries, and source that its own notes report does not
  compile. Current gaps and production differentials, not reconstructed files,
  own any surviving obligation.
- `claude/test-coverage-analysis-AYYVb` adds a duplicate Wasm command, retired
  shell-runner changes, and unmeasured generated-C policy. Its only valid
  backgrounding invariant is already satisfied by the current serial runner.
- `claude/wasm-wasi-compat-perf-HhvNU` adds benchmark-name policy, silent
  network/coroutine/time stubs, boxed sentinel behavior, distribution roots,
  shell harnesses, and a tracked compiler binary. Surviving Wasm capabilities
  are already independently present on main; current evidence defects belong
  to the current harness owner, not this compatibility bundle.
- `pass104-evaluator` reparses an S-expression text projection into a parallel
  evaluator with string dispatch, sentinels, and duplicated syntax knowledge.
  Evaluation remains downstream of the open parser/graph frontier recorded in
  `docs/bootstrap.md`; none of this carrier can implement that boundary.
- `evalmerge` adds the same evaluator plus brace/text recovery and a
  literal-specific lowering proof through fixed temporary paths. Current law
  requires resolved application ids and graph facts to reach lowering without
  formatted-text recovery.

| # | directive |
|---|---|
| 1 | These refs were retired from `origin` after the obligation-first audit. |
| 2 | The final four unique carriers were also retired: `pass108-grammar` is superseded by GAP-134/GAP-145, `gap083-mangle` by the current GAP-083 symbol-collision owner, `gap065-for-effect` by GAP-067/GAP-072 and the current differential owner, and `ci-cd-deployment-pipeline-5472020287481652681` by the current release-workflow owner. |
| 3 | Their unique commits remain in Git history as evidence; none remains a merge source or architectural template. |

## 2026-08-10 rejected historical-source rewrite

| # | directive |
|---|---|
| 1 | At public revision `9a4bd5a`, the shared dirty snapshot modified 24 tracked `.id` files and held 16 untracked `.id` candidates. |
| 2 | This is not a native source migration. |
| 3 | The changed scripts cosmetically rewrite historical syntax and helper names while retaining `std.script`, pass-number organization, sentinel and predicate control flow, host text parsing, and no production-executed `.id` owner. |

| # | directive |
|---|---|
| 1 | The snapshot also contains concrete evidence regressions: |

- `scripts/audit100.id` still calls tracked `.id` canonical authority, while
  its edited evidence cites nonexistent `scripts/convertcanonical.id` and the
  corrupted path `/tmp/duo_audit100canonicalical.list`;
- `scripts/abi_matrix.id` prints wrong-answer rows before its four-count line,
  then parses the first four whitespace fields as counts, so the failure case
  corrupts its own verdict; and
- `scripts/native_census.id` still removes programs from the denominator by
  lexical `std.compiler` spelling and recognizes compile success from output
  text rather than a process-bound outcome.

| # | directive |
|---|---|
| 1 | The untracked candidates are also inadmissible project-owned source. |
| 2 | They teach `std.*`, `nil` and predicate cases, historical project branding, pass-era organization, and host lexical ledgers. |
| 3 | Do not stage this snapshot. |
| 4 | Preserve the useful obligations, including private scratch ownership and exact process outcomes, then rederive them from canonical Idol authority instead of canonicalizing historical `.id` in place. |
