| field | value |
|---|---|
| title | GAP-241 — `p:len()` on a pack answers 0 while `p.len` answers 3 |
| status | OPEN |
| filed | !2026-09-06T06:30:00Z |
| priority | P1 |
| kind | correctness_gap |
| filed by | named-claude-oracle-20260906T061533 |
| files | src/dnir_lower.zig, lib/table.id, examples/text/brace/hole.id |

| section |
|---|---|
| The defect |

| # | directive |
|---|---|
| 1 | One fact, two faces, two answers, at exit 0, on both realizations that execute on this host. |
| 2 | The canonical subject-first face is the wrong one. |

| # | directive |
|---|---|
| 1 | main: i64 = () p = {10, 20, 30} a = p:len() print("{a}") 0 |

| subject | `--backend=c` | `--backend=wasm` |
| --- | --- | --- |
| `p = {10, 20, 30}` ; `p:len()` | **0** | **0** |
| `p = {9}` ; `p:len()` | **0** | **0** |
| `p = { a = 1, b = 2 }` ; `p:len()` | refused `operation-not-in-c99-slice` | **0** |
| `p = {10, 20, 30}` ; `p.len` | 3 | 3 |
| `p = {10, 20, 30}` ; `p[2]` | 20 | 20 |
| `s = "abc"` ; `s:len()` | 3 | 3 |

| # | directive |
|---|---|
| 1 | The last three rows are the controls. `p[2]` answering 20 says the pack holds the three members as written, so the 0 is not an empty pack. `p.len` answering 3 says the length is already a fact the compiler holds. `s:len()` answering 3 says `:len()` is not blind — it is right on text and wrong on a pack. |

| section |
|---|---|
| The producer |

| # | directive |
|---|---|
| 1 | `src/dnir_lower.zig:12736` — the `len` arm of the method-call lowering emits `str_len` against whatever `lowerExpr` returned for the subject, with no question asked about which descriptor that subject has: |

| # | directive |
|---|---|
| 1 | if (std.mem.eql(u8, mc.method, "len") and mc.args.len == 0) { ... try ctx.emit(.{ .op = .str_len, .result = t, .lhs = base }) |

| # | directive |
|---|---|
| 1 | `str_len` is a scan to the first NUL in both realizations. |
| 2 | Run against a pack slot it finds one immediately, which is where the 0 comes from and why both backends agree on it — the wrongness is upstream of either. |

| # | directive |
|---|---|
| 1 | Two places in the same file already hold the missing fact: |

| # | directive |
|---|---|
| 1 | `src/dnir_lower.zig:8193`, in the descriptor answer, states the rule this arm does not apply — *"The subject still must be text: `:len` on a table is a different relation owned elsewhere."* |
| 2 | `src/dnir_lower.zig:13882`, `exprIsIntegral`, guards the identical method name with `exprIsStr(ctx, mc.obj)`. |
| 3 | `staticTableLen` / `ctx.table_lens` (`src/dnir_lower.zig:9386` onward) already bind the length of a positional pack, and are what `p.len` reads to answer 3. |

| # | directive |
|---|---|
| 1 | So this is not a missing capability. |
| 2 | It is one arm of one dispatch that does not consult a subject fact its own file computes, records and uses elsewhere — `law.fact.producer.one`, and the same shape GAP-207 convicted for the byte face: a face the arm does not name is admitted by default and answered wrong. |

| section |
|---|---|
| Blast radius, measured not assumed |

| # | directive |
|---|---|
| 1 | `lib/table.id:3` declares `len` over a table and its own append idiom is `out[out:len() + 1] = k` (lines 11, 18). |
| 2 | That idiom does **not** reach the wrong answer today — it is refused before lowering: |

| # | directive |
|---|---|
| 1 | out = {} out[out:len() + 1] = 10 -> refused, graph-dnir-unsupported (both) |

| # | directive |
|---|---|
| 1 | `p = {}` ; `p:len()` is refused too (`call_extern/extern:duo_hash_new`). |
| 2 | The reachable shape is the one at the top: a positional pack literal bound to a name, `:len()` read into a value. |
| 3 | That is what the corpus should not be able to write again without a red row. |

| section |
|---|---|
| The oracle that found it |

| # | directive |
|---|---|
| 1 | `examples/text/brace/hole.id` declared `pack=3` for `{1, 2, 3}:len()` and no realization ever produced it — the file was refused whole at `binop-not-lowered:concat`, so its declared answer had never been compared. |
| 2 | The file now measures `p.len` (3) and `p[2]` (20) and names this gap for the face that does not answer. |
| 3 | It does **not** pin 0. |

| section |
|---|---|
| Repair |

| # | directive |
|---|---|
| 1 | Guard the `len` arm on the subject descriptor: text keeps `str_len`; a pack answers from `table_lens`/`staticTableLen`, which is what `p.len` already does; anything else refuses by name rather than falling through to a NUL scan. |
| 2 | The refusal must be named — an unnamed exit is the outcome `gate/byte/face.sh` already refuses to call a refusal. |

| # | directive |
|---|---|
| 1 | `src/dnir_lower.zig` was claimed by `claude-GAP-207-20260906T030713` when this was filed, so no compiler code moved in the filing land. |

| section |
|---|---|
| Measured with |

| # | directive |
|---|---|
| 1 | `zig build` at this worktree, `--backend=c --emit=c --target=c-source` linked against `tools/node/dev/grammar/idol_c_runtime_shim.c` with `cc -O2`, and `--backend=wasm --target=wasm32-wasi` run under `wasmtime`. `idol run` reports `✓` on this host without executing, so no row above was read off it. |
