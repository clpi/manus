| field | value |
|---|---|
| title | GAP-246 — zerotrip: -120.32% loss vs gcc; provably zero-trip inner loop still costs 2.2x |
| status | CLOSED |
| filed | 2026-09-12 |
| priority | P1 |
| kind | performance_gap |
| files | bench/programs/zerotrip.id, bench/programs/zerotrip.c, lib/compiler/native.id |

| section |
|---|---|
| Measured loss |

| # | directive |
|---|---|
| 1 | v6 baseline (2026-09-12T00:11:52, commit ae5532b2, 21 interleaved rounds, 3 warmup, median primary): |
| 2 | idol median 0.004734s vs gcc median 0.002149s (clang 0.002247s): loss, margin -120.32%, p=0.0000 (significant). |
| 3 | Worst loss in the corpus. Tracked in bench/RESULTS.md history section; any commit that moves it is visible there. |

| section |
|---|---|
| Reproducer |

| # | directive |
|---|---|
| 1 | bench/programs/zerotrip.id (idol native) vs bench/programs/zerotrip.c (clang/gcc -O3); correctness gate passes, exit codes agree. |

```text
t = 0
o = 0
while o < 10000000
  i = 10
  while i < 5
    t = t + 1
    i = i + 1
  o = o + 1
t
```

| section |
|---|---|
| Why this should not lose |

| # | directive |
|---|---|
| 1 | The inner loop is provably zero-trip on every outer iteration (i = 10, condition i < 5). |
| 2 | lib/compiler/native.id has a dedicated compiler.opt.zerotrip pass (landed e7c7bd0e) plus a countdown-loop fast path (cd==1) in the emitter. |
| 3 | gcc/clang delete the inner loop entirely; idol at best skips it at runtime but keeps the per-iteration setup cost. |
| 4 | Suspect: the zerotrip pass does not fire on this shape, or the countdown prologue/epilogue still emits per-outer-iteration work that dominates. |

| section |
|---|---|
| Suggested next step |

| # | directive |
|---|---|
| 1 | Dump the nativebench object for zerotrip.id; check whether the inner loop body is fully eliminated. |
| 2 | If the pass fires but the loss persists, the cost is in outer-loop codegen (bound reload, branch shape); compare against clang -O3 -S. |

| section |
|---|
| Resolution |

| # | directive |
|---|---|
| 1 | Closed 2026-09-12 as stale: the -120.32% was measured at the v6 baseline (ae5532b2, 2026-09-12T00:11:52) before the zerotrip fix e7c7bd0e landed (2026-09-12T02:23:24). Not a regression. |
| 2 | Verified on current main (d7bfebd4): the reproducer compiles to 7 straight-line ARM64 instructions plus ret; the inner loop is fully eliminated by the zerotrip pass plus LICM and cttry. |
| 3 | Correctness: idol, clang -O3, and gcc -O3 binaries all exit 0. bench/verify/verify.py --opts nest --quick: 20/20 pass, 0 mismatches. |
| 4 | The gap is closed by construction: both idol and gcc now emit loop-free code for this program. The section-4 suspicion (pass does not fire) is refuted. |
