# GAP-167 — COLLISION-ZERO machinery ownership closure

**Status:** OPEN · **Priority:** P0 · **Kind:** architecture
| # | directive |
|---|---|
| 1 | **Owner:** Cursor / bootstrap bridge deletion |

## Obligation

| # | directive |
|---|---|
| 1 | Idol already owns application resolution, projection/reachability, anchored facts, worlds, demand, transformation, realization, and provenance. |
| 2 | Bootstrap code and project-owned source must not introduce second owners named as machinery (router, gateway, dispatch, registry, manager, adapter, bridge, engine, context, pipeline, etc.). |

| # | directive |
|---|---|
| 1 | Law projection: `docs/spec/collision-zero.md`. |

## Gate

| # | directive |
|---|---|
| 1 | `gate/idiom.id` enforces `law.collision.zero` and `law.application.one` on **added lines** via decomposed checks (no `machinery()` mashed kingdom): |

- `collision(name)` in `eq` — identifier/path stem role classification
- selector `flag(..., "router(", ...)` rows in `adj`
- `subject(slotindex)` — rejects `semantic[`, `proof[`, `home[` bracket index
- mash binding detectors with fixes at `"lib/compiler/<home>.id":read()` /
  `audit(path)(pattern)` — never `check(semantic)(slot)` machinery

| # | directive |
|---|---|
| 1 | Production owner remains `GAP-124` graph canonicalization. |

## Required audit targets (live at HEAD)

| path | presumptive violation | redress owner |
|---|---|---|
| `src/lexer_dispatch.zig` | lexical dispatch kingdom | lexer/source law facts |
| `src/meta_dispatch.zig` | directive→handler map | anchored relation + stage facts |
| `src/derive_registry.zig` | key→semantic handler | graph relation index (non-authoritative) |
| `src/transform_engine.zig` | engine executor container | transformation facts + lineage |
| `tools/wasm/src/engine.id` | wasm engine ontology | decode/realize/run relations |
| `src/semantic_context.zig` | context responsibility bag | split to graph/world/stage owners |
| `lib/context.id` | context vocabulary debt | delete or decompose |
| `src/lexer_bridge.zig` | bridge identity | producer→consumer wiring + delete |
| `src/duo_keyword_bridge.zig` | bridge identity | producer→consumer wiring + delete |
| `src/c_frontend.zig` | frontend kingdom | foreign C provenance → graph |
| `src/explain_pipeline.zig` | pipeline controller | dependency/transform facts |

## Completed in this slice

- Removed `router(semantic)` and `check(semantic)(slot)` slot dispatch; gate
  seed uses subject-first `"lib/compiler/<home>.id":read()`.
- Fix messages no longer teach `semantic[slot]`, `proof[slot]`, or
  `router(...)(table[slot])`.
- `subject(slotindex)` + `collision(name)` + selector `flag` rows in `adj`.
- `examples/concept_c_emit_adapter_smoke.id` deleted (adapter ontology debt).
- Deleted the uncalled `tools/mcp/zls/bridge.id`; editor-owned zls remains a
  physical tool and no Idol bridge forwards or reinterprets it.
- Sole idiom gate is `gate/idiom.id`.

## Deletion conditions

1. Each audit target decomposed or deleted with negative control: renaming/
   deleting the physical helper does not change semantic resolution.
2. Zero new machinery identities on canonical added lines (gate pass).
3. No key→handler table owns meaning after graph facts are authoritative.

## Positive controls

- `"lib/compiler/lexer.id":read()` is subject-first; `check(semantic)("ingest")`
  and `semantic[ingest]` fail `subject(slotindex)`.
- `router(x)` fails collision + selector flag; `broker`/`service` renames
  still fail `collision(name)`.

## Negative controls

- Introduce `router`, `gateway`, `registry`, `adapter`, `bridge`, `context`,
  `engine` as selector objects in added `.id` — gate must reject all.
