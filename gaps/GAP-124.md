# GAP-124 — Graph world, subject, vocabulary, sentinel and control canonicality gates

**Status:** OPEN · **Filed:** !2026-08-09T21:08:20Z
**Priority:** P0 · **Kind:** expressiveness_gap
**Filed by:** codex_semantic_normalization_continue
**Files:** docs/spec/constitution.md,gate/idiom.id,lib/compiler,tools/mcp,sibling idol-native tooling

| # | directive |
|---|---|
| 1 | Implement identity-based canonicality from graph facts in Idol after GAP-121 unlocks real compiler modules: effectful applications prove required world edges; subject-callable relations reject operation-first homes independent of spelling; public standard exports resolve one admitted relation identity; semantic cases cannot collapse to sentinels; reducible conditional demand reports the stronger admitted relation. |
| 2 | The existing semanticgate text census remains only a staged-index migration ratchet and must never be reported as WORLDGATE, SUBJECTGATE, VOCABGATE, SENTINELGATE, or CONTROLREDUCE. |

| # | directive |
|---|---|
| 1 | **Progress:** `scripts/canon.id` keeps four scalar verdicts (world, vocab, sentinel, control) over interned states. |
| 2 | The `subject(possessed, callable, operation)` boolean-mirror row is deleted (`law.boolean.mirror.zero`) |
| 3 | SUBJECTGATE waits on application→subject edges from graph ingestion, not a catalog. |
| 4 | Every remaining proof call is subject-first and no relation accepts a home spelling. **`scripts/ledger/semantic.id`** owns GAP-124 file facts; **`scripts/ledger/graph.id`** audits graph sovereignty (TAG-AUTHORITY, FACT-CARDINALITY, AST-BACKEDGE, GRAPH-SOVEREIGNTY); **`scripts/proof/graph.id`** executes on direct backend; **`gate/graph.id`** has `graph: i64 = ()` entry; **`zig build graph-proof`** runs check + direct run. |
| 5 | Graph ingestion and the total structured finding remain open |
| 6 | GAP-126 records the native typed-string boundary exposed while attempting that finding without a scalar intern. |

## Graph sovereignty (`law.graph.sovereignty`)

| # | directive |
|---|---|
| 1 | Highest-impact graph work after catalog deletion: tags still decide validity (`graph_query` `kind != .func`), module-ness persists, ApplicationFact cardinality conflates unknown with absent, `ast_ref` backdoor remains, and Node is a god-record mixing semantic/physical concerns. |
| 2 | See BI register section and `scripts/ledger/graph.id`. |
| 3 | This is the milestone that prevents months of invisible partial transfer while the surface looks converged. |

## Historical adversarial precommit bypass (2026-08-09)

| # | directive |
|---|---|
| 1 | At the audited revision, the committed `scripts/idiomgate.id` firewall was lexical, not rename-invariant. |
| 2 | This exact added-file diff exits zero with **0 findings** and all **14/14** built-in controls green: |

```duo
script = req "std.script"
command = "printf ok"
script.run(command)
whatever.system.run(command)
message = "value " .. command
print(message)
platform.net.send(socket, data)
```

| # | directive |
|---|---|
| 1 | The authority detector recognizes only four literal `std.*` paths. |
| 2 | Aliasing the same operation or changing the home spelling therefore changes the verdict, which directly violates the authority law. |
| 3 | The materialization detector only recognizes `print` and `..` on the same line, so a one-use bridge binding erases the finding without changing meaning or demand. |

| # | directive |
|---|---|
| 1 | The claimed `scripts/canonicalitygate.id` identity layer was absent from that worktree, and `.githooks/pre-commit` then ran only `idiomgate.id` plus the explicitly textual `semanticgate.id` census. |
| 2 | Commit `1a7924f` was useful temporary pressure against four known spellings; it is not SUBJECTGATE, WORLDGATE, VOCABGATE, or demand/materialization closure. |

| # | directive |
|---|---|
| 1 | Required positive controls must vary the home spelling, introduce a local alias, and insert a single-consumer bridge while preserving the same resolved relation, subject, world, and demand. |
| 2 | All variants must receive the same graph verdict. |

## Historical rejected 19-control expansion

| # | directive |
|---|---|
| 1 | The reviewed dirty `scripts/idiomgate.id` expanded its built-in report from 14/14 to 19/19 controls, but the adversarial verdict does not change: |

```text
renamed/aliased RG0 fixture   exit 0   findings 0
callable alias `go(command)`  exit 0   findings 0
canonical `mask = 0xFF`       exit 1   uppercase finding
```

| # | directive |
|---|---|
| 1 | The added `readface` recognizer keys one terminal spelling, `read`, rather than a resolved relation identity. |
| 2 | It can reject `arbitrary.home.read(subject)` but has no proof that the callee is the admitted `read` relation, and the identical authority defect under `run`, `send`, or an aliased callable remains invisible. |
| 3 | The expanded capital scan inspects every remaining source byte and therefore misclassifies uppercase hexadecimal digits as identifiers. |

| # | directive |
|---|---|
| 1 | These changes may improve lexical migration pressure after the false positive is fixed. |
| 2 | They are not SUBJECTGATE, WORLDGATE, VOCABGATE, or rename-invariant semantic canonicality. |
| 3 | A built-in control count is not evidence unless it contains the required alias, rename, homonym, bridge, and valid-literal adversaries and proves them by resolved identity. |

| # | directive |
|---|---|
| 1 | The uppercase-literal regression now has a focused Idol repair in the working tree: numeric tokens are skipped before identifier-uppercase detection, with controls for `0xFF`, `1E9`, and `foo2Bar`. |
| 2 | The first two now pass and the last still fails. |
| 3 | This repairs only the lexical regression introduced by `cdbac4f`; the renamed/aliased RG0 fixture still exits zero with zero findings, so the semantic closure state is unchanged. |

| # | directive |
|---|---|
| 1 | The same review found that `scandiff` recognized `+++ b/` anywhere in an added source line as a file header. |
| 2 | A string containing those bytes could suppress the line's canonicality scan and corrupt subsequent attribution. |
| 3 | The working tree now requires the exact diff-header prefix and positive-controls the boundary. |
| 4 | A line containing `"+++ b/"` plus `std.script.run` is again rejected. |
| 5 | This is parser integrity for the temporary lexical firewall, not evidence for semantic identity. |

## Dirty corpus migration is not landable

| # | directive |
|---|---|
| 1 | A 2026-08-10 audit snapshot of the shared worktree covered an unstaged Idol diff across `lib/script.id`, scripts, LSP, and MCP. |
| 2 | Running that snapshot's exact unified diff through `scripts/idiomgate.id` produced blocking findings while every gate control passed. |
| 3 | This is rejection lineage, not a current corpus count or a reproducible release artifact. |
| 4 | The findings were not a clean migration hidden by old corpus debt: the added lines themselves introduced or retained forbidden authority homes, underscores, `end`, `string.*`, nil sentinels, and output-only concatenation. |

| # | directive |
|---|---|
| 1 | The highest-risk addition puts `runid` and `provenance` in `std.script` and makes the LSP gate and legend generator import that home. |
| 2 | Private temporary outputs are required for attributable concurrent evidence, but expanding the forbidden process/environment namespace is not the repair. |
| 3 | Preserve the private-output requirement, classify execution/environment/provenance as `vocabularyblocked`, and admit ordinary world/application facts before migrating those consumers. |
| 4 | Do not commit the dirty aggregate, raise ratchets, or report its mechanical callable-result rewrites as canonical corpus progress. |
