| field | value |
|---|---|
| title | GAP-160 — lib/std result/errors/check/property may be conventional subsystems |

| section |
|---|---|
| State |

| # | directive |
|---|---|

| section |
|---|---|
| Problem |

| # | directive |
|---|---|
| 1 | `lib/result.id` (276 lines), `lib/errors.id` (94 lines), `lib/check.id` (335 lines), and `lib/property.id` (278 lines) exist as standard library surfaces. |
| 2 | They may create conventional subsystems (Result wrapper, errors API, validation API, property API) where ordinary result packs + union/failure obligations + descriptor facts + shape/reflection suffice. |

| section |
|---|---|
| Evidence |

```
lib/result.id: 276 lines
lib/errors.id: 94 lines
lib/check.id: 335 lines
lib/property.id: 278 lines
```

| section |
|---|---|
| Required audit |

| # | directive |
|---|---|
| 1 | For each file, determine whether it creates: |

| # | directive |
|---|---|
| 1 | Result wrapper / ok-error object / success flag / boxed failure where ordinary result packs + union/failure obligations suffice |
| 2 | Errors API where failure is already a graph fact |
| 3 | Validation API where descriptor facts and constraints suffice |
| 4 | Property API where reflection/descriptor facts suffice |

| # | directive |
|---|---|
| 1 | Do not automatically delete if foreign compatibility requires it. |
| 2 | Classify each as: canonical, migration, foreign, or invalid. |
