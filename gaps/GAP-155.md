| field | value |
|---|---|
| title | GAP-155 — Root execution |
| status | CLOSED (acceptance met !2026-08-25; residuals delegated) |
| filed | !2026-08-12T04:20:00Z |
| priority | P0 |
| kind | architecture |
| owner | Cursor (realization) |
| files | src/ast.zig,src/dnir_lower.zig,src/native_backend.zig,src/main.zig,src/idol_io_bootstrap.c,examples/shc/application.id,examples/shc/lexer.id,examples/shc/bind.id,examples/shc/write.id,examples/shc/read.id,examples/shc/arg.id,examples/shc/env.id,examples/shc/cwd.id,examples/shc/compose.id,examples/shc/path.id |

> Closure reconciliation !2026-08-25: all ten acceptance items below were
> already met and the record sat at "OPEN (acceptance met)", which let
> downstream gaps cite a nominally open P0 whose acceptance was complete.
> The one residual this record still described — `gate/idiom.id` cannot
> execute natively because the native subset does not run curried
> relation-projection application (`subject(tail)("...")`) — is not a root
> -execution fact and is owned where the bridge rows live:
> `gaps/GAP-137.md` (subject-tail bridge, `lowerSubjectTail`) and the
> recorded DNB001 blocker on `gate/idiom.id` direct execution. Cite those,
> not this gap.

| # | directive |
|---|---|
| 1 | Disjoint from GAP-153 (layout/home/reach) and GAP-154 (host APIs). |

| section |
|---|---|
| Ruling |

| # | directive |
|---|---|
| 1 | The file-scope tail is the program. |
| 2 | No source `main` / `entry` wrapper. |
| 3 | Physical `_main` is linker ABI, not a source binding. |

| # | directive |
|---|---|
| 1 | A module with a file-scope tail or statement sequence is the process. |
| 2 | Helpers in the same file are not promoted to the entry. |
| 3 | A declared source `main` remains deliberate and still wins. |

| # | directive |
|---|---|
| 1 | Root applications have the module as caller. |
| 2 | Unknown effect/authority/witness stay unknown — not `world=""`. |

| section |
|---|---|
| Done (2026-08-12) |

| # | directive |
|---|---|
| 1 | `Module.program()` recognizes file-scope tail and statements. |
| 2 | Graph admits `.module` as application caller. |
| 3 | Lowerer emits physical `main` from the root tail when source has no `main`. |
| 4 | `examples/shc/application.id` is `42:read() - 42` (no wrapper). Exit 0. |
| 5 | `examples/shc/lexer.id` is `0`. Exit 0. |
| 6 | `0` as a whole file exits 0. |
| 7 | `examples/shc/bind.id` — helper `w` plus tail `0`. Process is the tail (exit 0), not the helper (would have been 42). |
| 8 | `examples/shc/write.id` — `stdout:write("hi")` is egress, not the exit status. `puts` return no longer leaks as status 61. Physical exit 0. |
| 9 | `examples/shc/read.id` — `stdin:read()` is ingress. The str result is not the process status (was exit 64). Physical exit 0. Integer/bool/f64 tails remain the status (`0`, `true`, `42:read() - 42`). |
| 10 | `examples/shc/arg.id` — `stdout:write(os.args[1])`. 1-based table under `os`, not `os.args()`. Missing index is NULL (unknown), not "". `idol_os_arg` is a bounded C bridge with the same death as stdin. |
| 11 | `examples/shc/env.id` — `stdout:write(os.env["PATH"])`. Index face, not `os.env()` / `getenv`. Write of NULL (missing) skips `puts` — no C `(null)` sentinel. Present empty still prints a blank line. |
| 12 | `examples/shc/cwd.id` — `stdout:write(os.cwd)`. Field, not `getcwd` / `os.cwd()`. Failure is NULL (unknown), not `""`. `idol_os_cwd` is a bounded C bridge with the same death as stdin/args. |
| 13 | `examples/shc/compose.id` — `stdout:write(stdin:read())`. Ingress observed by egress. Physical exit 0. A bare `stdin:read()` tail still discards the str (no invented stdout). |
| 14 | `examples/shc/path.id` — `stdout:write("examples/shc/lexer.id":read())`. Path subject, `read` relation. Missing path is NULL (unknown); write skips `puts`. The result is str — not a pointer printed as i64. |
| 15 | Declared `main: i64 = ()` plus a sibling file-scope tail still runs `main` (exit 7 vs tail 0). Not a teaching example; the tail remains the default. |
| 16 | `s:sub(i, i)` is `idol_str_at` (static one-byte intern), not a malloc'd slice. `bare` in `gate/idiom.id` was the 4.9G witness. Delete with the str-slice bridge. |

| section |
|---|---|
| Remaining (not this gap) |

| # | directive |
|---|---|
| 1 | Missing `os.args`/`os.env`/`os.cwd`/`path:read` is still a NULL hole (GAP-118: not a graph unknown; not `""`). Native no longer lowers `os.env(k)` — that call is refused; `os.env[k]` is the face. Corpus call spellings remain migratable debt. |
| 2 | Source `main: i64 = ()` teaching examples still in the corpus — migratable once this face is the default. Do not bulk-rewrite. |
| 3 | Effect/authority/witness on the root application remain unknown (lane 3). |
| 4 | A str *value* tail is discarded after production — it does not become an observed result (no silent stdout write). Observation is an admitted relation (`stdout:write`), not an invented egress. |
| 5 | **RESOLVED for MCP — native sentinel-free server on `idol run` (2026-08-13).** `tools/mcp/native.id` is a persistent JSON-RPC MCP server that runs on the direct native backend with **no `--backend=c`** and no Lua runtime. It serves `initialize`, `notifications/*` (ack), `tools/list`, `tools/call`, and `ping`; tool bodies (`status`, `head`, `orient`) run through `gatecap` + `jq -Rs .`. Verified end-to-end: ids `42/100/7/256` round-trip correctly, notifications ack, clean EOF exit 0. |
| 6 | ↳ ↳ **No sentinels (PREDICATE-ZERO).** Dispatch is entirely `line:has(needle)` presence facts consumed in refinement `if`; `find` results are used only as substring coordinates guarded by a `has` fact — never compared to a not-found sentinel. There is no `== 0` / `== ""` / `== nil` / `== true`. |
| 7 | ↳ ↳ **EOF is not a value.** `idol_io_read_line` (`src/idol_io_bootstrap.c`) ends the process on peer close, so the loop is `while true` with no empty-string EOF flag. stdout is line-buffered (`__attribute__((constructor))`) so each response reaches the peer before the next read. |
| 8 | ↳ ↳ New native line-read primitive wired through `native_ir.zig`, `main.zig` (symbol→bootstrap.c link), `dnir_lower.zig` (`lowerStdinLine`, `exprIsStr` for `stdin:line` and `gatecap`), `sema.zig`, `codegen.zig`, and `native_bootstrap.zig` (`stdin:line` method admission). |
| 9 | ↳ ↳ The native backend rejects nested method-calls-as-arguments and any block whose tail is a bare expression-statement when `allow_return=false` (`codegen.zig:4003` `block-tail-no-return`); the server therefore uses flat loop-body bindings and reads the next message as the loop-body tail statement. |
| 10 | ↳ ↳ Manifest: `tools/node/dev/mcp.manifest.json` now lists a `native`-backend `idol` server pointing at `tools/mcp/native.id`. The stale `bench.id` / `lsp.id` / `zls.id` (Lua-compat `std.*` / `then` / `end` / `~=` / `:push`) remain bootstrap-compatibility debt and are still gate-deferred. |
| 11 | ↳ ↳ `.cursor/mcp.json` is environment-protected (not writable from the harness); point it at `idol run tools/mcp/native.id` (no `--backend=c`) manually. |
| 12 | **Gate execution still native-blocked — advanced application frontier (2026-08-13).** `gate/idiom.id` compiles and passes `idol check`, but `idol run gate/idiom.id` exits 128 with its control self-tests failing (`subject(tail)`, `subject(has)`, `subject(read)`, `subject(eq)`) regardless of stdin. The primitives themselves are fine — string `==`, `:has` / `:find` / `:sub`, and early `return` from inside a `while` each execute correctly in isolation (probed). The gate fails because it uses **curried relation-projection application**: `subject(tail) = (code)` invoked as `subject(tail)("...")`, plus composed helper chains. The native subset does not yet execute that application shape, so the pre-commit hook stays `idol check` and no runtime sentinel/idiom conviction of the corpus is possible. Sentinel enforcement source is present and strengthened in `gate/idiom.id` (`== nil` / `!= nil` / `~= nil` / `= nil`, `== 0` / `!= 0` / `~= 0` / `== 1`, `== true` / `== false` / `~= true` / `~= false`, `== ""` / `~= ""` / `!= ""`), but only convicts on added diff lines once the gate can execute. |

| section |
|---|---|
| Acceptance |

| # | directive |
|---|---|
| 1 | A tracked SHC example with only a file-scope tail runs `idol run` without a source `main` binding. |
| 2 | A helper plus a file-scope body runs the body, not the helper. |
| 3 | A declared source `main` still wins over a sibling body. |
| 4 | No new source wrapper names (`entry`, `init`, `start`) as process identity. |
| 5 | `os.args[1]` observed through an admitted relation (`stdout:write`) runs without a source `main` and without `os.args()`. |
| 6 | `os.env[k]` observed the same way, without `os.env()` or `os.getenv`. |
| 7 | `os.cwd` observed the same way, without `getcwd` or `os.cwd()`. |
| 8 | `stdout:write(stdin:read())` composes ingress to egress; exit is 0, not a leaked `puts` or pointer. |
| 9 | `path:read()` observed through `stdout:write` is text, not a pointer. |
| 10 | A declared source `main` plus a sibling file-scope tail runs `main`. |
