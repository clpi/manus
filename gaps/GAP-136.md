| field | value |
|---|---|
| title | GAP-136 — relation identity gate accepts untracked C-only noncanonical evidence |
| status | OPEN |
| filed | !2026-08-10T00:08:10Z |
| priority | P0 |
| kind | script |
| filed by | codex-root-shc-audit-20260810 |
| files | gaps/GAP-103.md,scripts/relationid.id,examples/conversion/store.id |

| section |
|---|---|
| 2026-08-12 direct relation proof (partial) |

| # | directive |
|---|---|
| 1 | `scripts/proof/relation.id` now runs on direct backend (no `--backend=c`), checks tracked fixtures `examples/conversion/relation.id` and `examples/conversion/store.id`, and audits `src/relation.zig` for structural `Relation`/`Store` without C-bridge markers. |
| 2 | Remaining closure: graph-owned relation identity proof after GAP-124 and deletion of any C-only gate rows. |

| # | directive |
|---|---|
| 1 | A named relation identity gate can report closure from an untracked fixture that the canonical diff gate rejects and direct native cannot realize. |
| 2 | Closure must fail closed unless every fixture is tracked, current-canonical, and exercises the authoritative Idol semantic relation through direct realization. |
| 3 | Keep the stable store-unit result, but do not count C/default runner output or Zig-source scans as SHC/source authority. |
| 4 | No new Zig; repair the Idol gate/evidence topology and source semantic path. |

| section |
|---|---|
| Reproduction |

| # | directive |
|---|---|
| 1 | The gate names an untracked file as evidence: |

```text
$ git ls-files #error-unmatch examples/conversion/store.id
error: pathspec 'examples/conversion/store.id' did not match any file(s) known to git
```

| # | directive |
|---|---|
| 1 | The file is not landable under the repository's first commit chokepoint. |
| 2 | An added-file diff fails `scripts/idiomgate.id` with eight `LAW-ONE` findings for the `eq_*` identifiers. |
| 3 | It also contains the retired callable-result suffix and an imperative iteration face where the relation identity being proved should remain visible. |

| # | directive |
|---|---|
| 1 | The default bootstrap path nevertheless accepts and runs it: |

```text
$ ./zig-out/bin/idol check examples/conversion/store.id
$ ./zig-out/bin/idol run examples/conversion/store.id
16
```

| # | directive |
|---|---|
| 1 | The direct path does not: |

```text
$ ./zig-out/bin/idol run #backend=direct examples/conversion/store.id
error: direct backend refused native lowering at native-scalar precheck — assign-target
```

| # | directive |
|---|---|
| 1 | `scripts/relationid.id` calls default `duo run` for both fixtures, reaches processes through the retiring `std.script` namespace, and scans `src/relation.zig` with host text tools. |
| 2 | Its green result is useful bootstrap evidence for the existing relation store. |
| 3 | It is not evidence that canonical Idol source can name the identity, that the semantic graph owns it, or that the direct realization consumes it. |

| # | directive |
|---|---|
| 1 | The deleted pass-era extractor once recorded that `relation_store` was visible from a parallel session but uncommitted. |
| 2 | It is not current evidence. |
| 3 | A gate must never derive a released claim from session-private work merely because it is visible in the shared worktree. |

| section |
|---|---|
| Required separation |

| # | directive |
|---|---|
| 1 | Keep the current store result classified narrowly: |

```text
store unit semantics       proven in bootstrap implementation
source reachability        open under GAP-103
canonical corpus evidence  absent
semantic graph authority   absent
direct realization         refused
SHC evidence               absent
```

| # | directive |
|---|---|
| 1 | The bootstrap store now also refuses a pair when two lawful two-hop routes exist, regardless of declaration order. |
| 2 | This closes the store-level ambiguity fail-open only. |
| 3 | It does not provide the missing relation-level composition law, graph identity, canonical source reachability, witness identity, or direct realization required below. |

| # | directive |
|---|---|
| 1 | Do not weaken or delete the stable identity tests merely because their host is temporary. |
| 2 | They become an oracle until an equivalent Idol-owned proof exists. |
| 3 | What must stop is promoting their result into a stronger claim. |

| section |
|---|---|
| Closure |

| # | directive |
|---|---|
| 1 | Every proof fixture is tracked in the commit whose gate names it. |
| 2 | The exact added-file canonicality check passes before the fixture can contribute a row. |
| 3 | The fixture names an admitted semantic relation identity from canonical Idol source; it does not create a second operation family in gate code. |
| 4 | The semantic graph retains that same identity and direct realization consumes it without text reconstruction. |
| 5 | Direct native executes the value proof and reports lineage from source relation to realized operation. |
| 6 | C/bootstrap output may remain a differential oracle, but it is a separately labelled row and cannot satisfy direct or SHC closure. |
| 7 | The proof runner and result model are Idol-owned. No new Zig gate or runner is added. Existing Zig inspection is temporary oracle evidence with an explicit deletion condition: remove it when the Idol relation store and lineage query reproduce the positive and negative controls. |
| 8 | A positive control supplies an untracked but otherwise valid fixture and proves the gate refuses it before executing it. |
| 9 | A second positive control supplies a tracked noncanonical fixture and proves the gate refuses it before executing it. |
| 10 | GAP-103 remains open until origin, scope, lawset, and rename are reachable through the same source and graph authority. Closing this gate must not misclose that semantic reachability gap. |
