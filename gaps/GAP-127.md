# GAP-127 — SHC ledger contract drift makes the 100 percent Duon report fail for the wrong reasons

**Status:** OPEN · **Filed:** !2026-08-09T21:08:01Z
**Priority:** P0 · **Kind:** agent
**Filed by:** codex_shc_priority
**Files:** scripts/shcledger.duo,scripts/selfhost_manifest.duo,lib/std/script.duo,gaps/GAP-119.md

Live `duo run scripts/shcledger.duo` reports 286 bootstrap debt but also five check failures. Four are evidence drift: it greps obsolete identifiers `duo_impl_rows`, `oracle_backed_rows`, `duo_canonical_rows`, and old blocker text while selfhost_manifest.duo now prints JSON fields `implemented`, `oracle`, `canonical` and a different blocker. The fifth idiom-gate failure is caused by unrelated dirty added lines and should remain attributable, not folded into committed baseline. The ledger also imports forbidden std.script and cannot be canonical until GAP-119 admits process-world structured results. Repair the ledger to consume the manifest as structured data through the eventual world-backed process/result relation, preserve dirty-tree provenance, and keep the actual current counts visible: 115 Zig, 3 C bootstrap, 166 embedded-C sites, only 2 production-canonical rows. Do not merely update four grep strings or rename std.script.

## Forced-backend evidence

With compiler SHA-256
`23cf9eb6c66806c2f582c5a939f2fe23ebf3d56bb6d670d0c0427e59bd4087f5`, the
SH-04 parser corpus proof has two materially different answers:

```text
duo run examples/pass16_parser_corpus_proof.duo
exit 0, compile 55795 ms

duo run --backend=direct examples/pass16_parser_corpus_proof.duo
exit 1
DNB001: native-scalar precheck — keyed-table-export
```

The default answer may fall back to C, and `selfhost_manifest.duo` explicitly
runs every row with `--backend=c`. It therefore proves a Duon implementation and
oracle, not native self-host authority. The first currently observable direct
blocker is the demanded module-level positional table holding the parser corpus;
the module-initialization defect in GAP-121 can only be retested after this
earlier refusal is removed.

Do not make the proof smaller by hiding the corpus inside generated constants or
deleting the table. A parser requires structured token/case collections. The
vertical repair is general native realization for the demanded immutable table,
followed by its iteration relation, with no `lua_Value` intermediate. Only then
does it make sense to resume module initialization and production parser
dispatch. Additional file-count ledgers do not move this boundary.

## Current direct blocker is masked by a staged C cache

The current compiler artifact no longer reaches the immutable-table refusal in
SH-04. A staged `codegen.zig` change introduces a textual
`subject_relation_results` cache and refuses native mode when that cache is
empty. Forced-direct results are now:

```text
SH-01 source   exit 1   module-relations
SH-02 token    exit 1   module-relations
SH-03 lexer    exit 1   module-relations
SH-04 parser   exit 1   module-relations
```

This is one new bootstrap barrier across four rungs, not four newly discovered
language blockers. The cache repairs one C projection while direct lowering
consumes none of its facts. Delete the barrier by preserving authoritative
relation identity and result descriptors into direct realization, then
remeasure SH-04 to expose the real demanded immutable-table work again.

The cache is no longer merely staged. It was committed and pushed as
`c5bbc6c`. An isolated build of that exact tree, rather than the mutable shared
compiler artifact, reproduces all four `module-relations` failures. Therefore
the current released branch regressed from one honest SH-04 table blocker to a
single Zig-owned refusal masking every measured bootstrap rung. The active
repair must first restore the pre-commit direct frontier; it must not claim SHC
progress from the reader fixture's successful generated-C projection.

Commit `3cd3f07` has now restored that frontier. Exact isolated direct results
are `block-tail-no-return`, `keyed-table-export`, `ret-type:any`, and
`keyed-table-export` for SH-01 through SH-04 respectively. This closes the
four-rung regression only. All four direct proofs still fail, SH-04 still needs
the demanded immutable-table and iteration realization below, and the reverted
C path still reconstructs dependency relation results through
`probeSubjectRelationType`.

## Canonical authority correction

The manifest's `canonical = 2` does not prove two current-spec direct Duon
authorities.

The exact full-file current-canonical gate rejects every implementation and
proof in SH-01 through SH-04:

| row | implementation findings | proof findings |
|---|---:|---:|
| SH-01 source | 15 | 16 |
| SH-02 token | 95 | 20 |
| SH-03 lexer | 476 | 16 |
| SH-04 parser | 908 | 9 |

Each run retained the gate's 14/14 positive controls. These are not incidental
dirty-diff findings: each tracked file was presented as a complete added file.
The production manifest labels SH-02 and SH-03 `DUO_CANONICAL`, but neither its
implementation nor its proof is accepted by the current canonicality gate.
Consequently the honest current-spec canonical count for these four rows is
zero, before direct execution or production ownership is considered.

SH-01 has a second, independent evidence defect. The manifest names
`examples/pass16_source_cursor_proof.duo`, but that file never imports or calls
`lib/std/compiler/source.duo`. It defines a separate `walk_to_line` routine and
tests only that inline implementation. `src/selfhost_verify.zig` repeats the
same proof association. A real module consumer exists at
`examples/pass16_source_module_proof.duo`; it passes only through the default C
path, has 12 current-canonical findings, and direct first-fails at
`block-tail-no-return`.

Therefore SH-01 currently proves neither the named Duon implementation nor
production authority. Replace the manifest row with one current-canonical
consumer of the actual source relation, compare it against the independent
cursor oracle, and require direct execution before incrementing any SHC count.

- SH-02 `lib/std/token/classify.duo` declares that it is generated by
  `src/token_classify_gen.zig` and that `src/token_semantic.zig` owns the
  canonical token facts. The production C classifier is emitted directly from
  that Zig descriptor. The Duon file is a projection, not the authority.
- SH-03 executes checked-in `src/duo_lexer_tokenize.c` through Zig dispatch.
  Its Duon source still uses epoch-1 `self`, `end`, underscore names, module
  aliases, and prefix export directives, and its forced-direct proof fails.

Under the release requirement -- current canonical source, Duon semantic
ownership, and direct production execution -- the proven authority count is
zero. Report algorithm authored in `.duo`, semantic owner, production artifact,
canonical-source status, and direct execution as separate facts. Neither a
file extension nor a generated projection transfers authority.
