| field | value |
|---|---|
| title | GAP-137 — SHC-01 DNIR application identity remains optional and fail-open |
| status | OPEN |
| filed | !2026-08-10T01:08:39Z |
| priority | P0 |
| kind | backend |
| filed by | codex-root |
| files | src/sema.zig,src/semantic_graph.zig,src/dnir_lower.zig,src/native_backend.zig |

<!-- idol-gap-frontier:v1:begin -->
```json
{
  "schema": "idol.gap.frontier.v1",
  "gap": "GAP-137",
  "status": "OPEN",
  "root_program": "src/semantic_graph.zig -> src/dnir_lower.zig -> src/native_ir.zig -> src/native_backend.zig",
  "current_blockers": [
    "DNIR semantic identity fields remain optional rather than mandatory on every supported checked path.",
    "Some direct-call paths still lack exact relation, application, ordered-pack, and result lineage.",
    "Transformation inputs and outputs are not uniformly queryable through machine and object ranges.",
    "Remaining AST, name, and hash recovery must become exact fact consumption or explicit refusal."
  ],
  "superseded_observations": [
    "#2026-08-10-merge-review-of-the-replacement-projection",
    "#2026-08-10-exact-handle-projection-review",
    "#2026-08-23-application-consumer-zero-reconstruction-census-base-ce03c7eb"
  ]
}
```
<!-- idol-gap-frontier:v1:end -->

| section |
|---|---|
| Current frontier notes |

| # | directive |
|---|---|
| 1 | The machine-read frontier is the `idol.gap.frontier.v1` block above; it is the one `gate/frontier.sh` validates and the one to trust. |
| 2 | This section is the EVIDENCE behind those blockers -- what was measured, what is already crossed and must not be redone, and the command that re-measures each claim. |
| 3 | It is not a second frontier. |
| 4 | Where it and the JSON block disagree, the JSON block wins and this section is the bug. |

| # | directive |
|---|---|
| 1 | **Frontier:** APPLICATION-CONSUMER-ZERO is close to closed, and what remains is not this gap's to close. `scripts/ledger/application.id` prints `accessors PASS, closure OPEN` with its open rows named; the one it names is `subject tail still reconstructed (fn lowerSubjectTail)` -- the GAP-155 bootstrap subject faces (`text:tail()` and siblings), which are the only identity-free `call_direct` still reachable on the checked path. |
| 2 | The ROW COUNT is not written here on purpose: run the ledger, because that count is exactly what changes when someone closes a row. **Crossed:** callee-TEXT selection is gone from the readiness predicate -- `moduleIsChecked` derives "this module was lowered with graph facts required" from the resident graph, `calleeIsModuleLocal` survives only on the UNCHECKED path, and a checked `call_direct` now additionally proves target and provenance continuity against the graph. |
| 3 | Subject reconstruction is zero outside the producer: `fact.subject` reads occur only inside `src/semantic_graph.zig`. |
| 4 | Operand packs are consumed through `graph.applicationArguments`. |
| 5 | The three-way `orelse` cause splice is replaced by `selectedCause`, and a face crosses only across occurrence equality. `memberDescriptorFromShapeAst` is deleted -- a tombstone comment stands where it was -- and `applicationFace` / `occurrenceFace` now ask the graph FIRST, falling back to `ast_ref` only when the graph could not resolve the relation. **Next:** give `text:tail()` and its siblings a real relation node -- sema resolution, graph publication, then the checked lowering answers it. |
| 6 | Doing this from the consumer side would mint a second producer, so it is GAP-155 bridge death, not a consumer routing. **Verify:** `./zig-out/bin/idol run scripts/ledger/application.id` -- the rows it prints ARE the measurement; do not quote a count from this document. `sh gate/semantic-graph-authority.sh`. |
| 7 | For the checked-path guards, `grep -n 'fn moduleIsChecked\\|fn calleeIsModuleLocal' src/native_ir.zig` and `grep -n 'fn selectedCause' src/native_backend.zig`. **Blocked on:** GAP-155 (bootstrap subject faces). |
| 8 | GAP-202 depends on this gap's algebra being complete. **Measured at:** the census below is based at `ce03c7eb` with an AFTER table at `e3df9beb`; several of its line cites have since moved by hundreds of lines (`moduleIsNativeDirectReady` is no longer near `native_ir.zig:513`), and the worklist it re-measured "was partly stale in both directions" by its own account. |
| 9 | Re-run the ledger before acting on any row. |

| section |
|---|---|
| Original filing (2026-08-10) |

| # | directive |
|---|---|
| 1 | Kept verbatim as the gap's founding statement. |
| 2 | Two of its clauses have since been measured false on the CHECKED path -- callee text no longer selects the implementation there, and subject/operand reconstruction is closed -- see CURRENT FRONTIER above. |

| # | directive |
|---|---|
| 1 | The current bootstrap bridge adds optional relation, application, and result hashes to DNIR for a subset of checked subject calls. |
| 2 | It does not close SHC-01. |
| 3 | The relation is not the selected callable identity, ordinary and uncovered calls can still lower without it, callee text continues to select the implementation, and public object emission carries no queryable machine lineage. |
| 4 | Call labels, operand descriptors, constants, world, return-pack shape, witness, provenance, and meaning/content/incarnation identity are also absent. |
| 5 | Repair must begin with the Idol-owned application facts, then carry the same selected identity through a fail-closed lowering and public machine projection without a name/op translation table. |

| section |
|---|---|
| Algorithmic convergence gate |

| # | directive |
|---|---|
| 1 | Closing the object graph is not permission to recreate a conventional pass pipeline. |
| 2 | The final graph owner must expose three reusable mechanisms from the same ids, facts, relations, dependencies, witnesses, and provenance: |

| # | directive |
|---|---|
| 1 | a sparse monotone consequence worklist whose fact families supply lattices, with SCC condensation and exact invalidation so unchanged facts do not pay for repeated whole-graph scans; |
| 2 | bounded persistent witnessed equivalence attached to the semantic graph, never a second e-graph IR, with growth controlled by demand, profile, expected gain, reuse, compile work, and peak state; and |
| 3 | demand/profile/cost-budgeted realization extraction, where layout, outlining, allocation/scheduling, and verified local synthesis are physical candidate strategies rather than semantic operation vocabularies. |

| # | directive |
|---|---|
| 1 | Specialized columns, packed adjacency and pack ranges, bitsets, arenas, and derived indexes are the physical substrate. |
| 2 | Memory and observable effects use graph-owned version/dependency facts rather than repeated alias walks or a parallel MemorySSA ontology. |
| 3 | Module and foreign-unit summaries are exact projections that permit selective subgraph import; they never mint module-local semantic identity. |

| # | directive |
|---|---|
| 1 | This does not authorize implementing these engines in Zig ahead of the current authority boundary. |
| 2 | First make the complete application/fact query authoritative and remove AST reconstruction below resolution. |
| 3 | Then transfer the engines into canonical `.id` in compiler-B dependency order. |
| 4 | Any temporary host edit must delete more semantic authority in the same slice. |

| # | directive |
|---|---|
| 1 | Required controls eventually prove that changing the physical graph layout, forcing every accelerator hash to collide, changing region partitions, or removing an equivalence index preserves semantic query answers. |
| 2 | Fact perturbation must enqueue only exact dependents, and every extracted machine choice must retain its equivalence witness and source-to-object lineage. |

| section |
|---|---|
| 2026-08-23 APPLICATION-CONSUMER-ZERO reconstruction census (base ce03c7eb) |

| # | directive |
|---|---|
| 1 | Exact, re-runnable measurement of every consumer that reconstructs an application fact instead of reading it from the graph. |
| 2 | Every row is a shell command over `src/*.zig` at `ce03c7eb`, clean tree. |
| 3 | Counts are occurrences, not files. **The stated worklist was partly stale in both directions**: the subject rows are already closed, and two rows nobody had named are open. |

| # | directive |
|---|---|
| 1 | Baselines at `ce03c7eb`: `zig build --summary all` 5/5 succeeded; `zig build unit-test` exit 0; direct-backend sweep over 517 `examples/**/*.id` (`idol compile --backend=direct`) = 198 OK, 177 DNB001, 69 DNB011, 5 DNB007, 68 no-entry. |

| section |
|---|---|
| C1 — `call_direct` emitted with no application identity |

| # | directive |
|---|---|
| 1 | `grep -n 'op = \.call_direct' src/dnir_lower.zig`, then test each emit expression for `.application =`. |

| # | directive |
|---|---|
| 1 | **10 of 15 emitters carry no identity.** With `require_graph_facts` (checked path) the guards make all but one unreachable: |

| emitter | reachable when checked | why |
| --- | --- | --- |
| `dnir_lower.zig:7591` `lowerRecordCallAssign` | no | sole caller `tryAssignRecordCallFromExportMap:7598` returns false when `require_graph_facts` |
| `dnir_lower.zig:10568`, `10572` `lowerSubjectTail` | **YES** | `text:tail()` is a GAP-155 bootstrap face; `lowerSubjectCall:11030` prefers the checked call only when the graph published a relation |
| `dnir_lower.zig:12315`, `12319` `lowerNamedDirectCall` | no | all three callers (`10996`, `12328`, `12345`) are `!require_graph_facts` |
| `dnir_lower.zig:12747`, `12751` dotted member | bootstrap only | after the `12378` refusal guard |
| `dnir_lower.zig:12769`, `12777` bare-name callee | bootstrap only | after the `12378` refusal guard; every `.call`/`.name` bootstrap face (`gatecap`/`write`/`close`/`print`/`to`) is answered by an earlier branch |
| `dnir_lower.zig:12959` `lowerF64KernelCall` | bootstrap only | after the `12378` refusal guard |

| # | directive |
|---|---|
| 1 | Identity-carrying emitters: `7468`, `7486`, `7557`, `10239`, `10326`. |

| section |
|---|---|
| C2 — callee-TEXT selection survives in the readiness predicate |

| # | directive |
|---|---|
| 1 | `native_ir.zig:513-516`: |

```zig
if (i.application == null) {
    if (!calleeIsModuleLocal(m, i.callee)) return false;
    continue;
}
```

| # | directive |
|---|---|
| 1 | `calleeIsModuleLocal` (`native_ir.zig:485`) compares `i.callee` against `function.name` strings. |
| 2 | This is the exact construct this gap's 2026-08-10 semantic-graph-preservation entry forbids by name: *"an empty application census cannot authorize an identity-free `call_direct` or callee-text selection"*. |
| 3 | One site, and it is the whole checked-path hole: `Module` already carries `graph`, so `!m.graph.?.gateTransportModule()` derives "this module was lowered with graph facts required" with no new state, and the escape is admitted on the checked path anyway. |

| # | directive |
|---|---|
| 1 | The predicate also never asks for `target` or provenance, so a checked `call_direct` may realize with `applicationTarget`/`applicationProvenance` unknown. |

| section |
|---|---|
| C3 — subject: CLOSED, no reconstruction remains |

| # | directive |
|---|---|
| 1 | `grep -rn '\.subject' src/*.zig` = 114 occurrences. |
| 2 | Sema-shaped subject reads (`fact.subject` on a `sema.ApplicationFact`) survive at exactly **3** sites, all inside the single producer `src/semantic_graph.zig`: `4382` (operand-name lift), `5013` (`verifyCheckedApplicationOperandPacks` continuity check), `5087` (the lift itself). |
| 3 | Zero in `dnir_lower.zig`, `native_backend.zig`, `native_ir.zig`, `graph_query.zig`. |

| # | directive |
|---|---|
| 1 | Consumers read `graph.applicationSubject` at **40** sites across `dnir_lower.zig`, `native_backend.zig`, `native_ir.zig`, `graph_query.zig`. |
| 2 | Worklist item "subject fields still read directly from host rows in multiple consumers" **does not reproduce at this revision**. |

| section |
|---|---|
| C4 — operand packs: CLOSED on the checked path |

| # | directive |
|---|---|
| 1 | `graph.applicationArguments` live consumers: `dnir_lower.zig:9159`, `dnir_lower.zig:9951`, `native_backend.zig:8481`, plus producer-side `semantic_graph.zig:5004` (continuity) and `6690`. `checkedScalarOperands` is the single operand path for checked applications. |
| 2 | No consumer re-walks `call.args` on the checked path. |
| 3 | Worklist item "operand packs are not uniformly consumed through `applicationArguments`" **does not reproduce**. |

| section |
|---|---|
| C5 — the Sema bridge |

| # | directive |
|---|---|
| 1 | `sema.ApplicationFact` (`src/sema.zig:383`) is host-shaped: `*const ast.FuncDecl` applied/target, `?*const Expr` subject, `[]const *Expr` arguments, `RT` result, `?[]const u8` home. |
| 2 | It is consumed at exactly **2** sites, both the graph lift (`semantic_graph.zig:5003`, `5066`) — i.e. one producer, one ingress. |
| 3 | There is no second consumer to route. |

| # | directive |
|---|---|
| 1 | `scripts/ledger/application.id` reports this row as open by grepping `src/sema.zig` for the string `applicationFact`, which matches that file's own accessor definition. **That row can never reach zero while sema is the checker**; it measures SHC, not consumer reconstruction, and it is the sole reason the ledger prints `closure OPEN (1 row)`. |

| section |
|---|---|
| C6 — unresolved-application diagnostics splice independent causes |

| # | directive |
|---|---|
| 1 | `native_backend.formatDirectCause:233-241` picks each of the three fields from a *different* diagnostic by three independent `orelse` choices: |

```zig
const missing     = diag.lowering.note() orelse diag.note();
const application = diag.application     orelse diag.lowering.application;
const relation    = diag.relation        orelse diag.lowering.relation;
```

| # | directive |
|---|---|
| 1 | `missing` prefers the lowering cause; `application`/`relation` prefer the backend cause. |
| 2 | When both recorded something, the emitted DNB011 names the occurrence of cause A and the missing fact of cause B. |
| 3 | Second splice: `dnir_lower.Diagnostic.record` (`dnir_lower.zig:65`) overwrites `site`/`note` but leaves `application`/`relation` from an earlier, unrelated `bindOccurrence` — so on a gate-transport module, where `OccurrenceBridge.init:266` binds the first unresolved occurrence and lowering then continues, any later refusal inherits that stale occurrence. |

| section |
|---|---|
| C7 — visibility from AST/path shape: NOT PRESENT |

| # | directive |
|---|---|
| 1 | `grep -rn 'is_public\\|isPublic\\|visibility' src/*.zig` = 1 hit, a parser diagnostic string (`parser.zig:1837`). |
| 2 | There is no public/private relation visibility concept in this tree to reconstruct. **Nothing to build; no consumer.** |

| section |
|---|---|
| C8 — `ast_ref` (graph backdoor to syntax) |

| # | directive |
|---|---|
| 1 | 72 occurrences |
| 2 | 55 in `semantic_graph.zig` (the owner). |
| 3 | 17 outside it: |

| # | directive |
|---|---|
| 1 | Provenance/index bridges (legitimate — an AST pointer used as a key, or handed to an evaluator that must run the expression): `dnir_lower.zig:271`, `2283`, `4851`, `4915`, `9829`; `graph_query.zig:284`, `340`, `448`; `demand.zig:490`; `dnir_lower.zig:13852-13853` (test). |

| # | directive |
|---|---|
| 1 | Semantic reads (real debt): **3** |

| # | directive |
|---|---|
| 1 | `dnir_lower.zig:3108` `memberDescriptorFromShapeAst` — resolves a member's DESCRIPTOR by casting `ast_ref` to `ast.AliasDef` and re-running `types.resolve` on the syntax. A descriptor the graph already owns. |
| 2 | `dnir_lower.zig:94` `applicationFace` and `native_backend.zig:147` `occurrenceFace` — both derive the relation face NAME from syntax (`c.func.name.ident` / `mc.method`) *before* asking the graph, and only fall back to the graph relation when there is no `ast_ref`. Diagnostic-only, but the precedence is inverted: the graph knows the relation. |

| # | directive |
|---|---|
| 1 | `graph_query.zig:143-149` `collectionRelationSite` additionally compares a declaration `name` against `expr.method_call.method` — a name test, documented as a deliberate refusal rather than a selection. |

| section |
|---|---|
| C9 — `NodeKind`/`EdgeKind` as meaning |

| # | directive |
|---|---|
| 1 | Outside `semantic_graph.zig`: **2** occurrences, one a test assertion (`dnir_lower.zig:14950`) and one a doc comment (`subject_home.zig:884`). |
| 2 | The vocabulary is no longer exported into consumers. `node.kind ==`/`!=` tests across `src/*.zig` = 297, but they are overwhelmingly lexer/parser token kinds and build-target kinds, not graph tags: graph-tag tests are `demand.zig:489`, `dnir_lower.zig:259`, `4854`, `13852`, `graph_query.zig:147` — five, each already paired with a fact query. **No mass rename is justified by measurement.** |

| section |
|---|---|
| C10 — pinned host-architecture debt, re-measured |

| pinned | measured at ce03c7eb | method |
| --- | --- | --- |
| backend imports of Sema, 39 sites | **does not reproduce.** `src/native_backend.zig` has **1** live reference — the `@import` at line 6 — and 64 test-only ones (65 exact-word `\bSema\b` total, first `test` at 9272). Tree-wide `@import("sema.zig")` = **53** occurrences in 22 files, of which **19** are live (non-test) | `grep -n '\bSema\b' src/native_backend.zig`; `grep -rn '@import("sema.zig")' src/*.zig` split at each file's first `^test ` |
| codegen constructing a Parser, 6 live sites | **confirmed, exactly 6**: `codegen.zig:4316`, `24499`, `24713`, `24764`, `25369`, `26103` (101 `Parser.init(` total; first `test` at 32644) | `grep -n 'Parser\.init(' src/codegen.zig` split at 32644 |

| section |
|---|---|
| AFTER — same measurements at `e3df9beb` |

| category | before (ce03c7eb) | after | what moved |
| --- | --- | --- | --- |
| C1 identity-free `call_direct` emitters | 10 of 15 | 10 of 15 | unchanged; on the checked path only the GAP-155 subject faces reach them, and that is now an OPEN ledger row rather than an unnamed one |
| C2 callee-text selection in readiness | 1 (`native_ir.zig:513`) | **0** | escape deleted on the checked path; `moduleIsChecked` derives the path from the resident graph |
| C2 checked target / provenance continuity | absent | present (`native_ir.zig:568-569`) | a checked `call_direct` now proves both against the graph |
| C3 subject reconstruction | 0 outside the producer | 0 | already closed at base |
| C4 operand packs off `applicationArguments` | 0 | 0 | already closed at base |
| C5 host-shaped fact consumers | 2, both the lift | 2, both the lift | one producer-side ingress; no downstream reader to route |
| C6 cause splice in DNB formatter | 1 | **0** | `selectedCause`; a face crosses only across occurrence equality |
| C6 stale occurrence outliving its cause | 1 (`OccurrenceBridge.init`) | **0** | the id is held on the index and bound by the consumer that refuses |
| C8 semantic reads through `ast_ref` | 3 | **1** (`dnir_lower.zig:112` / `native_backend.zig:186`, now the unresolved-only fallback) | descriptor-from-AliasDef deleted; face precedence inverted to ask the graph first |
| C9 `NodeKind`/`EdgeKind` as meaning | 2 (one test, one comment) | 2 | no measured consumer; not built |
| C10 backend live Sema refs | 1 | 1 | untouched |
| C10 codegen live `Parser.init` | 6 | 6 | untouched |

| # | directive |
|---|---|
| 1 | Evidence for every change in this slice: `zig build --summary all` 5/5; `zig build unit-test` 1712 pass / 1712 (1709 at base plus three added negative controls); direct-backend sweep over 517 examples byte-identical to `ce03c7eb`; and a full DNB message differential over all 246 refusing examples byte-identical (`TOTALCHANGED=0`). |

| # | directive |
|---|---|
| 1 | `gate/idiom.id` preflight cannot run: it fails to compile at `ce03c7eb` with the base binary (`DNB011 application: 929 relation: dot__std missing: application-operand-binding`) — the idiom.id blocker recorded in `AGENTS.md`, pre-existing and untouched here. |

| section |
|---|---|
| What was measured and deliberately NOT built |

| # | directive |
|---|---|
| 1 | **Relation visibility from AST/path shape** (worklist item 7). One grep hit in the tree and it is a parser diagnostic string. No concept, no consumer. |
| 2 | **`NodeKind`/`EdgeKind` → fact queries.** Two occurrences outside the graph, one a test assertion and one a doc comment. The five graph-tag `node.kind` tests that remain are each already paired with a fact query. A rename pass would move text and no authority. |
| 3 | **Routing the GAP-155 subject faces onto graph applications.** This is the one live identity-free `call_direct` on the checked path, and closing it means giving `text:tail()` and its siblings a real relation node — sema resolution, graph publication, then the checked lowering answers it. That is GAP-155 bridge death, not a consumer routing, and doing it from the consumer side would mint a second producer. Recorded as the open row in `scripts/ledger/application.id`. |
| 4 | **A cause-scope discipline for `dnir_lower.Diagnostic`.** `record()` still leaves `application`/`relation` from an earlier `bindOccurrence`, and about fifty `invalidGraphFacts` call sites do not bind one of their own. The one path where a bind provably outlived its cause is repaired; the general discipline needs every call site to declare its cause and has no measured defect left to justify it. |

| section |
|---|---|
| Superseded observations |

| # | directive |
|---|---|
| 1 | The `idol.gap.frontier.v1` block at the top of this file is the authority on which observations this gap declares historical, and `gate/frontier.sh` requires every anchor it lists to resolve. |
| 2 | This section is the PHYSICAL grouping of the ones that are purely dated measurement, so that reading top-down no longer hands a fossil to someone looking for the blocker. |
| 3 | The two lists are not identical and do not need to be: a section can be listed above and still stand here, or stand above this boundary while being listed, when it carries a standing ruling or the most recent evidence as well as a dated measurement. |

| # | directive |
|---|---|
| 1 | Ten dated sections from 2026-08-10 and 2026-08-11 follow. |
| 2 | They record a rejected nine-path prototype, four successive merge reviews of its replacement, and the rulings those reviews produced (ONE-ID admission, descriptor identity owner deletion, the unwitnessed region rewrite refusal, the direct-to-C fail-open boundary, the semantic graph preservation gate). |
| 3 | The prohibitions they state are still law -- in particular "an empty application census cannot authorize an identity-free `call_direct` or callee-text selection", which is the sentence the 2026-08-23 census used to convict `native_ir.zig`. |
| 4 | What is superseded is their description of the CODE: the paths they measure have since been rewritten, and the 2026-08-23 census above measures the same ground at a later base. |
| 5 | Read them for the rulings, never for the state of the tree. |

| section |
|---|---|
| 2026-08-10 Idol owner slice (superseded 2026-08-13) |

| # | directive |
|---|---|
| 1 | `lib/compiler/application.id` was decomposed to **role documentation only** (APPLICATION-CONSUMER-ZERO). |
| 2 | It no longer declares pack/application tables or a coordinate schema. |
| 3 | Production application facts live on graph entities; audit via `scripts/ledger/application.id`. |
| 4 | Remaining debt: subject and other fields still partially reconstructed in sema/lowering — target zero downstream reconstruction before SHC closure. |

| # | directive |
|---|---|
| 1 | This is not production closure. |
| 2 | Current Sema and DNIR still omit pack identities and keep application lineage optional, and machine selection still uses callee text. |
| 3 | GAP-137 remains OPEN until graph-minted facts replace those paths and survive the public object boundary. |

| section |
|---|---|
| 2026-08-10 rejected prototype |

| # | directive |
|---|---|
| 1 | A nine-path Zig prototype added 739 lines and removed 195 while projecting a checked application through Sema, the semantic graph, DNIR, regions, and ARM64 byte ranges. |
| 2 | Two independent reviews rejected it before commit. |
| 3 | The prototype: |

| # | directive |
|---|---|
| 1 | used a store-local relation family rather than the selected callable identity; |
| 2 | made the DNIR application reference optional and allowed production fallback; |
| 3 | lost labels, operand descriptors, constants, world, and return-pack shape; |
| 4 | continued to select calls, transforms, and machine branches by callee text; |
| 5 | discarded lineage at the public object boundary; and |
| 6 | added host semantic machinery without an Idol owner or deletion trigger. |

| # | directive |
|---|---|
| 1 | A real current-source unit run remained at the known baseline, 1165 of 1168, but that is only regression evidence and does not establish the semantic contract. |
| 2 | The prototype was rolled back. |
| 3 | This gap remains open. |
| 4 | The next slice must establish the Idol-owned application/fact representation first, then add only the smallest fail-closed, deletable Zig projection needed to carry the same identity through realization. |

| section |
|---|---|
| 2026-08-10 merge review of the replacement projection |

| # | directive |
|---|---|
| 1 | The next in-flight projection was reviewed at `db48178` while its five source files remained claimed and uncommitted. |
| 2 | It improves one prior defect: a checked plain call whose relation edge is missing now refuses with `checked-application-identity`. |
| 3 | It remains blocked from merge for four concrete reasons: |

| # | directive |
|---|---|
| 1 | `StableId` is still only a Wyhash `u64`. `id_index` keeps the first occupant of a collision, and DNIR stores only that hash. This is neither reversible canonical identity nor the required meaning/content/incarnation triple. |
| 2 | Application demand is copied from `call_shape.return_consumption`, an AST face. It is not produced by checked semantic demand and therefore cannot be called authoritative downstream. |
| 3 | Fail-closed coverage is incomplete. Static field/module calls, string primitives, hardware intrinsics, and other special call paths can emit `call_direct` without querying `checkedApplication`. Native validation only validates instructions that already carry `Instr.application`; it accepts an additional untracked call instruction. |
| 4 | The out-of-line projection contains only one result descriptor and ordered value hashes. It omits argument/result pack identities, labels, positions as facts, operand descriptors, constants, world, effects, witness, and provenance already owned by `lib/compiler/application.id`. Public application-to-instruction and machine-byte lineage is separately required by C0 and still has no Idol owner. |

| # | directive |
|---|---|
| 1 | The patch adds substantially more host semantic surface than it removes. |
| 2 | Do not merge it by weakening the four requirements or by relabeling the hash and AST consumption as canonical facts. |
| 3 | The smallest admissible bridge must make every realized application fail closed, carry exact graph-minted identities, and have an explicit deletion path into the Idol owner. |

| section |
|---|---|
| 2026-08-10 exact-handle projection review |

| # | directive |
|---|---|
| 1 | The in-flight replacement at `3b4b05a` plus the shared dirty graph/backend paths closes important parts of the rejected prototype. `StableId`, `stable_id`, and `id_index` are removed; exact graph `NodeId` coordinates now reach checked DNIR applications, regions, ARM64 instruction ranges, and the lineage-bearing Mach-O object projection. |
| 2 | A lease rejects coordinates from a replacement graph at the same allocator address. |
| 3 | Checked call selection is validated relation-first and fails closed when required relation, application, value, subject, descriptor, realization range, or graph-incarnation facts are missing or inconsistent. |

| # | directive |
|---|---|
| 1 | This is not transformation-lineage closure. `SemanticGraph.addShapeTransformApp` and `attachCallTransforms` create exact graph transform nodes, then separately call `transform_engine.logProvenance` with only shape/input/output hashes. `transform_engine.ProvenanceEntry`, its snapshot, JSON output, and proof log retain no graph transform, application, input value, output value, or graph incarnation coordinate. |
| 2 | The proof subject is also synthesized text. |
| 3 | Deleting fingerprints therefore preserves semantic selection but does not preserve the existing transformation explanation. |
| 4 | The checked native path currently refuses any application instruction whose operation is no longer `call_direct`; that is honest fail-closed behavior, not proof that constant folding or another transformation preserves application lineage. |

| # | directive |
|---|---|
| 1 | The graph-incarnation safety also has a measured physical cost that must remain bounded. |
| 2 | An ignored compile-time size probe on this exact dirty tree reported: |

```text
Lease 8, SemanticGraph 208, Node 464, Edge 12
DNIR Module 112, DNIR Instr 304, Region 88, Region Node 192 bytes
```

| # | directive |
|---|---|
| 1 | The lease adds one pointer-sized field to each coordinate-bearing module, region, machine output, and object projection, one lazy page-allocator anchor per leased graph, and a retain/release pair per region. |
| 2 | Region collections are currently constructed and freed as one slice, so per-region lease cloning is not yet justified by an escaping-region control. |
| 3 | Do not call the compiler-state effect preserved until a compile-latency/peak-state comparison is recorded or the lease is lifted to the narrowest shared owner. |
| 4 | Exact incarnation safety is required; duplicated proof storage is not. |

| # | directive |
|---|---|
| 1 | Remaining admission gates for this slice: |

| # | directive |
|---|---|
| 1 | graph transform node and exact input/output/application identities survive transformation proof and machine/object lineage without a hash standing in; |
| 2 | a transformed checked application retains its original occurrence and result lineage instead of being refused or logged only by fingerprint; |
| 3 | public production object emission exposes the exact lineage projection, not only private tests; |
| 4 | the lease/state delta is measured and either justified or reduced; and |
| 5 | the host projection names its executed canonical `.id` replacement and deletion gate. Until then GAP-137 remains OPEN. |

| section |
|---|---|
| 2026-08-10 ONE-ID admission supersedes the exact-handle approval |

| # | directive |
|---|---|
| 1 | The exact-handle direction above is no longer admissible as an endpoint under the owner ONE-ID ruling. |
| 2 | On local `f1cda68` with the shared graph/backend work dirty, an exact source census reports 194 `NodeId`, 15 `NodeKind`, six `EdgeKind`, and five `SemanticId` references under `src/*.zig`. |
| 3 | One remaining `StableId` occurrence is a stale relation comment. |
| 4 | These are not merely private array offsets: `NodeId` crosses semantic graph, graph query, DNIR, region, native backend, machine lineage, and object projection APIs; `NodeKind` and `EdgeKind` are exported as tool/query vocabulary; region scheduling exposes a second `NodeKind`; and `pass26_semantic_operation.SemanticId` is another native semantic registry. |

| # | directive |
|---|---|
| 1 | The new graph lease also crosses DNIR, region, native output, and object boundaries as a separate incarnation proof wrapper. |
| 2 | It is directionally safer than allocator-address continuity, but it preserves another public identity concept instead of projecting one resident `id` plus facts from the exact graph owner. |
| 3 | The previously measured 464-byte graph `Node`, 192-byte region `Node`, and per-consumer lease fields are compiler-state evidence against calling this conceptual split physically closed. |

| # | directive |
|---|---|
| 1 | The surrounding identity roots remain merge blockers even though they are not all introduced by this dirty slice: |

| # | directive |
|---|---|
| 1 | The former `pass26_descriptor_intern.Registry` mapped one 64-bit semantic fingerprint directly to an intern slot and treated a hit as established equivalence. This owner, its declaration hash, and its graph/JSON identity projections are now deleted. Descriptor lifecycle, recursion, and completion remain facts produced directly during graph lift. |
| 2 | `persistent_semantic_state` indexes reusable artifacts by a textual `entity_id` and permits reuse after fingerprint/version/target comparisons; it has no exact graph correspondence fact. |
| 3 | transform provenance remains a separate text/fingerprint log rather than a transformation id and exact input/output/application facts in the same universe. |

| # | directive |
|---|---|
| 1 | Therefore the current dirty graph/backend projection must not merge as identity closure. |
| 2 | The replacement gate is not another wrapper rename. |
| 3 | The graph owner must expose one compact resident `id`; semantic categories and relationships must be facts/relations about that id; any physical row or tag must remain private and derived |
| 4 | DNIR/region/machine consumers must carry that same id without minting their own identity or kind vocabulary; and hash/intern candidates must either be verified by exact authoritative facts or fail closed. |
| 5 | The combined unit gate passing does not override this architectural rejection. |

| section |
|---|---|
| 2026-08-11 descriptor identity owner deletion |

| # | directive |
|---|---|
| 1 | Descriptor alias lift no longer constructs a fingerprint registry or publishes semantic fingerprint, declaration fingerprint, or intern-slot fields. |
| 2 | These values had no production consumer below graph lift and therefore represented state and identity vocabulary without capability. |
| 3 | The graph still publishes descriptor lifecycle state, recursion classification, completion, shape, provenance, knowledge, and stage. |

| # | directive |
|---|---|
| 1 | The forced-collision control is deletion: descriptor meaning and graph facts no longer query a descriptor fingerprint at all. |
| 2 | This removes one identity root but does not close GAP-137; application facts, transformation lineage, realization correspondence, and machine/object lineage remain open. |

| section |
|---|---|
| 2026-08-11 unwitnessed region rewrite refusal |

| # | directive |
|---|---|
| 1 | The graph-backed region path now declines constant fusion and dead-constant pruning until the graph owns the rewrite law, exact input and output ids, application, witness, provenance, and selected realization lineage. |
| 2 | The physical discovery and rewrite routines remain candidate algorithms, but they cannot mutate production DNIR from coordinates and dependencies alone. |

| # | directive |
|---|---|
| 1 | A direct-native control over `x = 10; y = 32; x + y` returned 42 before and after and produced a 33,512-byte Mach-O in both cases. |
| 2 | Declining the unwitnessed fold changes `main` from three instructions to seven; one compile observation was 93 ms before and 85 ms after, which is not a benchmark. |
| 3 | Restoring the three-instruction realization is an explicit repair gate: publish and retain the exact graph rewrite facts through machine and object lineage, then select the fold from those facts. |
| 4 | A local DNIR witness or coordinate record is not a substitute. |

| section |
|---|---|
| 2026-08-10 mechanical `NodeId` rename is not ONE-ID closure |

| # | directive |
|---|---|
| 1 | The latest dirty projection replaces many `semantic_graph.NodeId` spellings with `semantic_graph.id`, but preserves the split ontology and must not merge in that form: |

| # | directive |
|---|---|
| 1 | `semantic_graph` still publicly exports `NodeKind` and `EdgeKind`, stores a kind on each 464-byte `Node`, and exposes name-plus-kind query selection. |
| 2 | region realization still publicly exports its own `NodeKind` and stores `relation_id`, `application_id`, `value_id`, and `subject_id` fields beside a region `id`. These are qualification-bearing identity fields, not facts named `relation`, `application`, `value`, and `subject` over one id. |
| 3 | functions continue to expose `semantic_identity` and `func_identity`; DNIR, regions, native output, and object validation carry `semantic_graph.Lease` as a public incarnation wrapper. |
| 4 | tests and diagnostics still teach application identity, function identity, node identity, graph identity, identity counts, and identity collisions as separate layer concepts. |
| 5 | name-and-kind lookup, AST pointer maps, source fingerprints, and the separate hash-only transformation log remain downstream reconstruction paths. |

| # | directive |
|---|---|
| 1 | The useful part of the dirty work is retained: dense integer coordinates, explicit subject/application/result facts, missing-argument fail-closed checks, and resident-graph lifetime validation are real obligations. |
| 2 | Correct them in place rather than discarding the work, but factor them through one `id` and facts. |
| 3 | Private storage may keep compact tags and indices; public consumers may not observe a node/kind/lease identity vocabulary. |
| 4 | A spelling-only rename that leaves those public concepts intact fails the negative control. |

| section |
|---|---|
| 2026-08-10 direct-to-C fail-open boundary |

| # | directive |
|---|---|
| 1 | Current `main` still treats every direct-backend refusal as permission to enter generated-C realization in `auto` mode. `src/main.zig` catches the result of `emitObjectForExecutableWithGraph` at the direct executable seam and continues to `CodeGen.emit_module`. |
| 2 | The producer does not expose enough semantic facts for that decision: `native_backend.Error.UnsupportedProgram` currently represents both a lawful physical subset refusal and invalid or incomplete meaning such as missing application lineage, an orphan subject, partial realization lineage, an unwitnessed transform, a graph mismatch, and unknown machine lineage. |

| # | directive |
|---|---|
| 1 | The consumer must not classify those cases by matching `refusalNote()` text or by adding a second backend-local taxonomy. |
| 2 | The realization producer must expose an authoritative outcome over the same id/fact universe which establishes one of these facts: |

| # | directive |
|---|---|
| 1 | meaning is resolved and a generated-C realization is witnessed equivalent for the demanded semantics; or |
| 2 | semantic facts, correspondence, witness, or lineage are invalid, incomplete, or unknown and compilation must diagnose before any alternative backend. |

| # | directive |
|---|---|
| 1 | Until that fact crosses the interface, `auto` fallback is `OWNER-BLOCKED` and cannot be made fail-closed truthfully in `src/main.zig` alone. |
| 2 | Preserve and correct the current graph/backend work rather than discarding it, but do not let its generic error bucket authorize another semantic interpretation. |

| # | directive |
|---|---|
| 1 | Required negative controls: |

| # | directive |
|---|---|
| 1 | remove or corrupt one required relation, application, subject, result, witness, or lineage fact and prove `auto` exits diagnostically before C emission; |
| 2 | make direct machine realization physically unavailable while keeping meaning complete and prove C is considered only when exact equivalence and lineage are witnessed; and |
| 3 | vary the diagnostic text and prove realization selection is unchanged. |

| section |
|---|---|
| 2026-08-10 semantic graph preservation gate |

| # | directive |
|---|---|
| 1 | ONE-ID strengthens the semantic graph; it does not replace the graph with pipeline-local records. |
| 2 | The target remains one universe of ids, facts, and relations with specialized physical storage. |
| 3 | Binding, descriptor composition, subject, application packs, value flow, dependencies, worlds, effects, stages, transformations, provenance, demand, realization, invalidation, foreign lift, tool queries, and source-to-object lineage must remain directly queryable. |

| # | directive |
|---|---|
| 1 | Preserve the useful parts of the current dirty projection: exact resident ids, ambiguity refusing instead of first-name selection, checked application facts, and application-to-machine ranges. |
| 2 | Correct these parts in place rather than discarding them. |
| 3 | The current projection is still blocked because it retains a public giant optional `Node`, `NodeKind`, `EdgeKind`, a second region `NodeKind` and edge vocabulary, `semantic_identity`/`func_identity`, and qualification- bearing `relation_id`/`application_id`/`value_id`/`subject_id` fields. |

| # | directive |
|---|---|
| 1 | The physical replacement is compact graph storage, not a generic fact map: |

| # | directive |
|---|---|
| 1 | dense resident ids; |
| 2 | specialized dense or sparse fact columns; |
| 3 | packed relation, pack, provenance, and dependency ranges; |
| 4 | compact derived indexes for applications and other frequent queries; and |
| 5 | private lifetime/context enforcement that cannot become semantic identity. |

| # | directive |
|---|---|
| 1 | The last requirement is currently regressed by the dirty diff. |
| 2 | It removes graph ownership checks from DNIR, regions, machine lineage, and object lineage while bare `u32` ids still cross those boundaries. |
| 3 | Removing a public lease wrapper is correct only when ownership moves into one private shared graph context or structural lifetime makes cross-universe mixing impossible. |
| 4 | Preserve the negative control: equal numeric ids from two resident graphs must never satisfy each other's facts or lineage. |

| # | directive |
|---|---|
| 1 | Capability-preservation admission must answer from the one universe, without text/hash reconstruction: relation, subject, argument/result packs, descriptors, transformation inputs/outputs, world/effects, demand, lawful and selected realization, machine/object ranges, provenance, dependencies, invalidation, tooling navigation, and proved cross-build correspondence. |
| 2 | If removing a node/kind wrapper makes any answer harder, the migration is not yet the graph-native replacement required here. |

| # | directive |
|---|---|
| 1 | Current dirty-slice review adds four concrete closure requirements: |

| # | directive |
|---|---|
| 1 | one graph-owned packed application query must retain ordered arguments and results plus world, effects, demand, witness, transformation, provenance, and selected realization; the current region/native tuple retains only relation, application, one result, subject, descriptor, and caller; |
| 2 | every semantic direct call requires those facts even when the graph reports zero checked applications; an empty application census cannot authorize an identity-free `call_direct` or callee-text selection; |
| 3 | transformation and realization joins consume exact ids and facts rather than region-local transform kinds, textual `subject_entity`, or package-prefixed record names; and |
| 4 | refusal evidence belongs to the per-compilation outcome, not process-global mutable location/note buffers which concurrent compilations can overwrite. |

| # | directive |
|---|---|
| 1 | Retain the sound allocation-failure cleanup, ambiguity refusal, packed argument range algorithm, exact-once application validation, unwitnessed-transform refusal, and machine byte ranges. |
| 2 | These are obligations to move behind the one graph fact interface, not work to delete. |

| section |
|---|---|
| Algorithmic convergence gate |

| # | directive |
|---|---|
| 1 | Closing the object graph is not permission to recreate a conventional pass pipeline. |
| 2 | The final graph owner must expose three reusable mechanisms from the same ids, facts, relations, dependencies, witnesses, and provenance: |

| # | directive |
|---|---|
| 1 | a sparse monotone consequence worklist whose fact families supply lattices, with SCC condensation and exact invalidation so unchanged facts do not pay for repeated whole-graph scans; |
| 2 | bounded persistent witnessed equivalence attached to the semantic graph, never a second e-graph IR, with growth controlled by demand, profile, expected gain, reuse, compile work, and peak state; and |
| 3 | demand/profile/cost-budgeted realization extraction, where layout, outlining, allocation/scheduling, and verified local synthesis are physical candidate strategies rather than semantic operation vocabularies. |

| # | directive |
|---|---|
| 1 | Specialized columns, packed adjacency and pack ranges, bitsets, arenas, and derived indexes are the physical substrate. |
| 2 | Memory and observable effects use graph-owned version/dependency facts rather than repeated alias walks or a parallel MemorySSA ontology. |
| 3 | Module and foreign-unit summaries are exact projections that permit selective subgraph import; they never mint module-local semantic identity. |

| # | directive |
|---|---|
| 1 | This does not authorize implementing these engines in Zig ahead of the current authority boundary. |
| 2 | First make the complete application/fact query authoritative and remove AST reconstruction below resolution. |
| 3 | Then transfer the engines into canonical `.id` in compiler-B dependency order. |
| 4 | Any temporary host edit must delete more semantic authority in the same slice. |

| # | directive |
|---|---|
| 1 | Required controls eventually prove that changing the physical graph layout, forcing every accelerator hash to collide, changing region partitions, or removing an equivalence index preserves semantic query answers. |
| 2 | Fact perturbation must enqueue only exact dependents, and every extracted machine choice must retain its equivalence witness and source-to-object lineage. |

| section |
|---|---|
| 2026-08-23 APPLICATION-CONSUMER-ZERO reconstruction census (base ce03c7eb) |

| # | directive |
|---|---|
| 1 | Exact, re-runnable measurement of every consumer that reconstructs an application fact instead of reading it from the graph. |
| 2 | Every row is a shell command over `src/*.zig` at `ce03c7eb`, clean tree. |
| 3 | Counts are occurrences, not files. **The stated worklist was partly stale in both directions**: the subject rows are already closed, and two rows nobody had named are open. |

| # | directive |
|---|---|
| 1 | Baselines at `ce03c7eb`: `zig build --summary all` 5/5 succeeded; `zig build unit-test` exit 0; direct-backend sweep over 517 `examples/**/*.id` (`idol compile --backend=direct`) = 198 OK, 177 DNB001, 69 DNB011, 5 DNB007, 68 no-entry. |

| section |
|---|---|
| C1 — `call_direct` emitted with no application identity |

| # | directive |
|---|---|
| 1 | `grep -n 'op = \.call_direct' src/dnir_lower.zig`, then test each emit expression for `.application =`. |

| # | directive |
|---|---|
| 1 | **10 of 15 emitters carry no identity.** With `require_graph_facts` (checked path) the guards make all but one unreachable: |

| emitter | reachable when checked | why |
| --- | --- | --- |
| `dnir_lower.zig:7591` `lowerRecordCallAssign` | no | sole caller `tryAssignRecordCallFromExportMap:7598` returns false when `require_graph_facts` |
| `dnir_lower.zig:10568`, `10572` `lowerSubjectTail` | **YES** | `text:tail()` is a GAP-155 bootstrap face; `lowerSubjectCall:11030` prefers the checked call only when the graph published a relation |
| `dnir_lower.zig:12315`, `12319` `lowerNamedDirectCall` | no | all three callers (`10996`, `12328`, `12345`) are `!require_graph_facts` |
| `dnir_lower.zig:12747`, `12751` dotted member | bootstrap only | after the `12378` refusal guard |
| `dnir_lower.zig:12769`, `12777` bare-name callee | bootstrap only | after the `12378` refusal guard; every `.call`/`.name` bootstrap face (`gatecap`/`write`/`close`/`print`/`to`) is answered by an earlier branch |
| `dnir_lower.zig:12959` `lowerF64KernelCall` | bootstrap only | after the `12378` refusal guard |

| # | directive |
|---|---|
| 1 | Identity-carrying emitters: `7468`, `7486`, `7557`, `10239`, `10326`. |

| section |
|---|---|
| C2 — callee-TEXT selection survives in the readiness predicate |

| # | directive |
|---|---|
| 1 | `native_ir.zig:513-516`: |

```zig
if (i.application == null) {
    if (!calleeIsModuleLocal(m, i.callee)) return false;
    continue;
}
```

| # | directive |
|---|---|
| 1 | `calleeIsModuleLocal` (`native_ir.zig:485`) compares `i.callee` against `function.name` strings. |
| 2 | This is the exact construct this gap's 2026-08-10 semantic-graph-preservation entry forbids by name: *"an empty application census cannot authorize an identity-free `call_direct` or callee-text selection"*. |
| 3 | One site, and it is the whole checked-path hole: `Module` already carries `graph`, so `!m.graph.?.gateTransportModule()` derives "this module was lowered with graph facts required" with no new state, and the escape is admitted on the checked path anyway. |

| # | directive |
|---|---|
| 1 | The predicate also never asks for `target` or provenance, so a checked `call_direct` may realize with `applicationTarget`/`applicationProvenance` unknown. |

| section |
|---|---|
| C3 — subject: CLOSED, no reconstruction remains |

| # | directive |
|---|---|
| 1 | `grep -rn '\.subject' src/*.zig` = 114 occurrences. |
| 2 | Sema-shaped subject reads (`fact.subject` on a `sema.ApplicationFact`) survive at exactly **3** sites, all inside the single producer `src/semantic_graph.zig`: `4382` (operand-name lift), `5013` (`verifyCheckedApplicationOperandPacks` continuity check), `5087` (the lift itself). |
| 3 | Zero in `dnir_lower.zig`, `native_backend.zig`, `native_ir.zig`, `graph_query.zig`. |

| # | directive |
|---|---|
| 1 | Consumers read `graph.applicationSubject` at **40** sites across `dnir_lower.zig`, `native_backend.zig`, `native_ir.zig`, `graph_query.zig`. |
| 2 | Worklist item "subject fields still read directly from host rows in multiple consumers" **does not reproduce at this revision**. |

| section |
|---|---|
| C4 — operand packs: CLOSED on the checked path |

| # | directive |
|---|---|
| 1 | `graph.applicationArguments` live consumers: `dnir_lower.zig:9159`, `dnir_lower.zig:9951`, `native_backend.zig:8481`, plus producer-side `semantic_graph.zig:5004` (continuity) and `6690`. `checkedScalarOperands` is the single operand path for checked applications. |
| 2 | No consumer re-walks `call.args` on the checked path. |
| 3 | Worklist item "operand packs are not uniformly consumed through `applicationArguments`" **does not reproduce**. |

| section |
|---|---|
| C5 — the Sema bridge |

| # | directive |
|---|---|
| 1 | `sema.ApplicationFact` (`src/sema.zig:383`) is host-shaped: `*const ast.FuncDecl` applied/target, `?*const Expr` subject, `[]const *Expr` arguments, `RT` result, `?[]const u8` home. |
| 2 | It is consumed at exactly **2** sites, both the graph lift (`semantic_graph.zig:5003`, `5066`) — i.e. one producer, one ingress. |
| 3 | There is no second consumer to route. |

| # | directive |
|---|---|
| 1 | `scripts/ledger/application.id` reports this row as open by grepping `src/sema.zig` for the string `applicationFact`, which matches that file's own accessor definition. **That row can never reach zero while sema is the checker**; it measures SHC, not consumer reconstruction, and it is the sole reason the ledger prints `closure OPEN (1 row)`. |

| section |
|---|---|
| C6 — unresolved-application diagnostics splice independent causes |

| # | directive |
|---|---|
| 1 | `native_backend.formatDirectCause:233-241` picks each of the three fields from a *different* diagnostic by three independent `orelse` choices: |

```zig
const missing     = diag.lowering.note() orelse diag.note();
const application = diag.application     orelse diag.lowering.application;
const relation    = diag.relation        orelse diag.lowering.relation;
```

| # | directive |
|---|---|
| 1 | `missing` prefers the lowering cause; `application`/`relation` prefer the backend cause. |
| 2 | When both recorded something, the emitted DNB011 names the occurrence of cause A and the missing fact of cause B. |
| 3 | Second splice: `dnir_lower.Diagnostic.record` (`dnir_lower.zig:65`) overwrites `site`/`note` but leaves `application`/`relation` from an earlier, unrelated `bindOccurrence` — so on a gate-transport module, where `OccurrenceBridge.init:266` binds the first unresolved occurrence and lowering then continues, any later refusal inherits that stale occurrence. |

| section |
|---|---|
| C7 — visibility from AST/path shape: NOT PRESENT |

| # | directive |
|---|---|
| 1 | `grep -rn 'is_public\\|isPublic\\|visibility' src/*.zig` = 1 hit, a parser diagnostic string (`parser.zig:1837`). |
| 2 | There is no public/private relation visibility concept in this tree to reconstruct. **Nothing to build; no consumer.** |

| section |
|---|---|
| C8 — `ast_ref` (graph backdoor to syntax) |

| # | directive |
|---|---|
| 1 | 72 occurrences |
| 2 | 55 in `semantic_graph.zig` (the owner). |
| 3 | 17 outside it: |

| # | directive |
|---|---|
| 1 | Provenance/index bridges (legitimate — an AST pointer used as a key, or handed to an evaluator that must run the expression): `dnir_lower.zig:271`, `2283`, `4851`, `4915`, `9829`; `graph_query.zig:284`, `340`, `448`; `demand.zig:490`; `dnir_lower.zig:13852-13853` (test). |

| # | directive |
|---|---|
| 1 | Semantic reads (real debt): **3** |

| # | directive |
|---|---|
| 1 | `dnir_lower.zig:3108` `memberDescriptorFromShapeAst` — resolves a member's DESCRIPTOR by casting `ast_ref` to `ast.AliasDef` and re-running `types.resolve` on the syntax. A descriptor the graph already owns. |
| 2 | `dnir_lower.zig:94` `applicationFace` and `native_backend.zig:147` `occurrenceFace` — both derive the relation face NAME from syntax (`c.func.name.ident` / `mc.method`) *before* asking the graph, and only fall back to the graph relation when there is no `ast_ref`. Diagnostic-only, but the precedence is inverted: the graph knows the relation. |

| # | directive |
|---|---|
| 1 | `graph_query.zig:143-149` `collectionRelationSite` additionally compares a declaration `name` against `expr.method_call.method` — a name test, documented as a deliberate refusal rather than a selection. |

| section |
|---|---|
| C9 — `NodeKind`/`EdgeKind` as meaning |

| # | directive |
|---|---|
| 1 | Outside `semantic_graph.zig`: **2** occurrences, one a test assertion (`dnir_lower.zig:14950`) and one a doc comment (`subject_home.zig:884`). |
| 2 | The vocabulary is no longer exported into consumers. `node.kind ==`/`!=` tests across `src/*.zig` = 297, but they are overwhelmingly lexer/parser token kinds and build-target kinds, not graph tags: graph-tag tests are `demand.zig:489`, `dnir_lower.zig:259`, `4854`, `13852`, `graph_query.zig:147` — five, each already paired with a fact query. **No mass rename is justified by measurement.** |

| section |
|---|---|
| C10 — pinned host-architecture debt, re-measured |

| pinned | measured at ce03c7eb | method |
| --- | --- | --- |
| backend imports of Sema, 39 sites | **does not reproduce.** `src/native_backend.zig` has **1** live reference — the `@import` at line 6 — and 64 test-only ones (65 exact-word `\bSema\b` total, first `test` at 9272). Tree-wide `@import("sema.zig")` = **53** occurrences in 22 files, of which **19** are live (non-test) | `grep -n '\bSema\b' src/native_backend.zig`; `grep -rn '@import("sema.zig")' src/*.zig` split at each file's first `^test ` |
| codegen constructing a Parser, 6 live sites | **confirmed, exactly 6**: `codegen.zig:4316`, `24499`, `24713`, `24764`, `25369`, `26103` (101 `Parser.init(` total; first `test` at 32644) | `grep -n 'Parser\.init(' src/codegen.zig` split at 32644 |

| section |
|---|---|
| AFTER — same measurements at `e3df9beb` |

| category | before (ce03c7eb) | after | what moved |
| --- | --- | --- | --- |
| C1 identity-free `call_direct` emitters | 10 of 15 | 10 of 15 | unchanged; on the checked path only the GAP-155 subject faces reach them, and that is now an OPEN ledger row rather than an unnamed one |
| C2 callee-text selection in readiness | 1 (`native_ir.zig:513`) | **0** | escape deleted on the checked path; `moduleIsChecked` derives the path from the resident graph |
| C2 checked target / provenance continuity | absent | present (`native_ir.zig:568-569`) | a checked `call_direct` now proves both against the graph |
| C3 subject reconstruction | 0 outside the producer | 0 | already closed at base |
| C4 operand packs off `applicationArguments` | 0 | 0 | already closed at base |
| C5 host-shaped fact consumers | 2, both the lift | 2, both the lift | one producer-side ingress; no downstream reader to route |
| C6 cause splice in DNB formatter | 1 | **0** | `selectedCause`; a face crosses only across occurrence equality |
| C6 stale occurrence outliving its cause | 1 (`OccurrenceBridge.init`) | **0** | the id is held on the index and bound by the consumer that refuses |
| C8 semantic reads through `ast_ref` | 3 | **1** (`dnir_lower.zig:112` / `native_backend.zig:186`, now the unresolved-only fallback) | descriptor-from-AliasDef deleted; face precedence inverted to ask the graph first |
| C9 `NodeKind`/`EdgeKind` as meaning | 2 (one test, one comment) | 2 | no measured consumer; not built |
| C10 backend live Sema refs | 1 | 1 | untouched |
| C10 codegen live `Parser.init` | 6 | 6 | untouched |

| # | directive |
|---|---|
| 1 | Evidence for every change in this slice: `zig build --summary all` 5/5; `zig build unit-test` 1712 pass / 1712 (1709 at base plus three added negative controls); direct-backend sweep over 517 examples byte-identical to `ce03c7eb`; and a full DNB message differential over all 246 refusing examples byte-identical (`TOTALCHANGED=0`). |

| # | directive |
|---|---|
| 1 | `gate/idiom.id` preflight cannot run: it fails to compile at `ce03c7eb` with the base binary (`DNB011 application: 929 relation: dot__std missing: application-operand-binding`) — the idiom.id blocker recorded in `AGENTS.md`, pre-existing and untouched here. |

| section |
|---|---|
| What was measured and deliberately NOT built |

| # | directive |
|---|---|
| 1 | **Relation visibility from AST/path shape** (worklist item 7). One grep hit in the tree and it is a parser diagnostic string. No concept, no consumer. |
| 2 | **`NodeKind`/`EdgeKind` → fact queries.** Two occurrences outside the graph, one a test assertion and one a doc comment. The five graph-tag `node.kind` tests that remain are each already paired with a fact query. A rename pass would move text and no authority. |
| 3 | **Routing the GAP-155 subject faces onto graph applications.** This is the one live identity-free `call_direct` on the checked path, and closing it means giving `text:tail()` and its siblings a real relation node — sema resolution, graph publication, then the checked lowering answers it. That is GAP-155 bridge death, not a consumer routing, and doing it from the consumer side would mint a second producer. Recorded as the open row in `scripts/ledger/application.id`. |
| 4 | **A cause-scope discipline for `dnir_lower.Diagnostic`.** `record()` still leaves `application`/`relation` from an earlier `bindOccurrence`, and about fifty `invalidGraphFacts` call sites do not bind one of their own. The one path where a bind provably outlived its cause is repaired; the general discipline needs every call site to declare its cause and has no measured defect left to justify it. |

| section |
|---|---|
| 2026-08-23 transformation-generated value identity — the census, and why the correspondence was NOT built |

| # | directive |
|---|---|
| 1 | The tight-definition/unrolling work raised a question this gap owns: when a transformation copies a body, do the copies share one lowering identity, so that *same originating semantic value* and *same transformed occurrence* become indistinguishable? |
| 2 | The answer is measured below and it is **no** — and because it is no, the explicit `V → V1..V4` correspondence has no consumer and was deliberately not built. |

| section |
|---|---|
| The copying transforms, exact |

| # | directive |
|---|---|
| 1 | Two transformations on the graph-native path turn one source loop into more than one physical occurrence of its body. |
| 2 | Both were enumerated by reading every duplication site under `src/*.zig`; nothing else on that path copies a lowered entity (`recurrence.zig` and `loop_closure.zig` close loops to a closed form, `native_backend.immHoistEnter` hoists and reuses, `region*.zig` projects one node per instruction, `abi_specialize.zig` mutates in place). |

| transform | copy site | copies | default |
| --- | --- | --- | --- |
| `dnir_lower.emitUnrolledWhilePrologue` (`src/dnir_lower.zig:5978`) | `src/dnir_lower.zig:6113-6119` — copies the lowered range `[body_start, body_end)` verbatim through `unrollPhysicalCopy` (`:5969`), adjusting only `branch_target` | `factor-1` extra, plus the residual (the same `ws.body` lowered a second time) = K+1 occurrences, K ∈ 2..16 | **OFF.** `IDOL_UNROLL` unset, empty, `off`, `0`, `1` and malformed all give factor 1 (`src/dnir_lower.zig:5561`) |
| `dnir_lower.tryEmitVectorReductionPrologue` (`src/dnir_lower.zig:7140`) | emits a NEON group-sum ahead of the loop; the scalar loop then lowers unchanged as the residual | 1 source loop → 2 physical loops | **ON.** Both call sites (`src/dnir_lower.zig:4882`, `:6138`) are live `try` |

| # | directive |
|---|---|
| 1 | `src/mono.zig` is not on this path (its only consumers are `codegen.zig` and `explain_pipeline.zig`, i.e. the C bootstrap realization), and it deliberately holds no AST clone at all — `(template, substitution)`, module doc `:24-32`. |

| section |
|---|---|
| The census: how many ids cover more than one occurrence |

| # | directive |
|---|---|
| 1 | Measured at `dec7d509` with a throwaway instrument in `native_backend.validateDnirApplications` that counts, per module, the DNIR instruction multiplicity of every id in every identity role. |
| 2 | Subject: all `examples/**/*.id` |
| 3 | 228 modules reach the validator, 219 emit an object. |
| 4 | Both arms of the copying transform were run. |

| role | distinct ids | max instructions per id | ids on >1 instruction |
| --- | --- | --- | --- |
| `application` | 686 | **1** | **0** |
| `value` | 686 | **1** | **0** |
| `aggregate` | 4 | **1** | **0** |
| `relation` | 201 | 11 | 80 |
| `subject` | 646 | 2 | 2 |
| `target` | 201 | 11 | 80 |

| # | directive |
|---|---|
| 1 | Identical in both arms — `IDOL_UNROLL` unset and `IDOL_UNROLL=4`. |
| 2 | The six modules whose object bytes change at factor 4 (`native_differential/{canon10_helper_plus_record_scan, canon12_helper_call_record_scan, canon5_scan_shape, i8_whileparam, i9_nonlit, p02_loop}.id`) grow by 14–22 instructions each and their *identity-carrying* instruction count does not move at all (`carrying` 0→0 and 1→1). |
| 3 | All six answer the same value in both arms. |

| # | directive |
|---|---|
| 1 | `relation`, `subject` and `target` on more than one instruction is lawful sharing, not occurrence conflation: two applications of one relation on one subject are two occurrences of one relation. |
| 2 | Each of those instructions still carries its own distinct `application` and `value`. |

| # | directive |
|---|---|
| 1 | **So the exact count of sites where one id covers N>1 occurrences is zero**, under the production default and under the research setting alike. |

| section |
|---|---|
| Why it is zero, and that it is fail-closed rather than unexercised |

| # | directive |
|---|---|
| 1 | Three independent authorities, each refusing: |

| # | directive |
|---|---|
| 1 | `dnir_lower.unrollExprIsCopyable:5698` asks the graph's occurrence index (`ctx.occurrences`) and refuses any expression the graph published an application for — the producer's own record, not a guess about which syntax makes calls; |
| 2 | `dnir_lower.unrollRangeIsCopyable:5945` refuses any EMITTED instruction with `.relation`, `.application`, `.value`, `.subject`, `.aggregate`, `.target` or `.realization_start`, and admits only `.const`/`.store_local`/`.binop`/`.br`. `5418f032` widened the first four into that list and extracted `unrollPhysicalCopy` after the census below was taken at `dec7d509`; the census is unchanged by it, because it already measured zero; |
| 3 | `native_backend.validateDnirApplications:8876` requires `fact_count ∈ {0,3}` (`relation`, `application`, `value` travel together, `:8979`) and exactly one realization per published occurrence (`:9181`), and `native_backend.validateMachineLineage:9202` and `realization_validate.aggregateSchedule:322` require the same of machine lineage rows and of aggregate projections. |

| # | directive |
|---|---|
| 1 | Measured fail-closed, not assumed. |
| 2 | With **both** `dnir_lower` refusals destroyed in a scratch build (`unrollExprIsCopyable` no longer consulting `ctx.occurrences` and admitting `.call`; `unrollRangeIsCopyable` admitting `call_direct`/`mov_arg` and dropping the `.application`/`.target`/ `.realization_start` guards) and `IDOL_UNROLL=4`, a loop whose body is `t = t + bump()` does not answer wrongly — it refuses: |

```text
error: direct backend: DNB011 application: 8 missing: application-realization-count
hint: bail site: validateDnirApplications() at native_backend.zig:9181
```

| # | directive |
|---|---|
| 1 | Extending the damage to `.aggregate`/`load_index` and unrolling `t = t + xs[i]` likewise refuses, from the third authority: |

```text
error: direct backend: DNB011 application: unknown missing: aggregate-root-marker
hint: bail site: validateDnirApplications() at native_backend.zig:8886
```

| # | directive |
|---|---|
| 1 | The undamaged answers (9 and 450) are what both programs give at the default. |

| section |
|---|---|
| The control that was looked for and does not exist |

| # | directive |
|---|---|
| 1 | The admission bar for this work was a program where two occurrences of one semantic value must be distinguished and today's answer is wrong or ambiguous. |
| 2 | It was searched for and not found: |

| # | directive |
|---|---|
| 1 | five hand-written unrollable kernels (trip counts 0/1/3/4/5, a body with an `if`, a negative step, a two-term recurrence, a body with two intermediates) all fire the copy at factor 4 and all answer exactly what the rolled arm answers; |
| 2 | the six corpus modules that change bytes at factor 4 answer identically; |
| 3 | the one default-on duplicating transform reaches no example in the corpus at all (see below), and on a subject that does reach it (`gate/occurrence/reduce.id`) the loop it duplicates publishes **no application** — `load_index` there carries `application=null value=null aggregate=null`, and the module's only published application is the outer call, realized once. |

| # | directive |
|---|---|
| 1 | Without a control that fails before and passes after, the correspondence is unfalsifiable architecture, and this gap already records the standing prohibition on exactly that. |

| section |
|---|---|
| What was deliberately NOT built, and the measurement |

| # | directive |
|---|---|
| 1 | **`original V → occurrence V1..Vn` with per-occurrence machine lineage.** The only consumer such a fact could have is a transform that realizes one published occurrence more than once, and there is none: 0 of 228 modules, in both arms. |
| 2 | The projection it would have to split, `native_backend.MachineLineage`, has **zero readers outside `src/native_backend.zig`** — `main.zig:5649`, `:5765`, `:5811` and `:5820` call the four `…WithGraphLineage…` entry points and never touch `.lineage`; its only reader is the validator that counts it. |
| 3 | Splitting a one-row-per-application projection into one-row-per-occurrence would give a correct architecture one internal consumer and no external one: the producer-hollow refutation, not a win. |

| # | directive |
|---|---|
| 1 | **A distinct occurrence id on copied DNIR instructions.** What the copies actually share is `Instr.result` — a physical temp/slot number, which `law.md` §1 names as an encoding and not an identity. |
| 2 | Each copy writes it before reading it, and the arithmetic is measured equal on every subject above. |
| 3 | Renaming a physical slot per copy moves bytes and no authority. |

| # | directive |
|---|---|
| 1 | **Non-vacuity was landed instead.** `gate/occurrence/copy.sh` pins the census executable: §1 both copying transforms actually fire (a gate that measures a transform nothing reaches measures nothing), §2 the opt-in copy is byte-off at `IDOL_UNROLL` unset / `1` / `off` / `0` / empty, §3 both arms answer the same and the vector arm sums runtime data rather than a folded constant, §4 a loop whose body IS a published occurrence emits byte-identical objects at `IDOL_UNROLL=4`. |

| section |
|---|---|
| The determinism baseline the byte evidence rests on |

| # | directive |
|---|---|
| 1 | A byte comparison is evidence only if the emission reproduces, and a reachability census landed in this session reported that it may not. |
| 2 | Measured directly rather than assumed, at this head: |

| question | method | result |
| --- | --- | --- |
| does one subject reproduce? | `examples/native_differential/p02_loop.id` emitted twice to the same path | identical |
| does the OUTPUT path leak in? | same subject to `.../pa/x.o` and `.../pb/differentname.o` | identical |
| does the whole corpus reproduce? | two independent `--emit=obj` sweeps over all 547 examples into two different directories | **219 emitted objects, 219 identical, 0 differences** |
| do LINKED executables reproduce? | five subjects, same path twice and two different paths | identical in all cases |
| does the SOURCE path leak in? | the same text copied to `…/srcA/p02loop.id` and `…/srcB/p02loop.id` | **DIFFERENT** — `c1304ddc…` against `1fd2fa58…` |

| # | directive |
|---|---|
| 1 | So the artifact is deterministic and output-path independent, and the byte evidence above is not luck. |
| 2 | The one real hazard is the last row and it is a fact about identity, not noise: home mangling puts the source path into every symbol (`_idol_private_tmp_…_srcA_p02loop__main`), so **two arms that compile from different scratch directories differ in every byte**, which is indistinguishable from a realization change to a caller who did not control for it. |
| 3 | Every sweep reported here compiled the same relative source paths in both arms. `gate/occurrence/copy.sh` §0 now requires both halves — the digest must reproduce across output paths, and it must still MOVE when the source path does — before any section of it reads a digest. |

| section |
|---|---|
| One stale authority corrected on the way |

| # | directive |
|---|---|
| 1 | `src/dnir_hardware.zig` declined the `.vector` `Tier` on the measurement that `tryEmitVectorReductionPrologue` "is called from two sites and BOTH read `if (false) try tryEmitVectorReductionPrologue(...)`, so it has been unreachable since the commit that introduced it." The two tokens entered in `168b6afe` and were removed in `d58101bd`; both call sites are live plain `try`, and the producer fires on the default build with no env var, emitting `add.2d v16, v16, v17` / `addp.2d d16, v16`. |

| # | directive |
|---|---|
| 1 | The census NUMBER survives — re-run at `dec7d509`, assembly containing vector arithmetic is **0 of 219** emitting examples — but its cause was wrong: not an unreachable producer, but a recognizer no example's shape reaches (an all-literal table over a literal range folds before lowering, and a register-exploded table is declined). |
| 2 | The `.vector` declination is restated on the rule the same comment already gives — no intrinsic returns it, no check refuses on it — and the reduction is now shown to publish no capability fact at all (`hw == .none`, so `intrinsicOfOp` answers `null`). |
