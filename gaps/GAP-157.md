| field | value |
|---|---|
| title | GAP-157 — Zero std namespace elimination |
| status | OPEN |
| filed | 2026-08-12 |
| priority | P0 |
| kind | architecture |
| owner | Cursor (coordination / canonicality) |

<!-- idol-gap-frontier:v1:begin -->
```json
{
  "schema": "idol.gap.frontier.v1",
  "gap": "GAP-157",
  "status": "OPEN",
  "root_program": "lib/ layout home -> world projection -> gate/admission.sh code-position census",
  "current_blockers": [
    "Code-position std. reaches are nonzero and the census sits at its budget rather than under it.",
    "std.script.* is the bulk of the remainder and every canonical target for it is SEMANTIC-VOCABULARY-BLOCKED.",
    "gate/host.id states the law and cannot execute: it refuses at DNB001 application-operand-binding in checkedScalarOperand().",
    "Two different quantities are in circulation; only the gate/admission.sh code-position census is the ratchet."
  ],
  "superseded_observations": [
    "#the-ce03c7eb-denominator-reconciliation"
  ]
}
```
<!-- idol-gap-frontier:v1:end -->

| section |
|---|---|
| Current frontier notes |

| # | directive |
|---|---|
| 1 | The machine-read frontier is the `idol.gap.frontier.v1` block above; it is the one `gate/frontier.sh` validates and the one to trust. |
| 2 | This section is the EVIDENCE behind those blockers -- what was measured, what is already crossed and must not be redone, and the command that re-measures each claim. |
| 3 | It is not a second frontier. |
| 4 | Where it and the JSON block disagree, the JSON block wins and this section is the bug. |

| # | directive |
|---|---|
| 1 | **Frontier:** `law.std.zero` is not met. |
| 2 | Code-position `std.` reaches are nonzero and the census is sitting AT its budget, not under it. |
| 3 | The ratchet exists and holds; nothing is currently driving the number down. **Crossed:** `lib/std.id` exports no `std` binding -- its entire body is the ruling in comments plus the integer `0`, so acceptance item 2 is satisfied. |
| 4 | The position-blind census was deleted (`ee8adabf`) and replaced by two rules that deliberately disagree about position: the added-line QUARANTINE charges the raw spelling anywhere, the CENSUS charges only code position. |
| 5 | Acceptance item 5 is half superseded -- `gate/idiom.id` COMPILES AND RUNS at HEAD (it scans a piped unified diff and reports findings); only `gate/host.id` still refuses, and it refuses at DNB001 `application-operand-binding` in `checkedScalarOperand()`, not at DNB011 `dot__std`. **Next:** the reaches are concentrated, not diffuse. `std.script.*` is the bulk of them and every canonical target for it (`path:exists()`, `path:write()`, `path:open()`) is SEMANTIC-VOCABULARY-BLOCKED, so the lever is admitting that vocabulary, not editing call sites. |
| 6 | Repair `gate/host.id`'s lowering so the second law-stating gate can execute. **Verify:** `sh gate/admission.sh --census-only` IS the ratchet -- it prints the code-position reach count, the pinned exemptions and the budget, and that printed number is the budget. |
| 7 | Do not quote a `std.` count from prose; two different quantities are in circulation and `tools/node/dev/hostcensus` `std_dot` is a line-based `.id`-only trend row, not the ratchet. |
| 8 | Cross-check that the gate is not vacuous: it must report a nonzero subject count. **Blocked on:** semantic vocabulary admission for the filesystem/world faces (`path:exists`, `path:write`, `path:open`) -- see `docs/spec/std-zero-ledger.json` for the per-file second-blocker probe. **Measured at:** the arithmetic under `## Superseded observations` is `ce03c7eb` and does not describe HEAD. |
| 9 | Run the census. |

| section |
|---|---|
| Ruling |

| # | directive |
|---|---|
| 1 | **No `std` anywhere.** Not in source spellings, not as a runtime table, not as a migration alias, not as documentation teaching a namespace. |
| 2 | Vocabulary reaches through layout/home/world projection (`fs:read`, `json:encode`, `os.env[k]`, `io:read`) — never `std.*`. |

| # | directive |
|---|---|
| 1 | `lib/std.id` must not export a `std` binding. |
| 2 | The `lib/` tree is filesystem bootstrap provenance until renamed; it is not semantic authority and not a namespace to call. |

| section |
|---|---|
| Acceptance |

| # | directive |
|---|---|
| 1 | Law projections state zero `std` without softening language. |
| 2 | `lib/std.id` exports no `std` table. |
| 3 | Agent/MCP/gate surfaces contain zero `std.` spellings at CODE position. A detector needle inside a `"` literal is not a reach; a gate that cannot name the spelling it forbids cannot enforce it. |
| 4 | The ratchet is `sh gate/admission.sh --census-only`. It lexes each line and charges only code-position reaches. `tools/node/dev/hostcensus` `std_dot` is a `.id`-only line-based trend row, not the ratchet. |
| 5 | Added-line firewall: new code-position `std.` = 0 (`gate/admission.sh` rule B; `gate/idiom.id` and `gate/host.id` state the law but neither currently compiles — both refuse at DNB011 `dot__std`). |

| section |
|---|---|
| The denominator, ruled 2026-08-23 |

| # | directive |
|---|---|
| 1 | **The code-position count is the budget.** A `std.` inside a `#` comment or a `"` text literal resolves no home and reaches no world, so it cannot violate `law.std.zero`. |
| 2 | Charging it is not conservatism — it is a currency the ratchet should not accept, because deleting 123 characters of prose bought 123 real reaches with no gate objection. |

| section |
|---|---|
| Measured negative result |

| # | directive |
|---|---|
| 1 | Stripping `std.` from all 40 canonical files unblocked **zero** of them on the direct backend. |
| 2 | 36 of 40 still pass `idol check` afterwards, which is why check-pass was never evidence. `std.script.*` is 269 of the 350 code reaches and every canonical target for it — `path:exists()`, `path:write()` — is `SEMANTIC-VOCABULARY-BLOCKED`. |
| 3 | So is `path:open()`, the canon C0 `law.world.grant` names by hand. |

| section |
|---|---|
| Negative control |

| # | directive |
|---|---|
| 1 | Deleting `std` from prelude: canonical homes (`fs`, `json`, `os`, `io`, …) still resolve through layout projection. |

| section |
|---|---|
| Superseded observations |

| # | directive |
|---|---|
| 1 | The block below is the `ce03c7eb` reconciliation that produced the code-position ruling above. |
| 2 | Its numbers are that revision's, not HEAD's, and they are exactly the kind of frozen count this repository does not carry in prose -- they are kept only because the ruling is unintelligible without the arithmetic that forced it. |
| 3 | For the live number run `sh gate/admission.sh --census-only`. |

| section |
|---|---|
| The ce03c7eb denominator reconciliation |

| # | directive |
|---|---|
| 1 | Two numbers were live for one ratchet: `473` (all-position tokens minus whole-file exemptions) and `335`/`350` (code-position sites). |
| 2 | They are not two estimates of one quantity; they measure different things. |

| # | directive |
|---|---|
| 1 | Since `ee8adabf` main carries NO census at all — the position-blind one was deleted along with `gate/std-fixtures.manifest`, and the objection recorded against it was correct. |
| 2 | But a quarantine on added lines cannot ratchet: it stops growth and applies no downward pressure to the reaches already in the tree. |
| 3 | Both rules now ship, and they deliberately disagree about position. |
| 4 | The QUARANTINE charges the raw spelling at any position on an added line; the CENSUS charges only code-position reaches. |

| # | directive |
|---|---|
| 1 | The arithmetic, measured at `ce03c7eb`, residual zero: |

```text
547  word-bounded `std.` tokens, all positions = 350 code + 114 text + 83 comment
512  ... with an identifier after the dot (the old regex's tail); the 35 it
     dropped are bare `std.` and ALL 35 sit in text or comments
473  = 512 - 39 whole-file pins over 6 gate scanners; ALL 39 are non-code
350  code-position reaches, every class
335  ... of those in files classed `canonical`

473 - 335 = 138 = 123 text/comment charged outside the pins + 15 code reaches
                  in non-canonical files (14 historical, 1 compile-fail fixture)
```

| # | directive |
|---|---|
| 1 | Moving to code position **lowers** the enforced number and removes headroom: the old shape also exempted six whole files from the added-line rule, so a real reach added to `gate/idiom.id` passed unseen. |
| 2 | Full reconciliation, the per-file second-blocker probe, and the hand-merge instruction for `reconcile/shadow-clean-20260822` are in `docs/spec/std-zero-ledger.json`. |
