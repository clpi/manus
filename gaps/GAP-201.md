# GAP-201 — semantic graph authority completion / physical-only DNIR

**Status:** OPEN · **Filed:** !2026-08-19T18:00:00Z  
**Priority:** P0 · **Kind:** architecture_and_performance_gap  
**Filed by:** canonical-convergence-20260819  
**Files:** src/semantic_graph.zig, src/dnir_lower.zig, src/native_ir.zig, src/native_backend.zig, src/codegen.zig, src/table_apply.zig, src/place.zig, src/escape.zig, src/table_facts.zig, src/const_table.zig, src/home_resolve.zig, src/native_bootstrap.zig, src/ast.zig, docs/spec/law.md

The active compiler still reconstructs some semantic decisions from AST pointers,
names, paths, syntax variants, local type/result/effect classifiers,
string-matched builtins/worlds, and DNIR op/field tags after resolution. Each
duplicate classifier is both a soundness seam and a barrier to whole-graph
specialization.

All divergent research histories found in the 2026-08-19 census are direct
ancestors of `main`; `research/branches/manifest.json` gives every retained head
an explicit current disposition. A branch is research provenance, never an
alternate current tree.

## Landed vertical slices

### Call and computed-projection category split — LANDED, UNMEASURED

- `6ded6d2c380ad5b3b8a74d5bcacc2fc2129dfbb8` completed the active
  `src/table_apply.zig` replacement;
- general typed-array/table `call -> index` rewriting was deleted;
- `calleeIsArray`, `positionalResultDepth`, `TableNames`, `array_types`, and
  process-global `active_names` were deleted;
- ordinary `t(1)` remains application and `t[1]` remains indexed projection;
- user-bound `env` wins over bounded ambient-world compatibility;
- bounded OS-world ingress carries an explicit bound-name census;
- `sema.TypeMap` remains only as an inert public compatibility parameter until
  the large driver/test callers move together.

This closes the active general call-shaped indexing rewrite. It does **not** yet
replace ambient source spelling with exact graph world-member identities.

### Aggregate-only place production — LANDED, UNMEASURED

- `f5857e0ac20011e0b2eedefb815f4d4376304f21` activated aggregate-only place
  production while retaining current `place.Site` compatibility;
- scalar values, parameters and homes are no longer produced as places;
- `[]` is the only computed aggregate-place access face;
- `()` never records an indexed place read/write;
- multiplicity, constant/runtime index, alias, escape, lifetime, extent,
  determinacy, immutability and residency refusal remain;
- more than a thousand lines of rejected place ontology/census work disappeared.

Exact access/subplace ids are still not graph-owned; name/AST provenance remains
an explicit bridge.

### Aggregate escape authority — LANDED, UNMEASURED

`c51f724a2526809b4b09907f5cae79cfc389476c` removed the zero-production-consumer
Symbol escape/ARC authority (`canStackAllocate`, `shouldPruneArc`, `typeNeedsArc`)
and its incomplete `address_taken` model. One conservative aggregate escape
analysis remains:

```text
t[k]       projection; no whole-aggregate escape by itself
t(k)       ordinary application; aggregate is consumed
t:len()    extent observation; does not require aggregate escape
```

Focused parser controls prove bracket projection, ordinary application, argument
escape and whole-binding rebind. This deletes a duplicate weaker authority
without changing a production consumer, because the removed API had none.

### Positional table facts — LANDED, UNMEASURED

`638e2251d28956c6ae040a36997a573e8d2ce514` collapsed the research-era table
facts implementation to its live conservative fact/decision interface:

- no `sema.TypeMap`;
- no `table_apply` dependency;
- all source fixtures use canonical `[]`;
- `t(k)` has a negative control and is aggregate escape, never an index;
- constant/runtime index, contents, extent, mutation, escape, out-of-range trap,
  no-size-bound elimination, uniform materialization and fail-closed unmodelled
  behavior remain.

The physical threshold remains one exported value consumed by lowering; width
chooses a physical family only after the existence/elimination question.

### Constant aggregate data — LANDED, UNMEASURED

`5f29db84f5559450124b928d4773645bc361c0cf` retained the live native
read-only-data optimization while removing its obsolete source-test normalizer:

- source licensing consumes `table_facts` directly under canonical `[]`;
- no sema/type-map/table-apply dependency remains in this optimization;
- DNIR independently verifies exact literal initialization, one store per slot,
  initialization-before-read, no redefinition, read-only subsequent mentions and
  no base escape;
- empty licence, mutation, duplicate initialization and escape controls remain;
- runtime index is allowed for read-only data but correctly blocks full absence.

The source-name/content-to-DNIR-base join is intentionally conservative until
exact aggregate/place graph ids replace it. Content collisions can only refuse an
optimization, never license a dirty table.

### Source-view length mechanism — PRESENT, UNMEASURED

`src/source_cursor.zig` carries a byte slice and reads `self.bytes.len` in O(1).
The historical repeated-`strlen` quadratic defect is not active in this host
cursor. Generated-output agreement, authoritative lexer transfer and large-input
scaling still require exact-head execution.

### Research disposition — LANDED

`research/branches/manifest.json` v2 assigns every retained divergent head one of:

- absorbed;
- partial gap with exact current owner;
- control/evidence only;
- historical rejected surface.

It separately records the current disposition of quotienting, recursive demand,
guarded order statistics, observer virtualization, cache/artifact soundness,
lexer length and machine-code losses. A stale branch ref therefore carries no
semantic admission power.

### Regression enforcement — LANDED, UNEXECUTED ON CURRENT HEAD

`gate/semantic-graph-authority.sh` protects:

- application/index category separation;
- aggregate-only place production;
- deleted Symbol escape authority;
- aggregate bracket/call escape controls;
- O(1) source length;
- the remaining exact bridge classes as visible OPEN findings.

CI invokes the authority and semantic-graph gates before building. No current
workflow execution has been observed through the available connector, so this is
source-level enforcement awaiting an actual runner, not green evidence.

## Current exact debt

### Occurrence and application identity

- `OccurrenceBridge` joins applications to expressions by `*const Expr`.
- `applicationFace` in DNIR lowering and `occurrenceFace` in native realization
  still reconstruct diagnostic relation faces from `Node.ast_ref`; the DNIR
  version currently consults AST provenance before the already-available graph
  application relation.
- AST-driven realization walkers recover application ids from provenance rather
  than receiving exact graph occurrence ids in work items.
- graphless convenience lowering strips orphan handles after the fact instead of
  making graph ownership/id lifetime explicit at its API boundary.
- `native_bootstrap.zig` still recognizes bootstrap application meaning from
  source names/roots/method strings and is consulted by graph/lowering gates.

### Projection, world and place identity

- general call-to-index rewriting is gone, but bounded ambient OS compatibility
  still recognizes world members from source spelling before exact graph
  world-member identities exist;
- aggregate place kind and source semantics are corrected, but source place facts
  are still keyed by names/AST bindings and constant-data DNIR recognition by
  base temp/content;
- exact root-place, projection/subplace, read/write occurrence and provenance ids
  are not published once and carried through all consumers;
- parser, sema, graph, direct, C, Wasm and tooling candidate sets are not yet one
  shared graph query.

### Name/path/string reconstruction

- name-keyed place lookup and AST initializer provenance;
- path/home/symbol reconstruction after exact entity resolution;
- string-matched worlds, ambient roots, builtin families, conversions, egress,
  memory/string/math/OS operations;
- callee spelling used to infer ABI/result class where graph descriptors exist or
  should exist.

### Descriptor, result, demand and effect duplication

- local predicates such as string/integer/bool/f64/result/record classifiers;
- separate parameter/result ABI maps reconstructed by export name;
- syntax-shaped effect and void/result-demand classification;
- backend eligibility decided by local enums rather than graph-owned laws/target facts;
- `ast.FuncBody` still owns a large optimizer/codegen payload (`use_*`, dense
  table, algorithm, device/profile/autodiff/unroll state) that must migrate as
  graph transformation/realization facts rather than be deleted piecemeal.

### Transformation lineage

- lowering performs folds, tail rewrites, vector reductions, bounds/trap
  insertion, representation decisions and other semantic transformations without
  graph transformation ids/witnesses;
- occurrence/value/result lineage is repaired retrospectively instead of derived
  from exact replacement edges;
- retained quotient, recurrence, guarded-termination and observer research cannot
  be fully reactivated until this lineage exists.

### DNIR semantic leakage

- DNIR op/tag/field conventions carry vector, trap, bounds, builtin, world or
  transformation meaning instead of only target realization encoding;
- physical operands and semantic facts are not cleanly separated at every API;
- direct, C and Wasm backends still contain local semantic candidate logic.

### Consumer and authority duplication

- grammar/formatter/Tree-sitter/LSP/MCP projections retain local spelling/role
  knowledge in places;
- consumer-zero or weakly consumed research modules must continue to be deleted
  or explicitly quarantined when found;
- direct, C, Wasm and tool paths do not consume exactly one closed graph
  representation for every admitted construct;
- native/self-host projections must pin the exact current upstream subject and
  execute agreement gates after authority changes.

## Required vertical order

1. **Occurrence work items:** graph publishes exact application/value ids into
   resolver/lowering work items; delete AST-pointer joins and orphan-handle
   stripping.
2. **World/application closure:** graph publishes exact ambient/foreign/bootstrap
   relation ids and witnesses; make diagnostics graph-first, then delete
   name/root/method recognition and the inert table-ingress type-map API.
3. **Exact access/place ids:** graph-owned projection/access occurrence ids,
   root-place/subplace facts, determinacy, alias, escape, lifetime, extent and
   residency; replace source-name/content/base-temp joins while preserving the
   landed conservative controls.
4. **Descriptor/result/effect facts:** one producer supplies operand/result packs,
   descriptors, effects, world demands and ABI facts to every backend.
5. **Transformation lineage:** every fold, quotient, contraction, early exit,
   vectorization, fusion or layout change is a graph transformation with witness
   and occurrence/value/machine lineage.
6. **Physical-only DNIR:** DNIR carries graph ids plus target realization choices
   and physical operands; semantic local taxonomies monotonically reach zero.
7. **One backend/tool contract:** direct, C, Wasm, JIT/interpreter, LSP, MCP and
   evidence consume the same graph semantics and differ only in physical
   realization/projection.
8. **Performance exploitation:** rerun retained algorithmic and machine research
   on the closed graph; integrate only Pareto wins or physical optima and expose
   every same-algorithm loss.

## Permanent negative controls

- two source faces with one witnessed meaning share semantic identity while
  retaining distinct provenance;
- application and computed projection never share occurrence identity merely
  because compatibility can rewrite a historical spelling;
- local/parameter bindings shadow ambient worlds without process-global state or
  downstream string-name exceptions;
- scalars, parameters and homes never become places because an implementation
  stores or names them;
- a dead semantic helper with no production consumer is deleted or moved to
  research rather than kept as apparent compiler capability;
- unknown graph facts refuse or conservatively fall back, never infer from
  opcodes, paths, spelling, declaration/load order or host type;
- direct and C backends produce behavior-equivalent observations for the admitted
  subset, including traps, effects and ordering;
- graphless APIs cannot return handles whose owner has been destroyed;
- concurrent compilation cannot cross-wire objects, graph ids, diagnostics or
  cache entries;
- deleting any required semantic fact producer makes a focused gate fail;
- every gate examining zero subjects fails rather than reporting green.

## Performance acceptance

Each vertical slice reports separately:

1. semantic/algorithmic work removed;
2. same-algorithm machine-code quality;
3. compiler/analysis work and memory;
4. startup, artifact, import and relocation cost;
5. applicability over ordinary corpus and constructed controls;
6. exact refusals and no-gain cases.

The landed source deletions reduce classifier/census work by construction, but no
runtime, compile-time or memory number is claimed without an exact clean-head
run. Historical research speedups remain attached to their historical subjects
until rerun. A semantic cleanup with no activated performance mechanism must not
silently reset or hide the strongest executable baseline.

## Closure acceptance

Close only when:

- every production application, projection, place, result, effect and
  transformation reaches machine/evidence through exact graph ids with no
  AST/name/path/string semantic reconstruction;
- DNIR is demonstrably physical-only and regenerable from graph facts;
- all backend/tool consumers agree by construction rather than duplicated tests;
- retained FTCFTW mechanisms are remeasured on the exact clean closed-graph head;
- compiler B/SHC and Wasm end-to-end status are reported independently and
  honestly;
- ablation of any required graph fact turns a focused gate red.

## C0 alignment

- **constitutional owners:** id · fact · relation · application · value · place · demand · observer · transformation · realization · machine · evidence
- **kind delta:** negative; rejected place kinds, Symbol escape authority and call-shaped indexing were removed
- **registry delta:** negative; table call/type/name registries and dead escape classifiers were deleted
- **source syntax delta:** 0 after delimiter closure
- **semantic invariant:** one id/fact universe survives every phase; physical encodings never decide meaning
- **physical mechanism:** compact graph-indexed work items and target realization records
- **foreign implication:** foreign laws ingest into the same graph with exact provenance/witnesses
- **world implication:** world/effect/authority facts are graph inputs, not ambient host globals
- **demand implication:** unobserved work is deleted before representation
- **evidence:** exact-head differential, ablation, corpus applicability, instruction/cycle/compile/memory/artifact matrices
- **landed deletion witnesses:** general call-to-index conversion, `active_names`, `calleeIsArray`, `positionalResultDepth`, scalar/parameter/home place production, dead Symbol escape/ARC helpers, table-facts sema/table-apply harness, const-table sema/table-apply harness
- **remaining deletion witnesses:** inert production `table_apply` type-map parameter, `OccurrenceBridge`, AST relation-face reconstruction, bootstrap name classifier, name-keyed/content-joined place authority, local result/type/effect classifiers, AST optimizer payloads, semantic DNIR tags, retrospective application accounting
