# GAP-201 — semantic graph authority completion / physical-only DNIR

**Status:** OPEN · **Filed:** !2026-08-19T18:00:00Z
**Priority:** P0 · **Kind:** architecture_and_performance_gap
**Filed by:** canonical-convergence-20260819
**Files:** src/semantic_graph.zig, src/dnir_lower.zig, src/native_ir.zig, src/native_backend.zig, src/codegen.zig, src/table_apply.zig, src/place.zig, src/home_resolve.zig, docs/spec/law.md

The active compiler still contains semantic decisions reconstructed from AST
pointers, names, paths, syntax variants, local type classifiers, string-matched
builtins/worlds, and DNIR op tags after graph resolution. This violates the one
semantic graph and limits FTCFTW because each duplicated classifier is both a
soundness seam and a barrier to whole-graph specialization.

The complete divergent research histories that explored pieces of this closure
are now direct ancestors of `main`; see `research/branches/manifest.json`. Their
implementations are preserved but not bulk-admitted because they target stale
trees or legacy source surfaces.

## Current exact debt

### Occurrence and application identity

- `OccurrenceBridge` joins applications to expressions by `*const Expr`.
- AST-driven realization walkers recover application ids from provenance rather
  than carrying exact graph occurrence ids in work items.
- Graphless convenience lowering strips orphan handles after the fact instead of
  making graph ownership/identity explicit at the API boundary.

### Projection versus application

- `placeFold`/related lowering historically admit `.name`, `.call`, and `.index`
  under one path; scalar and call-shaped place folds belong to rejected models.
- `table_apply` and compatibility lowering can reinterpret call shape as index
  shape, preserving a syntax-driven semantic decision after delimiter closure.
- Read/write/index/call candidate sets are not yet proved identical between
  parser, sema, graph, direct lowering, C lowering, and native backend.

### Name/path/string reconstruction

- name-keyed place lookup (`placeNamed(name)`) and AST initializer provenance;
- path/home symbol reconstruction after exact entity resolution;
- string-matched worlds, ambient roots, builtin families, conversions, egress,
  memory/string/math/OS operations;
- callee spelling used to infer ABI/result class where graph descriptors exist or
  should exist.

### Descriptor/result/effect duplication

- local predicates such as string/integer/bool/f64/result/record classifiers;
- separate parameter/result ABI maps reconstructed by export name;
- syntax-shaped effect classification and void/result demand;
- backend eligibility decided by local enums rather than graph-owned laws and
  exact target facts.

### DNIR semantic leakage

- DNIR op/tag/field conventions carry vector, trap, bounds, builtin, world, or
  transformation meaning instead of only physical realization encoding;
- lowering performs semantic transformations without graph transformation ids,
  witnesses, and occurrence/value lineage;
- folded/application accounting is repaired retrospectively rather than derived
  from exact replacement lineage.

### Consumer and authority duplication

- grammar/formatter/Tree-sitter/LSP/MCP projections still contain local spelling
  or role knowledge in places;
- consumer-zero or weakly consumed research modules remain in production paths;
- direct, C, Wasm, and tool paths do not yet consume exactly one closed graph
  representation for every admitted construct.

## Required vertical order

1. **Occurrence work items:** graph publishes exact application/value ids into
   resolver/lowering work items; delete AST-pointer joins and orphan-handle
   stripping.
2. **Delimiter closure:** parser and graph keep `()` application and `[]`
   projection distinct; delete `.call` place/index folds and compatibility
   reinterpretation from semantic lowering.
3. **Exact access/place ids:** graph-owned projection/access occurrence ids,
   root-place/subplace facts, determinacy, alias, escape, lifetime, extent, and
   residency; delete name-keyed place authority.
4. **Descriptor/result/effect facts:** one producer supplies operand/result packs,
   descriptors, effects, world demands, and ABI facts to every backend.
5. **Transformation lineage:** every fold, quotient, contraction, early exit,
   vectorization, fusion, or layout change is a graph transformation with witness
   and occurrence/value/machine lineage.
6. **Physical-only DNIR:** DNIR carries graph ids plus target realization choices
   and physical operands; semantic local taxonomies monotonically reach zero.
7. **One backend contract:** direct, C, Wasm, JIT/interpreter, LSP/MCP, and
   evidence consume the same graph semantics and differ only in physical
   realization/projection.
8. **Performance exploitation:** rerun retained algorithmic and machine research
   on the closed graph; integrate only Pareto wins or physical optima and expose
   every same-algorithm loss.

## Permanent negative controls

- two syntax faces with one witnessed meaning share identity but retain distinct
  provenance;
- application and computed projection never share occurrence identity merely
  because a compatibility pass can rewrite syntax;
- local/parameter bindings shadow ambient worlds without string-name exceptions;
- unknown graph facts refuse or conservatively fall back, never infer from
  opcodes, path, spelling, declaration order, or host type;
- direct and C backends produce byte/behavior-equivalent observations for the
  admitted subset, including traps/effects/ordering;
- graphless APIs cannot return handles whose owner has been destroyed;
- concurrent compilation cannot cross-wire objects, graph ids, diagnostics, or
  cache entries;
- deleting any semantic fact producer makes a focused control fail.

## Performance acceptance

Each vertical slice must report separately:

1. semantic/algorithmic work removed;
2. same-algorithm machine code quality;
3. compiler/analysis work and memory;
4. startup/artifact/import/relocation cost;
5. applicability over ordinary corpus and constructed controls;
6. exact refusals and no-gain cases.

A research speedup on constructed inputs is preserved but does not close this gap
without current-head applicability and machine evidence. A semantic cleanup with
no performance mechanism may land only with no measured regression on the
strongest executable baseline.

## Closure acceptance

Close only when:

- every production application/projection/place/result/effect/transformation
  reaches machine/evidence through exact graph ids with no AST/name/path/string
  semantic reconstruction;
- DNIR is demonstrably physical-only and can be regenerated from graph facts;
- all backend/tool consumers agree by construction rather than duplicated tests;
- retained FTCFTW mechanisms are remeasured on the exact clean closed-graph head;
- compiler B/SHC and Wasm end-to-end status are reported independently and
  honestly;
- ablation of any required graph fact turns a focused gate red.

## C0 alignment

- **constitutional owners:** id · fact · relation · application · value · place · demand · observer · transformation · realization · machine · evidence
- **kind delta:** 0
- **registry delta:** negative; duplicate registries are deleted
- **source syntax delta:** 0 after delimiter closure
- **semantic invariant:** one id/fact universe survives every phase; physical encodings never decide meaning
- **physical mechanism:** compact graph-indexed work items and target realization records
- **foreign implication:** foreign laws ingest into the same graph with exact provenance/witnesses
- **world implication:** world/effect/authority facts are graph inputs, not ambient host globals
- **demand implication:** unobserved work is deleted before representation
- **evidence:** exact-head differential, ablation, corpus applicability, instruction/cycle/compile/memory/artifact matrices
- **deletion witness:** `OccurrenceBridge`, `.call` place folds, name-keyed place authority, result/type/effect string classifiers, semantic DNIR tags, retrospective application accounting
