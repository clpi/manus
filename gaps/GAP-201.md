| field | value |
|---|---|
| title | GAP-201 — semantic graph authority completion / physical-only DNIR |
| status | OPEN |
| filed | !2026-08-19T18:00:00Z |
| priority | P0 |
| kind | architecture_and_performance_gap |
| filed by | canonical-convergence-20260819 |
| files | src/semantic_graph.zig, src/dnir_lower.zig, src/graph_query.zig, src/comptime.zig, src/realization_validate.zig, src/native_ir.zig, src/native_backend.zig, src/wasm_backend.zig, src/codegen.zig, src/table_apply.zig, src/place.zig, src/escape.zig, src/table_facts.zig, src/const_table.zig, src/home_resolve.zig, src/native_bootstrap.zig, src/ast.zig, docs/spec/law.md |

| # | directive |
|---|---|
| 1 | The active compiler still reconstructs some semantic decisions from AST pointers, names, paths, syntax variants, local type/result/effect classifiers, string-matched builtins/worlds, and DNIR op/field tags after resolution. |
| 2 | Each duplicate classifier is both a soundness seam and a barrier to whole-graph specialization. |

| # | directive |
|---|---|
| 1 | All divergent research histories found in the 2026-08-19 census are direct ancestors of `main`; `research/branches/manifest.json` gives every retained head an explicit current disposition. |
| 2 | A branch is research provenance, never an alternate current tree. |

| section |
|---|---|
| Landed vertical slices |

| section |
|---|---|
| Compile-time projection occurrence carrying — LANDED, MEASURED |

| # | directive |
|---|---|
| 1 | `effectFreeClosure` retains the exact application occurrence ids already proved by the effect/authority query instead of returning only callee ids; |
| 2 | the native whole-body evaluator receives those sites as its application work and carries the selected occurrence on the evaluator work item; |
| 3 | computed aggregate projection asks `aggregateAccess(occurrence)` directly; the graph no longer scans `Node.ast_ref` to recover an application from the expression, and `aggregateAccessForExpression` is deleted; |
| 4 | the remaining expression pointer is provenance used by the AST-driven evaluator to select one already-carried work item. Its deletion condition is the first evaluator schedule whose work items originate from graph value and application ids rather than `ast.FuncBody`. |

| # | directive |
|---|---|
| 1 | This changes no source semantics and selects no new realization. |
| 2 | The change subject passed the locked unit suite (including a positive damage control that poisons the source aggregate initializer after graph publication), the ordinary build, `gate/semantic-graph-authority.sh`, `gate/architecture-negative.sh`, and mechanical admission against its exact base. |
| 3 | The repository aggregate remained red on the separately reported archive-size, ReleaseFast-artifact and sibling runner prerequisites; that is not green evidence for those surfaces. |

| section |
|---|---|
| Immutable flat i64 projection continuity — TOPIC CANDIDATE, MEASURED |

| # | directive |
|---|---|
| 1 | Against exact base `c9480b77189c8ce308403bd377b6c509046796e4`, the `flat-scalar-application` topic carries one bounded projection route through graph, DNIR, Native and Wasm: |

| # | directive |
|---|---|
| 1 | one-level immutable positional i64 `subject[key]` publishes an ordinary application occurrence, subject, one key operand, one result and exact result content when the key is exact; |
| 2 | mutable, aliased/escaped, f64 and text aggregates remain outside this producer domain, and mixed-consumer controls in both source orders prove the binding is not erased merely because one projection exists; |
| 3 | exact projection lowers to a graph-linked immediate with no dense object; dynamic projection lowers to one bounds-checked graph-linked dense load; |
| 4 | the native fold reads aggregate/member contents from the graph, not the source initializer, while a relation owning a projection retains an explicit DNIR realization until folded-application witnesses exist; |
| 5 | one shared graph-to-DNIR schedule validator is used by Native and Wasm. It rejects graphless lineage, invalid/unknown/duplicate/unused/damaged dense rows, wrong exact contents and damaged application/result lineage before bytes are emitted. The former Native-only aggregate validator was deleted; |
| 6 | Wasm reserves only the bytes actually selected for dense tables rather than a fixed 1 MiB hole. Zero dense rows preserve the previous memory base. |

| # | directive |
|---|---|
| 1 | Executable evidence with ReleaseFast compiler SHA-256 `17f3e6abb7592ef5b8e923c30bfebd9f4dbffe98085d00859f7babd8a4ec8e6b`: |

| # | directive |
|---|---|
| 1 | `examples/native/flat_scalar_projection.id` answers 40 under direct/native AArch64 and wasm32-wasi/Wasmtime 47.0.3; |
| 2 | `_main` retains the exact first projection as immediate 20 and calls the dynamic `_pick(2)` path; `_pick` performs one checked static-data load, so the expected answer is not a whole-body-fold-only control; |
| 3 | an exact projection retains the two-instruction `mov x0,#20; ret` floor, which the exact-base compiler already achieved; this is continuity/authority, not a new native speed win; |
| 4 | full `examples lib` reporting on the same source tree moves application publication from 4,993/14,933 (33.4%) to 5,036/14,976 (33.6%), with the blocking count unchanged at 4,655. Exact i64 moves 20,117 to 20,123 of 20,426; |
| 5 | application witness and realization remain unproduced in exported graph facts. DNIR/machine lineage is exact for this slice, but GAP-202 closure is not claimed; |
| 6 | the coverage reporter emitted its pre-existing invalid-UTF-8 AWK diagnostics for two negative fixtures and is reporting-only without a budget. |

| # | directive |
|---|---|
| 1 | After the final flat-bounds control, Debug, ReleaseSafe and ReleaseFast each passed 1,705/1,705 unit tests. |
| 2 | An earlier ReleaseFast run before that control observed the known environment-sensitive one-test movement (1,703/1,704) and immediately repeated at 1,704/1,704; the final current-source ReleaseFast run passed without a retry. |
| 3 | The broader `zig build test --summary all` is not green: current scripts refuse the pre-existing `runtime-global-call:tostring` direct path and the lock/agent-smoke aggregate has independent failures, so no broad suite admission is claimed. |
| 4 | Compiler wall/RSS, startup and broad native/Wasm artifact cost remain `UNMEASURED` beyond the narrow instruction/byte evidence above. |

| section |
|---|---|
| Call and computed-projection category split — LANDED, UNMEASURED |

| # | directive |
|---|---|
| 1 | `6ded6d2c380ad5b3b8a74d5bcacc2fc2129dfbb8` completed the active `src/table_apply.zig` replacement; |
| 2 | general typed-array/table `call -> index` rewriting was deleted; |
| 3 | `calleeIsArray`, `positionalResultDepth`, `TableNames`, `array_types`, and process-global `active_names` were deleted; |
| 4 | ordinary `t(1)` remains application and `t[1]` remains indexed projection; |
| 5 | user-bound `env` wins over bounded ambient-world compatibility; |
| 6 | bounded OS-world ingress carries an explicit bound-name census; |
| 7 | `sema.TypeMap` remains only as an inert public compatibility parameter until the large driver/test callers move together. |

| # | directive |
|---|---|
| 1 | This closes the active general call-shaped indexing rewrite. |
| 2 | It does **not** yet replace ambient source spelling with exact graph world-member identities. |

| section |
|---|---|
| Aggregate-only place production — LANDED, UNMEASURED |

| # | directive |
|---|---|
| 1 | `f5857e0ac20011e0b2eedefb815f4d4376304f21` activated aggregate-only place production while retaining current `place.Site` compatibility; |
| 2 | scalar values, parameters and homes are no longer produced as places; |
| 3 | `[]` is the only computed aggregate-place access face; |
| 4 | `()` never records an indexed place read/write; |
| 5 | multiplicity, constant/runtime index, alias, escape, lifetime, extent, determinacy, immutability and residency refusal remain; |
| 6 | more than a thousand lines of rejected place ontology/census work disappeared. |

| # | directive |
|---|---|
| 1 | Exact access/subplace ids are still not graph-owned; name/AST provenance remains an explicit bridge. |

| section |
|---|---|
| Aggregate escape authority — LANDED, UNMEASURED |

| # | directive |
|---|---|
| 1 | `c51f724a2526809b4b09907f5cae79cfc389476c` removed the zero-production-consumer Symbol escape/ARC authority (`canStackAllocate`, `shouldPruneArc`, `typeNeedsArc`) and its incomplete `address_taken` model. |
| 2 | One conservative aggregate escape analysis remains: |

```text
t[k]       projection; no whole-aggregate escape by itself
t(k)       ordinary application; aggregate is consumed
t:len()    extent observation; does not require aggregate escape
```

| # | directive |
|---|---|
| 1 | Focused parser controls prove bracket projection, ordinary application, argument escape and whole-binding rebind. |
| 2 | This deletes a duplicate weaker authority without changing a production consumer, because the removed API had none. |

| section |
|---|---|
| Positional table facts — LANDED, UNMEASURED |

| # | directive |
|---|---|
| 1 | `638e2251d28956c6ae040a36997a573e8d2ce514` collapsed the research-era table facts implementation to its live conservative fact/decision interface: |

| # | directive |
|---|---|
| 1 | no `sema.TypeMap`; |
| 2 | no `table_apply` dependency; |
| 3 | all source fixtures use canonical `[]`; |
| 4 | `t(k)` has a negative control and is aggregate escape, never an index; |
| 5 | constant/runtime index, contents, extent, mutation, escape, out-of-range trap, no-size-bound elimination, uniform materialization and fail-closed unmodelled behavior remain. |

| # | directive |
|---|---|
| 1 | The physical threshold remains one exported value consumed by lowering; width chooses a physical family only after the existence/elimination question. |

| section |
|---|---|
| Constant aggregate data — LANDED, UNMEASURED |

| # | directive |
|---|---|
| 1 | `5f29db84f5559450124b928d4773645bc361c0cf` retained the live native read-only-data optimization while removing its obsolete source-test normalizer: |

| # | directive |
|---|---|
| 1 | source licensing consumes `table_facts` directly under canonical `[]`; |
| 2 | no sema/type-map/table-apply dependency remains in this optimization; |
| 3 | DNIR independently verifies exact literal initialization, one store per slot, initialization-before-read, no redefinition, read-only subsequent mentions and no base escape; |
| 4 | empty licence, mutation, duplicate initialization and escape controls remain; |
| 5 | runtime index is allowed for read-only data but correctly blocks full absence. |

| # | directive |
|---|---|
| 1 | The source-name/content-to-DNIR-base join is intentionally conservative until exact aggregate/place graph ids replace it. |
| 2 | Content collisions can only refuse an optimization, never license a dirty table. |

| section |
|---|---|
| Source-view length mechanism — PRESENT, UNMEASURED |

| # | directive |
|---|---|
| 1 | `src/source_cursor.zig` carries a byte slice and reads `self.bytes.len` in O(1). |
| 2 | The historical repeated-`strlen` quadratic defect is not active in this host cursor. |
| 3 | Generated-output agreement, authoritative lexer transfer and large-input scaling still require exact-head execution. |

| section |
|---|---|
| Research disposition — LANDED |

| # | directive |
|---|---|
| 1 | `research/branches/manifest.json` v2 assigns every retained divergent head one of: |

| # | directive |
|---|---|
| 1 | absorbed; |
| 2 | partial gap with exact current owner; |
| 3 | control/evidence only; |
| 4 | historical rejected surface. |

| # | directive |
|---|---|
| 1 | It separately records the current disposition of quotienting, recursive demand, guarded order statistics, observer virtualization, cache/artifact soundness, lexer length and machine-code losses. |
| 2 | A stale branch ref therefore carries no semantic admission power. |

| section |
|---|---|
| Regression enforcement — LANDED, UNEXECUTED ON CURRENT HEAD |

| # | directive |
|---|---|
| 1 | `gate/semantic-graph-authority.sh` protects: |

| # | directive |
|---|---|
| 1 | application/index category separation; |
| 2 | aggregate-only place production; |
| 3 | deleted Symbol escape authority; |
| 4 | aggregate bracket/call escape controls; |
| 5 | O(1) source length; |
| 6 | the remaining exact bridge classes as visible OPEN findings. |

| # | directive |
|---|---|
| 1 | CI invokes the authority and semantic-graph gates before building. |
| 2 | No current workflow execution has been observed through the available connector, so this is source-level enforcement awaiting an actual runner, not green evidence. |

| section |
|---|---|
| Current exact debt |

| section |
|---|---|
| Occurrence and application identity |

| # | directive |
|---|---|
| 1 | `OccurrenceBridge` joins applications to expressions by `*const Expr`. |
| 2 | `applicationFace` in DNIR lowering and `occurrenceFace` in native realization still reconstruct diagnostic relation faces from `Node.ast_ref`; the DNIR version currently consults AST provenance before the already-available graph application relation. |
| 3 | AST-driven realization walkers still recover application ids from provenance rather than receiving exact graph occurrence ids in work items. The bounded compile-time projection consumer is no longer one of them: its effect query carries exact occurrences into evaluator work, but the evaluator still uses expression provenance to select among those carried items. |
| 4 | graphless convenience lowering strips orphan handles after the fact instead of making graph ownership/id lifetime explicit at its API boundary. |
| 5 | `native_bootstrap.zig` still recognizes bootstrap application meaning from source names/roots/method strings and is consulted by graph/lowering gates. |

| section |
|---|---|
| Projection, world and place identity |

| # | directive |
|---|---|
| 1 | general call-to-index rewriting is gone, but bounded ambient OS compatibility still recognizes world members from source spelling before exact graph world-member identities exist; |
| 2 | aggregate place kind and source semantics are corrected, but source place facts are still keyed by names/AST bindings and constant-data DNIR recognition by base temp/content; |
| 3 | exact root-place, projection/subplace, read/write occurrence and provenance ids are not published once and carried through all consumers; |
| 4 | parser, sema, graph, direct, C, Wasm and tooling candidate sets are not yet one shared graph query. |

| section |
|---|---|
| Name/path/string reconstruction |

| # | directive |
|---|---|
| 1 | name-keyed place lookup and AST initializer provenance; |
| 2 | path/home/symbol reconstruction after exact entity resolution; |
| 3 | string-matched worlds, ambient roots, builtin families, conversions, egress, memory/string/math/OS operations; |
| 4 | callee spelling used to infer ABI/result class where graph descriptors exist or should exist. |

| section |
|---|---|
| Descriptor, result, demand and effect duplication |

| # | directive |
|---|---|
| 1 | local predicates such as string/integer/bool/f64/result/record classifiers; |
| 2 | separate parameter/result ABI maps reconstructed by export name; |
| 3 | syntax-shaped effect and void/result-demand classification; |
| 4 | backend eligibility decided by local enums rather than graph-owned laws/target facts; |
| 5 | `ast.FuncBody` still owns a large optimizer/codegen payload (`use_*`, dense table, algorithm, device/profile/autodiff/unroll state) that must migrate as graph transformation/realization facts rather than be deleted piecemeal. |

| section |
|---|---|
| Transformation lineage |

| # | directive |
|---|---|
| 1 | lowering performs folds, tail rewrites, vector reductions, bounds/trap insertion, representation decisions and other semantic transformations without graph transformation ids/witnesses; |
| 2 | occurrence/value/result lineage is repaired retrospectively instead of derived from exact replacement edges; |
| 3 | retained quotient, recurrence, guarded-termination and observer research cannot be fully reactivated until this lineage exists. |

| section |
|---|---|
| DNIR semantic leakage |

| # | directive |
|---|---|
| 1 | DNIR op/tag/field conventions carry vector, trap, bounds, builtin, world or transformation meaning instead of only target realization encoding; |
| 2 | physical operands and semantic facts are not cleanly separated at every API; |
| 3 | direct, C and Wasm backends still contain local semantic candidate logic. |

| section |
|---|---|
| Consumer and authority duplication |

| # | directive |
|---|---|
| 1 | grammar/formatter/Tree-sitter/LSP/MCP projections retain local spelling/role knowledge in places; |
| 2 | consumer-zero or weakly consumed research modules must continue to be deleted or explicitly quarantined when found; |
| 3 | direct, C, Wasm and tool paths do not consume exactly one closed graph representation for every admitted construct; |
| 4 | native/self-host projections must pin the exact current upstream subject and execute agreement gates after authority changes. |

| section |
|---|---|
| Required vertical order |

| # | directive |
|---|---|
| 1 | **Occurrence work items:** graph publishes exact application/value ids into resolver/lowering work items; delete AST-pointer joins and orphan-handle stripping. |
| 2 | **World/application closure:** graph publishes exact ambient/foreign/bootstrap relation ids and witnesses; make diagnostics graph-first, then delete name/root/method recognition and the inert table-ingress type-map API. |
| 3 | **Exact access/place ids:** graph-owned projection/access occurrence ids, root-place/subplace facts, determinacy, alias, escape, lifetime, extent and residency; replace source-name/content/base-temp joins while preserving the landed conservative controls. |
| 4 | **Descriptor/result/effect facts:** one producer supplies operand/result packs, descriptors, effects, world demands and ABI facts to every backend. |
| 5 | **Transformation lineage:** every fold, quotient, contraction, early exit, vectorization, fusion or layout change is a graph transformation with witness and occurrence/value/machine lineage. |
| 6 | **Physical-only DNIR:** DNIR carries graph ids plus target realization choices and physical operands; semantic local taxonomies monotonically reach zero. |
| 7 | **One backend/tool contract:** direct, C, Wasm, JIT/interpreter, LSP, MCP and evidence consume the same graph semantics and differ only in physical realization/projection. |
| 8 | **Performance exploitation:** rerun retained algorithmic and machine research on the closed graph; integrate only Pareto wins or physical optima and expose every same-algorithm loss. |

| section |
|---|---|
| Permanent negative controls |

| # | directive |
|---|---|
| 1 | two source faces with one witnessed meaning share semantic identity while retaining distinct provenance; |
| 2 | application and computed projection never share occurrence identity merely because compatibility can rewrite a historical spelling; |
| 3 | local/parameter bindings shadow ambient worlds without process-global state or downstream string-name exceptions; |
| 4 | scalars, parameters and homes never become places because an implementation stores or names them; |
| 5 | a dead semantic helper with no production consumer is deleted or moved to research rather than kept as apparent compiler capability; |
| 6 | unknown graph facts refuse or conservatively fall back, never infer from opcodes, paths, spelling, declaration/load order or host type; |
| 7 | direct and C backends produce behavior-equivalent observations for the admitted subset, including traps, effects and ordering; |
| 8 | graphless APIs cannot return handles whose owner has been destroyed; |
| 9 | concurrent compilation cannot cross-wire objects, graph ids, diagnostics or cache entries; |
| 10 | deleting any required semantic fact producer makes a focused gate fail; |
| 11 | every gate examining zero subjects fails rather than reporting green. |

| section |
|---|---|
| Performance acceptance |

| # | directive |
|---|---|
| 1 | Each vertical slice reports separately: |

| # | directive |
|---|---|
| 1 | semantic/algorithmic work removed; |
| 2 | same-algorithm machine-code quality; |
| 3 | compiler/analysis work and memory; |
| 4 | startup, artifact, import and relocation cost; |
| 5 | applicability over ordinary corpus and constructed controls; |
| 6 | exact refusals and no-gain cases. |

| # | directive |
|---|---|
| 1 | The landed source deletions reduce classifier/census work by construction, but no runtime, compile-time or memory number is claimed without an exact clean-head run. |
| 2 | Historical research speedups remain attached to their historical subjects until rerun. |
| 3 | A semantic cleanup with no activated performance mechanism must not silently reset or hide the strongest executable baseline. |

| section |
|---|---|
| Closure acceptance |

| # | directive |
|---|---|
| 1 | Close only when: |

| # | directive |
|---|---|
| 1 | every production application, projection, place, result, effect and transformation reaches machine/evidence through exact graph ids with no AST/name/path/string semantic reconstruction; |
| 2 | DNIR is demonstrably physical-only and regenerable from graph facts; |
| 3 | all backend/tool consumers agree by construction rather than duplicated tests; |
| 4 | retained FTCFTW mechanisms are remeasured on the exact clean closed-graph head; |
| 5 | compiler B/SHC and Wasm end-to-end status are reported independently and honestly; |
| 6 | ablation of any required graph fact turns a focused gate red. |

<!-- idol-gap-c0:v1:begin -->
| field | value |
|---|---|
| constitutional owners | id · fact · relation · application · value · place · demand · observer · transformation · realization · machine · evidence |
| kind delta | negative; rejected place kinds, Symbol escape authority and call-shaped indexing were removed |
| registry delta | negative; table call/type/name registries and dead escape classifiers were deleted |
| source syntax delta | 0 after delimiter closure |
| semantic invariant | one id/fact universe survives every phase; physical encodings never decide meaning |
| physical mechanism | compact graph-indexed work items and target realization records |
| foreign implication | foreign laws ingest into the same graph with exact provenance/witnesses |
| world implication | world/effect/authority facts are graph inputs, not ambient host globals |
| demand implication | unobserved work is deleted before representation |
| evidence | exact-head differential, ablation, corpus applicability, instruction/cycle/compile/memory/artifact matrices |
| landed deletion witnesses | general call-to-index conversion, `active_names`, `calleeIsArray`, `positionalResultDepth`, scalar/parameter/home place production, dead Symbol escape/ARC helpers, table-facts sema/table-apply harness, const-table sema/table-apply harness |
| remaining deletion witnesses | inert production `table_apply` type-map parameter, `OccurrenceBridge`, AST relation-face reconstruction, bootstrap name classifier, name-keyed/content-joined place authority, local result/type/effect classifiers, AST optimizer payloads, semantic DNIR tags, retrospective application accounting |
<!-- idol-gap-c0:v1:end -->
