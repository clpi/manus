| field | value |
|---|---|
| title | GAP-179 — observer-quotient compilation |
| status | OPEN |
| filed | !2026-08-18T15:10:00Z |
| priority | P1 |
| kind | research_gap |
| filed by | devin-semantic-20260818 |
| files | docs/spec/constitution.md, docs/spec/law.md, src/demand.id, src/obseq.zig |

| # | directive |
|---|---|
| 1 | Promote demand from "is this value used?" to "which distinctions can the admitted observer detect?" Compile only enough computation to preserve those equivalence classes. |
| 2 | This generalizes DCE, partial-result demand, low-bit quotienting, approximate computing, privacy, aggregate-only queries, header-only decoding, and theorem-answer elimination. |

| section |
|---|---|
| Current measured boundary |

| # | directive |
|---|---|
| 1 | The periodicity/quotient work already collapses exact state spaces by demanded bit/field observability. |
| 2 | It is not yet a universal compiler phase that decides legality by observer equivalence classes. |

| # | directive |
|---|---|
| 1 | **Crossed:** |

| # | directive |
|---|---|
| 1 | `demand` facts exist and drive elimination. |
| 2 | Local quotienting demonstrates the power of observation-constrained search. |

| # | directive |
|---|---|
| 1 | **Still missing:** |

| # | directive |
|---|---|
| 1 | A canonical `observer` and `observer quotient` model. |
| 2 | `legal(candidate)` defined as `Q(original) = Q(candidate)` for the demanded quotient `Q`. |
| 3 | Standard observer quotients: sign, low bits, length, parity, membership, aggregate, existence, range check, order relation. |
| 4 | Cross-law application: approximate computing, privacy redaction, aggregate queries, theorem-answer. |
| 5 | Realization search that stops as soon as the quotient is preserved. |

| section |
|---|---|
| Required order |

| # | directive |
|---|---|
| 1 | Define `observer` as a semantic entity with admitted distinguishability. |
| 2 | Add `quotient` facts to every demanded value: `Q(value)`. |
| 3 | Replace DCE and partial-demand with `Q = none` / `Q = exact` extremes. |
| 4 | Implement `Q(original) = Q(candidate)` as the legality test. |
| 5 | Demonstrate theorem-answer elimination and aggregate-only query compaction. |

| section |
|---|---|
| Closure acceptance |

| # | directive |
|---|---|
| 1 | Close when every elimination of unused computation is justified by an explicit observer quotient, and when the compiler can stop materializing a value as soon as the demanded equivalence classes are preserved. |

<!-- idol-gap-c0:v1:begin -->
<!-- c0-schema: Instance of the schema in `gaps/RESEARCH-SPINE.md`; `tools/node/dev/gapc0` fails closed without it. -->
| field | value |
|---|---|
| constitutional owners | demand · witness |
| kind delta | 0 |
| registry delta | 0 |
| source syntax delta | 0 |
| semantic invariant | every elimination is justified by an explicit observer quotient; DCE (`Q = none`) and exact use (`Q = exact`) are extremes of one fact family, not separate phases. |
| physical mechanism | quotient-preservation legality check as the universal elimination test. |
| prerequisites | GAP-176 (this is its compilation-phase face). |
| foreign implication | foreign boundaries preserve only the quotient the Idol-side observer demands. |
| world implication | the admitted observer set is world-qualified. |
| projection implication | partial parsing, header-only decoding, and aggregate-only access are quotient facts, not mechanisms. |
| demand implication | `Q` ranges over sign, low bits, length, parity, membership, aggregate, existence, range check, order relation. |
| realization implication | realization search stops as soon as the quotient is preserved; theorem-answer elimination and aggregate-only compaction delete whole computation families. |
| evidence | theorem-answer elimination demonstrated; the DCE row re-expressed as `Q = none`. |
| deletion witness | dead-code flags, partial-result heuristics, approximate-computing switches. |
<!-- idol-gap-c0:v1:end -->
