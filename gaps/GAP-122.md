# GAP-122 — Semantic architecture gate census executes pathspecs and reports false zeros

**Status:** CLOSED · **Filed:** !2026-08-09T21:08:33Z · **Closed:** !2026-08-10T06:02:00Z
**Priority:** P0 · **Kind:** agent
**Filed by:** codex_shc_priority
**Files:** scripts/semanticgate.duo

Current claimed refactor replaces git-ls-files/printf commands with bare pathspec strings while downstream capture still executes the variables. Gate logs Permission denied/command not found and reports false zero debt counts. Its process ratchet also counts its own positive-control string. Commit chokepoint is both blocked and evidentially unsound until producer/consumer contract and self-exclusion are fixed.

The staged-index producer/consumer contract is now explicit. Every census command
invokes `git grep --cached`; path variables contain only pathspec operands. A
source positive control proves the tracked inputs exist, the parser and
post-parser pathspecs return known evidence, and the Duon exclusion removes the
gate's own unique fixture with exit status exactly one. Command or pathspec
errors therefore cannot masquerade as an empty census. The process row excludes
its indented positive-control assignment and measures 34 real bootstrap calls.

Evidence:

- `duo run scripts/semanticgate.duo` reports all staged counts and passes.
- corrupting the post-parser pathspec makes the new source control fail before
  repository counts are emitted.
- the runtime boxing row reports 1271 tracked bootstrap sites while the direct
  typed `read` proof rejects `lua_Value`, `lua_invoke`, and `lua_require` in its
  complete generated projection.
