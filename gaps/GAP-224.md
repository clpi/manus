# GAP-224 — the declaration freeze's advertised door is a spelling C0 forbids

**Status:** OPEN
**Priority:** P1 · **Kind:** evidence_gap
**Filed by:** claude-coordinator
**Files:** gate/vocab-extract.awk,gate/vocabulary.sh,gate/idiom.id

## The defect

### Current boundary — parameterised relations now have a graph door

| # | directive |
|---|---|
| 1 | `gate/vocabulary.sh` now admits an added parameterised relation only when the production `idol graph` export proves one root-scoped entity with the same source provenance, an exact Idol callable-linkage row, an exact body row keyed by that id, and the current one-card source-law edition. |
| 2 | Its normal path damages body, linkage, exposure, root scope, schema version, and source-law cardinality; `--selftest` also proves a real relation passes while a missing relation, a descriptor, a missing compiler, and a diff path escaping the repository refuse. |
| 3 | No word registry was added. |

| # | directive |
|---|---|
| 1 | The original finding is narrowed, not erased: the AWK candidate extractor still drops leading-underscore names, and cases/descriptors/bindings still have no graph reach/cardinality projection. |
| 2 | Parameterised relations are no longer universally refused; every other declaration kind remains fail-closed. |

## Superseded freeze measurement

| # | directive |
|---|---|
| 1 | Before the graph witness, `gate/vocabulary.sh` refused every newly declared non-private module-scope identity and said "no word-list edit can waive this". |
| 2 | Its only extractor escape was a leading underscore (`gate/vocab-extract.awk:12`, "PUBLIC is `name does not begin with _`"), and `gate/idiom.id:931` refuses that spelling by name. |

| # | directive |
|---|---|
| 1 | Measured — the extractor and the parser are both the subject, so run both: |

| # | directive |
|---|---|
| 1 | printf '+brk(text: str, from: i64)\n' \| awk -v MODE=diff -f gate/vocab-extract.awk -> relation brk REFUSED by the freeze printf '+brk = (text: str, from: i64)\n' \| awk -v MODE=diff -f gate/vocab-extract.awk -> relation brk REFUSED by the freeze |

| # | directive |
|---|---|
| 1 | **Every parameterised declaration form the language has is still classified.** Before the graph witness, every such row was refused. |
| 2 | Now relation rows proceed only through the exact graph proof; the classifier does not admit them itself. |

## Earlier lawful door: the zero-parameter relation

| # | directive |
|---|---|
| 1 | printf '+check(): i64\n' \| awk -v MODE=diff -f gate/vocab-extract.awk -> (nothing) |

| # | directive |
|---|---|
| 1 | `check(): i64` compiles, runs, composes, carries no underscore, and is not classified — the extractor's two parenthesised patterns both require a `:` INSIDE the parens, which an empty parameter list cannot have. |
| 2 | It is a genuine canonical form on its own merits, not a spelling chosen to slip past a regex. |

| # | directive |
|---|---|
| 1 | Its price is real: every parameter becomes duplication at each call site. `lib/compiler/token.id` was already paying it before this gap was filed ("a shared `pick(row, kind)` is a new module-scope relation and cannot be committed; a nested binding can"). |

## Two corrections to this gap, both mine

| # | directive |
|---|---|
| 1 | **First filing** claimed the underscore was the only door and the freeze was deadlocked. |
| 2 | False — the zero-parameter relation is a lawful door. |

| # | directive |
|---|---|
| 1 | **Second filing** claimed the freeze fails open on the canonical definition form `name(...) = (...)`. |
| 2 | Also false, and worse because it was asserted from an extractor result without checking the parser: |

| # | directive |
|---|---|
| 1 | pick(row, kind) = (row) error: expected expression, got '=' rc=1 add(a: i64, b: i64) = a + b error: expected expression, got '=' rc=1 |

| # | directive |
|---|---|
| 1 | **That string is not a declaration form in Idol at all.** The extractor misses it because it is not a declaration, not because it is the canonical one. |
| 2 | There is no fail-open on any form the language accepts. |

| # | directive |
|---|---|
| 1 | The remaining true finding is narrow and unchanged: `vocab-extract.awk` advertises a private marker that C0 forbids (`underscore = false`, `constitution.md:235` |
| 2 | "native underscore identifiers are zero", :5565), where `law.md` is silent on identifier spelling and C0 therefore governs. `gate/idiom.id` projects C0 correctly; the advertised door is the unlawful half. |
| 3 | But it is not the only door, and nothing fails open. |

## Acceptance

1. `gate/vocab-extract.awk` stops advertising a C0-forbidden spelling as the
   definition of private. Controls keep the zero-parameter relation admissible,
   admit a parameterised relation only with the graph witness, and refuse a
   missing relation plus every unprojected declaration kind.
2. The `_`-prefixed declarations admitted under the old door are enumerated
   by a runner and given a migration disposition.
3. `gate/idiom.id` is unchanged, or changed only in a direction C0 licenses.
4. Whatever population figures this repair needs are produced by an executable
   enumerator. No count is written into this file: the commands above are the
   authority, and `sh gate/vocabulary.sh --base <rev>` is the verdict.

## Related

| # | directive |
|---|---|
| 1 | GAP-134 (the lane that hit the underscore half and escalated it rather than editing the gate blocking it), GAP-145, `law.md`, `constitution.md` LAW-ONE. |
