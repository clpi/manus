# GAP-034 — the native precheck ratchets behind its own lowering

**Filed** 2026-08-07. **Status** open (structural). **Cost so far** four
separate bugs, ~17 programs.

## The pattern

`can_emit_native_scalar_module` / `expr_is_native_scalar` (src/codegen.zig) is a
*conservative approximation* of what `src/dnir_lower.zig` can lower. Nothing
keeps the two in sync. Every time the lowering grows a capability, the gate in
front of it keeps refusing programs that would now succeed — silently, because
the module falls back to the C backend and still compiles.

Four instances, all found and fixed 2026-08-07 by instrumenting the bail sites:

| construct | lowering had it | gate said |
|---|---|---|
| `string.byte(s, i)` | indexed load, added for tokenizer loops | `runtime-global:string` |
| `s:byte(1)` (FACE-CALL) | — (added with the fix) | `expr-unhandled:method_call` |
| `Kind = @{ eof = 0 }` | `collectModuleConsts`, doc names this exact form | `assign-value:compile` |
| `blob = { 0x00, 0x61 }` | memory-backed positional tables since SH-04 | `keyed-table-export` |

The third and fourth are the sharpest: in both, the lowering's own doc comment
names the construct it supports, and the gate rejected it anyway.

Worse, two of these were the **canonical** spelling. `s:byte(1)` (rule 6,
FACE-CALL) and `@{ … }` (the enum descriptor) both disqualified their module
while the deny-listed vestige either worked or failed identically — writing
canonical Duo bought no native coverage over the form the canon forbids.

## Why this is a gap and not four fixes

The fifth instance is already latent. The gate is hand-written prose about a
subset that is defined somewhere else, so it drifts by default and only
un-drifts when someone measures. The fix is to derive both from one description
of the subset — the lowering states what it accepts, the gate consumes that
statement — rather than re-deriving it by hand each time.

Interim, and cheap: when a program bails, **grep `dnir_lower.zig` for the
construct before writing any lowering code.** It is probably already there.

## Ranked worklist as of this filing (85/140 native, 61%)

Measured with `duo compile --backend=direct` over `examples/` minus
`compile_fail/`; see the `bail site:` diagnostic added the same day.

    14  mod-top-stmt:global_decl   UNREACHABLE — see below
     5  runtime-global:mem         real work: 17 intrinsics, zero DNIR support
     4  lowerCall dnir_lower:1826  real work: cross-module record return (SH-04)
     3  runtime-global:string      string.char — allocates
     2  runtime-global:os          zero DNIR support
     2  keyed-table-export         genuine keyed exports
     2  assign-value:nil           genuine
     2  codegen.zig:2946/2949      `any` param/return — correctly rejected, `any` IS boxed
     1  runtime-global:math        zero DNIR support
     …  singletons in dnir_lower

No drift remains in this list. Every row above needs new backend capability.

## The rows merge into three subsystems, not sixteen fixes

Re-measured over the REACHABLE set only (`zig build native-census`), and two
rows that look unrelated are the same missing capability:

  NATIVE STRING ALLOCATION -- 5 programs. RE-MEASURED 2026-08-08, and the
  blocker is NOT allocation. `mem.alloc` -> malloc already lowers (three mem
  primitives landed as plain libc calls). `string.char(n)` composes to
  malloc(2) + two byte stores + return, and the DNIR opcodes for all of it
  exist. What does not exist is the ARM64 ENCODING: `store_index` at byte width
  refuses at native_backend.zig:1751 -- only the `.i64` scaled 8-byte store is
  emitted. The READ side has been there all along (`string.byte` -> byte load),
  so this is a one-sided pair. `strb w, [base, idx]` is the missing
  instruction, and it unblocks string.char; `..` (concat) needs it too plus a
  length-driven copy loop.

  THE ENCODING, and the hazard. `emitLdrb` (native_backend.zig:3287) uses
  0x39400000 -- LDRB, immediate, unsigned offset. The counterpart is STRB with
  the same addressing form, 0x39000000, and `emitStrb` mirrors it field for
  field. That part is mechanical.

  What is NOT mechanical is the OFFSET CONVENTION, and it is why this was not
  landed blind. The existing byte READ is string.byte, which computes
  `base + (i - 1)` because Duo indexes strings from 1 and C pointers from 0.
  A raw buffer store (string.char writing the character at 0 and the NUL at 1)
  wants no such adjustment. So `store_index` at byte width serves two callers
  with different origins, and picking one silently corrupts the other -- a
  write to a wrong address, which the 68-program differential would NOT catch,
  because nothing else in the corpus performs a byte store. The verification
  would rest entirely on one hand-written probe.

  ATTEMPTED 2026-08-08, AND THE PROBE CAUGHT IT. The full chain was built:
  emitStrb (0x39000000, mirroring emitLdrb field for field), a byte-width
  store_index arm sharing the load's single `base + (i - 1)` origin,
  string.char lowering with 1-BASED indices so the raw buffer normalizes at the
  lowering instead of giving the backend a second convention, and exprIsStr
  taught that string.char PRODUCES a str (without which the local never enters
  str_slots and the very next string.byte does not recognize its own argument
  -- a producer the type tracker does not know about makes every consumer
  downstream fail).

  It compiles. It SIGSEGVs: `s: str = string.char(65); string.byte(s, 1)`
  returns 139, not 65. So the address computation or the register lifetime
  around the malloc result is wrong, and the corpus differential stayed green
  through all of it -- 89 programs, none of which perform a byte store.

  That is the predicted failure, observed. The reason this row was held back
  was that "it compiles and the gates are green" is not evidence here, and the
  one hand-written probe was the only thing that could fail. It did.

  Next session: keep the chain above (it is the right shape) and debug the
  segfault -- first suspect is the malloc result temp being clobbered before
  the two stores, since call_extern goes through emitSaveCallerRegs and the
  stores evaluate three operands afterward.

  CONCAT ATTEMPTED 2026-08-08 AND REVERTED — one layer past string.char.
  `a .. b` lowers cleanly as strlen, strlen, add, add 1, malloc, strcpy,
  strcat, and every piece exists: `mov_arg` marshals DNIR values into x0..x7,
  which is how every multi-argument call in dnir_lower already works. Also
  needed, and the same lesson string.char taught: exprIsStr must know that a
  concat PRODUCES a str, or `string.len` on its own result falls through to an
  undefined `string_len`.

  It compiles and is WRONG, not crashing: `"AB" .. "CD"` gives length 2, so
  strcpy ran and strcat did not append.

  FIRST SUSPECT WAS WRONG, and the correction matters more than the guess.
  I blamed `emitSaveCallerRegs` clobbering the staged x0/x1. It does not --
  measured, it saves x9..x28 only (native_backend.zig:3211), so mov_arg
  staging survives it untouched.

  What IS true and unverified: `Instr.lhs` defaults to `.void`
  (duo_native_ir.zig:127), so a call_extern emitted with no `lhs` correctly
  skips the move-to-x0 and leaves the mov_arg staging alone. That means the
  marshalling path is probably fine and the fault is elsewhere -- most likely
  in which register `evalDnirValue` hands back while x0 already holds the first
  argument, since `mov_arg` writes x0/x1 directly without any record that they
  are now live.

  The measurable fact, independent of cause: multi-argument extern calls have
  NO existing caller. Every current call_extern passes one argument through
  `ins.lhs`. So the path is untested.

  AND THE OBVIOUS DEBUGGING STEP IS BLOCKED. "Dump the assembly" is the right
  move and does not work: `duo compile --backend=direct --emit asm x.duo -o
  out.s` writes a MACH-O EXECUTABLE. Magic bytes cf fa ed fe, mode rwxr-xr-x,
  and it reports `ok compile` -- the wrong format is silent. Same class as
  gap[015] (`--emit obj`/`dylib` emit executables) and gap[019] (`--emit wasm`
  emits Mach-O), so this is the third member of that family and the first one
  that blocks an investigation rather than a deliverable.

  The capability exists internally: native_backend has `emitAssembly` and an
  `asm_text` buffer that every emitter already appends to (that is where the
  "strb w{d}, [x{d}]" listings go). Only the CLI route fails to select it.
  ROOT CAUSE FOUND, FIX NOT LANDED. `--target native-asm` works and emits real
  assembly (`.section __TEXT,__text` … `adrp x9, Lduo_str_0@PAGE`), so the
  emitter is fine and only the CLI mapping is broken.

  `ResolvedTarget.toLegacyTargetName` (target_model.zig) opens with
  `if (self.legacy_name) |n| return n;`. The default `--target` is the BARE
  `native`, which carries no emit information, so it is returned and the
  `--emit` kind is discarded before the switch below it — the switch that
  already maps `.assembly => "native-asm"` correctly.

  The fix is to let an information-free `native` defer to the emit kind while
  an explicit legacy name still wins:

      if (self.legacy_name) |n| {
          if (!std.mem.eql(u8, n, "native")) return n;
          if (self.emit == .exe) return n;
      }

  THAT FIX IS NECESSARY BUT NOT SUFFICIENT — measured 2026-08-08 once the tree
  built again. Applied it; `--emit asm` still wrote a Mach-O executable. The
  next guard is two lines further down:

      if (!self.triple.directBackendSupported()) return null;

  With `legacy_name` no longer short-circuiting, control reaches that line,
  returns null, and `resolveCompileTarget` falls back to the ORIGINAL input
  (`native`). `resolveCompileBackend` then sees `--backend=direct` with a
  non-machine target and rewrites it to `native-exe` (main.zig:2994) — the
  executable. So there are TWO layers discarding the emit kind, and the second
  only becomes visible after the first is fixed.

  THIRD CORRECTION, and the one that matters: `toLegacyTargetName` IS NOT ON
  THE PATH AT ALL. Both fixes above were applied together and `--emit asm`
  still wrote a Mach-O executable, which means `resolveTargetInput("native",
  .assembly)` returns NULL — so resolveCompileTarget's whole `if` block is
  skipped, `target_in` ("native") is returned unchanged, and
  resolveCompileBackend rewrites it to `native-exe`. Every edit to
  toLegacyTargetName was dead code.

  ASSEMBLY DUMPED 2026-08-08 once --emit asm was fixed (179d322), and it
  CONTRADICTS the diagnosis above. The emitted sequence is correct:

      bl _malloc
      ldr x10,[sp,#0] … ldr x30,[sp,#24]; add sp,sp,#32   restore caller regs
      mov x9, x0        malloc result saved
      mov x0, x9        arg0 = buf
      mov x1, x10       arg1 = "AB"
      bl _strcpy
      mov x13, x0
      mov x0, x9        arg0 = buf again
      mov x1, x11       arg1 = "CD"
      bl _strcat

  Both calls receive the malloc result and the right operand. mov_arg staging
  survives emitSaveCallerRegs, as expected once that save was measured to cover
  x9..x28 only. So "multi-argument extern calls do not work" is NOT established
  — the earlier length-2 result was observed BEFORE exprIsStr learned that a
  concat produces a str, and may simply have been the consumer misreading the
  result rather than the producer building it wrong.

  RE-RUN 2026-08-08 WITH BOTH CHANGES TOGETHER: still 211, i.e. length 2 and a
  garbage byte 3. So the "consumer, not producer" theory is ALSO wrong — that
  is the fourth failed explanation for this row.

  What is now established by measurement rather than argument:
    - the emitted call sequence is correct (assembly, above)
    - landing exprIsStr's `.binop` case alongside lowerConcat changes nothing
    - the result is consistently length 2, i.e. strcpy's output survives and
      strcat's does not

  What has NOT been done and is the actual next step: dump the assembly for a
  program that uses BOTH `string.len` and `string.byte` on the concat result
  (the dump above was len-only), and single-step or disassemble the strcat
  call's operands at that point. Every explanation offered so far has been
  inference from a partial dump.

  NARROWED BY CONTROL, 2026-08-08 — and the narrowing is the useful part.
  `string.len("ABCDE")` returns 5 on BOTH backends. So str_len works on a
  literal-backed str, the consumer is fine, and the length-2 result means the
  concat BUFFER is genuinely wrong. Producer, not consumer — settled by a
  positive control rather than by reading instructions.

  (I first read the dumped str_len loop as broken — lines `b.eq .Lduo_0` and
  `b .Lduo_0` appearing to share a target — and the control above disproves it.
  That was the FIFTH wrong inference in this row, all of the same kind: a
  conclusion drawn from a partial listing. The dump is evidence about what was
  emitted; it is not evidence about what executes, and a two-line control
  settled in one command what five readings did not.)

  So: strcpy's output lands, strcat's does not, on a buffer malloc returned and
  both calls received correctly. The remaining suspects are the malloc SIZE
  (total = la + lb + 1, computed from two strlen results that themselves come
  back through the caller-save restore) and whether strcat's own return path
  disturbs the buffer register. Test the size first — it is one `mov` to
  inspect and would explain a truncated result exactly.

  NEXT STEP IS TO RE-RUN IT, not to reason further: reapply the lowerConcat arm
  plus the exprIsStr `.binop` case together, and check
  `a .. b` for "AB".."CD" gives length 4 and byte 3 == 'C' (67). The assembly
  says it should. Three of my diagnoses in this area were wrong before the
  dump existed; this one is at least backed by the instructions.

  Start at `resolveTargetInput` (target_model.zig:~200), not at
  toLegacyTargetName. The question is why a bare `native` with an explicit
  emit kind does not resolve, and `--target native-asm` resolving correctly
  (line 218 handles that name explicitly) is the reference to diff against.

  I was wrong about this function three times in a row — first blaming
  legacy_name, then the directBackendSupported guard, then both together. Each
  guess was consistent with the code I had read and none was tested before
  being written down. The lesson is in the record because the next reader
  should trust the MEASUREMENTS in this file, not its explanations.

  Both need to hold for `--emit asm`: let a bare `native` defer to the emit
  kind, AND make the assembly emit survive the directBackendSupported guard
  (assembly is a machine-target emit, so the guard is probably just wrong for
  `.assembly`). Verification is unchanged: `file out.s` must say "assembler
  source text", and `--target native-asm` already produces exactly that, so
  there is a working reference to diff against.

  NOT COMMITTED: the tree does not currently build (a parallel session has ~900
  lines in flight across sema.zig/ast.zig/codegen.zig), so this could not be
  verified, and an unverified fix is exactly what the rest of this file argues
  against. Apply and check `file out.s` says "assembler source text".

  Fixing `--emit asm` is therefore small, and it is a PREREQUISITE for the
  concat work rather than a separate errand -- without it the next attempt is
  back to reasoning about the register allocator, which is exactly what
  produced the wrong suspect above.

  Resolve the convention first (either a distinct opcode/flag for raw vs
  string-indexed stores, or normalize at the lowering so the backend only ever
  sees 0-based), then emit. This is the one place in the remaining worklist
  where "it compiles and the differential is green" is not evidence.

    `lowerBinop` bail = `..` (concat), not a missing bitwise op as the site
    suggests; DNIR's BinOpTag genuinely lacks band/bor/bxor/lshift/rshift, but
    NEITHER blocked program uses them. Both use `..`. Add `string.char` (3) and
    the lever is one thing: a native string allocator (malloc + copy) in DNIR.

  MEM POINTER TYPES -- 5 programs.
    Not 17 extern calls. `mem.*` is a typed pointer family: `mem_pointer_to`,
    `.pointer` as a resolved type, type-argument strata (`mem.load(i64)(p)`).
    DNIR has no pointer type beyond a `ptr` parameter, so this extends the type
    system rather than adding call arms.

  CROSS-MODULE RECORD RETURNS -- 4 programs. SH-04 territory, already tracked.

Everything else is singletons or correct rejections (`any` params/returns at
codegen.zig:2946/2949 -- `any` IS boxed, refusing it is right).

So the honest shape of "the rest of native" is three subsystems worth roughly
14 of the 43 remaining reachable programs, and a long tail of ones.

## mandelbrot: a five-deep blocker chain, fully named

Worth recording as the worked example of what the instrumentation buys, and of
where it runs out. Each fix moved the bail one layer and revealed the next:

  1. precheck `runtime-global-call:print`   -> gap[033], fixed
  2. `lowerModule` function-count mismatch  -> `: void` ineligible, fixed
  3. `lowerExprCons — HEIGHT`               -> `const NAME: T = v` never
                                               collected, fixed
  4. `lowerModule — mandel`                 -> f64 params required an f64
                                               return, fixed
  5. `evalDnirValueFp:1791`                 -> integer immediate in a float
                                               position, fixed (scvtf)
  6. `compileDnirInstr:1343`                -> OPEN

Row 6 is a float binop the ARM64 emitter does not have. The comparison path
(fcmp + cset, producing a GP result) already exists and is selected on
`ins.ty == .f64`; the float-FUNCTION arm beside it emits only add/sub/mul/div
and refuses everything else.

TRIED AND REVERTED: typing the comparison from its operands in `lowerBinop`
(`exprTouchesF64` instead of `exprIsF64`, on the theory that `zx*zx + zy*zy >
4.0` is integer-VALUED so `exprIsF64` says false and the f64 path is never
selected). It builds and changes nothing measurable, so it was backed out
rather than kept as an unverified broad change to float typing.

The remaining suspect is the backend arm itself, and it carries a real hazard:
`temps` maps slots to u5 register numbers, and GP and FP registers share that
numbering. Routing a comparison inside a float function to a GP destination
puts a GP index into a map that `evalDnirValueFp` will later read as an FP
register. That is the same class as the three register-lifetime bugs the table
work hit, so it wants a session with room to differential it.

## The denominator is wrong

The 14 `global_decl` rows are all `global A = std.compiler.arm64`-style module
aliases inside compiler proof fixtures — programs whose PURPOSE is driving the
C emitter. They cannot compile to native ARM64, by construction. **The
reachable ceiling is ~126/140 (90%), not 140.** Any target stated as "100%
native" needs this denominator corrected first, or it is unsatisfiable for a
reason that has nothing to do with the compiler.

## Concat, isolated by controls (2026-08-08)

`string.len(a) + string.len(b)` returns 4 on the direct backend, so both strlen
results survive the caller-save restore, the addition is right, and malloc
receives 5. **The size is not the fault.**

As far as controls reach:

| step | status | how established |
|---|---|---|
| malloc size | correct (4 + 1) | control |
| strcpy | works — buffer is `"AB"`, hence length 2 | inferred from length |
| **strcat** | **does nothing** | the entire remaining fault |
| call operands | correct | assembly dump |
| consumer (`str_len`) | correct — `len("ABCDE")` = 5 | control |

So a two-argument call whose operands are demonstrably correct performs no
work.

**Next control, prescribed rather than theorized:** read byte 3 of the buffer
immediately after the concat. NUL means strcat never wrote (strcpy's terminator
intact); garbage means it wrote somewhere unexpected. One command, and it
halves the last remaining question.

**Method:** five *explanations* for this row were wrong. Four *controls* in a
row each eliminated a suspect cleanly. Continue with controls, not with reading
listings.

### Correction: "strcat does nothing" is wrong — the two consumers disagree

The 211 result is **internally inconsistent**, which I missed while treating it
as a single number.

    211 = len * 100 + byte(c, 3)  =>  len = 2, byte = 11

`len = 2` means `str_len` found a NUL at offset 2. `byte(c, 3)` is 1-based, so
it reads that **same offset 2**, and got **11**. A buffer cannot have both 0
and 11 at one address.

So the two consumers are not reading the same memory, and the fault is not
"strcat performed no work" — it is that the concat's result **register or
address differs between uses**. The candidate answers change accordingly:

- `buf` is a temp whose register is reallocated between the two consumers, so
  the second read walks a different pointer, or
- the value returned from `lowerConcat` is re-evaluated rather than reused,
  producing a second (empty) buffer.

For reference, a correct `"AB".."CD"` gives 467; a strcpy-only buffer gives
**200**, not 211. The 200 case is what "strcat did nothing" would actually look
like, and it is not what was observed.

**Next control:** bind the result once and read it twice —
`n = string.len(c)` then `m = string.byte(c, 3)` as separate statements — and
compare against reading it once. If a single read is correct and two reads
disagree, it is the register; that is one command and it decides between the
two candidates above.

**Method, reinforced:** this correction came from taking the number apart, not
from new instrumentation. A composite result hides a contradiction that either
component alone would have exposed — the probe should have returned one fact.

## Instrumentation hole (found 2026-08-08, not closed)

Two programs still refuse with **no bail site at all**:

    decode_differential_smoke.duo      refused with: UnsupportedProgram
    instruction_descriptor_smoke.duo   refused with: UnsupportedProgram

Every known path now records itself — the native-scalar precheck (tag or
`@src()` line), `dnir_lower.bail`, `native_backend.refuse` across all 73+7
sites, and the link-graph refusal in main.zig. So these two clear every
instrumented gate and are refused somewhere still silent.

That is the same condition this whole gap started from, one layer deeper, and
the same method applies: print the error name unconditionally (already done —
that is how these were spotted), then bisect by adding `refuse(@src())` to
whichever remaining site can produce `UnsupportedProgram`. Candidates not yet
audited: helpers reached from `emitObject`/`emitAssembly` that construct the
error indirectly, and anything that re-raises a caught error (the mechanism
that made `DUO_DNIR_TRACE` print an empty stack trace).

Worth doing early: every previous round of this instrumentation converted an
opaque row into a specific, usually small, fix.

### Correction: there is no instrumentation hole

Measured against a clean HEAD worktree, `decode_differential_smoke` reports:

    bail site: native-scalar precheck — codegen.zig:4119

The instrumentation works. The two programs that appeared to refuse silently
were built against a working tree carrying **another session's uncommitted
`codegen.zig`**, which does not include the precheck tagging. The observation
was an artifact of a dirty tree, not a gap in coverage.

**Lesson, and it generalizes past this repo:** on a shared branch, measure
against `git worktree add --detach HEAD` before recording a finding. A dirty
working tree silently attributes someone else's in-progress state to your own
code — and here it manufactured a defect that did not exist, in the exact
system built to stop that from happening.

That makes two phantom bugs this session: this one, and the concat failure that
lived entirely inside a composite probe. Both were disproved by a single
control. Neither was disproved by reasoning.

## math.* — precheck and lowering both work; the f64 extern ABI does not

`math.sqrt(x)` needs BOTH halves (precheck arm + dnir arm), and with both it
COMPILES AND RETURNS THE WRONG VALUE: `sqrt(16.0) == 4.0` is false on the
direct backend, true on C. Reverted rather than landed.

Root cause, from reading the emitter: `call_extern`'s `ins.ty == .f64` arm does

    ensureExternalSymbol → emitSaveCallerRegs → emitBl → restore → result in d0

and **never moves the argument into d0**. The integer arm below it does move
`ins.lhs` into x0; the f64 arm does not have the equivalent. So `sqrt` is
called with whatever happened to be in d0.

Fix: give the f64 arm the same argument move its integer sibling has —
`evalDnirValueFp(ins.lhs)` then `emitFmovReg(0, arg)` before the save. Verify
with `sqrt(16.0) == 4.0`, which is exact in binary and cannot pass by accident.

Note the ordering that made this findable: applying only the PRECHECK half
moved the failure to `patchCalls — math_sqrt`, which named the missing lowering
exactly. Applying both halves at once (an earlier attempt) reported only
`runtime-global:math` and taught nothing. **Land one half, read the new
failure, then land the other** — the intermediate error is the diagnostic.

### math: the argument move is necessary but NOT sufficient

Added the missing `evalDnirValueFp` + `emitFmovReg(0, arg)` to call_extern's
f64 arm — the move every sibling arm has and that one lacked — and
`sqrt(16.0) == 4.0` is STILL false on the direct backend.

So the missing move was real but is not the whole fault. Next suspect, and it
should be CONTROLLED rather than reasoned about: an f64 LOCAL lives in
`fp_locals` / `fp_stack_slots`, not in `temps`, and the argument move reads
through `evalDnirValueFp(temps, …)`. If the local is not in `temps`, the move
sources from the wrong place — and the program still compiles, because
`evalDnirValueFp` has a `.void => allocFpReg()` arm that silently yields a
fresh register instead of failing.

That silent `.void` arm is worth auditing on its own: it converts "no value
here" into "an arbitrary register", which is exactly how a wrong number reaches
the exit code without any diagnostic.

Three attempts at math have now failed (precheck only, both halves, both halves
plus the ABI move). Per this file's own rule, the next step is a CONTROL — emit
the assembly for `math.sqrt(x)` and read which register d0 is loaded from —
not a fourth guess.
