| field | value |
|---|---|
| title | GAP-034 — the native precheck ratchets behind its own lowering |

| # | directive |
|---|---|
| 1 | **Filed** 2026-08-07. **Status** open (structural). **Cost so far** four separate bugs, ~17 programs. |

| section |
|---|---|
| The pattern |

| # | directive |
|---|---|
| 1 | `can_emit_native_scalar_module` / `expr_is_native_scalar` (src/codegen.zig) is a *conservative approximation* of what `src/dnir_lower.zig` can lower. |
| 2 | Nothing keeps the two in sync. |
| 3 | Every time the lowering grows a capability, the gate in front of it keeps refusing programs that would now succeed — silently, because the module falls back to the C backend and still compiles. |

| # | directive |
|---|---|
| 1 | Four instances, all found and fixed 2026-08-07 by instrumenting the bail sites: |

| construct | lowering had it | gate said |
|---|---|---|
| `string.byte(s, i)` | indexed load, added for tokenizer loops | `runtime-global:string` |
| `s:byte(1)` (FACE-CALL) | — (added with the fix) | `expr-unhandled:method_call` |
| `Kind = @{ eof = 0 }` | `collectModuleConsts`, doc names this exact form | `assign-value:compile` |
| `blob = { 0x00, 0x61 }` | memory-backed positional tables since SH-04 | `keyed-table-export` |

| # | directive |
|---|---|
| 1 | The third and fourth are the sharpest: in both, the lowering's own doc comment names the construct it supports, and the gate rejected it anyway. |

| # | directive |
|---|---|
| 1 | Worse, two of these were the **canonical** spelling. `s:byte(1)` (rule 6, FACE-CALL) and `@{ … }` (the enum descriptor) both disqualified their module while the deny-listed vestige either worked or failed identically — writing canonical Duo bought no native coverage over the form the canon forbids. |

| section |
|---|---|
| Why this is a gap and not four fixes |

| # | directive |
|---|---|
| 1 | The fifth instance is already latent. |
| 2 | The gate is hand-written prose about a subset that is defined somewhere else, so it drifts by default and only un-drifts when someone measures. |
| 3 | The fix is to derive both from one description of the subset — the lowering states what it accepts, the gate consumes that statement — rather than re-deriving it by hand each time. |

| # | directive |
|---|---|
| 1 | Interim, and cheap: when a program bails, **grep `dnir_lower.zig` for the construct before writing any lowering code.** It is probably already there. |

| section |
|---|---|
| Ranked worklist as of this filing (85/140 native, 61%) |

| # | directive |
|---|---|
| 1 | Measured with `duo compile --backend=direct` over `examples/` minus `compile_fail/`; see the `bail site:` diagnostic added the same day. |

| # | directive |
|---|---|
| 1 | 14 mod-top-stmt:global_decl UNREACHABLE — see below 5 runtime-global:mem real work: 17 intrinsics, zero DNIR support 4 lowerCall dnir_lower:1826 real work: cross-module record return (SH-04) 3 runtime-global:string string.char — allocates 2 runtime-global:os zero DNIR support 2 keyed-table-export genuine keyed exports 2 assign-value:nil genuine 2 codegen.zig:2946/2949 `any` param/return — correctly rejected, `any` IS boxed 1 runtime-global:math zero DNIR support … singletons in dnir_lower |

| # | directive |
|---|---|
| 1 | No drift remains in this list. |
| 2 | Every row above needs new backend capability. |

| section |
|---|---|
| The rows merge into three subsystems, not sixteen fixes |

| # | directive |
|---|---|
| 1 | Re-measured over the REACHABLE set only (`zig build native-census`), and two rows that look unrelated are the same missing capability: |

| # | directive |
|---|---|
| 1 | NATIVE STRING ALLOCATION -- 5 programs. |
| 2 | RE-MEASURED 2026-08-08, and the blocker is NOT allocation. `mem.alloc` -> malloc already lowers (three mem primitives landed as plain libc calls). `string.char(n)` composes to malloc(2) + two byte stores + return, and the DNIR opcodes for all of it exist. |
| 3 | What does not exist is the ARM64 ENCODING: `store_index` at byte width refuses at native_backend.zig:1751 -- only the `.i64` scaled 8-byte store is emitted. |
| 4 | The READ side has been there all along (`string.byte` -> byte load), so this is a one-sided pair. `strb w, [base, idx]` is the missing instruction, and it unblocks string.char; `..` (concat) needs it too plus a length-driven copy loop. |

| # | directive |
|---|---|
| 1 | THE ENCODING, and the hazard. `emitLdrb` (native_backend.zig:3287) uses 0x39400000 -- LDRB, immediate, unsigned offset. |
| 2 | The counterpart is STRB with the same addressing form, 0x39000000, and `emitStrb` mirrors it field for field. |
| 3 | That part is mechanical. |

| # | directive |
|---|---|
| 1 | What is NOT mechanical is the OFFSET CONVENTION, and it is why this was not landed blind. |
| 2 | The existing byte READ is string.byte, which computes `base + (i - 1)` because Duo indexes strings from 1 and C pointers from 0. |
| 3 | A raw buffer store (string.char writing the character at 0 and the NUL at 1) wants no such adjustment. |
| 4 | So `store_index` at byte width serves two callers with different origins, and picking one silently corrupts the other -- a write to a wrong address, which the 68-program differential would NOT catch, because nothing else in the corpus performs a byte store. |
| 5 | The verification would rest entirely on one hand-written probe. |

| # | directive |
|---|---|
| 1 | ATTEMPTED 2026-08-08, AND THE PROBE CAUGHT IT. |
| 2 | The full chain was built: emitStrb (0x39000000, mirroring emitLdrb field for field), a byte-width store_index arm sharing the load's single `base + (i - 1)` origin, string.char lowering with 1-BASED indices so the raw buffer normalizes at the lowering instead of giving the backend a second convention, and exprIsStr taught that string.char PRODUCES a str (without which the local never enters str_slots and the very next string.byte does not recognize its own argument -- a producer the type tracker does not know about makes every consumer downstream fail). |

| # | directive |
|---|---|
| 1 | It compiles. |
| 2 | It SIGSEGVs: `s: str = string.char(65); string.byte(s, 1)` returns 139, not 65. |
| 3 | So the address computation or the register lifetime around the malloc result is wrong, and the corpus differential stayed green through all of it -- 89 programs, none of which perform a byte store. |

| # | directive |
|---|---|
| 1 | That is the predicted failure, observed. |
| 2 | The reason this row was held back was that "it compiles and the gates are green" is not evidence here, and the one hand-written probe was the only thing that could fail. |

| # | directive |
|---|---|
| 1 | Next session: keep the chain above (it is the right shape) and debug the segfault -- first suspect is the malloc result temp being clobbered before the two stores, since call_extern goes through emitSaveCallerRegs and the stores evaluate three operands afterward. |

| # | directive |
|---|---|
| 1 | CONCAT ATTEMPTED 2026-08-08 AND REVERTED — one layer past string.char. `a .. b` lowers cleanly as strlen, strlen, add, add 1, malloc, strcpy, strcat, and every piece exists: `mov_arg` marshals DNIR values into x0..x7, which is how every multi-argument call in dnir_lower already works. |
| 2 | Also needed, and the same lesson string.char taught: exprIsStr must know that a concat PRODUCES a str, or `string.len` on its own result falls through to an undefined `string_len`. |

| # | directive |
|---|---|
| 1 | It compiles and is WRONG, not crashing: `"AB" .. "CD"` gives length 2, so strcpy ran and strcat did not append. |

| # | directive |
|---|---|
| 1 | FIRST SUSPECT WAS WRONG, and the correction matters more than the guess. |
| 2 | I blamed `emitSaveCallerRegs` clobbering the staged x0/x1. |
| 3 | It does not -- measured, it saves x9..x28 only (native_backend.zig:3211), so mov_arg staging survives it untouched. |

| # | directive |
|---|---|
| 1 | What IS true and unverified: `Instr.lhs` defaults to `.void` (duo_native_ir.zig:127), so a call_extern emitted with no `lhs` correctly skips the move-to-x0 and leaves the mov_arg staging alone. |
| 2 | That means the marshalling path is probably fine and the fault is elsewhere -- most likely in which register `evalDnirValue` hands back while x0 already holds the first argument, since `mov_arg` writes x0/x1 directly without any record that they are now live. |

| # | directive |
|---|---|
| 1 | The measurable fact, independent of cause: multi-argument extern calls have NO existing caller. |
| 2 | Every current call_extern passes one argument through `ins.lhs`. |
| 3 | So the path is untested. |

| # | directive |
|---|---|
| 1 | AND THE OBVIOUS DEBUGGING STEP IS BLOCKED. |
| 2 | "Dump the assembly" is the right move and does not work: `duo compile --backend=direct --emit asm x.id -o out.s` writes a MACH-O EXECUTABLE. |
| 3 | Magic bytes cf fa ed fe, mode rwxr-xr-x, and it reports `ok compile` -- the wrong format is silent. |
| 4 | Same class as gap[015] (`--emit obj`/`dylib` emit executables) and gap[019] (`--emit wasm` emits Mach-O), so this is the third member of that family and the first one that blocks an investigation rather than a deliverable. |

| # | directive |
|---|---|
| 1 | The capability exists internally: native_backend has `emitAssembly` and an `asm_text` buffer that every emitter already appends to (that is where the "strb w{d}, [x{d}]" listings go). |
| 2 | Only the CLI route fails to select it. |
| 3 | ROOT CAUSE FOUND, FIX NOT LANDED. `--target native-asm` works and emits real assembly (`.section __TEXT,__text` … `adrp x9, Lduo_str_0@PAGE`), so the emitter is fine and only the CLI mapping is broken. |

| # | directive |
|---|---|
| 1 | `ResolvedTarget.toLegacyTargetName` (target_model.zig) opens with `if (self.legacy_name) \|n\| return n;`. |
| 2 | The default `--target` is the BARE `native`, which carries no emit information, so it is returned and the `--emit` kind is discarded before the switch below it — the switch that already maps `.assembly => "native-asm"` correctly. |

| # | directive |
|---|---|
| 1 | The fix is to let an information-free `native` defer to the emit kind while an explicit legacy name still wins: |

| # | directive |
|---|---|
| 1 | if (self.legacy_name) \|n\| { if (!std.mem.eql(u8, n, "native")) return n; if (self.emit == .exe) return n; } |

| # | directive |
|---|---|
| 1 | THAT FIX IS NECESSARY BUT NOT SUFFICIENT — measured 2026-08-08 once the tree built again. |
| 2 | Applied it; `--emit asm` still wrote a Mach-O executable. |
| 3 | The next guard is two lines further down: |

| # | directive |
|---|---|
| 1 | if (!self.triple.directBackendSupported()) return null |

| # | directive |
|---|---|
| 1 | With `legacy_name` no longer short-circuiting, control reaches that line, returns null, and `resolveCompileTarget` falls back to the ORIGINAL input (`native`). `resolveCompileBackend` then sees `--backend=direct` with a non-machine target and rewrites it to `native-exe` (main.zig:2994) — the executable. |
| 2 | So there are TWO layers discarding the emit kind, and the second only becomes visible after the first is fixed. |

| # | directive |
|---|---|
| 1 | THIRD CORRECTION, and the one that matters: `toLegacyTargetName` IS NOT ON THE PATH AT ALL. |
| 2 | Both fixes above were applied together and `--emit asm` still wrote a Mach-O executable, which means `resolveTargetInput("native", .assembly)` returns NULL — so resolveCompileTarget's whole `if` block is skipped, `target_in` ("native") is returned unchanged, and resolveCompileBackend rewrites it to `native-exe`. |
| 3 | Every edit to toLegacyTargetName was dead code. |

| # | directive |
|---|---|
| 1 | ASSEMBLY DUMPED 2026-08-08 once --emit asm was fixed (179d322), and it CONTRADICTS the diagnosis above. |
| 2 | The emitted sequence is correct: |

| # | directive |
|---|---|
| 1 | bl _malloc ldr x10,[sp,#0] … ldr x30,[sp,#24]; add sp,sp,#32 restore caller regs mov x9, x0 malloc result saved mov x0, x9 arg0 = buf mov x1, x10 arg1 = "AB" bl _strcpy mov x13, x0 mov x0, x9 arg0 = buf again mov x1, x11 arg1 = "CD" bl _strcat |

| # | directive |
|---|---|
| 1 | Both calls receive the malloc result and the right operand. mov_arg staging survives emitSaveCallerRegs, as expected once that save was measured to cover x9..x28 only. |
| 2 | So "multi-argument extern calls do not work" is NOT established — the earlier length-2 result was observed BEFORE exprIsStr learned that a concat produces a str, and may simply have been the consumer misreading the result rather than the producer building it wrong. |

| # | directive |
|---|---|
| 1 | RE-RUN 2026-08-08 WITH BOTH CHANGES TOGETHER: still 211, i.e. length 2 and a garbage byte 3. |
| 2 | So the "consumer, not producer" theory is ALSO wrong — that is the fourth failed explanation for this row. |

| # | directive |
|---|---|
| 1 | What is now established by measurement rather than argument: |

| # | directive |
|---|---|
| 1 | the emitted call sequence is correct (assembly, above) |
| 2 | landing exprIsStr's `.binop` case alongside lowerConcat changes nothing |
| 3 | the result is consistently length 2, i.e. strcpy's output survives and strcat's does not |

| # | directive |
|---|---|
| 1 | What has NOT been done and is the actual next step: dump the assembly for a program that uses BOTH `string.len` and `string.byte` on the concat result (the dump above was len-only), and single-step or disassemble the strcat call's operands at that point. |
| 2 | Every explanation offered so far has been inference from a partial dump. |

| # | directive |
|---|---|
| 1 | NARROWED BY CONTROL, 2026-08-08 — and the narrowing is the useful part. `string.len("ABCDE")` returns 5 on BOTH backends. |
| 2 | So str_len works on a literal-backed str, the consumer is fine, and the length-2 result means the concat BUFFER is genuinely wrong. |
| 3 | Producer, not consumer — settled by a positive control rather than by reading instructions. |

| # | directive |
|---|---|
| 1 | (I first read the dumped str_len loop as broken — lines `b.eq .Lduo_0` and `b .Lduo_0` appearing to share a target — and the control above disproves it. |
| 2 | That was the FIFTH wrong inference in this row, all of the same kind: a conclusion drawn from a partial listing. |
| 3 | The dump is evidence about what was emitted; it is not evidence about what executes, and a two-line control settled in one command what five readings did not.) |

| # | directive |
|---|---|
| 1 | So: strcpy's output lands, strcat's does not, on a buffer malloc returned and both calls received correctly. |
| 2 | The remaining suspects are the malloc SIZE (total = la + lb + 1, computed from two strlen results that themselves come back through the caller-save restore) and whether strcat's own return path disturbs the buffer register. |
| 3 | Test the size first — it is one `mov` to inspect and would explain a truncated result exactly. |

| # | directive |
|---|---|
| 1 | NEXT STEP IS TO RE-RUN IT, not to reason further: reapply the lowerConcat arm plus the exprIsStr `.binop` case together, and check `a .. b` for "AB".."CD" gives length 4 and byte 3 == 'C' (67). |
| 2 | The assembly says it should. |
| 3 | Three of my diagnoses in this area were wrong before the dump existed; this one is at least backed by the instructions. |

| # | directive |
|---|---|
| 1 | Start at `resolveTargetInput` (target_model.zig:~200), not at toLegacyTargetName. |
| 2 | The question is why a bare `native` with an explicit emit kind does not resolve, and `--target native-asm` resolving correctly (line 218 handles that name explicitly) is the reference to diff against. |

| # | directive |
|---|---|
| 1 | I was wrong about this function three times in a row — first blaming legacy_name, then the directBackendSupported guard, then both together. |
| 2 | Each guess was consistent with the code I had read and none was tested before being written down. |
| 3 | The lesson is in the record because the next reader should trust the MEASUREMENTS in this file, not its explanations. |

| # | directive |
|---|---|
| 1 | Both need to hold for `--emit asm`: let a bare `native` defer to the emit kind, AND make the assembly emit survive the directBackendSupported guard (assembly is a machine-target emit, so the guard is probably just wrong for `.assembly`). |
| 2 | Verification is unchanged: `file out.s` must say "assembler source text", and `--target native-asm` already produces exactly that, so there is a working reference to diff against. |

| # | directive |
|---|---|
| 1 | NOT COMMITTED: the tree does not currently build (a parallel session has ~900 lines in flight across sema.zig/ast.zig/codegen.zig), so this could not be verified, and an unverified fix is exactly what the rest of this file argues against. |
| 2 | Apply and check `file out.s` says "assembler source text". |

| # | directive |
|---|---|
| 1 | Fixing `--emit asm` is therefore small, and it is a PREREQUISITE for the concat work rather than a separate errand -- without it the next attempt is back to reasoning about the register allocator, which is exactly what produced the wrong suspect above. |

| # | directive |
|---|---|
| 1 | Resolve the convention first (either a distinct opcode/flag for raw vs string-indexed stores, or normalize at the lowering so the backend only ever sees 0-based), then emit. |
| 2 | This is the one place in the remaining worklist where "it compiles and the differential is green" is not evidence. |

| # | directive |
|---|---|
| 1 | `lowerBinop` bail = `..` (concat), not a missing bitwise op as the site suggests |
| 2 | DNIR's BinOpTag genuinely lacks band/bor/bxor/lshift/rshift, but NEITHER blocked program uses them. |
| 3 | Both use `..`. |
| 4 | Add `string.char` (3) and the lever is one thing: a native string allocator (malloc + copy) in DNIR. |

| # | directive |
|---|---|
| 1 | MEM POINTER TYPES -- 5 programs. |
| 2 | Not 17 extern calls. `mem.*` is a typed pointer family: `mem_pointer_to`, `.pointer` as a resolved type, type-argument strata (`mem.load(i64)(p)`). |
| 3 | DNIR has no pointer type beyond a `ptr` parameter, so this extends the type system rather than adding call arms. |

| # | directive |
|---|---|
| 1 | CROSS-MODULE RECORD RETURNS -- 4 programs. |
| 2 | SH-04 territory, already tracked. |

| # | directive |
|---|---|
| 1 | Everything else is singletons or correct rejections (`any` params/returns at codegen.zig:2946/2949 -- `any` IS boxed, refusing it is right). |

| # | directive |
|---|---|
| 1 | So the honest shape of "the rest of native" is three subsystems worth roughly 14 of the 43 remaining reachable programs, and a long tail of ones. |

| section |
|---|---|
| mandelbrot: a five-deep blocker chain, fully named |

| # | directive |
|---|---|
| 1 | Worth recording as the worked example of what the instrumentation buys, and of where it runs out. |
| 2 | Each fix moved the bail one layer and revealed the next: |

| # | directive |
|---|---|
| 1 | precheck `runtime-global-call:print`   -> gap[033], fixed |
| 2 | `lowerModule` function-count mismatch  -> `: void` ineligible, fixed |
| 3 | `lowerExprCons — HEIGHT`               -> `const NAME: T = v` never collected, fixed |
| 4 | `lowerModule — mandel`                 -> f64 params required an f64 return, fixed |
| 5 | `evalDnirValueFp:1791`                 -> integer immediate in a float position, fixed (scvtf) |
| 6 | `compileDnirInstr:1343`                -> OPEN |

| # | directive |
|---|---|
| 1 | Row 6 is a float binop the ARM64 emitter does not have. |
| 2 | The comparison path (fcmp + cset, producing a GP result) already exists and is selected on `ins.ty == .f64`; the float-FUNCTION arm beside it emits only add/sub/mul/div and refuses everything else. |

| # | directive |
|---|---|
| 1 | TRIED AND REVERTED: typing the comparison from its operands in `lowerBinop` (`exprTouchesF64` instead of `exprIsF64`, on the theory that `zx*zx + zy*zy > 4.0` is integer-VALUED so `exprIsF64` says false and the f64 path is never selected). |
| 2 | It builds and changes nothing measurable, so it was backed out rather than kept as an unverified broad change to float typing. |

| # | directive |
|---|---|
| 1 | The remaining suspect is the backend arm itself, and it carries a real hazard: `temps` maps slots to u5 register numbers, and GP and FP registers share that numbering. |
| 2 | Routing a comparison inside a float function to a GP destination puts a GP index into a map that `evalDnirValueFp` will later read as an FP register. |
| 3 | That is the same class as the three register-lifetime bugs the table work hit, so it wants a session with room to differential it. |

| section |
|---|---|
| The denominator is wrong |

| # | directive |
|---|---|
| 1 | The 14 `global_decl` rows are all `global A = std.compiler.arm64`-style module aliases inside compiler proof fixtures — programs whose PURPOSE is driving the C emitter. |
| 2 | They cannot compile to native ARM64, by construction. **The reachable ceiling is ~126/140 (90%), not 140.** Any target stated as "100% native" needs this denominator corrected first, or it is unsatisfiable for a reason that has nothing to do with the compiler. |

| section |
|---|---|
| Concat, isolated by controls (2026-08-08) |

| # | directive |
|---|---|
| 1 | `string.len(a) + string.len(b)` returns 4 on the direct backend, so both strlen results survive the caller-save restore, the addition is right, and malloc receives 5. **The size is not the fault.** |

| # | directive |
|---|---|
| 1 | As far as controls reach: |

| step | status | how established |
|---|---|---|
| malloc size | correct (4 + 1) | control |
| strcpy | works — buffer is `"AB"`, hence length 2 | inferred from length |
| **strcat** | **does nothing** | the entire remaining fault |
| call operands | correct | assembly dump |
| consumer (`str_len`) | correct — `len("ABCDE")` = 5 | control |

| # | directive |
|---|---|
| 1 | So a two-argument call whose operands are demonstrably correct performs no work. |

| # | directive |
|---|---|
| 1 | **Next control, prescribed rather than theorized:** read byte 3 of the buffer immediately after the concat. |
| 2 | NUL means strcat never wrote (strcpy's terminator intact); garbage means it wrote somewhere unexpected. |
| 3 | One command, and it halves the last remaining question. |

| # | directive |
|---|---|
| 1 | **Method:** five *explanations* for this row were wrong. |
| 2 | Four *controls* in a row each eliminated a suspect cleanly. |
| 3 | Continue with controls, not with reading listings. |

| section |
|---|---|
| Correction: "strcat does nothing" is wrong — the two consumers disagree |

| # | directive |
|---|---|
| 1 | The 211 result is **internally inconsistent**, which I missed while treating it as a single number. |

| # | directive |
|---|---|
| 1 | 211 = len * 100 + byte(c, 3) => len = 2, byte = 11 |

| # | directive |
|---|---|
| 1 | `len = 2` means `str_len` found a NUL at offset 2. `byte(c, 3)` is 1-based, so it reads that **same offset 2**, and got **11**. |
| 2 | A buffer cannot have both 0 and 11 at one address. |

| # | directive |
|---|---|
| 1 | So the two consumers are not reading the same memory, and the fault is not "strcat performed no work" — it is that the concat's result **register or address differs between uses**. |
| 2 | The candidate answers change accordingly: |

| # | directive |
|---|---|
| 1 | `buf` is a temp whose register is reallocated between the two consumers, so the second read walks a different pointer, or |
| 2 | the value returned from `lowerConcat` is re-evaluated rather than reused, producing a second (empty) buffer. |

| # | directive |
|---|---|
| 1 | For reference, a correct `"AB".."CD"` gives 467; a strcpy-only buffer gives **200**, not 211. |
| 2 | The 200 case is what "strcat did nothing" would actually look like, and it is not what was observed. |

| # | directive |
|---|---|
| 1 | **Next control:** bind the result once and read it twice — `n = string.len(c)` then `m = string.byte(c, 3)` as separate statements — and compare against reading it once. |
| 2 | If a single read is correct and two reads disagree, it is the register; that is one command and it decides between the two candidates above. |

| # | directive |
|---|---|
| 1 | **Method, reinforced:** this correction came from taking the number apart, not from new instrumentation. |
| 2 | A composite result hides a contradiction that either component alone would have exposed — the probe should have returned one fact. |

| section |
|---|---|
| Instrumentation hole (found 2026-08-08, not closed) |

| # | directive |
|---|---|
| 1 | Two programs still refuse with **no bail site at all**: |

| # | directive |
|---|---|
| 1 | decode_differential_smoke.id refused with: UnsupportedProgram instruction_descriptor_smoke.id refused with: UnsupportedProgram |

| # | directive |
|---|---|
| 1 | Every known path now records itself — the native-scalar precheck (tag or `@src()` line), `dnir_lower.bail`, `native_backend.refuse` across all 73+7 sites, and the link-graph refusal in main.zig. |
| 2 | So these two clear every instrumented gate and are refused somewhere still silent. |

| # | directive |
|---|---|
| 1 | That is the same condition this whole gap started from, one layer deeper, and the same method applies: print the error name unconditionally (already done — that is how these were spotted), then bisect by adding `refuse(@src())` to whichever remaining site can produce `UnsupportedProgram`. |
| 2 | Candidates not yet audited: helpers reached from `emitObject`/`emitAssembly` that construct the error indirectly, and anything that re-raises a caught error (the mechanism that made `DUO_DNIR_TRACE` print an empty stack trace). |

| # | directive |
|---|---|
| 1 | Worth doing early: every previous round of this instrumentation converted an opaque row into a specific, usually small, fix. |

| section |
|---|---|
| Correction: there is no instrumentation hole |

| # | directive |
|---|---|
| 1 | Measured against a clean HEAD worktree, `decode_differential_smoke` reports: |

| # | directive |
|---|---|
| 1 | bail site: native-scalar precheck — codegen.zig:4119 |

| # | directive |
|---|---|
| 1 | The instrumentation works. |
| 2 | The two programs that appeared to refuse silently were built against a working tree carrying **another session's uncommitted `codegen.zig`**, which does not include the precheck tagging. |
| 3 | The observation was an artifact of a dirty tree, not a gap in coverage. |

| # | directive |
|---|---|
| 1 | **Lesson, and it generalizes past this repo:** on a shared branch, measure against `git worktree add --detach HEAD` before recording a finding. |
| 2 | A dirty working tree silently attributes someone else's in-progress state to your own code — and here it manufactured a defect that did not exist, in the exact system built to stop that from happening. |

| # | directive |
|---|---|
| 1 | That makes two phantom bugs this session: this one, and the concat failure that lived entirely inside a composite probe. |
| 2 | Both were disproved by a single control. |
| 3 | Neither was disproved by reasoning. |

| section |
|---|---|
| math.* — precheck and lowering both work; the f64 extern ABI does not |

| # | directive |
|---|---|
| 1 | `math.sqrt(x)` needs BOTH halves (precheck arm + dnir arm), and with both it COMPILES AND RETURNS THE WRONG VALUE: `sqrt(16.0) == 4.0` is false on the direct backend, true on C. |
| 2 | Reverted rather than landed. |

| # | directive |
|---|---|
| 1 | Root cause, from reading the emitter: `call_extern`'s `ins.ty == .f64` arm does |

| # | directive |
|---|---|
| 1 | ensureExternalSymbol → emitSaveCallerRegs → emitBl → restore → result in d0 |

| # | directive |
|---|---|
| 1 | and **never moves the argument into d0**. |
| 2 | The integer arm below it does move `ins.lhs` into x0; the f64 arm does not have the equivalent. |
| 3 | So `sqrt` is called with whatever happened to be in d0. |

| # | directive |
|---|---|
| 1 | Fix: give the f64 arm the same argument move its integer sibling has — `evalDnirValueFp(ins.lhs)` then `emitFmovReg(0, arg)` before the save. |
| 2 | Verify with `sqrt(16.0) == 4.0`, which is exact in binary and cannot pass by accident. |

| # | directive |
|---|---|
| 1 | Note the ordering that made this findable: applying only the PRECHECK half moved the failure to `patchCalls — math_sqrt`, which named the missing lowering exactly. |
| 2 | Applying both halves at once (an earlier attempt) reported only `runtime-global:math` and taught nothing. **Land one half, read the new failure, then land the other** — the intermediate error is the diagnostic. |

| section |
|---|---|
| math: the argument move is necessary but NOT sufficient |

| # | directive |
|---|---|
| 1 | Added the missing `evalDnirValueFp` + `emitFmovReg(0, arg)` to call_extern's f64 arm — the move every sibling arm has and that one lacked — and `sqrt(16.0) == 4.0` is STILL false on the direct backend. |

| # | directive |
|---|---|
| 1 | So the missing move was real but is not the whole fault. |
| 2 | Next suspect, and it should be CONTROLLED rather than reasoned about: an f64 LOCAL lives in `fp_locals` / `fp_stack_slots`, not in `temps`, and the argument move reads through `evalDnirValueFp(temps, …)`. |
| 3 | If the local is not in `temps`, the move sources from the wrong place — and the program still compiles, because `evalDnirValueFp` has a `.void => allocFpReg()` arm that silently yields a fresh register instead of failing. |

| # | directive |
|---|---|
| 1 | That silent `.void` arm is worth auditing on its own: it converts "no value here" into "an arbitrary register", which is exactly how a wrong number reaches the exit code without any diagnostic. |

| # | directive |
|---|---|
| 1 | Three attempts at math have now failed (precheck only, both halves, both halves plus the ABI move). |
| 2 | Per this file's own rule, the next step is a CONTROL — emit the assembly for `math.sqrt(x)` and read which register d0 is loaded from — not a fourth guess. |

| section |
|---|---|
| math: TWO backend bugs, both read off the assembly |

| # | directive |
|---|---|
| 1 | The dump settled it (this is what `--emit asm` was fixed for): |

| # | directive |
|---|---|
| 1 | mov x9, #0 <-- `x: f64 = 16.0` materialized as ZERO fmov d0, x9 bl _sqrt fmov d1, d0 <-- result move (added, works) mov x10, #0 <-- the 4.0 literal ALSO materialized as zero fmov d2, x10 fcmp d9, d2 <-- compares d9, which nothing in the function writes |

| # | directive |
|---|---|
| 1 | **f64 literals materialize as 0** in this path. `emitMovImm(r, @bitCast(n))` should place 0x4030000000000000 for 16.0; the listing shows `#0`. Both literals in the program are affected, so it is the literal path, not sqrt. |
| 2 | **The call result never reaches the local.** `r: f64 = …` lives in d9 and nothing writes d9. Mapping the result temp to an allocated register (my fix) moves d0 -> d1 correctly, but the store from the temp into the f64 LOCAL is missing — consistent with the earlier `fp local 'd.status' has no register` finding: f64 locals live in `fp_locals`/`fp_stack_slots`, a different space from `temps`, and the two are not bridged on this path. |

| # | directive |
|---|---|
| 1 | So `math.*` is blocked by f64 literal materialization and the temp/fp-local bridge — NOT by anything math-specific. |
| 2 | The precheck arm and the dnir arm are both correct and are recorded above for reuse once those two land. |

| # | directive |
|---|---|
| 1 | This also explains why every math attempt COMPILED: `evalDnirValueFp`'s `.void` arm hands back a fresh register instead of failing, so a missing value becomes an arbitrary register rather than a diagnostic. |

| section |
|---|---|
| `evalDnirValueFp`'s `.void` arm: a real hazard with a wrong fix |

| # | directive |
|---|---|
| 1 | `.void => try self.allocFpReg()` turns "there is no value here" into "an arbitrary register". |
| 2 | That is why three separate `math.sqrt` attempts COMPILED and returned wrong numbers with no diagnostic. |

| # | directive |
|---|---|
| 1 | Making it refuse (`refuseWith(@src(), "f64 operand is void")`) breaks SIX unit tests — measured, 1245 pass -> 1240. |
| 2 | So `.void` is load-bearing: some callers pass it deliberately meaning "allocate me a scratch register", not "the value is missing". |
| 3 | The two meanings share one encoding. |

| # | directive |
|---|---|
| 1 | Reverted. |
| 2 | The fix is NOT in the shared helper; it is either (a) a distinct `scratchFpReg()` for the deliberate callers, leaving `.void` to mean genuinely-absent and refuse, or (b) refusing at the specific call sites where a void operand is a bug. |

| # | directive |
|---|---|
| 1 | (a) is the ONE EDGE answer — two meanings should not share an encoding — and it is mechanical: find the six tests' paths, give them the explicit scratch call, then make `.void` refuse. |
