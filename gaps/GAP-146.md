# GAP-146 — build gate reports success after failed child process

**Status:** OWNER-BLOCKED · **Filed:** !2026-08-10T12:08:03Z
**Priority:** P0 · **Kind:** regression
**Filed by:** codex-root-audit-20260810
**Files:** lib/proc.id,gaps/GAP-115.md

| # | directive |
|---|---|
| 1 | At the audited revision, `duo_build_run(gate=unit-test)` returned `{passed:true, exit_code:0}` while the same locked invocation output said maker exited 1 and reported seven failures. |
| 2 | The former reproduction endpoint was `tools/mcp/bench.id`; it is now deleted and must not be restored. |
| 3 | Gate truth is still not bound to child process status until the graph-owned execution outcome closes. |
| 4 | Repair must make structured status reflect the actual completed child result and add a negative control that runs a known failing child. |
| 5 | Do not infer success from transport/server exit. |

| # | directive |
|---|---|
| 1 | The faulty MCP endpoint was deleted on 2026-08-17. |
| 2 | The broader obligation remains open for the graph-owned execution outcome; no new text wrapper replaces the removed endpoint. |

| # | directive |
|---|---|
| 1 | **2026-09-01 progress (devin):** Added negative control to `lib/process.id` that verifies the actual status code from a failing child process (`false` must exit with status 1). |
| 2 | Updated `lib/proc.id` to return structured status `{ status: i64 }` for `exec` and `{ output: str, status: i64 }` for `capture`, ensuring the status reflects the actual completed child result rather than inferring success from transport/server exit. |
| 3 | The `capture` implementation now appends `$?` to the command output and parses the exit code from the final line, matching the pattern used in `lib/os/process.id`. |

| # | directive |
|---|---|
| 1 | **2026-09-02 progress (devin):** Removed non-canonical `@comp.c.emit` directives from `lib/os/process.id` `env_set` and `env_unset` functions. |
| 2 | These violated closed lexical law (`@comp.*` directives are not lawful Idol source). |
| 3 | The bootstrap-level env manipulation now uses the admitted `env` table directly; persistence behavior remains bootstrap-defined. |

| # | directive |
|---|---|
| 1 | **2026-09-02 landed:** Bootstrap-level canonicality slice landed on origin/main (commit 1fd1e314, b17c92e0, 2535dc4c). |
| 2 | Removed non-canonical `@comp.c.emit` directives from `lib/os/process.id` `env_set` and `env_unset` functions, improving closed lexical law compliance at the bootstrap level. |
| 3 | This bounded slice is complete. |

| # | directive |
|---|---|
| 1 | **Current classification:** OWNER-BLOCKED on the execution-world outcome owned by GAP-119. |
| 2 | The structured status projection added in prior work (`lib/proc.id`) and the negative control (`lib/process.id`) are bootstrap-level fixes that improve the current Lua-root process bindings, but full closure requires the graph-owned execution-world relation defined in GAP-119. |
| 3 | No further progress on this gap is possible until GAP-119 closes and provides the admitted execution-world relation that returns child outcome case, command, backend, artifact, workload, revision, dirty snapshot, and lock observation directly. |
