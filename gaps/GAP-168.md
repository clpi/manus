| field | value |
|---|---|
| title | GAP-168 — scripts/ plural root rehome |
| status | OPEN |
| priority | P0 |
| kind | architecture |
| owner | Cursor / path convergence |

| section |
|---|---|
| Obligation |

| # | directive |
|---|---|
| 1 | `scripts/` violates `law.singular.one` (C0, total semantic closure §3). |
| 2 | Plurality encodes cardinality, not identity. |
| 3 | The root is migration debt — **not** repaired by renaming to `script/`. |

| section |
|---|---|
| Rehome map (by semantic owner) |

| Current | Target | Owner |
|---|---|---|
| `scripts/proof/` | `proof/` | semantic proof fixtures |
| `scripts/ledger/` | `ledger/` | evidence / perf / shc / ftcftw ledgers |
| `scripts/census/` | `census/` | census runners (infer, foreign, projection, …) |
| `scripts/ingress/` | `ingress/` | bootstrap foreign ingress (`GAP-155`) |
| `scripts/*gate*.id` | delete → `gate/` | gate authority already at `gate/` |
| `scripts/canon.id` | `gate/` | canon verdict owner |
| `scripts/run_*`, `scripts/*bench*` | `tool/` or delete | operational bootstrap until graph owns workflow |

| # | directive |
|---|---|
| 1 | Do **not** bulk-move without updating every reference (`build.zig`, pre-commit, gate controls, docs). `lib/semantic/*` is deleted (`law.catalog.zero`) — do not rehome anything into a relation catalog. |

| section |
|---|---|
| Gate |

| # | directive |
|---|---|
| 1 | **New staged paths:** `gate/path.id` `list(staged)` rejects plural roots via `plural(root(...))`. |
| 2 | **Existing tree:** tracked `scripts/**` remains debt until rehome completes; tree scan does not yet ratchet plural roots (would block all commits). |

| section |
|---|---|
| Deletion conditions |

| # | directive |
|---|---|
| 1 | Zero tracked paths under `scripts/`. |
| 2 | All references repointed to rehomed homes. |
| 3 | `list(staged)` negative control: `scripts/` fails; `proof/`, `ledger/` pass. |

| section |
|---|---|
| Evidence |

| # | directive |
|---|---|
| 1 | Law: `docs/spec/path-migration.md` § scripts rehome · C0 `law.singular.one`. |
