# GAP-118 — Environment lookup erases absence into empty string

**Status:** CLOSED · **Filed:** !2026-08-09T20:08:27Z · **Closed:** 2026-08-25
**Priority:** P0 · **Kind:** expressiveness_gap
**Filed by:** codex_semantic_normalization
**Files:** lib/os.id,lib/script.id,docs/spec/constitution.md,gate/gap-118-env-absence.sh

Closed at 7b025afe plus this increment. The record's premise moved under it:
the family is no longer vocabulary-blocked. `os.env[key]` is the admitted
environment face (`docs/spec/law.md` §2 "Unknown, absent, false, zero, empty,
and not-asked are distinct"; C0 `law.host`; `docs/spec/host.md`), it lowers to
the host environment read on both executing backends (`src/dnir_lower.zig`
index lowering to the `getenv` extern; `src/codegen.zig` `lua_os_getenv` boxed
face, which answers nil for NULL), and absence survives as a distinct runtime
fact. Measured 2026-08-25 on the emitted-C realization (`idol dump-c` + cc —
this host refuses direct-native with DNB004); the probe's exit IS the outcome:

    os.env["G118PROBE"]   unset -> 10   set-to-"" -> 20   set to "hello" -> 30

The erasure this record names was the `getenv` wrapper in `lib/os.id`
(absent -> `""`). The same probe routed through that wrapper shape answers
20/20/30 — absent collapses onto present-empty, measured, exactly the filed
defect. The wrapper was semantically unreachable (`std.os.getenv` refuses at
check under `law.std.zero`, structured diagnostic naming the namespace root;
zero call sites in the tree) and is deleted rather than repaired: environment
lookup has one owner and it is the projection, per `law.environment.one`.
`gate/gap-118-env-absence.sh` is the executed pin — it compiles the probe on
the exercised path, requires the three distinguishable exits 10/20/30, and
ratchets the deleted specimen; it runs green under `tools/node/dev/idol-lock`
against this tree's `zig build` artifact (exit 0). C0 `law.environment.one`
is corrected from `.unsettled` to `.proved` to match `docs/spec/law.md` §2 and
`law.host`, citing the gate.

Remaining, owned elsewhere: `lib/script.id` `getenv`/`getenv_or` still bake
defaulting and deliberately fire the fallback on present-empty by documented
contract; that frozen bootstrap bridge and its call sites are GAP-157/GAP-119
deletion debt, not an expressiveness gap — the vocabulary they lacked now
exists. `lib/env.id` carries the same dead-wrapper shape and falls with the
same deletion family.

Original record:

The authoritative vocabulary cannot currently express environment lookup while preserving absent versus present empty string. `lib/os.id` maps both missing and present-empty to `""`; `std.script.getenv(name, fallback)` then bakes defaulting into a namespace API. Required audit: name/key subject, environment world authority, absent|present(value) structured result, conversion/default composition, build-stage folding, and structured canonical diagnostic. Do not invent a replacement relation spelling until this family is admitted.
