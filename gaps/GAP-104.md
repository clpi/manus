# GAP-104 — `law.c.floor` exists as architecture and holds ZERO cost facts; the twelve dimensions are empty

**Status:** OPEN for the §47 cost vector · the separate tail-slot miscompile
| # | directive |
|---|---|
| 1 | recorded below is **FIXED 2026-08-09** (`lowerBlockTailEffect`, gate `zig build tail-slot`). · **Filed:** 2026-08-09 **Law:** constitution §47 (`law.c.floor`, `law.perf.floor`, `law.perf.dominance`) · §49 `cost.objective` · §3 MEASUREMENT HONESTY **Follows:** `gaps/GAP-096` — which said the comparison was measurable but not monotone **Gate:** `zig build c-floor` (`scripts/cfloor.id`) · `zig build tail-slot` (`scripts/tailslot.id`) |

## What landed

| # | directive |
|---|---|
| 1 | §47's three laws are no longer text with nothing behind them. |
| 2 | For every module-scope callable the compiler now builds a `.lowering` realization variable holding **two** candidates: |

| candidate | what it is | status |
|---|---|---|
| `lower.cequiv` | the C-equivalent lowering | **baseline, unconditional** |
| `lower.native` | the direct machine-code lowering | legal iff every declared descriptor lowers native |

| # | directive |
|---|---|
| 1 | `duo explain` renders it under `c_floor`. `selectAgainstFloor` starts at the baseline and moves off it only for a candidate that dominates on the §47 frontier **and** covers the active objective's relevance set **and** carries `proven` or `measured` evidence. `monotoneAgainst` checks a candidate set never shrinks when a fact is added. |

| # | directive |
|---|---|
| 1 | `zig build c-floor`, measured 2026-08-09, all rows green: |

```
  ok C1 law.c.floor        callables carrying a C-equivalent baseline: 2 of 2
  ok C2 law.c.floor        typed callable keeps both: total=2 viable=2
  ok F1 law.perf.floor     floor retained; why(skip): relation=insufficient
                           compared=0 missing=12 of 12 · unseats=false
  ok D1 law.perf.dominance candidate set across the fact: 2 -> 2
  ok D2 control            the fact was not inert: viable 1 -> 2
  ok D3 plan vs world      --backend=direct refuses the `any` version and
                           accepts the typed one
  ok M0 same answer        direct=262556 c-equivalent=262556 awk-oracle=262556
  ok M1 compile            direct=90ms  c-equivalent=120ms
  ok M2 size               direct=33608B  c-equivalent=33432B
  ok M3 latency            direct=510..620ms  c-equivalent=390..490ms
     verdict               the C-EQUIVALENT lowering is resolvably faster
```

## The gap, stated as the number it is

| # | directive |
|---|---|
| 1 | **Cost facts attached to either candidate: 0 of 12.** |

| # | directive |
|---|---|
| 1 | `src/realization.zig` carries `LOWERING_COST_FACTS_ATTACHED = 0` so this is a constant a test asserts, not a claim in prose. |
| 2 | Nothing in the semantic graph tells the builder what the two lowerings cost in latency, throughput, code size, compile time, peak memory, allocations, cache footprint, branch misses, energy, startup, binary size or tail latency — so it asserts nothing about any of them, `why(skip)` answers `insufficient · compared 0 · missing 12`, and the C floor is retained for want of evidence rather than on merit. |

| # | directive |
|---|---|
| 1 | That is the whole distance between the architecture and the goal. |
| 2 | The selector, the frontier, the evidence ladder and the monotonicity check are all real and tested; **the vector they read is empty.** |

## Two things the measurement killed, recorded so they are not re-derived

| # | directive |
|---|---|
| 1 | **1. `compile_time` is NOT proven from the process-spawn fact.** The first draft attached `compile_time` as `proven`: the C-equivalent path spawns an external C compiler and the direct path does not, so the ordering looked structural. |
| 2 | The gate measured it and the ordering was **backwards** — 220ms direct vs 110ms c-equivalent on the first run. |
| 3 | Later runs put it the other way (100 vs 180), because clang's invocation is cache-sensitive. |
| 4 | So the dimension is not merely mislabelled, it is **not stably ordered at all** on this workload. |

| # | directive |
|---|---|
| 1 | The fact was **deleted, not demoted to `estimated`**. |
| 2 | An estimate pointing the wrong way is still a wrong number, and an estimate can never unseat a baseline anyway — it would have been decoration on the render with no effect on selection. |
| 3 | This is §3's second rule ("a detector must verify every constant its emitter assumes, and decline to the general path otherwise") applied to a cost model instead of a kernel recognizer. |

| # | directive |
|---|---|
| 1 | The direct lowering LOSES to its own C-equivalent on the fixture.** On `examples/cfloor/fact.id` — 100M iterations of an integer recurrence, answer independently verified by awk — five runs each measured: |

```
direct         510..620ms
c-equivalent   390..490ms
```

| # | directive |
|---|---|
| 1 | The 120ms gap clears the 110ms spread, so the gate resolves it: **the C-equivalent lowering is faster.** On a noisier run the same fixture came back 610-710 against 430-660 and the gate declined to resolve it at all. |
| 2 | Both outcomes pass — the row is published, not gated — and neither is a big result on one kernel on one machine. |
| 3 | What it does establish is that `law.c.floor` is not a formality here: on the one workload anybody has measured, the floor is the better realization, and a compiler that shipped native "because it is native" would be shipping the slower code. |
| 4 | That is the failure §47's `fails` clause names in as many words. |

| # | directive |
|---|---|
| 1 | **No claim of C-dominance is made or supported by any of this**, in either direction. |
| 2 | The comparison is idol's direct lowering against idol's own C-equivalent lowering; awk is the answer oracle only. |
| 3 | Clearing the architecture makes a claim possible to state; it does not make one true. |

## What the empty vector needs, in order

1. **A latency fact with a witness.** The gate can measure whole-program wall
   time; the plan needs a per-candidate number. That is a profile-feedback edge,
   and it is the one dimension the goal is actually about.
2. **`code_size` from the emitters.** Both backends know how many bytes they
   emitted for a callable. This is the cheapest honest row on the board and it
   needs no measurement harness — it is a fact each lowering already has and
   throws away.
3. **`allocations` from the effect algebra.** `semantic_algebra.EffectSet`
   already tracks allocation effects; wiring it into the vector is a projection
   of a fact that exists.
4. **An objective that is not `balanced`.** Under `balanced` all twelve are
   relevant, so nothing will ever unseat the floor. §49 makes the objective an
   ordinary target/world fact; nothing reads one yet, so `C_FLOOR_OBJECTIVE` is
   a constant.

## A SEPARATE DEFECT found while writing this, and it is worse than the gap

| # | directive |
|---|---|
| 1 | **An `if` whose condition is a compile-time-known local silently DROPS ITS BODY.** Found while rewriting the fixtures away from `end` for the idiom gate; it has nothing to do with §47 and it is recorded here because it needs to be somewhere today. |

```duo
main: i64 = ()
    n = 42
    if n > 10
        print("big")
    print("done")
    0
```

| # | directive |
|---|---|
| 1 | prints `done`. |
| 2 | Only `done`. |
| 3 | The true branch is discarded. |
| 4 | It is **not** an offside-layout artifact — the identical program with `end` on both blocks behaves the same way — and it is **not** a parse error: the program compiles clean and answers wrongly, which is the worst available shape. |

| # | directive |
|---|---|
| 1 | Narrowed by control: replace `n = 42` with `n = std.script.status("true")` and the same program takes the branch correctly and declines the false one. |
| 2 | So the trigger is the CONSTANT, and the family is the file-scope scalar fold already recorded in this repository's memory as having produced confident wrong numbers once before. |

| # | directive |
|---|---|
| 1 | Every gate in `scripts/` that writes `if <literal-derived local>` is exposed to this. `scripts/cfloor.id` is not: every condition in it comes from a shell capture or a function parameter, and rows P0/D2/M4 are positive controls that would fail loudly if a branch vanished. |

## What is stubbed, named as stubs

- **Selection is a PLAN, not a decision.** Nothing consults the `.lowering`
  variable to choose a backend. `duo compile --backend=…` still decides, and the
  native backend's own precheck still decides bail-vs-native inside the backend
  — which is the `law.stack.direction` violation §47's mechanism is supposed to
  retire. Row D3 measures that the plan's legality verdict and the backend's
  actual behaviour AGREE, which is the strongest statement available while the
  two are separate mechanisms.
- **Legality is per-callable, the backend refuses per-module.** `funcLowersNative`
  reads one callable's declared descriptors. `--backend=direct` refuses the whole
  program when any part of it is outside the subset. D3 passes because the
  fixtures have a single interesting callable each; a mixed module would show the
  two disagreeing, and that disagreement is real, not a harness artifact.
- **`ModuleRealizations.lowering` is a second list** rather than more rows in
  `variables`, because `variables.len` is a gated number (`scripts/realize.id`
  asserts `"variable_count":1`) and `region_graph` sums legal candidates over it
  into a pending-decision count. Merging is right once both read a dimension
  filter instead of a length.
- **The twelve dimensions extend an existing enum** (`CostDimension` went 11 →
  16) rather than replacing it, so §47's twelve are a named subset and
  `transform_engine`'s four extra dimensions survive. `CostDimension.constitutional`
  is the §47 list and a test asserts it has twelve distinct rows.
- **Every JSON key this change introduces is one lowercase word** — `viable`,
  `total`, `unseats`, `retained`, `choice`, `decided`, `lowerings`, `missing`,
  `compared`. The older keys beside them (`selected_id`, `subject_entity`,
  `static_cost`, `optionality_retained`, `hard_constraints`) are not, and were
  left alone because `scripts/realize.id` and `scripts/explain.id` read them.
  H-8 makes an emission graph data rendered through the taxonomy and LAW-ONE
  governs the taxonomy, so the old keys are migration debt on this schema and
  the new ones are not added to it.

---

## SHARPENED 2026-08-09 — it is not the local, it is the TRUE constant, and `else` dies too

| # | directive |
|---|---|
| 1 | Reproduced independently and narrowed. |
| 2 | The filed repro blamed a compile-time constant LOCAL. |
| 3 | The local is incidental — a bare literal comparison does it too, and an `if/else` loses **both** arms. |

| program | prints | verdict |
|---|---|---|
| `n = 42` · `if n > 10 print("BIG")` | *nothing* | WRONG |
| `if 42 > 10 print("BIG")` | *nothing* | WRONG — no local involved |
| `if 42 < 10 print("X")` · `print("done")` | `done` | correct — a FALSE constant skips properly |
| `if 42 > 10 print("T") else print("F")` | *nothing at all* | WRONG — **neither arm runs** |
| `print(42)` | `42` | control — printing and constants both fine |

| # | directive |
|---|---|
| 1 | **The rule: a constant condition that evaluates TRUE drops the branch it should take.** False conditions are correct, so the folder is deciding and then discarding the arm it decided on — an inversion of the emission, not of the test. |
| 2 | The `else` case is the sharpest: a two-armed `if` where control reaches neither arm cannot be explained by a mis-taken branch. |

| # | directive |
|---|---|
| 1 | Every one exits 0 with `ok compile`. |
| 2 | Nothing warns. |

## Why this is worse than a wrong answer

| # | directive |
|---|---|
| 1 | `if <true constant>` is the shape of every feature flag, every debug guard, every `if DEBUG`, and every specialization written by hand. |
| 2 | It is also the shape a CONSTANT-FOLDING OPTIMIZER CREATES: propagate a constant into any conditional and this defect activates on code the author never wrote that way. |
| 3 | So the blast radius is not "programs containing a literal condition" — it is "programs the optimizer folds a condition in", which is far larger and impossible to grep for. |

| # | directive |
|---|---|
| 1 | It is the same family as the file-scope scalar fold and gap[063]'s table fill+sum: the fold is right, the emission after it is not. |

## Controls run

| # | directive |
|---|---|
| 1 | `print(42)` alone answers 42, so neither printing nor constant handling is broken in general. |
| 2 | The FALSE row is the positive control for the classifier — if constants were simply not evaluated, that row would print `X`. |

---

## CORRECTED AGAIN 2026-08-09 — it is not constants and it is not the branch. `print` inside an `if` is dropped on the NATIVE backend.

| # | directive |
|---|---|
| 1 | Two diagnoses were wrong, including my own sharpening directly above. |
| 2 | Both were wrong the same way: inferring the mechanism from what the program printed, without asking whether the branch ran. |

| # | directive |
|---|---|
| 1 | The test that settles it uses the EXIT CODE instead of stdout, so the branch reports itself without going through `print`: |

```duo
main: i64 = ()
    if 42 > 10
        return 7
    end
    return 3
end
```

```
--backend=direct   rc=7          the branch IS taken
--backend=c        rc=7
```

| # | directive |
|---|---|
| 1 | So nothing is dropping the branch. |
| 2 | The claim "a true constant condition drops its taken arm", and the earlier "an `if` on a compile-time-constant local silently drops its body", are both **withdrawn**. |

| # | directive |
|---|---|
| 1 | It is also not about constants. |
| 2 | A value produced by a loop behaves identically: |

```duo
n: i64 = 0
while n < 1
    n += 42
end
if n > 10
    print("RUNTIME")     -- not printed
end
print("after")           -- printed
```

| program | backend | prints |
|---|---|---|
| `if 42 > 10 print("BIG")` | direct | *nothing* |
| same | **c** | **BIG** |
| `if <runtime n> > 10 print("RUNTIME")` | direct | *nothing* |
| `print` outside any `if` | direct | prints |
| `if 42 > 10 return 7` | direct | **rc=7** |

| # | directive |
|---|---|
| 1 | **THE DEFECT: on the native/direct backend, a `print` call inside an `if` body emits nothing. |
| 2 | The branch executes; the call does not.** The C backend is correct throughout, which is why `--backend=c` masked it. |

## Why three readings missed it

| # | directive |
|---|---|
| 1 | Every earlier probe used `print` as its instrument, and the instrument was the thing that was broken. |
| 2 | The dnir `if_stmt` lowering and the `br_if_not` emission both read correct on inspection — and they ARE correct; the branch is taken. |
| 3 | An audit of the branch machinery would have found nothing wrong, forever. |

| # | directive |
|---|---|
| 1 | The rule this earns: **when a program produces no output, do not test the control flow with output.** Use the exit code, which the earlier gap note itself warned about from the other direction ("exit codes are 8-bit"). |

## Blast radius

| # | directive |
|---|---|
| 1 | Far larger than the constant-condition reading suggested. |
| 2 | Every `print` guarded by a condition — every error path, every debug branch, every conditional diagnostic — is silent under the default backend. `duo run` uses `direct`. |

## What closes it

| # | directive |
|---|---|
| 1 | Find why a `call_extern` to `print` inside a branch body is not emitted or not reached in `src/native_backend.zig`, given the branch demonstrably executes. |
| 2 | Positive control: the same `print` outside the `if` works, so the call lowering is fine in isolation — it is the interaction with the branch. |

## LOCALIZED — it is the TAIL statement of the branch, and only that one

```duo
if 42 > 10
    print("FIRST")     -- prints
    print("LAST")      -- DOES NOT PRINT
end
```

| # | directive |
|---|---|
| 1 | One `print` in the body vanishes; two, and only the last one does. |
| 2 | So this is tail-slot handling, not `print` and not branching. |

| # | directive |
|---|---|
| 1 | The machinery, all in `src/dnir_lower.zig`: |

- `lowerBlockReturns` walks the statements, marking one with
  `stmtIsTailSlot(block, i)`, and only afterwards calls
  `tryEmitTailDemandReturn` — and only `if (allow_return)`.
- `tryEmitTailDemandReturn:967` special-cases a void tail call:
  `if (isVoidTailCall(r.expr)) { _ = try lowerExprCons(ctx, r.expr, .discard); return false; }`
  Its comment cites gap[033]: emitting `ret` there made `if c print(" ") end`
  compile to `return print(" ")`, exit 59.
- `isVoidTailCall` matches the callee spelled exactly `print` — the one builtin
  whose voidness is unconditional.

| # | directive |
|---|---|
| 1 | **The suspected shape: the tail statement is skipped by the statement walk on the understanding that `tryEmitTailDemandReturn` will lower it, and for a branch body reached with `allow_return == false` that call never happens — so nothing lowers it.** Two paths, each correct alone, with a gap between them. |
| 2 | That is consistent with every observation: the branch runs (rc=7), non-tail prints survive, and the C backend — which has no such split — is correct. |

| # | directive |
|---|---|
| 1 | Not yet fixed: confirm which of the two paths drops it before editing. `print` is dispatched in `lowerExprCons` BEFORE the `discard` branch (`if (eql(callee,"print")) return lowerPrint(...)`), so `.discard` is not eliding the call and the gap[033] special case is likely innocent. |

| # | directive |
|---|---|
| 1 | Fix must keep gap[033] green: `if c print(" ") end` must not become `return print(" ")`. |
| 2 | That fixture is the regression test. |

## WIDER THAN `if` — `while` bodies drop their tail statement too

```duo
while n < 3        while n < 3
    print("A")         n += 1
    n += 1             print("B")
end                end

3x A  (correct)    NOTHING  (wrong)
```

| # | directive |
|---|---|
| 1 | Same discriminator, different construct: the statement in the block's TAIL SLOT is dropped. |
| 2 | So this is not the `if` arm — it is `lowerBlockReturns` / `stmtIsTailSlot`, which every block body goes through. |

| # | directive |
|---|---|
| 1 | `while` makes the defect worse than `if` does, because a loop whose last statement is its effect is an ordinary shape — accumulate-then-emit, read-then- advance, the print-at-end-of-iteration idiom. |
| 2 | Those loops run the right number of times and produce nothing. |

| # | directive |
|---|---|
| 1 | This also raises the priority of confirming which path drops the statement: whatever it is, it is reached by both the `if` arm and the `while` arm, so a fix aimed only at `.if_stmt` would leave `while` broken and look green on the `if` fixtures. |

## NARROWED AGAIN — only a tail VOID CALL drops. A tail assignment is fine.

```duo
while n < 3          while n < 3
    n += 1               n += 1
    t += 10              print("B")
end                  end
print(t)

30   CORRECT         NOTHING  WRONG
```

| # | directive |
|---|---|
| 1 | The loop runs (counting `n` to 3 and printing after the loop gives 3), and a tail ASSIGNMENT survives. |
| 2 | So the tail-slot machinery is working for ordinary statements and the discriminator is narrower than the previous section said: |

| # | directive |
|---|---|
| 1 | **a void-valued CALL in tail slot is dropped; everything else in tail slot is fine.** |

| # | directive |
|---|---|
| 1 | That points back at `tryEmitTailDemandReturn:967` — the `isVoidTailCall` branch, which matches callee spelled exactly `print` — and away from `lowerBlockReturns`/`stmtIsTailSlot`. |
| 2 | The section above is superseded on the mechanism while its `while` evidence stands: both constructs are affected, because both reach the same void-tail-call special case. |

| # | directive |
|---|---|
| 1 | The `.discard` reading was dismissed earlier in this file on the grounds that `print` is dispatched in `lowerExprCons` before the discard branch. **That dismissal is unverified and this measurement reopens it.** What to check: is `lowerPrint` actually reached under `.discard`, does it emit, and does the `return false` afterwards cause the caller to discard the emitted instructions rather than merely report "no return"? |

| # | directive |
|---|---|
| 1 | Fourth narrowing of this defect. |
| 2 | Each one was produced by changing exactly one thing in the fixture and re-running — constant to runtime, `if` to `while`, `print` to `+=`. |
| 3 | None came from reading the code. |

| # | directive |
|---|---|
| 1 | **Required controls for any fix**: `if` and `while`, each with a tail void call AND a tail assignment. |
| 2 | The assignment rows are what catch a repair that fixes the call by breaking the statement path. |

---

## RESOLVED 2026-08-09 — the tail slot of a NON-ANSWERING block was lowered by nobody

| # | directive |
|---|---|
| 1 | **Fix:** `src/dnir_lower.zig`, `lowerBlockTailEffect`, called from both `lowerBlock` and `lowerBlockReturns` when `allow_return == false`. **Gate:** `zig build tail-slot` (`scripts/tailslot.id`, fixtures in `examples/tailslot/`). |

### Which of the two paths dropped it, and how that was proved

| # | directive |
|---|---|
| 1 | Neither the `.if_stmt` arm nor the `isVoidTailCall` / `.discard` special case. **A block's final expression is not in `block.stmts` at all** — the parser puts it in `block.tail_expr` — and the only code in `dnir_lower` that ever read that field was `tryEmitTailDemandReturn`, which both block lowerers reach exclusively under `if (allow_return)`. |
| 2 | A branch body and a loop body are lowered with `allow_return = false`, correctly, because neither is the function's answer. |
| 3 | So the statement loop walked past the tail expression (it is not a statement) and the return path was never called (it is not an answer). |
| 4 | Two paths, each right on its own, with the tail expression falling between them. |

| # | directive |
|---|---|
| 1 | Proved by instrumenting the fall-through of `lowerBlockReturns` rather than by reading it — the same discipline the four withdrawn diagnoses lacked: |

```
G104 lowerBlockReturns allow_return=false stmts=1 tail_expr=true   -- the if
G104 lowerBlockReturns allow_return=false stmts=1 tail_expr=true   -- the while
```

| # | directive |
|---|---|
| 1 | One statement lowered, one tail expression present, `tryEmitTailDemandReturn` not called, function returns. |
| 2 | The instrumentation was removed in the same edit. |

### Every previous narrowing, reconciled — none of them was wrong about its data

| section | its datum | why the datum was true |
|---|---|---|
| "constant condition drops its branch" | `if 42 > 10 print("BIG")` printed nothing | the print was the branch's tail expression; the branch ran |
| "`print` inside an `if` dies" | non-tail `print` survived | a non-tail print is a `stmt` and rides the statement loop |
| "the TAIL statement, and only that one" | two prints, only the first | correct, and the closest of the four |
| "`while` bodies too" | `n += 1` then `print("B")` printed nothing | loop bodies are `allow_return = false` for the same reason branches are |
| "only a VOID CALL drops; a tail assignment is fine" | `t += 10` accumulated to 30 | **an assignment is a `stmt`.** It never occupied `tail_expr`, so it was never at risk — the discriminator was *expression vs statement*, which looks exactly like *void call vs assignment* because a bare call is the common expression to end a block with |

| # | directive |
|---|---|
| 1 | The last row is the one that would have sent a fix to the wrong place: it reads as evidence for the `isVoidTailCall` special case and is actually evidence for the parser's expression/statement split. `.discard` was innocent after all, and so was gap[033]'s guard. |

### The rule, stated generally

| # | directive |
|---|---|
| 1 | **A block's tail expression whose value nobody demands is a STATEMENT.** Not "a void call is an effect" — the callee's return type is not consulted and does not need to be, which is why this does not widen `isVoidTailCall` and does not need the declared-return-type work that section named as a blocker. |
| 2 | Demand is the discriminator, and demand is already known at the call site: `allow_return` IS the demand. |

| # | directive |
|---|---|
| 1 | The C backend, the oracle, already states the same rule — `emit_block` in `src/codegen.zig` switches on `tail_mode`, and `.statement` emits `expr;`. `.discard` consumption is the DNIR spelling of that semicolon. |
| 2 | A bare `.table` is skipped for the reason `lowerStmt`'s `.expr_stmt` arm skips it: `lowerExprCons` has no `.table` arm, and a table construction in a discarded position carries no effect, so dropping it is an answer rather than a second hole. |

### Measured by value — every fixture, both backends

```
                        direct                        --backend=c
branch.id   rc=0  first|last|                   rc=0  first|last|
loop.id     rc=0  b|b|b|3|                      rc=0  b|b|b|3|
arms.id     rc=0  then|done|else|done|4|        rc=0  then|done|else|done|4|
answer.id   rc=7  ` `|30|                       rc=7  ` `|30|
```

| # | directive |
|---|---|
| 1 | and the same four fixtures against the UNFIXED lowering, which is what makes them a regression test rather than four programs that happen to pass: |

```
branch.id   first|                 the tail print, gone
loop.id     3|                     three tail prints, gone
arms.id     done|done|             BOTH arms and the trailing print, gone
answer.id   ` `|30|  rc=7          unchanged — it is the negative control
```

| # | directive |
|---|---|
| 1 | `arms.id` carries a note about why its `if/else` has a trailing `print("done")`: without it the arms ARE the function's tail, go through `tryEmitTailDemandReturn`, and print correctly even on the unfixed compiler. |
| 2 | The fixture would have been green against the bug. |

| # | directive |
|---|---|
| 1 | `answer.id` is the three-part negative control in one program: gap[033]'s `if c print(" ") end` as the function's answer (exit 0 and one space, not the 59/224 register leak), a taken branch that still returns 7, and a tail compound assignment still summing 30. |

| # | directive |
|---|---|
| 1 | Two more shapes measured outside the fixture set and agreeing across backends: a numeric `for` body with a tail print (`1\|2\|3\|6\|`) and an `if` nested inside a `while` body (`3\|4\|end\|`). |

### GAP-102 is NOT closed by this

| # | directive |
|---|---|
| 1 | Checked directly, because the two were plausibly the same machinery and are not. `examples/pass7/explain_smoke.id` still fails identically: |

```
explain_smoke.id:9:15: error: return type mismatch: expected 'i64', got 'Point'
```

| # | directive |
|---|---|
| 1 | GAP-102 lives in `tail_result_demand.blockTailResultWithDemand` — transparent- trailer stripping walks back off the real tail and anchors on the preceding binding — and it is a SEMA verdict reached before any lowering runs. |
| 2 | This gap was in the lowering of a tail expression the resolver had already identified correctly. `agent-smoke` therefore stays red at exactly its baseline reason (`explain: FAIL`, two rows) and at no other. |

### Gates, exit codes, 2026-08-09

```
zig build                 0
zig build unit-test       1    1162 pass / 1 fail — baseline, the self-contradictory
                               `relation.test.an eq family composes independently of to`
zig build native-census   0    native 127 (baseline 123 + this gap's 4 fixtures, each
                               lowering native; control run with the fixtures moved
                               aside reproduces 123 exactly — no regression)
zig build tail-slot       0    8 rows, and the failure path positive-controlled
zig build agent-smoke     1    baseline red: `explain: FAIL` = GAP-102, nothing else
zig build language-census 0    duo 893 · sh/py 0 · js 2 · zig 115
zig build repo-hygiene    0
```
