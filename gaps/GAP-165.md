# GAP-165 — PROJECTION-ONE graph census and enforcement

**Status:** OPEN · **Filed:** 2026-08-12
**Priority:** P0 · **Kind:** architecture
**Owner:** Cursor (coordination / canonicality)

## Ruling

C0 §67 `law.projection.one` reconciles conversion, protocol, home, world, shell,
std/lib, and staging into **one projection algebra**. Text gates ratchet; graph
census owns verdicts when GAP-124 closes.

## Acceptance

1. `scripts/projection_census.id` and `scripts/infer_census.id` report projection debt classes (gatecap ingress, no `std.script`).
2. `law.gate.projection` controls 1–30 execute on staged diff via graph.
3. `docs/spec/world.md` teaches WORLD-ONE not monolithic os/io namespaces. **Done (2026-08-12).**
4. `examples/conversion/relation.id` teaches subject-first `to` not `from` (law.from.zero). **Done (2026-08-12).**
5. New canonical `std.*`, `lib.*`, `:from(`, `process.*` = 0 on added lines (text ratchet via `gates/admission.id` + idiomgate).

## Debt classes (census)

- explicit / inferred / redundant explicit projections
- `:from(` inverse faces
- `std.*` / `lib.*` canonical source lookups
- `process.*` namespace calls
- protocol adjective identities
- projection reconstruction in DNIR/codegen string compares

## Blocked on

GAP-124 resolver graph application records with projection pack facts.

## Step 4 progress

- `docs/spec/application-record.md` — resolver→graph application record contract
- `lib/semantic/application.id` — bootstrap projection pack schema and samples
- `lib/semantic/producer.id` — projection pack / explicit / inferred producer ownership

Resolver minting live records remains open (Codex lane).

Debt classification: `docs/spec/projection-debt.md` (live census + repair classes).
