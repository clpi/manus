| field | value |
|---|---|
| title | GAP-165 — PROJECTION-ONE graph census and enforcement |
| status | OPEN |
| filed | 2026-08-12 |
| priority | P0 |
| kind | architecture |
| owner | Cursor (coordination / canonicality) |

| section |
|---|---|
| Ruling |

| # | directive |
|---|---|
| 1 | C0 §67 `law.projection.one` reconciles conversion, protocol, home, world, shell, std/lib, and staging into **one projection algebra**. |
| 2 | Text gates ratchet; graph census owns verdicts when GAP-124 closes. |

| section |
|---|---|
| Acceptance |

| # | directive |
|---|---|
| 1 | `scripts/census/projection.id` and `scripts/census/infer.id` report projection debt classes (gatecap ingress, no `std.script`). |
| 2 | `law.gate.projection` controls 1–30 execute on staged diff via graph. |
| 3 | `docs/spec/world.md` teaches WORLD-ONE not monolithic os/io namespaces. **Done (2026-08-12).** |
| 4 | `examples/conversion/relation.id` teaches subject-first `to` not `from` (law.from.zero). **Done (2026-08-12).** |
| 5 | New canonical `std.*`, `lib.*`, `:from(`, `process.*` = 0 on added lines (text ratchet via `gates/admission.id` + idiomgate). |

| section |
|---|---|
| Debt classes (census) |

| # | directive |
|---|---|
| 1 | explicit / inferred / redundant explicit projections |
| 2 | `:from(` inverse faces |
| 3 | `std.*` / `lib.*` canonical source lookups |
| 4 | `process.*` namespace calls |
| 5 | protocol adjective identities |
| 6 | projection reconstruction in DNIR/codegen string compares |

| section |
|---|---|
| Blocked on |

| # | directive |
|---|---|
| 1 | GAP-124 resolver graph application records with projection pack facts. |

| section |
|---|---|
| Step 4 progress |

| # | directive |
|---|---|
| 1 | `docs/spec/application-record.md` — resolver→graph application record contract |
| 2 | `lib/semantic/*` relation catalogs deleted (`law.catalog.zero` / `law.boolean.mirror.zero`) |

| # | directive |
|---|---|
| 1 | Resolver minting live records remains open (Codex lane). |

| # | directive |
|---|---|
| 1 | Debt classification: `docs/spec/projection-debt.md` (live census + repair classes). |
