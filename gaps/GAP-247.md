| field | value |
|---|---|
| title | GAP-247 — nest: -44.04% loss vs clang; doubly-nested counter loop 1.44x slower |
| status | OPEN |
| filed | 2026-09-12 |
| priority | P1 |
| kind | performance_gap |
| files | bench/programs/nest.id, bench/programs/nest.c, lib/compiler/native.id |

| section |
|---|---|
| Measured loss |

| # | directive |
|---|---|
| 1 | v6 baseline (2026-09-12T00:11:52, commit ae5532b2, 21 interleaved rounds, 3 warmup, median primary): |
| 2 | idol median 0.003887s vs clang median 0.002698s (gcc 0.002757s): loss, margin -44.04%, p=0.0000 (significant). |
| 3 | Second-worst loss in the corpus. Tracked in bench/RESULTS.md history section. |

| section |
|---|---|
| Reproducer |

| # | directive |
|---|---|
| 1 | bench/programs/nest.id (idol native) vs bench/programs/nest.c (clang/gcc -O3); correctness gate passes, exit codes agree. |

```text
t = 0
a = 0
while a < 2000
  b = 0
  while b < 2000
    t = t + 1
    b = b + 1
  a = a + 1
t
```

| section |
|---|---|
| Why this should not lose |

| # | directive |
|---|---|
| 1 | Both loops are countable with constant bounds; the nest optimization (bound registers) exists in the verify suite's opt list. |
| 2 | clang collapses this to near a single multiply-add per inner iteration; idol's per-iteration overhead (bound compare, branch, increment) is measurably higher. |
| 3 | Suspect: inner-loop bound reload per iteration, or the countdown fast path emitting a heavier epilogue than clang's. |

| section |
|---|---|
| Suggested next step |

| # | directive |
|---|---|
| 1 | Compare nativebench disassembly of the inner loop against clang -O3 -S instruction counts. |
| 2 | Check whether the nest bound-register optimization fires; if it does, the residual gap is pure codegen density. |
