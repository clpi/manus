# GAP-117 — Canonical callable result-demand face requires deleted end token

**Status:** OPEN · **Filed:** !2026-08-09T20:08:02Z
**Priority:** P0 · **Kind:** expressiveness_gap
**Filed by:** codex_semantic_normalization
**Files:** src/parser.zig,docs/spec/constitution.duo,scripts/semanticgate.duo

Live proof on 2026-08-09: `budget: i64 = (fallback: i64)` followed by an offside body fails at EOF with `expected 'end'`. C0 and CLAUDE require result demand on the binding and delete `end`; the current parser therefore cannot express the canonical callable form. Do not work around this by teaching new code the legacy `budget = (...): i64` suffix. Parser is presently claimed by codex_interpolation_index_fix, so this gate records the capability gap without editing that owned surface.
