# GAP-123 — Agent law still mandates std.script-backed lock wrapper

**Status:** CLOSED · **Filed:** !2026-08-09T21:08:55Z
**Priority:** P0 · **Kind:** agent
**Filed by:** codex_shc_priority
**Files:** AGENTS.md,scripts/duo_lock.id,lib/script.id

## CLOSED — verified 2026-08-23 at `dec7d509`

| # | directive |
|---|---|
| 1 | **Verdict:** CLOSED. **Superseded by:** `ace5f7d5` "Purge retired pre-rename identity and reconcile superseded debt" (deleted `scripts/duo_lock.id`) and `97753361` "Converge shared coordination authorities" (AGENTS coordination item 4 now names `tools/node/dev/idol-lock` and explicitly forbids teaching a `std.script` or MCP text wrapper as canonical authority). |

| # | directive |
|---|---|
| 1 | **Pinned, so it cannot silently return:** |

| # | directive |
|---|---|
| 1 | git ls-files scripts/duo_lock.id -> no output tools/node/dev/census/convergence -> "census/convergence: pass duo_lock retired" |

| # | directive |
|---|---|
| 1 | Both were run at `dec7d509`. |
| 2 | The census check is the durable half: it FAILS if the file reappears, so this closure is a ratchet and not an assertion. |

| # | directive |
|---|---|
| 1 | Commit 534942d purged stale architecture but AGENTS coordination item 4 still requires scripts/duo_lock.id, whose implementation imports std.script. |
| 2 | This directly contradicts owner correction and law.std.zero. |
| 3 | Agent entrypoint continues training every session on forbidden execution authority. |
