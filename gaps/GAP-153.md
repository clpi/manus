| field | value |
|---|---|
| title | GAP-153 — Layout resolution; projection scope package capability closure |
| status | OPEN |
| filed | !2026-08-12T03:45:00Z |
| priority | P0 |
| kind | vocabulary |
| owner | Codex (semantic graph / resolution) |
| files | docs/spec/constitution.md,docs/spec/source.md,src/sema.zig,src/semantic_graph.zig,lib/**,gate/idiom.id,gate/path.id |

| section |
|---|---|
| Ruling |

| # | directive |
|---|---|
| 1 | Canonical **import is deleted**. |
| 2 | Do not add or restore `import`, `module`, `package`, `namespace`, `include`, `require`, `req`, `use`, `using`, `inject`, `admit`, `open`, `bring`, `provide`, `register`, `install`, `mount`, or `expose` as semantic permission, visibility admission, or loader mechanisms. |
| 3 | Bootstrap `req("std.*")` and historical corpus uses are migration debt only. |

| # | directive |
|---|---|
| 1 | Native reachability is **projection** over ordinary values, tables/homes, bindings, scope facts, and anchored roots — not admission syntax in source. |
| 2 | There is no operation meaning “make this already-known thing visible here.” Change scope facts at the owner boundary instead. |

| # | directive |
|---|---|
| 1 | Do not create privileged module edges: `filebind`, `modulebind`, `packagebind`, or `sourcebind`. |

| # | directive |
|---|---|
| 1 | A file is not a module construct. |
| 2 | A file contributes an ordinary table into an ordinary lexical/table graph. |
| 3 | The filesystem is provenance and discovery; it must not manufacture a second semantic abstraction. |

| # | directive |
|---|---|
| 1 | See `docs/spec/source.md` for the full projection / scope / package / capability closure. |

| section |
|---|---|
| Three orthogonal facts |

| fact | answers |
|---|---|
| home | where does this meaning live? |
| reach | can this scope resolve this meaning? |
| world | what authority can this application exercise? |

| # | directive |
|---|---|
| 1 | **Package/table ≠ world.** A directory is an anchored home, not an authority world. |
| 2 | Possession of a table never implies possession of a world. |

| section |
|---|---|
| Source layout → table topology |

| # | directive |
|---|---|

```text
compiler/
    lexer.id
    parser.id
    graph.id
```

| # | directive |
|---|---|
| 1 | the compiler derives (nobody writes the wrapper): |

```text
compiler.lexer
compiler.parser
compiler.graph
```

| # | directive |
|---|---|
| 1 | Inside `compiler/parser.id`, sibling resolution is direct: |

```id
tokens = lexer:scan(source)
g = graph:new()
```

| # | directive |
|---|---|
| 1 | No dependency syntax. |
| 2 | The **reference is the dependency fact**. |

| # | directive |
|---|---|
| 1 | Same-directory references require nothing: if `a.id` and `b.id` share a home, `b:foo()` resolves as a sibling binding — statically, graph-native, with incremental invalidation from actual semantic references. |

| # | directive |
|---|---|
| 1 | Cross-directory visibility uses **anchored root tables** provided by build/project configuration (an ordinary binding such as `codec`), not per-file import declarations. |
| 2 | Unused dependency elimination follows from the reference graph. |

| section |
|---|---|
| Resolution ladder (shape) |

```text
local bindings
→ enclosing table/home
→ enclosing home ancestors
→ explicitly projected bindings
→ provided root anchors
→ canonical vocabulary
→ failure (ambiguity diagnosed, not order-selected)
```

| # | directive |
|---|---|
| 1 | Exact canonical order is implementation work; architecture is fixed. |

| section |
|---|---|
| Implementation invariant for agents |

| # | directive |
|---|---|
| 1 | **Never load a file. |
| 2 | Resolve a table.** |

| # | directive |
|---|---|
| 1 | Files and directories are source provenance for ordinary semantic tables. |
| 2 | A reference to a sibling or anchored table is the dependency edge. |
| 3 | No req, require, import, module registry, runtime namespace table, package loader, or admission syntax participates in native semantics. |
| 4 | Worlds remain authority values resolved by existing world law, but possession of a table never implies possession of a world. |

| # | directive |
|---|---|
| 1 | Agents must not introduce host-shaped concepts (`Module`, `PackageLoader`, `StdTable`, `WorldRegistry`, …) when graph facts suffice. |

| section |
|---|---|
| Acceptance |

| # | directive |
|---|---|
| 1 | Constitution, `docs/spec/source.md`, and operative projections state the model above without reintroducing import-as-permission or admission-as-visibility (aligns with `law.authority.grant`, `law.distribution`, `law.layout`, `law.projection`). |
| 2 | New canonical `.id` adds no `req` / `require` / `import` / `using` / `use(` / `inject` / `admit` / `module` / `namespace` / `include` / privileged `*bind` loader or admission surface. `gate/path.id` is executable; `gate/idiom.id` states the lexical law but direct execution is currently blocked by DNB001 `concat`, so static findings are migration pressure rather than closure. |
| 3 | Compiler resolution path derives home topology from source layout and records reference-induced dependency edges without a separate import graph. |
| 4 | Bootstrap `req("std.*")` count ratchets down as prelude graph facts replace loader calls. |

| section |
|---|---|
| Disjoint debt |

| # | directive |
|---|---|
| 1 | Embedded @c governance uses separate single-word gate roles — **floor** (unclassified foreign files in `foreign_census`), **advertise** (aggregate @c count), **audit** (per-file sites in `embedledger`), **enforce** (zero-site gate) — not compound labels like `embedfloor` or `edge audit`. |
