# GAP-212 — most cited gate authority does not exist

**Status:** OPEN · **Filed:** !2026-08-22T02:05:00Z
**Priority:** P0 · **Kind:** evidence_gap
**Filed by:** claude-gap145
**Files:** AGENTS.md,docs,src,gate

GAP-206 recorded that `gate/control.sh`, cited three times by `region.zig` as
its authority, has never existed in any commit. **It is not one file.**

## The number is now produced by a runnable artifact

`sh gate/all.sh` counts it on every run rather than asserting it, so it cannot
rot the way `AGENTS.md:60`'s figure did:

```
gate/all.sh: ran 21 gate(s): 17 passed, 4 failed
gate/all.sh: FAILED: gate/corpus-status.sh gate/defaults.sh gate/researchgap.sh gate/world-launch.sh
gate/all.sh: citation debt: 81 of 133 gate citations name 27 gate(s) with no commit in any history
```

**`gate/all.sh` itself is closed** — it existed only as an instruction in
`AGENTS.md` (*"`sh gate/all.sh` runs the lot"*, line 53, plus a finding
attributed to it at line 60) and now runs. That is why the count moved from 88
to 81: seven citations stopped being ghosts. **27 remain.**

Four gates that *do* exist fail: `corpus-status`, `defaults`, `researchgap`,
`world-launch`. They were failing before; they were simply never run together.

## Measured at `4cce7c93`, before `gate/all.sh` existed

Every `gate/*.sh` path cited in `src/`, `gate/`, `docs/`, `AGENTS.md` and
`CLAUDE.md`, each checked against `git log --all -- <path>`:

```
total citations                126
distinct gates cited            40
citations to gates with ZERO commits in all history   88   across 28 gates
```

**70% of cited gate authority is prose.**

| ghost gate | citations |
| --- | --- |
| `gate/recon.sh` | 10 |
| `gate/selfhost.sh` | 8 |
| `gate/fmt.sh` | 8 |
| ~~`gate/all.sh`~~ | ~~7~~ — **written, closed** |
| `gate/obseq.sh` | 6 |
| `gate/negative.sh` | 5 |
| **`gate/narrow.sh`** | **4** |
| `gate/dialect.sh` | 4 |
| `gate/design.sh` | 4 |
| `gate/simd.sh`, `gate/observation.sh`, `gate/control.sh`, `gate/collection.sh` | 3 each |
| `gate/roster.sh`, `gate/place.sh`, `gate/edge.sh`, `gate/claim.sh`, `gate/arrival.sh` | 2 each |
| 11 more | 1 each |

## The three that matter most

**`gate/all.sh` — `AGENTS.md` gives it to the reader as a command to run.**
A new agent following the bootstrap document runs a file that has never
existed. `AGENTS.md`'s own section *"Numbers live in exactly one place, and
that place runs"* is violated by `AGENTS.md`.

**`gate/narrow.sh` is cited as the authority for a live production refusal**
in `dnir_lower.zig` — *"Deleting this check admits 36 oracle rows that answer
wrong."* The 36 rows have no reproducible artifact.

**`gate/control.sh`** is cited by `region.zig` three times including
*"gate/control.sh measures the hole this closes, and the measurement is the
reason"*, with a digest `ebede795efad` that survives only as prose.

## Why this is P0 and not tidying

A citation to a non-existent gate is indistinguishable, to a reader, from a
citation to a passing one. Every refusal, invariant and design decision resting
on one of these 88 citations is unfalsifiable: there is no artifact to run, no
number to reproduce, and no way to learn that the claim was never measured.
This session filed four wrong answers (GAP-207 through GAP-210) that every
*existing* instrument missed; a ghost instrument cannot even miss them.

## Acceptance

Each of the 28 either
1. gets written and wired, with its cited number reproduced; or
2. has the claim it supports re-measured and the citation replaced by the
   artifact that actually establishes it; or
3. has the citation deleted and the claim marked unproven.

`gate/all.sh` is first, because `AGENTS.md` instructs every new agent to run it.
