| field | value |
|---|---|
| title | GAP-094 - single-hole interpolation preserves an unnecessary concat realization |
| status | CLOSED |
| filed | 2026-08-09 |
| reopened | 2026-08-10 |
| closed | 2026-08-25 |
| priority | P1 |
| owner | realization and generated-C projection |

| section |
|---|---|
| Current boundary |

| # | directive |
|---|---|
| 1 | Single-hole interpolation now preserves text semantics, but the parser represents the conversion as concatenation with an empty text value. |
| 2 | That source-shaped realization is not the semantic relation and must not force physical concat work. |

| # | directive |
|---|---|
| 1 | At `335af08` with the existing shared dirty tree, a temporary canonical `.id` probe returned the same value on both backends. |
| 2 | Machine inspection did not agree on work: |

| Path | Result | Physical work for one integer hole |
| --- | --- | --- |
| direct native | `7` | one `malloc`; no `strlen` or `memcpy` in `main` |
| generated C | `7` | two `malloc` calls plus `strlen` and `memcpy` in `main` |

| # | directive |
|---|---|
| 1 | `dump-c` exposes the second allocation directly: the integer-to-text result is passed through an empty-text concat. |
| 2 | The optimized C executable retains both allocations, so this is not only an intermediate C artifact. |

| section |
|---|---|
| Required closure |

| # | directive |
|---|---|
| 1 | Preserve interpolation as one semantic conversion/application with exact source provenance. |
| 2 | Realization may concatenate when literal or additional dynamic segments demand it. |
| 3 | A single dynamic segment must not materialize an empty segment or a concat operation. |

| # | directive |
|---|---|
| 1 | Closure requires: |

| # | directive |
|---|---|
| 1 | equal output on direct-native and generated-C paths; |
| 2 | one conversion allocation or a cheaper lawful realization on both paths; |
| 3 | no empty concat in the generated C or direct machine lineage; |
| 4 | the original interpolation application and value lineage remain queryable; |
| 5 | no parser-local special case becomes semantic authority. |

| # | directive |
|---|---|
| 1 | The parser and codegen files are concurrently owned. |
| 2 | Do not reconstruct the missing semantic conversion in another backend. |
| 3 | The owner boundary is the source-recognition to graph/application projection; realization must consume that exact fact. |

| section |
|---|---|
| Closure (2026-08-25, tree `fc48acea` + this change) |

| # | directive |
|---|---|
| 1 | Reproduced live at `fc48acea` before the change: `i = 7 ; a: str = "{i}" ; print(a)` through `dump-c` emitted `const char* a = duo_str_concat("", duo_str_from_i64((int64_t)(i)));`, and the `cc -O2` executable's `main` carried two `malloc` calls plus `strlen` — the filed measurement exactly. |

| # | directive |
|---|---|
| 1 | The repair lives at the C-projection owner named above, in `src/codegen.zig` (`concat_operand_is_empty_text` beside the two concat emission arms). |
| 2 | The parser's single-hole seed (`"" .. i`) is a SOURCE-shaped realization; the C projection now consumes the exact graph fact — a text-literal operand of length zero — and elides the concat, realizing the value as the hole's display conversion alone. |
| 3 | This is `law.observation.minimum` / INTERMEDIATE-ZERO: the empty text is the identity operand of `..` on the one observation the value makes; the witness (no effect dropped, no evaluation-order change, byte-identical result through the same display conversion, allocation identity not a semantic observation for immutable text) is written on the helper. |
| 4 | The BYTE face `''` is excluded — the witness speaks only for text — and the boxed `lua_concat` path is excluded because under Lua law `"" .. x` errors on nil/table and stringifies, so the equivalence is witnessed only on the full-native arm. |
| 5 | The parser is untouched: no parser-local special case, and the interpolation application and its value lineage in the AST/graph are unchanged — the elision is emission-only. |

| # | directive |
|---|---|
| 1 | Measured after the change, same probe, same tree: |

| # | directive |
|---|---|
| 1 | generated C: `const char* a = duo_str_from_i64((int64_t)(i));` — no `duo_str_concat(""` anywhere in the artifact; `cc -O2` executable prints exactly bytes `37 0a` (`7\n`), exit 0, and `main` carries ONE `malloc`, no `strlen`, no `memcpy`. |
| 2 | positive control `b: str = "x{i}"`: emits `duo_str_concat("x", duo_str_from_i64((int64_t)(i)))` and prints exactly bytes `78 37 0a` (`x7\n`), exit 0 — a genuine two-part interpolation still concatenates and answers right. |
| 3 | pinned by the unit test `codegen: gap[094] single-hole interpolation realizes as the conversion, not an empty concat` in `src/codegen.zig`: value line, artifact-shape assertion that no empty-seeded concat survives, and the two-part positive control. |

| # | directive |
|---|---|
| 1 | The direct-native leg cannot be re-measured on this host: the direct backend refuses x86_64-linux with DNB004, so the 335af08 direct measurement (one `malloc`, no empty concat in the direct lineage) stands as the direct-path evidence — the direct lineage never carried the empty concat, which is why this gap's owner is the generated-C projection. |
| 2 | The repair does not touch admission, `dnir_lower.zig`, or the direct backend, so the separately recorded DNB001 `concat` face (which blocks `gate/idiom.id` direct execution) is unaffected in both directions. |

| # | directive |
|---|---|
| 1 | Validation on this tree: `zig build` green through the lock; unit aggregate 1766 passed / 62 skipped / 0 failed; `gate/gap-145-consumer.sh` PASS (the helper is a quote-observing switch arm, `.quoted` tag-test surface unchanged at 88); `sh gate/realize/census.sh` identical before and after — 243 subjects, 54 refused by check, 13 realized, 176 refused by the realizer. |
| 2 | The remaining red build steps are the known host-conditional set (DNB004 direct refusals, MCP infrastructure), unchanged by this repair. |
| 3 | No gate files were touched. |

| section |
|---|---|
| Evidence |

| # | directive |
|---|---|
| 1 | Focused runs, serialized through the repository lock: |

| # | directive |
|---|---|
| 1 | C execution: exit 0, output `7`. |
| 2 | direct-native execution: exit 0, output `7`. |
| 3 | direct-native compile trace: `direct-proof-artifact`, native link, exit 0. |
| 4 | generated C: two allocation sites survive optimization. |
| 5 | direct-native disassembly: one allocation site in `main`. |

| # | directive |
|---|---|
| 1 | This evidence exercises native direct and generated-C paths only. |
| 2 | It is not Wasm evidence, an aggregate pass, or compiler-B ownership evidence. |

| # | directive |
|---|---|
| 1 | After the 2026-08-25 closure the generated-C row reads: ONE allocation site in `main`, no `strlen`, no `memcpy`, output bytes `37 0a`, exit 0; the two-part control keeps its concat and answers `78 37 0a`, exit 0. |
