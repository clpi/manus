| field | value |
|---|---|
| title | GAP-174 — a multi-statement `if`/`else` branch loses its value in every non-direct realization |
| status | OPEN |
| filed | !2026-08-15 |
| priority | P0 |
| kind | correctness |
| found by | GAP-173 re-measurement, -vs- then -vs- corpus differential |
| files | src/wasm_backend.zig,src/codegen.zig,density/sieve.id (idol-native),density/perf/sieve.id (idol-native) |

| section |
|---|---|
| Obligation |

| # | directive |
|---|---|
| 1 | An `if`/`else` used as a VALUE loses that value whenever the branch that decides the answer holds MORE THAN ONE STATEMENT. |
| 2 | It compiles, it runs, it prints `ok compile`, and the function answers `0` — or, worse, the OTHER branch's constant. |
| 3 | No diagnostic. `law.fallback.zero`. |

| # | directive |
|---|---|
| 1 | **The direct backend is CORRECT. |
| 2 | Every other realization measured is wrong**, and the two wrong ones are wrong identically, row for row, which is what says the defect is upstream of both emitters rather than a coincidence in either. |

| # | directive |
|---|---|
| 1 | --backend direct CORRECT (and this is what plain `idol run` selects) --backend wasm WRONG LIVE at idol 24b6c21c --backend c WRONG measured live at idol 9b5d54d5; the spelling was RETIRED mid-measurement (docs/rulings.md, "NO C BACKEND, PERIOD"), so this column is now unreachable from the CLI — the defect was DELETED OUT OF REACH, never fixed |

| section |
|---|---|
| Minimal reproduction (a three-line function body) |

```
f: i64 = (n: i64)
    if n < 2
        0
    else
        answer = 7
        answer

main: i64 = ()
    f(11)
```

| # | directive |
|---|---|
| 1 | idol run --backend direct -> exit 7 CORRECT idol compile --backend wasm --target wasm32-wasi ; wasmtime -> exit 0 WRONG idol run --backend c (at 9b5d54d5) -> exit 0 WRONG |

| # | directive |
|---|---|
| 1 | `idol check` is clean. |
| 2 | Measured on the vendored `idol-native/bin/idol` at HEAD `24b6c21c` and on a clean isolated build of `9b5d54d5`, with a fresh build-cache key both times. |

| section |
|---|---|
| The boundary, measured |

| # | directive |
|---|---|
| 1 | `direct` is the CORRECT column throughout; `wasm` is the current wrong one and answered identically to the retired `c` on every row. |

| body of the branch that decides the answer | direct | wasm | |
| --- | ---: | ---: | --- |
| `7` — one expression | 7 | 7 | ok |
| `7 + 0` — one expression, compound | 7 | 7 | ok |
| `answer = 7` then `answer` | 7 | **0** | WRONG |
| `answer = 7` then `answer + 0` | 7 | **0** | WRONG |
| `a = 1`, `b = 6`, then `a + b` | 7 | **0** | WRONG |
| the multi-statement arm is the `if`, not the `else` | 7 | **0** | WRONG |
| `answer = 7` then `return answer` | 7 | 7 | ok |
| early `return` then a fall-through tail, no `else` | 7 | 7 | ok |

| # | directive |
|---|---|
| 1 | So it is not about `else`, not about the tail being a bare name, and not about how many bindings precede it: **one statement before the tail is enough, in either arm.** An explicit `return` inside the branch avoids it entirely, which is why so much of the corpus never met it. |

| # | directive |
|---|---|
| 1 | **The failure is not uniformly zero, and anyone grepping for `0` will miss half of it.** With the other branch taken, the same function returns the value of the branch that was NOT taken: |

```
f: i64 = (n: i64)
    if n > 0
        7
    else
        answer = 9
        answer
```

| # | directive |
|---|---|
| 1 | f(-1) direct -> 9 CORRECT f(-1) wasm -> 7 WRONG — the arm that did not run |

| section |
|---|---|
| Blast radius as measured |

| # | directive |
|---|---|
| 1 | Differenced `--backend direct` against `--backend wasm` (run under `wasmtime`) over every `.id` under `idol-native` (maxdepth 2) and `idol/examples` (maxdepth 2) — 526 files, `examples/boring/roman.id` excluded as nondeterministic by a separate defect. |
| 2 | 179 files were accepted by BOTH realizations; **34 of those 179 disagree**, 13 of them on stdout. |

| # | directive |
|---|---|
| 1 | The headline is a benchmark row, and its truth was computed outside both compilers: |

| # | directive |
|---|---|
| 1 | idol-native/density/sieve.id wasm: count=2 last=3 direct: count=2262 last=19997 truth: count=2262 last=19997 |

| # | directive |
|---|---|
| 1 | 2262 is π(20000) and 19997 is the largest prime below it, recomputed in Python from the file's own algorithm. `prime`'s deciding arm is multi-statement, so `answer` never leaves the branch. |
| 2 | Under the retired C backend the same file said `count=9998` — at `limit = 30` it printed `p 9 p 15 p 21 p 25 p 27` beside the real primes, i.e. **it called every odd number prime**; under wasm the same defect lands on the other side and it finds two. |
| 3 | Same cause, opposite direction, both silent. `density/perf/sieve.id` is the same program at `limit = 200000`. |

| # | directive |
|---|---|
| 1 | **A density/perf benchmark row has been measurable through a realization that computes the wrong answer.** `HPLS.md` §68 in its sharpest form: a workload whose verification observer never ran. |

| # | directive |
|---|---|
| 1 | The other 11 stdout divergences are NOT this defect and are listed so nobody re-derives them: 2 are the `known_divergent/` residents already recorded there |
| 2 | 4 are `examples/shc/*` world access (`cwd`, `env`, `arg`, `path`) which WASI does not have and which should refuse rather than answer `nil`; `native_differential/io_read_path.id` and `seed_test.id` trap under wasm (exit 134); and `examples/table/div/sign.id`, `examples/table/divsign.id` and `examples/cbackend/modsign.id` are the floored-modulo ruling, which has landed in `direct` — it now matches those files' `# expect:` lines — and has NOT landed in wasm. |
| 3 | That last group belongs to the modulo lane, not here, but it is the same shape of finding: **one law, two realizations, and only one of them was migrated.** |

| # | directive |
|---|---|
| 1 | Of the 21 exit-status-only divergences, 6 are the file-scope-tail convention, 3 are gates whose count is lower under wasm (`gate/numeric.id` 53 vs 32, `gate/width.id` 90 vs 89, `gate/match.id` 17 vs 15 — the same one-law-two-realizations shape), and the remaining 12 exit 1 under `wasmtime`, which is a WASI entry-point question and not evidence of a miscompile. |

| section |
|---|---|
| Why nothing saw it |

| # | directive |
|---|---|
| 1 | `--backend auto`, which is what plain `idol run` means, resolves to the direct backend and REFUSES rather than falling back. Measured on `examples/anchor/bare.id`: `auto` and `direct` emit the identical `DNB011`, and at 9b5d54d5 only an explicit `--backend c` compiled it. So the everyday path never reaches the broken lowering, and `density/sieve.id` is correct under `idol run`. **The defect is only reachable by asking for a second realization — which is exactly what nobody does except a differential.** |
| 2 | The direct backend refused 339 of 521 corpus files at 9b5d54d5 and 339 of 526 at HEAD (187 accepted; the wasm backend accepts 343, so each realization compiles a DIFFERENT subset and the intersection where a differential is even possible is 179 files). For those there is no second column at all, so this defect could be live in one of them with nothing to detect it. That number is UNKNOWN and nothing in this tree measures it. |
| 3 | The retirement of `--backend c` removed one of the only two instruments that could see this class. **`gate/expect.sh`'s entire design — "the one detector in the tree that can convict BOTH backends at once" — was built on a two-backend differential that no longer has two backends.** It still shells out to `compile --backend=c` for one of its two columns, so at HEAD that column is uniformly `dead`. MEASURED on `idol-native`, same command, same day: |

| # | directive |
|---|---|
| 1 | before the re-vendor expect gate: 11 verdict(s) moved over 120 file(s) after the re-vendor expect gate: 109 verdict(s) moved over 123 file(s) |

| # | directive |
|---|---|
| 1 | **108 of the 109 are `[c]` rows**, and not one of them is about a program. |
| 2 | A gate that is 90% red for a reason unrelated to a program's correctness is a gate that will be ignored, which is the state a defect needs to survive in. |
| 3 | Repointing that column at `--backend wasm` restores the instrument, re-pins the ledger against something that exists, and would have caught this defect with nobody looking for it — the two sieves are already in that corpus. |
| 4 | (The three GAP-173 fixtures are pinned `pass pass`, measured on a compiler that still had both realizations, deliberately: pinning them `dead` would have made them the only three rows re-pinned and hidden them inside the wave of 105.) |

| section |
|---|---|
| Acceptance |

| # | directive |
|---|---|
| 1 | The reproduction above answers 7 under `--backend wasm`, and all five WRONG rows of the table agree with their `direct` column. |
| 2 | `density/sieve.id` prints `count=2262 / last=19997` under `--backend wasm`. |
| 3 | Root-caused in the lowering the wasm and C emitters SHARE — the identical row-for-row signature says it is not in either emitter — and not by rewriting the corpus to add `return`: "the current compiler cannot do X" must never become "Idol should not permit X". |
| 4 | A fixture with an `# expect:` line graded under every surviving realization, so a re-regression is a moved verdict and not a silent one. |

| section |
|---|---|
| Measured land |

| # | directive |
|---|---|
| 1 | **Test fixtures added in commit d3ca3aaa** (`src/wasm_backend.zig`): |

| # | directive |
|---|---|
| 1 | `runTestSourceWasm` helper function that compiles and runs Idol source through the wasm backend |
| 2 | Test case `"wasm backend preserves values from multi-statement conditional arms"` covering all five WRONG rows from the boundary table: |
| 3 | ↳ `answer = 7` then `answer` (basic multi-statement else arm) |
| 4 | ↳ `answer = 7` then `answer + 0` (compound expression tail) |
| 5 | ↳ `a = 1`, `b = 6`, then `a + b` (multiple bindings before tail) |
| 6 | ↳ Multi-statement in the `if` arm instead of `else` |
| 7 | ↳ Reverse case showing the other arm can also be wrong |
| 8 | Test case `"wasm backend density sieve observes its multi-statement prime result"` covering the actual `density/sieve.id` bug pattern |

| # | directive |
|---|---|
| 1 | These fixtures name the observations the surviving realization must satisfy; their presence alone is not evidence that the branch result is preserved. |

| section |
|---|---|
| Open face at `832c089a` |

| # | directive |
|---|---|
| 1 | Commit `9e59d168` did not preserve the branch value. |
| 2 | In the `saved_answering`/`!allow_return` path, `lowerBlockReturns` calls `lowerExprCons(ctx, tail, .single)` and discards the returned operand. |
| 3 | This preserves evaluation of the tail, but no fact carries its value through the branch join to the enclosing result demand. |
| 4 | GAP-174 therefore remains open until shared lowering preserves that operand and the surviving realization observes the five boundary rows above. |

| # | directive |
|---|---|
| 1 | The executable check is currently blocked before lowering at `832c089a`: `zig build unit-test` reaches the claimed parser frontier, where repository fixtures are rejected and `tools/node/dev/parser/artifact` fails. |
| 2 | That parser work is separately owned; it is not a GAP-174 repair and must not be bypassed by marking this obligation closed. |

| section |
|---|---|
| Open face beyond `08fb1d5c6` |

| # | directive |
|---|---|
| 1 | The first repair carries the five originally measured scalar tails into the answer. |
| 2 | The next bounded face is composition: a multi-statement deciding arm whose own tail is another value `if`. |
| 3 | The outer arm must preserve the inner arm's selected value through both joins; observing only the outer scalar-tail rows does not establish that. |

| # | directive |
|---|---|
| 1 | The Wasm execution fixture now includes that nested-tail shape and requires an answer of 7 through shared DNIR lowering and the surviving non-direct realization under Wasmtime. |
| 2 | The direct column remains the oracle recorded in `idol-native/gate/nest.id`; this land does not remeasure the five rows already established by `08fb1d5c6`. |

| # | directive |
|---|---|
| 1 | At this revision the fixture compiles into the Zig test binary, but its run is blocked before lowering by the same separately owned parser frontier above: the five existing rows and the new row all stop at their first declaration. |
| 2 | That is not evidence against the branch-value repair, and it is not permission to close GAP-174. |
| 3 | The nested observation stays executable and red until the parser owner restores fixture ingress; no parser bypass is part of this land. |

| section |
|---|---|
| Open face beyond `8353391c4` |

| # | directive |
|---|---|
| 1 | The nested-tail row above reaches the deciding arm through a plain `else`. |
| 2 | It does not cross an `elseif` chain join. |
| 3 | The next bounded composition face is a multi-statement deciding `elseif` arm whose own tail is another value `if`: the selected value must survive the inner join and the chain join before it reaches the enclosing answer demand. |

| # | directive |
|---|---|
| 1 | The Wasm execution fixture now includes that shape and requires an answer of 7. |
| 2 | The earlier scalar rows and plain-`else` nested row remain unchanged; this row adds one distinct control composition rather than remeasuring them. |
| 3 | GAP-174 remains open until the surviving non-direct realization executes this observer and preserves the selected value. |

| section |
|---|---|
| Open face beyond `2a9049ef` |

| # | directive |
|---|---|
| 1 | The `elseif` row above takes the intermediate arm. |
| 2 | It does not follow the chain's failed conditions into the final `else`. |
| 3 | The next bounded composition face is a multi-statement deciding final arm whose own tail is another value `if`: the selected value must survive the inner join and the chain's final join before it reaches the enclosing answer demand. |

| # | directive |
|---|---|
| 1 | The Wasm execution fixture now includes that shape and requires an answer of 7 after both earlier conditions decline. |
| 2 | The earlier scalar, plain-`else` nested, and deciding-`elseif` rows remain unchanged; this row adds the distinct final-arm path rather than remeasuring them. |
| 3 | GAP-174 remains open until the surviving non-direct realization executes this observer and preserves the selected value. |

| section |
|---|---|
| Open face beyond `acee493e2` |

| # | directive |
|---|---|
| 1 | The nested-tail rows above reach a plain `else`, an intermediate `elseif`, and the chain's final `else`. |
| 2 | They do not select the chain's initial `if` arm when that arm has multiple statements and its own tail is another value `if`. |
| 3 | That path must preserve the inner selected value through the inner join and the initial chain join before it reaches the enclosing answer demand. |

| # | directive |
|---|---|
| 1 | The Wasm execution fixture now includes that shape and requires an answer of 7 while the later arms remain unselected. |
| 2 | The earlier rows remain unchanged; this row adds the distinct initial-arm path rather than remeasuring them. |
| 3 | GAP-174 remains open until the surviving non-direct realization executes this observer and preserves the selected value. |

| section |
|---|---|
| Open face beyond `a4d08bf3a` |

| # | directive |
|---|---|
| 1 | The initial-arm row above selects the nested conditional's own initial `if` arm. |
| 2 | It does not select a multi-statement nested `else` arm while carrying that arm's value through both the nested join and the enclosing initial chain join. |

| # | directive |
|---|---|
| 1 | The Wasm execution fixture now includes that distinct composition and requires an answer of 7. |
| 2 | The earlier scalar and nested rows remain unchanged; this row does not remeasure their already-landed faces. |
| 3 | GAP-174 remains open until the surviving non-direct realization executes this observer and preserves the selected value. |

| section |
|---|---|
| Open face beyond `65f3418dd` |

| # | directive |
|---|---|
| 1 | The nested-`else` row above reaches the enclosing chain through its initial `if` arm. |
| 2 | It does not select an intermediate `elseif` arm whose own nested conditional chooses a multi-statement `else` value. |
| 3 | That value must survive the nested join and then the intermediate chain join before reaching the enclosing answer demand. |

| # | directive |
|---|---|
| 1 | The Wasm execution fixture now includes that distinct composition and requires an answer of 7. |
| 2 | The earlier scalar and nested rows remain unchanged; this row adds one unmeasured pair of selected arms rather than remeasuring an established face. |
| 3 | GAP-174 remains open until the surviving non-direct realization executes this observer and preserves the selected value. |

| section |
|---|---|
| Open face beyond `6af507bf` |

| # | directive |
|---|---|
| 1 | The intermediate-`elseif` row above selects the nested conditional's multi-statement `else` arm. |
| 2 | It does not follow the enclosing chain into its final `else` and then select the nested conditional's multi-statement `else` arm. |
| 3 | That value must survive the nested join and then the enclosing chain's final join before reaching the answer demand. |

| # | directive |
|---|---|
| 1 | The Wasm execution fixture now includes that distinct composition and requires an answer of 7. |
| 2 | The earlier scalar and nested rows remain unchanged; this row adds the remaining unmeasured pair of selected arms rather than remeasuring an established face. |
| 3 | GAP-174 remains open until the surviving non-direct realization executes this observer and preserves the selected value. |

| # | directive |
|---|---|
| 1 | Measured on the rebased lane, the fixture pins the answer at 7, but `zig build unit-test -- --test-filter "wasm backend preserves values from multi-statement conditional arms"` still stops before the fixture through `tools/node/dev/parser/artifact` at `98d1be408ee0ac2ca5403d17f475fa1c3f504f76`. |
| 2 | The first diagnostic is now `lib/compiler/parser.id:248:16: error: expected expression, got 'true'` at `return true`; the Wasm answer is therefore still unreachable at this head. |
| 3 | That parser lane is separately owned, so there is no honest Wasm-backend unblocking step at this revision. `zig build --summary all` succeeded around the landed row; the focused command above is the current residual measurement. |

| section |
|---|---|
| Open face beyond `f078c010` |

| # | directive |
|---|---|
| 1 | The final-`else` nested-`else` row above reaches the enclosing chain's final `else` arm, which contains a nested conditional with a multi-statement `else`. |
| 2 | It does not select a case where the nested conditional's multi-statement `else` is inside an intermediate `elseif` arm of the enclosing chain. |
| 3 | That value must survive the nested join and then the intermediate chain join before reaching the answer demand. |

| # | directive |
|---|---|
| 1 | The Wasm execution fixture now includes that distinct composition and requires an answer of 7. |
| 2 | The earlier scalar and nested rows remain unchanged; this row adds the remaining unmeasured pair of selected arms rather than remeasuring an established face. |
| 3 | GAP-174 remains open until the surviving non-direct realization executes this observer and preserves the selected value. |

| # | directive |
|---|---|
| 1 | Measured on the rebased lane, the fixture pins the answer at 7, but `zig build unit-test -- --test-filter "wasm backend preserves values from multi-statement conditional arms"` still stops before the fixture through `tools/node/dev/parser/artifact` at `98d1be408ee0ac2ca5403d17f475fa1c3f504f76`. |
| 2 | The first diagnostic is now `lib/compiler/parser.id:248:16: error: expected expression, got 'true'` at `return true`; the Wasm answer is therefore still unreachable at this head. |
| 3 | That parser lane is separately owned, so there is no honest Wasm-backend unblocking step at this revision. `zig build --summary all` succeeded around the landed row; the focused command above is the current residual measurement. |

| section |
|---|---|
| Open face beyond `53ca9487` |

| # | directive |
|---|---|
| 1 | The current intermediate-`elseif` nested-`else` row selects the first `elseif` arm after the initial condition declines. |
| 2 | It does not traverse an earlier unselected `elseif` before selecting a later `elseif` arm whose own nested conditional chooses a multi-statement `else` value. |
| 3 | That value must survive the nested join and the later chain join after two prior branch conditions decline before it reaches the answer demand. |

| # | directive |
|---|---|
| 1 | The Wasm execution fixture now includes that distinct composition and requires an answer of 7. |
| 2 | The earlier scalar and nested rows remain unchanged; this row adds the later-`elseif` chain path rather than remeasuring an established face. |
| 3 | Measured on this lane with a temporary Wasmtime 48.0.1 binary, the focused Zig test compiled the production frontend bridge and passed: |

| # | directive |
|---|---|
| 1 | zig test src/tests.zig -lc --test-filter "wasm backend preserves values from multi-statement conditional arms" [production frontend C bridge] |

| # | directive |
|---|---|
| 1 | GAP-174 remains open because this land pins one bounded residual face rather than claiming closure over every branch-value composition. |

| section |
|---|---|
| Open face beyond `78d74ada` |

| # | directive |
|---|---|
| 1 | The rows above test multi-statement arms in `if`/`else` positions (rows 1–5) and multi-statement arms in `elseif` positions that contain a nested conditional (rows 7–14). |
| 2 | None tests a plain multi-statement `elseif` arm whose tail is a compound binding result with no nested conditional: the value must survive the chain join through the `elseif` target alone. |

| # | directive |
|---|---|
| 1 | The Wasm execution fixture now includes that distinct composition — `if n < 0 \ 1 else if n > 2 \ a = 3; b = 4; a + b else \ 0` with `f(11)` requiring an answer of 7 — and adds it as row 15. |
| 2 | The earlier scalar and nested rows remain unchanged; this row adds the plain `elseif` multi-statement path rather than remeasuring an established face. `zig build --summary all` succeeded around the landed row. |
| 3 | The focused Wasm execution test requires Wasmtime, which is not available on this host; the test returns `SkipZigTest` when Wasmtime is absent, matching the established fixture contract. |

| # | directive |
|---|---|
| 1 | GAP-174 remains open because this land pins one bounded residual face rather than claiming closure over every branch-value composition. |

| section |
|---|---|
| Open face beyond `78d74ada` |

| # | directive |
|---|---|
| 1 | The plain-`elseif` multi-statement row above reaches the enclosing chain through an `elseif` arm with multiple statements and a compound tail. |
| 2 | It does not select a case where the `elseif` arm contains a nested three-arm `if`/`elseif`/`else` and the `elseif` arm of that nested conditional is itself multi-statement. |
| 3 | That value must survive the nested `elseif` join and then the enclosing chain join before reaching the answer demand. |

| # | directive |
|---|---|
| 1 | The Wasm execution fixture now includes that distinct composition and requires an answer of 7. |
| 2 | The earlier scalar and nested rows remain unchanged; this row adds one unmeasured three-arm nested composition rather than remeasuring an established face. |
| 3 | GAP-174 remains open until the surviving non-direct realization executes this observer and preserves the selected value. |

| section |
|---|---|
| Open face beyond `49b4e946` |

| # | directive |
|---|---|
| 1 | The nested-`elseif` row above reaches the enclosing chain through its `elseif` arm. |
| 2 | It does not select the enclosing chain's initial `if` arm when that arm contains a nested three-arm `if`/`elseif`/`else` whose `elseif` arm is multi-statement. |
| 3 | That value must survive the nested `elseif` join and then the enclosing chain's initial join before reaching the answer demand. |

| # | directive |
|---|---|
| 1 | The Wasm execution fixture now includes that distinct composition and requires an answer of 7. |
| 2 | The earlier scalar and nested rows remain unchanged; this row adds the initial-arm three-arm nested path rather than remeasuring an established face. |
| 3 | GAP-174 remains open until the surviving non-direct realization executes this observer and preserves the selected value. |

| section |
|---|---|
| Open face beyond `c4b97422` |

| # | directive |
|---|---|
| 1 | The initial-arm three-arm row above selects the nested conditional's multi-statement `elseif` arm while the enclosing chain enters through its initial `if` arm. |
| 2 | It does not follow both enclosing chain conditions into decline and then select the chain's final `else` arm when that arm contains a nested three-arm `if`/`elseif`/`else` whose `elseif` arm is multi-statement. |
| 3 | That value must survive the nested `elseif` join and then the enclosing chain's final join before reaching the answer demand. |

| # | directive |
|---|---|
| 1 | The Wasm execution fixture now includes that distinct composition and requires an answer of 7. |
| 2 | The earlier scalar, plain-`else` nested, deciding-`elseif` nested, three-arm initial-arm, and three-arm `elseif` rows remain unchanged; this row adds the three-arm final-arm path rather than remeasuring an established face. `zig build --summary all` succeeded around the landed row. |
| 3 | The focused Wasm execution test reaches the separately owned parser frontier; fixture stays executable and red until that clears, matching the established fixture contract. |

| # | directive |
|---|---|
| 1 | GAP-174 remains open because this land pins one bounded residual face rather than claiming closure over every branch-value composition. |
