# GAP-232 — a reached partition's own bootstrap needs never reach the link line

**Status:** OPEN · **Filed:** !2026-08-25T05:10:00Z
**Priority:** P1 · **Kind:** realization_gap
**Filed by:** claude-reconcile
**Files:** src/main.zig,gate/crosspartition.sh

## Current state — the repair landed; one execution remains

The accumulation is in the tree. `realizeReachedPartition` now reports each
reached artifact's `need` alongside what it reaches, `reachedHomeObjects` became
`reachedHomeClosure` and returns objects and needs as ONE value, and that call
now runs BEFORE `directLinkInputs` so the link line is decided from the UNION of
the entry's needs and every reached partition's. Acceptance 1 and 3 are done;
acceptance 2's fourth shape (`text`) exists, is named by the roster, and carries
the pins described below — and THAT NAMING IS NOW ENFORCED ON EVERY HOST, which
is the half of acceptance 2 that never needed macOS; see the section after this
one.

WHAT IS VERIFIED, AND WHERE:

    zig build                       # the change compiles
    zig build unit-test             # includes the new main.zig test:
                                    #   "a bootstrap unit is selected by a
                                    #    REACHED partition's need, not only the
                                    #    entry's"
    idol compile --backend=c --emit=c helper.id   # the `text` shape's reached
                                                  # partition really does emit a
                                                  # `duo_str_to_i64` call — it is
                                                  # not folded away, so the shape
                                                  # is not vacuous

The unit test sits on `bootstrapUnits`, which was extracted from
`directLinkInputs` precisely so that the selection is measurable without a native
realization. It pins the union in both directions: a reached `duo_str_to_i64`
selects the string runtime, and it selects NOTHING ELSE — a union that pulled in
every unit would link and answer too, so the negatives are what make the positive
mean something.

WHAT IS NOT VERIFIED, AND WHY. The COMPILED-RUN-ANSWERED half of
`gate/crosspartition.sh` needs macOS/aarch64: `src/native_backend.zig` gates
every emit path on `builtin.os.tag == .macos and builtin.cpu.arch == .aarch64`,
so on aarch64-linux `gate/realization/direct.sh` classifies the host `no` and
this gate exits 3 NOT MEASURED. The executed half of acceptance 2 is therefore
owed, and it is one command:

    sh gate/crosspartition.sh       # on macOS/aarch64; expect 4 subjects, 0 failed

Until that runs, this gap stays OPEN. Marking it closed from a host that cannot
execute the instrument would be the confident-wrong-number class this repository
treats as worst.

## The half of acceptance 2 that never needed macOS, and now runs everywhere

Acceptance 2 has two halves and they have different host requirements. That the
`text` shape LINKS AND ANSWERS needs a direct-native realization. That the
roster NAMES it — and keeps naming it, and names nothing that has stopped
existing — is a fact about `gate/crosspartition.sh` and
`gate/crosspartition.subjects` and asks nothing of any compiler.

The second half was measured BELOW the host classification, which on every host
but one means never. Measured on aarch64-linux at `5a87be146`: the gate exited 3
without ever opening its roster, so all four of these were unobservable here —

    a row naming a shape the gate cannot materialize
    a shape the gate can materialize that no row names   (the `text` row deleted)
    a malformed row
    a roster emptied to zero rows                        (GAP-201, the rule the
                                                          roster is a file FOR)

The ratchet now runs ABOVE the classification, where it belongs, and each of
those four convicts with exit 1 on this host. Reproduce by planting into a copy
(the roster path is `$here/crosspartition.subjects`, so a copy is the clean
plant) and reading the exit status:

    plant=$(mktemp -d); mkdir -p "$plant/gate/realization"
    cp gate/crosspartition.sh gate/crosspartition.subjects "$plant/gate/"
    cp gate/realization/direct.sh "$plant/gate/realization/"
    printf 'ghost 1 _x\n' >> "$plant/gate/crosspartition.subjects"
    IDOL="$PWD/zig-out/bin/idol" sh "$plant/gate/crosspartition.sh"; echo $?

AND THE PIN COLUMN JOINED THEM, which is the column acceptance 2 actually rests
on. Which direction a row must pin `duo_str_to_i64` in is decided by the
`materialize` arm — it writes a text literal into the reached partition or it does
not — and the roster carried a second copy of that decision. The arm has already
run when the ratchet reads the row, so the direction is derived from the sources
on disk. Three further damages convict that were exit 3 here, and are not caught
by the host-bound half either, for a reason that is about `run_subject` and not
about any host: a pin can only refuse a subject it is ASKED to check, so removing
one or aiming it elsewhere cannot turn a passing measurement into a failing one
anywhere.

    `duo` loses its `!duo_str_to_i64` entirely
    all three absence pins redirected to `!duo_str_sub` — a real symbol in
        `idol_str_runtime.o`, quoted in this file's own `## The hole` section, and
        one that no shape here can put on a link line, so the pin guards nothing
    `text` loses the `duo_str_to_i64` presence pin

and three more that keep the derivation itself honest: a row pinning the symbol
in both directions, a subject arm that materializes no partition beyond its
entry, and either pin direction being required of no row at all — the last being
GAP-201's rule at the level of the argument, since the presence pin and the
absence pins are one claim in two halves.

AND NONE OF IT NEEDED A BUILT COMPILER, which one line did. The `-x $IDOL` guard
sat above the ratchet, so with `zig-out/bin/idol` removed every conviction above
became exit 3 "compiler is not executable" without the roster being opened. The
`unbuilt` arm of `gate/realization/direct.sh` is the one producer of that fact
and already answers it, so the requirement moved down to it; measured, a damaged
roster now convicts in an unbuilt tree.

An UNDAMAGED copy still exits 3 on this host, which is the control that keeps
the split honest: nothing about the answered half was claimed here. The
convictions carry neither `DNB004` nor `NOT MEASURED —`, so `gate/all.sh` files
them as law rather than as a host limit — checked against that exact grep.

MATERIALIZING IS NOT REPORTING, so the controls-before-subjects ordering is
intact: the ratchet writes subject sources, and no subject is compiled, run or
reported until P, N1, N2, N3, P2 and N4 have passed. The validated rows are
handed on as `$work/measure`, so the roster's fields keep ONE parser, and a row
that passes the ratchet and then goes unmeasured is now itself a failure.

    sh gate/posix.sh                # every shell gate parses under dash
    sh gate/vacuity.sh              # VACUITY OK — this gate refuses under both
                                    # plants, as it did before (its roster and
                                    # compiler checks are upstream of the split)

## The `text` shape, and why its pins are shaped the way they are

Its reached partition converts text (`"21":to(i64)` → `duo_str_to_i64` in
`idol_str_runtime.o`); its entry only does arithmetic and needs no runtime. Its
call boundary is deliberately `duo`'s — one i64 in, one i64 out — so the only
variable between the two shapes is the reached need, and a failure cannot be
argument lowering wearing a link-line costume.

The roster's third column became a comma-separated pin list, and `!name` inverts
a pin to require ABSENCE. `duo`, `trio` and `fan` each carry
`!duo_str_to_i64`, because a compiler that materialized all three bootstrap units
on every link line would satisfy `text` without ever having read a `need`.
Control N3 proves both pin directions actually refuse, against the positive
control's image, so the pins are not decoration. And the ratchet proves the
COLUMN is not decoration either: which direction each row must take is derived
from the source its own `materialize` arm wrote, so the roster cannot quietly stop
requiring what makes the presence pin mean anything.

## Superseded observations

The sections below are the filing rationale and the acceptance it set. They
remain the RULING — what this gap was for and what would settle it — and they are
not the state of the tree; that is `## Current state` above.

## The hole

`land/homeclosure-20260823` closed the case where a direct-native build reached
a partition and emitted no object for it. Each reached partition is now parsed,
lowered and emitted, content-addressed, and put on the link line.

What is NOT carried across is each reached artifact's `need` list.
`emitObjectWithGraphLineageObserved` returns an artifact whose `need` names the
bootstrap objects that artifact references; the recursive realization keeps the
BYTES and drops the needs, and `directLinkInputs` is then handed the ENTRY
artifact's needs alone. An entry that needs no bootstrap runtime, reaching a
partition that does, links without it.

Measured, an entry reaching `lib/token/grammarrole.id`:

    _idol_<…>_token_grammarrole__roleprecedence   DEFINED  (was the GAP-134
                                                           undefined symbol)
    _duo_str_sub                                  UNDEFINED, referenced from
                                                  the reached partition's own
                                                  object

## Why it is P1 and not P0

It fails LOUDLY, at the linker, by symbol name. It is not the class this
repository treats as worst — a wrong answer under exit 0 — and it is one layer
past the hole `land/homeclosure` actually closed.

## Why it was not already a gap

It was recorded, but only inside `gate/crosspartition.sh`, under a heading that
says so plainly: "WHAT THIS GATE DELIBERATELY DOES NOT COVER, and why saying so
here". That paragraph also explains why every subject in that gate is
arithmetic — so the gate measures the reach and not this second gap — and ends
"When the needs of reached partitions join the link line, the honest edit is a
fourth shape whose reached partition needs the string runtime."

That is an honest scope boundary and it did its job: review of #155 found this
from the code, and the gate comment is what confirmed it was known rather than
missed. But a limitation that lives only in the instrument is not on any
obligation list, and `gaps/` is where the obligation belongs. Filing it does not
change the code; it stops the boundary from being visible only to whoever opens
that gate.

## Acceptance

1. The recursive realization accumulates each reached artifact's `need` and
   `directLinkInputs` receives the union, not the entry's alone.
2. `gate/crosspartition.sh` gains the fourth shape its own comment names — a
   reached partition that needs the string runtime — and that shape links and
   answers.
3. The paragraph quoted above is deleted in the same commit, because a gate
   that says it does not cover something it now covers is a stale instrument.
