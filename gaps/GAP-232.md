# GAP-232 — a reached partition's own bootstrap needs never reach the link line

**Status:** OPEN · **Filed:** !2026-08-25T05:10:00Z
**Priority:** P1 · **Kind:** realization_gap
**Filed by:** claude-reconcile
**Files:** src/main.zig,gate/crosspartition.sh

## Current state — the repair landed; one execution remains

The accumulation is in the tree. `realizeReachedPartition` now reports each
reached artifact's `need` alongside what it reaches, `reachedHomeObjects` became
`reachedHomeClosure` and returns objects and needs as ONE value, and that call
now runs BEFORE `directLinkInputs` so the link line is decided from the UNION of
the entry's needs and every reached partition's. Acceptance 1 and 3 are done;
acceptance 2's fourth shape (`text`) exists, is named by the roster, and carries
the pins described below.

WHAT IS VERIFIED, AND WHERE:

    zig build                       # the change compiles
    zig build unit-test             # includes the new main.zig test:
                                    #   "a bootstrap unit is selected by a
                                    #    REACHED partition's need, not only the
                                    #    entry's"
    idol compile --backend=c --emit=c helper.id   # the `text` shape's reached
                                                  # partition really does emit a
                                                  # `duo_str_to_i64` call — it is
                                                  # not folded away, so the shape
                                                  # is not vacuous

The unit test sits on `bootstrapUnits`, which was extracted from
`directLinkInputs` precisely so that the selection is measurable without a native
realization. It pins the union in both directions: a reached `duo_str_to_i64`
selects the string runtime, and it selects NOTHING ELSE — a union that pulled in
every unit would link and answer too, so the negatives are what make the positive
mean something.

WHAT IS NOT VERIFIED, AND WHY. `gate/crosspartition.sh` needs macOS/aarch64:
`src/native_backend.zig` gates every emit path on
`builtin.os.tag == .macos and builtin.cpu.arch == .aarch64`, so on
aarch64-linux the gate's own positive control refuses at DNB004
(`no native machine realization for target 'native-exe' on this host`) and it
exits 3 NOT MEASURED — before and after this change alike. The executed half of
acceptance 2 is therefore owed, and it is one command:

    sh gate/crosspartition.sh       # on macOS/aarch64; expect 4 subjects, 0 failed

Until that runs, this gap stays OPEN. Marking it closed from a host that cannot
execute the instrument would be the confident-wrong-number class this repository
treats as worst.

## The `text` shape, and why its pins are shaped the way they are

Its reached partition converts text (`"21":to(i64)` → `duo_str_to_i64` in
`idol_str_runtime.o`); its entry only does arithmetic and needs no runtime. Its
call boundary is deliberately `duo`'s — one i64 in, one i64 out — so the only
variable between the two shapes is the reached need, and a failure cannot be
argument lowering wearing a link-line costume.

The roster's third column became a comma-separated pin list, and `!name` inverts
a pin to require ABSENCE. `duo`, `trio` and `fan` each carry
`!duo_str_to_i64`, because a compiler that materialized all three bootstrap units
on every link line would satisfy `text` without ever having read a `need`.
Control N3 proves both pin directions actually refuse, against the positive
control's image, so the pins are not decoration.

## Superseded observations

The sections below are the filing rationale and the acceptance it set. They
remain the RULING — what this gap was for and what would settle it — and they are
not the state of the tree; that is `## Current state` above.

## The hole

`land/homeclosure-20260823` closed the case where a direct-native build reached
a partition and emitted no object for it. Each reached partition is now parsed,
lowered and emitted, content-addressed, and put on the link line.

What is NOT carried across is each reached artifact's `need` list.
`emitObjectWithGraphLineageObserved` returns an artifact whose `need` names the
bootstrap objects that artifact references; the recursive realization keeps the
BYTES and drops the needs, and `directLinkInputs` is then handed the ENTRY
artifact's needs alone. An entry that needs no bootstrap runtime, reaching a
partition that does, links without it.

Measured, an entry reaching `lib/token/grammarrole.id`:

    _idol_<…>_token_grammarrole__roleprecedence   DEFINED  (was the GAP-134
                                                           undefined symbol)
    _duo_str_sub                                  UNDEFINED, referenced from
                                                  the reached partition's own
                                                  object

## Why it is P1 and not P0

It fails LOUDLY, at the linker, by symbol name. It is not the class this
repository treats as worst — a wrong answer under exit 0 — and it is one layer
past the hole `land/homeclosure` actually closed.

## Why it was not already a gap

It was recorded, but only inside `gate/crosspartition.sh`, under a heading that
says so plainly: "WHAT THIS GATE DELIBERATELY DOES NOT COVER, and why saying so
here". That paragraph also explains why every subject in that gate is
arithmetic — so the gate measures the reach and not this second gap — and ends
"When the needs of reached partitions join the link line, the honest edit is a
fourth shape whose reached partition needs the string runtime."

That is an honest scope boundary and it did its job: review of #155 found this
from the code, and the gate comment is what confirmed it was known rather than
missed. But a limitation that lives only in the instrument is not on any
obligation list, and `gaps/` is where the obligation belongs. Filing it does not
change the code; it stops the boundary from being visible only to whoever opens
that gate.

## Acceptance

1. The recursive realization accumulates each reached artifact's `need` and
   `directLinkInputs` receives the union, not the entry's alone.
2. `gate/crosspartition.sh` gains the fourth shape its own comment names — a
   reached partition that needs the string runtime — and that shape links and
   answers.
3. The paragraph quoted above is deleted in the same commit, because a gate
   that says it does not cover something it now covers is a stale instrument.
