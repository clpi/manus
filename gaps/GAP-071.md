# GAP-071 — Pass 116's new deny rows, measured on the day they became law

**Status:** OPEN — the SELF-ZERO row now has an instrument for the first time;
every other row unchanged or GROWN · **Filed:** 2026-08-08 ·
**Re-measured:** 2026-08-09

## 2026-08-09 — the falsifier fired, one day early and in the wrong direction

> *"If `CLAUDE.md` §1 still lists these rows in a month and this file still
> reads 29 / 16 / 1 / 1, the deny list gained rows it cannot enforce."*

One day later, and the rows did not hold still — they **grew**:

| row | filed 2026-08-08 | 2026-08-09 |
|---|---|---|
| files declaring a parameter named `self` | 29 | **30** |
| `std.math.` files | 16 | **17** |
| `std.mem.` files | 1 | **3** |
| `std.fmt.` files | 2 | **2** |
| `ns` sites outside `lib/std` | 42 | **45** |
| bare `math.` files in `lib/std` | 79 | 79 |
| `lib/std` entries | 138 | 138 |

`recognition-scan` reads **512 / 51 / 13** — *exactly* the CORRECTION
section's numbers — and **BLOCKER 1 still probes ABSENT** (`a:abs()` lowers
the receiver away and emits an undeclared zero-argument `a__abs()`). So the
corrected sequencing is vindicated: the `pure` row cannot move, and it hasn't.

## THE FINDING: the AUDIT v4 `self` scan did not exist

`CLAUDE.md` §2 lists a **"self scan (a parameter named `self` is a finding,
§0a)"** among the AUDIT v4 rows. Measured 2026-08-09:

- not among `audit100`'s 20 rows
- not a `build.zig` step (all 62 step names enumerated)
- not a script under `scripts/`

**There was no instrument anywhere.** This is the `upperdecl` defect one level
worse. `upperdecl` was a row *measuring somewhere other than where the rule
is*, reading green forever. This row read as satisfied **by being absent**
while the audit table named it — and this file is the only thing that has ever
measured it, by hand, once, on the day the rule landed.

That is precisely the pattern this gap was written to stop: *"a rule is
written, the audit lists it, and the number behind it is never measured — so
the list reads as satisfied."* It happened again, to this gap's own first
section.

### Landed 2026-08-09 — `audit100` row 21, `self`, at 41

Measured over the 554-file canonical partition with string bodies stripped:
**41 points**. Budget 41, no headroom.

**The DECLINE half is the delicate one**, and is why this is not a two-line
regex. `self` is a banned spelling that appears constantly in the two forms
the law REPLACES it with — `self.x` is a field walk, `self:len()` is an
invoke. A row convicting those would count one defect three times and could
never reach zero by removing parameters. So the pattern anchors on
parameter-list position (`(` or `,` to the left) and accepts only a closer, a
comma, or a **spaced** copula to the right. §0d's spacing rule does real work
here: `self: any` is a typed parameter, `self:peek()` is an invoke, and one
space decides it.

Eight probes run before any count prints:

| must SEE | must DECLINE |
|---|---|
| `scale = (self, k) @{ .x * k }` | `n = self.pos + 1` (field walk) |
| `len = (self): i64` | `n = self:peek() + 1` (invoke) |
| `fh.close = (self: any) nil` | `scale = (k) @{ .x * k }` (the canonical lens form) |
| `write = (fh, self) nil` (second position) | `myself = (herself, itself) nil` (word boundary) |

**End-to-end calibration.** Injecting `probe = (self, k) k` into a canonical
file moves the row 41 → 42, marks it `!!`, and the per-diff delta names it
`self +1 ROW IS OVER BY 1`. Reverting returns it to 41 green.

Three parallel arrays had to move together — `keeps` and `enforced` each carry
their own exactly-N-entries control, and both caught the insertion. Inserted
at position 8 rather than appended, because `pat[#id]` is the `convert` row's
probe.

### Which AUDIT v4 rows now have instruments

| v4 row | instrument |
|---|---|
| self scan | **`audit100` row `self`, landed 2026-08-09** |
| recognition scan | `zig build recognition-scan` — exists, ratchets, probes its blockers live |
| role scan | `zig build highlight-corpus` (GAP-078 deleted `role_scan.id`; see GAP-073) |
| runtime row | **still no instrument found** — no build step names it |

The `runtime row` (*"every proof workload cites its oracle delta, answers
verified equal first"*) is the remaining unmeasured v4 row. Recorded rather
than fixed, so it is not read as satisfied by absence a second time.

---

*Original filing follows.*
**Law:** Pass 116 §6d SELF-ZERO + RECOGNITION · Pass 112 §7 AUDIT v4

Pass 116 adds rows to the deny list. A deny list is only pressure if its rows
can reach zero, and it is only honest if the starting distance is recorded. This
is that record, so nobody reads the regenerated `CLAUDE.md` §1 as a description
of a property the repository already has.

## SELF-ZERO

> *"THERE IS NO SELF: `.x` reads the subject's fields · leading `:name()`
> invokes on it · a void body in chain position RETURNS it · bare `.` is its
> value · `...` spreads it. **A parameter named `self` = audit finding**;
> parameter lists count true arguments only."*

Measured over tracked `.id`:

```
29 files declare a parameter named `self`
```

Not a stylistic sweep. The rule says parameter lists count TRUE ARGUMENTS ONLY,
so removing `self` changes each function's arity — which changes native
signature promotion, the `f64AbiParamSlots` count, record explosion, and every
call site. Several of tonight's miscompilations lived in exactly that machinery
(gap[056] argument classification, gap[058] register files). This is a real
migration with a real blast radius, not a rename.

## RECOGNITION

> *"no `std.mem`/`fmt`/`math` — pure ops are value edges (`x:abs()`,
> `s:copy()`); worlds are the only capability namespaces."*

```
std.math.   16 files
std.mem.     1 file
std.fmt.     1 file
```

`std.math` is the substantive one. The replacement is not a rename either: a
pure op becomes a VALUE EDGE on a descriptor (`x:abs()`), which means the edge
has to exist on every numeric descriptor it is called for, and `RECOGNITION`
also says idiom recognition lowers to intrinsics UNDER WITNESS — so the target
form depends on machinery (`why(realization)`) that does not exist yet
(gap[064] records `why` as an undeclared function).

Sequencing consequence: the `std.math` row cannot reach zero before the witness
surface exists. Turning it on now would force 16 files into a spelling with no
way to prove its lowering.

## Why this file exists rather than a silent deny row

Tonight produced a repeated pattern worth not repeating: a rule is written, the
audit lists it, and the number behind it is never measured — so the list reads
as satisfied. It happened with `std@{ ambient = false }` (gap[061]: assumed
clean, measured 53 of 257 modules) and with the `rand` class inside that same
scan (assumed zero, actually 50 sites — the probe was broken and the file said
so and shipped anyway).

The rows above are in `CLAUDE.md` §1 as of this commit. They are **not** at zero.

## Order of work, argued

1. **`std.mem` and `std.fmt`** — one file each. Cheap, and they prove the
   value-edge form on a small surface before it is asked to carry 16 files.
2. **`self`** — mechanical per file, but arity-changing, so it wants the ABI
   gates green throughout and should go a few files at a time with
   `abi-matrix` and the corpus differential run between batches. Do NOT batch
   it with anything else; if a differential reddens, the cause must be
   unambiguous.
3. **`std.math`** — blocked on `why(realization)` and the intrinsic-recognition
   path. Record the blocker rather than forcing the spelling.

## Falsifier

If `CLAUDE.md` §1 still lists these rows in a month and this file still reads
29 / 16 / 1 / 1, the deny list gained rows it cannot enforce, and the audit is
describing an aspiration.

## AMENDED 2026-08-08 — Passes 117-119 add three more rows, also not at zero

Same measurement discipline, same result: the new rows describe targets, not
properties.

| row | law | measured now |
|---|---|---|
| `;` anywhere | Pass 119 §6g "THE SEMICOLON DOES NOT EXIST" | **2193 semicolons in 306 files** |
| std module tree / import | Pass 118 §6f "STD IS THE PRELUDE GRAPH — no module tree, no import" | **138 entries under `lib/std`** |
| plain-text output | Pass 117 §6e G-TOTAL + Pass 116 H-8 | every diagnostic today (gap[064]: `check`/`why` are undeclared functions) |

**The semicolon row is a DIRECT CONFLICT with standing law, not new ground.**
Pass 100 §4 listed `;` as "one-line induction tail (the only ; in the
language)". Pass 119 retires it. Higher pass wins, so §4 is amended in the same
commit as this measurement — but 2193 occurrences across 306 files is a corpus
migration, and the canonical replacement (`while (i += 1) <= n consume(i)`) is a
different construct, not a delete-the-character rewrite.

**The std row is the largest structural gap in the repository.** "No module
tree, no import" against 138 entries under `lib/std` is not a refactor; it is
the prelude-graph architecture, and it depends on the graph service that §22
still lists as owed. Recording it so the deny row is not read as achievable by
tidying.

**G-TOTAL cannot be measured at all yet.** It requires role coverage over
rendered artifacts, and the role taxonomy has no implementation — there is no
`highlights.scm` generator, no LSP legend generator, and no golden token corpus
(`fixtures/highlight/*.id` + `*.roles`) in the tree. A gate with no instrument
reports nothing, which is worse than reporting a bad number.

---

## CORRECTION 2026-08-08 — the RECOGNITION number above is wrong by 4x, and the two blockers are now probed rather than asserted

The counts in the RECOGNITION section (`std.math. 16 files / std.mem. 1 /
std.fmt. 1`) are the **dotted-namespace** counts. They are correct for what they
measured and they are not what the rule denies. AUDIT v4 denies
`std.mem`/`std.fmt`/`std.math` **and bare `math.`**, and bare `math.` is the
dominant spelling in this tree:

```
bare math.   79 files in lib/std        <- never counted above
std.math.    16 files                    <- what was counted
```

A row measured at 16 that is really 79 reads as nearly-satisfied when it is not.
That is the precise failure this file and GAP-061 were both written to stop
repeating, and it happened again inside the file that named it. Recorded here
rather than quietly fixed, because the pattern is the finding.

`zig build recognition-scan` (`scripts/recognition_scan.id`) now measures it on
every run and ratchets. Baseline over 258 tracked `lib/std/**/*.id`, counted in
LINES:

| row | sites | files | outside lib/std | repair |
|---|---|---|---|---|
| `pure` | 512 | 73 | 50 | VALUE EDGE `x:abs()` — no capability |
| `rand` | 51 | 15 | 4 | WORLD — ambient authority |
| `ns` | 13 | 3 | 42 | the three namespaces named by the ruling |
| `sub` | 0 | 0 | 0 | module refs — reported, not ratcheted |

### The split is the point, and it is why this is one edit and not two

`rand` is not a rounding error on the `pure` row. It is the **entire `rand`
class of Pass 105 U1** — the third-largest ambient class in `lib/std`. GAP-061
reported `rand` at zero because it grepped `rand(`; this tree spells it
`math.random(`. Retiring `math.random` and closing U1's rand class are the same
edit, and `zig build capability-scan` is the instrument that proves it.

### Both repairs are BLOCKED, and the gate probes each one live

**BLOCKER 1 — the value-edge surface does not exist.** `x:abs()` does not build.
The compiler lowers the receiver away and emits a call to an undeclared
`a__abs()` taking **zero arguments** — a shape that would bind to any same-named
free function and silently drop the subject.

> `duo check` **exits 0** on `a:abs()`. Only codegen rejects it. The first draft
> of the gate reported this blocker as LIFTED for exactly that reason. Any probe
> of this surface must `duo compile` and **run the result asserting the value**;
> `check` is a false green here.

**BLOCKER 2 — there is no world.** `world` appears once in all of `lib/std`, in
prose, and **zero times** in `docs/spec/grammar.md` (Pass 108, normative). There
is no construct to move `math.random` behind. **Pass 118 §0h raises this cost
rather than lowering it**: worlds are now the only namespaces std has at all, so
`math.random` waits on the same construct the whole prelude graph waits on.

**BLOCKER 3 — the witness.** §0a requires idiom recognition to lower UNDER
WITNESS, `why(realization)` answering. gap[064] records `why` as undeclared. A5
WITNESS makes an unwitnessed lowering illegal, so this blocks the `pure` row
*even if* BLOCKER 1 lifts.

Each of 1 and 2 is probed on every gate run and **paired with a positive
control**, because "the target form failed" and "the apparatus is broken" are
the same red otherwise.

### The shortcut that is foreclosed

Moving the pure ops into another namespace — `std.numeric`, `num.`, a renamed
`math` — is **not** a repair. Pass 118 §0h: std has no module tree and worlds are
the only namespaces. A fresh namespace for capability-free ops is the same defect
respelled, and it would drive this row to zero while changing nothing. The only
legal destination is the descriptor.

### Order of work, corrected

The original order says *"`std.mem` and `std.fmt` — one file each. Cheap."*
That is true of the dotted call sites and false of the modules: `lib/std/mem.id`
is 354 lines and `lib/std/fmt.id` is 177. More importantly it puts **cheap
first**, and cheap is not the constraint — the value-edge surface is. Revised:

1. **BLOCKER 1 first** — the value edge on numeric descriptors. Nothing in the
   `pure` row (512 sites, 73 files, the bulk of the debt) can move until
   `x:abs()` builds and answers. Verify by VALUE: `(-3.0):abs()` = 3.0,
   `(2.5):floor()` = 2.0, `(9.0):sqrt()` = 3.0.
2. **BLOCKER 3 alongside it** — `why(realization)`, or the lowering is illegal
   under A5 even once it compiles.
3. **`rand` only after worlds exist.** It is 51 sites and it is the one row that
   also moves `capability-scan`. Do not convert it into an intermediate
   namespace to make the number fall; that is the foreclosed shortcut and it
   would make U1 *look* closed while the authority is untouched.

### Falsifier, corrected

If `recognition-scan` still reads `512 / 51 / 13` in a month **and** BLOCKER 1
still probes ABSENT, the sequencing above is right and the rows are simply not
yet reachable. If the rows fall while BLOCKER 1 still probes ABSENT, something
was respelled rather than repaired — read that commit closely.
