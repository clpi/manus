# GAP-109 — interpolation silently passes through what it cannot resolve; a file-scope keyed table emits broken C

**Status:** CLOSED — both recorded defects measured stale · **Filed:** 2026-08-09 · **Closed:** 2026-08-25
**Law:** §0.8 (interpolation is the canonical form) · H-8 OUTPUT TOTALITY ·
§3 MEASUREMENT HONESTY
**Answers** the audit's "interpolation crash / wrong-type path" P0 with two
measured defects, neither of which is a crash.

## 1. Interpolation silently emits its own source text

```duo
print("expr={1 + 2}")     ->  expr={1 + 2}
```

Not `3`. Not a diagnostic. **The braces and the expression are printed
literally**, so a program that asks for a value gets the source that asked for
it.

What resolves and what does not:

| hole | result |
|---|---|
| `"{a}"` a plain name | resolves |
| `"{p.x}"` a field walk | resolves |
| `"{1 + 2}"`, `"{a + 1}"` any expression | **passes through as text** |
| `"{t}"` a table | prints the word `table` |
| `"{n}"` nil | prints `nil` |

The name and field cases are the documented working forms, so this is a known
boundary — but the FAILURE MODE is the finding. §0.8 makes interpolation the
canonical spelling and bans concat chains, so a user reaching for `"{a + b}"` is
writing what the law tells them to write and getting source text back with no
diagnostic at all.

**H-8 makes this a finding twice over**: plain-string output is a finding, and an
unresolvable hole should DIAGNOSE with a repair edge, not degrade to its own
bytes. Degrading silently is the one behaviour that cannot be noticed.

`"{t}"` printing `table` is the same class one step down — an inspectable value
rendered as its type name carries no information and no way to ask for more.

## 2. A file-scope keyed table emits C that does not compile

```duo
p = { x = 7 }          -- FILE SCOPE
main: i64 = ()
    print(p.x)
```

```
error: call to undeclared function 'lua_to_display_str'
error: call to undeclared function 'lua_table_set_raw_lit'
error: use of undeclared identifier 'tmp'
```

The identical table INSIDE a function works and prints `7`. So the defect is
scope, not shape: a keyed table at file scope emits calls to runtime helpers the
module never declared, plus a reference to an undeclared `tmp`.

This is loud, which is the good news, and it is the same family as gap[108] —
**file-scope bindings are handled by a path that has not kept up with the one
handling locals.** gap[108] is the silent half of that family (a file-scope
binding is per-function storage in the native backend); this is the noisy half.
Two symptoms, one under-maintained scope.

## Repair order

1. **The interpolation diagnostic first**, before any expression support. An
   unresolvable hole must diagnose — `"{a + b}"` should say so and name the
   repair. That converts a silent wrong output into a fixable one and costs
   nothing that works today.
2. **Then expression holes**, if they are wanted at all: §0.8's ban on concat
   chains means the alternatives are an expression hole or a bound local, and a
   language that forbids `..` while silently mangling `"{a + b}"` leaves no
   correct spelling for the common case.
3. **The file-scope keyed table** is independent and small — the emitter needs
   the declarations its local-scope sibling already gets.

## Do not

**Do not "fix" the pass-through by rendering the expression's text more
prettily.** The bytes are not the answer; the value is. A hole that cannot be
resolved is a diagnostic, and a hole that can be resolved is a value — there is
no third behaviour worth having.

## Related

gap[108] — the silent half of the file-scope family. Direct-backend
interpolation additionally bails at `lowerBinop()`, so the whole feature is
C-only today; that is a coverage gap, not a wrong answer, and is not counted
here.

## The diagnostic cannot be blanket — measured blast radius

`desugar_string_interpolation` (`parser.zig:5073`) walks the string, and for each
`{…}` calls `parseInterpolationPath`. When that returns null the loop does
`i += 1; continue;` — **the silent pass-through is that one line.** The obvious
repair is to diagnose there instead. Measured, that would be wrong:

```
brace holes in .id strings that are NOT a simple path
  46  {}
   7  { return a + b; }        C source inside a string
   4  {a = 1, b = 2, c = 3}    a idol table literal, quoted
   4  {1, 2}
   3  {print $1}               awk
   3  {out_buf, out_len}       C
   4  {files[i]}               <- SOMEONE ACTUALLY TRYING TO INTERPOLATE
```

Roughly eighty sites are legitimate literal braces — C in `@c.emit` payloads,
awk programs, JSON, prose. A diagnostic firing on those would be worse than the
defect: it would break working code to report a non-problem.

**And `{files[i]}` is the defect in the wild** — four sites where someone wrote
an index expecting interpolation and shipped the source text instead. So the
population contains both classes and the repair must separate them.

### The discriminator that would work

**Try to parse the hole as a idol expression.**

- parses AND is a path (`a`, `p.x`) -> resolve, as today
- parses BUT is not a path (`files[i]`, `a + 1`, `1 + 2`) -> **an interpolation
  attempt. DIAGNOSE**, naming the repair
- does not parse as an expression at all (`{}`, `{ return a + b; }`,
  `{print $1}`) -> literal braces, leave alone

That rule catches every real instance above and fires on none of the literals,
because C bodies, awk and empty braces are not idol expressions. It also
generalizes: the day expression holes are supported, the middle row becomes
"resolve" and the rule does not change shape.

**Not implemented here.** The rule is cheap but its false-positive rate must be
measured against the whole corpus before it ships, and this session has already
established that asserting a cost without measuring it is how the spiller
estimate came out wrong by 50%. Whoever takes it should print the middle row
across `lib/ tools/ scripts/ examples/` first and read every hit.

## Closed — both defects measured stale at `7b025afe`

Host `x86_64-linux`, compiler `zig-out/bin/idol` from
`tools/node/dev/idol-lock -- zig build` (exit 0). On this host the direct
backend refuses by name — `DNB004 … auto backend has no native machine
realization for target 'native' on this host` — so the executing realization
is `--backend=wasm` under wasmtime, and the C realizer is
`--backend=c --emit=c` (graph-backed C99 slice, source only).

### 1. The pass-through is dead; the discriminator shipped, generalized

The discriminator this record proposed — *try to parse the hole as an
expression* — is exactly what `desugar_string_interpolation` does now: every
hole is parsed by the real grammar via `parseInterpolationHole`
(`src/parser.zig`), so the middle row became "resolve", which is the
generalization §"The discriminator that would work" predicted. Measured:

| recorded row | then | now (measured) |
|---|---|---|
| `"expr={1 + 2}"` | `expr={1 + 2}` literally, silent | **`expr=3`** — wasm, wasmtime, exit 0 |
| `"sum={a + b}"` | source text, silent | **`sum=3`** |
| `"{t[2]}"` — the `{files[i]}` class | source text, silent | resolves; `"{t[2]}":len()` exits 2 |
| `"awk={print $1}"` — literal braces | silent literal | literal, **STR-1 warning** naming the projected reading + hints naming `\{`/`\}` and the byte face |
| `"v={nosuch}"` — parses, unresolvable | n/a | **refusal**: `no native realization (UnsupportedProgram)`, `refused at: concat` — no value invented |

Nothing passes through silently: a hole that resolves is a value, a hole that
parses but cannot resolve refuses at realization, and only deliberate literal
braces survive — warned, with the repair named. The parser's own header
comment carries the corpus measurement (5,783 path holes unaffected; 75 holes
in 21 files that emitted literal text now interpolate) and stages the
stray-brace refusal behind the `\{` migration; that staging cites
`docs/text-law.md`, which no longer exists in this tree — the staging record
survives only as the `parser.zig` comment above `desugar_string_interpolation`.

`"{t}"` no longer prints the word `table`; it prints `0`, and so does bare
`print(t)` on the wasm realization — the uninformative rendering moved out of
interpolation into value display generally. That is a different producer
(aggregate display, not hole resolution) and is not counted in this close;
repro: `t = { x = 1 }` / `print(t)` → `0`.

### 2. The broken-C emitter is retired and fenced; the C path refuses by name

The producer of `lua_to_display_str` / `lua_table_set_raw_lit` / `tmp` — the
AST/Lua C bridge in `src/codegen.zig` — is unreachable by every backend:
`src/main.zig` ("LEGACY AST/LUA C BRIDGE") refuses by name if any routing ever
reaches it again. The recorded program now measures:

```text
p = { x = 7 } / main: i64 = () / print(p.x)

--backend=c --emit=c   error: C99 realizer: no realization (UnsupportedProgram)
                       hint: refused at: operation-not-in-c99-slice
                       hint: in relation: main          (exit 1, no TU written)
--backend=wasm         prints 7, exit 0                  (wasmtime)
```

No broken translation unit exists to compile: the refusal happens before any
source does, with the cause and relation named. The identical local-scope
program measures the same on both routes, so the file-scope/local split this
record found is gone with its producer.

### Pins

- `src/c_backend.zig` — "C backend refuses the file-scope keyed table by name
  instead of emitting broken C": the exact recorded program through the checked
  graph and DNIR into `emitSource`, asserting `error.UnsupportedProgram` with
  note `operation-not-in-c99-slice` in relation `main`.
- `src/comptime.zig` — "a resolvable interpolation hole answers by value, not
  as its own source text": `"{s:sub(2, 3)}":len()` folds to 2 by value;
  pass-through would answer its own 13 source bytes.
- `examples/text/brace/hole.id` (pre-existing, shrink-only) pins the
  arithmetic/expression hole classes end-to-end with an `# expect:` oracle.

### Census

`sh gate/realize/census.sh` at this tree: 253 subjects, 64 refused by check,
13 realized, 176 refused by the realizer; `operation-not-in-c99-slice` 26 —
the note this gap's file-scope program lands under. The histogram did not move
in this close and could not: the diff is two test pins and this record, and
the compiler binary is built from the unmodified sources. The movement this
gap asked for happened in the earlier landed work this close measures —
the parser hole rewrite and the AST/Lua C bridge retirement — and the census's
before lives in `gaps/GAP-233.md` (210 check-accepts, 141
`missing-application-id` at its tree; 189 and 120 here).

### Repair order, settled

1. the diagnostic — DONE (STR-1 + hints; refusal for parsed-but-unresolvable
   holes lands at realization, named);
2. expression holes — DONE (real grammar, not a widened path grammar);
3. the file-scope keyed table — the emitting producer was deleted rather than
   repaired; the surviving C99 realizer refuses outside its slice by name, and
   the wasm realization answers by value.
