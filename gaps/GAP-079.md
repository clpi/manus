# GAP-079 — SUPERSEDED BY GAP-080 (duplicate filing of one finding)

**Status:** SUPERSEDED · **Filed:** 2026-08-08 · **Closed:** 2026-08-09

| # | directive |
|---|---|
| 1 | This gap and `gaps/GAP-080.md` are **the same finding, filed twice within hours** by two sessions that could not see each other's work: *the compiler is the one corpus the deny table cannot see.* Same title, same law citations (§0.1 LAW-ONE · §0.3 INFERRED CASES · `law.arch.relation` · `law.host.projection` · `law.add.open`), same five sections. |

| # | directive |
|---|---|
| 1 | **GAP-080 is the live one and carries strictly more:** it ships the instrument. `zig build bootstrap-scan` (`scripts/bootstrap_scan.id`) ratchets DOWN from a measured baseline — **upper 770 · companion 47 · relation 47 · 864 sites** — over tracked files only, positive-controlled two ways before any number prints. |
| 2 | This file has the argument and no gate. |

| # | directive |
|---|---|
| 1 | Its sharpest datum is worth repeating here so a reader of 079 does not miss it: `addNode`/`addEdge`/`addChild` are 3 of **47** `add*`/`register*`/`emit*` host functions carrying a semantic specialization in their name. **The archetype is not three functions; it is a habit with 47 instances.** |

| # | directive |
|---|---|
| 1 | Nothing is lost by closing this number. |
| 2 | Read GAP-080. |

## Why this is recorded rather than deleted

| # | directive |
|---|---|
| 1 | The collision is itself the finding that `gaps/GAP-076` names — *"the gap allocator is atomic across PROCESSES, blind across BRANCHES"*. |
| 2 | Two sessions each ran `ls gaps/ \| tail -1`, each got the same answer, and each wrote a different number for one defect. |
| 3 | It happened at least three times on 2026-08-08/09: this pair, a clobbered GAP-065 recovered as GAP-067, and two gap files overwritten mid-write during the stable-id work. |

| # | directive |
|---|---|
| 1 | Deleting the file would erase the evidence that the allocator is broken. |
| 2 | Leaving it OPEN would double-count a defect in every "how many gaps remain" measurement — and that count is a gate input. |
| 3 | SUPERSEDED is the honest third option: one live finding, one live number, and the collision still legible. |
