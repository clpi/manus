| field | value |
|---|---|
| title | GAP-237 — a method applied directly to an indexed element segfaults through the generated-C bridge |
| status | OPEN |
| filed | !2026-08-25T09:20:00Z |
| priority | P1 |
| kind | wrong_answer |
| filed by | gap133-alignment |
| files | src/codegen.zig |

| # | directive |
|---|---|
| 1 | Tree `fc48acea`, executed path `idol dump-c` + cc (the direct backend answers DNB004 on this host). `rows[i]:relation()` — a method applied in the same face as the computed projection that produced its subject — compiles without diagnostic and the emitted binary dies with SIGSEGV before any output: |

```id
main: i64 = ()
    t: str = "  hello law  "
    rows = t:split(" ")
    v: str = rows[3]:trim()   # SIGSEGV at runtime; no compile diagnostic
    print("c [{v}]")
    0
```

| # | directive |
|---|---|
| 1 | Binding the element first is correct on the identical input: |

```id
    r: str = rows[3]
    v: str = r:trim()         # prints as expected
```

| # | directive |
|---|---|
| 1 | The class, not the specimen: the subject of the method arrives from a computed projection and is still boxed/unnarrowed when the method lowering selects its receiver face. `:trim` is the measured specimen; whether every string relation on an indexed receiver dies the same way is unmeasured — the fixture below must sweep the family rather than pin one method. `scripts/alignment_emit.id` was the discovery site and now binds every indexed element before applying a relation, and says why. |
| 2 | This is the GAP-114 family — a boxed receiver reaching a representation-specific intrinsic — but the outcome is memory-unsafe rather than a plausible wrong value, so `idol check` green plus a segfault is the entire observable. |

| # | directive |
|---|---|
| 1 | What closes this: the lowering either narrows the indexed element like an annotated binding would, or refuses the unprovable receiver at compile time with a DNB diagnostic. |
| 2 | A fixture must assert the VALUE of a `table[index]:method()` chain through the generated-C bridge, control (pre-bound) first. |
| 3 | Generated-C evidence only; not direct-native or wasm evidence. |

| # | directive |
|---|---|
| 1 | Adjacent measured limits on the same bridge, recorded here so the next reader does not rediscover them: `os.env(name)` and `os.args` refuse honestly at codegen (unrealized callee / unrepresentable binding) — diagnosed, so merely missing, not silent; scripts executed through the bridge take configuration from the working directory instead (see `scripts/alignment_emit.id`). |
