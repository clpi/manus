# GAP-088 — the constructor ladder's first rung does not lower, and syntax cannot fix it

**Status:** MOSTLY STALE — rung (a) LANDED · **Filed:** 2026-08-08 · **Re-measured:** 2026-08-09

## RE-MEASURED 2026-08-09 — the ROOT ROW is closed

The headline — *"the constructor ladder's first rung does not lower"* — is no
longer true. Re-run of the gap's own table, by value, `--backend=direct` and
`--backend=c` agreeing on every passing row:

| form | filed | now |
|---|---|---|
| `p: point = { x = 3.0, y = 4.0 }` | WORKS | WORKS |
| `point{ 3.0, 4.0 }` — spec §15.1's own example | FAILS | **WORKS** |
| `point{ x = 3.0, y = 4.0 }` — rung (a), named | FAILS | **WORKS** |
| `Point{ x = 3.0, y = 4.0 }` — the brief's spelling | FAILS | **WORKS** |
| `feet{ 3.0 }` over `feet: @{ v: f64 }` | FAILS | **WORKS** |
| `@{ 0.0, 0.0 }` — spec §6's own example | FAILS | FAILS — **but see below** |
| `3.0:to(feet)` — conversion edge | FAILS | FAILS |
| `point.origin()` — calling a descriptor slot | FAILS | FAILS |

Read BY VALUE, not by "it compiled". Both fields are read back through
`p.x * 100.0 + p.y`, so a construction that filled one slot and left the other
zero is convicted: named reads **304**, positional **607**, the typed-binding
control **102**, identically on both backends. `error: call to undeclared
function 'point'` is gone.

So the sentence that made this a ROOT BLOCKER — *"the canonical form does not
exist"* — is retired. `desc{…}` is writable today, and the §41 SELF-ZERO form
`point{ x *= k, y *= k }` can now be written.

### One claim in this gap was WRONG when filed, and the correction matters

> `@{…}` is *already* in the grammar (Pass 108) … **It parses.** It does not lower.

It does **not** parse in VALUE position. Measured:

```
n.id:4:6: error: expected 'name', got '{'
error: parse failed: ExpectedToken
```

`@{ … }` parses only in DESCRIPTOR-DECLARATION position (`point: @{ x: f64 }`).
That changes what is owed: the remaining `@{…}` work is a PARSE gap, not the
lowering gap this file argued it was, and the NNS reasoning built on "it already
parses" does not apply to it.

### What is still owed — narrow it to these three

- `@{ … }` in value position — **parser**, per above.
- `3.0:to(feet)` — a conversion edge into a descriptor. Emits
  `invalid suffix '__to' on floating constant`; this is the same wall
  gap[106] names (a descriptor cannot attach to a representation).
- `point.origin()` — a descriptor SLOT as a callable: `use of undeclared
  identifier 'point'`.

Rungs (b) and (d) are what remains. Rung (a) is done.

---

**Original filing follows, retained for the record.**
**Severity:** ROOT BLOCKER. Upstream of gap[082] scaling, gap[087], §41's
canonical form, and the entire constructor ladder.
**Law:** CLAUDE.md §0.4 CONSTRUCTOR LADDER · Pass 100 §6, §15.1 · `law.canon.performance`

## Measured 2026-08-08

| form | source | result |
|---|---|---|
| `p: point = { x = 3.0, y = 4.0 }` | typed binding + bare table | **WORKS** — prints 3 |
| `point{ 3.0, 4.0 }` | **spec §15.1's own example** | **FAILS** |
| `point{ x = 3.0, y = 4.0 }` | ladder rung (a), named | **FAILS** |
| `@{ 0.0, 0.0 }` | **spec §6's own example** | **FAILS** |
| `point.origin()` | calling a descriptor slot | **FAILS** |
| `feet{ 3.0 }` over `feet: { v: f64 }` | ladder rung (a) | **FAILS** |
| `3.0:to(feet)` | conversion edge into a descriptor | **FAILS** |

```
error: call to undeclared function 'point'
```

`point{ … }` lowers to a CALL to `point`, and nothing declares it. The
constructor ladder's rung (a) — `desc{…}`, the form §0.4 makes canonical while
BANNING `.new`/`.create`/`make_*`/`init` — **is not implemented**. The banned
alternatives are banned and the canonical form does not exist.

Exactly one construction path works, and it is the one nothing in the spec
writes.

## SYNTAX DOES NOT UNBLOCK THIS, and that is the finding

The question asked was whether minimal `.` or `@` syntax opens this up. It does
not, and inventing some would be the mistake A2 exists to prevent.

**The working form already needs no new syntax.** `p: point = { … }` is a typed
binding plus a bare table — two forms that already existed, composed. So:

- the SEMANTICS exist. A descriptor-shaped table with typed fields lowers
  natively today; that is what the working row proves.
- what is missing is only the NAMED CONSTRUCTOR PATH.
- `@{…}` is *already* in the grammar (Pass 108 `construct ::= "{" …`) and
  already means enclosing-descriptor construction. It parses. It does not lower.
  Adding syntax beside a form that already parses and fails to lower would add a
  second spelling for one missing lowering.

NNS says: *if a proposed repair needs new surface, you have not yet found the
existing form it is hiding.* Here the existing form IS found. **The repair is
that `point{…}` and `@{…}` must lower to what `p: point = {…}` already lowers
to.** That is a codegen repair, and no surface change is licensed.

## Why this is the root blocker

- **gap[082]** reported "a user descriptor cannot be attached to a value" and
  named it the thing capping the conversion algebra at N=4. Measured here, the
  cause is narrower and deeper: construction never worked, so there was never a
  value to attach.
- **gap[087]** found descriptor construction with a case field failing for BOTH
  the dotted and bare spellings. Same cause. It is not a dot question.
- **§41's canonical SELF-ZERO form** is `point{ x *= k, y *= k }` — rung
  (a). It cannot be written today.
- **The constructor ladder** (§0.4) has four rungs. Rung (a) does not lower,
  rung (c) `from(mode)` does not exist (recorded previously), and rungs (b) and
  (d) both produce values that rung (a) would have to construct.

## Already known, and that is its own finding

`duo-constructor-ladder-state` records: *"Pass 86 `from(mode)` does not exist and
`desc{...}` is broken; do NOT repair the 24 `from_*` sites in lib/std, the
target form won't compile."*

So this was measured before and written down, and the tree has been legislating
`desc{…}` as canonical ever since — including in a constitution that cannot be
migrated to it. A known-broken canonical form is worse than an unknown one: it
teaches every reader a spelling that fails.

## Fix

`point{ … }` lowers exactly as `p: point = { … }` lowers. The named path
resolves the descriptor, then emits the same shape the typed binding emits.
`@{…}` resolves the enclosing descriptor and does the same.

Fixture asserts BY VALUE, and asserts the two forms agree:

```
p: point = { x = 3.0, y = 4.0 }     -- works today
q = point{ x = 3.0, y = 4.0 }       -- must equal p field for field
r = point{ 3.0, 4.0 }               -- positional, §15.1
```

on BOTH backends. A construction that compiles and produces a zeroed record is
this tree's known failure mode — `tostring(d)` once reported `d=table` for a
decoder returning an EMPTY table, and a bisect built on that was wrong. Assert
field contents, never that it built.

## Do not

Do not add syntax. Do not repair the `from_*` sites. Do not migrate the
constitution to `point{…}` until this lands — `law.canon.performance` forbids a
canonical form that lowers worse than the one it replaces, and right now it does
not lower at all.

---

## ATTEMPTED 2026-08-08, REVERTED — and the revert is the finding

I implemented the repair in `src/codegen.zig`: recognise `desc{…}` where the
callee name resolves through `record_aliases`, and lower it as a C compound
literal through `emit_record_initializer` — the SAME function the working typed
binding already uses. The design was right and needs no new syntax.

**Reverted on the standing directive: NO ZIG, NO C.**

That leaves a stated conflict rather than a repair, and the conflict is worth
more than the patch would have been:

- **The constructor ladder is broken in the bootstrap, and only in the
  bootstrap.** `emit_record_initializer` lives in `src/codegen.zig`. There is no
  Duo-native owner of record construction to fix instead — `lib/std/compiler/`
  has a lexer, a parser and a projection, and NO code generator that emits
  records.
- So the repair is currently *only* expressible in the language the directive
  excludes. That is not an argument for making an exception. **It is the
  measurement of how far self-hosting actually is on this axis**, and it is
  sharper than any LOC count: a canonical language form cannot be fixed in the
  language.

## What this makes the real work

Not "patch codegen". The ordering the directive implies:

1. **A Duo-native owner for record construction** — the self-hosted compiler
   needs a lowering path that emits records, at which point rung (a) is a Duo
   repair and the bootstrap copy becomes `law.host.projection` debt with this
   gap as its deletion gate.
2. Until then `desc{…}` stays broken and **the constitution stays unmigrated**,
   because `law.canon.performance` forbids canonicalizing onto a form that does
   not lower.

Recorded so the next agent does not spend the same hour rediscovering that the
obvious fix is in the excluded language.

---

## THE BLOCKER IS SMALLER THAN RL-04 STATES — measured 2026-08-09

Both halves of the missing capability now exist in duon and are proven:

| owner | answers | proof |
|---|---|---|
| `lib/std/compiler/symbol.id` | what C symbol a duon name takes | `symbol_proof` 13 rows, controls exit 3 |
| `lib/std/compiler/record.id` | what applying a descriptor emits | `record_proof` 5 rows, `law.pack` asserted |

`record.id` emits `((duo_point){.x = 3.0, .y = 4.0})` — a compound literal,
not a table — which is rung (a) of the ladder, the exact form this gap says does
not lower.

**And the structural bridge exists too.** `src/removal_ledger.zig` RL-04 records
the blocker as *"parser.id emits sexpr TEXT, not the AST sema and codegen
consume"*. That is now only half true: **`lib/std/compiler/eval.id` carries
`scan(src: str): any`, a duon reader that turns exactly that text back into
tables** — 1100 lines, with a measured fixed point of 48 held / 0 broken.

So the path is complete in duon, end to end:

```
parser.id  ->  sexpr text  ->  eval.scan  ->  tables
                                                 |
                              symbol.id + record.id  ->  C
```

Not one piece of that is missing. What is missing is the WIRING, and one merge.

## What actually remains

`eval.id` is on branch `pass104-evaluator`, unmerged, and merging it conflicts
in four places — `build.zig`, `scripts/audit100.id`, and `gaps/GAP-072.md` /
`GAP-073.md` from the numbering collisions. Two of those are shared hot files
that other sessions are actively editing, so it is a real merge and not a
hurried one.

**The honest correction to the frontier**: this is no longer "the self-hosted
compiler has no code generator". It is "three duon pieces exist, are proven
independently, and have never been connected." That is a materially smaller and
more tractable statement, and it is what the next agent should be given instead
of RL-04's.

---

## THE ROOT IS THE PARSER — measured 2026-08-09, and it is `law.apply.one` violated

Wiring the three duon pieces together failed at the first step, and the reason
is upstream of every emitter. The self-hosted parser's projection:

```
p = point(3.0, 4.0)     ->  (program (assign p (call point 3.0 4.0)))
p = point{ 3.0, 4.0 }   ->  (program (assign p point) (desc (item 3.0) (item 4.0)))
p = point{ x = 3.0 }    ->  (program (assign p point) (desc x 3.0))
p = @{ 3.0, 4.0 }       ->  (program (assign p (at)) (desc (item 3.0) (item 4.0)))
```

**The paren face produces ONE application node. The brace face produces TWO
disconnected statements** — the name binds alone, and the pack is torn off as a
sibling `(desc …)` at top level.

So there is no construction node to walk. `symbol.id` and `record.id` are
correct and proven, and they cannot be reached, because the tree they would walk
does not contain the application.

## This is `law.apply.one` violated in the parser

§43a rules that duon has ONE application mechanism and that parenthesized and
braced faces PROJECT ONTO THE SAME semantic application relation. Measured, they
project onto two different tree SHAPES — which is the same defect §44 diagnosed
at the codegen layer, present one layer earlier and worse: codegen at least
received a call node it could mis-lower. The parser emits no application at all.

It is also trap 1 from §43a, in its original location: the split this law
deletes, sitting in the parser rather than relocated to codegen.

## Already recorded, unconnected

`gaps/GAP-046.md` §3 lists this under **"Accepted but WRONG (2)"** —
*"`m = nn { relu }` → `(program (assign m nn) (desc (item relu)))` — the block is
torn off into a separate top-level statement."* Filed as a projection defect in
a coverage audit. Nobody connected it to the constructor ladder, because at the
time rung (a) was thought to be a codegen gap.

**It is the same defect, and it is the root one.** gap[046] found it, gap[088]
found its symptom, and §43/§44 named the law it breaks — three separate
findings about one node.

## The corrected repair order

1. **The parser emits ONE application node for the brace face**, exactly as it
   already does for the paren face. `(call point 3.0 4.0)` is the shape that
   works; the brace face must reach the same shape with its pack attached.
2. Then `eval.scan` yields a walkable construction.
3. Then `record.id` emits it — already proven to compile and answer 3 and 4.

Step 1 is a duon change in `lib/std/compiler/parser.id`. Nothing about it needs
the bootstrap, which makes it the first piece of this chain that the standing
directive does not exclude.
