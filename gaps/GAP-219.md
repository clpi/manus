| field | value |
|---|---|
| title | GAP-219 — three PR-review findings that are real but not closed here |
| status | OPEN |
| priority | P1 |
| kind | correctness_gap |

| # | directive |
|---|---|
| 1 | Raised by automated review on PR #70. |
| 2 | A fourth finding from the same review (the attribution gate scoring failed compiler runs as clean compiles) WAS a live defect and is fixed in `aa46940c`. |
| 3 | These three are real and open. |

| section |
|---|---|
| 1. Field-write collection matches a shadowing binding by spelling |

| # | directive |
|---|---|
| 1 | `registerTableFieldGlobals` -> `collectFieldWriteTypes` scans "every write anywhere in the module, module scope and relation bodies alike" for `name.field`. |
| 2 | A relation that declares a parameter or local ALSO named `name` writes its own binding, but the scan matches by SPELLING and counts it as a write to the module table. |

| # | directive |
|---|---|
| 1 | MEASURED, and it is milder than reported. |
| 2 | A local shadow |

| # | directive |
|---|---|
| 1 | M = { x = 1 } bump: i64 = () M = { x = 5 } M.x = 99 M.x main: i64 = () a = bump() M.x |

| # | directive |
|---|---|
| 1 | REFUSES with `global-init-not-constant:M`. |
| 2 | It does not emit a read or mutation against the wrong binding, so this is not a live wrong-answer bug. |
| 3 | A parameter shadow refuses earlier still, at `param-type:inferred`, because a relation taking a table parameter is outside the native scalar subset. |

| # | directive |
|---|---|
| 1 | It remains a defect: the refusal is incidental rather than intended, and it is identity-by-spelling in a project whose whole thesis is that spelling never decides identity. |
| 2 | The fix is to stop descending into scopes where the base name is rebound, and apply the same scope handling to the sibling scans (`moduleHasFieldWrite`, `moduleWritesNameBySubscript`). |

| # | directive |
|---|---|
| 1 | ACCEPTANCE: a fixture where the shadow case COMPILES and answers the module value, plus a negative control proving the non-shadowed write still takes storage. |

| section |
|---|---|
| 2. `lib/compiler/token.id` carries noncanonical identifiers |

| # | directive |
|---|---|
| 1 | The executable grammar owner uses `KIND_TEXT_LIT`, `KIND_COMPAT_LONG_TEXT_LIT`, `ROLESLOTCOUNT` and the mashed `rolebeginexpr`. |
| 2 | Uppercase and underscores are denied for canonical `.id`, and `rolebeginexpr` violates one-lowercase-word. |

| # | directive |
|---|---|
| 1 | Partly inherited: 104 of the uppercase identifiers predate the transfer, which added roughly 78 more. |
| 2 | A sibling commit (`06feca94`) already lowercased seven in the generated projection. |

| # | directive |
|---|---|
| 1 | Not fixed inline because the names are load-bearing in two GENERATED artifacts (`src/grammar_role_table.zig`, `lib/token/grammarrole.id`) and in `src/parser.zig`'s consumers; renaming is a mechanical pass that must regenerate both projections and prove byte-identity through `gate/grammar-projection.sh`. |
| 2 | It is a rename, not a redesign, and it should happen before anything else generates from this owner. |

| section |
|---|---|
| 3. Path names violate `law.path.name`, and its gate is broken |

| # | directive |
|---|---|
| 1 | `AGENTS.md:550-561` forbids snake_case, kebab-case and mashed stems in project-controlled path components. `src/grammar_role_table.zig`, `gate/grammar-projection.sh`, `gate/module-zero-precheck.sh` and `examples/keyed_table_*.id` all violate it. |

| # | directive |
|---|---|
| 1 | Two facts decide how to treat this. |
| 2 | The rule is essentially unenforced in the trees involved — `src/` already carries `abi_resource.zig`, `assumption_guard.zig`, `async_lower.zig`, `semantic_graph.zig`, and `gate/` already carries `architecture-companion.sh`, `cache-home.sh` — so renaming four new files while ~200 siblings keep the same forms buys consistency with the law at the cost of consistency with the tree. |

| # | directive |
|---|---|
| 1 | And the enforcer is broken: `gate/path.id` fails its own positive controls. |

| # | directive |
|---|---|
| 1 | MEASURED SUBJECT, pinned. |
| 2 | Compilers built from `idol/main` at `7483b1b4` (and the identical `gate/path.id` at local `f5d87d40`, byte-identical between the two). |
| 3 | Instrumented arms and the contaminated worktree build are named individually below. |
| 4 | Relative `HEAD` labels are deliberately avoided: once the branch advances they name nothing. |

| # | directive |
|---|---|
| 1 | CORRECTED MEASUREMENT — the first reading of this named `control FAIL: utils organizational home not rejected`, which was taken with a binary built from the working tree while it carried another lane's uncommitted changes. |
| 2 | That binary fails TEN controls; a compiler built from clean HEAD fails TWO, and `utils` is not among them. |
| 3 | Verified across four binaries (pre-projection-fold, `7483b1b4`, `7483b1b4`+one-producer, and a build from the contaminated live worktree): the first three all report exactly 2, the worktree build reports 10. |

| # | directive |
|---|---|
| 1 | The two genuine failures at `7483b1b4` are: |

| # | directive |
|---|---|
| 1 | control FAIL: view(edges) curried binding rejected control FAIL: zerohistorycensus mashed census path not rejected |

| # | directive |
|---|---|
| 1 | The conclusion is unchanged and the evidence for it is now correct: a gate whose own positive controls fail cannot be trusted to reject anything, which is why every commit in PR #70 passed it. `utils` in particular IS handled correctly at `7483b1b4` — `known("utils")` is false (it is absent from the `words` list) and `member(orghomes, "utils")` is true, so `orghome` fires as designed. |

| # | directive |
|---|---|
| 1 | A separate finding falls out of the same measurement: the uncommitted working-tree changes cause EIGHT additional control failures on top of the two, in the same tree whose changes also make `lib/compiler/token.id` emit every token kind with an empty name. |
| 2 | That is the higher-priority defect of the two: a law with a non-functioning gate is decoration, and repairing the control must come before any bulk rename, or the rename cannot be verified. |

| # | directive |
|---|---|
| 1 | ACCEPTANCE: repair `gate/path.id`'s positive control first; then decide tree-wide rename or explicit scoped exemption, and record which. |

| section |
|---|---|
| Measured transfer past tip 394127cd |

| # | directive |
|---|---|
| 1 | Finding 1 (shadow field write by spelling): the scope-aware walk already at tip (`walkBindingAssigns` in `src/graph/lower.zig`) had no fail-closed derived authority — inline scans only. |
| 2 | Transfer: `src/graph/field.zig` holds one producer (`build` derives a scope-aware field-write index from the authority write set, skipping shadowed entries, with a content checksum) and two consumers (`hasWrite`, `writeKind`). |
| 3 | Each consumer re-checks length plus checksum, then re-reads the authority write on every candidate it reports — a perturbed index entry or a drifted authority entry returns `error.StaleIndex`, never a wrong admit or a wrong kind; genuine kind disagreement declines to null, mirroring the agreed=false decline. |
| 4 | The intended false-accept is a shadowed `M.x` write counted as a module-table write; the missing consumers are scope-aware `hasWrite`/`writeKind` keyed by binding identity instead of spelling; the predicate that fails closed before any PASS is the length-plus-checksum re-check with per-candidate authority re-read. |
| 5 | Perturbation probes on both sides (index flip, authority scope drift, cross-set reuse, phantom-read-without-consumer) are executed in the module tests, with a shadow-only field as negative control and a surviving module write as positive control. |
| 6 | Reproduce with `tools/node/dev/idol-lock -- zig test src/graph/field.zig`. |

| section |
|---|---|
| Measured transfer past tip 0b8a1dfe (wiring) |

| # | directive |
|---|---|
| 1 | Production consumer: `moduleHasFieldWrite` in `src/graph/lower.zig`, at both call sites — field-storage admission in `registerTableFieldGlobals` and the const-stability check in `collectModuleConsts`. |
| 2 | Older path replaced: the inline `FieldWriteScan` visitor, deleted with no remaining references. |
| 3 | The consumer collects the same scope-aware `walkBindingAssigns` walk into authority `Write` rows, derives the `src/graph/field.zig` index (`fieldindex.build`), and answers through `fieldindex.hasWrite` (the `fieldWriteExists` seam); a dropped fact maps to `error.GraphFactsInvalid`, refusing the lowering instead of admitting storage or folding a constant. |
| 4 | Fail-closed probe: `fieldindex: production existence refuses a drifted authority set` (a cross-set query returns `StaleIndex`, and the seam maps it to `GraphFactsInvalid`), beside the module-level `fieldwrite` pair. |
| 5 | Positive and negative controls: `fieldindex: module field write reaches the index consumer` answers true, while `fieldindex: shadow-only field write stays absent from the index consumer` answers false on the `M: cell` shadow program from Finding 1. |
| 6 | Parity after wiring: `shadow` 14/14, `GAP-204` 5/5, `fieldwrite` 3/3, `table` 77/79 with 2 skips on macOS-only platform guards in untouched `src/native.zig`. |
| 7 | Reproduce with `tools/node/dev/idol-lock -- zig build unit-test -Dtest-filter="fieldindex"`. |
| 8 | The `parser/artifact` step failure is unchanged before and after (pre-existing on a clean tree: `zig-out/bin/idol` interplay plus `lib/compiler/parser.id` self-host debt). |
