| field | value |
|---|---|
| title | GAP-189 — recovery / continuation algebra |
| status | OPEN |
| filed | !2026-08-18T16:20:00Z |
| priority | P1 |
| kind | research_gap |
| filed by | devin-semantic-20260818 |
| files | docs/spec/constitution.md, docs/spec/law.md, src/semantic_graph.zig, src/sema.zig |

| # | directive |
|---|---|
| 1 | Effects are not only "what occurred." A condition is a semantic fact, and the admissible recovery continuations are themselves semantic capabilities. |
| 2 | This unifies return, exception, yield, async, generator, backtracking, restart, coroutine, and transaction retry. |

| section |
|---|---|
| Current measured boundary |

| # | directive |
|---|---|
| 1 | `effect` is a graph fact; `world` models authority. |
| 2 | There is no `condition` or `continuation` fact family, and no list of available restarts. |

| # | directive |
|---|---|
| 1 | **Crossed:** |

| # | directive |
|---|---|
| 1 | `law.effect` and `law.world` exist in C0. |
| 2 | `async`, `exception`, `yield` are mentioned as research targets. |

| # | directive |
|---|---|
| 1 | **Still missing:** |

| # | directive |
|---|---|
| 1 | A continuation is an ORDINARY ID with facts: resumption cardinality, escape, captured world, effects crossed, lifetime — no `Continuation` kingdom. |
| 2 | A condition is an obligation/effect fact carrying its payload — no `Condition` kingdom. |
| 3 | `restarts(condition)` = set of admissible recovery relations. |
| 4 | Realization table: never invoked → erase; nonescaping once → direct branch; escaping once → movable frame; multiple invocations → persistent/copied continuation; only failure → exception/result branch; only generator → state-machine cursor. |
| 5 | Compiler conditions as first-class events: `missing proof: nonalias(x,y)` with restarts `guard`, `profile`, `copy`, `generic`, `refuse`. |

| section |
|---|---|
| Required order |

| # | directive |
|---|---|
| 1 | Express continuations as ids with resumption/lifetime/effect facts and conditions as obligations with available recovery relations — no new kinds. |
| 2 | Add facts for capture count, escape, dynamic extent, effect obligations. |
| 3 | Implement `restarts(condition)` as a graph relation. |
| 4 | Derive exception/result, yield, async, generator, backtracking, restart from the same algebra. |
| 5 | Use the algebra for compiler diagnostics (conditions with restarts). |

| section |
|---|---|
| Closure acceptance |

| # | directive |
|---|---|
| 1 | Close when `return`, `exception`, `yield`, `async`, `generator`, `backtracking`, `restart`, and `retry` are all realizations of one continuation/condition algebra, and compiler conditions expose available restarts. |

<!-- idol-gap-c0:v1:begin -->
<!-- c0-schema: Instance of the schema in `gaps/RESEARCH-SPINE.md`; `tools/node/dev/gapc0` fails closed without it. -->
| field | value |
|---|---|
| constitutional owners | id (a continuation is an id with facts) · obligation (a condition is an obligation/effect fact) · relation (recovery relations) · world (captured world) · lifetime · effect |
| kind delta | 0 |
| registry delta | 0 |
| source syntax delta | 0 |
| semantic invariant | `restarts(condition)` is the set of admissible recovery relations for an obligation — return, exception, generator, yield, await, backtracking, transaction retry, and CL restart are competing realizations, not core constructs. |
| physical mechanism | realization table — never invoked → erase; nonescaping once → branch; escaping once → movable frame; many → persistent continuation; failure-only → result/error path; generator-only → state-machine cursor. |
| prerequisites | GAP-187. |
| foreign implication | foreign exception systems and coroutine frames ingest as resumption/effect facts. |
| world implication | a continuation captures the smallest exact necessary world fact, never the whole world. |
| projection implication | restarts are projected like every other relation set. |
| demand implication | demanded resumption count selects the realization (zero demanded resumptions ⇒ erase). |
| realization implication | compiler conditions become first-class events (`missing proof: nonalias(x,y)` with restarts `guard`, `profile`, `copy`, `generic`, `refuse`). |
| evidence | one algebra realizing at least an exception path and a generator path with measured parity. |
| deletion witness | exception hierarchies, continuation object models, async runtime kingdoms. |
<!-- idol-gap-c0:v1:end -->
