# GAP-166 — MODULE-ZERO: delete native_req_support

**Status:** OPEN · **Priority:** P0 · **Kind:** architecture
**Owner:** Poolside / Cursor (bootstrap bridge deletion)

## Current obligation

`src/native_req_support.zig` is **deleted**. Dotted-callee, module-alias export,
and constant-fallback paths in `src/dnir_lower.zig` now fail closed with no
host rediscovery.

**Remaining bridge:** `src/codegen.zig` still carries `req_module_bindings`,
`find_module_file_for_req`, and C-prefix dispatch for embedded native-direct
modules. This is path/alias reconstruction after resolution and violates
MODULE-ZERO until graph linkage facts replace it.

## Replacement

```text
source reachability → binding/home facts → semantic ids → linkage/ABI at realization
```

No semantic lookup through `source_path`, module alias, dotted path, or C prefix
after resolution.

## Deletion condition

1. Direct-native lowering reads relation/application ids from graph facts.
2. No caller uses path-based export symbol lookup (former `exportSymbolByPath`).
3. `req` admission in source reaches zero on canonical lines.
4. Positive control: rename module alias → same relation id in lowering.
5. Negative control: remove fallback → fail closed, not host rediscovery.

## Bridge record

| field | value |
|---|---|
| responsibility | load lib modules for direct-native constant/export lookup |
| semantic authority | none — path/alias reconstruction |
| replacement owner | resolver + graph linkage facts |
| facts lost today | export symbol spelling, constant scan |
| deletion gate | graph linkage supplies same facts |

## Progress (2026-08-12)

- `native_req_support.zig` deleted; no `@import("native_req_support")` in `src/`.
- `duo_native_ir.zig` → `src/native_ir.zig`; gates updated.
- `ParamAbi.duo` → `ParamAbi.native` in codegen.
- `convergencecensus` P0-B subset tracks deletion + retired IR name.

## Acceptance

- File deleted or reduced to foreign-linkage bootstrap only with written deletion date.
- `convergencecensus` passes MODULE-ZERO checks for new code paths.
- `req_module_bindings` and path-based module resolution removed from codegen.
