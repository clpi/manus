# GAP-131 — Session bootstrap reports false empty P0 queue

**Status:** OPEN · **Filed:** !2026-08-09T21:08:55Z
**Priority:** P0 · **Kind:** agent
**Filed by:** codex_semantic_normalization_continue
**Files:** tools/mcp/duo_bench.duo,tools/mcp/duo_lsp.duo,tools/mcp/duo_shared.duo,gaps

duo_agent_session_start returned open_p0_count 0 and 'Coordination buffer not yet initialized' while the live checkout contains multiple OPEN P0 gap files including GAP-124, GAP-125, GAP-126, GAP-127, GAP-128, and GAP-129 and active claim state exists. Session bootstrap therefore gives every new parallel agent a false empty obligation queue. Derive the count and routing summary from the canonical per-gap files plus live claims, preserve run provenance, and positive-control a known open gap.

## 2026-08-09 positive-control failure

The uncommitted MCP repair adds a session-bootstrap check and the exact locked
gate passes:

```text
zig build mcp-gate --summary all
  ok   duo_agent_session_start routes without retired control-plane errors
mcp-gate: PASS
```

In the same tree, an actual `duo_agent_session_start` call still returns:

```text
open_p0_count: 0
coordination_excerpt: Coordination buffer not yet initialized.
```

The checkout contains fifteen files with explicit P0 labels. The new check only
asserts that the response has an id, mentions `AGENT_CANONICAL.md`, and does not
contain `unknown command`; it never checks the obligation value. This is a
false-positive gate.

The required positive control is semantic: select or inject one known OPEN P0
gap, assert that its stable gap id is returned, and assert that the reported
count agrees with the canonical `gaps/GAP-*.md` records. A handshake or path
string is not evidence that session obligations were loaded.

## 2026-08-10 ROOT-ZERO admission

The current dirty repair is rejected. It adds `gapcensus` to
`tools/mcp/duo_shared.duo`, runs a shell loop over gap files, parses `grep`
output, and exports that new status owner to both MCP servers. A correct count
would not make this admissible: it grows project-owned `.duo`, process and
filesystem namespace authority, and the host agent control plane.

Until an executed Idsem owner projects the exact gap facts, session start must
return an explicit incomplete/unknown census and must never translate missing
evidence into zero. The bootstrap MCP may pass an already-owned fact physically;
it may not derive semantic project status from paths and shell text. Closure
requires the positive control above on that Idsem projection and deletion of
the bootstrap parser, not promotion of the dirty implementation.

## 2026-08-11 combined-header negative control

The current dirty `gapcensus` remains incorrect even as a temporary physical
projection. Its shell expression requires `**Status:**` and `**Priority:**` to
start separate matching lines. Current gap records `GAP-092`, `GAP-141`, and
`GAP-142` carry both facts on one header line, so the implementation omits all
three.

An authority-independent current-tree scan finds 30 OPEN/P0 files. Applying
the dirty implementation's two expressions would return 27. Closure therefore
also requires a positive control covering both admitted gap-header layouts and
an equality assertion against the exact current-tree census; merely changing
the displayed field name from `open_p0_count` to `p0count` is not evidence.

## 2026-08-10 rejected host admission aggregate

The reviewed `.githooks/pre-commit`, `build.zig`, CI, and historical idiom-gate
changes were withdrawn. Expanding the shell hook into an admission engine would
create a foreign control plane and preserve the wrong owner. The rejected form
also had these concrete fail-open defects:

1. Aggregate `source-admission` checks `.duo` paths added since the law anchor,
   then exits after admitting the complete tracked `.id` snapshot. It never
   checks lines added to already tracked `.duo` files across that revision
   range. An agent can therefore extend any of the 910 tracked historical files
   with `--`, `std.*`, legacy syntax, or semantic host behavior without adding a
   path and escape this projection if local pre-commit is bypassed.
2. The dirty `scripts/idiomgate.duo` edits add their own `--` comments and a new
   predicate helper while teaching the hook to reject those same forms on
   touched historical source. The staged pre-commit should correctly reject
   this self-host contradiction; changing only comment punctuation would not
   repair the continued historical-source and helper architecture.
3. The current rule recognizes only `std.`. It does not cover `std:`, `req
   "std..."`, generated native source, renamed universal roots, or semantic
   authority inferred from package path.
4. No current aggregate control rejects newly exported `NodeId`, `NodeKind`,
   `EdgeKind`, `SemanticId`, stable/path/hash identity, or semantic `*Id`
   wrappers. The shared dirty graph demonstrates that this is not hypothetical:
   the exact current source census contains 194 `NodeId`, 15 `NodeKind`, six
   `EdgeKind`, and five `SemanticId` references.
5. No current control proves canonical resolution cannot reach `lua_Value`,
   `lua_table_*`, `lua_to_*`, or `lua_val_*` after a direct realization refuses.
   A text census is useful pressure but is not the required reachability
   negative control.
6. PREDICATE-ZERO and SENTINEL-ZERO require semantic classification. Adding
   more name regexes cannot prove whether a truth relation is irreducible or a
   helper erased case, unknown, transition, descriptor, world, demand, or
   realization facts.

Do not repair or enlarge the shell gate. The replacement owner is an executed
canonical `.id` admission service over an exact candidate snapshot. It consumes
producer-issued lexical identities, generated grammar roles, graph facts,
demand, realization and lineage; it returns structured canonical findings and
projects one verdict to commit admission, CI, MCP, LSP and release. Repository
and process access require admitted world facts. Until that vocabulary and the
GAP-145, GAP-134 and GAP-124 producers exist, the result is
`SEMANTIC-VOCABULARY-BLOCKED`, not another host scanner. The existing hook is
deletion debt and may only shrink. GAP-131 remains OPEN until the Idsem service
and live session response both fail closed under intentional bypass controls.

## 2026-08-10 IDSEM ONE admission controls

Law revision `91d05a1` closes the destination ambiguity: semantic identity is
`id`; facts qualify it; public node, kind, edge, typed identity, stable identity,
and layer identity vocabularies are forbidden. The current hook has no control
for this boundary. Its only anchored aggregate is `source-zero`; no named
`idsem-admission` aggregate exists on current main.

The bootstrap ratchet and its eventual Idsem replacement must prove all of the
following independently:

1. Adding any forbidden public semantic identity concept fails even if the same
   change deletes a different forbidden concept. A pooled total is fail-open:
   deleting one `StableId` must not buy permission to add one `NodeId`.
2. Private dense integers, array offsets, compact tags, and graph rows remain
   representation details. Exporting or querying them as semantic identity,
   node, kind, edge, or correspondence fails.
3. Renaming a forbidden wrapper, changing its case or punctuation, or moving it
   to DNIR, region, machine, MCP, LSP, or object lineage does not admit it. Text
   matching is temporary pressure, not semantic proof.
4. Hash removal, forced collisions, cache removal, and hash-algorithm changes
   preserve semantic selection. Every accelerated candidate receives exact fact
   verification before reuse.
5. A canonical application that direct realization declines cannot reach C,
   boxed, or Lua semantic interpretation. Equivalent physical realization may
   vary only after meaning is fixed.
6. Old or unmerged work is corrected against current main and its valid
   obligation is preserved before the legacy carrier is deleted. No old branch
   merges wholesale, and no deletion is accepted merely because migration would
   require work.
7. Each isolated repair is committed and pushed promptly after its exact gate so
   other agents review a public revision rather than depending on private state.

Required adversarial controls include: add `NodeId` while deleting `StableId`;
export a private row offset as `id`; force all fingerprints to collide; remove
the fingerprint index; make direct realization refuse a resolved application;
and present an old branch with one valid fix mixed into `.duo`, `std`, Lua, and
parallel-identity debt. Each must fail closed or salvage only the current-law
obligation on current main.

## 2026-08-10 false MCP build verdict

At public HEAD `5532d81` with the shared dirty state recorded by `git status`,
the canonical MCP call:

```text
duo_build_run(gate="test", timeout=1200)
```

returned `passed=true` and `exit_code=0`. The actual serialized inner command:

```text
./zig-out/bin/duo run scripts/duo_lock.duo -- zig build test --summary all
```

exited 1. SOURCE-ZERO rejected 65 untracked or ignored `.duo` paths. The dirty
graph projection also failed compilation on a non-mutated local, a missing
`semantic_graph.Lease`, a missing `functionNode`, and concurrent
`native_backend.zig` content changes. The exact compile details are volatile;
the contradictory outcomes are the stable evidence defect.

Therefore the MCP build response is not authoritative completion, outcome, or
evidence for the requested gate. Closure requires the MCP result to expose the
exact inner command, revision, dirty snapshot, lock acquisition, inner exit,
and output artifact. A request that maps to a different command or cannot bind
those facts fails closed rather than returning green. The positive control must
force the inner command to fail after transport succeeds and assert that the
MCP verdict is failure.
