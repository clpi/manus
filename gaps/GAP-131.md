# GAP-131 — Session bootstrap reports false empty P0 queue

**Status:** CLOSED · **Filed:** !2026-08-09T21:08:55Z · **Closed:** 2026-08-17
**Priority:** P0 · **Kind:** agent
**Filed by:** codex_semantic_normalization_continue
**Files:** tools/mcp/bench.id,tools/mcp/lsp.id,tools/mcp/shared.id,gaps

## Closure

| # | directive |
|---|---|
| 1 | The session-start transport and its shell-parsed `tools/mcp/shared.id` owner were deleted. `tools/node/dev/orient` now derives `activep0` directly from the exact gap headers and `tools/node/dev/gap` reserves numbers atomically in the Git common directory. |
| 2 | A missing projection no longer becomes a false zero, and there is no second MCP census to drift from the file authority. |

## 2026-08-12 gaps-directory census (partial)

| # | directive |
|---|---|
| 1 | `tools/mcp/shared.id:open_p0_gaps` now scans canonical `gaps/GAP-*.md` headers (both separate-line and combined `**Status:** … **Priority:** P0` layouts) instead of the uninitialized coordination buffer table. |
| 2 | Session start in `tools/mcp/bench.id` and `tools/mcp/lsp.id` exposes `open_p0_gaps`, `open_p0_census_source`, and `open_p0_census_complete`. |
| 3 | Orient reports `activep0: 44` on the same tree (2026-08-13; authority-independent scan finds 41 OPEN files with P0 priority — combined-header layouts still require the dual-layout positive control below). `tools/mcp/gate.id` now includes a session-start round trip (id 10) asserting nonzero `open_p0_count`, known gaps GAP-124/GAP-131, and census source `gaps/GAP-*.md` when serve round-trip runs. |
| 4 | Remaining closure: Idol-owned projection replacing shell/orient parity and fail-closed admission service per sections below. |

| # | directive |
|---|---|
| 1 | duo_agent_session_start returned open_p0_count 0 and 'Coordination buffer not yet initialized' while the live checkout contains multiple OPEN P0 gap files including GAP-124, GAP-125, GAP-126, GAP-127, GAP-128, and GAP-129 and active claim state exists. |
| 2 | Session bootstrap therefore gives every new parallel agent a false empty obligation queue. |
| 3 | Derive the count and routing summary from the canonical per-gap files plus live claims, preserve run provenance, and positive-control a known open gap. |

## 2026-08-09 positive-control failure

| # | directive |
|---|---|
| 1 | The uncommitted MCP repair adds a session-bootstrap check and the exact locked gate passes: |

```text
zig build mcp-gate #summary all
  ok   duo_agent_session_start routes without retired control-plane errors
mcp-gate: PASS
```

| # | directive |
|---|---|
| 1 | In the same tree, an actual `duo_agent_session_start` call still returns: |

```text
open_p0_count: 0
coordination_excerpt: Coordination buffer not yet initialized.
```

| # | directive |
|---|---|
| 1 | The checkout contains fifteen files with explicit P0 labels. |
| 2 | The new check only asserts that the response has an id, mentions `AGENT_CANONICAL.md`, and does not contain `unknown command`; it never checks the obligation value. |
| 3 | This is a false-positive gate. |

| # | directive |
|---|---|
| 1 | The required positive control is semantic: select or inject one known OPEN P0 gap, assert that its stable gap id is returned, and assert that the reported count agrees with the canonical `gaps/GAP-*.md` records. |
| 2 | A handshake or path string is not evidence that session obligations were loaded. |

## 2026-08-10 ROOT-ZERO admission

| # | directive |
|---|---|
| 1 | The current dirty repair is rejected. |
| 2 | It adds `gapcensus` to `tools/mcp/shared.id`, runs a shell loop over gap files, parses `grep` output, and exports that new status owner to both MCP servers. |
| 3 | A correct count would not make this admissible: it grows project-owned `.id`, process and filesystem namespace authority, and the host agent control plane. |

| # | directive |
|---|---|
| 1 | Until an executed Idol owner projects the exact gap facts, session start must return an explicit incomplete/unknown census and must never translate missing evidence into zero. |
| 2 | The bootstrap MCP may pass an already-owned fact physically; it may not derive semantic project status from paths and shell text. |
| 3 | Closure requires the positive control above on that Idol projection and deletion of the bootstrap parser, not promotion of the dirty implementation. |

## 2026-08-11 combined-header negative control

| # | directive |
|---|---|
| 1 | The current dirty `gapcensus` remains incorrect even as a temporary physical projection. |
| 2 | Its shell expression requires `**Status:**` and `**Priority:**` to start separate matching lines. |
| 3 | Current gap records `GAP-092`, `GAP-141`, and `GAP-142` carry both facts on one header line, so the implementation omits all three. |

| # | directive |
|---|---|
| 1 | An authority-independent current-tree scan finds 30 OPEN/P0 files. |
| 2 | Applying the dirty implementation's two expressions would return 27. |
| 3 | Closure therefore also requires a positive control covering both admitted gap-header layouts and an equality assertion against the exact current-tree census; merely changing the displayed field name from `open_p0_count` to `p0count` is not evidence. |

## 2026-08-10 rejected host admission aggregate

| # | directive |
|---|---|
| 1 | The reviewed `.githooks/pre-commit`, `build.zig`, CI, and historical idiom-gate changes were withdrawn. |
| 2 | Expanding the shell hook into an admission engine would create a foreign control plane and preserve the wrong owner. |
| 3 | The rejected form also had these concrete fail-open defects: |

1. Aggregate `source-admission` checks `.id` paths added since the law anchor,
   then exits after admitting the complete tracked `.id` snapshot. It never
   checks lines added to already tracked `.id` files across that revision
   range. An agent can therefore extend any of the 910 tracked historical files
   with `--`, `std.*`, legacy syntax, or semantic host behavior without adding a
   path and escape this projection if local pre-commit is bypassed.
2. The dirty `scripts/idiomgate.id` edits add their own `--` comments and a new
   predicate helper while teaching the hook to reject those same forms on
   touched historical source. The staged pre-commit should correctly reject
   this self-host contradiction; changing only comment punctuation would not
   repair the continued historical-source and helper architecture.
3. The current rule recognizes only `std.`. It does not cover `std:`, `req
   "std..."`, generated native source, renamed universal roots, or semantic
   authority inferred from package path.
4. No current aggregate control rejects newly exported `NodeId`, `NodeKind`,
   `EdgeKind`, `SemanticId`, stable/path/hash identity, or semantic `*Id`
   wrappers. The shared dirty graph demonstrates that this is not hypothetical:
   the reviewed `f1cda68` dirty snapshot contained 194 `NodeId`, 15 `NodeKind`,
   six `EdgeKind`, and five `SemanticId` references. Live counts must be derived
   from the candidate snapshot rather than copied from this evidence record.
5. No current control proves canonical resolution cannot reach `lua_Value`,
   `lua_table_*`, `lua_to_*`, or `lua_val_*` after a direct realization refuses.
   A text census is useful pressure but is not the required reachability
   negative control.
6. PREDICATE-ZERO and SENTINEL-ZERO require semantic classification. Adding
   more name regexes cannot prove whether a truth relation is irreducible or a
   helper erased case, unknown, transition, descriptor, world, demand, or
   realization facts.

| # | directive |
|---|---|
| 1 | Do not repair or enlarge the shell gate. |
| 2 | The replacement owner is an executed canonical `.id` admission service over an exact candidate snapshot. |
| 3 | It consumes producer-issued lexical identities, generated grammar roles, graph facts, demand, realization and lineage; it returns structured canonical findings and projects one verdict to commit admission, CI, MCP, LSP and release. |
| 4 | Repository and process access require admitted world facts. |
| 5 | Until that vocabulary and the GAP-145, GAP-134 and GAP-124 producers exist, the result is `SEMANTIC-VOCABULARY-BLOCKED`, not another host scanner. |
| 6 | The existing hook is deletion debt and may only shrink. |
| 7 | GAP-131 remains OPEN until the Idol service and live session response both fail closed under intentional bypass controls. |

## 2026-08-10 IDOL ONE admission controls

| # | directive |
|---|---|
| 1 | Law revision `91d05a1` closes the destination ambiguity: semantic identity is `id`; facts qualify it; public node, kind, edge, typed identity, stable identity, and layer identity vocabularies are forbidden. |
| 2 | The current hook has no control for this boundary. |
| 3 | Its only anchored aggregate is `source-zero`; no named `idol-admission` aggregate exists on current main. |

| # | directive |
|---|---|
| 1 | The bootstrap ratchet and its eventual Idol replacement must prove all of the following independently: |

1. Adding any forbidden public semantic identity concept fails even if the same
   change deletes a different forbidden concept. A pooled total is fail-open:
   deleting one `StableId` must not buy permission to add one `NodeId`.
2. Private dense integers, array offsets, compact tags, and graph rows remain
   representation details. Exporting or querying them as semantic identity,
   node, kind, edge, or correspondence fails.
3. Renaming a forbidden wrapper, changing its case or punctuation, or moving it
   to DNIR, region, machine, MCP, LSP, or object lineage does not admit it. Text
   matching is temporary pressure, not semantic proof.
4. Hash removal, forced collisions, cache removal, and hash-algorithm changes
   preserve semantic selection. Every accelerated candidate receives exact fact
   verification before reuse.
5. A canonical application that direct realization declines cannot reach C,
   boxed, or Lua semantic interpretation. Equivalent physical realization may
   vary only after meaning is fixed.
6. Old or unmerged work is corrected against current main and its valid
   obligation is preserved before the legacy carrier is deleted. No old branch
   merges wholesale, and no deletion is accepted merely because migration would
   require work.
7. Each isolated repair is committed and pushed promptly after its exact gate so
   other agents review a public revision rather than depending on private state.

| # | directive |
|---|---|
| 1 | Required adversarial controls include: add `NodeId` while deleting `StableId`; export a private row offset as `id`; force all fingerprints to collide; remove the fingerprint index; make direct realization refuse a resolved application; and present an old branch with one valid fix mixed into `.id`, `std`, Lua, and parallel-identity debt. |
| 2 | Each must fail closed or salvage only the current-law obligation on current main. |

## 2026-08-10 false MCP build verdict

| # | directive |
|---|---|
| 1 | At public HEAD `5532d81` with the shared dirty state recorded by `git status`, the canonical MCP call: |

```text
duo_build_run(gate="test", timeout=1200)
```

| # | directive |
|---|---|
| 1 | returned `passed=true` and `exit_code=0`. |
| 2 | The actual serialized inner command: |

```text
./zig-out/bin/duo run scripts/duo_lock.id # zig build test #summary all
```

| # | directive |
|---|---|
| 1 | exited 1. |
| 2 | SOURCE-ZERO rejected 65 untracked or ignored `.id` paths. |
| 3 | The dirty graph projection also failed compilation on a non-mutated local, a missing `semantic_graph.Lease`, a missing `functionNode`, and concurrent `native_backend.zig` content changes. |
| 4 | The exact compile details are volatile; the contradictory outcomes are the stable evidence defect. |

| # | directive |
|---|---|
| 1 | Therefore the MCP build response is not authoritative completion, outcome, or evidence for the requested gate. |
| 2 | Closure requires the MCP result to expose the exact inner command, revision, dirty snapshot, lock acquisition, inner exit, and output artifact. |
| 3 | A request that maps to a different command or cannot bind those facts fails closed rather than returning green. |
| 4 | The positive control must force the inner command to fail after transport succeeds and assert that the MCP verdict is failure. |

## 2026-08-10 rejected MCP/LSP contraction

| # | directive |
|---|---|
| 1 | At public revision `b5a4234`, the shared dirty tooling rewrite is not admissible as one merge. |
| 2 | It contains these independent blockers: |

1. `tools/lsp/gate.id` calls `script.provenance()`, which
   `lib/script.id` does not export. The gate cannot reach its verdict.
2. `mcp_gate.id` replaces exact 35-tool and 32-tool controls with a nonzero
   count while the candidate reduces the servers to 12 and six tools. The
   candidate deletes diagnostics, foreign lifting, native-reachability audits,
   and semantic navigation without an `id`/facts/relations replacement, yet
   still reports a full census.
3. `shared.id:gapcensus` makes shell `grep` the status/priority owner and
   marks the census complete when a literal `census` line prints. Its current
   result omits live P0 records `GAP-113` and `GAP-115`; the gate checks only two
   records that happen to be included.
4. Gap note, claim, delegate, and close updates write only ignored coordination
   state. The new readers consume only `gapcensus`, so the updates are
   unobservable and close cannot change the authoritative gap status.
5. Session start omits revision, dirty state, live claims, current frontier,
   gates, and denied facts. Build, smoke, and benchmark tools still ignore the
   requested timeout and return no exact command, tree state, lock result,
   output artifact, backend, or workload. No control forces an inner failure
   after successful MCP transport.

| # | directive |
|---|---|
| 1 | Preserve the valid deletion of retired `duo dev`, pass-catalog, directive, generic repo-script, and static metaprogramming onboarding tools. |
| 2 | Preserve the claim grant/refuse/list/release round trip and private scratch intent. |
| 3 | Before merging, replace removed demanded capabilities with graph-native queries, derive an exact tool manifest, make every gap update observable from its authority, and bind every execution verdict to one private per-run outcome. |
