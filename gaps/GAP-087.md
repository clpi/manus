# GAP-087 — §41's bare-identity case parses but does not resolve

**Status:** COMPARISON RESOLVES (steps 1-2 landed 2026-08-09) · CONSTRUCTION OPEN
**Filed:** 2026-08-08 · **Measured again:** 2026-08-09
**Law:** `law.selfzero` (§41) · `law.subtract` (§40) · `resolution.rule`
**Blocks:** the entire §40/§41 leading-dot retirement — 231 "free" sites and 36
SELF-ZERO sites alike

## The defect

§41 rules that bare identity replaces the leading dot:

```duo
law{ kind = invariant }      -- canonical
law{ kind = .invariant }     -- retirement debt
```

and §40 obligation 1 was recorded as *satisfied by construction* because both
forms **parse**. Measured by value, that was the wrong test — parsing is not
resolving.

| form | result |
|---|---|
| dotted case in comparison — `k = .circle`, `k == .circle` | **compiles** |
| **bare case in comparison** — `k = circle`, `k == circle` | **FAILS** |
| dotted case in construction — `{ kind = .circle }` | FAILS |
| bare case in construction — `{ kind = circle }` | FAILS |

```
error: use of undeclared identifier 'circle'
```

The bare form lowers to an undeclared identifier. It is not resolved against the
case-set at all — the parser accepts the name and nothing binds it.

## Why obligation 1 was mis-recorded, and the lesson

§40 requires five proofs before any deletion. Obligation 1 is *"every semantic
role has a strictly SIMPLER EXISTING spelling"*, and I recorded it satisfied
because `duo check` accepted `kind = invariant`.

**`duo check` accepting a form is not the form working.** This tree has the
identical trap on record twice already — `duo-ambient-std-string-nil` ("a
resolving ambient path is not a working one") and `duo-mod1-unwrap-hazards`
("`duo check` passes on the broken result"). I hit it a third time, in the file
that legislates against it.

Obligation 2 (deterministic resolution) and obligation 3 (canonical lowering no
worse) both FAIL for the bare form. Obligation 3 fails maximally: the canonical
form does not lower at all while the retirement-debt form does.

## Same shape as gap[081], and that is now a pattern

gap[081]: canonical orientation `x:to(i64)` does not compile in either face
while the noncanonical `to(i64)(x)` does — so 1647 sites chose the working form.

gap[087]: canonical bare case does not resolve while the dotted form does — so
every site in the tree uses the dotted form.

**Twice now the law has prescribed a spelling the compiler cannot produce**, and
in both cases the corpus is not evidence of bad taste but of authors writing
what works. The rule this establishes:

> A retirement obligation is not satisfied by the replacement PARSING. It is
> satisfied by the replacement RESOLVING AND LOWERING, proven by value, on both
> backends.

That belongs in §40's obligation list and is the durable output of this gap.

## Construction is separately broken

Note both construction rows fail — dotted AND bare. `a: shape = { kind = .circle }`
does not compile either. So descriptor construction with a case field is broken
independently of the dot question, and any migration would have hit that wall
regardless. Worth its own narrowing; not the same defect.

## Fix, in order

1. **Bare identity resolves against the expected descriptor's case-set.**
   §41's `resolution.rule` states the mechanism: *bare identity + semantic demand
   + available homes → one identity, or a diagnostic.* The demand here is the
   field's declared case-set. Fixture asserts the VALUE — that `k == circle` is
   true and `k == square` is false, since a case that resolves to nothing
   compares equal to nothing and would pass a weaker test.
2. **The shadowing diagnostic** (`law.shadow`) lands with it, not after: a local
   `circle` in scope must diagnose rather than silently win.
3. **Then** the 231 free sites migrate, canonicalizer-driven with the §18
   witness.
4. Narrow the construction failure separately.

## Do not migrate anything until 1 lands

The 231 sites currently use the only form that works. Rewriting them to a form
that does not resolve would replace a working tree with a broken one — which is
precisely what §40's five obligations exist to prevent, and what this gap
demonstrates they catch when actually run.

## MEASURED AGAIN 2026-08-09 — fix steps 1 and 2 LAND; step 4 is still open

The comparison half resolves and lowers. Step 3 (the 231-site migration) is
UNBLOCKED for comparisons and still blocked for construction.

| form | before | after |
|---|---|---|
| dotted case in comparison — `k == .circle` | compiles | compiles, unchanged |
| **bare case in comparison** — `k == circle` | `error: use of undeclared identifier 'circle'` | **resolves, and lowers to `k == duo_token__kind_circle`** |
| **bare case, reversed operands** — `circle == k` | same error | **resolves** |
| **one spelling, two case-sets** | could not arise | **resolves to two different homes by demand** |
| a lexical binding shadowing a case | could not arise | **DIAGNOSES** (`law.shadow`) |
| a case of the wrong case-set | `undeclared identifier` | **DIAGNOSES**, naming both homes |
| dotted case in construction | FAILS | FAILS — unchanged, step 4 |
| bare case in construction | FAILS | FAILS — unchanged, step 4 |

`examples/spec100/barecase.id` prints **216**, and the number is chosen so that
every wrong resolution prints something else: a case resolving to nothing prints
0, one comparing equal to everything prints 31216, and a resolver that picked
`number`'s home by INDEX rather than by demand prints 16 or 1216. Two case-sets
in that fixture both declare `number`, and it resolves to a different home in
each of two comparisons eight lines apart.

### Where the resolution lives, and why not in the parser

The dotted form is resolved in `parse_anchor_case` — the parser knows the
position (it just consumed `==`) and knows the cases, because it declared them.
The bare form cannot be: deciding it requires knowing whether the spelling is
also a LEXICAL BINDING, which is scope, which is sema. So the resolver is in
`Sema.check_binop`, and it hands codegen the SAME `field` node the parser hands
it for the dotted form. A3 ONE EDGE holds — nothing downstream learns that a
bare name was written, which is what makes this a canonicalization rather than a
second lowering path, and it is why `law.canon.performance` (§42) is satisfied
by construction rather than by measurement.

**Ordering was the whole correctness argument.** `check_expr` on an unbound name
IMPLICITLY DEFINES it as an `any` local in duo mode. A resolver running after
the operands were checked would find every bare case already bound and would
fire `law.shadow` on all of them — blaming the reader for the checker's own side
effect. The resolver therefore runs BEFORE either operand is checked.

### Two prerequisites that were not visible from the gap

Neither is about cases, and both had to land first:

1. **A descriptor name in a WALK had no type.** `token.kind.eof` reported `any`,
   because `token` is an `alias_def` and not a scope binding, so `check_expr`
   took the implicit-local arm. The whole walk lost its type and `k` had no
   case-set to be a case of. Recovered in the `.field` arm only — a descriptor
   name in VALUE position is APPLY-ONE's question (§43) and is answered by the
   call path; `x.y` where `x` names a descriptor has exactly one reading.
2. **An implicit local discarded its value's type.** `check_assign_target` binds
   an undeclared duo-mode name as `any`, so `k = token.kind.eof` produced a `k`
   with no case-set. Narrowed here to case-sets only.

**Row 2 is a gap of its own and is stated rather than closed:** the general rule
is that an implicit local takes the DESCRIPTOR of the value that created it, and
`= any` throws away a fact the right-hand side had already computed. That is a
much larger change than this gap needs — it would retype implicit locals across
the whole tree — so it is recorded here and left. Anything that depends on an
implicit local's type today is depending on `any`.

### Still open — step 4, construction

`a: shape = { kind = .circle }` fails, and so does the bare spelling. Both
construction rows in the table above are unchanged. §41's own canonical example
is `law{ kind = invariant }`, which is CONSTRUCTION, so **the constitution's own
migration (`migrate.first`) is still blocked** — on a different defect than the
one this gap was filed for. `migrate.blocked` should be re-pointed at the
construction narrowing rather than at "bare identity does not resolve", which is
no longer true.

The 231 free sites and the 36 SELF-ZERO sites split accordingly: comparison
sites may migrate now; field-designation sites in constructors may not.

### Evidence

- `examples/spec100/barecase.id` — 216 in the legacy fixture set once measured
  by the deleted pass-era extractor.
- `examples/compile_fail/barecase_shadow.id` — `law.shadow` fires:
  `'eof' is both a lexical binding and a case of 'token__kind' here, so this
  comparison has two readings`.
- `examples/compile_fail/barecase_mixed.id` — mixed space:
  `'circle' is a case of 'shape__form', but this comparison demands
  'token__kind'`.
- Positive control: changing one weight in the fixture drops the corpus to
  20/21 against a floor of 21 and the row prints `want '216' got '217'`.
