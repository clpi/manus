check: i64 = (code: str, spell: str)
    m = spell:len()
    sub = code:sub(1, m)
    if sub == spell
        1
    else
        0

main: i64 = ()
    r = check("table.insert(t,v)", "table.insert")
    print("r={r}")
    0
