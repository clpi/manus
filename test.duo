@packed P: { x: i32, y: i32 } = { x = 1, y = 2 }
@align(16) @ffi("my_struct_t") ExtP: { a: f64 } = { a = 3.14 }
@packed enum E
  A
  B(value: i32)
end
fun add(a: i64, b: i64) -> i64
  a + b
end
fun main() -> void
  z = add(P.x, P.y)
  e = E.B({ value = z })
end
