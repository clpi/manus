# clock home — epoch and formatted observation at clock world boundary.
# os.time / os.date are foreign ingress (origin/provenance) until clock world executes.

now: i64 = ()
  os.time()

utc: str = ()
  os.date("!%Y-%m-%dT%H:%m:%SZ")

stamp: str = ()
  os.date("%Y-%m-%d %H:%m:%S")
