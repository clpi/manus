exec: i64 = (cmd: str)
  rc = os.execute(cmd)
  if rc == nil
    return -1
  rc

capture: str = (cmd: str)
  f = io.popen(cmd, "r")
  if f == nil
    ""
  out = ""
  line = f:read()
  while line != nil
    out = out .. line .. "\n"
    line = f:read()
  f:close()
  out
