# env home — os.env table projection. No @c.emit. No lua.

get(name: str): str
    v = os.env[name]
    if v == nil
        ""
    v
end

or(name: str, fallback: str): str
    v = get(name)
    if v:len() == 0
        fallback
    v
end

has(name: str): bool
    v = os.env[name]
    v != nil and v:len() > 0
end
