-- std.hash.sha1 — SHA-1 hash (re-exports from std.crypto.sha)

crypto_sha = req "std.crypto.sha"

hash_string(s: str): str
    crypto_sha.sha1(s)
end

M = {}
M.hash_string = hash_string
M.sha1 = hash_string
M
