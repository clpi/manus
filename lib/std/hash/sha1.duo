-- std.hash.sha1 — SHA-1 hash (re-exports from std.crypto.sha)

local crypto_sha = req "std.crypto.sha"

fun hash_string(s: str): str
    crypto_sha.sha1(s)
end

M = {}
M.hash_string = hash_string
M.sha1 = hash_string
M
