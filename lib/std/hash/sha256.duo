-- std.hash.sha256 — SHA-256 hash (re-exports from std.crypto.sha)

crypto_sha = req "std.crypto.sha"

hash_string(s: str): str
    crypto_sha.sha256(s)
end

M = {}
M.hash_string = hash_string
M.sha256 = hash_string
M
