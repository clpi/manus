# Idsem agent projection

This historical filename is retained only as a redirect. It does not define a
hook architecture, metaprogramming vocabulary, tool catalog, or source API.

Read [AGENTS.md](../AGENTS.md), the
[canonical router](../.agents/AGENT_CANONICAL.md), and the
[coordination workflow](../.agents/AGENT_COORDINATION.md). The sole semantic
law is [C0](spec/constitution.md). Canonical Idsem source uses `.id`; every
tracked project-owned `.duo` file is SOURCE-ZERO debt to migrate or delete.

Agent tools consume graph-owned facts. They do not create language meaning,
teach implementation paths as semantic namespaces, or reconstruct truth from
text. `std` is migration distribution and `std.script` is frozen debt; do not
replace either with another universal root. A known field is named directly;
`[]` is used only when evaluating an expression supplies the key.
Semantic facts, cases, unknowns, and transitions are not boolean predicates or
sentinel values; agent tooling must preserve their structured form.

Begin through `duo_agent_session_start`, inspect `duo_dev_claim_files` and live
numbered gap files, claim exact paths through `duo_dev_claim_acquire`, and use
`duo_agent_gaps_update` for obligations. Serialize heavy commands as directed
by the router. Fail closed on conflicting law, stale or incomplete evidence,
absent semantic vocabulary, or cross-owner state.
