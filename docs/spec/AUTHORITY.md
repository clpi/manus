# Idol authority

| # | directive |
|---|---|
| 1 | `clpi/idol` `main` is the sole living semantic authority for Idol. |

## Order

1. `docs/spec/law.md` — supreme compact law.
2. `docs/spec/constitution.md` — structured expansion and stable `law.*` identity owner.
3. Generated or human projections (`source.md`, `agent.md`, `canonical.md`, gates, LSP/MCP/docs) — consumers that must agree; never competing law.
4. Implementation and evidence — must consume exact graph facts and prove behavior/performance.
5. Research/history — preserved source and ancestry; inactive until reconciled.

| # | directive |
|---|---|
| 1 | Where the compact law and constitution differ, the compact law is the current owner ruling and the constitution must be repaired without minting an amendment subsystem. |
| 2 | Where a projection differs from either, the projection is wrong. |

## Current closure

- identity: **Idol** / `idol` / `.id`;
- `()` ordinary application;
- `[]` computed/indexed projection;
- `{}` structured pack/table/descriptor structure;
- `.` one static projection;
- `:` subject relation/constraint face;
- `@` current-world access/injection/qualification;
- one semantic graph, one exact id concept qualified by facts, and one producer
  per authoritative fact;
- DNIR and backends are physical realization, never semantic authority;
- demand/observation remove work before representation choice;
- FTCFTW requires exact-head Pareto evidence or a proven physical optimum.

## Executable source-law edition

| # | directive |
|---|---|
| 1 | Canonical `.id` source is qualified by `idol.source.law.v1` edition `9631704ed20a823b9c22f0c772a5a627f552b97ffc9eb1df970659b6c67dbd4a`. |
| 2 | The digest is derived from the schema name and the exact bytes of `law.md` and `constitution.md`, with NUL framing recorded by `AUTHORITY.json`. |
| 3 | The non-authoritative `source.md` projection is checked independently and cannot change semantic authority identity. |
| 4 | The edition is not a release marketing version and it is not inferred from the `.id` suffix. |
| 5 | A foreign source law with no exact edition remains unknown; it never inherits the current Idol edition. |

| # | directive |
|---|---|
| 1 | The edition names the law this exact compiler applies to admitted source; it is not evidence of the edition under which the bytes were originally authored. |
| 2 | The graph currently publishes it as `root_source_law`; imported source positions remain unknown until source/home-level edition facts exist. |

| # | directive |
|---|---|
| 1 | `AUTHORITY.json` is the machine-readable authority graph seed. |
| 2 | Each projected ruling states its stable law identity, disposition, effective source-law edition, superseded historical identities, source faces, semantic form, negative controls, required generated consumers, and current implementation state. |
| 3 | The typed cross-class identity/lineage correction is recorded separately as an open architecture gap, not asserted as current compiler enforcement. |
| 4 | The constitution remains the complete law-id owner while that projection is incomplete; an omitted row does not become permission to consult historical Pass prose. |

## Repository split

| # | directive |
|---|---|
| 1 | `clpi/idol-native` is the native/self-host/target realization and evidence projection. |
| 2 | It may retain machine-specific research, controls, and implementation, but it may not establish a second language identity, delimiter law, graph ontology, optimizer law, or evidence truth. |

## Research

| # | directive |
|---|---|
| 1 | The complete Pass 2 corpus is preserved at `research/archive/pass-2/`. |
| 2 | Recovery from the original inputs reproduced the exact expected archive digest and byte count; every member also matches its original filename, size and digest. |
| 3 | The earlier truncated container remains historical Git provenance, not the current archive. |
| 4 | This research corpus is not active semantic authority. |
| 5 | All formerly divergent Git branch histories found in the 2026-08-19 census are direct ancestors of `main` through the lineage commits recorded in `research/branches/manifest.json`. |
| 6 | Stale trees were not bulk-activated; their exact commits, controls, gaps, measurements, and non-results remain available for current-law integration where those bytes actually exist. |

| # | directive |
|---|---|
| 1 | Every Pass archive row is explicitly `historical` or `superseded`. |
| 2 | Even when a historical document contains an idea retained by current law, the document is non-authoritative and must not be used as current-law training or agent context without an exact current ruling that owns the idea. |
| 3 | Identical archive bytes are recorded as duplicates rather than treated as independent corroboration. |

## No session authority

| # | directive |
|---|---|
| 1 | Chat prompts, ad hoc personal questions, agent session state, branch-local pickup notes, and dated status summaries never become language law. |
| 2 | Kira’s ordinary ad hoc questions are unrelated to Idol unless the user explicitly connects a question to the project. |
