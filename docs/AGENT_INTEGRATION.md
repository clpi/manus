| field | value |
|---|---|
| title | Idol agent integration redirect |

| # | directive |
|---|---|
| 1 | This legacy path does not define agent configuration, tool inventories, or language law. |
| 2 | Current agents begin at [AGENTS.md](../AGENTS.md), then follow the [canonical router](../.agents/AGENT_CANONICAL.md) and [integration workflow](../.agents/AGENT_INTEGRATION.md). |
| 3 | The supreme law is [`docs/spec/law.md`](spec/law.md) and [C0](spec/constitution.md) is its structured expansion. |

| # | directive |
|---|---|
| 1 | The language is Idol. |
| 2 | Canonical source uses `.id` and new canonical `.id` is freely admitted — it is the Idol source extension. |
| 3 | Only tracked *noncanonical* `.id` content (host-shaped patterns, namespace dispatch, duplicate authority) is SOURCE-ZERO migration debt, migrated by repairing the pattern; the `.id` file itself is not debt. |
| 4 | Historical names remain only when their exact provenance is the topic. |

| # | directive |
|---|---|
| 1 | At session start, inspect the dirty tree, `tools/node/dev/claim list`, and live `gaps/GAP-*.md`, then claim exact paths through `tools/node/dev/claim acquire` before editing. |
| 2 | Edit the exact gap authority and reserve new numbers through `tools/node/dev/gap reserve`; do not create a second coordination buffer. |
| 3 | Serialize heavy commands through `tools/node/dev/idol-lock`. |

| # | directive |
|---|---|
| 1 | Canonical Idol has no standard semantic namespace. `std` is migration distribution, `std.script` is frozen debt, and another universal root is not a replacement. |
| 2 | Relations own meaning, possessed values supply subjects, and worlds supply authority. |
| 3 | Static identity uses named projection; computed and ordinal aggregate retrieval use indexed projection (`table[key]`, `row[1]`). |
| 4 | Parentheses remain ordinary application and never table indexing. |

| # | directive |
|---|---|
| 1 | PREDICATE-ZERO is part of admission. |
| 2 | Preserve semantic facts, cases, refinements, capabilities, demands, unknowns, and transitions directly. |
| 3 | Do not replace them with boolean helpers, sentinel comparisons, or query-then-mutate control flow. |

| # | directive |
|---|---|
| 1 | If authority conflicts, state cannot be verified, semantic vocabulary is missing, or another owner has not published the required fact, stop that branch and report the exact blocker. |
| 2 | A tool response, focused pass, or zero count is not evidence without a positive control and the inner semantic outcome. |
