# Duo Agent Gaps Buffer (redirect)

Canonical location: [`.agents/AGENT_COORDINATION.md#cross-agent-gap-buffer`](.agents/AGENT_COORDINATION.md#cross-agent-gap-buffer).

Do not append findings here. MCP tools, `duo_agent_gaps_read`/`duo_agent_gaps_update`, `@comp.agent.gaps()`, and `std.agent.gaps_index()` route to the canonical coordination buffer.
