# Idsem gap routing

This legacy path is a redirect, not a tracker or authority.

Start at [AGENTS.md](../AGENTS.md), use the
[agent router](../.agents/AGENT_CANONICAL.md), and inspect live
[`gaps/GAP-*.md`](../gaps/) plus
[coordination state](../.agents/AGENT_COORDINATION.md). Create or update a
numbered obligation only through `duo_agent_gaps_update`. Do not append
findings here.

The sole semantic law is [C0](spec/constitution.md). Canonical Idsem source uses
`.id`; every remaining tracked project-owned `.duo` file is SOURCE-ZERO debt
that must be semantically migrated or deleted, never a pattern source.

`std` is migration distribution, not semantic architecture. Do not add
`std.script` or a replacement universal namespace. Static identity uses a
named projection or field; `[]` is reserved for a genuinely computed key.
Do not replace a semantic fact, case, refinement, world, demand, or transition
with a boolean predicate or sentinel. Missing direct vocabulary is a blocker,
not permission to add `has`, `is`, `can`, or `exists` helpers.

Fail closed on conflicting authority, unverifiable current state, missing
semantic vocabulary, or a fact owned by another live claim. Until `GAP-131`
closes, a session summary reporting zero open P0 gaps is not a complete census.
