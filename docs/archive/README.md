# Archived Pass Plans

**Status:** Historical — retained for decision context only.

These documents are **not** canonical public architecture. For current behavior see [docs/compiler.md](../../compiler.md) and [docs/language.md](../../language.md). For active program tracking use `duo catalog`.

Superseded pairs were collapsed to one canonical filename; redirects remain at the old paths.
