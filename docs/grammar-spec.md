# Duo Grammar Specification — SUPERSEDED, THIS FILE IS A POINTER

**The normative grammar is `docs/spec/grammar.md` (Pass 108).** Read that.

This file was the epoch-1 "cross-session grammatical evolution ledger" and it
taught forms current law denies: `end` terminating every block, optional `then`,
`fun` for untyped declarations, `@comp.*` combinators. Under the epoch-2
precedence rule (`CLAUDE.md`) all of it is **VOID**. It is preserved as a stub
rather than deleted so that an agent arriving with the old content in context —
or following the stale `duo_grammar_spec_read` MCP tool — lands here and is
corrected instead of proceeding.

| you want | read |
|---|---|
| the normative grammar | `docs/spec/grammar.md` |
| the law | `CLAUDE.md` + `docs/spec/` |
| the mechanical deny list and canonical forms | `AGENTS.md` |
| the teaching, with worked before/after pairs | `docs/src/idiomatic-duon.md` |
| the gate that blocks a non-canonical diff | `.githooks/pre-commit` |

`end` is DELETED. `;` does not exist. Prefix `@` does not exist. Identifiers are
one lowercase word. If you are about to write otherwise, read `AGENTS.md` first.
